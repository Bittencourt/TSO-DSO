---
phase: 18-directional-thesis-reproduction
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - scripts/thesis_case123_repro.jl
  - docs/literate/thesis_reproduction_ieee123.jl
  - docs/literate/thesis_reproduction_assumptions.jl
  - docs/make.jl
autonomous: true
requirements: [REPRO-01, REPRO-02]

must_haves:
  truths:
    - "A live-executed literate doc page reproduces the DSO-surplus sign flip on real IEEE-123 data with reactive pricing and real impedances both active, calling the real solve_welfare/fit_baseline/welfare_accounting/decompose_dlmp entrypoints, never a re-derivation"
    - "Every cited reproduction number on that page (and its promotion-source script) carries the fixed \"directional, public-data\" qualifier phrase"
    - "A consolidated assumptions/reduction doc page enumerates the full chain: units resolution, Fortescue reduction fidelity, omitted regulators/caps/switches, the aggregator population re-tune, the PV scenario, the welfare-ratio-vs-surplus-sign metric caveat, and the asymmetric voltage-binding caveat"
    - "Both new literate pages are registered in docs/make.jl (render loop + pages= nav) and the full docs build succeeds"
  artifacts:
    - path: "scripts/thesis_case123_repro.jl"
      provides: "IEEE-123 real-impedance promotion-source script (DADP vs FIT, DSO-surplus sign flip, reactive DLMP, directional-public-data qualifier)"
      min_lines: 150
    - path: "docs/literate/thesis_reproduction_ieee123.jl"
      provides: "live-executed literate rung page (REPRO-01)"
      min_lines: 40
    - path: "docs/literate/thesis_reproduction_assumptions.jl"
      provides: "consolidated assumptions/reduction narrative page (REPRO-02)"
      min_lines: 30
    - path: "docs/make.jl"
      provides: "2 new Literate.markdown render entries + 2 new pages= nav entries"
      contains: "thesis_reproduction_ieee123"
  key_links:
    - from: "docs/make.jl"
      to: "docs/literate/thesis_reproduction_ieee123.jl"
      via: "Literate.markdown render-loop tuple entry"
      pattern: "thesis_reproduction_ieee123\\.jl"
    - from: "docs/make.jl"
      to: "docs/literate/thesis_reproduction_assumptions.jl"
      via: "Literate.markdown render-loop tuple entry"
      pattern: "thesis_reproduction_assumptions\\.jl"
    - from: "docs/literate/thesis_reproduction_ieee123.jl"
      to: "src/pricing/welfare.jl welfare_accounting"
      via: "live executed call, never a re-derivation"
      pattern: "welfare_accounting\\("
---

<objective>
Promote the phase's IEEE-123 real-impedance DADP-vs-FIT reproduction to a live-executed
Documenter/Literate.jl page, and write the consolidated assumptions/reduction page that
enumerates every caveat behind the reported numbers — completing ROADMAP Phase 18 success
criteria 1-3 (literate page, "directional, public-data" qualifier on every citation, and the
assumptions enumeration).

Purpose: CLAUDE.md's hard documentation requirement means the DSO-surplus sign-flip finding
(18-RESEARCH.md's central discovery) must live beside the thesis equations, live-executed so it
cannot silently drift from `src/`, and honestly caveated so a reader never mistakes a directional,
public-data reproduction for an exact-figure claim.

Output: `scripts/thesis_case123_repro.jl` (new promotion-source script, mirrors
`scripts/thesis_caseA.jl`'s shape on the real-impedance IEEE-123 fixture), two new
`docs/literate/*.jl` pages, and `docs/make.jl` updated with 2 render entries + 2 nav entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-directional-thesis-reproduction/18-RESEARCH.md
@.planning/phases/18-directional-thesis-reproduction/18-PATTERNS.md
@.planning/phases/18-directional-thesis-reproduction/18-01-SUMMARY.md
@.planning/phases/18-directional-thesis-reproduction/18-02-SUMMARY.md
@scripts/thesis_caseA.jl
@docs/literate/ieee123_impedances.jl
@docs/make.jl
@test/fixtures_phase7.jl
</context>

<interfaces>
From `scripts/thesis_caseA.jl:33-47` (DrWatson scaffold to mirror, new `OUT` dir):
```julia
using DrWatson
@quickactivate "TSODSO"
using TSODSO
using CairoMakie
using Printf
using Statistics
using JuMP: value

const OUT = projectdir("results", "thesis_case123_repro")
mkpath(OUT)
saveboth(name, fig) = (save(joinpath(OUT, "$name.pdf"), fig); save(joinpath(OUT, "$name.png"), fig))
```

From `18-PATTERNS.md` "sign-safe directional metric" (REPLACES `thesis_caseA.jl:132`'s
`ratio = welfare_dadp / welfare_fit` anti-pattern):
```julia
ctx, welfare_dadp, _ = solve_welfare(feeder, PF, aggs; T=T, λ₀=λ₀, allow_export=true)
acct = welfare_accounting(ctx; T=T)
fb   = fit_baseline(feeder, PF, aggs; T=T, λ₀=λ₀)          # feasible on IEEE-123 (confirmed)
fit_dso = fb.social_fit - fb.prosumer_surplus
@assert acct.dso > 0.0 && fit_dso < 0.0
@assert acct.prosumer < fb.prosumer_surplus
d = decompose_dlmp(ctx)   # d.reactive populated on a PLAIN solve_welfare ctx (Pitfall 3 —
                          # no reactive_consensus kwarg needed/accepted here)
```

From `18-PATTERNS.md` "directional, public-data" qualifier (the ONE genuinely new convention
this phase introduces — apply to every printed/cited number in both the script and both pages):
```julia
const REPRO_QUALIFIER = "directional, public-data"
cite_repro(x) = "$x ($REPRO_QUALIFIER)"
```

From `docs/literate/ieee123_impedances.jl:1-9, 95-121` (literate header + live-execution-ends-
the-page convention to mirror):
```julia
# # Title
#
# ... calls the real [`fn`](@ref) end-to-end — never a re-implemented reduction — so the
# numbers below cannot silently drift from the committed data ...
...
using TSODSO
feeder = ieee123_modified()
(length(feeder.buses), length(feeder.branches))
```

From `docs/make.jl:20-31, 55-71` (the exact render-loop tuple and `pages=` nav list to extend
by 2 entries each):
```julia
for src in (
    "toy_dc.jl", "lindistflow.jl", "convex_branch_flow.jl", "ac_oracle.jl",
    "prosumer_welfare.jl", "pricing_dlmp.jl", "admm.jl", "stackelberg_benders.jl",
    "nash_diagonalization.jl", "ieee123_impedances.jl",
)
    Literate.markdown(joinpath(LITERATE_DIR, src), GENERATED_DIR; flavor = Literate.DocumenterFlavor())
end
...
pages = [
    "Home" => "index.md",
    "Models" => [ ..., "IEEE-123 Real Impedances" => "generated/ieee123_impedances.md" ],
    "Planning" => [ ... ],
    "API Reference" => "api.md",
],
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/thesis_case123_repro.jl</name>
  <files>scripts/thesis_case123_repro.jl</files>
  <read_first>
    scripts/thesis_caseA.jl (full file, 399 lines — reuse the DrWatson scaffold and the
    figure/plotting conventions; do NOT reuse the manual `S_max`-relaxed FIT solve at lines
    97-131, that exists only for IEEE-13's congestion regime); test/fixtures_phase7.jl lines
    92-95 and 178-264 (the exact IEEE-123 population constants/builder to re-implement inline);
    src/pricing/dlmp.jl lines 100-152 (`extract_dlmp`, `extract_reactive_dlmp`,
    `decompose_dlmp`).
  </read_first>
  <action>
    Create `scripts/thesis_case123_repro.jl` with the DrWatson scaffold (`OUT =
    projectdir("results", "thesis_case123_repro")`). Re-implement the IEEE-123 population
    inline exactly as in Plan 18-01/Plan 18-02 (same constants: `SEED_IEEE123=20260719`,
    `LOAD_SCALE_IEEE123=0.05`, `PV_SCALE_IEEE123=0.12`, `DEV_SCALE_IEEE123=0.05*(0.05/0.03)`).
    Define `const REPRO_QUALIFIER = "directional, public-data"` and `cite_repro(x) = "$x
    ($REPRO_QUALIFIER)"`. Solve `ctx, welfare_dadp, _ = solve_welfare(feeder,
    ConvexBranchFlow(), aggs; T=24, λ₀=λ₀, allow_export=true)`, `acct = welfare_accounting(ctx;
    T=24)`, `fb = fit_baseline(feeder, ConvexBranchFlow(), aggs; T=24, λ₀=λ₀)` (the plain
    seam — confirmed feasible on IEEE-123, do NOT hand-roll a manual FIT solve), `fit_dso =
    fb.social_fit - fb.prosumer_surplus`, `d = decompose_dlmp(ctx)` (citing `d.reactive`, no
    `reactive_consensus` kwarg anywhere in this file — Pitfall 3). Print the headline using
    `cite_repro(...)` on every reported number: the DSO-surplus sign flip (`fit_dso ->
    acct.dso`), the prosumer-surplus decrease, and the aggregate welfare delta
    (`welfare_dadp - fb.social_fit`) reported explicitly as a SECONDARY, thin number — never the
    primary headline (Pitfall 1: never reprint `thesis_caseA.jl`'s `ratio =
    welfare_dadp/welfare_fit` as the primary claim; report it, if at all, only as an
    explicitly-labeled fragile secondary line). Reuse `scripts/thesis_caseA.jl`'s
    `saveboth`/CairoMakie figure conventions to produce at least one "DSO surplus sign flip"
    bar chart (mirror Fig D's `axS` surplus-split bar-pair layout) into
    `results/thesis_case123_repro/`. End with `@assert acct.dso > 0.0 && fit_dso < 0.0` and
    `@assert acct.prosumer < fb.prosumer_surplus` so the script itself fails loudly if the
    finding stops holding.
  </action>
  <verify>
    <automated>julia --project=. scripts/thesis_case123_repro.jl</automated>
  </verify>
  <acceptance_criteria>
    `julia --project=. scripts/thesis_case123_repro.jl` exits 0 and writes at least one figure
    into `results/thesis_case123_repro/`. `grep -c "directional, public-data"
    scripts/thesis_case123_repro.jl` >= 3 (qualifier applied to multiple cited numbers).
    `grep -c "reactive_consensus" scripts/thesis_case123_repro.jl` == 0 (Pitfall 3).
    `grep -c "welfare_dadp / \|welfare_dadp/fb" scripts/thesis_case123_repro.jl` == 0 (the
    sign-unsafe ratio is never the primary metric — Pitfall 1).
  </acceptance_criteria>
  <done>`scripts/thesis_case123_repro.jl` runs end-to-end, reproduces the DSO-surplus sign
  flip on the real-impedance IEEE-123 fixture, cites every reported number with the
  "directional, public-data" qualifier, and never uses the aggregate welfare ratio as the
  primary metric.</done>
</task>

<task type="auto">
  <name>Task 2: Promote to docs/literate/thesis_reproduction_ieee123.jl + register in docs/make.jl</name>
  <files>docs/literate/thesis_reproduction_ieee123.jl, docs/make.jl</files>
  <read_first>
    docs/literate/ieee123_impedances.jl (full file, 121 lines — the header/citation/live-
    execution-ends-the-page convention); scripts/thesis_case123_repro.jl (just written in
    Task 1 — the promotion SOURCE, do not hand-derive different numbers); docs/make.jl lines
    20-31 (render-loop tuple) and 55-71 (`pages=` nav).
  </read_first>
  <action>
    Create `docs/literate/thesis_reproduction_ieee123.jl` as a Literate.jl source (`# # Thesis
    Case A Reproduction — Real-Impedance IEEE-123` header) narrating: (1) what is being
    reproduced (thesis Case A's DSO-surplus welfare result, citing `` `[CITED: thesis p.98,
    Case A]` ``) and why the AGGREGATE welfare ratio is NOT the claim here (link forward to the
    assumptions page for the full metric-caveat narrative); (2) the live solve — call the REAL
    `solve_welfare`/`fit_baseline`/`welfare_accounting`/`decompose_dlmp` end-to-end (never a
    hand-copied re-derivation) on the same real-impedance IEEE-123 fixture Plan 18-02's test
    pins; (3) the DSO-surplus sign flip and prosumer-surplus decrease as the terminal, cited
    findings, EVERY cited number wrapped in the "directional, public-data" qualifier (reuse
    `cite_repro` from the promoted script, or restate the `const REPRO_QUALIFIER` +
    `cite_repro` pair locally). End the page with the live DSO-surplus tuple
    `(dso=acct.dso, fit_dso=fit_dso, prosumer=acct.prosumer, fit_prosumer=fb.prosumer_surplus)`
    as the terminal expression so Documenter renders it. Add `"thesis_reproduction_ieee123.jl",`
    to the `for src in (...)` render-loop tuple in `docs/make.jl` (commented `# NEW: thesis
    reproduction — IEEE-123 real-impedance DSO-surplus sign flip (REPRO-01)`) and
    `"Thesis Reproduction — IEEE-123" => "generated/thesis_reproduction_ieee123.md",` under the
    `"Models"` section of `pages = [...]`.
  </action>
  <verify>
    <automated>julia --project=docs -e 'using Literate; Literate.markdown("docs/literate/thesis_reproduction_ieee123.jl", "docs/src/generated"; flavor=Literate.DocumenterFlavor())'</automated>
  </verify>
  <acceptance_criteria>
    The Literate render command above exits 0 and writes
    `docs/src/generated/thesis_reproduction_ieee123.md`. `grep -c "directional, public-data"
    docs/literate/thesis_reproduction_ieee123.jl` >= 2. `grep -c
    "thesis_reproduction_ieee123" docs/make.jl` == 2 (one render-tuple entry, one nav entry).
  </acceptance_criteria>
  <done>The promoted literate page live-executes the DADP-vs-FIT DSO-surplus comparison and is
  registered in both `docs/make.jl`'s render loop and its `pages=` nav tree.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/literate/thesis_reproduction_assumptions.jl, register it, and verify the full docs build</name>
  <files>docs/literate/thesis_reproduction_assumptions.jl, docs/make.jl</files>
  <read_first>
    docs/literate/ieee123_impedances.jl lines 80-93 ("Reduction caveats" bullet-list convention
    — the direct template for this page's assumptions enumeration); test/fixtures_phase7.jl
    lines 54-95 (the population re-tune rationale: `LOAD_SCALE_IEEE123=0.05`,
    `PV_SCALE_IEEE123=0.12`, `DEV_SCALE_IEEE123≈0.0833`, and the asymmetric voltage-binding
    finding); 18-RESEARCH.md "Central Research Tension" + Pitfall 1 (the welfare-ratio-vs-
    surplus-sign metric caveat, verbatim numbers to cite); 18-01-SUMMARY.md (the stability/
    sensitivity findings to cite, including whether the sign flip survived the sweep).
  </read_first>
  <action>
    Create `docs/literate/thesis_reproduction_assumptions.jl` (`# # Thesis Reproduction —
    Assumptions & Reduction Chain` header) as a narrative-first literate page (lighter
    code:prose ratio than the reproduction page, per 18-RESEARCH.md Open Question 2's
    recommendation) enumerating: (1) units resolution (the kft/feet-vs-miles trap already
    resolved in `ieee123_impedances.jl`, cross-referenced not re-derived); (2) Fortescue
    reduction fidelity (transposition assumption, single/two-phase laterals, cross-referenced);
    (3) omitted regulators/caps/switches (absorbed into near-ideal switch-class impedance, per
    REQUIREMENTS Out-of-Scope); (4) the aggregator population re-tune (`LOAD_SCALE_IEEE123=0.05
    / PV_SCALE_IEEE123=0.12 / DEV_SCALE_IEEE123≈0.0833`, and WHY — Phase 17's asymmetric
    voltage-binding finding); (5) the PV scenario assumptions; (6) the welfare-ratio-vs-
    surplus-sign metric caveat — state PLAINLY that the aggregate welfare gap is small
    (+0.045% on this fixture) and comparatively fragile, while the DSO-surplus sign flip is the
    robust, correctly-signed, thesis-faithful signal this phase actually pins, per
    18-RESEARCH.md's central finding (this is the HONESTY-mandate paragraph — do not soften or
    bury it); (7) the asymmetric voltage-binding caveat from Phase 17 (strong lower band, weak/
    boundary-limited upper band); (8) a summary of Plan 18-01's stability/sensitivity findings
    (whether the sign flip survived the ±2-5% sweep). Keep a small amount of live code (e.g.
    `length(ieee123_load_nodes())` and restating the retune constants as a live-checked
    `@assert`) so the page cannot silently drift from `src/`, per Open Question 2's
    recommendation. Apply the "directional, public-data" qualifier to every cited number here
    too. Add `"thesis_reproduction_assumptions.jl",` to `docs/make.jl`'s render-loop tuple
    (commented `# NEW: thesis reproduction assumptions/reduction chain (REPRO-02)`) and
    `"Thesis Reproduction — Assumptions" => "generated/thesis_reproduction_assumptions.md",`
    under `"Models"` in `pages = [...]`. Finally, run the FULL `julia --project=docs
    docs/make.jl` build (not just the two new pages in isolation) to confirm no regression in
    the existing rung pages or the `checkdocs=:exports` gate. Do NOT modify `docs/src/api.md`
    unless a genuinely new exported symbol requires it — neither new file introduces one
    (scripts and literate pages, not `src/` exports); confirm this with `git diff
    docs/src/api.md` being empty after the build.
  </action>
  <verify>
    <automated>julia --project=docs docs/make.jl</automated>
  </verify>
  <acceptance_criteria>
    `julia --project=docs docs/make.jl` exits 0 (the full Documenter build, including both new
    pages and all pre-existing rung pages). `grep -c "thesis_reproduction_assumptions.jl"
    docs/make.jl` == 2. `grep -c "directional, public-data"
    docs/literate/thesis_reproduction_assumptions.jl` >= 2. `git diff --stat docs/src/api.md`
    is empty (no unnecessary export-surfacing edit).
  </acceptance_criteria>
  <done>The consolidated assumptions/reduction page exists, is registered in `docs/make.jl`,
  and the full Documenter build (both new pages plus every pre-existing page) succeeds
  cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Literate source -> executed Documenter page | Both new pages' live code blocks run real `solve_welfare`/`fit_baseline`/`welfare_accounting`/`decompose_dlmp` calls during `makedocs`, so a stale/incorrect narrative is caught by a build failure, not silently shipped |
| Reported number -> reader's interpretation | The "directional, public-data" qualifier is the control preventing a reader from mistaking a directional reproduction for an exact-figure claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | `docs/make.jl` `checkdocs = :exports` gate | mitigate | `docs/src/api.md` is left untouched unless a genuinely new exported `src/` symbol requires it (neither new file exports anything) — avoids the exact Phase-15 "unsurfaced exported docstring breaks the build" trap |
| T-18-07 | Repudiation | The "directional, public-data" qualifier | mitigate | Grep-checkable (`grep -c "directional, public-data"`) across both new pages and the promotion script — omission is mechanically detectable, not left to review discipline alone |
| T-18-08 | Information Disclosure | N/A | accept | Public OpenDSS-derived data + synthetic seeded population; no sensitive content |
</threat_model>

<verification>
`julia --project=docs docs/make.jl` exits 0 with both new pages rendered and registered.
`grep -c "directional, public-data"` >= 2 on each new file. `git diff --stat docs/src/api.md`
is empty.
</verification>

<success_criteria>
Both literate pages live-execute via `docs/make.jl`, every cited reproduction number carries the
"directional, public-data" qualifier, and the assumptions page enumerates the full chain
(units, reduction fidelity, omissions, population re-tune, PV scenario, the metric caveat, and
the voltage-binding caveat) — completing ROADMAP Phase 18 success criteria 1-3.
</success_criteria>

<output>
Create `.planning/phases/18-directional-thesis-reproduction/18-03-SUMMARY.md` when done
</output>
</content>
