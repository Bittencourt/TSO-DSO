---
milestone: v1.0
audited: 2026-07-20T00:00:00Z
status: tech_debt
scores:
  requirements: 35/35
  phases: 9/9
  integration: sound
  flows: complete
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 04-convex-branch-flow-correctness-milestone
    items:
      - "Thesis welfare-headline (+$1819) not quantitatively reproduced — MEM price / temperature / house-count inputs are figure-bound; COMPUTED golden pinned as the regression anchor (accepted by researcher). Follow-up: digitize thesis figures 4.2/4.5 to recover exact inputs."
  - phase: 05-distribution-pricing-dadp-dlmp-decomposition
    items:
      - "+25% social-welfare headline not quantitatively reproduced (computed ratio ≈1.0 due to negative-welfare ¢$/kWh calibration); dynamic pricing improves welfare directionally. Same figure-digitization follow-up as Phase 4. All decomposition/surplus/KKT identities PASS."
  - phase: 07-admm-convergence-scale
    items:
      - "IEEE-123 fixture ships canonical topology with REPRESENTATIVE in-band per-unit impedances at 1 MVA base (not thesis App. E exact per-terminal r/x — not vendored). ADMM converges (~17 iters, exact 1e-9). Follow-up: transcribe App. E / public IEEE-123 impedances for a thesis-numeric reproduction."
      - "CairoMakie convergence-figure visual eyeball — RESOLVED during Phase-9 verification (verifier rendered + inspected the figure in the docs build)."
  - phase: 08-experiment-harness-reproducibility
    items:
      - "WR-02: sub_seed derives per-component seeds via Base.hash (no cross-Julia-version stability contract). Empirically stable on Julia 1.10/1.11/1.12 today. Mechanical fix caused a numerical regression needing researcher ρ/τ re-tuning — deferred to researcher decision."
  - phase: 09-documentation-regression-acceptance-gate
    items:
      - "WR-04: JuliaFormatter does not cover docs/; a mechanical reformat detaches inline equation-comments from device args (harms 'equations beside code'). Needs a deliberate hand-reviewed formatting pass before adding docs/ to the CI format job."
      - "Docstring-manual wiring: ~104 exported-symbol docstrings exist in src/ but are not yet spliced into the rendered manual via @docs/@autodocs blocks. checkdocs=:exports warns (non-fatal per locked green-build decision). Out of EXP-03 literate-page scope; post-v1 follow-up."
      - "WR-05: deploydocs repo slug is a documented placeholder (github.com/PLACEHOLDER-ORG/PLACEHOLDER-REPO) — settled user decision (no git remote configured); replace + wire DOCUMENTER_KEY before first gh-pages deploy."
nyquist:
  compliant_phases: []
  partial_phases: ["09"]
  missing_phases: ["01","02","03","04","05","06","07","08"]
  overall: "partial (VALIDATION.md introduced in Phase 9 only; earlier phases predate the Nyquist artifact but all carry passed VERIFICATION.md with automated test coverage — 1946 passing tests)"
---

# Milestone v1.0 — Audit Report

**Milestone:** TSO-DSO Integration Optimization Framework (Julia) — operational transactive-energy layer + extension seams
**Audited:** 2026-07-20
**Status:** `tech_debt` — all requirements met, integration sound, no blockers; documented deferred items carried forward.

## Definition of Done — Achieved

The v1 core value is delivered: **a researcher can express a scenario and model variant declaratively, run it end-to-end with an open-source solver (Clarabel/HiGHS/Ipopt), and get trustworthy, reproducible results and prices — with every model assumption documented and every layer swappable.**

- **35/35 v1 requirements** satisfied (3-source cross-reference: all 9 phase VERIFICATION.md = `passed`, all SUMMARY frontmatter lists the requirement complete, REQUIREMENTS.md traceability = Complete).
- **9/9 phases** verified passed; **43/43 plans** executed with summaries.
- **Test suite:** 1946 pass / 0 fail / 0 error / 2 documented-broken (non-failing thesis-figure cross-checks). Docs build (`docs/make.jl`) exits 0 with all 6 literate pages + rendered CairoMakie figure.
- **Cross-phase integration:** sound (see below).

## Requirements Coverage (35/35)

| Group | IDs | Status |
|-------|-----|--------|
| Infrastructure & Reproducibility | INFRA-01..05 | ✓ satisfied |
| Network Data Model | DATA-01..04 | ✓ satisfied |
| Prosumer Device Library | DEV-01..05 | ✓ satisfied |
| Power-Flow Formulations | PF-01..04 | ✓ satisfied |
| Objective & Centralized Solve | OPT-01..03 | ✓ satisfied |
| Pricing & Welfare Accounting | PRICE-01..04 | ✓ satisfied |
| ADMM Decomposition | ADMM-01..05 | ✓ satisfied |
| Experiments, Docs & Seams | EXP-01..04, SEAM-01 | ✓ satisfied |

No unsatisfied or orphaned requirements. SEAM-01 is delivered as deliberately-inert v1 stubs (multi-scenario hook, rolling-horizon param, meshed slot, coupling-flow `z↔p_ag`/`λ_j↔π_s` + leader/follower role) — exercised (not orphaned), scoped for a future v2 planning milestone.

## Cross-Phase Integration — Sound

Verified by the integration checker with end-to-end call-site evidence (not just imports):
1. **Single residual seam** (`contribute!`/`add_to_residual!`, Phase 2) reused verbatim by ConvexBranchFlow (Phase 4), DsoOpt/AgrOpt (Phase 6/7) — no divergent branch/voltage math.
2. **ADMM ≈ centralized** (ADMM-03) is a genuine numeric cross-validation in `test_acceptance.jl` / `test_ieee123_admm.jl` (welfare rtol 1e-4, DADP atol 1e-2/rtol 1e-3).
3. **DADP extraction** (Phase 5) reads the dual of the exact `:balance_p`/`:cone`/… constraint handles ConvexBranchFlow (Phase 4) registers.
4. **Experiment harness** (Phase 8) `run_scenario`/`run_sweep` dispatch the real Phase 4-7 entrypoints; **Phase 9 acceptance gate + docs** call the real `src/` APIs end-to-end.
5. **SEAM-01 `operational_oracle`** contract is consistent between Phase 4 (definition) and Phase 8/9 (usage).
6. Solve-before-dual gating (`assert_solved!(; dual=true)` + `assert_socp_exact!`) consistently protects every price/dual read.

## Tech Debt (documented, accepted deferrals — no blockers)

All items below are pre-existing, explicitly accepted by the researcher, and recorded in phase `deferred-items.md` / STATE.md. None block the v1 definition of done.

1. **Thesis welfare-headline figure digitization** (Phase 4 +$1819, Phase 5 +25%) — computed goldens pinned as regression anchors; thesis figures are non-failing cross-checks. The inputs are figure-bound and not recoverable from the vendored PDFs without digitization.
2. **IEEE-123 exact App. E impedances** (Phase 7) — fixture uses representative in-band per-unit values at 1 MVA base; ADMM converges correctly. Transcribe App. E for a thesis-numeric reproduction.
3. **WR-02 sub_seed cross-version hash stability** (Phase 8) — `Base.hash` derivation, empirically stable on 1.10/1.11/1.12; a stable-hash fix needs ρ/τ re-tuning (researcher decision).
4. **WR-04 JuliaFormatter coverage of docs/** (Phase 9) — needs a hand-reviewed pass that preserves inline equation-comments.
5. **Docstring-manual `@docs` wiring** (Phase 9) — ~104 existing docstrings not yet surfaced on manual pages; out of EXP-03 literate scope, post-v1.
6. **WR-05 deploydocs repo slug placeholder** (Phase 9) — settled user decision; replace before first gh-pages deploy.

## Nyquist Coverage

VALIDATION.md was introduced in Phase 9 only (`partial` — `nyquist_compliant: false` draft). Phases 1-8 predate the Nyquist artifact but each carries a `passed` VERIFICATION.md backed by automated test coverage (1946 passing tests). This is a process-artifact gap, not a test-coverage gap.

## Verdict

Milestone v1.0 **achieved its definition of done**. All requirements satisfied, integration sound, no blockers. Six documented, researcher-accepted deferred items are carried forward as tech debt / v2-follow-ups. Recommended: proceed to complete-milestone, tracking the deferrals in the backlog.
