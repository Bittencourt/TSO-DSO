---
phase: 01-plumbing-solver-abstraction
verified: 2026-07-18T17:20:20Z
status: passed
score: 5/5 must-haves verified
reverified: 2026-07-18
resolution: >-
  Gap on truth 1 (INFRA-01) CLOSED via gap closure. Committed version-specific
  manifests Manifest-v1.10.toml / Manifest-v1.11.toml / Manifest-v1.12.toml
  (commit 908adab); Julia auto-selects the manifest matching the running version.
  Re-verified directly: `Pkg.instantiate()` clean on Julia 1.10.11 AND 1.11.9;
  full suite 44/44 on 1.11 and 1.12. `juliaup add 1.10` performed so the LTS
  floor is now locally testable. All 5 success criteria hold.
overrides_applied: 0
resolved_gaps:
  - truth: "Package resolves cleanly from a clean checkout (committed Manifest.toml) on Julia 1.10 LTS and 1.11"
    status: resolved
    reason: >-
      The [compat] floor (julia = "1.10", SparseArrays = "1") and the CI matrix
      (1.10/1.11/1.12) are correct, but the COMMITTED Manifest.toml (and
      test/Manifest.toml) are pinned to `julia_version = "1.12.5"`. A clean
      `Pkg.instantiate()` on the locally-installed Julia 1.11 hard-errors
      ("Could not locate the source code for the OpenSSL_jll package. Are you
      trying to use a manifest generated by a different version of Julia?").
      A single-version manifest cannot be instantiated on 1.10/1.11, so the
      CI-delegated buildpkg step (julia-actions/julia-buildpkg) on the 1.10 and
      1.11 matrix legs is expected to fail for the same root cause. The
      reproducibility guarantee currently holds only on 1.12.
    artifacts:
      - path: "Manifest.toml"
        issue: "julia_version = 1.12.5 — does not instantiate on Julia 1.11 (locally verified) or 1.10"
      - path: "test/Manifest.toml"
        issue: "julia_version = 1.12.5 — same cross-version instantiate failure"
    missing:
      - "Provide version-resolvable manifests for the 1.10 LTS floor and 1.11 — e.g. commit Julia-version-specific manifests (Manifest-v1.10.toml, Manifest-v1.11.toml) alongside the 1.12 Manifest.toml (Julia 1.11+ multi-manifest feature)"
      - "OR decide the reproducibility target is 1.12-only and record an override (but note criterion 1 wording requires clean resolution on 1.10 AND 1.11, and CI buildpkg on 1.10/1.11 will be red as configured)"
      - "Confirm the CI matrix legs for 1.10 and 1.11 actually go green with whatever manifest strategy is chosen"
human_verification:
  - test: "CI matrix leg for Julia 1.10 LTS (julia-actions/julia-buildpkg + julia-runtest)"
    expected: "Clean-checkout instantiate + full test suite (44 pass) succeeds on 1.10"
    why_human: "Julia 1.10 LTS is not installed locally (only 1.11/1.12); requires CI or `juliaup add 1.10`. NOTE: given the 1.12-pinned committed manifest, this leg is expected to FAIL until the manifest gap above is resolved."
---

# Phase 1: Plumbing & Solver Abstraction Verification Report

**Phase Goal:** Lock the architectural keystone — pure JuMP-free data model, `ModelContext` + residual registry, solver abstraction (`select_optimizer(::ProblemClass)`), and status/per-unit discipline — validated against a toy DC single-node/single-period centralized solve.

**Verified:** 2026-07-18T17:20:20Z
**Status:** passed (gap closed on re-verification)
**Re-verification:** Yes — INFRA-01 manifest gap closed via version-specific manifests (commit 908adab); clean instantiate confirmed on Julia 1.10.11 + 1.11.9, suite 44/44 on 1.11 + 1.12.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Package resolves cleanly from a clean checkout on Julia 1.10 LTS and 1.11 | ✓ VERIFIED (gap closed) | [compat] floor `julia = "1.10"` ✓ and CI matrix `1.10/1.11/1.12` ✓. Gap closed by committing version-specific manifests `Manifest-v1.10.toml` / `Manifest-v1.11.toml` / `Manifest-v1.12.toml` (commit 908adab) — Julia auto-selects the matching one. Re-verified: `julia +1.10 -e 'Pkg.instantiate()'` and `julia +1.11 -e 'Pkg.instantiate()'` both succeed; full suite 44/44 on 1.11 and 1.12. Plain `Manifest.toml` retained as fallback. |
| 2 | Toy single-node single-period DC builds, solves via `select_optimizer(::ProblemClass)`, returns objective — no model names a concrete solver | ✓ VERIFIED | `src/models/toy_dc.jl:42` builds `Model(select_optimizer(LP()))`; `grep` of `src/models/` and all `src/` (excluding `factory.jl`/`ProblemClass.jl`) finds ZERO solver names. `rung0` testitem passes: obj + dual finite. Full suite 44/44 on 1.12. |
| 3 | Every solve path asserts termination status / `is_solved_and_feasible`, fails loudly on non-optimal or hidden slack | ✓ VERIFIED | `optimize!` appears in `src` ONLY inside `assert_solved!` (`status.jl:29`); `toy_dc.jl:59` routes through it. `is_solved_and_feasible(...; allow_local=false)` errors with full diagnostics on failure. `test_status` confirms pass-on-optimal + throw-on-infeasible. (WARNING: `assert_no_slack` is defined/exported but not wired into the toy solve path or any test — see Anti-Patterns.) |
| 4 | Feeder immutable JuMP-free structs, validated radial (N nodes → N−1 branches, connected, one root); non-tree raises clear error | ✓ VERIFIED | `Bus`/`Branch`/`Feeder` immutable, JuMP-free (`data/Feeder.jl`). Inner constructor calls `assert_radial` (edge-count theorem + BFS connectivity + exactly-one-root) which throws `ArgumentError`. `test_feeder`/`test_topology` verify wrong-count, disconnected, zero-root, two-root all raise. |
| 5 | All quantities pass magnitude-sanity assertions under one documented per-unit system, converted once at ingestion — tripwire fires on the LIVE construction path | ✓ VERIFIED | Documented base `S_base=1.0 MVA, V_base=4.16 kV`. `to_pu_*` helpers at ingestion only. `assert_magnitudes(feeder)` called inside the `Feeder` inner constructor (`Feeder.jl:69`) — the live path. `test_feeder` proves `Feeder(bad_mag_buses, ...)` throws `AssertionError` during construction (not a standalone assert call). |

**Score:** 4/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Project.toml` | deps + [compat] floors incl. `julia="1.10"`, weakdeps/extensions | ✓ VERIFIED | Floors match CLAUDE.md pins; `SparseArrays="1"` to admit 1.10 stdlib; Gurobi/Mosek under [weakdeps]+[extensions] only. |
| `Manifest.toml` | committed pinned env, resolvable on 1.10/1.11/1.12 | ✗ STUB-VERSION | Pinned `julia_version = 1.12.5`; not instantiable on 1.11 (verified) or 1.10. |
| `src/solver/ProblemClass.jl` | singleton problem-class taxonomy | ✓ VERIFIED | `abstract ProblemClass` + `LP/MILP/QP/SOCP/NLP` + `GurobiChoice`/`MosekChoice`; exported. |
| `src/solver/factory.jl` | `select_optimizer(::ProblemClass)` dispatch; only core file naming solvers | ✓ VERIFIED | Multiple-dispatch methods (no `if class ==`); HiGHS/Clarabel/Ipopt mapped; `commercial_optimizer` errors by default. |
| `src/core/ModelContext.jl` | mutable ctx + constraints/residuals/meta registries | ✓ VERIFIED | `add_to_residual!` accumulates (never overwrites); `register_constraint!` stores handles. |
| `src/core/status.jl` | `assert_solved!` choke point + `assert_no_slack` | ✓ VERIFIED (1 orphan) | `assert_solved!` wired; `assert_no_slack` present/exported but unused. |
| `src/data/Feeder.jl` | immutable JuMP-free structs, validating constructor | ✓ VERIFIED | Inner constructor runs `assert_radial` + `assert_magnitudes`. |
| `src/data/topology.jl` | `assert_radial` returning incidence | ✓ VERIFIED | Edge-count + BFS + root checks; returns sparse incidence. |
| `src/units/PerUnit.jl` | per-unit base + convert-once + magnitude tripwires | ✓ VERIFIED | Documented base, `to_pu_*`, `assert_magnitudes*`. |
| `src/powerflow/AbstractPowerFlow.jl` | abstract type + `contribute!` contract | ✓ VERIFIED | Interface-only seam (Phase 2+ fills concrete). |
| `src/models/toy_dc.jl` | rung-0 end-to-end solve | ✓ VERIFIED | Full spine; returns `(ctx, objective, dual)`. |
| `ext/TSODSOGurobiExt.jl` / `ext/TSODSOMosekExt.jl` | weakdep commercial-solver extensions | ✓ VERIFIED | Add `commercial_optimizer` methods only; no hard dep. |
| `.github/workflows/CI.yml` | 1.10/1.11/1.12 matrix + buildpkg + runtest + format | ✓ VERIFIED (efficacy at risk) | Matrix and steps present; 1.10/1.11 legs depend on the manifest gap being fixed. |
| `docs/literate/toy_dc.jl` | Literate reproducibility page executing `solve_toy_dc` | ✓ VERIFIED | Executes the real solve during docs build. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `toy_dc.jl` | `factory.jl` | `Model(select_optimizer(LP()))` | ✓ WIRED | Solver chosen by problem class only. |
| `toy_dc.jl` | `ModelContext` | `add_to_residual!(:nodal_balance)` + `register_constraint!(:balance)` | ✓ WIRED | Nodal balance flows through the PF-01 residual seam even at rung 0. |
| `toy_dc.jl` | `status.jl` | `assert_solved!(...; dual=true, allow_local=false)` | ✓ WIRED | Single solve choke point. |
| `Feeder` constructor | `topology.jl` | `assert_radial(...)` | ✓ WIRED | Radial invariant on live construction. |
| `Feeder` constructor | `PerUnit.jl` | `assert_magnitudes(feeder)` | ✓ WIRED | Magnitude tripwire on live construction. |
| solve path | `assert_no_slack` | (none) | ⚠️ ORPHANED | Defined/exported, never called in `src` or tests. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite on default Julia (1.12) | `julia --project=. -e 'import Pkg; Pkg.instantiate(); Pkg.test()'` | `Package | 44 44` — 0 fail / 0 error | ✓ PASS |
| Clean instantiate on Julia 1.11 (committed manifest) | `julia +1.11 --project=. -e 'import Pkg; Pkg.instantiate()'` | ERROR: manifest from different julia version (OpenSSL_jll) | ✗ FAIL |
| No concrete solver named in any model | `grep -rniE 'HiGHS|Clarabel|Ipopt|Gurobi|Mosek|\.Optimizer' src/models/` | NONE | ✓ PASS |
| No solver named outside the factory | `grep ... src/ (excl. factory.jl, ProblemClass.jl)` | NONE | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| INFRA-01 | 01-01 | Instantiate pinned, reproducible env | ✗ BLOCKED (partial) | Compat floor + CI matrix correct; committed manifest is 1.12-only → fails clean resolution on 1.11 (and 1.10). |
| INFRA-02 | 01-03/01-04 | Solver via `select_optimizer(::ProblemClass)` | ✓ SATISFIED | Factory dispatch; no model names a solver. |
| INFRA-03 | 01-03/01-04 | Assert OPTIMAL / tight gap | ✓ SATISFIED | `assert_solved!` choke point; SOCP tol_gap set in factory. |
| INFRA-05 | 01-02 | One per-unit system, convert-once, tripwires | ✓ SATISFIED | `PerUnit.jl` + live-path `assert_magnitudes`. |
| DATA-01 | 01-02 | Define radial feeder structs | ✓ SATISFIED | Immutable JuMP-free `Bus`/`Branch`/`Feeder`. |
| DATA-02 | 01-02 | Validate radial, clear error | ✓ SATISFIED | `assert_radial` throws `ArgumentError`. |
| PF-01 | 01-03/01-04 | Swappable `AbstractPowerFlow` residual seam | ✓ SATISFIED | Abstract type + `contribute!`; residual accumulation wired in toy_dc. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/core/status.jl` | 55 | `assert_no_slack` defined/exported but never called in `src` or tests | ⚠️ Warning | Hidden-slack guard exists but is not exercised on the live solve path; criterion 3's core (`is_solved_and_feasible`) is fully wired, so not blocking. Consider wiring into `solve_toy_dc` or covering with a test. |

No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`PLACEHOLDER`) found in `src/` or `ext/`.

### Human Verification Required

**1. Julia 1.10 LTS CI leg**
- **Test:** Run the CI matrix (or `juliaup add 1.10 && julia +1.10 --project=. -e 'import Pkg; Pkg.instantiate(); Pkg.test()'`).
- **Expected:** Clean-checkout instantiate + 44 passing tests on 1.10.
- **Why human:** 1.10 not installed locally. NOTE: given the 1.12-pinned committed manifest, this leg is expected to FAIL until the manifest gap (Truth 1) is resolved — this is CI-delegated per criterion 1's note but is now coupled to a concrete, locally-observed 1.11 failure.

### Gaps Summary

The architectural keystone itself is fully achieved and locked: the JuMP-free immutable data model, `ModelContext` residual registry, `select_optimizer(::ProblemClass)` solver abstraction (no model names a solver), the `assert_solved!` status choke point, per-unit convert-once discipline with live-path magnitude tripwires, and the rung-0 toy DC end-to-end solve all verify against the code, and the full suite is green (44/44) on Julia 1.12. Truths 2–5 are solidly met.

The single gap is **reproducibility across the declared Julia floor (INFRA-01 / criterion 1)**. The committed `Manifest.toml` and `test/Manifest.toml` are pinned to `julia_version = "1.12.5"`; a clean `Pkg.instantiate()` on the locally-installed Julia 1.11 hard-errors with a cross-version manifest incompatibility. Because the same root cause affects any non-1.12 leg, the CI `julia-actions/julia-buildpkg` step on the 1.10 and 1.11 matrix legs is expected to fail as configured. The `[compat]` floor (`julia = "1.10"`, `SparseArrays = "1"`) and the CI matrix definition are correct — what is missing is a manifest strategy that actually instantiates on 1.10/1.11 (e.g. Julia-version-specific manifests), or an explicit decision (override) to target 1.12-only reproducibility. The SUMMARY's INFRA-01 verification exercised `Pkg.instantiate()` on the 1.12 dev machine only and conflated "[compat] floor allows 1.10" with "committed manifest instantiates on 1.10/1.11" — the latter does not hold.

---

_Verified: 2026-07-18T17:20:20Z_
_Verifier: Claude (gsd-verifier)_
