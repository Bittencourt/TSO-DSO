---
phase: 01-plumbing-solver-abstraction
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/models/toy_dc.jl
  - docs/make.jl
  - docs/literate/toy_dc.jl
autonomous: true
requirements: [INFRA-02, INFRA-03, PF-01]
must_haves:
  truths:
    - "A toy single-node, single-period DC model builds, solves via select_optimizer(LP()), asserts OPTIMAL, and returns a finite objective"
    - "The toy solve also returns the nodal-balance dual (price seam ready for Phase 5)"
    - "The toy model routes its nodal balance through ctx.residuals (PF-01 seam exercised end-to-end)"
    - "The toy model file names no concrete solver"
    - "A Literate page documents the per-unit base and the toy DC math and executes end-to-end"
  artifacts:
    - path: "src/models/toy_dc.jl"
      provides: "solve_toy_dc(feeder) walking-skeleton build+solve"
      contains: "function solve_toy_dc"
    - path: "docs/literate/toy_dc.jl"
      provides: "executable Literate page for rung 0"
    - path: "docs/make.jl"
      provides: "Documenter build wiring the Literate page"
  key_links:
    - from: "src/models/toy_dc.jl"
      to: "select_optimizer(LP()) / ModelContext / assert_solved!"
      via: "builds Model(select_optimizer(LP())), add_to_residual!, assert_solved!"
      pattern: "select_optimizer\\(LP\\(\\)\\)"
    - from: "src/models/toy_dc.jl"
      to: "dual(balance)"
      via: "returns nodal-balance dual"
      pattern: "dual\\("
---

<objective>
Close the walking skeleton: wire the toy single-node, single-period DC solve end-to-end through every
seam built in Waves 1–2 (per-unit feeder → `select_optimizer(LP())` factory → `ModelContext` residual
registry → `assert_solved!` status choke point → objective + nodal-balance dual), and prove the docs
pipeline with an executable Literate page. This drives the `rung0` integration @testitem green — the
phase's headline deliverable.

Purpose: this is the integration slice — the first moment a researcher can *do* something end-to-end
(express a trivial feeder, get a trustworthy objective and price). It exercises INFRA-02, INFRA-03, and
PF-01 together and proves the Documenter+Literate reproducibility path.
Output: `src/models/toy_dc.jl`, `docs/literate/toy_dc.jl`, `docs/make.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-plumbing-solver-abstraction/01-RESEARCH.md
@.planning/phases/01-plumbing-solver-abstraction/SKELETON.md

<interfaces>
<!-- Consumes contracts delivered by plans 01-02 and 01-03. Fills the toy_dc.jl stub from plan 01-01. -->

From plan 01-02 (data/units):  Feeder, Bus, Branch, PerUnitBase, assert_magnitudes
From plan 01-03 (solver/core): LP, select_optimizer, ModelContext, register_constraint!,
                               add_to_residual!, assert_solved!, assert_no_slack

Target (RESEARCH §Code Examples toy DC):
  function solve_toy_dc(feeder::Feeder) -> (ctx::ModelContext, objective::Float64, price::Float64)
    model = Model(select_optimizer(LP()))         # factory — NO solver named here
    ctx = ModelContext(model); ctx.meta[:feeder] = feeder
    @variable p_import >= 0 ; @variable 0 <= p_load <= 1.0
    balance = @constraint(model, p_import - p_load == 0)   # route via add_to_residual! + register_constraint!
    @objective(model, Max, 3.0*p_load - 1.0*p_import)      # toy welfare
    assert_solved!(model; dual=true, allow_local=false)    # INFRA-03
    return ctx, objective_value(model), dual(balance)      # dual ready for Phase 5

Also wire the Parameter pattern as a commented example for Phase 6 (RESEARCH §Parameter pattern) — wired-but-unused.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end toy DC single-node solve through all seams (rung 0)</name>
  <files>src/models/toy_dc.jl, test/test_toy_dc.jl</files>
  <read_first>
    - .planning/phases/01-plumbing-solver-abstraction/01-RESEARCH.md (§Code Examples toy DC build+solve, §Parameter pattern, §Pattern 6 residual registry, §Open-Question 2 single-node sufficiency)
    - src/models/toy_dc.jl (empty stub from plan 01-01)
    - test/test_toy_dc.jl (red rung0 @testitem, tags=[:rung0])
    - src/solver/factory.jl, src/core/status.jl, src/core/ModelContext.jl, src/data/Feeder.jl (contracts to consume)
  </read_first>
  <behavior>
    - `solve_toy_dc(feeder)` on a trivial valid single-node feeder returns a finite objective and a finite nodal-balance dual.
    - The returned `ctx.residuals` contains the nodal-balance contribution (PF-01 seam populated, not empty).
    - The returned `ctx.constraints[:balance]` is registered (dual retrievable).
    - The solve passes `is_solved_and_feasible(ctx.model; allow_local=false)`.
  </behavior>
  <action>
    Fill `src/models/toy_dc.jl` with `solve_toy_dc(feeder::Feeder)` per <interfaces>: build
    `Model(select_optimizer(LP()))` (factory only — name NO solver here), wrap in `ModelContext`, add the
    trivial `p_import`/`p_load` variables and the nodal-balance `@constraint`, route the balance through
    `register_constraint!(ctx, :balance, balance)` AND `add_to_residual!(ctx, :nodal_balance, ...)` so the
    PF-01 seam is exercised even at rung 0 (RESEARCH Open-Question 2 recommendation), set the toy welfare
    `@objective`, call `assert_solved!(model; dual=true, allow_local=false)`, and return
    `(ctx, objective_value(model), dual(balance))`. Include a commented `@variable(model, ρ in
    Parameter(1.0))` + `set_parameter_value` example (wired-but-unused, for Phase 6 ADMM). `export
    solve_toy_dc`. Drive the `rung0` @testitem green: assert finite objective, finite dual, populated
    `ctx.residuals[:nodal_balance]`, and `is_solved_and_feasible(ctx.model; allow_local=false)`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("toy", ti.name)' && ! grep -qE '\b(using|import)\s+(HiGHS|Clarabel|Ipopt|Gurobi|Mosek)' src/models/toy_dc.jl</automated>
  </verify>
  <acceptance_criteria>
    - `src/models/toy_dc.jl` contains `function solve_toy_dc` and builds `Model(select_optimizer(LP()))`.
    - `src/models/toy_dc.jl` names no concrete solver (grep for HiGHS/Clarabel/Ipopt/Gurobi/Mosek finds none).
    - `@run_package_tests filter=ti->occursin("toy", ti.name)` passes (rung0 green).
    - The rung0 test asserts a finite objective, a finite nodal-balance `dual`, populated `ctx.residuals[:nodal_balance]`, and `is_solved_and_feasible(ctx.model; allow_local=false)`.
  </acceptance_criteria>
  <done>Toy DC single-node solve builds via the factory, routes through the residual registry, asserts OPTIMAL, and returns objective + dual; rung0 @testitem green.</done>
</task>

<task type="auto">
  <name>Task 2: Literate rung-0 doc page + Documenter wiring (reproducibility pipeline)</name>
  <files>docs/literate/toy_dc.jl, docs/make.jl</files>
  <read_first>
    - .planning/phases/01-plumbing-solver-abstraction/01-RESEARCH.md (§Standard Stack Documenter/Literate, §Pattern 5 documented per-unit base)
    - .planning/phases/01-plumbing-solver-abstraction/SKELETON.md (per-unit base decision, capability statement)
    - docs/make.jl (PkgTemplates-generated skeleton from plan 01-01)
    - src/models/toy_dc.jl (the function the page demonstrates)
  </read_first>
  <action>
    Create `docs/literate/toy_dc.jl` as a Literate source that: (1) documents the chosen per-unit base
    (`S_base=1.0 MVA`, `V_base=4.16 kV` placeholder, noting supersession by Phase-4 fixtures), (2) states
    the toy DC math (trivial nodal balance + welfare objective) in prose/KaTeX, and (3) executes
    `solve_toy_dc` on a trivial feeder end-to-end, showing the objective and dual — so the page is a
    runnable reproducibility proof. Wire `docs/make.jl` to run Literate over `docs/literate/toy_dc.jl`
    into a Documenter page and include it in `makedocs`. Keep it minimal — one page proving the pipeline;
    full per-model math docs are Phase 9 (EXP-03). Do not name a solver in the doc example (use
    `solve_toy_dc`).
  </action>
  <verify>
    <automated>julia --project=docs -e 'import Pkg; Pkg.develop(Pkg.PackageSpec(path=".")); Pkg.instantiate(); include("docs/make.jl")' 2>&1 | tail -5; test -f docs/literate/toy_dc.jl && grep -q 'solve_toy_dc' docs/literate/toy_dc.jl</automated>
  </verify>
  <acceptance_criteria>
    - `docs/literate/toy_dc.jl` exists, documents the per-unit base, and calls `solve_toy_dc` end-to-end.
    - `docs/make.jl` runs Literate over the page and includes it in `makedocs`.
    - `include("docs/make.jl")` builds the docs without error (the Literate page executes the toy solve).
    - The doc example names no concrete solver.
  </acceptance_criteria>
  <done>Literate rung-0 page documents the per-unit base + toy math and executes the solve; Documenter build succeeds — reproducibility pipeline proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feeder input → solve → returned price | The toy solve consumes a validated feeder and returns an objective + dual that downstream phases will trust as a price |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | `solve_toy_dc` returning an objective/dual from a non-optimal solve | mitigate | `solve_toy_dc` returns only after `assert_solved!(...; allow_local=false)` (INFRA-03 choke point) — inherited from plan 01-03 |
| T-01-07 | Tampering | Toy model naming a concrete solver, breaking swappability | mitigate | Model built via `select_optimizer(LP())`; Task 1 verify greps `src/models/toy_dc.jl` for solver names and finds none (INFRA-02) |
| T-01-09 | Repudiation | Non-reproducible doc example drifting from code | mitigate | Literate page executes the real `solve_toy_dc` during the Documenter build — the doc cannot pass while diverging from the code |
</threat_model>

<verification>
- `@run_package_tests filter=ti->occursin("toy", ti.name)` green (rung0 end-to-end).
- Full suite green: `julia --project=. -e 'import Pkg; Pkg.test()'` (includes Aqua/JET).
- `include("docs/make.jl")` builds docs; Literate page executes the toy solve.
- Phase gate (INFRA-01): CI matrix green on Julia 1.10 LTS + 1.11 (+1.12) before `/gsd:verify-work` (1.10 verified via CI or `juliaup add 1.10`).
</verification>

<success_criteria>
- A toy single-node, single-period DC model builds, solves via `select_optimizer(::ProblemClass)`, and returns an objective — no model file names a solver (INFRA-02, phase success criterion #2).
- The solve asserts OPTIMAL and would fail loudly otherwise (INFRA-03, criterion #3).
- The nodal balance routes through the shared residual registry (PF-01) and the dual is available (price seam for Phase 5).
- The docs/reproducibility pipeline executes end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-plumbing-solver-abstraction/01-04-SUMMARY.md` when done.
</output>
