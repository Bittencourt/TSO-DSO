---
phase: 07-admm-convergence-scale
plan: 06
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - ext/TSODSOMakieExt.jl
  - test/test_diagnostics_plot.jl
autonomous: true
requirements: [ADMM-05]
must_haves:
  truths:
    - "plot_convergence(res::AdmmResiduals) and plot_price_convergence(res) return a Makie Figure ONLY when CairoMakie is loaded, rendering the primal/dual residual traces (with ε threshold lines) and the price-convergence trajectory."
    - "Without CairoMakie loaded, the plot functions are exported generic functions with NO applicable method for AdmmResiduals (core solve + headless CI stay plot-free)."
    - "The plotting reads the extended AdmmResiduals traces (primal/dual/ρ/ε/price) and never pulls JuMP or a solver."
  artifacts:
    - path: "ext/TSODSOMakieExt.jl"
      provides: "CairoMakie-backed TSODSO.plot_convergence / plot_price_convergence methods"
      contains: "TSODSO.plot_convergence"
    - path: "test/test_diagnostics_plot.jl"
      provides: "Plotting-extension isolation @testitems (undefined-without / defined-with CairoMakie)"
      contains: "plot"
  key_links:
    - from: "ext/TSODSOMakieExt.jl"
      to: "TSODSO.plot_convergence / plot_price_convergence stubs"
      via: "extension methods on the core generic functions"
      pattern: "TSODSO.plot_convergence"
    - from: "ext/TSODSOMakieExt.jl"
      to: "AdmmResiduals traces"
      via: "lines!(primal_trace/dual_trace/eps_*_trace)"
      pattern: "primal_trace|dual_trace"
---

## Phase Goal

**As a** PhD researcher, **I want to** run ADMM with correct primal+dual stopping,
per-unit-normalized adaptive ρ, and plottable convergence diagnostics that scale to the
IEEE 123-node voltage case, **so that** the decomposed transactive prices are trustworthy
on the research-target regimes.

<objective>
Fill the `ext/TSODSOMakieExt.jl` scaffold (from 07-01) with the CairoMakie-backed plotting methods
for the convergence diagnostics, and prove the extension is ISOLATED: the plot functions light up
only when CairoMakie is loaded, so the core solve and headless CI never import a heavy viz stack.
This completes Success Criterion 2 (diagnostics reported and plottable) alongside the ledger
extension (07-01).

Purpose: ADMM-05 — convergence diagnostics reported and PLOTTABLE. RESEARCH Pattern 6 keeps
plotting a weakdep extension (STACK.md thesis-figure choice) so `using TSODSO` stays plot-free.

Output: `TSODSO.plot_convergence` / `plot_price_convergence` methods returning a Figure; isolation
@testitems; a human-verified figure aesthetics check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-admm-convergence-scale/07-RESEARCH.md
@ext/TSODSOGurobiExt.jl
@src/admm/residuals.jl
@CLAUDE.md

<interfaces>
<!-- Core stubs (src/diagnostics/plots.jl, from 07-01) the extension extends: -->
```julia
function plot_convergence end         # exported generic fn, no methods in core
function plot_price_convergence end   # exported generic fn, no methods in core
```
<!-- Extended ledger (src/admm/residuals.jl, from 07-01) the plots read (JuMP-free): -->
```julia
res.iters; res.primal_trace; res.dual_trace; res.rho_trace;
res.eps_pri_trace; res.eps_dual_trace; res.price_gap_trace
```
<!-- Makie API (RESEARCH Pattern 6 example): Figure, Axis(f[1,1]; xlabel,ylabel,yscale=log10),
     lines!/scatterlines!, axislegend, save(filename, f). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the CairoMakie extension methods (return a Figure)</name>
  <read_first>
    - ext/TSODSOGurobiExt.jl + ext/TSODSOMosekExt.jl (the weakdep-module pattern: `module …; using
      TSODSO, <Dep>; TSODSO.<fn>(…) = …; end`) and ext/TSODSOMakieExt.jl (the scaffold from 07-01)
    - src/diagnostics/plots.jl (the exported generic-function stubs to extend) and src/admm/residuals.jl
      (the extended trace fields the plots read)
    - 07-RESEARCH.md Pattern 6 (the plot_convergence example: Figure + Axis(yscale=log10) + lines!
      for primal/dual/ε traces + axislegend + optional save; plot_price_convergence similar with
      ρ_trace on a twin axis / price_gap_trace / per-node λ) and the "Plotting" Don't-Hand-Roll row
  </read_first>
  <action>
    Fill `ext/TSODSOMakieExt.jl` (module TSODSOMakieExt; `using TSODSO, CairoMakie`) with the two
    methods extending the core generic functions (Pattern 6): `TSODSO.plot_convergence(res::TSODSO.
    AdmmResiduals; filename=nothing)` builds a Figure with a log-scale residual axis, plots
    `primal_trace` and `dual_trace` vs `1:res.iters` with the `eps_pri_trace`/`eps_dual_trace`
    threshold lines (dashed), an axis legend, optionally `save`s to `filename`, and returns the
    Figure; `TSODSO.plot_price_convergence(res; filename=nothing)` plots the price-convergence
    trajectory (`price_gap_trace`, with `rho_trace` on a twin axis) and returns the Figure. Read
    ONLY the JuMP-free ledger fields — no solver, no JuMP. Mirror the Gurobi/Mosek extension module
    structure exactly. Do NOT add CairoMakie to [deps] (it stays a weakdep — 07-01 wired it).
  </action>
  <acceptance_criteria>
    ext/TSODSOMakieExt.jl defines TSODSO.plot_convergence / plot_price_convergence returning a
    Figure, reading only the extended AdmmResiduals traces; without CairoMakie the methods do not
    exist (core stays plot-free); the module mirrors the existing weakdep extension pattern.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; @assert !hasmethod(TSODSO.plot_convergence, Tuple{TSODSO.AdmmResiduals}) "core suite must stay plot-free (no method without CairoMakie)"; println("core plot-free OK")'</automated>
  </verify>
  <done>
    ext/TSODSOMakieExt.jl defines TSODSO.plot_convergence / plot_price_convergence returning a
    Figure, reading only the extended AdmmResiduals traces; without CairoMakie the methods do not
    exist (core stays plot-free); the module mirrors the existing weakdep extension pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plotting-extension isolation @testitems</name>
  <read_first>
    - test/test_diagnostics_plot.jl (the RED "plot"/"makie" items from 07-01) and test/fixtures_phase7.jl
      (a synthetic AdmmResiduals can be constructed + record!-populated with no solve)
    - src/admm/residuals.jl (record! extended arity, to populate a small synthetic trace)
    - 07-RESEARCH.md §"Phase Requirements → Test Map" (plot_convergence errors helpfully / returns a
      Figure with CairoMakie) and 07-VALIDATION.md (the plotting extension test loads CairoMakie in a
      SEPARATE env; the aesthetics are the manual-only check)
  </read_first>
  <action>
    Fill the isolation @testitems in test/test_diagnostics_plot.jl (names carrying "plot"/"makie").
    In the CORE suite (no CairoMakie): construct a small synthetic AdmmResiduals, record! a few
    iterations of traces, and assert `!hasmethod(TSODSO.plot_convergence, Tuple{AdmmResiduals})` and
    `!hasmethod(TSODSO.plot_price_convergence, ...)` — the plot functions are defined generic
    functions but have NO applicable method (isolation). For the WITH-CairoMakie path, add a test
    that (in a separate environment / after `import CairoMakie`) the methods exist and return a
    `Makie.Figure` from the synthetic ledger — gated so it is skipped (not failed) when CairoMakie
    is absent, so the headless core suite stays green and plot-free. Document the separate-env
    invocation in the test header.
  </action>
  <acceptance_criteria>
    The core-suite isolation items assert the plot functions have no AdmmResiduals method without
    CairoMakie; the with-CairoMakie item (separate env) confirms a Figure is returned and is skipped
    (not failed) when CairoMakie is absent; the "plot"/"makie" @testitems are GREEN in the headless
    suite.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("plot", ti.name) || occursin("makie", ti.name))'</automated>
    <human-check>In an environment with CairoMakie installed (import CairoMakie; using TSODSO), run a small ADMM solve (or load a saved AdmmResiduals), then save("conv.pdf", plot_convergence(res)) and save("price.pdf", plot_price_convergence(res)); open the PDFs and confirm the residual curves decay below the ε threshold lines, axes are labeled/log-scaled, and the figures are thesis-grade vector output (07-VALIDATION Manual-Only aesthetics check).</human-check>
  </verify>
  <done>
    The core-suite isolation items assert the plot functions have no AdmmResiduals method without
    CairoMakie; the with-CairoMakie item (separate env) confirms a Figure is returned and is skipped
    (not failed) when CairoMakie is absent; the "plot"/"makie" @testitems are GREEN in the headless
    suite.
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viz extension → core solve | Makie must never load in the core solve / headless CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Tampering | CairoMakie leaking into the core solve / headless CI precompile | mitigate | Plotting lives ONLY in the weakdep extension; isolation @testitem asserts no AdmmResiduals method without CairoMakie; core suite runs plot-free (verify command) |
| T-07-18 | Tampering | Plotting reaching into JuMP/solver state | mitigate | Methods read ONLY the JuMP-free AdmmResiduals traces; residuals.jl imports no JuMP (07-01 contract) |
| T-07-SC | Tampering | CairoMakie/Makie supply-chain | accept | Canonical Makie-org packages, registry-verified + pinned in the committed Manifest (07-01); weakdep only |
</threat_model>

<verification>
- plot_convergence/plot_price_convergence return a Figure with CairoMakie; no method without it.
- Core suite stays plot-free; plotting reads only JuMP-free ledger traces.
- Human-verify confirms thesis-grade vector figures.
</verification>

<success_criteria>
- CairoMakie-backed convergence + price plotting via the weakdep extension.
- Isolation proven (core plot-free); figures human-verified.
</success_criteria>

<output>
Create `.planning/phases/07-admm-convergence-scale/07-06-SUMMARY.md` when done.
</output>
