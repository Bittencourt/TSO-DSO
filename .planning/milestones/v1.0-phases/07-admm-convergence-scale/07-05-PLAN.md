---
phase: 07-admm-convergence-scale
plan: 05
type: execute
wave: 4
depends_on: [07-02, 07-04]
files_modified:
  - test/test_ieee123_admm.jl
autonomous: true
requirements: [ADMM-02]
must_haves:
  truths:
    - "ADMM on the IEEE-123 voltage-constrained fixture converges in ~tens of iterations (both residuals below their per-unit thresholds) with the same adaptive-ρ config used for 2-bus/IEEE-13."
    - "At convergence the recovered coupling price λ_j equals the DADP — cross-validated against the centralized SOCP where monolithically solvable, else certified by primal+dual→0 + PF-04 exactness + PRICE-04 economic-direction sanity."
    - "The PF-04 SOC exactness invariant holds at the converged point (assert_socp_exact! passes / exact_maxgap below tolerance)."
    - "Zero-injection transit buses are handled (no throw); the Phase-6 centralized cross-validation on 2-bus and IEEE-13 still passes (regression)."
  artifacts:
    - path: "test/test_ieee123_admm.jl"
      provides: "IEEE-123 ADMM convergence + λ→DADP + PF-04 exactness integration @testitems"
      contains: "ieee123"
  key_links:
    - from: "test/test_ieee123_admm.jl"
      to: "solve_admm on ieee123_modified() + Phase7Fixtures population"
      via: "end-to-end convergence + cross-validation"
      pattern: "solve_admm"
    - from: "test/test_ieee123_admm.jl"
      to: "solve_welfare / extract_dlmp (centralized oracle)"
      via: "λ_j → DADP cross-validation (where solvable)"
      pattern: "extract_dlmp|solve_welfare"
---

## Phase Goal

**As a** PhD researcher, **I want to** run ADMM with correct primal+dual stopping,
per-unit-normalized adaptive ρ, and plottable convergence diagnostics that scale to the
IEEE 123-node voltage case, **so that** the decomposed transactive prices are trustworthy
on the research-target regimes.

<objective>
Prove the hardened ADMM (07-04) on the IEEE-123 voltage-constrained fixture (07-02) end-to-end:
converges in ~tens of iterations with the SAME adaptive-ρ config, recovers λ_j → DADP, holds the
PF-04 exactness invariant at convergence, and handles the ~37 transit buses — while the Phase-6
2-bus/IEEE-13 cross-validation still passes. This is the final assembly slice for Success
Criterion 3.

Purpose: Success Criterion 3 (IEEE-123 converges ~tens of iters, λ_j → DADP, exactness holds).
This is the phase's scale target — the research-regime the whole hardening effort exists for.

Output: green IEEE-123 convergence @testitems; a documented convergence certificate (iters,
welfare/DADP gap vs centralized, exact_maxgap); the Phase-6 regression re-confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-admm-convergence-scale/07-RESEARCH.md
@.planning/phases/07-admm-convergence-scale/07-VALIDATION.md
@src/admm/solve_admm.jl
@CLAUDE.md

<interfaces>
<!-- Under test (built by 07-02/07-04, consumed here): -->
```julia
ieee123_modified() -> Feeder                          # 07-02: radial per-unit voltage-constrained fixture
Phase7Fixtures.<population helper>(feeder; seed=…)    # 07-01: seeded per-load-node aggregators
solve_admm(feeder, ConvexBranchFlow(), aggregators;   # 07-04: adaptive-ρ, two-residual stop
           T, λ₀, ρ, ε_abs, ε_rel, τ, μ, ρ_min, ρ_max, maxiter, allow_export)
  -> (; welfare, dadp, λ, iters, residuals, dso_ctx, exact_maxgap)
solve_welfare(feeder, ConvexBranchFlow(), aggs; T, λ₀, allow_export) -> (ctx, obj, _)   # centralized oracle
extract_dlmp(ctx; bus, T)                                                                # centralized DADP
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IEEE-123 ADMM convergence + λ→DADP cross-validation + PF-04 exactness</name>
  <read_first>
    - test/test_ieee123_admm.jl (the RED "ieee123/crossval" convergence items from 07-01) and
      test/test_admm.jl (the IEEE-13 crossval item shape: centralized solve_welfare/extract_dlmp vs
      solve_admm, welfare rtol + λ atol/rtol, PF-04 exact_maxgap)
    - test/fixtures_phase7.jl (the Phase7Fixtures population helper + λ₀ + adaptive-ρ config consts,
      from 07-01) and src/data/ieee123.jl (07-02)
    - 07-RESEARCH.md convergence expectation (A5: the monolithic IEEE-123 SOCP still solves in
      Clarabel for cross-validation; fallback = primal+dual→0 + PF-04 exactness + PRICE-04 sanity),
      Pitfall 6 (iteration count ≫ tens ⇒ retune within the clamp, not per-fixture ρ), Pitfall 7
      (exactness at scale under reverse flow/over-voltage is a genuine physical finding, surfaced via
      assert_socp_exact! — not suppressed), Open Q2 (seeded aggregators sized voltage-binding)
    - 07-VALIDATION.md Manual-Only row (if the centralized SOCP is too large/slow, use the
      residual+exactness+price-sanity certificate)
  </read_first>
  <behavior>
    - solve_admm on ieee123_modified() + the seeded population converges (both residuals below
      per-unit thresholds) in ~tens of iterations using the SAME (ε_abs, ε_rel, τ, μ, ρ_min, ρ_max)
      as 2-bus/IEEE-13 — no per-fixture ρ. res.iters is asserted to be in the tens range (a loose
      upper bound, e.g. ≤ ~100, not ≫ that).
    - Where the centralized SOCP is monolithically solvable: res.welfare ≈ objective_value and
      res.λ ≈ extract_dlmp at every load node (welfare rtol + λ atol/rtol, as IEEE-13).
    - The converged exact_maxgap is below the PF-04 tolerance (assert_socp_exact! passed on the
      final DSO solve).
    - The transit buses are handled (the run does not throw at build_dso_opt).
    - PRICE-04 economic-direction sanity holds (DADP < λ₀ at PV glut hours, > λ₀ at congested
      hours) as an additional certificate.
  </behavior>
  <action>
    Fill the RED IEEE-123 convergence @testitems in test/test_ieee123_admm.jl: build
    `ieee123_modified()`, populate seeded aggregators via the Phase7Fixtures helper, and run
    `solve_admm(...; ConvexBranchFlow(), the adaptive-ρ config consts, allow_export=true)`. Assert
    convergence in ~tens of iterations (a loose bound, not a brittle exact count — Pitfall 6). For
    the λ→DADP check, attempt the centralized cross-validation first (solve_welfare + extract_dlmp,
    welfare rtol and per-load-node λ match, as the IEEE-13 item); if the monolithic SOCP is
    infeasible/too slow to solve in CI (A5), fall back to the certificate: both residuals → 0,
    exact_maxgap below the PF-04 tolerance, and PRICE-04 economic-direction sanity — document which
    path was taken in the test comments. Assert the converged exact_maxgap and that the run handled
    transit buses (no throw). Keep item names carrying "ieee123"/"crossval". Do NOT weaken any gate
    to force a pass — if exactness genuinely fails at scale, that is a physical finding to surface
    (Pitfall 7), not to suppress.
  </action>
  <acceptance_criteria>
    IEEE-123 ADMM converges in ~tens of iterations with the shared adaptive-ρ config; λ_j → DADP is
    certified (centralized cross-validation where solvable, else primal+dual→0 + PF-04 exactness +
    PRICE-04 sanity); exact_maxgap below tolerance; transit buses handled; the "ieee123"/"crossval"
    @testitems are GREEN.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("ieee123", ti.name))'</automated>
  </verify>
  <done>
    IEEE-123 ADMM converges in ~tens of iterations with the shared adaptive-ρ config; λ_j → DADP is
    certified (centralized cross-validation where solvable, else primal+dual→0 + PF-04 exactness +
    PRICE-04 sanity); exact_maxgap below tolerance; transit buses handled; the "ieee123"/"crossval"
    @testitems are GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase-6 regression assertion + full-suite green gate</name>
  <read_first>
    - test/test_admm.jl (the Phase-6 2-bus + IEEE-13 crossval items that must still pass under
      adaptive ρ) and test/runtests.jl (@run_package_tests full-suite discovery)
    - 07-04-SUMMARY.md (the adaptive-ρ changes whose regression this gate guards) — read once
      available; else the 07-04 plan behavior blocks
  </read_first>
  <action>
    Add (or, if already covered, explicitly reference and re-run) a same-suite regression assertion
    that the Phase-6 centralized cross-validation on the 2-bus and IEEE-13 fixtures is still GREEN
    after adaptive ρ — welfare ≈ centralized objective_value AND λ ≈ extract_dlmp at every load
    node, with the 2-bus DADP sign still positive. Confirm no PF-04 or App. C gate was weakened to
    force any pass (the exact_maxgap and battery-complementarity assertions remain live). Run the
    FULL suite green as the phase gate. Record the IEEE-123 iteration count and welfare/DADP gap (or
    the fallback certificate) in the test output / SUMMARY for the convergence certificate.
  </action>
  <acceptance_criteria>
    The Phase-6 2-bus/IEEE-13 crossval items pass under adaptive ρ; no gate weakened; the full suite
    is GREEN; the IEEE-123 convergence certificate (iters + gap/exact_maxgap) is recorded.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <done>
    The Phase-6 2-bus/IEEE-13 crossval items pass under adaptive ρ; no gate weakened; the full suite
    is GREEN; the IEEE-123 convergence certificate (iters + gap/exact_maxgap) is recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADMM result → reported price | a wrong/inexact converged point yields a wrong DADP at scale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | Silently-wrong prices from an inexact SOC relaxation at IEEE-123 scale (reverse flow/over-voltage) | mitigate | assert_socp_exact! on the converged DSO solve refuses prices on an inexact cone; exact_maxgap asserted below tolerance; a genuine failure is surfaced, not suppressed (Pitfall 7) |
| T-07-14 | Repudiation | IEEE-123 "convergence" that disagrees with the true optimum | mitigate | Centralized cross-validation (welfare + per-node DADP) where solvable (A5); else primal+dual→0 + PF-04 exactness + PRICE-04 economic-direction certificate |
| T-07-15 | Denial of service | IEEE-123 fails to converge / iteration count ≫ tens | mitigate | Shared clamped/frozen adaptive ρ; loose tens-range bound asserted; fail-loud cap throws rather than returning a non-consensus iterate (Pitfall 6) |
| T-07-16 | Repudiation | Adaptive ρ silently regresses the Phase-6 2-bus/IEEE-13 result | mitigate | Phase-6 crossval items asserted still green in the same suite (Task 2); no gate weakened; full-suite green gate |
</threat_model>

<verification>
- IEEE-123 converges ~tens of iters; λ→DADP certified; exact_maxgap below tolerance; transit handled.
- Phase-6 2-bus/IEEE-13 crossval regression green; full suite green.
</verification>

<success_criteria>
- IEEE-123 voltage case converges with λ_j → DADP and PF-04 exactness at convergence.
- No regression to the Phase-6 centralized cross-validation; full suite green.
</success_criteria>

<output>
Create `.planning/phases/07-admm-convergence-scale/07-05-SUMMARY.md` when done.
</output>
