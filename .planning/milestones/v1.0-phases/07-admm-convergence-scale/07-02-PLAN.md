---
phase: 07-admm-convergence-scale
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/data/ieee123.jl
  - test/test_ieee123.jl
autonomous: true
requirements: [ADMM-02]
must_haves:
  truths:
    - "ieee123_modified() returns an immutable, JuMP-free radial Feeder built from thesis App. E per-unit R/X data."
    - "The feeder is radial by construction (edges == N−1) with contiguous 1-based bus ids and the substation/frontier as root."
    - "All branch impedances and the head-branch apparent-power limit pass the per-unit magnitude bands; voltage bounds are V∈[0.9,1.1] (thesis Case B)."
    - "The relabeling map (thesis terminal → 1..N) is documented in the fixture header and asserted by the construction test."
  artifacts:
    - path: "src/data/ieee123.jl"
      provides: "ieee123_modified() / build_ieee123() radial per-unit Feeder fixture (thesis App. E)"
      contains: "function ieee123_modified"
    - path: "test/test_ieee123.jl"
      provides: "Fixture-construction @testitems (radial, magnitudes, ids, root, edge count, relabel)"
      contains: "ieee123"
  key_links:
    - from: "src/data/ieee123.jl"
      to: "Feeder / assert_radial / assert_magnitudes"
      via: "Feeder(buses, branches, root) inner-constructor validation"
      pattern: "Feeder\\(buses, branches"
    - from: "src/data/ieee123.jl"
      to: "SparseArrays"
      via: "sparse incidence for the 123-node topology"
      pattern: "SparseArrays|sparse"
---

## Phase Goal

**As a** PhD researcher, **I want to** run ADMM with correct primal+dual stopping,
per-unit-normalized adaptive ρ, and plottable convergence diagnostics that scale to the
IEEE 123-node voltage case, **so that** the decomposed transactive prices are trustworthy
on the research-target regimes.

<objective>
Fill the `src/data/ieee123.jl` seam (stubbed by plan 07-01) with the modified IEEE 123-node
voltage-constrained feeder from thesis Appendix E — a radial, per-unit, JuMP-free `Feeder`,
validated by construction. This is the scale target's topology; the aggregator population lives in
Phase7Fixtures (07-01), and the ADMM convergence run is 07-05.

Purpose: The IEEE-123 case is the phase's scale success criterion (converges in ~tens of iters,
λ_j → DADP, exactness holds). It needs a correct radial fixture first. The two genuinely new
work-items of the phase are this transcription and the transit-node relaxation (07-03) —
everything else already has a canonical solution (RESEARCH Summary).

Output: `ieee123_modified()` (per RESEARCH fixture skeleton) + green fixture-construction
@testitems.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-admm-convergence-scale/07-RESEARCH.md
@src/data/ieee13.jl
@src/data/Feeder.jl
@CLAUDE.md

<interfaces>
<!-- Fixture construction contract (src/data/Feeder.jl, src/units/PerUnit.jl, src/data/topology.jl): -->
```julia
struct Bus{T<:Real}    id::Int; vmin::T; vmax::T; is_root::Bool end
struct Branch{T<:Real} from::Int; to::Int; r::T; x::T; smax::T end
Feeder(buses::Vector{Bus{T}}, branches::Vector{Branch{T}}, root::Integer)  # runs assert_radial + assert_magnitudes
# assert_radial: edges == N-1, root in 1:N, bus.id == 1-based position, endpoints in 1:N
# assert_magnitudes bands: VOLTAGE_PU∈[0.8,1.2]; 0 ≤ r,x < IMPEDANCE_PU_MAX(5.0); 0 < smax < SMAX_PU_MAX(100.0)
const SMAX_NO_LIMIT = 99.0   # sentinel for effectively-unconstrained interior branches
# to_pu_power(MVA, base) converts a head-branch limit once at ingestion
```
<!-- ieee13.jl pattern to mirror: a `raw = [(thesis_from, thesis_to, r_pu, x_pu, smax_pu), ...]`
     table + a documented terminal→struct-index shift, then Branch/Bus vectors + Feeder(...). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transcribe thesis App. E into ieee123_modified() (radial, per-unit, SparseArrays)</name>
  <read_first>
    - src/data/ieee13.jl (the FULL fixture pattern: base const, SMAX handling, the raw R/X table,
      the documented node→index shift, the Feeder(...) call that validates by construction)
    - src/data/Feeder.jl + src/units/PerUnit.jl (the Bus/Branch structs and the magnitude bands
      r,x < 5.0, 0 < smax < 100.0, voltage ∈ [0.8,1.2]) + src/data/topology.jl assert_radial
      (edges == N−1, contiguous bus.id == position, one root)
    - 07-RESEARCH.md "IEEE-123 fixture skeleton" code example, Pitfall 4 (non-contiguous labels /
      tie switches → relabel map + open the 4 normally-open ties so edges == N−1), Open Q1 (root =
      the terminal with no parent, thesis 150/substation), Assumption A3 (head-branch 3.8 MVA on
      the thesis S_base for the head smax; r/x are already pu)
    - docs/references thesis App. E p.170 "Datos del alimentador de 123 nodos" (the per-unit r/x
      terminal-i→j table — the source data)
  </read_first>
  <behavior>
    - ieee123_modified() returns a Feeder without throwing (radial + magnitudes validated in the
      constructor).
    - length(branches) == length(buses) − 1 (radial tree; the 4 normally-open tie switches are
      EXCLUDED).
    - Every bus.id equals its 1-based position; exactly one is_root; feeder.root indexes the
      substation/frontier terminal.
    - Voltage bounds are 0.9/1.1 (thesis Case B); every r,x < 5.0 pu and every smax ∈ (0, 100) pu.
    - The transit (zero-injection / non-load) bus count is a positive number (~37) — asserted so
      the fixture genuinely exercises the transit path 07-03 relaxes.
  </behavior>
  <action>
    Implement `ieee123_modified()` (and, if the population helper expects it, an alias
    `build_ieee123`) in src/data/ieee123.jl, mirroring the ieee13.jl structure. Transcribe the
    thesis App. E per-unit R/X branch table into a `raw = [(term_i, term_j, r_pu, x_pu, smax_pu), …]`
    literal (data is ALREADY per-unit — no SI→pu step for r/x). Build a documented relabeling map
    `Dict(thesis_terminal => 1..N)` and record it as a table in the fixture header (as ieee13.jl
    documents its k→k+1 shift), so `bus.id == position` holds; pick root = the terminal with no
    parent (thesis substation/frontier, per Open Q1). Include ONLY the radial branch set (open the
    4 normally-open tie switches) so `edges == N−1` (Pitfall 4). Use per-node voltage bounds
    vmin=0.9, vmax=1.1 (thesis Case B). Set the head-branch smax by converting the thesis head
    apparent-power limit once via `to_pu_power` on the thesis S_base (A3); interior branches use the
    `SMAX_NO_LIMIT` sentinel (as ieee13). Assemble sparse incidence via `SparseArrays` for the
    123-node topology (CLAUDE.md perf). Return `Feeder(buses, branches, root)` so `assert_radial` +
    `assert_magnitudes` validate by construction; add `export ieee123_modified`. Do NOT populate
    aggregators here (that is Phase7Fixtures / the test layer — keep topology separate from
    population, RESEARCH Open Q2).
  </action>
  <acceptance_criteria>
    ieee123_modified() constructs a radial per-unit Feeder (edges == N−1, contiguous ids, one root)
    without throwing; the relabel map is documented in the header; head-branch smax and all r/x are
    in-band; SparseArrays used for incidence.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; f=ieee123_modified(); @assert length(f.branches)==length(f.buses)-1; @assert count(b->b.is_root,f.buses)==1; @assert all(i->f.buses[i].id==i, eachindex(f.buses)); println("N=",length(f.buses)," root=",f.root)'</automated>
  </verify>
  <done>
    ieee123_modified() constructs a radial per-unit Feeder (edges == N−1, contiguous ids, one root)
    without throwing; the relabel map is documented in the header; head-branch smax and all r/x are
    in-band; SparseArrays used for incidence.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Green the IEEE-123 fixture-construction @testitems</name>
  <read_first>
    - test/test_ieee123.jl (the RED items authored in 07-01; fill/confirm their assertions)
    - test/test_feeder.jl + test/test_topology.jl (the existing radial/magnitude assertion style to
      mirror for the new fixture)
    - src/data/ieee123.jl (the fixture + its documented relabel map, from Task 1)
  </read_first>
  <behavior>
    - "ieee123 fixture radial": edges == N−1, one root, contiguous ids — all pass.
    - "ieee123 magnitudes": voltage bounds 0.9/1.1, all r,x in-band, head smax in-band.
    - "ieee123 relabel/root": feeder.root is the intended substation terminal (per the documented
      map); a spot-check of two known thesis terminals maps to the expected 1-based index.
    - "ieee123 transit count": the number of non-root, non-load buses is > 0 (~37) — pins that the
      fixture exercises the transit path.
  </behavior>
  <action>
    Fill the RED "ieee123" fixture-construction @testitems in test/test_ieee123.jl (authored as
    guarded stubs in 07-01) with concrete assertions: the feeder constructs without throwing; the
    edge-count / single-root / contiguous-id invariants; the magnitude bands; the documented
    root/relabel spot-checks; and the transit-bus count (> 0). These items must NOT be edited later
    to pass — they encode the fixture contract. Keep item names carrying the "ieee123" substring.
    The ADMM convergence items in test_ieee123_admm.jl stay RED until 07-05 (out of scope here).
  </action>
  <acceptance_criteria>
    All IEEE-123 fixture-construction @testitems (radial, magnitudes, relabel/root, transit count)
    are GREEN; the IEEE-123 ADMM convergence items remain RED (owned by 07-05); no Phase 1–6 test
    regresses.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("ieee123", ti.name) && !occursin("crossval", ti.name))'</automated>
  </verify>
  <done>
    All IEEE-123 fixture-construction @testitems (radial, magnitudes, relabel/root, transit count)
    are GREEN; the IEEE-123 ADMM convergence items remain RED (owned by 07-05); no Phase 1–6 test
    regresses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| thesis App. E data → fixture | hand-transcribed R/X + topology (error-prone input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | IEEE-123 not radial / non-contiguous labels / tie switches left in | mitigate | Feeder inner constructor runs assert_radial (edges == N−1, contiguous ids, one root) — fail-loud at construction; ties opened; relabel map documented + spot-checked (Pitfall 4) |
| T-07-04 | Tampering | Out-of-band magnitude (wrong S_base on head smax, r/x mis-scaled) | mitigate | assert_magnitudes enforces the pu bands at construction; head smax converted once via to_pu_power on the thesis S_base; r/x given directly in pu (A3) |
| T-07-05 | Information disclosure | Silent transcription error (wrong branch) yielding a plausible-but-wrong feeder | accept | Cross-validation against the centralized SOCP at 07-05 is the load-bearing net; a gross error trips assert_radial/assert_magnitudes; transcription risk flagged MEDIUM in RESEARCH |
</threat_model>

<verification>
- `ieee123_modified()` constructs without throwing; edges == N−1; one root; contiguous ids.
- Magnitude bands and voltage bounds (0.9/1.1) hold; transit-bus count > 0.
- Fixture-construction @testitems green; convergence items still RED (07-05).
</verification>

<success_criteria>
- Radial, per-unit, JuMP-free IEEE-123 Feeder validated by construction, documented relabel map.
- SparseArrays used for the 123-node incidence.
- Fixture-construction tests green; no regression.
</success_criteria>

<output>
Create `.planning/phases/07-admm-convergence-scale/07-02-SUMMARY.md` when done.
</output>
