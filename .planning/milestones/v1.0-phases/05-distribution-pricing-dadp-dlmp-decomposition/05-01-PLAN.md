---
phase: 05-distribution-pricing-dadp-dlmp-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/powerflow/ConvexBranchFlow.jl
  - src/models/welfare_solve.jl
  - src/TSODSO.jl
  - src/pricing/dlmp.jl
  - src/pricing/fit.jl
  - src/pricing/checks.jl
  - src/pricing/welfare.jl
  - test/test_dlmp.jl
  - test/test_fit.jl
  - test/test_economic_direction.jl
  - test/test_pricing_welfare.jl
autonomous: true
requirements: [PRICE-02, PRICE-03]
must_haves:
  truths:
    - "The SOCP branch-flow constraint duals needed for the DLMP split (voltage-drop 3.33, copy-drop 3.43, rotated cone 3.39, apparent-power limit 3.36) are recoverable by name from a solved ctx"
    - "Every solved welfare ctx carries the per-aggregator net active injection and utility needed for the surplus split"
    - "The four new src/pricing files are on the include graph and their RED @testitems are discoverable (name substrings dlmp/fit/econ/welfare/surplus)"
    - "The additive changes do not regress the existing green Phase-4 suite or the PF-04 exactness gate (IEEE-13 golden unchanged)"
  artifacts:
    - path: "src/powerflow/ConvexBranchFlow.jl"
      provides: "register_constraint! of :vdrop, :cpydrop, :cone, :smax so their duals feed decompose_dlmp"
      contains: "register_constraint!"
    - path: "src/models/welfare_solve.jl"
      provides: "ctx.meta[:agg_net] — per-aggregator (bus, net-injection vector, utility) stash"
      contains: ":agg_net"
    - path: "src/TSODSO.jl"
      provides: "include graph for src/pricing/{dlmp,fit,checks,welfare}.jl after models/oracle.jl"
      contains: "pricing/dlmp.jl"
    - path: "test/test_dlmp.jl"
      provides: "RED @testitem stubs for extract/decompose (name substring dlmp)"
      contains: "@testitem"
  key_links:
    - from: "src/powerflow/ConvexBranchFlow.jl"
      to: "ctx.constraints[:smax]"
      via: "register_constraint! of the apparent-power SOC container"
      pattern: "register_constraint!\\(ctx, :smax"
    - from: "src/models/welfare_solve.jl"
      to: "ctx.meta[:agg_net]"
      via: "capture contribute!(agg,...) return + net = p_inject - Pdc"
      pattern: ":agg_net"
---

<objective>
Lay the additive Phase-4 foundation Phase-5 pricing consumes, wire the new `src/pricing/`
include graph, and stand up the RED test harness — WITHOUT regressing the Phase-4 baseline
or the PF-04 exactness gate. This is the ONLY plan that edits shared Phase-4 source
(`ConvexBranchFlow.jl`, `welfare_solve.jl`, `TSODSO.jl`); every downstream pricing plan
touches only its own disjoint `src/pricing/*.jl` + test file.

Two substantive additive changes:
1. Register the branch-flow constraint handles (`:vdrop`, `:cpydrop`, `:cone`, `:smax`) in
   `ConvexBranchFlow.contribute!` so their duals are recoverable for the four-way DLMP
   decomposition (PRICE-02). Currently these constraints are built as LOCAL/anonymous
   `@constraint`s and their duals are unreachable.
2. Stash per-aggregator net active injection + utility under `ctx.meta[:agg_net]` in
   `solve_welfare` so the welfare accounting (PRICE-03) can split social = prosumer + DSO
   surplus. Currently only device vars (`:agg_device_vars`) and the TOTAL objective are
   stashed — the per-aggregator net injection `p_agⱼ[t]` needed for the price-transfer term
   is not recoverable.

Purpose: Make the Phase-4 solve output price-decomposable and surplus-splittable, additively,
with the sum-to-nodal-price and surplus-identity assertions as the correctness nets.
Output: Registered duals, the `:agg_net` stash, the `src/pricing/` include wiring, four RED
`@testitem` files, and a green (unchanged) Phase-4 suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the codebase; use directly, no exploration needed. -->

ModelContext (src/core/ModelContext.jl):
  register_constraint!(ctx::ModelContext, name::Symbol, cref) -> cref   # stores under ctx.constraints[name]
  ctx.constraints::Dict{Symbol,Any}   ctx.residuals::Dict{Symbol,Any}   ctx.meta::Dict{Symbol,Any}

ConvexBranchFlow.contribute! (src/powerflow/ConvexBranchFlow.jl) currently builds, unregistered:
  cone[b,t]   — @constraint [0.5*l[b,t], v[B[b].from,t], P[b,t], Q[b,t]] in RotatedSecondOrderCone()  (3.39)
  vdrop[b,t]  — @constraint v[to]==v[from]-2(rP+xQ)+(r²+x²)l                                          (3.33)
  cpydrop[b,t]— @constraint v̂[to]==v̂[from]-2{r(P+rl)+x(Q+xl)}                                        (3.43)
  (anonymous loop) — @constraint [br.smax,P[b,t],Q[b,t]] in SecondOrderCone() where br.smax<_SMAX_NO_LIMIT (3.36)
  stashes ctx.meta[:pf_vars] = (; v, v̂, P, Q, l)

solve_welfare (src/models/welfare_solve.jl):
  for agg in aggregators: contribute!(agg, ctx; T=T)     # currently ignores the return
  register_constraint!(ctx, :balance_p, balance_p)       # bus×time; dual = DADP λ_j
  ctx.meta[:p_import], ctx.meta[:objective] (Σ U_agj QuadExpr), ctx.meta[:agg_device_vars]

Aggregator.contribute! (src/devices/Aggregator.jl) RETURNS:
  (; vars=device_vars, p_inject::Vector{AffExpr}, utility::QuadExpr)
  and writes net active (p_inject[t] - agg.Pdc[t]) into :Rp at agg.bus.  # agg.Pdc is the inelastic demand
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the branch-flow constraint duals in ConvexBranchFlow.contribute!</name>
  <files>src/powerflow/ConvexBranchFlow.jl</files>
  <read_first>
    - src/powerflow/ConvexBranchFlow.jl (the `cone`, `vdrop`, `cpydrop`, and anonymous apparent-power `@constraint`s in `contribute!`, and the `ctx.meta[:pf_vars]` stash)
    - src/core/ModelContext.jl (`register_constraint!` signature and `ctx.constraints`)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Pattern 2 "REQUIRED additive Phase-4 change" + Pitfall 2)
  </read_first>
  <action>
    In `contribute!(::ConvexBranchFlow, ctx, feeder; T)`, make the four branch-flow constraints'
    duals recoverable by registering their handles via `register_constraint!(ctx, name, cref)`
    AFTER each `@constraint` block, WITHOUT changing any constraint math:
    (a) register the rotated cone as `:cone` (3.39) — its dual feeds the loss/voltage split;
    (b) register the true voltage drop as `:vdrop` (3.33) — dual β feeds loss+voltage;
    (c) register the exactness copy-drop as `:cpydrop` (3.43) — dual feeds the exactness-copy
        contribution to the split (Pitfall 2: forgetting this leaves a residual);
    (d) convert the anonymous apparent-power loop `@constraint` into a SINGLE named JuMP
        container built with the filter guard `br.smax < _SMAX_NO_LIMIT` over `[b,t]`, then
        register it as `:smax` (3.36) — its dual ν is the congestion component. Use a filtered
        `@constraint(m, smax[...] , [br.smax, P[b,t], Q[b,t]] in SecondOrderCone())`-style
        comprehension so only genuinely-limited branches (the head branch) get a cone, exactly
        as the current loop does; the resulting sparse container's entries have recoverable duals.
    Keep `ctx.meta[:pf_vars]` and every existing balance/residual write BYTE-IDENTICAL in effect.
    Cite the thesis equation number (3.39/3.33/3.43/3.36) in a comment beside each registration.
    Do NOT register anything for DC/LinDistFlow (this method is ConvexBranchFlow-only).
  </action>
  <acceptance_criteria>
    - `src/powerflow/ConvexBranchFlow.jl` contains `register_constraint!(ctx, :vdrop, ...)`,
      `register_constraint!(ctx, :cpydrop, ...)`, `register_constraint!(ctx, :cone, ...)`, and
      `register_constraint!(ctx, :smax, ...)` (grep for each symbol).
    - A `@testitem` with name substring `socp` builds a ConvexBranchFlow `contribute!` on the
      modified IEEE-13 feeder and asserts `haskey(ctx.constraints, k)` for `k in (:vdrop,:cpydrop,:cone,:smax)`.
    - The PF-04 exactness gate still passes on the IEEE-13 ground solve and the pinned golden
      (`v9[16]`, welfare, DADP) is UNCHANGED — verified by the existing `ieee13`/`socp`/`exactness` items staying green.
    - Full suite green with zero regressions (no previously-green item turns red).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <done>The four branch-flow constraint duals are recoverable via ctx.constraints; the IEEE-13 golden and exactness gate are unchanged; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Stash per-aggregator net injection + utility for welfare accounting</name>
  <files>src/models/welfare_solve.jl</files>
  <read_first>
    - src/models/welfare_solve.jl (the `for agg in aggregators: contribute!(agg, ctx; T=T)` loop and the meta stashes)
    - src/devices/Aggregator.jl (`contribute!` return `(; vars, p_inject, utility)` and the `p_inject[t] - agg.Pdc[t]` :Rp write)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Pattern 3 surplus definitions: prosumer = Σ U_agj − Σ λ_j·p_agj)
  </read_first>
  <action>
    In `solve_welfare`, capture the return of each `contribute!(agg, ctx; T)` call (currently
    discarded) and build a per-aggregator record: `net = [p_inject[t] - agg.Pdc[t] for t in 1:T]`
    (the aggregator's NET active injection at its bus, exactly what is written to `:Rp`) and the
    returned `utility` QuadExpr. Accumulate these into `ctx.meta[:agg_net]` as a
    `Vector{NamedTuple}` of `(; bus = agg.bus, net, utility)` in aggregator order. This is a
    PURELY ADDITIVE stash — do not alter the residual writes, the objective, the balance
    registration, the exactness gate, the battery check, or the returned tuple. Comment the
    surplus role (per D-less thesis Pattern 3: `p_agⱼ[t]` is the price-transfer term Σ_t λ_j[t]·net[t],
    thesis eqs. 3.46/3.47) and cite that `Σ_j U_agj = value(ctx.meta[:objective])` remains the
    total-utility source. The stash makes the prosumer/DSO split recoverable post-solve.
  </action>
  <acceptance_criteria>
    - `src/models/welfare_solve.jl` populates `ctx.meta[:agg_net]` (grep `:agg_net`) with one
      entry per aggregator, each a NamedTuple carrying `bus`, `net` (length-T), and `utility`.
    - A `@testitem` (substring `welfare` or `surplus`) solves a small multi-aggregator LinDistFlow
      welfare problem and asserts `length(ctx.meta[:agg_net]) == length(aggregators)` and that
      each `.net` has length `T`.
    - The returned `(ctx, objective, dadp)` tuple and all existing `solve_welfare` behavior are
      byte-identical in effect (existing `welfare`/`oracle`/`ieee13` items stay green).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <done>ctx.meta[:agg_net] carries per-aggregator (bus, net, utility); no change to any existing solve_welfare output; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the src/pricing include graph + RED @testitem harness</name>
  <files>src/TSODSO.jl, src/pricing/dlmp.jl, src/pricing/fit.jl, src/pricing/checks.jl, src/pricing/welfare.jl, test/test_dlmp.jl, test/test_fit.jl, test/test_economic_direction.jl, test/test_pricing_welfare.jl</files>
  <read_first>
    - src/TSODSO.jl (the include graph; add the pricing block AFTER models/oracle.jl)
    - test/test_convex_branch_flow.jl (the RED-stub `@testitem` + `isdefined`-guard convention)
    - test/test_oracle.jl (inline feeder/aggregator construction convention)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Recommended Project Structure + Validation Wave 0 gaps)
  </read_first>
  <action>
    Create the four `src/pricing/` files as COMMENT-ONLY stubs, each with a SEAM/OWNER header
    naming its downstream plan and the functions it will export (`dlmp.jl`: extract_dlmp,
    decompose_dlmp / plan 05-02; `fit.jl`: fit_baseline / plan 05-03; `checks.jl`:
    economic_direction_checks / plan 05-04; `welfare.jl`: welfare_accounting / plan 05-05).
    Add an include block to `src/TSODSO.jl` AFTER `include("models/oracle.jl")` in dependency
    order (dlmp, fit, checks, welfare), each with an owning-plan comment. Follow the existing
    file-header convention exactly; TSODSO.jl exports nothing itself.
    Then create four RED `@testitem` files following the `isdefined`-guarded pattern in
    test_convex_branch_flow.jl so the runner never crashes while stubs are empty:
    - test/test_dlmp.jl — item names contain "dlmp" (extract sign on a 2-bus; decompose sum-to-price).
    - test/test_fit.jl — item names contain "fit" (FIT baseline solve + ratio).
    - test/test_economic_direction.jl — item names contain "econ" and/or "direction" (PV-glut / congestion).
    - test/test_pricing_welfare.jl — item names contain "welfare" and "surplus" (surplus identity),
      DISTINCT file from the Phase-3 test_welfare_solve.jl.
    Each stub item asserts `isdefined(TSODSO, :function_name)` (RED now) and guards any behavioral
    assert behind that `isdefined` so it goes live automatically when the owning plan lands.
  </action>
  <acceptance_criteria>
    - `src/pricing/dlmp.jl`, `fit.jl`, `checks.jl`, `welfare.jl` exist; `src/TSODSO.jl` includes
      all four after `models/oracle.jl` (grep `pricing/dlmp.jl` in TSODSO.jl).
    - `using TSODSO` loads with no error (the empty stubs are valid Julia).
    - `@run_package_tests filter=ti->occursin("dlmp", ti.name)` discovers ≥1 item; likewise for
      `fit`, `econ`, and `welfare` substrings (items exist, currently RED on the missing symbol).
    - No previously-green item regresses (the new REDs are the only failures, and they fail on a
      clean `isdefined` assertion, not a runner crash).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TSODSO' && julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("dlmp", ti.name) || occursin("fit", ti.name) || occursin("econ", ti.name) || occursin("welfare", ti.name)'</automated>
  </verify>
  <done>Four pricing stubs on the include graph; four RED @testitem files discoverable by their name substrings; package loads clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solved SOCP ctx → pricing post-processing | Duals crossing here are only physically meaningful if the PF-04 exactness gate passed; a registration change must not silently weaken that gate |
| additive Phase-4 edits → existing 4-phase baseline | Any non-additive drift silently corrupts already-validated prices/voltages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ConvexBranchFlow constraint restructure (:smax loop → container) | mitigate | Keep math identical; assert IEEE-13 golden + PF-04 exactness gate unchanged via full-suite regression |
| T-05-06 | Information disclosure (wrong data to next phase) | ctx.meta[:agg_net] stash | mitigate | net := p_inject − Pdc exactly matches the :Rp write; assert length == #aggregators and per-entry length == T |
| T-05-07 | Denial (runner crash on empty stubs) | RED @testitem harness | mitigate | isdefined-guarded stubs (test_convex_branch_flow.jl pattern) — RED via assertion, never a load crash |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (pure post-processing over the pinned Phase-1..4 stack; RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- Full suite green with ZERO regressions vs the pre-plan baseline: `julia --project=. -e 'import Pkg; Pkg.test()'`.
- IEEE-13 pinned golden (`v9[16]`, welfare, DADP) and `socp_maxgap` unchanged (existing `ieee13`/`exactness` items green).
- `ctx.constraints` exposes `:vdrop, :cpydrop, :cone, :smax` after a ConvexBranchFlow solve.
- `ctx.meta[:agg_net]` populated per aggregator after a welfare solve.
- Four pricing includes present; four RED test files discoverable by substring.
</verification>

<success_criteria>
The additive Phase-4 seam (registered branch-flow duals + per-aggregator net-injection stash) is
in place and the include graph + RED harness are wired, WITHOUT regressing the Phase-4 baseline or
the PF-04 exactness gate. Downstream Wave-2 plans can now read the duals and the surplus inputs.
</success_criteria>

<output>
Create `.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-01-SUMMARY.md` when done.
</output>
