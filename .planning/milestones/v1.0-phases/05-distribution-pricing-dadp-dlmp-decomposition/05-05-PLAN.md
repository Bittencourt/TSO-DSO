---
phase: 05-distribution-pricing-dadp-dlmp-decomposition
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/pricing/welfare.jl
  - test/test_pricing_welfare.jl
autonomous: true
requirements: [PRICE-03]
must_haves:
  truths:
    - "A researcher can split the welfare into social / DSO / prosumer surplus from a solved DADP ctx"
    - "The surplus split obeys the exact accounting identity: social == prosumer + DSO == the GLB-CVX objective value (the price-transfer term cancels), asserted within a relative tolerance"
    - "With the FIT baseline, the +25%-social-welfare headline is reported as a COMPUTED ratio (social_DADP / social_FIT), pinned as a golden, with the thesis ~1.25 as a NON-FAILING cross-check"
    - "A wrong surplus sign or dropped term is caught by the identity assertion, not shipped"
  artifacts:
    - path: "src/pricing/welfare.jl"
      provides: "welfare_accounting(ctx; baseline=nothing) -> (; social, dso, prosumer[, ratio]) with the surplus identity assertion"
      exports: ["welfare_accounting"]
      contains: "function welfare_accounting"
    - path: "test/test_pricing_welfare.jl"
      provides: "@testitems for the surplus identity and the computed +25% ratio + non-failing thesis cross-check (name substrings welfare/surplus)"
      contains: "@testitem"
  key_links:
    - from: "src/pricing/welfare.jl"
      to: "extract_dlmp(ctx) + ctx.meta[:agg_net] + objective_value(ctx.model) + ctx.meta[:p_import]"
      via: "prosumer = ΣU_agj − Σ_j Σ_t λ_j·net_j ; dso = transfer − Σ_t λ₀·p_import"
      pattern: "extract_dlmp"
    - from: "src/pricing/welfare.jl"
      to: "fit_baseline(...).social_fit"
      via: "ratio = social_DADP / social_FIT (the +25% headline)"
      pattern: "social_fit|social_FIT|\\.social"
---

<objective>
Deliver the welfare accounting in `src/pricing/welfare.jl` (PRICE-03, the assembly): split the
Phase-4 welfare solve into social / DSO / prosumer surplus, assert the exact accounting identity
`social == prosumer + DSO == the GLB-CVX objective`, and — against the FIT baseline (05-03) —
report the headline +25%-social-welfare number as a COMPUTED ratio pinned as a golden with the
thesis ~1.25 as a non-failing cross-check.

This is the Wave-3 assembly: it consumes `extract_dlmp` (05-02) for the per-node price, the
per-aggregator net-injection stash `ctx.meta[:agg_net]` (05-01) for the price-transfer term, and
`fit_baseline` (05-03) for the ratio denominator. The surplus identity is the load-bearing
correctness gate — the price-transfer `Σ_j λ_j·p_agj` cancels between prosumer and DSO, so a sign
error or dropped term breaks the identity loudly.

Purpose: The headline research result (+25% social welfare) reproduced as a trustworthy computed
ratio, with the surplus split validated by an exact identity. Output: `welfare_accounting` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md
@CLAUDE.md

<interfaces>
extract_dlmp(ctx) -> Matrix{Float64}   # (N,T) DADP, from src/pricing/dlmp.jl (05-02)
fit_baseline(feeder, pf, prosumers; T, λ₀, seed) -> (; ctx, social_fit)   # src/pricing/fit.jl (05-03)

On a solved DADP welfare ctx:
  objective_value(ctx.model)     # GLB-CVX social welfare = ΣU_agj − Σ_t λ₀[t]·p_import[t] (thesis 3.38)
  ctx.meta[:objective]           # ΣU_agj QuadExpr; value(...) = total prosumer utility
  ctx.meta[:agg_net]             # Vector of (; bus, net::Vector, utility) per aggregator (from 05-01)
  ctx.meta[:p_import]            # p_import[t] JuMP var; value.(...) = frontier import
  ctx.meta[:feeder], ctx.meta[:T]

Thesis anchors (RESEARCH Pattern 3, page 98): prosumer = value of AGR-OPT (3.46);
  DSO = negative value of DSO-OPT (3.47); social = their sum = GLB-CVX objective (3.38);
  +25% = social_DADP/social_FIT (thesis $1819/$1457 ≈ 1.25). STATE Phase-4 caveat: absolute
  welfare is figure-bound; pin the RATIO, cross-check ~1.25 NON-FAILING.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: welfare_accounting + the surplus identity assertion (3.38/3.46/3.47)</name>
  <files>src/pricing/welfare.jl, test/test_pricing_welfare.jl</files>
  <read_first>
    - src/pricing/welfare.jl (the stub header from plan 05-01)
    - src/models/welfare_solve.jl (objective assembly `ΣU_agj − λ₀ᵀp_import`; ctx.meta[:objective], :p_import, :agg_net)
    - src/pricing/dlmp.jl (extract_dlmp signature from 05-02)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Pattern 3 exact definitions + surplus-identity Code Example, Pitfall 4/5, Assumption A3, Open Q2)
  </read_first>
  <behavior>
    - welfare_accounting(ctx) returns (; social, dso, prosumer).
    - social == objective_value(ctx.model) (the GLB-CVX optimum, thesis 3.38).
    - prosumer == value(ctx.meta[:objective]) − Σ_j Σ_t λ_j[t]·net_j[t]  (AGR-OPT value, 3.46).
    - dso == Σ_j Σ_t λ_j[t]·net_j[t] − Σ_t λ₀[t]·value(p_import[t])  (−DSO-OPT value, 3.47).
    - The identity social ≈ prosumer + dso ≈ objective_value holds within a relative tolerance
      (the transfer term Σ_j λ_j·net_j cancels) — asserted; a sign error breaks it.
  </behavior>
  <action>
    Implement `welfare_accounting(ctx; λ₀, baseline=nothing) -> (; social, dso, prosumer[, ratio])`.
    Compute, reading the stashed primals/duals (NO re-solve):
    - `social := objective_value(ctx.model)` (thesis 3.38);
    - the per-node price `λ = extract_dlmp(ctx)` (05-02); the transfer
      `transfer := Σ over ctx.meta[:agg_net] entries of Σ_t λ[entry.bus, t]·entry.net[t]`
      (thesis price-transfer Σ_j λ_j·p_agj);
    - `prosumer := value(ctx.meta[:objective]) − transfer` (AGR-OPT value, 3.46);
    - `dso := transfer − Σ_t λ₀[t]·value(ctx.meta[:p_import][t])` (−DSO-OPT value, 3.47).
    ASSERT the identity `isapprox(social, prosumer + dso; rtol)` AND `isapprox(social,
    objective_value(ctx.model); rtol)` (mirror the relative-tolerance style of assert_socp_exact!),
    throwing an `error` (never `@assert`) that reports the mismatch magnitude — this catches a sign
    error or dropped term (Open Q2: if it fails by a loss-sized amount, add the documented loss
    term and re-derive; verify on the lossless 2-bus first, then IEEE-13). Add a magnitude-sanity
    guard on all three surpluses (finite, within the per-unit band — Pitfall 5). Cite 3.38/3.46/3.47.
    Tests (@testitems, names contain "welfare" and "surplus"), setup=[Phase4Fixtures], in a file
    DISTINCT from the Phase-3 test_welfare_solve.jl:
    - a lossless 2-bus: identity holds exactly; surpluses finite and correctly signed.
    - the IEEE-13 ground solve: `social ≈ prosumer + dso ≈ objective_value` within rtol.
  </action>
  <acceptance_criteria>
    - `src/pricing/welfare.jl` defines and exports `welfare_accounting`; `grep "function welfare_accounting"` succeeds.
    - @testitem `occursin("welfare", ti.name) || occursin("surplus", ti.name)`: `social ≈ prosumer + dso ≈ objective_value(ctx.model)` within rtol on the 2-bus AND IEEE-13.
    - The three surpluses are finite and magnitude-sane; prosumer and dso have the thesis-expected signs.
    - A deliberately sign-flipped transfer (in a guarded check) makes the identity assertion throw — proving it is load-bearing.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("welfare", ti.name) || occursin("surplus", ti.name)'</automated>
  </verify>
  <done>welfare_accounting splits social/DSO/prosumer surplus and the exact identity social == prosumer + DSO == GLB-CVX objective holds (relative tolerance) on the 2-bus and IEEE-13.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIT ratio — the +25% headline as a computed golden + non-failing thesis cross-check</name>
  <files>src/pricing/welfare.jl, test/test_pricing_welfare.jl</files>
  <read_first>
    - src/pricing/welfare.jl (Task 1 welfare_accounting)
    - src/pricing/fit.jl (fit_baseline signature from 05-03)
    - .planning/phases/04-convex-branch-flow-correctness-milestone/04-06-SUMMARY.md (the computed-golden + non-failing thesis cross-check pattern: @info gap + @test broken + generous physical band)
    - .planning/STATE.md (the Phase-4 welfare-gap caveat: absolute welfare figure-bound; ratio is the robust anchor)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Pattern 3 ratio, Pitfall 4)
  </read_first>
  <behavior>
    - welfare_accounting(ctx; λ₀, baseline) with a solved FIT baseline additionally returns
      `ratio = social_DADP / social_FIT` (the +25% headline; thesis ≈ 1.25).
    - The computed ratio is pinned as a regression golden (the primary anchor).
    - The thesis ~1.25 is a NON-FAILING cross-check: the gap is @info-logged, the assertion is a
      broken/generous-band @test, NOT a hard exact match (STATE Phase-4 figure-bound caveat).
  </behavior>
  <action>
    Extend `welfare_accounting` so that when `baseline` is a solved FIT context (from
    `fit_baseline`, 05-03), it computes `ratio = social / baseline.social_fit` (thesis
    social_DADP/social_FIT ≈ 1.25) and returns it in the NamedTuple. In the test, PIN the computed
    ratio as a golden (a numeric literal with a tight rtol — the primary reproducibility anchor,
    matching the Phase-4 computed-golden pattern) and add the thesis ~1.25 as a NON-FAILING
    cross-check exactly like 04-06: `@info` the absolute gap `|ratio − 1.25|`, a `@test ...
    broken=(gap ≥ tol)` so it never fails the suite, and a generous physical-band ceiling assertion
    (e.g. `0.8 < ratio < 2.0`) so a wildly-wrong ratio (a real bug, unlike the figure-bound absolute
    gap) still fires. Document the German-FIT prices used and the figure-bound caveat in the test
    and in a code comment (Pitfall 4). Tests (@testitems, names contain "welfare"), setup=[Phase4Fixtures]:
    - build the FIT baseline for the IEEE-13 scenario, solve the DADP welfare, compute the ratio,
      assert it matches the pinned golden (tight rtol) and lands in the generous physical band;
      @info the thesis-1.25 gap as the non-failing cross-check.
  </action>
  <acceptance_criteria>
    - `welfare_accounting(...; baseline)` returns a `ratio` field; `grep` shows `ratio` computed from `social` and the FIT baseline's `social_fit`.
    - @testitem `occursin("welfare", ti.name)`: the computed ratio matches a PINNED golden (tight rtol) and lies in the generous physical band.
    - The thesis-1.25 cross-check is NON-FAILING: implemented via `@info` gap + `@test ... broken` + band ceiling (grep for `broken` and `@info`), NOT a hard `≈ 1.25` assertion.
    - A code comment + the SUMMARY document the German-FIT prices and the figure-bound absolute-welfare caveat.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("welfare", ti.name) || occursin("surplus", ti.name)'</automated>
  </verify>
  <done>The +25% headline is reported as a computed social_DADP/social_FIT ratio pinned as a golden, with the thesis ~1.25 as a non-failing cross-check (@info gap + broken test + physical band).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solved primals/duals → surplus split | A sign error or dropped term in prosumer/DSO surplus is internally invisible without the cancellation identity |
| figure-bound thesis absolute → headline claim | Matching the thesis absolute welfare ($1819) is impossible (figure-bound); only the ratio is a trustworthy claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering (wrong surplus sign) | welfare_accounting prosumer/DSO split | mitigate | Assert social == prosumer + DSO == objective_value (transfer cancels) within rtol; throw with the mismatch magnitude; a sign-flipped guarded check proves the assertion is load-bearing |
| T-05-04 | Tampering (inflated headline) | FIT ratio | mitigate | Pin the COMPUTED ratio as the golden; thesis ~1.25 is a NON-FAILING cross-check (@info + broken @test + physical band), never a hard match; document German-FIT prices + figure-bound caveat |
| T-05-05 | Spoofing (unit mismatch) | surplus / ratio magnitudes | mitigate | Magnitude-sanity guards on all surpluses; the generous physical band on the ratio catches a clean power-of-ten error (Pitfall 5) |
| T-05-01 | Elevation (accounting an ungated solve) | reading ctx from an inexact SOCP | mitigate | welfare_accounting consumes only a solve_welfare ctx (PF-04 gate ran) + extract_dlmp (which refuses ungated ctx) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `@run_package_tests filter=ti->occursin("welfare", ti.name) || occursin("surplus", ti.name)` green.
- Identity `social ≈ prosumer + dso ≈ objective_value` holds (rtol) on the 2-bus and IEEE-13.
- Computed FIT ratio matches the pinned golden and lands in the physical band; thesis ~1.25 is a non-failing cross-check.
- Full suite green: `julia --project=. -e 'import Pkg; Pkg.test()'` (wave-merge gate).
</verification>

<success_criteria>
The welfare accounting splits social / DSO / prosumer surplus with the exact identity assertion and
reproduces the +25%-social-welfare headline as a computed FIT ratio (golden + non-failing thesis
cross-check) — Success Criterion #3, completing the phase.
</success_criteria>

<output>
Create `.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-05-SUMMARY.md` when done.
</output>
