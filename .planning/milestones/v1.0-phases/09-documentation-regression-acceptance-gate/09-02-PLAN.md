---
phase: 09-documentation-regression-acceptance-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/literate/prosumer_welfare.jl
  - docs/literate/pricing_dlmp.jl
autonomous: true
requirements: [EXP-03]

must_haves:
  truths:
    - "A researcher opening the prosumer-welfare literate source sees the device math (thermostatic 3.2-3.3, deferrable 3.4-3.5, PV+battery 3.6-3.9, App. C utilities) and the aggregator roll-up (3.21-3.23) beside the real solve_welfare/GLB-CVX (3.38) code."
    - "A researcher opening the pricing-DLMP literate source sees the DADP dual (3.31) and the four-way decomposition beside the real extract_dlmp/decompose_dlmp/welfare_accounting code, with every component computed from a genuine solved point."
  artifacts:
    - path: "docs/literate/prosumer_welfare.jl"
      provides: "Rung 2a prosumer devices + GLB-CVX literate page"
      min_lines: 45
    - path: "docs/literate/pricing_dlmp.jl"
      provides: "Rung 4 DADP/DLMP decomposition + welfare-accounting literate page"
      min_lines: 45
  key_links:
    - from: "docs/literate/prosumer_welfare.jl"
      to: "src/models/welfare_solve.jl solve_welfare + src/devices/Aggregator.jl"
      via: "direct function call over Thermostatic/Deferrable/PVBattery + Aggregator"
      pattern: "solve_welfare\\("
    - from: "docs/literate/pricing_dlmp.jl"
      to: "src/pricing/dlmp.jl extract_dlmp/decompose_dlmp + src/pricing/welfare.jl welfare_accounting"
      via: "direct function calls on a solved ConvexBranchFlow ctx"
      pattern: "extract_dlmp\\(|decompose_dlmp\\("
---

<objective>
Author the remaining "middle" literate documentation pages (EXP-03): Rung 2a (the full
prosumer device library + aggregator roll-up + GLB-CVX centralized social-welfare solve)
and Rung 4 (DADP/DLMP extraction, four-way decomposition, welfare accounting). Same
math -> assumptions -> validation structure as `toy_dc.jl`, calling ONLY real `src/`
entrypoints.

Purpose: cover the two device/pricing rungs of the abstraction ladder that plan 09-01
does not, completing 4 of 5 new literate pages before docs/make.jl wiring (plan 09-04).
Output: `docs/literate/prosumer_welfare.jl`, `docs/literate/pricing_dlmp.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-CONTEXT.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-RESEARCH.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-PATTERNS.md

@docs/literate/toy_dc.jl
@src/devices/Thermostatic.jl
@src/devices/Deferrable.jl
@src/devices/PVBattery.jl
@src/devices/Aggregator.jl
@src/data/profiles.jl
@src/models/welfare_solve.jl
@src/powerflow/ConvexBranchFlow.jl
@src/pricing/dlmp.jl
@src/pricing/welfare.jl
@test/fixtures_phase4.jl
</context>

<interfaces>
Real entrypoints this plan calls (never reimplemented):

From `src/data/profiles.jl`:
```
generate_profiles(; seed::Integer, T::Int=24, ...) -> (; demand::Vector{Float64}, pv::Vector{Float64})
```

From `src/devices/Thermostatic.jl` (fields in constructor order, mirroring `test/fixtures_phase4.jl`):
```
Thermostatic(bus, α, β, Tmin, Tmax, Tin0, Pmin, Pmax, b, Tout::Vector)   # eqs 3.2-3.3, 3.11
```

From `src/devices/Deferrable.jl`:
```
Deferrable(bus, t_start, t_end, E, E_min, b)   # eqs 3.4-3.5, 3.12
```

From `src/devices/PVBattery.jl`:
```
PVBattery(bus, η, Δt, Pmax, Emin, Emax, soc0, λ_min, λ_med, λ_max, Ppv::Vector)  # eqs 3.6-3.9, 3.15-3.20 (App. C)
```
Requires the STRICT ordering `λ_min < λ_med < λ_max` (App. C no-binary guarantee).

From `src/devices/Aggregator.jl`:
```
Aggregator(bus::Integer, φ::Real, devices::AbstractVector{<:AbstractDevice}, Pdc::AbstractVector{<:Real})
```

From `src/models/welfare_solve.jl`:
```
solve_welfare(feeder, pf, aggregators; T=24, λ₀, allow_export=false) -> (ctx, objective, dadp)
```

From `src/pricing/dlmp.jl`:
```
extract_dlmp(ctx::ModelContext; bus=nothing, T=nothing) -> Matrix|Vector   # thesis 3.31, PF-04-gated
decompose_dlmp(ctx::ModelContext; bus=nothing, T=nothing, rtol=1e-5, atol=1e-7)
    -> (; energy, loss, congestion, voltage, total)
```

From `src/pricing/welfare.jl`:
```
welfare_accounting(ctx::ModelContext; T=ctx.meta[:T], λ₀=nothing, baseline=nothing) -> (; social, dso, prosumer[, ratio])
```
Both `extract_dlmp`/`decompose_dlmp`/`welfare_accounting` REQUIRE a ctx solved with
`ConvexBranchFlow()` and `allow_export = true` on this project's feeders (the registered
`:cone`/`:vdrop`/`:cpydrop`/`:smax` duals only exist on that formulation).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Prosumer devices + GLB-CVX literate page (rung 2a, thesis 3.2-3.23/3.38)</name>
  <files>docs/literate/prosumer_welfare.jl</files>
  <read_first>docs/literate/toy_dc.jl, src/devices/Thermostatic.jl, src/devices/Deferrable.jl, src/devices/PVBattery.jl, src/devices/Aggregator.jl, src/data/profiles.jl, src/models/welfare_solve.jl</read_first>
  <action>
  Create `docs/literate/prosumer_welfare.jl` in the `toy_dc.jl` shape. Title:
  "# # Rung 2a — Prosumer Devices, Aggregator Roll-Up \& GLB-CVX Social Welfare". Math
  block cites, in order: thesis 3.2-3.3 (thermostatic RC/ETP recursion + comfort band),
  3.4-3.5 (deferrable energy-window budget), 3.6-3.9 (PV+battery SOC dynamics + PV-limited
  charge), 3.15-3.20 (App. C battery no-binary utility parametrization — name the STRICT
  `λ_min < λ_med < λ_max` ordering as the load-bearing invariant), 3.21-3.23 (aggregator
  roll-up: summed utility, net active injection, power-factor reactive), and 3.38
  (GLB-CVX = Σ aggregator utility − wholesale purchase). Build a small 2-bus radial
  `Feeder` (same shape as prior pages), one seeded `generate_profiles(seed = 20260720, T = 4)`
  draw, one `Aggregator` at bus 2 holding a `Thermostatic` + `Deferrable` + `PVBattery`
  (mirror `test/fixtures_phase4.jl`'s `_house_aggregator` SHAPE — small T=4 horizon for a
  fast, self-contained doc solve; do NOT reuse the fixture's calibration constants, this
  page is illustrative, not a regression). Call
  `solve_welfare(feeder, ConvexBranchFlow(), [agg]; T = 4, λ₀ = fill(value, 4), allow_export = true)`.
  Display `objective`, `dadp`, and `ctx.meta[:agg_net]` length (proving the PRICE-03
  surplus stash the next page's welfare accounting consumes) as the validation step.
  </action>
  <acceptance_criteria>
  `docs/literate/prosumer_welfare.jl` exists, standalone-includable, cites "3.2", "3.6",
  "3.21", and "3.38" in math blocks, constructs `Thermostatic`/`Deferrable`/`PVBattery`
  instances and rolls them into one `Aggregator`, calls `solve_welfare(...)` (no hand-rolled
  device constraint), and displays `objective`/`dadp` as bare expressions.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'include("docs/literate/prosumer_welfare.jl")'</automated>
  </verify>
  <done>Page created; `include(...)` runs to completion with a finite objective and length-4 dadp.</done>
</task>

<task type="auto">
  <name>Task 2: DADP/DLMP decomposition + welfare-accounting literate page (rung 4, thesis 3.31/3.46-3.47)</name>
  <files>docs/literate/pricing_dlmp.jl</files>
  <read_first>docs/literate/toy_dc.jl, src/pricing/dlmp.jl, src/pricing/welfare.jl, test/test_pricing_welfare.jl</read_first>
  <action>
  Create `docs/literate/pricing_dlmp.jl` in the `toy_dc.jl` shape. Title:
  "# # Rung 4 — DADP/DLMP Decomposition \& Welfare Accounting". Math block cites thesis
  3.31 (the DADP as the dual of the nodal active balance), the four-way split derivation
  (energy = root MEM price, loss from the SOC/DistFlow dual 3.39, congestion from the
  apparent-power dual 3.36, voltage from the voltage-drop duals 3.33/3.43 — name each
  component's SOURCE dual per `src/pricing/dlmp.jl`'s header derivation), and 3.46/3.47
  (prosumer/DSO surplus split, welfare accounting). Build a small radial `Feeder` (2-3 bus,
  same construction shape as prior pages) with one or two `Aggregator`s (reuse the
  `Interruptible`-only construction from plan 09-01 Task 2, or a small device mix — keep it
  minimal for a fast doc build). Solve via
  `solve_welfare(feeder, ConvexBranchFlow(), aggs; T = ..., λ₀ = ..., allow_export = true)`.
  Call `extract_dlmp(ctx)` and `decompose_dlmp(ctx)` (display each component matrix's sum
  or a single bus/hour slice), then `welfare_accounting(ctx; T = ..., λ₀ = ...)` and display
  the returned `(; social, dso, prosumer)` NamedTuple — this IS the validation step:
  `decompose_dlmp` internally asserts the four components sum to the nodal price (throws
  otherwise), and `welfare_accounting` internally asserts `social == prosumer + dso`
  (throws otherwise), so reaching these rendered numbers at all certifies both identities.
  </action>
  <acceptance_criteria>
  `docs/literate/pricing_dlmp.jl` exists, standalone-includable, cites "3.31" in a math
  block, calls `extract_dlmp(...)`, `decompose_dlmp(...)`, and `welfare_accounting(...)` on
  a `ConvexBranchFlow` ctx (no hand-rolled dual extraction), and displays the
  `(; social, dso, prosumer)` NamedTuple as a bare expression.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'include("docs/literate/pricing_dlmp.jl")'</automated>
  </verify>
  <done>Page created; `include(...)` runs to completion; `decompose_dlmp` and `welfare_accounting` return without throwing (their internal sum-to-price / surplus-identity assertions passed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Literate page -> `src/` entrypoint | `@example`/`include`-executed blocks call real `solve_welfare`/`extract_dlmp`/`decompose_dlmp`/`welfare_accounting`; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering (silent math fork) | `docs/literate/prosumer_welfare.jl`, `docs/literate/pricing_dlmp.jl` | mitigate | Every page calls ONLY the real `src/` entrypoint; the internal sum-to-price / surplus-identity assertions inside `decompose_dlmp`/`welfare_accounting` throw on a dropped or mis-signed term, so a page reaching its displayed numbers is itself evidence no term was silently forked. |
</threat_model>

<verification>
Both pages `include(...)` cleanly (exit 0, no thrown error). `decompose_dlmp` and
`welfare_accounting`'s internal HARD assertions (sum-to-price, surplus identity) pass —
their absence of a thrown error IS part of the page's validation content.
</verification>

<success_criteria>
`docs/literate/prosumer_welfare.jl` and `docs/literate/pricing_dlmp.jl` exist, follow the
`toy_dc.jl` math -> assumptions -> validation structure, cite the correct thesis equation
numbers, and execute cleanly via plain `include(...)`.
</success_criteria>

<output>
Create `.planning/phases/09-documentation-regression-acceptance-gate/09-02-SUMMARY.md` when done
</output>
