---
phase: 04-convex-branch-flow-correctness-milestone
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/data/ieee13.jl
  - test/test_ieee13.jl
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "ieee13_modified() returns an immutable, radial-validated, per-unit Feeder with 11 buses and 10 branches (thesis Table 4.1, base 100 MVA / 13.2 kV)"
    - "The head branch (thesis 0→1, struct index 1→2) carries S_max=0.0686 pu; interior branches are effectively unconstrained but pass the magnitude tripwire (smax strictly < 100 pu)"
    - "Bus voltage bounds are 0.95/1.05 pu; the root is struct index 1 (thesis node 0)"
  artifacts:
    - path: "src/data/ieee13.jl"
      provides: "built-in modified IEEE-13 feeder fixture"
      contains: "ieee13_modified"
      min_lines: 25
  key_links:
    - from: "src/data/ieee13.jl"
      to: "Feeder constructor"
      via: "Feeder(buses, branches, 1) — runs assert_radial + assert_magnitudes"
      pattern: "Feeder\\("
---

<objective>
Ship the modified IEEE 13-node feeder as a built-in, immutable, JuMP-free `Feeder`
fixture (`ieee13_modified()`) from thesis Table 4.1 / Figure 4.1, per-unit on the
100 MVA / 13.2 kV base, radial-validated at construction.

Purpose: The centralized ground-truth network for the correctness milestone (DATA-03).
Output: `src/data/ieee13.jl` exporting `ieee13_modified`; the "ieee13" fixture
@testitems turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-convex-branch-flow-correctness-milestone/04-RESEARCH.md
@src/data/Feeder.jl
@src/units/PerUnit.jl

<interfaces>
From src/data/Feeder.jl:
  Bus(id::Int, vmin, vmax, is_root::Bool)
  Branch(from::Int, to::Int, r, x, smax)
  Feeder(buses::Vector{Bus}, branches::Vector{Branch}, root::Integer)  # runs assert_radial + assert_magnitudes
From src/units/PerUnit.jl (assert_magnitudes bands — HARD constraints on the fixture):
  voltage bounds must satisfy 0.8 ≤ vmin ≤ vmax ≤ 1.2
  branch r,x must satisfy 0 ≤ r,x < 5.0
  branch smax must satisfy 0 < smax < 100.0   # STRICT upper bound — 100.0 itself FAILS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ieee13_modified() built-in feeder from Table 4.1</name>
  <files>src/data/ieee13.jl</files>
  <read_first>
    - 04-RESEARCH.md "Modified IEEE-13 feeder fixture (DATA-03)" code sketch (the 10-branch (from,to,r,x) table, node k → struct index k+1, root=1, base 100 MVA/13.2 kV, head-branch S_max=6.86 MVA ⇒ 0.0686 pu) and Assumption A4 / Open Q2 (interior branches effectively unconstrained)
    - src/data/Feeder.jl (Bus/Branch/Feeder signatures; construction runs both invariants)
    - src/units/PerUnit.jl assert_magnitudes — NOTE the smax band is `0 < smax < 100.0` STRICT: the RESEARCH sketch uses `100.0` for interior branches, which WILL THROW. Use a large sentinel strictly below 100 (e.g. 99.0) and document it.
  </read_first>
  <action>
    Create `ieee13_modified()` returning `Feeder(buses, branches, 1)`. Build 11 `Bus`es indexed 1:11 where struct index 1 = thesis node 0 (the root/MEM frontier, `is_root=true`), all with `vmin=0.95, vmax=1.05`. Build 10 `Branch`es from the thesis Table 4.1 (from,to,r,x) pairs, shifting each thesis node `k` to struct index `k+1`: (0,1)r0.310x0.155, (1,2)r0.310x0.155, (2,3)r0.310x0.155, (1,4)r0.150x0.075, (4,5)r0.150x0.075, (1,6)r0.300x0.150, (2,7)r0.300x0.150, (3,8)r0.300x0.150, (3,9)r0.600x0.300, (2,10)r0.300x0.150. Set the head branch (0→1 ⇒ index 1→2) `smax=0.0686` pu (6.86 MVA / 100 MVA base — the single binding, congestion-driving limit). Set interior branches to a large sentinel STRICTLY below the `SMAX_PU_MAX=100.0` tripwire (use 99.0) so `assert_magnitudes` passes and only the head branch binds (Assumption A4 / Open Q2). Add a docstring documenting the base (100 MVA, 13.2 kV), the node→index shift, that only the head branch is thermally binding, and the 99.0 interior sentinel choice. `export ieee13_modified`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; f=TSODSO.ieee13_modified(); @assert length(f.buses)==11; @assert length(f.branches)==10; @assert f.root==1; @assert f.buses[1].is_root; @assert f.branches[1].smax ≈ 0.0686; println("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `ieee13_modified()` constructs without throwing (radial + magnitude invariants pass at `Feeder` construction).
    - `length(buses)==11`, `length(branches)==10`, `root==1`, `buses[1].is_root==true`.
    - The head branch (index 1, 1→2) has `smax ≈ 0.0686`; interior branch smax values are `< 100.0` (pass the strict tripwire).
    - Every bus has `vmin==0.95`, `vmax==1.05`.
    - `src/data/ieee13.jl` contains `ieee13_modified` and is exported.
  </acceptance_criteria>
  <done>Built-in modified IEEE-13 feeder constructs as a validated radial per-unit Feeder.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the ieee13 fixture @testitems green</name>
  <files>test/test_ieee13.jl</files>
  <read_first>
    - test/test_feeder.jl (how feeder construction / invariants are asserted in tests)
    - 04-RESEARCH.md "Phase Requirements → Test Map" DATA-03 row (fixture constructs, radial+magnitudes pass, topology matches Table 4.1)
  </read_first>
  <action>
    Fill the RED "ieee13:" fixture items (created in 04-01) to assert: `ieee13_modified()` constructs; 11 buses / 10 branches / root index 1 / `buses[1].is_root`; the head branch smax is 0.0686 pu and interior branches are < 100 pu; voltage bounds are 0.95/1.05; and topology matches Table 4.1 (spot-check a couple of (from,to) pairs after the node→index shift, e.g. branch (3,9)→index (4,10) with r 0.600). Do NOT add the SOCP solve here — that is 04-06.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ieee13", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - All fixture-level "ieee13" @testitems pass (construction, counts, root, head-branch limit, voltage bounds, topology spot-checks).
    - No solve is attempted in this file (that is deferred to 04-06's "ground" items).
  </acceptance_criteria>
  <done>ieee13 fixture tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SI thesis data → per-unit fixture | Table 4.1 values enter as per-unit; an SI/pu or base error silently corrupts every downstream voltage and price |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | per-unit / base conversion on IEEE-13 | mitigate | Single base (100 MVA, 13.2 kV) documented; `assert_magnitudes` at `Feeder` construction guards r,x,smax,voltage bands; S_max=6.86 MVA→0.0686 pu conversion done once and asserted in test |
| T-04-11 | Denial of Service | interior smax at the 100.0 tripwire boundary | mitigate | Use 99.0 sentinel strictly below `SMAX_PU_MAX`; the RESEARCH sketch's 100.0 would throw at construction — documented |
| T-04-SC | Tampering | package installs | accept | No packages installed this plan |
</threat_model>

<verification>
- `ieee13_modified()` builds a radial, magnitude-valid 11-bus/10-branch Feeder.
- Head branch 0.0686 pu; interior < 100 pu; voltages 0.95/1.05; root index 1.
- "ieee13" fixture @testitems green.
</verification>

<success_criteria>
The modified IEEE 13-node test feeder ships as a built-in fixture with the thesis
parameters, radial-validated and per-unit-converted once. (DATA-03)
</success_criteria>

<output>
Create `.planning/phases/04-convex-branch-flow-correctness-milestone/04-03-SUMMARY.md` when done.
</output>
