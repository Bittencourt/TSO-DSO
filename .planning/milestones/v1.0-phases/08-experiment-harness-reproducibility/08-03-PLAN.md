---
phase: 08-experiment-harness-reproducibility
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/experiments/run.jl
autonomous: true
requirements: [EXP-01, INFRA-04]
must_haves:
  truths:
    - "A researcher runs a declared Scenario end-to-end with run_scenario(s) and gets a ScenarioResult (welfare, node×T DADP, exactness certificate, iters/residuals, timings)."
    - "run_scenario dispatches :centralized→solve_welfare+extract_dlmp and :admm→solve_admm, reusing the validated Phase 3–7 builders — no new model."
    - "Centralized and ADMM DADP are normalized to the SAME node×T shape so results are comparable."
    - "An unknown strategy throws a clear ArgumentError; timings are recorded but EXCLUDED from any reproducibility comparison."
    - "Same Scenario + seed → IDENTICAL welfare/dadp/exact_maxgap through the full solve (single-thread Clarabel, == same-process); a different seed changes the result."
  artifacts:
    - path: "src/experiments/run.jl"
      provides: "ScenarioResult record + run_scenario strategy dispatch + normalization"
      contains: "function run_scenario"
      min_lines: 40
  key_links:
    - from: "src/experiments/run.jl"
      to: "solve_welfare / extract_dlmp / solve_admm"
      via: "strategy dispatch over materialized objects"
      pattern: "solve_welfare|solve_admm|extract_dlmp"
    - from: "run_scenario"
      to: "build_feeder / build_price / build_population / ConvexBranchFlow"
      via: "materialize from selectors + seed then solve"
      pattern: "build_population|ConvexBranchFlow"
---

<objective>
The first true end-to-end capability of the phase: `run_scenario(s::Scenario)` materializes the heavy
objects (08-02) and DISPATCHES on `s.strategy` — `:centralized` → `solve_welfare` + `extract_dlmp`,
`:admm` → `solve_admm` — then NORMALIZES both into one comparable `ScenarioResult` (welfare, node×T
DADP, exactness certificate, iters/final residuals for ADMM, timings). An unknown strategy throws
`ArgumentError`. Because the seed is threaded end-to-end (08-02) and the Clarabel path is
single-threaded, same-Scenario+seed runs are bit-for-bit identical — the load-bearing INFRA-04
reproducibility gate goes GREEN here.

Purpose: EXP-01 (declarative scenario → end-to-end with a swappable strategy) and INFRA-04
(same-seed→identical through the full solve) both land in this plan. The harness is pure orchestration
over already-validated builders (RESEARCH Summary / Pattern 1) — no new math, no new solver.
Output: `ScenarioResult` + `run_scenario` with :centralized and :admm branches, the strategy guard,
and the reproducibility gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-experiment-harness-reproducibility/08-RESEARCH.md

<interfaces>
From src/experiments/Scenario.jl (08-02): `struct Scenario` (primitive selectors + ADMM knobs).
From src/experiments/materialize.jl (08-02): `sub_seed`, `build_feeder`, `build_price`,
  `build_population`.
From src/models/welfare_solve.jl: `solve_welfare(feeder, pf, aggs; T, λ₀, allow_export, …)
  -> (ctx, objective_value, dadp)`; maxgap at `ctx.meta[:socp_maxgap]`.
From src/pricing/dlmp.jl: `extract_dlmp(ctx; bus=nothing, T=nothing)` -> bus×T matrix; index by
  sorted load buses to normalize to node×T.
From src/admm/solve_admm.jl: `solve_admm(feeder, pf::ConvexBranchFlow, aggs; T, λ₀, ρ, maxiter,
  ε_abs, ε_rel, τ, μ, allow_export) -> (; welfare, dadp, λ, iters, residuals, dso_ctx, exact_maxgap)`;
  `residuals.primal_trace` / `residuals.dual_trace` (final via `last(...)`), `residuals.iters`;
  ADMM `dadp` is ALREADY node×T (load-node rows) — matches extract_dlmp[load_buses,:] shape (RESEARCH A5).
From src/powerflow/ConvexBranchFlow.jl: `ConvexBranchFlow()` (zero-arg singleton).
RESEARCH §Pattern 1 dispatch body + §Code Examples reproducibility gate + §Anti-Patterns
  (timings never in the equality check; run_scenario stays path-free — persistence is a separate fn).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScenarioResult record + run_scenario :centralized branch (end-to-end slice + repro gate)</name>
  <files>src/experiments/run.jl</files>
  <read_first>
    - src/experiments/run.jl (the empty stub from 08-01)
    - src/experiments/Scenario.jl + materialize.jl (08-02 — the inputs)
    - src/models/welfare_solve.jl (solve_welfare return tuple + ctx.meta[:socp_maxgap])
    - src/pricing/dlmp.jl (extract_dlmp bus×T matrix; how to slice to sorted load buses)
    - .planning/phases/08-experiment-harness-reproducibility/08-RESEARCH.md §Pattern 1 (the
      :centralized branch), §Solver Determinism (Clarabel single-thread, == same-process), §Code
      Examples (the INFRA-04 same-seed gate), §Anti-Patterns (timings excluded; run_scenario path-free)
    - test/test_experiments.jl (the RED "EXP-01 scenario centralized", "INFRA-04 same-seed repro",
      "INFRA-04 seed sensitivity" testitems this must turn green)
  </read_first>
  <behavior>
    - run_scenario(Scenario(strategy=:centralized, feeder=:ieee13, seed=7)) returns a ScenarioResult
      with a Float64 welfare, a node×T Matrix dadp (rows = sorted load buses), a Float64 exact_maxgap,
      and iters/final_r/final_s == missing (centralized has no ADMM iteration).
    - Two same-Scenario same-seed centralized runs in one process give `r1.welfare == r2.welfare`,
      `r1.dadp == r2.dadp`, `r1.exact_maxgap == r2.exact_maxgap` (bit-for-bit, single-thread Clarabel).
    - A different seed (seed=8) yields a different dadp (`r3.dadp != r1.dadp`).
    - Timings are present on the result but never entered into the equality comparison.
  </behavior>
  <action>
    In `src/experiments/run.jl` define an immutable `ScenarioResult` holding the Scenario, `welfare`,
    `dadp` (node×T Matrix), `exact_maxgap`, `iters`/`final_r`/`final_s` (Union{Missing,…} for the
    centralized branch), and `elapsed` timing fields flagged NON-reproducible. Implement `run_scenario(s)`
    that FIRST materializes (build_feeder(s.feeder); generate_profiles(seed=sub_seed(s.seed,:profiles),
    T=s.T); build_price(s.price, s.T, profiles); build_population(s.population, feeder, profiles,
    sub_seed(s.seed,:population)); `pf = ConvexBranchFlow()`), then handles the `:centralized` branch:
    call solve_welfare(feeder, pf, aggs; T=s.T, λ₀, allow_export=s.allow_export), read
    ctx.meta[:socp_maxgap], normalize `dadp = extract_dlmp(ctx)[sorted_load_buses, :]` to node×T, and
    build a ScenarioResult with iters/final_r/final_s = missing. Record wall-clock via `@elapsed` into
    the timing field but comment that it is EXCLUDED from reproducibility comparisons (Anti-Pattern).
    Keep run_scenario PATH-FREE — it returns a ScenarioResult, never writes a file (persistence is
    08-04). Export run_scenario and ScenarioResult. Leave the :admm branch + strategy guard for Task 2
    (an unknown/`:admm` strategy may fall through to a temporary error until Task 2 — do not fake it).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; s=Scenario(name="repro",feeder=:ieee13,strategy=:centralized,seed=7,T=24); r1=run_scenario(s); r2=run_scenario(s); @assert r1.welfare==r2.welfare && r1.dadp==r2.dadp && r1.exact_maxgap==r2.exact_maxgap; @assert ismissing(r1.iters); r3=run_scenario(Scenario(name="repro",feeder=:ieee13,strategy=:centralized,seed=8,T=24)); @assert r3.dadp != r1.dadp; println("EXP-01 centralized + INFRA-04 repro OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `ScenarioResult` and `run_scenario` are exported; run_scenario writes no file (path-free).
    - `run_scenario(:centralized)` returns welfare::Float64, dadp::Matrix (node×T, sorted load-bus rows),
      exact_maxgap::Float64, iters/final_r/final_s == missing.
    - Same Scenario+seed ⇒ `r1.welfare==r2.welfare`, `r1.dadp==r2.dadp`, `r1.exact_maxgap==r2.exact_maxgap`.
    - seed=8 ⇒ `r3.dadp != r1.dadp` (seed sensitivity).
    - Timing captured but not part of any equality assert.
    - "EXP-01 scenario centralized", "INFRA-04 same-seed repro", "INFRA-04 seed sensitivity" testitems pass.
  </acceptance_criteria>
  <done>run_scenario(:centralized) works end-to-end through the validated solve; ScenarioResult normalizes to node×T; the INFRA-04 same-seed reproducibility gate is green; timings excluded from equality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_scenario :admm branch + strategy dispatch guard</name>
  <files>src/experiments/run.jl</files>
  <read_first>
    - src/experiments/run.jl (the :centralized branch + ScenarioResult from Task 1)
    - src/admm/solve_admm.jl (return NamedTuple: welfare, dadp, iters, residuals; residuals.primal_trace
      / dual_trace via last(...); dadp already node×T — RESEARCH A5)
    - test/test_admm.jl (the ieee13 solve_admm invocation: ρ/maxiter/tol/allow_export magnitudes)
    - .planning/phases/08-experiment-harness-reproducibility/08-RESEARCH.md §Pattern 1 (the :admm
      branch + the else-throw ArgumentError guard)
    - test/test_experiments.jl (the RED "EXP-01 scenario admm" and "EXP-01 strategy guard" testitems)
  </read_first>
  <behavior>
    - run_scenario(Scenario(strategy=:admm, feeder=:ieee13, seed=7)) returns a ScenarioResult whose
      welfare/dadp match the centralized ground truth within the Phase-6 cross-validation tolerance,
      with iters::Int ≥ 1 and final_r/final_s populated from the residual traces.
    - The :admm dadp is node×T (same shape as the :centralized branch) so results are comparable.
    - run_scenario(Scenario(strategy=:bogus)) throws ArgumentError naming :centralized/:admm.
  </behavior>
  <action>
    Extend `run_scenario`'s dispatch with the `:admm` branch: call solve_admm(feeder, pf, aggs; T=s.T,
    λ₀, ρ=s.ρ, maxiter=s.maxiter, ε_abs=s.ε_abs, ε_rel=s.ε_rel, τ=s.τ_ratio, μ=s.μ,
    allow_export=s.allow_export), and build a ScenarioResult with welfare=r.welfare, dadp=r.dadp
    (already node×T), exact_maxgap=r.exact_maxgap, iters=r.iters, final_r=last(r.residuals.primal_trace),
    final_s=last(r.residuals.dual_trace). Add the terminal `else` throwing
    `ArgumentError("run_scenario: unknown strategy …; expected :centralized or :admm")` (RESEARCH
    §Pattern 1). Do not name a solver (the solve path routes through select_optimizer inside
    solve_admm). Keep run_scenario path-free.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; ra=run_scenario(Scenario(name="a",feeder=:ieee13,strategy=:admm,seed=7,T=24,ρ=100.0,maxiter=200)); @assert ra.iters isa Int && ra.iters>=1 && !ismissing(ra.final_r); @assert size(ra.dadp,2)==24; try; run_scenario(Scenario(name="x",feeder=:ieee13,strategy=:bogus)); error("no throw"); catch e; @assert e isa ArgumentError; end; println("EXP-01 admm + strategy guard OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `run_scenario(:admm)` returns a ScenarioResult with iters::Int ≥ 1, populated final_r/final_s, and
      a node×T dadp matching the :centralized shape.
    - The ADMM welfare/dadp agree with the centralized result within the Phase-6 cross-validation
      tolerance on the shared fixture (asserted by the "EXP-01 scenario admm" testitem).
    - `run_scenario(:bogus)` throws ArgumentError naming :centralized/:admm.
    - No solver named in run.jl.
    - "EXP-01 scenario admm" and "EXP-01 strategy guard" testitems pass.
  </acceptance_criteria>
  <done>run_scenario dispatches both strategies into one comparable ScenarioResult; the strategy guard throws on unknown selectors; EXP-01 swappable-strategy capability is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scenario+seed → full solve result | Same input must yield an identical result (research-integrity / INFRA-04). |
| strategy selector → solve dispatch | An unknown/misrouted strategy must fail loudly, not silently pick a default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Repudiation | non-reproducible run (solver nondeterminism / seed leak) | mitigate | Seed threaded end-to-end (08-02) + single-thread native-Julia Clarabel path; assert `==` same-process on welfare/dadp/exact_maxgap; the same-seed gate is the load-bearing INFRA-04 test (RESEARCH §Solver Determinism). |
| T-08-07 | Tampering | wall-clock timing polluting the reproducibility check | mitigate | Timings recorded on ScenarioResult but EXCLUDED from every equality assert (Anti-Pattern). |
| T-08-08 | Spoofing | unknown strategy silently defaulting | mitigate | Terminal else throws ArgumentError naming the valid strategies (V5 input validation). |
| T-08-09 | Tampering | non-comparable centralized vs ADMM DADP | mitigate | Both branches normalized to node×T (extract_dlmp[load_buses,:] == solve_admm node×T rows, RESEARCH A5), enabling a like-for-like reproducibility/cross-validation comparison. |
</threat_model>

<verification>
- run_scenario(:centralized) and (:admm) both return a node×T ScenarioResult through the validated solve.
- Same Scenario+seed ⇒ identical welfare/dadp/exact_maxgap (== same-process, single-thread Clarabel).
- Different seed ⇒ different dadp; unknown strategy ⇒ ArgumentError.
- Timings excluded from equality; no solver named in run.jl.
</verification>

<success_criteria>
`run_scenario(s::Scenario)` runs a declared scenario end-to-end with either the centralized or ADMM
strategy, reusing the validated Phase 3–7 builders, normalizes both into one comparable node×T
ScenarioResult, throws on unknown strategies, and satisfies the INFRA-04 same-seed→identical
reproducibility gate.
</success_criteria>

<output>
Create `.planning/phases/08-experiment-harness-reproducibility/08-03-SUMMARY.md` when done.
</output>
