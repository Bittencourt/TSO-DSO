---
phase: 08-experiment-harness-reproducibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Project.toml
  - Manifest.toml
  - Manifest-v1.10.toml
  - Manifest-v1.11.toml
  - Manifest-v1.12.toml
  - test/Project.toml
  - test/Manifest.toml
  - .gitignore
  - src/TSODSO.jl
  - src/experiments/Scenario.jl
  - src/experiments/materialize.jl
  - src/experiments/run.jl
  - src/experiments/store.jl
  - src/experiments/sweep.jl
  - test/fixtures_phase8.jl
  - test/test_experiments.jl
autonomous: true
requirements: [EXP-01, EXP-02, INFRA-04]
must_haves:
  truths:
    - "The package resolves and `using TSODSO` loads on Julia 1.10/1.11/1.12 with DrWatson, CSV, DataFrames as hard [deps] (not weakdeps)."
    - "src/experiments/ include-graph stubs are wired into TSODSO.jl in dependency order (after pricing/ and admm/) and load without error."
    - "data/ is gitignored; results/ is committed; the two-tier storage split is established before any artifact is written."
    - "A RED @testitem harness (EXP-01 centralized/admm/strategy-guard, EXP-02 sweep/diff-friendly, INFRA-04 same-seed/seed-sensitivity/provenance) exists and is guarded so the runner never crashes while stubs are unfilled."
  artifacts:
    - path: "src/experiments/Scenario.jl"
      provides: "Include-graph stub for the Scenario struct (filled by plan 08-02)"
      min_lines: 1
    - path: "src/experiments/run.jl"
      provides: "Include-graph stub for run_scenario dispatch (filled by plan 08-03)"
      min_lines: 1
    - path: "test/test_experiments.jl"
      provides: "RED @testitem harness for EXP-01/EXP-02/INFRA-04"
      contains: "@testitem"
    - path: "test/fixtures_phase8.jl"
      provides: "@testmodule Phase8Fixtures: a minimal Scenario spec + a mktempdir storage-dir helper (hermetic tests)"
      contains: "@testmodule"
  key_links:
    - from: "src/TSODSO.jl"
      to: "src/experiments/*.jl"
      via: "include after pricing/ and admm/"
      pattern: "include\\(\"experiments/"
    - from: "Project.toml"
      to: "DrWatson/CSV/DataFrames"
      via: "[deps] + [compat]"
      pattern: "DrWatson"
---

<objective>
Lay the Phase-8 foundation as the SINGLE owner of every shared file: add DrWatson/CSV/DataFrames as
hard `[deps]` (+ `[compat]`) to the root and test environments, re-resolve and commit the
version-specific manifests on Julia 1.10/1.11/1.12, wire the `src/experiments/` include-graph stubs into
`TSODSO.jl`, gitignore `data/`, and stand up a RED `@testitem` harness (`test/test_experiments.jl`) plus
a `Phase8Fixtures` `@testmodule` covering EXP-01 / EXP-02 / INFRA-04. No harness logic yet — only the
scaffold that plans 08-02…08-04 fill in file-disjointly.

Purpose: This is the only wave that touches shared foundation files (Project.toml, manifests,
TSODSO.jl, .gitignore, the test harness). Isolating it here keeps waves 2–4 file-disjoint and parallel-safe.
Output: New hard deps + re-resolved manifests; five `src/experiments/*.jl` stub includes; committed
`results/` dir + gitignored `data/`; the complete RED test harness (all Phase-8 behavior asserted, all
failing because `src/experiments/` is empty).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-experiment-harness-reproducibility/08-RESEARCH.md
@.planning/phases/08-experiment-harness-reproducibility/08-VALIDATION.md

<interfaces>
<!-- Seams the harness will orchestrate — extracted from source. Executors use these directly. -->
From src/TSODSO.jl (include graph, current tail): pricing/*.jl (line ~83-86) then admm/*.jl
  (line ~96-99) then diagnostics/plots.jl (~106). `experiments/*.jl` includes go AFTER admm/ and
  diagnostics/ (run_scenario calls solve_welfare, solve_admm, extract_dlmp).
From src/models/welfare_solve.jl: `solve_welfare(feeder, pf::AbstractPowerFlow, aggregators; T=24, λ₀,
  optimizer=select_optimizer(problem_class(pf)), allow_local=false, allow_export=false, …)
  -> (ctx, objective_value, dadp)`; maxgap stashed at `ctx.meta[:socp_maxgap]`.
From src/admm/solve_admm.jl: `solve_admm(feeder, pf::ConvexBranchFlow, aggregators; T=24, λ₀, ρ,
  maxiter=200, ε_abs=1e-4, ε_rel=1e-3, τ=2.0, μ=10.0, allow_export=true, …)
  -> (; welfare, dadp, λ, iters, residuals, dso_ctx, exact_maxgap)` (residuals has
  primal_trace/dual_trace/iters).
From src/pricing/dlmp.jl: `extract_dlmp(ctx; bus=nothing, T=nothing)` -> bus×T matrix.
From src/powerflow/ConvexBranchFlow.jl: `struct ConvexBranchFlow <: AbstractPowerFlow end` (ZERO-arg).
From src/data/ieee13.jl: `ieee13_modified()`. From src/data/ieee123.jl: `ieee123_modified()`.
From src/data/profiles.jl: `generate_profiles(; seed::Integer, T::Int=24, …) -> (; demand, pv)`.
From test/Project.toml [deps]: Aqua, JET, JuMP, StableRNGs, Test, TestItemRunner, TestItems.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DrWatson/CSV/DataFrames hard deps + re-resolve committed manifests + two-tier storage dirs</name>
  <files>Project.toml, test/Project.toml, Manifest.toml, Manifest-v1.10.toml, Manifest-v1.11.toml, Manifest-v1.12.toml, test/Manifest.toml, .gitignore</files>
  <read_first>
    - Project.toml (current [deps]/[weakdeps]/[compat] — mirror the exact-pin style: Clarabel = "0.11.1")
    - test/Project.toml (current test [deps])
    - .gitignore (Manifest.toml is intentionally COMMITTED — do NOT gitignore manifests)
    - .planning/phases/08-experiment-harness-reproducibility/08-RESEARCH.md §Standard Stack (pins:
      DrWatson 2.19.1, CSV 0.10.16, DataFrames 1.8.2; JLD2/FileIO arrive transitively) and
      §Environment Availability (must Pkg.add + re-resolve main + test manifests on 1.10/1.11/1.12)
    - .planning/phases/07-admm-convergence-scale/07-01-PLAN.md (the manifest re-resolution precedent:
      Manifest.toml + Manifest-v1.10/1.11/1.12.toml + test/Manifest.toml, via Pkg.resolve per version)
  </read_first>
  <action>
    Add DrWatson, CSV, DataFrames to root Project.toml `[deps]` (resolve real UUIDs via Pkg — a wrong
    UUID must fail loudly at Pkg.resolve, never be trusted from a literal) and to `[compat]` with the
    RESEARCH pins (DrWatson = "2.19.1", CSV = "0.10.16", DataFrames = "1.8.2"), matching the existing
    exact-pin style. Add the same three to test/Project.toml `[deps]` (the provenance and repro
    testitems load DrWatson). Do NOT add JLD2/FileIO explicitly — they arrive transitively via DrWatson.
    Re-resolve and commit ALL version-specific manifests (Manifest.toml, Manifest-v1.10.toml,
    Manifest-v1.11.toml, Manifest-v1.12.toml, test/Manifest.toml) via `Pkg.resolve` under each available
    Julia version so the pinned environment stays reproducible (INFRA-01). Add `/data/` to .gitignore
    with a comment citing RESEARCH Pitfall 3 (per-run JLD2 artifacts are binary, gitignored) while
    leaving `results/` committed. Create the empty `data/sims/` (gitignored) and `results/sweeps/`
    (committed, add a `.gitkeep`) directories establishing the two-tier storage split (EXP-02).
  </action>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.resolve(); using DrWatson, CSV, DataFrames, TSODSO; @assert isdir("data") && isdir(joinpath("results","sweeps")); println("deps+manifests+dirs OK")'</automated>
  </verify>
  <acceptance_criteria>
    - Project.toml `[deps]` contains DrWatson, CSV, DataFrames with real resolved UUIDs; `[compat]` pins
      DrWatson = "2.19.1", CSV = "0.10.16", DataFrames = "1.8.2".
    - test/Project.toml `[deps]` contains DrWatson, CSV, DataFrames.
    - `Pkg.resolve()` succeeds and all five manifests (Manifest.toml, Manifest-v1.10/1.11/1.12.toml,
      test/Manifest.toml) are re-resolved and committed.
    - `.gitignore` contains `/data/` (with a comment) and does NOT gitignore any Manifest.
    - `data/sims/` exists (gitignored) and `results/sweeps/` exists (committed, with `.gitkeep`).
    - `using DrWatson, CSV, DataFrames, TSODSO` loads without error.
  </acceptance_criteria>
  <done>Hard deps added and pinned; all manifests re-resolved on 1.10/1.11/1.12; two-tier storage dirs created; data/ gitignored, results/ committed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire src/experiments/ include-graph stubs + RED @testitem harness + Phase8Fixtures</name>
  <files>src/TSODSO.jl, src/experiments/Scenario.jl, src/experiments/materialize.jl, src/experiments/run.jl, src/experiments/store.jl, src/experiments/sweep.jl, test/fixtures_phase8.jl, test/test_experiments.jl</files>
  <read_first>
    - src/TSODSO.jl (the include graph; add experiments/ includes after admm/ and diagnostics/)
    - test/fixtures_phase7.jl (the @testmodule pattern: SEED constants, _house_aggregator shape,
      generate_profiles usage — mirror for a minimal Phase8Fixtures)
    - test/test_admm.jl (the @testitem style: filter-substring names, solve_welfare/solve_admm calls)
    - .planning/phases/08-experiment-harness-reproducibility/08-RESEARCH.md §Validation Architecture
      (the Req→Test map: EXP-01 centralized/admm/strategy-guard, EXP-02 sweep/diff-friendly,
      INFRA-04 same-seed/seed-sensitivity/provenance) and §Code Examples (the reproducibility gate)
    - .planning/phases/08-experiment-harness-reproducibility/08-VALIDATION.md (Per-Task Verification Map
      — the exact filter substrings: scenario, run, sweep, provenance, repro/bitfor, harness)
  </read_first>
  <action>
    Create five stub files under `src/experiments/`: `Scenario.jl`, `materialize.jl`, `run.jl`,
    `store.jl`, `sweep.jl` — each a valid loadable file with a one-line comment header naming its owning
    plan (08-02 fills Scenario.jl + materialize.jl; 08-03 fills run.jl; 08-04 fills store.jl + sweep.jl)
    and NO logic yet. Wire all five into `src/TSODSO.jl`'s include graph AFTER the admm/ and
    diagnostics/ includes, in dependency order (Scenario → materialize → run → store → sweep), each with
    a comment citing its plan + requirement. Create `test/fixtures_phase8.jl` as a `@testmodule
    Phase8Fixtures` providing a minimal declarative Scenario spec (feeder = :ieee13, small T) and a
    `with_tempdir(f)` helper wrapping `mktempdir` (hermetic storage tests — RESEARCH Pitfall 6). Create
    `test/test_experiments.jl` holding the COMPLETE Phase-8 @testitem suite (RED — failing because
    src/experiments is empty), with names containing the VALIDATION filter substrings: "EXP-01
    scenario centralized", "EXP-01 scenario admm", "EXP-01 strategy guard", "EXP-02 sweep", "EXP-02
    diff-friendly", "INFRA-04 same-seed repro", "INFRA-04 seed sensitivity", "INFRA-04 provenance
    tagsave". Each testitem asserts the FINAL intended behavior (per RESEARCH §Code Examples) so later
    plans turn them green by implementing source, NOT by editing this file. Guard the harness (wrap the
    body so a missing symbol yields a clean test failure, not a runner crash) and register
    test/test_experiments.jl + test/fixtures_phase8.jl in test/runtests.jl if that file enumerates test
    files.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; @assert all(isfile, ["src/experiments/Scenario.jl","src/experiments/materialize.jl","src/experiments/run.jl","src/experiments/store.jl","src/experiments/sweep.jl"]); using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("EXP-01",ti.name)||occursin("EXP-02",ti.name)||occursin("INFRA-04",ti.name))' ; echo "harness ran (RED items expected until waves 2-4)"</automated>
  </verify>
  <acceptance_criteria>
    - Five files exist under `src/experiments/` and each is included by `src/TSODSO.jl` after the admm/
      and diagnostics/ includes (`grep -c 'include(\"experiments/' src/TSODSO.jl` == 5).
    - `using TSODSO` loads with the stub includes present (empty stubs must not break load).
    - `test/test_experiments.jl` contains @testitems whose names include every VALIDATION filter
      substring (scenario, run, sweep, provenance, repro/bitfor for EXP-01/EXP-02/INFRA-04).
    - `test/fixtures_phase8.jl` defines `@testmodule Phase8Fixtures` with a minimal Scenario spec and a
      mktempdir storage helper.
    - The TestItemRunner invocation runs to completion (RED items failing, runner NOT crashing).
  </acceptance_criteria>
  <done>Include-graph stubs wired; complete RED @testitem harness + Phase8Fixtures exist; runner completes with expected RED items; no plan after this edits the test harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package env → registry | New deps (DrWatson/CSV/DataFrames) enter the committed manifest (supply chain). |
| repo → git | data/ (binary JLD2) vs results/ (committed CSV) split established here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | DrWatson/CSV/DataFrames dependency add | accept | Curated Julia General registry (PR-gated, not npm/PyPI); pins verified in RESEARCH §Package Legitimacy Audit against the local General clone; committed version-specific manifests pin the tree (INFRA-01). Real UUIDs resolved by Pkg (a wrong literal fails loudly at Pkg.resolve). |
| T-08-02 | Repudiation | data/ vs results/ storage split | mitigate | Gitignore data/ so binary JLD2 never churns the repo; keep results/ committed for the diff-friendly summary — established before any artifact is written. |
| T-08-SC | Tampering | package-manager installs (Pkg.add) | mitigate | Julia General registry (curated); slopcheck is N/A to Julia (RESEARCH); all four packages (incl. transitive JLD2) are long-established JuliaDynamics/JuliaData/JuliaIO flagships — dispositioned Approved in the audit table. No [ASSUMED]/[SUS] packages ⇒ no blocking human checkpoint required. |
</threat_model>

<verification>
- `Pkg.resolve()` succeeds on 1.10/1.11/1.12; `using DrWatson, CSV, DataFrames, TSODSO` loads.
- `src/TSODSO.jl` includes all five experiments/ stubs after admm/ and diagnostics/.
- The RED @testitem harness runs to completion (items failing, runner stable).
- data/ gitignored; results/sweeps/ committed.
</verification>

<success_criteria>
DrWatson/CSV/DataFrames are hard [deps] with [compat] pins; all five manifests re-resolved on
1.10/1.11/1.12; src/experiments/ stubs wired into the include graph; two-tier storage dirs established
(data/ gitignored, results/ committed); the complete RED Phase-8 @testitem harness + Phase8Fixtures
exist and run to completion.
</success_criteria>

<output>
Create `.planning/phases/08-experiment-harness-reproducibility/08-01-SUMMARY.md` when done.
</output>
