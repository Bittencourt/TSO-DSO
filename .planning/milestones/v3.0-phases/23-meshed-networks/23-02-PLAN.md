---
phase: 23-meshed-networks
plan: 2
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/powerflow/MeshedFlow.jl
  - src/TSODSO.jl
  - docs/src/api.md
  - test/fixtures_phase23.jl
  - test/test_mesh_flow.jl
autonomous: true
requirements: [MESH-02]

must_haves:
  truths:
    - "A researcher can solve the meshed SOCP branch-flow problem via MeshedFlow through the
       SEAM-01 pf dispatch seam (solve_welfare), with zero new constraint-writing code --
       MeshedFlow delegates to ConvexBranchFlow.contribute!, which is ALREADY graph-generic
       (D-03, RESEARCH's Critical Codebase Finding)."
    - "A committed loop fixture exists (smallest honest loop, 3-4 bus, single independent
       cycle, D-02) with a TOGGLABLE impedance profile: :uniform and :heterogeneous branches
       on the SAME topology, exercising both certificate branches the next plan needs (D-10)."
    - "The small loop fixture is the sole CI-gated evidence this phase commits; a
       meshed-IEEE-13-with-tie-switch variant is explicitly NOT built (D-02/D-12 -- deferred,
       see MESH-STRETCH), since the ROADMAP's success criterion only requires 'at least one
       committed meshed fixture'."
  artifacts:
    - path: "src/powerflow/MeshedFlow.jl"
      provides: "MeshedFlow <: AbstractPowerFlow -- contribute! delegates to
        contribute!(ConvexBranchFlow(), ctx, feeder; T), problem_class(::MeshedFlow) = SOCP()"
      contains: "struct MeshedFlow"
    - path: "test/fixtures_phase23.jl"
      provides: "Phase23Fixtures @testmodule: mesh_feeder(profile), mesh_aggregators(),
        mesh_lambda0() -- the 3-bus triangle loop, both impedance profiles"
      contains: "@testmodule Phase23Fixtures"
  key_links:
    - from: "src/powerflow/MeshedFlow.jl"
      to: "src/powerflow/ConvexBranchFlow.jl"
      via: "contribute!(pf::MeshedFlow, ctx, feeder; T) calls
        contribute!(ConvexBranchFlow(), ctx, feeder; T = T) FIRST"
      pattern: "contribute!\\(ConvexBranchFlow\\(\\)"
    - from: "test/fixtures_phase23.jl"
      to: "test/test_mesh_flow.jl and test/test_mesh_angle_certificate.jl (plan 23-03)"
      via: "setup=[Phase23Fixtures] on every Phase-23 @testitem"
      pattern: "Phase23Fixtures"
---

<objective>
Give the meshed SOCP a formulation (MESH-02) and the phase's one committed CI fixture
(D-02/D-12): `MeshedFlow <: AbstractPowerFlow` is a thin delegation to
`ConvexBranchFlow.contribute!` (RESEARCH's own finding: the constraint set is ALREADY
graph-generic, so there is no new model-time math here -- D-03 resolves "explicit cycle/loop
consistency" as the a-posteriori certificate the NEXT plan builds, never a hard convex
constraint), and a small 3-bus single-loop fixture with a togglable `:uniform`/
`:heterogeneous` impedance profile that will exercise BOTH the certificate's recoverable and
unrecoverable branches without any knife-edge parameter search (D-10).

Purpose: prove the formulation slots into `solve_welfare` with zero code change to that file
(RESEARCH's "Critical codebase finding: solve_welfare needs NO sibling entry point"), and give
plan 23-03's certificate a concrete, already-solved `ModelContext` to certify against.
Output: `src/powerflow/MeshedFlow.jl` (new), `src/TSODSO.jl` (wired), `docs/src/api.md`
(wired), `test/fixtures_phase23.jl` (new), `test/test_mesh_flow.jl` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-meshed-networks/23-CONTEXT.md
@.planning/phases/23-meshed-networks/23-RESEARCH.md
@.planning/phases/23-meshed-networks/23-PATTERNS.md
@.planning/phases/23-meshed-networks/23-01-SUMMARY.md

<interfaces>
From src/powerflow/RestrictedBranchFlow.jl (lines 174-175, 283-291 -- the delegation +
provenance-stash + trait-registration pattern to copy, NOT its OPF-m body which is
RestrictedBranchFlow-specific and does not apply here):

function contribute!(pf::RestrictedBranchFlow, ctx::ModelContext, feeder; T::Int = 1)
    contribute!(ConvexBranchFlow(), ctx, feeder; T = T)
    ... # RestrictedBranchFlow-specific additions -- NOT applicable to MeshedFlow
    ctx.meta[:restriction_ε] = pf.ε
    ctx.meta[:formulation] = :RestrictedBranchFlow
    return ctx
end
problem_class(::RestrictedBranchFlow) = SOCP()
export RestrictedBranchFlow

`MeshedFlow`'s delegation delta is EMPTY at the constraint-writing level (RESEARCH's own
"Critical codebase finding": `ConvexBranchFlow.contribute!`'s KCL/v-drop/cone/exactness-copy
loops are already graph-generic, no tree-order assumption anywhere) -- the resulting shape is
purely: delegate, stash `:formulation`, return.

From src/data/MeshedFeeder.jl (plan 23-01, just landed): `MeshedFeeder(buses, branches, root)`
constructs on a genuinely cyclic edge list; exposes `.buses`/`.branches`/`.root` identically
to `Feeder`.

From src/TSODSO.jl line 57 (the exact include slot): `include("powerflow/RestrictedBranchFlow.jl")`
is the last powerflow/ include before `solver/problem_class_trait.jl` -- `MeshedFlow.jl` goes
immediately after it (same "delegates to ConvexBranchFlow, must load after it" rationale).

From docs/src/api.md line 69 (the "Power-Flow Formulations" `@autodocs` block): `Pages =
["powerflow/AbstractPowerFlow.jl", "powerflow/DCPowerFlow.jl", "powerflow/LinDistFlow.jl",
"powerflow/ConvexBranchFlow.jl", "powerflow/ACPowerFlow.jl", "powerflow/RestrictedBranchFlow.jl"]`
-- append `"powerflow/MeshedFlow.jl"`.

From test/fixtures_phase22.jl (whole file, the exact `@testmodule` structural analog):
header contract comment (self-contained -- every feeder-consuming builder takes arguments,
nothing evaluated at module load time), a constants block, feeder-builder-as-function,
aggregator-builder-as-function, an `export` list at the bottom.

Device constructor signatures needed for the fixture (read directly, do not re-derive):
  Bus(id::Int, vmin::Real, vmax::Real, is_root::Bool)
  Branch(from::Int, to::Int, r::Real, x::Real, smax::Real)
  MeshedFeeder(buses::Vector{Bus}, branches::Vector{Branch}, root::Int)
  Thermostatic(bus, α, β, Tmin, Tmax, Tin0, Pmin, Pmax, b, Tout::Vector) -- with T=1 and
    `Pmin == Pmax` this PINS the device's served power to an exact, deterministic value
    (the comfort-band recursion is a no-op at T=1: `contribute!` only builds the
    `Tin[t+1]=...` constraint for `t=1:T-1`, so `Tout` is read only for its length guard,
    `length(d.Tout) >= T`, and must have at least 1 entry even though it is otherwise unused)
  Aggregator(bus, φ, devices::Vector{<:AbstractDevice}, Pdc::Vector)
  solve_welfare(feeder, pf::AbstractPowerFlow, aggregators; T::Int, λ₀) ->
    (ctx, objective_value, dadp)
  SMAX_NO_LIMIT -- the project's canonical "no thermal limit" sentinel (units/PerUnit.jl)

Fixture design (RESEARCH.md "Honest-Gap Fixture Design" + the empirically-verified spike
table -- reuse the SAME asymmetric loads and R/X ratios the spike measured, now inside this
project's real `MeshedFeeder`/`Aggregator`/`solve_welfare` path rather than a standalone
script): a 3-bus triangle -- bus 1 (root, MEM frontier), bus 2, bus 3; branches `(1,2)`,
`(2,3)`, `(3,1)` (nB=3 > N-1=2, a genuine single loop). Asymmetric pinned loads: bus 2 draws
`P2_LOAD = 0.30` pu, bus 3 draws `P3_LOAD = 0.05` pu (the spike's own Case-A/A2 asymmetric
pair, chosen so the chord flow stays strictly nonzero -- never the degenerate symmetric
case). Two impedance profiles on the SAME topology: `:uniform` -- all three branches
`r=0.01, x=0.02` (R/X ratio 0.5 everywhere, the spike's recoverable Case A/A2); `:heterogeneous`
-- branch(1,2) `r=0.04,x=0.01` (ratio 4.0), branch(2,3) `r=0.01,x=0.06` (ratio ~0.167),
branch(3,1) `r=0.02,x=0.02` (ratio 1.0) -- the spike's own unrecoverable Case C ratios exactly.
All branches carry `smax = SMAX_NO_LIMIT` (this fixture is about the LOOP, not congestion).
Bus voltage bounds: root `vmin=vmax` irrelevant (fixed at 1.0 by `contribute!`); buses 2/3
`vmin=0.90, vmax=1.10` (wide headroom so the small pinned loads never bind a voltage
constraint -- this fixture isolates the loop/angle question from the Phase-20 overvoltage
question). With no device utility curvature doing any work (loads are PINNED, not chosen by
the optimizer) and `Pdc = [0.0]` at both aggregators (no additional φ-driven reactive draw),
`solve_welfare`'s objective reduces to minimizing the cost of imported power at `feeder.root`
-- i.e. a genuine LOSS-MINIMIZING SOCP over the loop, faithfully reproducing the RESEARCH
spike's own objective through this project's real solve path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MeshedFlow -- delegation formulation (D-03, MESH-02)</name>
  <files>src/powerflow/MeshedFlow.jl, src/TSODSO.jl, docs/src/api.md</files>
  <read_first>
    src/powerflow/RestrictedBranchFlow.jl (lines 1-60, 174-176, 283-293 -- the delegation +
    provenance + trait pattern, per the interfaces block), src/powerflow/ConvexBranchFlow.jl
    (lines 76-77, 116-120, 213-227 -- confirming the KCL loop is graph-generic, no tree
    assumption), src/solver/problem_class_trait.jl (whole file -- the trait contract),
    src/TSODSO.jl lines 46-57 (powerflow/ include block)
  </read_first>
  <action>
    Create `src/powerflow/MeshedFlow.jl` defining `struct MeshedFlow &lt;: AbstractPowerFlow end`
    (zero fields, mirroring `ConvexBranchFlow`'s own singleton shape -- no field to validate,
    per CONTEXT.md's discretion list). Define `contribute!(pf::MeshedFlow, ctx::ModelContext,
    feeder; T::Int = 1)` that FIRST calls `contribute!(ConvexBranchFlow(), ctx, feeder; T = T)`
    (byte-identical KCL/v-drop/rotated-cone/exactness-copy/apparent-power-cone constraint set
    -- RESEARCH's "Critical codebase finding": this is ALREADY graph-generic, zero new
    constraint code required), THEN stashes `ctx.meta[:formulation] = :MeshedFlow` (D-08-style
    provenance the next plan's certificate reads), and returns `ctx`. Add
    `problem_class(::MeshedFlow) = SOCP()` (routes to the SAME tight-gap Clarabel factory
    `ConvexBranchFlow`/`RestrictedBranchFlow` already use). Docstring explains explicitly WHY
    there is no new model-time constraint here (D-03's resolution: cycle/loop consistency is
    realized as the NEW a-posteriori angle-recoverability certificate, plan 23-03 -- never a
    hard convex constraint, since a genuine loop-closure condition is a nonconvex
    trigonometric identity in angles this branch-flow model has already eliminated). Export
    `MeshedFlow`. Wire `src/TSODSO.jl`: insert `include("powerflow/MeshedFlow.jl")`
    immediately after `include("powerflow/RestrictedBranchFlow.jl")` (currently line 57),
    with a comment citing "plan 23-02, MESH-02" and the delegation dependency on
    `ConvexBranchFlow.jl`. Wire `docs/src/api.md`: append `"powerflow/MeshedFlow.jl"` to the
    "Power-Flow Formulations" `@autodocs` block's `Pages` list (line 69).
  </action>
  <acceptance_criteria>
    On an IDENTICAL small radial `Feeder` (2-bus, near-lossless), solving via `ConvexBranchFlow()`
    and via `MeshedFlow()` produces IDENTICAL `objective_value`/`dadp` (proving the delegation
    is byte-faithful, not merely "similar") and `ctx.meta[:formulation] == :MeshedFlow` only
    on the `MeshedFlow` solve. `problem_class(MeshedFlow()) isa SOCP`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e "using Test, TSODSO; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false)]; branches = [Branch(1,2,1e-3,1e-3,SMAX_NO_LIMIT)]; f = Feeder(buses, branches, 1); therm1 = Thermostatic(2,0.0,1.0,20.0,20.0,20.0,0.1,0.1,0.5,[20.0]); agg1 = Aggregator(2,0.95,[therm1],[0.0]); ctx1, w1, d1 = solve_welfare(f, ConvexBranchFlow(), [agg1]; T=1, λ₀=[4.0]); therm2 = Thermostatic(2,0.0,1.0,20.0,20.0,20.0,0.1,0.1,0.5,[20.0]); agg2 = Aggregator(2,0.95,[therm2],[0.0]); ctx2, w2, d2 = solve_welfare(f, MeshedFlow(), [agg2]; T=1, λ₀=[4.0]); @test isapprox(w1, w2; atol=1e-6); @test isapprox(d1, d2; atol=1e-6); @test ctx2.meta[:formulation] == :MeshedFlow; @test problem_class(MeshedFlow()) isa SOCP; println(\"OK\")"</automated>
  </verify>
  <done>
    `src/powerflow/MeshedFlow.jl` exists, exports `MeshedFlow`, is wired into `src/TSODSO.jl`
    and `docs/src/api.md`, and its delegation to `ConvexBranchFlow` is proven byte-faithful on
    a radial regression fixture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase23Fixtures -- the committed loop fixture, both profiles (D-02/D-10, MESH-02)</name>
  <files>test/fixtures_phase23.jl</files>
  <read_first>
    test/fixtures_phase22.jl (whole file -- the exact structural analog), src/data/MeshedFeeder.jl
    (plan 23-01, just landed), src/devices/Thermostatic.jl (constructor field order + T=1
    no-op recursion, confirmed in the interfaces block above), .planning/phases/23-meshed-networks/23-RESEARCH.md
    "Honest-Gap Fixture Design" section
  </read_first>
  <action>
    Create `test/fixtures_phase23.jl` as `@testmodule Phase23Fixtures`, mirroring
    `test/fixtures_phase22.jl`'s whole-file shape (header contract: self-contained, every
    feeder-consuming builder takes arguments, nothing evaluated at module load time).
    Constants: `const T_MESH = 1`; `const LAMBDA0_MESH = 4.0`; `const P2_LOAD = 0.30`;
    `const P3_LOAD = 0.05`; per-branch impedance literals for BOTH profiles, named
    (`UNIFORM_RX = [(0.01,0.02),(0.01,0.02),(0.01,0.02)]`,
    `HETEROGENEOUS_RX = [(0.04,0.01),(0.01,0.06),(0.02,0.02)]` -- exactly the ratios
    RESEARCH.md's spike measured: 4.0, ~0.167, 1.0). Define `mesh_feeder(profile::Symbol) ->
    MeshedFeeder`: buses `Bus(1,0.95,1.05,true)`, `Bus(2,0.90,1.10,false)`,
    `Bus(3,0.90,1.10,false)`; branches `(1,2)`, `(2,3)`, `(3,1)` with `(r,x)` pulled from
    `UNIFORM_RX`/`HETEROGENEOUS_RX` by `profile`, each `smax = SMAX_NO_LIMIT`; throw
    `ArgumentError` for any `profile` other than `:uniform`/`:heterogeneous`. Define
    `mesh_aggregators() -> Vector{&lt;:Aggregator}` returning TWO aggregators: bus 2 with ONE
    `Thermostatic(2, 0.0, 1.0, 20.0, 20.0, 20.0, P2_LOAD, P2_LOAD, 0.5, [20.0])` (pinned via
    `Pmin==Pmax`, `Tmin==Tmax==Tin0` collapses the comfort band to a point -- deterministic,
    asymmetric load), wrapped `Aggregator(2, 0.95, [therm2], [0.0])`; bus 3 symmetric with
    `P3_LOAD` in place of `P2_LOAD`. Define `mesh_lambda0() = [LAMBDA0_MESH]`. Export every
    constant and function (`T_MESH, LAMBDA0_MESH, P2_LOAD, P3_LOAD, UNIFORM_RX,
    HETEROGENEOUS_RX, mesh_feeder, mesh_aggregators, mesh_lambda0`).
  </action>
  <acceptance_criteria>
    `mesh_feeder(:uniform)` and `mesh_feeder(:heterogeneous)` both construct without throwing
    (`nB=3 > N-1=2` on both). `mesh_feeder(:bogus)` throws `ArgumentError`. The equivalent
    inline construction solves via `solve_welfare(mesh_feeder(:uniform), MeshedFlow(),
    mesh_aggregators(); T=1, λ₀=mesh_lambda0())` to `OPTIMAL` with `assert_socp_exact!`
    passing (no thrown error), and likewise for `mesh_feeder(:heterogeneous)` -- proving BOTH
    profiles are solvable/exact on the EXISTING cone-tightness gate before the next plan's NEW
    certificate is asked to distinguish them (Pitfall 14's point: cone-tightness alone cannot
    tell these two cases apart).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e "using Test, TSODSO; function feeder_for(profile) rx = profile==:uniform ? [(0.01,0.02),(0.01,0.02),(0.01,0.02)] : [(0.04,0.01),(0.01,0.06),(0.02,0.02)]; buses=[Bus(1,0.95,1.05,true),Bus(2,0.90,1.10,false),Bus(3,0.90,1.10,false)]; branches=[Branch(1,2,rx[1]...,SMAX_NO_LIMIT),Branch(2,3,rx[2]...,SMAX_NO_LIMIT),Branch(3,1,rx[3]...,SMAX_NO_LIMIT)]; return MeshedFeeder(buses,branches,1) end; function aggs() therm2=Thermostatic(2,0.0,1.0,20.0,20.0,20.0,0.30,0.30,0.5,[20.0]); therm3=Thermostatic(3,0.0,1.0,20.0,20.0,20.0,0.05,0.05,0.5,[20.0]); return [Aggregator(2,0.95,[therm2],[0.0]), Aggregator(3,0.95,[therm3],[0.0])] end; for profile in (:uniform,:heterogeneous) ctx,w,d = solve_welfare(feeder_for(profile), MeshedFlow(), aggs(); T=1, λ₀=[4.0]); @test isfinite(w); println(profile, \" socp_maxgap=\", ctx.meta[:socp_maxgap]) end; println(\"OK\")"</automated>
  </verify>
  <done>
    `test/fixtures_phase23.jl` exists, exports the documented symbols, and both impedance
    profiles are solvable/exact via `MeshedFlow` -- ready for `setup=[Phase23Fixtures]` in
    every downstream Phase-23 `@testitem`.
  </done>
</task>

<task type="auto">
  <name>Task 3: test/test_mesh_flow.jl (MESH-02)</name>
  <files>test/test_mesh_flow.jl</files>
  <read_first>
    test/test_mesh_feeder.jl (plan 23-01 -- the sibling test's qualification style),
    src/models/welfare_solve.jl (the `solve_welfare` return-tuple contract)
  </read_first>
  <action>
    Create `test/test_mesh_flow.jl` with `setup=[Phase23Fixtures]`, one `@testitem
    "MeshedFlow solves the loop fixture via solve_welfare on both impedance profiles
    (MESH-02)" begin using TSODSO, Test; ... end` asserting, for BOTH `:uniform` and
    `:heterogeneous` profiles built via `Phase23Fixtures.mesh_feeder(profile)`: `solve_welfare`
    returns without throwing (i.e. `assert_solved!`/`assert_socp_exact!` both pass);
    `ctx.meta[:formulation] == :MeshedFlow`; `length(dadp) == Phase23Fixtures.T_MESH`. Also
    assert the D-09-adjacent defense-in-depth check at the flow level: constructing
    `TSODSO.Feeder` (not `MeshedFeeder`) on the SAME 3-branch triangle edge list still throws
    `ArgumentError` (re-affirms the radial gate is untouched, even after `MeshedFlow` has been
    exercised).
  </action>
  <acceptance_criteria>
    The file defines exactly one `@testitem` with `setup=[Phase23Fixtures]`; when run via the
    phase's full-suite gate (plan 23-04) it passes with zero failures.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e "using Test, TSODSO; function feeder_for(profile) rx = profile==:uniform ? [(0.01,0.02),(0.01,0.02),(0.01,0.02)] : [(0.04,0.01),(0.01,0.06),(0.02,0.02)]; buses=[Bus(1,0.95,1.05,true),Bus(2,0.90,1.10,false),Bus(3,0.90,1.10,false)]; branches=[Branch(1,2,rx[1]...,SMAX_NO_LIMIT),Branch(2,3,rx[2]...,SMAX_NO_LIMIT),Branch(3,1,rx[3]...,SMAX_NO_LIMIT)]; return buses,branches end; for profile in (:uniform,:heterogeneous) buses,branches = feeder_for(profile); mf = MeshedFeeder(buses,branches,1); therm2=Thermostatic(2,0.0,1.0,20.0,20.0,20.0,0.30,0.30,0.5,[20.0]); therm3=Thermostatic(3,0.0,1.0,20.0,20.0,20.0,0.05,0.05,0.5,[20.0]); ctx,w,d = solve_welfare(mf, MeshedFlow(), [Aggregator(2,0.95,[therm2],[0.0]),Aggregator(3,0.95,[therm3],[0.0])]; T=1, λ₀=[4.0]); @test ctx.meta[:formulation]==:MeshedFlow; @test length(d)==1; @test_throws ArgumentError Feeder(buses,branches,1) end; println(\"OK\")"</automated>
  </verify>
  <done>
    `test/test_mesh_flow.jl` exists with exactly one `@testitem` exercising MESH-02 on both
    fixture profiles, ready for TestItemRunner discovery at the phase's full-suite gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `MeshedFlow.contribute!` -> `ConvexBranchFlow.contribute!` | Delegation surface -- a divergence here would silently duplicate (and risk drifting from) the SOC cone / exactness-copy math |
| `Phase23Fixtures.mesh_feeder(profile)` | Untrusted `profile::Symbol` input crosses into branch construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | `MeshedFlow.contribute!` | mitigate | Byte-faithful delegation proven by Task 1's radial-regression verify (identical objective/dadp between `ConvexBranchFlow` and `MeshedFlow` on the same feeder) -- no bespoke constraint-writing code exists to drift |
| T-23-04 | Tampering | `mesh_feeder(profile::Symbol)` | mitigate | Construction-time `ArgumentError` on any `profile` outside `{:uniform, :heterogeneous}` -- a malformed profile symbol can never silently fall through to an undefined impedance tuple |

No new package installs this phase (RESEARCH.md Package Legitimacy Audit: "Not applicable" --
zero new runtime dependencies) -- the SC (supply-chain) row is intentionally omitted.
</threat_model>

<verification>
All three tasks' `&lt;verify&gt;` scripts pass under `julia --project=.` (no TestItemRunner, no
bare `include()` of the `@testmodule`/`@testitem` files -- both are inline-duplicated). Confirm
`git diff --stat` touches only this plan's `files_modified` paths.
</verification>

<success_criteria>
`MeshedFlow` solves through `solve_welfare` unmodified via ordinary dispatch, proven
byte-faithful to `ConvexBranchFlow` on a radial regression. The committed loop fixture
(`Phase23Fixtures`) exists with both impedance profiles, each solvable/exact via the EXISTING
cone-tightness gate -- setting up plan 23-03's certificate to be the FIRST thing that can tell
them apart.
</success_criteria>

<output>
Create `.planning/phases/23-meshed-networks/23-02-SUMMARY.md` when done.
</output>
