---
phase: 23-meshed-networks
plan: 4
type: execute
wave: 4
depends_on: ["23-03"]
files_modified:
  - docs/literate/meshed_reactive_price.jl
  - docs/make.jl
autonomous: true
requirements: [MESH-06]

must_haves:
  truths:
    - "A live-executed literate rung page documents the meshed formulation (MESH-02), the
       angle-recoverability certificate's two verdicts on the committed fixture (MESH-03),
       and the live reactive price via a 4Q-BESS device on the meshed loop (MESH-06/D-04) --
       every number on the page is recomputed at Documenter build time."
    - "The reactive price is read as the genuine centralized dual of the meshed network's own
       balance_q closure -- no meshed ADMM is built this rung (D-04); the page explicitly
       references Phase 19's live radial mu-ascent by name rather than re-deriving it."
    - "The anti-feature is honored: no IEEE-1547 Volt-VAR droop controller is built anywhere
       on this page (D-11); the meshed-IEEE-13-with-tie-switch quarantined variant is
       explicitly noted as NOT built this phase (D-02/D-12 -- deferred, MESH-STRETCH)."
    - "The full test suite is green (or shows only the pre-existing known-false failures
       already documented in STATE.md/CONTEXT.md) after this phase's additions."
  artifacts:
    - path: "docs/literate/meshed_reactive_price.jl"
      provides: "Rung 10 -- Meshed Networks + Live Reactive Price literate page"
      min_lines: 60
    - path: "docs/make.jl"
      provides: "literate-list registration + Models page entry for
        meshed_reactive_price.jl"
      contains: "meshed_reactive_price.jl"
  key_links:
    - from: "docs/literate/meshed_reactive_price.jl"
      to: "src/models/mesh_angle_certificate.jl"
      via: "r_u = certify_angle_recoverable!(ctx_uniform); r_h =
        certify_angle_recoverable!(ctx_heterogeneous)"
      pattern: "certify_angle_recoverable!"
    - from: "docs/make.jl"
      to: "docs/literate/meshed_reactive_price.jl"
      via: "Literate.markdown(...) render + pages list entry"
      pattern: "meshed_reactive_price"
---

<objective>
Close the phase with a live-executed literate rung page (MESH-06) documenting the meshed
formulation (MESH-02), both angle-recoverability verdicts on the committed fixture (MESH-03),
and the 4Q-BESS live reactive price on the SAME meshed loop via the centralized `:balance_q`
dual (D-04) -- mirroring `stochastic_pv_demand.jl`'s (Rung 9) structure. Then run the phase's
full-suite acceptance gate, mirroring the exact closing-plan convention Phases 20/21/22
already established.

Purpose: the phase's final deliverable -- a researcher-facing, self-verifying document -- and
the project-wide regression gate before the phase is considered shipped.
Output: `docs/literate/meshed_reactive_price.jl` (new), `docs/make.jl` (wired), a green
`Pkg.test()` run, a successful `docs/make.jl` build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-meshed-networks/23-CONTEXT.md
@.planning/phases/23-meshed-networks/23-RESEARCH.md
@.planning/phases/23-meshed-networks/23-03-SUMMARY.md
@docs/literate/stochastic_pv_demand.jl

<interfaces>
From docs/literate/stochastic_pv_demand.jl (Rung 9, whole file already read in full during
phase planning -- mirror its shape exactly): a `# # Rung N -- Title` opening comment block
framing what is genuinely NEW relative to every prior rung; `using TSODSO`; each subsequent
`# ##` numbered section runs ONE live computation and immediately shows a bare
expression/value on its own line (Documenter's `@example`-block convention); a closing
`## Finding` section stating the measured result PLAINLY, never smoothed over.

From docs/make.jl (the exact two edit points): the `for src in (...)` tuple listing every
literate source (add `"meshed_reactive_price.jl"` as the LAST entry, trailing comment
`# NEW: Rung 10 Meshed Networks + Live Reactive Price (MESH-01..03,06)`, mirroring the
`"stochastic_pv_demand.jl"` entry's own comment style); the `pages = [...]` "Models" list
(add `"Rung 10: Meshed Networks + Live Reactive Price" =>
"generated/meshed_reactive_price.md"` as the LAST entry, mirroring the `"Rung 9: Stochastic
PV/Demand Uncertainty"` entry).

From plans 23-01..23-03 (already on disk): `MeshedFeeder(buses, branches, root)`,
`MeshedFlow()`, `certify_angle_recoverable!(ctx; report=true)` returning `(; recoverable,
worst_residual, status, angles)`, and `Phase23Fixtures`'s committed 3-bus triangle design
(`:uniform`/`:heterogeneous` R/X profiles, asymmetric pinned loads `P2_LOAD=0.30`,
`P3_LOAD=0.05` at buses 2/3) -- the literate page reconstructs this SAME small fixture
inline (never `include()`s the test `@testmodule`, mirroring every prior literate page's own
self-contained construction).

FourQuadBESS constructor + scale (mirrors test/fixtures_phase19.jl's own committed values,
the project's standard battery price triple): `FourQuadBESS(bus, η, Δt, Pch_max, Pdch_max,
Smax, Emin, Emax, soc0, λ_min, λ_med, λ_max)` with `η=0.95, Δt=1.0, Pch_max=Pdch_max=0.05,
Smax=0.08, Emin=0.0, Emax=0.2, soc0=0.1, λ_min=3.8, λ_med=6.2, λ_max=8.9`. It returns `(;
vars, p_inject, q_inject, utility)` from `contribute!`; the `Aggregator` rolls its
`q_inject` additively into `:Rq` (byte-identical to zero when absent elsewhere).

`solve_welfare(feeder, pf, aggregators; T, λ₀) -> (ctx, objective_value, dadp)`; the reactive
DADP at a given bus is `dual.(ctx.constraints[:balance_q][bus, :])` -- the genuine convex
dual of the meshed network's own reactive nodal balance (`welfare_solve.jl`'s
`register_constraint!(ctx, :balance_q, balance_q)`, present whenever the formulation
allocates `:Rq` -- `ConvexBranchFlow`/`MeshedFlow` both do).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/literate/meshed_reactive_price.jl -- Rung 10 + docs/make.jl wiring (MESH-06)</name>
  <files>docs/literate/meshed_reactive_price.jl, docs/make.jl</files>
  <read_first>
    docs/literate/stochastic_pv_demand.jl (whole file -- the structural template, per this
    plan's interfaces block), docs/make.jl (the two edit points), src/models/mesh_angle_certificate.jl
    (plan 23-03's final docstring/measured tolerances, for accurate prose), src/devices/FourQuadBESS.jl
    (constructor + field docstrings)
  </read_first>
  <action>
    Create `docs/literate/meshed_reactive_price.jl` mirroring `stochastic_pv_demand.jl`'s
    whole structure. Opening comment block: `# # Rung 10 -- Meshed Networks + Live Reactive
    Price`, framing prose explaining that every prior rung page solved a RADIAL feeder; this
    page fills the SEAM-01 meshed slot (MESH-01/02/03) with a genuine loop topology solved
    through the SAME `ConvexBranchFlow` constraint set via `MeshedFlow`'s delegation (D-03),
    validated by the NEW angle-recoverability certificate rather than the per-branch cone
    gate alone (Pitfall 14), combined with Phase 19's 4Q-BESS reactive price (MESH-06/D-04).
    `using TSODSO`. Section `## Building the 3-bus loop fixture (D-02/D-10)`: construct the
    triangle inline for BOTH `:uniform` and `:heterogeneous` impedance profiles (the exact
    `Phase23Fixtures` literals -- asymmetric loads `P2=0.30`, `P3=0.05`, ratios 4.0/0.167/1.0
    for the heterogeneous case), with a one-line comment per profile citing the R/X-ratio
    design choice as qualitative and structural, never a tuned knife-edge (D-10/Pitfall 15).
    Section `## 1. Solving via MeshedFlow (MESH-02)`: solve both profiles via `solve_welfare`,
    show `termination_status`/`objective_value`/`ctx.meta[:socp_maxgap]` for BOTH -- note in
    prose that both pass the EXISTING per-branch cone gate identically (Pitfall 14's point
    made concrete: this gate alone cannot distinguish them). Section `## 2. The
    angle-recoverability certificate (MESH-03, D-05/D-06/D-07)`: call
    `certify_angle_recoverable!` on both `ctx`s (default `report=true`), show
    `status`/`recoverable`/`worst_residual` for both; state plainly (D-10): uniform -> a
    genuine AC-recoverable operating point, angles returned and certified; heterogeneous ->
    the SOCP value is a valid LOWER BOUND ONLY, never a certified AC point -- reported, not
    thrown. Section `## 3. Live reactive price on the meshed loop (MESH-06/D-04)`: rebuild the
    bus-2 aggregator with a `FourQuadBESS` (the scale/triple from the interfaces block) ADDED
    alongside its existing `Thermostatic`, re-solve the `:uniform` profile via `MeshedFlow`,
    show `dual.(ctx.constraints[:balance_q][2, :])` as the reactive DADP at bus 2 -- prose
    explicitly notes this is the CENTRALIZED analog of Phase 19's LIVE radial mu-ascent
    (D-04: no meshed ADMM is built this rung; the dual is a genuine convex dual of the SOLVED
    meshed SOCP either way). Closing `## Finding` section: state the qualitative R/X
    structural gap plainly (D-10), the anti-feature honored (D-11 -- no IEEE-1547 Volt-VAR
    droop controller anywhere; whatever `q(v)` the SOCP itself chose is characterized here
    post-hoc only, never a control law), and the explicit scope note that a
    meshed-IEEE-13-with-tie-switch quarantined variant (D-02/D-12, MESH-STRETCH) was NOT built
    this phase -- the single small loop fixture with its two impedance profiles is the
    phase's sole committed evidence, sufficient for the ROADMAP Phase 23 success criterion
    "at least one committed meshed fixture." Wire `docs/make.jl`: add
    `"meshed_reactive_price.jl"` as the LAST entry in the `for src in (...)` literate-source
    tuple (trailing comment `# NEW: Rung 10 Meshed Networks + Live Reactive Price
    (MESH-01..03,06)`); add `"Rung 10: Meshed Networks + Live Reactive Price" =>
    "generated/meshed_reactive_price.md"` as the LAST entry in the `pages = [...]` "Models"
    sub-list.
  </action>
  <acceptance_criteria>
    `docs/literate/meshed_reactive_price.jl` exists, builds the loop fixture on both impedance
    profiles, calls `certify_angle_recoverable!` on both, demonstrates the 4Q-BESS reactive
    price via a real `:balance_q` dual, and shows a `## Finding` section -- every displayed
    value is a live expression, never a hand-typed literal. `docs/make.jl`'s literate-source
    tuple and `pages` Models list both reference `meshed_reactive_price.jl`/`.md`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e "using TSODSO; feeder_for(profile) = (rx = profile==:uniform ? [(0.01,0.02),(0.01,0.02),(0.01,0.02)] : [(0.04,0.01),(0.01,0.06),(0.02,0.02)]; buses=[Bus(1,0.95,1.05,true),Bus(2,0.90,1.10,false),Bus(3,0.90,1.10,false)]; branches=[Branch(1,2,rx[1]...,SMAX_NO_LIMIT),Branch(2,3,rx[2]...,SMAX_NO_LIMIT),Branch(3,1,rx[3]...,SMAX_NO_LIMIT)]; MeshedFeeder(buses,branches,1)); bess = FourQuadBESS(2,0.95,1.0,0.05,0.05,0.08,0.0,0.2,0.1,3.8,6.2,8.9); therm2 = Thermostatic(2,0.0,1.0,20.0,20.0,20.0,0.30,0.30,0.5,[20.0]); therm3 = Thermostatic(3,0.0,1.0,20.0,20.0,20.0,0.05,0.05,0.5,[20.0]); ctx,w,d = solve_welfare(feeder_for(:uniform), MeshedFlow(), [Aggregator(2,0.95,[therm2,bess],[0.0]), Aggregator(3,0.95,[therm3],[0.0])]; T=1, λ₀=[4.0]); r = certify_angle_recoverable!(ctx; report=true); q_dadp = dual.(ctx.constraints[:balance_q][2,:]); @assert r.status == :angle_certified; @assert length(q_dadp) == 1; println(\"q_dadp=\", q_dadp); println(\"OK\")"</automated>
  </verify>
  <done>
    `docs/literate/meshed_reactive_price.jl` exists with the documented structure and
    `docs/make.jl` is wired at both edit points; the inline-verified `MeshedFlow`/
    `certify_angle_recoverable!`/`FourQuadBESS` call used in the page's own construction
    solves and certifies successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite acceptance gate</name>
  <files></files>
  <read_first>
    .planning/phases/23-meshed-networks/23-01-SUMMARY.md through 23-03-SUMMARY.md (the
    cumulative new-@testitem count this phase added, for the expected passed-count delta),
    STATE.md's test-invocation hazard note (NEVER `julia --project=test -e
    '...@run_package_tests...'`; ALWAYS `julia --project=. -e 'import Pkg; Pkg.test()'`)
  </read_first>
  <action>
    Run `julia --project=. -e 'import Pkg; Pkg.test()'` in the background (~12-20 minutes --
    the ONLY entrypoint that actually executes `@testitem` bodies; TestItemRunner cannot be
    invoked directly under `--project=.`). While it runs, tally the total number of NEW
    `@testitem`s added across plans 23-01 through 23-03 (grep `@testitem` counts across
    `test/test_mesh_feeder.jl`, `test/test_mesh_flow.jl`, `test/test_mesh_angle_certificate.jl`
    -- expected 3, one per file per this phase's plans). Once the run completes, confirm: `0`
    failed, the SAME pre-existing errored/broken counts as the documented green reference
    (2752 passed / 0 failed / 3 pre-existing errored / 3 broken, per this phase's planning
    context -- +the known-false Aqua pair on a drifted checkout, +the documented D-06
    stochastic sandbox-skew diagnostic, neither a regression), and `passed` equal to that
    baseline PLUS the tallied new-item count. If the failed count is nonzero, or the
    errored/broken counts differ from the documented baseline, STOP and diagnose -- do NOT
    close the phase on a red suite. Then build the docs (`julia --project=docs docs/make.jl`)
    and confirm the build completes without error (this exercises `checkdocs = :exports`
    against every symbol this phase exported -- `MeshedFeeder`, `assert_connected`,
    `MeshedFlow`, `certify_angle_recoverable!` -- a gap here means an exported symbol was
    missed in `docs/src/api.md`'s `@autodocs` `Pages` lists across plans 23-01/23-02/23-03;
    fix it as part of THIS task if found, since this is the phase's own closing
    docs-integrity gate).
  </action>
  <acceptance_criteria>
    The background `Pkg.test()` run's final summary line reports `0` failed; the
    errored/broken counts match the documented pre-phase baseline unchanged; the passed count
    increased by exactly the tallied new-`@testitem` count for this phase (documented in the
    SUMMARY). `julia --project=docs docs/make.jl` completes without error.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <done>
    Full suite green: `0` failed, pre-existing errored/broken counts unchanged, passed count
    matches the pre-phase baseline plus this phase's tallied new-item count. The Documenter
    build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live-executed literate page -> rendered docs site | The page's numbers must be genuinely recomputed at build time, never a stale/hand-typed value that could silently drift from the real `src/` code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Repudiation | `docs/literate/meshed_reactive_price.jl`'s displayed numbers | mitigate | Documenter's `@example`-block execution (via `Literate.DocumenterFlavor()`) recomputes every shown value at build time -- no hand-typed literal can silently drift from the real `src/` code (mirrors every prior rung page's own threat disposition) |
| T-23-08 | Denial of Service | The Documenter build fails on `checkdocs = :exports` if any exported Phase-23 symbol was never wired into `docs/src/api.md` | mitigate | Task 2's own `julia --project=docs docs/make.jl` run is the closing docs-integrity gate -- any gap discovered here is fixed within this same task, not deferred |

No new package installs this phase -- the SC row is intentionally omitted.
</threat_model>

<verification>
Task 1's verify script passes under `julia --project=.`. Task 2's full-suite verify
(`julia --project=. -e 'import Pkg; Pkg.test()'`) is the phase's closing gate -- the only
plan in this phase permitted to run it, per the orchestrator's "reserve the full suite for
the final acceptance plan only" constraint.
</verification>

<success_criteria>
`docs/literate/meshed_reactive_price.jl` exists and is wired into `docs/make.jl`. The full
test suite is green (0 failed, pre-existing errored/broken counts unchanged, passed count
grown by exactly this phase's new-item tally). The Documenter build succeeds. Every one of
MESH-01/02/03/06 is demonstrably satisfied across the four plans of this phase.
</success_criteria>

<output>
Create `.planning/phases/23-meshed-networks/23-04-SUMMARY.md` when done.
</output>
