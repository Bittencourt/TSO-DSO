---
phase: 20-overvoltage-capable-relaxation
plan: 05
type: execute
wave: 5
depends_on: ["20-04"]
files_modified:
  - docs/literate/restricted_branch_flow.jl
  - docs/make.jl
autonomous: true
requirements: [OVR-04]

must_haves:
  truths:
    - "A live-executed literate rung page documents the restriction mechanism, its measured optimality loss, and the fallback semantics beside the Gan & Low condition it implements (D-12)."
    - "The page shows the Gan-Low C1 condition and the ε modification-gap measurement as their OWN narrative subsection, beyond the ac_oracle.jl template's existing shape (D-12 new requirement)."
    - "Every number shown on the page is RECOMPUTED live at doc-build time, never hardcoded from a prior test run — the same discipline docs/literate/ac_oracle.jl already follows."
    - "The full test suite remains green end to end, confirming zero default-path drift across the whole phase (OVR-01..04 acceptance)."
  artifacts:
    - path: "docs/literate/restricted_branch_flow.jl"
      provides: "The OVR-04 literate rung page: fixture -> ConvexBranchFlow(rtol_exact=1.0) diagnostic -> ACPowerFlow ground truth -> RestrictedBranchFlow(ε) -> assert_socp_exact! free check -> assert_restriction_exact! certificate -> Gan-Low condition narrative -> fallback semantics -> Finding"
      contains: "# # Rung"
    - path: "docs/make.jl"
      provides: "restricted_branch_flow.jl registered in both the Literate.markdown loop and the pages list"
      contains: "restricted_branch_flow.jl"
  key_links:
    - from: "docs/literate/restricted_branch_flow.jl"
      to: "src/powerflow/RestrictedBranchFlow.jl"
      via: "solve_welfare(feeder, RestrictedBranchFlow(), aggs; ...) executed live during makedocs"
      pattern: "RestrictedBranchFlow\\(\\)"
    - from: "docs/make.jl"
      to: "docs/literate/restricted_branch_flow.jl"
      via: "Literate.markdown(joinpath(LITERATE_DIR, \"restricted_branch_flow.jl\"), GENERATED_DIR; flavor = Literate.DocumenterFlavor())"
      pattern: "restricted_branch_flow.jl"
---

<objective>
Implement OVR-04: a live-executed literate rung page documenting the full Phase 20 mechanism —
the restriction, its measured optimality loss, and the fallback semantics — beside the Gan & Low
theorem/condition it implements, mirroring `docs/literate/ac_oracle.jl`'s established structure
plus a NEW "Gan-Low condition" subsection D-12 requires beyond that template. Close the phase with
a full-suite acceptance run confirming zero default-path drift across all four prior plans.

Purpose: this is the phase's final deliverable and the standing project requirement ("rich,
literate, thesis-traceable documentation... a hard requirement, not optional" — CLAUDE.md). Every
number on the page must be a REAL, re-executed number (Documenter's `@example` blocks run live
during `makedocs`), so the page can never silently drift from the `src/` code it documents.

Output: `docs/literate/restricted_branch_flow.jl` (new), `docs/make.jl` gains the new page in both
the `Literate.markdown` loop and the `pages` list, and a full-suite + docs-build acceptance pass
closes the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-overvoltage-capable-relaxation/20-CONTEXT.md
@.planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md
@.planning/phases/20-overvoltage-capable-relaxation/20-PATTERNS.md
@.planning/phases/20-overvoltage-capable-relaxation/20-01-SUMMARY.md
@.planning/phases/20-overvoltage-capable-relaxation/20-02-SUMMARY.md
@.planning/phases/20-overvoltage-capable-relaxation/20-03-SUMMARY.md
@.planning/phases/20-overvoltage-capable-relaxation/20-04-SUMMARY.md

@docs/literate/ac_oracle.jl
@docs/make.jl
</context>

<interfaces>
From `docs/literate/ac_oracle.jl` (verified, the section-shape template this new page mirrors —
copy the fixture-construction block VERBATIM, since literate pages cannot load test-only
`Phase4Fixtures`):
```
# # Rung N — <Title>
using TSODSO
const T = 24
mem_price = Float64[...]         # digitized MEM price profile
temperature = Float64[...]       # digitized exterior temperature
buses = [Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false), Bus(3, 0.95, 1.05, false)]
branches = [Branch(1, 2, 0.05, 0.05, 99.0), Branch(2, 3, 0.05, 0.05, 99.0)]
feeder = Feeder(buses, branches, 1)
pv_scale = 1.2
aggs = map(2:length(feeder.buses)) do bus ... end
ctx_socp, cost_socp, _ = solve_welfare(feeder, ConvexBranchFlow(), aggs; T=T, λ₀=mem_price,
    allow_export=true, rtol_exact=1.0)
ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T=T, λ₀=mem_price,
    allow_local=true, allow_export=true)
report = assert_ac_exact!(ctx_socp, ctx_ac; rtol=1e-4, atol=1e-6)
```

From `docs/make.jl` (verified, exact registration points this plan edits):
```
for src in (
    "toy_dc.jl", "lindistflow.jl", "convex_branch_flow.jl",
    "ac_oracle.jl",             # NEW: Rung 3 AC-exactness oracle (EXACT-04)
    "prosumer_welfare.jl", ...
)
    Literate.markdown(joinpath(LITERATE_DIR, src), GENERATED_DIR; flavor = Literate.DocumenterFlavor())
end
...
pages = [
    "Home" => "index.md",
    "Models" => [
        "Rung 0: Toy DC" => "generated/toy_dc.md",
        ...
        "Rung 3: AC-Exactness Oracle" => "generated/ac_oracle.md",
        ...
    ],
    ...
]
```
Add `"restricted_branch_flow.jl"` to the `for src in (...)` tuple (a natural position: right after
`"ac_oracle.jl"`, since this page builds directly on it) and
`"Rung 3: Overvoltage-Capable Restriction" => "generated/restricted_branch_flow.md"` to the
`"Models"` pages list, right after the `"Rung 3: AC-Exactness Oracle"` entry.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: docs/literate/restricted_branch_flow.jl + docs/make.jl registration</name>
  <files>docs/literate/restricted_branch_flow.jl, docs/make.jl</files>
  <read_first>
    - docs/literate/ac_oracle.jl (full file — the exact fixture-construction block this new page
      copies verbatim, and the "## Finding" closing-section convention)
    - docs/make.jl (full file — the two exact registration points this task edits)
    - .planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md, "Architecture Patterns"
      section (Theorem 1/2, Lemma 1, Definition 3/eq. 18 — the exact citations for the new
      "Gan-Low condition" subsection) and "Open Questions #2" (the C1-margin numeric check this
      page computes live, per RESEARCH.md's own recommendation to place it here rather than in an
      earlier plan)
    - .planning/phases/20-overvoltage-capable-relaxation/20-01..04-SUMMARY.md (the measured
      values — `ε_measured`, `socp_maxgap`, certificate `rtol`/`atol`, fallback spread — this page
      narrates; confirm each number is RECOMPUTED live in the page's own code, not pasted from
      the summaries as a hardcoded literal)
  </read_first>
  <action>
    Create `docs/literate/restricted_branch_flow.jl` following `docs/literate/ac_oracle.jl`'s
    exact section shape, with these sections in order: (1) `# # Rung 3 — Overvoltage-Capable
    Relaxation: A Gan-Low OPF-ε Restriction` header + 1-2 paragraphs motivating from the previous
    page's documented EXACT-04 finding (cite Gan, Li, Topcu & Low 2015, IEEE TAC 60(1):72-87,
    arXiv:1311.7170 — Theorem 1/2, Lemma 1, Definition 3). (2) `# ## Building the high-PV stress
    fixture` — copy `docs/literate/ac_oracle.jl`'s fixture-construction block VERBATIM (same
    `mem_price`/`temperature`/`buses`/`branches`/`feeder`/`aggs` with `pv_scale = 1.2`). (3) `# ##
    The Gan-Low condition` — a NEW subsection (D-12's requirement beyond the `ac_oracle.jl`
    template): state Theorem 1's C1/C2 conditions in prose, state that C2 is exactly what fails at
    EXACT-04 (voltage pinned at `V²max`), and state Theorem 2/OPF-ε's fix (shrink `v`'s own bound
    by `ε`). Live-compute the C1 margin on this fixture's actual parameters (RESEARCH.md Open
    Question #2's recommendation: "compute A₁·u₂ numerically... confirm strict positivity... a
    citable 'C1 margin' number") — since the paper's exact linear-chain worked-example formula for
    a 2-branch chain was not independently re-derived by this project's own research pass, if you
    cannot derive the precise `A₁·u₂` expression with confidence from RESEARCH.md alone, substitute
    a clearly-labeled, honestly-scoped alternative live computation instead: recompute this page's
    own measured `ε` (mirroring plan 20-01 Task 2's `recover_lossfree_shadow_voltage`-based
    recipe, inlined here) and state explicitly in prose that C1's FULL symbolic verification is
    deferred (cite it as a literature-grounded expectation per RESEARCH.md's Section VI empirical
    finding that C1 holds broadly on IEEE test feeders, not as an independently re-derived proof on
    THIS fixture) — do not fabricate a numeric "C1 margin" figure that was not actually derived
    from the paper's formula. (4) `# ## Solving all three formulations on the same data` —
    `ConvexBranchFlow(rtol_exact=1.0)` diagnostic, `ACPowerFlow()` ground truth,
    `RestrictedBranchFlow()` (default `ε`, no override — proving the measured default from plan
    20-01/20-02 works standalone). (5) `# ## The free PF-04 signal` — show
    `ctx_restricted.meta[:socp_maxgap]` live, contrasted in prose against `ctx_socp.meta[:socp_maxgap]`
    from the diagnostic `ConvexBranchFlow` solve (the "collapses from ≈10.4 to ~1e-7" narrative).
    (6) `# ## The OVR-02 certificate` — call `assert_restriction_exact!(ctx_restricted, ctx_ac;
    unrestricted_cost = cost_socp)` live, print its full returned NamedTuple. (7) `# ## Fallback
    semantics` — prose-only subsection (do NOT actually trigger the fallback if the certificate
    passed, per D-09) explaining `ac_dual_fallback_price`'s `price_status = :local_ac_dual`
    contract and citing the quarantined spike's verdict from plan 20-04's summary as evidence the
    mechanism is sound when it IS needed. (8) `# ## Finding` — closing prose stating the phase's
    result: EXACT-04 is now priceable via a genuine restriction, the measured optimality loss
    (from step 6's certificate), and when the fallback semantics apply — mirroring
    `docs/literate/ac_oracle.jl`'s closing-section tone exactly.

    In `docs/make.jl`: add `"restricted_branch_flow.jl",` to the `for src in (...)` tuple
    immediately after the existing `"ac_oracle.jl",` entry, with the SAME inline comment style
    (`# NEW: Rung 3 overvoltage-capable restriction (OVR-01..04)`). Add
    `"Rung 3: Overvoltage-Capable Restriction" => "generated/restricted_branch_flow.md",`
    immediately after the existing `"Rung 3: AC-Exactness Oracle" => "generated/ac_oracle.md",`
    entry in the `"Models"` pages list.
  </action>
  <verify>
    <automated>julia --project=docs docs/make.jl 2>&1 | tail -60</automated>
  </verify>
  <done>docs/make.jl runs to completion; docs/src/generated/restricted_branch_flow.md is produced with every shown number computed live during the build (no `checkdocs`/`cross_references` build failure attributable to this page).</done>
  <acceptance_criteria>
    - `grep -n '# ## The Gan-Low condition' docs/literate/restricted_branch_flow.jl` matches
    - `grep -n 'RestrictedBranchFlow()' docs/literate/restricted_branch_flow.jl` matches (default
      kwarg, no explicit `ε` override — proving the measured default is self-sufficient)
    - `grep -n 'assert_restriction_exact!' docs/literate/restricted_branch_flow.jl` matches
    - `grep -n 'restricted_branch_flow' docs/make.jl` matches at least twice (the `Literate.markdown` loop entry and the `pages` list entry)
    - `julia --project=docs docs/make.jl` exits 0 and `ls docs/src/generated/restricted_branch_flow.md` exists after the run
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-suite acceptance — zero default-path drift across the whole phase</name>
  <files></files>
  <read_first>
    - .planning/STATE.md (the documented full-suite green baseline: "2513 passed / 0 failed / 3
      pre-existing broken" after Phase 19 — this task's own full-suite run must match that baseline
      PLUS this phase's new items, with no NEW failures)
    - test/test_restricted_branch_flow.jl (all seven `@testitem`s from plans 20-01..04, confirming
      the file's final shape before the whole-suite run)
  </read_first>
  <action>
    Run the FULL test suite in the background (per the project's mandatory verification-tooling
    constraint: `julia --project=. -e 'import Pkg; Pkg.test()'`, NOT `--project=test`, NOT
    `@run_package_tests` under the wrong project — this is the one designated place in the whole
    phase where the full-suite entrypoint is appropriate, since it is the FINAL acceptance gate for
    all five plans, not a per-task quick check). Confirm the pass count is AT LEAST the pre-phase
    baseline (2513 passed) plus the 7 new `test_restricted_branch_flow.jl` items (i.e. at least
    2520 passed), with 0 NEW failures (the same pre-existing 3 broken / Aqua drift entries are
    acceptable, no additional failures are). If any regression appears in a file this phase did NOT
    touch, STOP and investigate before considering the phase complete — that would indicate an
    unintended side effect of one of this phase's `TSODSO.jl` include-list edits (plans 20-02/03/04
    each added exactly one line to that shared file).
  </action>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()' 2>&1 | tail -40</automated>
  </verify>
  <done>Full test suite passes with 0 new failures beyond the documented 3 pre-existing broken/Aqua-drift items; pass count is at least the pre-phase baseline plus the 7 new test_restricted_branch_flow.jl items.</done>
  <acceptance_criteria>
    - `Pkg.test()` output ends with a summary line reporting 0 new failures relative to the
      documented pre-phase baseline
    - The reported pass count is greater than or equal to 2520 (2513 baseline + 7 new items)
    - No test file this phase did not touch shows a NEW failure
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documenter/Literate build execution | Runs entirely locally/in CI over researcher-authored code; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Repudiation | Literate page shows a hardcoded/pasted number instead of a live-recomputed one, silently drifting from `src/` over time | mitigate | Task 1's action explicitly forbids pasted literals from the SUMMARYs; every shown number must come from a live `@example` block execution, matching `docs/literate/ac_oracle.jl`'s own established discipline |
| T-20-14 | Tampering | The new page fabricates a "C1 margin" number not actually derivable from RESEARCH.md's citations | mitigate | Task 1's action explicitly requires an honest fallback narrative (measured ε recomputation + a stated literature-grounded expectation) rather than a fabricated figure if the precise C1 formula cannot be confidently derived |
| T-20-15 | Denial of Service | `docs/make.jl`'s `checkdocs = :exports` build gate fails because a newly-exported symbol (`RestrictedBranchFlow`, `assert_restriction_exact!`, `ac_dual_fallback_price`, `recover_lossfree_shadow_voltage`) has no docstring surfaced in `api.md` | mitigate | every new exported symbol across plans 20-01..04 was given a full docstring (per each plan's action); `api.md`'s existing `@autodocs` block already sweeps the whole module, requiring no per-symbol registration |
</threat_model>

<verification>
Run `julia --project=docs docs/make.jl` — exits 0, `docs/src/generated/restricted_branch_flow.md`
is produced. Run `julia --project=. -e 'import Pkg; Pkg.test()'` (background, ~12-20 min) — full
suite green, pass count at least 2520, 0 new failures beyond the documented 3 pre-existing broken
items.
</verification>

<success_criteria>
- The literate page builds live, cites the Gan & Low theorem/condition explicitly, and shows the
  measured optimality loss and fallback semantics — never a fabricated number.
- The full test suite is green end to end with zero default-path drift across the entire phase.
- OVR-01, OVR-02, OVR-03, and OVR-04 are all satisfied and independently verifiable in `src/`,
  `test/`, and `docs/`.
</success_criteria>

<output>
Create `.planning/phases/20-overvoltage-capable-relaxation/20-05-SUMMARY.md` when done. This is the
phase's final summary — record the full-suite pass count, the docs build result, and note the
Open Question 1 terminology correction (this phase's new code/docs consistently use "the EXACT-04
high-PV stress fixture", never "IEEE-13", per RESEARCH.md's own recommendation) for STATE.md's
next update.
</output>
