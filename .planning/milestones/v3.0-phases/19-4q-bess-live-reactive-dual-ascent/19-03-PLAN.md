---
phase: 19-4q-bess-live-reactive-dual-ascent
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/admm/DsoOpt.jl
autonomous: true
requirements: [MESH-05]

must_haves:
  truths:
    - "D-12: reactive_consensus accepts Bool (back-compat), :off/:certified/:live Symbols, or a ReactiveMode value; false/:off is byte-identical to today's constant-draw path, true/:certified is byte-identical to today's pinned qag_dso path."
    - "A genuinely NEW :live branch exists that declares qag_dso the same way as :certified but registers NO :qag_pin equality, instead leaving it as an open coupling variable with its own ρ_q quadratic penalty in the objective."
    - "set_rho_q! exists as a batch quadratic-coefficient mutator for the reactive block's own ρ_q, mirroring set_rho!'s build-once/no-rebuild contract exactly."
  artifacts:
    - path: "src/admm/DsoOpt.jl"
      provides: "build_dso_opt(...; reactive_consensus=OFF, ρ_q) with 3 explicit branches; DsoOpt.qag field; set_rho_q!"
      contains: "normalize_reactive_mode"
  key_links:
    - from: "src/admm/DsoOpt.jl"
      to: "src/admm/ReactiveMode.jl"
      via: "reactive_consensus = normalize_reactive_mode(reactive_consensus)"
      pattern: "normalize_reactive_mode\\(reactive_consensus\\)"
    - from: "src/admm/DsoOpt.jl"
      to: "existing test/test_admm_reactive.jl regression"
      via: "the :off/:certified branches reproduce today's exact code paths"
      pattern: "qag_pin"
---

<objective>
Promote `build_dso_opt`'s `reactive_consensus::Bool` kwarg to the 3-state `ReactiveMode`
(D-12): `OFF` (today's `false`), `CERTIFIED` (today's `true`, pinned one-shot dual read,
byte-identical), and a genuinely NEW `LIVE` branch that declares the same `qag_dso[j,t]`
coupling variable but leaves it UNPINNED (no `:qag_pin` equality), instead giving it its own
`ρ_q`-scaled quadratic penalty in the objective — the DSO-OPT half of MESH-05's live μ-ascent.
Add `set_rho_q!` (peer of the existing `set_rho!`) so plan 19-07's outer loop can adapt `ρ_q`
independently of `ρ`, build-once/no-rebuild, exactly like the active block.

Purpose: this is the network-side (DSO-OPT) half of the live reactive dual-ascent. It must NOT
touch the byte-identical `OFF`/`CERTIFIED` behavior `test_admm_reactive.jl` already pins
(Pitfall 4) — the 3 branches must be genuinely separate code paths, not one shared branch with a
conditional skip.

Output: `build_dso_opt` accepting the 3-state mode via `normalize_reactive_mode` (plan 19-01);
a `DsoOpt.qag` field (mirrors the existing `.pag` field) holding `nothing` under `OFF`/
`CERTIFIED` and the live coupling-variable container under `LIVE`; `set_rho_q!`; existing
`test_admm_reactive.jl` items passing UNMODIFIED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-CONTEXT.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-RESEARCH.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-PATTERNS.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-01-SUMMARY.md
</context>

<interfaces>
From `src/admm/ReactiveMode.jl` (plan 19-01, already implemented):
`normalize_reactive_mode(m)::ReactiveMode` — accepts `Bool|Symbol|ReactiveMode`, throws
`ArgumentError` otherwise. Values `OFF`, `CERTIFIED`, `LIVE`.

Current `src/admm/DsoOpt.jl` (full file already read by the planner) — the exact branch being
promoted, `build_dso_opt` lines 244-256:
```
if reactive_consensus
    @variable(model, qag_dso[j=load_nodes, t=1:T])
    for j in load_nodes, t in 1:T
        add_to_residual!(ctx, :Rq, j, t, qag_dso[j,t])
    end
    @constraint(model, qag_pin[j=load_nodes, t=1:T], qag_dso[j,t] == q_draw[j][t])
    register_constraint!(ctx, :qag_pin, qag_pin)
    ctx.meta[:qag_dso] = qag_dso
else
    for j in load_nodes, t in 1:T
        add_to_residual!(ctx, :Rq, j, t, q_draw[j][t])
    end
end
```
Objective assembly (lines 287-292):
```
@objective(model, Min,
    sum(λ₀[t]*p_import[t] for t in 1:T) +
    0.5*ρ*sum(pag_dso[j,t]^2 for j in load_nodes, t in 1:T))
```
`set_rho!` (lines 402-410) — the batch-mutation idiom `set_rho_q!` mirrors:
```
function set_rho!(dso::DsoOpt, ρ::Real)
    v = VariableRef[dso.pag[j,t] for j in dso.load_nodes for t in 1:dso.T]
    set_objective_coefficient(dso.model, v, v, fill(0.5*ρ, length(v)))
    return dso
end
```
`DsoOpt` struct fields today: `model, ctx, pag, p_import, load_nodes, T, feeder, ρ, λ₀`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-refactor regression baseline + normalize reactive_consensus to 3-state</name>
  <files>src/admm/DsoOpt.jl</files>
  <read_first>
    src/admm/DsoOpt.jl (full file, especially lines 1-45 header comment, 89-306 `build_dso_opt`)
    src/admm/ReactiveMode.jl (from plan 19-01 — `normalize_reactive_mode` signature)
    test/test_admm_reactive.jl (the 3 existing `@testitem`s this task must NOT break — items
    (1)/(2) probe `build_dso_opt` directly)
  </read_first>
  <action>
    First run `julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("reactive", ti.name))'` and record the current pass/fail
    result verbatim (gate-then-golden — this is the pre-refactor baseline for THIS file's
    behavioral change). Then edit `build_dso_opt`'s signature: change
    `reactive_consensus::Bool = false` to `reactive_consensus = false` (accepting
    `Bool|Symbol|ReactiveMode`) and add, as the FIRST line of the function body,
    `mode = normalize_reactive_mode(reactive_consensus)`. Add a new kwarg `ρ_q::Real = ρ`
    (Claude's Discretion default: track the active-block ρ unless the caller overrides — plan
    19-07 will pass an independently-adapted value). Replace the `if reactive_consensus ...
    else ... end` block (lines 244-256) with THREE explicit branches keyed on `mode`:
    `mode == OFF` — byte-identical to today's `else` (inject the constant `q_draw[j][t]`, no
    `qag_dso` declared, no `ctx.meta[:qag_dso]` key). `mode == CERTIFIED` — byte-identical to
    today's `if reactive_consensus` branch VERBATIM (declare `qag_dso`, inject it, register
    `:qag_pin` UNCONDITIONALLY, stash `ctx.meta[:qag_dso]`) — do not skip or condition the
    `:qag_pin` registration in this branch under any circumstance (Pitfall 4). `mode == LIVE` —
    a NEW branch: declare `qag_dso` the SAME way as `CERTIFIED` (same `@variable` call, same
    `:Rq` injection, same `ctx.meta[:qag_dso]` stash), but register NO `:qag_pin` equality —
    instead leave `qag_dso` as a genuinely open coupling variable (plan 19-07's outer loop will
    drive it). Guard against an unreachable 4th case with an `else`
    `error("unreachable: normalize_reactive_mode returned an unhandled ReactiveMode")`.

    Update the docstring's `reactive_consensus` section to describe all 3 states explicitly
    (keep the existing `OFF`/`CERTIFIED` prose verbatim where possible — only ADD `LIVE`'s
    description, per the byte-identical-documentation-of-unchanged-behavior convention).

    Re-run the SAME filtered test command from the top of this action and confirm the result is
    IDENTICAL to the recorded baseline (both existing `reactive_consensus`-probing items still
    pass; item (2), the byte-identity regression, is UNCHANGED).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("reactive", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `build_dso_opt(feeder, aggs, T; ρ=ρ, λ₀=λ₀)` (no `reactive_consensus` kwarg) produces
      `dso.load_nodes == [2]` (2-bus fixture), `haskey(dso.ctx.constraints, :balance_q) == true`,
      `!haskey(dso.ctx.meta, :qag_dso)` — unchanged from today.
    - `build_dso_opt(...; reactive_consensus = true)` still registers `:qag_pin` and stashes
      `ctx.meta[:qag_dso]` — unchanged from today.
    - `build_dso_opt(...; reactive_consensus = :live)` stashes `ctx.meta[:qag_dso]` but
      `!haskey(dso.ctx.constraints, :qag_pin)`.
    - `build_dso_opt(...; reactive_consensus = :bogus)` throws `ArgumentError` (propagated from
      `normalize_reactive_mode`).
    - All 3 pre-existing `test_admm_reactive.jl` items pass, with the SAME pass/fail signature
      recorded at the top of this task.
  </acceptance_criteria>
  <done>
    `build_dso_opt` accepts the 3-state mode via `normalize_reactive_mode`; OFF/CERTIFIED are
    byte-identical to pre-refactor; LIVE is a new, distinct, unpinned branch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LIVE mode's ρ_q objective term, DsoOpt.qag field, and set_rho_q!</name>
  <files>src/admm/DsoOpt.jl</files>
  <read_first>
    src/admm/DsoOpt.jl (post-Task-1 state — the LIVE branch just added, the `@objective` block
    at lines 287-292, `set_rho!` at 402-410, the `DsoOpt` struct definition at 77-87 and its
    single construction call site at the end of `build_dso_opt`)
  </read_first>
  <behavior>
    - Under `OFF`/`CERTIFIED`, `dso.qag === nothing` and the objective is byte-identical to
      pre-Task-1 (no `ρ_q` term appears in the built model at all).
    - Under `LIVE`, `dso.qag` is the SAME `qag_dso` container declared in the LIVE branch (a
      `(load_nodes, T)`-shaped JuMP `DenseAxisArray`/container of `VariableRef`s), and the
      objective contains an ADDITIONAL `0.5*ρ_q*Σ qag_dso[j,t]^2` term, mirroring the existing
      `0.5*ρ*Σ pag_dso[j,t]^2` term exactly in form.
    - `set_rho_q!(dso, new_ρ_q)` mutates ONLY the diagonal quadratic coefficient of every
      `qag_dso[j,t]²` to `0.5*new_ρ_q`, without changing `num_variables`/`num_constraints`
      (build-once preserved), and throws `ArgumentError` if `dso.qag === nothing` (calling it
      on an OFF/CERTIFIED-built DsoOpt is a caller error, fail loud rather than silently no-op).
  </behavior>
  <action>
    Add a new field `qag` to the `DsoOpt` struct (position it immediately after `pag`,
    mirroring the field-ordering convention), typed loosely enough to hold either `nothing` or
    the JuMP coupling-variable container (e.g. `Union{Nothing, Any}` — match whatever concrete
    parametrization the struct already uses for `pag`'s type parameter `P`, adding an analogous
    type parameter for `qag` rather than abandoning the struct's existing parametric-type
    discipline). Update the SINGLE `DsoOpt(...)` construction call at the end of
    `build_dso_opt` to pass `mode == LIVE ? qag_dso : nothing` for the new field. In the `LIVE`
    branch of Task 1's `if`, after declaring `qag_dso`, add the objective's `ρ_q` term: extend
    the existing `@objective(model, Min, sum(...) + 0.5*ρ*sum(pag_dso...))` call so that, ONLY
    when `mode == LIVE`, it becomes `sum(λ₀[t]*p_import[t] for t in 1:T) + 0.5*ρ*sum(pag_dso...)
    + 0.5*ρ_q*sum(qag_dso[j,t]^2 for j in load_nodes, t in 1:T)` — build the objective
    expression with a single `@objective` call whose summed expression conditionally includes
    the `ρ_q` term (e.g. via a `QuadExpr`/`AffExpr` accumulator built before the one
    `@objective` call, so `OFF`/`CERTIFIED` literally never construct or touch the `ρ_q` term).
    Add `set_rho_q!(dso::DsoOpt, ρ_q::Real)` mirroring `set_rho!`'s exact batch-flatten-then-
    one-call shape, operating on `dso.qag` instead of `dso.pag`; guard
    `dso.qag !== nothing || throw(ArgumentError("set_rho_q!: this DsoOpt was built without a " *
    "live reactive coupling block (reactive_consensus != :live); nothing to update"))` as the
    first line. Export `set_rho_q!` alongside the file's existing exports.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("reactive", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `build_dso_opt(...; reactive_consensus = :off).qag === nothing` and
      `build_dso_opt(...; reactive_consensus = true).qag === nothing`.
    - `build_dso_opt(...; reactive_consensus = :live, ρ_q = 3.0).qag !== nothing` and has shape
      `(length(load_nodes), T)`.
    - `set_rho_q!` on an OFF-built `DsoOpt` throws `ArgumentError`; on a LIVE-built one it
      mutates the coefficient without changing `num_variables(dso.model)`/
      `num_constraints(dso.model; count_variable_in_set_constraints=true)` before/after.
    - Objective value at zero linear coefficients under `LIVE` differs from under `OFF` only by
      the `ρ_q` quadratic term evaluated at `qag_dso == 0` (i.e. `0` at the origin — assert the
      two objective EXPRESSIONS differ, e.g. via `objective_function(model)` string/coefficient
      inspection, not just that they happen to evaluate equal at a trivial point).
    - `test_admm_reactive.jl`'s 3 pre-existing items still pass, matching Task 1's recorded
      baseline exactly.
  </acceptance_criteria>
  <done>
    LIVE mode carries its own ρ_q-scaled quadratic penalty and coupling-variable handle;
    set_rho_q! mutates it build-once-style; OFF/CERTIFIED remain byte-identical.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `reactive_consensus` kwarg | Now accepts 3 input shapes (Bool/Symbol/enum); an unrecognized value must fail loud, never silently fall through to a wrong branch |
| Byte-identity of OFF/CERTIFIED | The load-bearing non-regression guarantee this whole plan exists to preserve while adding LIVE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering | `build_dso_opt`'s 3-branch dispatch | mitigate | Explicit `mode == OFF / CERTIFIED / LIVE` branches (never a shared branch with a conditional skip) + an `error("unreachable")` else-guard against a 4th enum value ever silently falling through |
| T-19-07 | Repudiation | OFF/CERTIFIED byte-identity | mitigate | Task 1/2 both re-run the SAME filtered `test_admm_reactive.jl` command before and after the edit and require an IDENTICAL pass signature — a regression here would silently corrupt the v2.1-shipped reactive DLMP feature |
</threat_model>

<verification>
`julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("reactive", ti.name))'` — all 3 pre-existing items green, unchanged.
</verification>

<success_criteria>
`build_dso_opt` and `DsoOpt` expose a genuine 3-state `reactive_consensus` with `set_rho_q!`,
with OFF/CERTIFIED byte-identical to pre-Phase-19 and LIVE structurally ready for plan 19-07's
outer-loop μ-ascent to drive.
</success_criteria>

<output>
Create `.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-03-SUMMARY.md` when done
</output>
