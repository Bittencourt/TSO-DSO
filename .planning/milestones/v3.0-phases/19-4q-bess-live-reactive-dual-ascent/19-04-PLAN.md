---
phase: 19-4q-bess-live-reactive-dual-ascent
plan: 04
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/devices/Aggregator.jl
  - test/test_aggregator.jl
autonomous: true
requirements: [MESH-04]

must_haves:
  truths:
    - "D-09: With no FourQuadBESS present (optional q_inject absent means zero), Aggregator.contribute!'s :Rq residual is byte-identical (same constant, empty terms) to pre-Phase-19 behavior — the existing test_aggregator.jl assertion still passes unmodified."
    - "D-10: With a FourQuadBESS present, its q_inject flows purely additively into :Rq on top of the untouched inelastic term (:Rq = -Pdc*tanφ + Σ device q_inject) and Aggregator.contribute! returns the aggregate q_inject total for AgrOpt (plan 19-06) to consume as its live coupling target."
  artifacts:
    - path: "src/devices/Aggregator.jl"
      provides: "widened contribute! roll-up: hasproperty(res, :q_inject) accumulator, additive :Rq, q_inject in the return NamedTuple"
      contains: "hasproperty(res, :q_inject)"
  key_links:
    - from: "src/devices/Aggregator.jl"
      to: "src/devices/FourQuadBESS.jl"
      via: "hasproperty(res, :q_inject) ? q_inject[t] += res.q_inject[t] : nothing"
      pattern: "hasproperty\\(res, :q_inject\\)"
    - from: "src/devices/Aggregator.jl"
      to: "ctx.residuals[:Rq]"
      via: "add_to_residual!(ctx, :Rq, agg.bus, t, -agg.Pdc[t]*tanφ + q_inject[t])"
      pattern: "add_to_residual!\\(ctx, :Rq, agg\\.bus, t, -agg\\.Pdc\\[t\\] \\* tanφ \\+ q_inject\\[t\\]\\)"
---

<objective>
Widen `Aggregator.contribute!`'s device roll-up to accumulate an OPTIONAL `q_inject` from any
member device (D-09), compose it additively into `:Rq` alongside the existing inelastic
`-Pdc·tanφ` term (D-10), and return the aggregate total as a NEW `q_inject` field in
`Aggregator.contribute!`'s own return NamedTuple — the analog of the existing `p_inject` field —
so plan 19-06's `AgrOpt` can use it as the live reactive consensus target, mirroring exactly how
`p_inject` already feeds `pag`'s pinning constraint today.

Purpose: this is the aggregator-layer half of MESH-04's "device → aggregator → `:Rq`" data flow,
and the load-bearing byte-identity mechanism (D-09/D-10 combined): every pre-existing device's
`contribute!` return is untouched, `hasproperty` fails safe to zero, and the additive `:Rq`
composition reproduces today's expression when no device carries `q_inject`.

Output: `src/devices/Aggregator.jl`'s `contribute!` widened; `test/test_aggregator.jl` extended
with (a) a byte-identity regression re-asserting the existing non-4Q `:Rq` assertion still holds,
and (b) a new item proving a `FourQuadBESS`-bearing aggregator sums `q_inject` correctly into
both `:Rq` and the returned total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-CONTEXT.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-PATTERNS.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-02-SUMMARY.md
</context>

<interfaces>
Current `src/devices/Aggregator.jl:136-178` (`contribute!`, full function already read by the
planner):
```
p_inject = AffExpr[zero(AffExpr) for _ in 1:T]
utility = zero(QuadExpr)
device_vars = Any[]
for d in agg.devices
    res = contribute!(d, ctx; T = T)
    for t in 1:T
        p_inject[t] += res.p_inject[t]
    end
    utility += res.utility
    push!(device_vars, res.vars)
end
for t in 1:T
    add_to_residual!(ctx, :Rp, agg.bus, t, p_inject[t] - agg.Pdc[t])   # (3.22)
    add_to_residual!(ctx, :Rq, agg.bus, t, -agg.Pdc[t] * tanφ)         # (3.23)
end
add_to_objective!(ctx, utility)
store = get!(ctx.meta, :agg_device_vars, Dict{Int, Vector{Any}}())
append!(get!(store, agg.bus, Vector{Any}()), device_vars)
return (; vars = device_vars, p_inject, utility)
```
`FourQuadBESS.contribute!` (plan 19-02) returns `(; vars, p_inject, q_inject, utility)` where
`q_inject` is a `Vector{VariableRef}` (device's own `q`). Every pre-existing device
(`Thermostatic`, `Deferrable`, `PVBattery`) returns a NamedTuple with NO `q_inject` key.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen contribute!'s roll-up (hasproperty guard, additive :Rq, q_inject return)</name>
  <files>src/devices/Aggregator.jl</files>
  <read_first>
    src/devices/Aggregator.jl (full file, especially `contribute!` lines 114-178)
    src/devices/FourQuadBESS.jl (from plan 19-02 — the exact `q_inject` field shape/type on the
    `contribute!` return NamedTuple)
    test/test_aggregator.jl (the EXISTING byte-identity assertion at lines 57-63 this task must
    keep passing unmodified: `Rq[bus,t].constant == -Pdc[t]*tanφ` and `isempty(Rq[bus,t].terms)`)
  </read_first>
  <behavior>
    - With every member device lacking `q_inject` (today's `Thermostatic`/`Deferrable`/
      `PVBattery`), `Rq[bus,t]` is `.constant == -Pdc[t]*tanφ` with `isempty(.terms)` — IDENTICAL
      to pre-widening.
    - With a `FourQuadBESS` member present, `Rq[bus,t]` gains a non-empty `.terms` entry equal
      to the device's `q[t]` variable with coefficient `1.0`, on top of the SAME
      `-Pdc[t]*tanφ` constant.
    - `Aggregator.contribute!`'s returned NamedTuple has a NEW `q_inject::Vector{AffExpr}` field
      equal, per `t`, to the SUM of every member device's `q_inject[t]` (zero if none present).
  </behavior>
  <action>
    First run `julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("aggregator", ti.name))'` and record the pass result (gate-then-golden baseline for this file). Then, inside the existing
    `for d in agg.devices` loop in `contribute!`, add a `q_inject = AffExpr[zero(AffExpr) for _
    in 1:T]` accumulator declared alongside `p_inject`/`utility`/`device_vars` (before the
    loop), and inside the loop body add: `if hasproperty(res, :q_inject); for t in 1:T;
    q_inject[t] += res.q_inject[t]; end; end` — placed immediately after the existing
    `p_inject[t] += res.p_inject[t]` accumulation, changing NOTHING else in the loop body.
    Change ONLY the `:Rq` line in the post-loop residual-write block from
    `add_to_residual!(ctx, :Rq, agg.bus, t, -agg.Pdc[t] * tanφ)` to
    `add_to_residual!(ctx, :Rq, agg.bus, t, -agg.Pdc[t] * tanφ + q_inject[t])` (D-10 — purely
    additive; the `:Rp` line and everything else in the function body is untouched). Add
    `q_inject` to the function's final `return (; vars = device_vars, p_inject, q_inject,
    utility)` — the new field, positioned after `p_inject` to mirror it. Update the function's
    docstring to describe the new `q_inject` roll-up and returned field (extend, do not rewrite,
    the existing "Roll the aggregator's member devices..." docstring).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("aggregator", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - The pre-existing `test_aggregator.jl` item "sole :Rp/:Rq writer at its bus" still passes
      UNMODIFIED (its `Rq[bus,t].constant`/`isempty(.terms)` assertions untouched, non-4Q
      aggregator).
    - `res.q_inject` is present on `Aggregator.contribute!`'s return and equals `zero(AffExpr)`
      per `t` for the same non-4Q aggregator.
    - `grep -n "hasproperty(res, :q_inject)" src/devices/Aggregator.jl` finds exactly 1 match.
    - The ONLY changed line in the residual-write block is the `:Rq` line (diff the function
      body against Task 0's pre-edit read: the `:Rp` line is byte-identical).
  </acceptance_criteria>
  <done>
    Aggregator.contribute! accumulates optional q_inject, composes it additively into :Rq, and
    returns the total; the pre-existing non-4Q regression is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression test — byte-identity (no 4Q) + correct summation (with FourQuadBESS)</name>
  <files>test/test_aggregator.jl</files>
  <read_first>
    test/test_aggregator.jl (full file, 109 lines — the existing 3 `@testitem`s and their
    fixture-construction style)
    src/devices/FourQuadBESS.jl (from plan 19-02 — constructor argument order/types)
  </read_first>
  <action>
    Add a new `@testitem` (tag `[:aggregator]`, name containing "aggregator" and "q_inject")
    that: (a) re-asserts, on a FRESH `Thermostatic` + `PVBattery` aggregator (mirroring the
    existing "sole :Rp/:Rq writer" item's fixture exactly), that `Rq[bus,t].constant ==
    -Pdc[t]*tanφ` and `isempty(Rq[bus,t].terms)` AND `res.q_inject[t] == zero(AffExpr)` for
    every `t` — the explicit byte-identity regression for THIS plan's change, kept as its own
    item so it is independently greppable/citable; (b) builds a SECOND aggregator at the same
    bus with the SAME `Thermostatic` plus a `FourQuadBESS` (valid asymmetric
    `Pch_max`/`Pdch_max`/`Smax`/η/λ triple — pick concrete illustrative values, e.g.
    `Pch_max=0.3, Pdch_max=0.3, Smax=0.4, η=0.95, Emin=0.0, Emax=1.0, soc0=0.5, λ_min=1.0,
    λ_med=2.0, λ_max=3.0`), calls `contribute!`, and asserts `Rq[bus,t].terms` is now
    NON-empty and contains the `FourQuadBESS`'s `q[t]` variable with coefficient `1.0`, and that
    `res.q_inject[t]` is an `AffExpr` referencing that same `q[t]` variable (not a numeric
    constant) — proving the roll-up is genuinely wired, not merely non-crashing (mirrors the
    project's CR-01 "tests passing ≠ mechanism live" lesson).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("aggregator", ti.name) && occursin("q_inject", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - New `@testitem` passes; both the byte-identity sub-case and the FourQuadBESS-summation
      sub-case are asserted (either as one item with both cases or two clearly-named items).
    - `julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("aggregator", ti.name))'` — ALL `test_aggregator.jl` items pass, matching Task 1's
      recorded baseline for the 3 pre-existing items exactly.
  </acceptance_criteria>
  <done>
    test_aggregator.jl proves both non-regression (no 4Q device) and genuine wiring (with a
    FourQuadBESS) of the widened q_inject roll-up.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aggregator roll-up | The sole `:Rp`/`:Rq` network-facing write point (LOCKED since v1.0) — any bug here corrupts every downstream price for every device type, not just FourQuadBESS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering | `:Rq` composition | mitigate | The `hasproperty` guard is the ONLY new conditional in the loop body; Task 2's byte-identity sub-case re-proves the pre-existing non-4Q assertion verbatim, so a regression here fails a test that already existed before this phase |
| T-19-09 | Repudiation | "the roll-up is wired" claim | mitigate | Task 2's FourQuadBESS sub-case asserts the returned `AffExpr` REFERENCES the device's own `q[t]` `VariableRef` (not merely a non-crash), directly refuting the CR-01 "tests passing ≠ mechanism live" failure mode |
</threat_model>

<verification>
`julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("aggregator", ti.name))'` — all items green, including the 3 pre-existing ones unchanged.
</verification>

<success_criteria>
`Aggregator.contribute!` additively rolls up any device's optional `q_inject` into `:Rq` and its
own return tuple, with zero effect on any device lacking that field.
</success_criteria>

<output>
Create `.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-04-SUMMARY.md` when done
</output>
