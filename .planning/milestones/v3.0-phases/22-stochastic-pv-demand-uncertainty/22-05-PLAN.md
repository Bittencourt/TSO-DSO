---
phase: 22-stochastic-pv-demand-uncertainty
plan: 5
type: execute
wave: 5
depends_on: ["22-04"]
files_modified:
  - docs/literate/stochastic_pv_demand.jl
  - docs/make.jl
autonomous: true
requirements: [STOCH-04]

must_haves:
  truths:
    - "A live-executed literate rung page documents the extensive form, the per-scenario
       price semantics decision (D-05/D-07), and the out-of-sample result (D-09) — every
       number on the page is recomputed at Documenter build time, never a frozen screenshot."
    - "The full test suite is green (or shows only the pre-existing known-false failures
       already documented in STATE.md/CONTEXT.md) after this phase's additions."
  artifacts:
    - path: "docs/literate/stochastic_pv_demand.jl"
      provides: "Rung 9 — Stochastic PV/Demand Uncertainty literate page"
      min_lines: 60
    - path: "docs/make.jl"
      provides: "literate-list registration + Models page entry for
        stochastic_pv_demand.jl"
      contains: "stochastic_pv_demand.jl"
  key_links:
    - from: "docs/literate/stochastic_pv_demand.jl"
      to: "src/experiments/run_stochastic.jl"
      via: "r = run_stochastic(s) — the page's one entry-point call"
      pattern: "run_stochastic"
    - from: "docs/make.jl"
      to: "docs/literate/stochastic_pv_demand.jl"
      via: "Literate.markdown(...) render + pages list entry"
      pattern: "stochastic_pv_demand"
---

<objective>
Close the phase with a live-executed literate rung page (STOCH-04) documenting the extensive
form, the per-scenario DADP price-semantics decision (D-05/D-07 — the honesty-load-bearing
distinction between the primary per-scenario prices and the derived expectation), and the
out-of-sample welfare-gap result (D-09) — mirroring `mpc_rolling_horizon.jl`'s (Rung 8)
structure exactly. Then run the phase's full-suite acceptance gate, mirroring the exact
closing-plan convention Phases 20 and 21 already established (a normal autonomous task
running `Pkg.test()` in the background, not a human checkpoint).

Purpose: the phase's final deliverable — a researcher-facing, self-verifying document — and
the project-wide regression gate before the phase is considered shipped.
Output: `docs/literate/stochastic_pv_demand.jl` (new), `docs/make.jl` (wired), and a green
`Pkg.test()` run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-CONTEXT.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-RESEARCH.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-04-SUMMARY.md
@docs/literate/mpc_rolling_horizon.jl

<interfaces>
From docs/literate/mpc_rolling_horizon.jl (Rung 8, the structural template — whole file
already read in full during phase planning; mirror its shape exactly): a `# # Rung N — ...`
title comment, a `using TSODSO`, a `Scenario(...)` construction with a short prose block
explaining WHY a `Scenario` is built rather than a bespoke feeder (mirror the same rationale
sentence, substituted for this phase's own entry point), a single `r = <entry point>(s)`
call, then numbered `## 1. / ## 2. / ...` sections each showing a real computed value (never
a hand-typed literal) with `round.(...; digits = 4)` for vectors, and a closing `## Finding`
section stating the result PLAINLY (never tuned to look small or large).

From docs/make.jl (the exact two edit points): the `for src in (...)` tuple listing every
literate source (add `"stochastic_pv_demand.jl"` as the LAST entry, with a trailing comment
`# NEW: Rung 9 Stochastic PV/Demand Uncertainty (STOCH-01..04)`, mirroring the
`"mpc_rolling_horizon.jl"` entry's own comment style); the `pages = [...]` "Models" list
(add `"Rung 9: Stochastic PV/Demand Uncertainty" => "generated/stochastic_pv_demand.md"` as
the LAST entry in that sub-list, mirroring the `"Rung 8: MPC / Rolling-Horizon RTP"` entry).

From src/experiments/run_stochastic.jl (plan 22-04, already on disk):
run_stochastic(s::Scenario) -> (; in_sample = (; welfare, dadp, expected_dadp, probabilities,
socp_maxgap), oos = (; welfare_h, realized_welfare, welfare_gap))

From src/experiments/Scenario.jl: `stoch_S::Int` (band 3-5), `stoch_probabilities::Vector`,
`stoch_H_oos::Int` (band 5-10) — the page should construct a `Scenario` with a NON-UNIFORM
`stoch_probabilities` (D-04's own CI-fixture requirement generalizes to the demonstration
page too — a uniform-probability demo would not visibly exercise the weighting).

From STATE.md / 22-CONTEXT.md (the pre-phase green reference this plan's acceptance task
diffs against): 2685 passed / 0 real failures / 3 pre-existing broken (+2 known-false Aqua
drift failures on the drifted main checkout; +3 intermittent pre-existing Clarabel errors in
test_experiments.jl) BEFORE this phase's own additions — the closing task tallies this
phase's new `@testitem` count (plans 22-01..22-04) and confirms the post-phase passed count
equals this baseline plus that tally, with the broken/failed counts unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/literate/stochastic_pv_demand.jl — Rung 9 + docs/make.jl wiring (STOCH-04)</name>
  <files>docs/literate/stochastic_pv_demand.jl, docs/make.jl</files>
  <read_first>
    docs/literate/mpc_rolling_horizon.jl (whole file — the structural template, per this
    plan's <interfaces> block), docs/make.jl (the two edit points), src/experiments/Scenario.jl
    (`stoch_*` field docstrings), src/experiments/run_stochastic.jl (the full docstring plan
    22-04 wrote, for accurate prose)
  </read_first>
  <action>
    Create `docs/literate/stochastic_pv_demand.jl` mirroring `mpc_rolling_horizon.jl`'s whole
    structure. Opening comment block: `# # Rung 9 — Stochastic PV/Demand Uncertainty`,
    framing prose explaining that every prior rung page solves ONE realization and reports
    ONE price path, while this page demonstrates a genuinely scenario-conditional extensive
    form where per-scenario DADPs are the PRIMARY output and the probability-weighted
    expectation is reported ONLY as a derived summary (cite D-02/D-05/D-07 by number).
    Construct `s = Scenario(; name = "stochastic-pv-demand-demo", feeder = :ieee13, T = <a
    value the executor confirms solves comfortably within a few seconds — start from `T =
    9` mirroring the Pitfall-3 `:default`-population floor, re-measure and adjust if the
    live build is slow>, stoch_S = 5, stoch_probabilities = <a genuinely non-uniform
    5-vector summing to 1, e.g. reflecting a plausible "increasingly likely toward a central
    scenario" shape — document the choice in a one-line comment>, stoch_H_oos = 10)`. Call
    `r = run_stochastic(s)`. Section `## 1. Per-scenario DADPs (PRIMARY output, D-02/D-05)`:
    show `round.(r.in_sample.dadp[k]; digits = 4)` for each `k in 1:s.stoch_S`, with prose
    noting the values genuinely differ across scenarios (if the live build shows them
    identical or near-identical, say so honestly rather than implying variation that isn't
    there). Section `## 2. Expected DADP — a DERIVED SUMMARY, never a constraint-backed price
    (D-07)`: show `round.(r.in_sample.expected_dadp; digits = 4)` with prose restating D-07's
    caveat verbatim in spirit. Section `## 3. Per-scenario SOCP exactness (D-06, never
    aggregated)`: show `r.in_sample.socp_maxgap` (the `Vector{Float64}`, one entry per
    scenario) with prose noting each was gated INDEPENDENTLY. Section `## 4. Out-of-sample
    realized-vs-in-sample welfare gap (STOCH-03/D-09)`: show `r.oos.welfare_gap` and, for
    scale, `r.in_sample.welfare` and `r.oos.realized_welfare` (with the SAME "not directly
    comparable to each other" caveat `mpc_rolling_horizon.jl`'s own section 3 uses for its
    two raw welfare totals, adapted to this page's own scale-mismatch, if any). Close with a
    `## Finding` section stating the measured result PLAINLY — the sign and magnitude of
    `welfare_gap`, whether the per-scenario DADP spread is economically meaningful on this
    fixture, and an honest one-sentence acknowledgment if the demonstration fixture turns out
    too small/homogeneous to show dramatic scenario variation (mirrors this project's
    standing "report, don't tune" discipline — see `mpc_rolling_horizon.jl`'s own Finding
    section for the tone). Wire `docs/make.jl`: add `"stochastic_pv_demand.jl"` as the LAST
    entry in the `for src in (...)` literate-source tuple (with a trailing comment `# NEW:
    Rung 9 Stochastic PV/Demand Uncertainty (STOCH-01..04)`); add `"Rung 9: Stochastic
    PV/Demand Uncertainty" => "generated/stochastic_pv_demand.md"` as the LAST entry in the
    `pages = [...]` "Models" sub-list.
  </action>
  <acceptance_criteria>
    `docs/literate/stochastic_pv_demand.jl` exists, builds a `Scenario` with non-uniform
    `stoch_probabilities`, calls `run_stochastic` exactly once, and shows every one of the
    four documented sections plus a `## Finding` section — every displayed value is a live
    expression, never a hand-typed literal. `docs/make.jl`'s literate-source tuple and
    `pages` Models list both reference `stochastic_pv_demand.jl`/`.md`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; s = Scenario(name = "stochastic-pv-demand-demo", feeder = :ieee13, T = 9, stoch_S = 5, stoch_probabilities = [0.1, 0.15, 0.2, 0.25, 0.3], stoch_H_oos = 10); r = run_stochastic(s); @assert length(r.in_sample.dadp) == 5; @assert isfinite(r.oos.welfare_gap); println("welfare_gap=", r.oos.welfare_gap); println("expected_dadp=", round.(r.in_sample.expected_dadp; digits = 4)); println("OK")'</automated>
  </verify>
  <done>
    docs/literate/stochastic_pv_demand.jl exists with the documented structure and
    docs/make.jl is wired at both edit points; the inline-verified `Scenario`/`run_stochastic`
    call used in the page's own construction solves successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite acceptance gate</name>
  <files></files>
  <read_first>
    .planning/phases/22-stochastic-pv-demand-uncertainty/22-01-SUMMARY.md through
    22-04-SUMMARY.md (the cumulative new-@testitem count this phase added, for the expected
    passed-count delta), STATE.md's test-invocation hazard note (NEVER
    `julia --project=test -e '...@run_package_tests...'`; ALWAYS
    `julia --project=. -e 'import Pkg; Pkg.test()'`)
  </read_first>
  <action>
    Run `julia --project=. -e 'import Pkg; Pkg.test()'` in the background (~13-20 minutes —
    the ONLY entrypoint that actually executes `@testitem` bodies; TestItemRunner cannot be
    invoked directly under `--project=.`). While it runs, tally the total number of NEW
    `@testitem`s added across plans 22-01 through 22-04 (grep `@testitem` counts across
    `test/test_stochastic_welfare.jl`, `test/test_stochastic_oos_harness.jl`,
    `test/test_run_stochastic.jl`). Once the run completes, confirm: `0` failed, exactly `3`
    pre-existing broken (the known Aqua CairoMakie/Makie drift, unchanged by this phase), and
    `passed` equal to the pre-phase baseline (2685, per STATE.md/22-CONTEXT.md's Phase-21
    green reference) PLUS the tallied new-item count. If the failed count is nonzero, or the
    broken count differs from 3, STOP and diagnose — do NOT close the phase on a red suite.
    Then build the docs (`julia --project=docs docs/make.jl`) and confirm the build completes
    without error (this exercises `checkdocs = :exports` against every symbol this phase
    exported — a gap here means an exported symbol was missed in `docs/src/api.md`'s
    `@autodocs` `Pages` lists across plans 22-02/22-04; fix it as part of THIS task if found,
    since this is the phase's own closing docs-integrity gate).
  </action>
  <acceptance_criteria>
    The background `Pkg.test()` run's final summary line reports `0` failed; the broken count
    is exactly `3` (unchanged from the Phase-21 baseline); the passed count increased by
    exactly the tallied new-`@testitem` count for this phase (documented in the SUMMARY).
    `julia --project=docs docs/make.jl` completes without error.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <done>
    Full suite green: 0 failed, exactly 3 pre-existing broken, passed count matches the
    pre-phase baseline plus this phase's tallied new-item count. The Documenter build
    succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live-executed literate page -> rendered docs site | The page's numbers must be genuinely recomputed at build time, never a stale/hand-typed value that could silently drift from the real code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Repudiation | `docs/literate/stochastic_pv_demand.jl`'s displayed numbers | mitigate | Documenter's `@example`-block execution (via `Literate.DocumenterFlavor()`) recomputes every shown value at build time — no hand-typed literal can silently drift from the real `src/` code (mirrors every prior rung page's own threat disposition) |
| T-22-09 | Denial of Service | The Documenter build fails on `checkdocs = :exports` if any exported Phase-22 symbol was never wired into `docs/src/api.md` | mitigate | Task 2's own `julia --project=docs docs/make.jl` run is the closing docs-integrity gate — any gap discovered here is fixed within this same task, not deferred |

No new package installs this phase — the SC row is intentionally omitted.
</threat_model>

<verification>
Task 1's `<verify>` script passes under `julia --project=.`. Task 2's full-suite `<verify>`
(`julia --project=. -e 'import Pkg; Pkg.test()'`) is the phase's closing gate — the only plan
in this phase permitted to run the full suite, per the orchestrator's "reserve the full suite
for the final acceptance plan only" constraint.
</verification>

<success_criteria>
`docs/literate/stochastic_pv_demand.jl` exists and is wired into `docs/make.jl`. The full test
suite is green (0 failed, 3 pre-existing broken, passed count grown by exactly this phase's
new-item tally). The Documenter build succeeds. Every one of STOCH-01..04 is demonstrably
satisfied across the five plans of this phase.
</success_criteria>

<output>
Create `.planning/phases/22-stochastic-pv-demand-uncertainty/22-05-SUMMARY.md` when done.
</output>
