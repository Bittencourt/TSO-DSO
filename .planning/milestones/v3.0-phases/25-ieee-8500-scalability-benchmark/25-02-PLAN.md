---
phase: 25-ieee-8500-scalability-benchmark
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Project.toml
  - src/solver/ProblemClass.jl
  - src/solver/factory.jl
  - ext/TSODSOSCSExt.jl
  - src/admm/solve_admm.jl
  - test/test_admm_timeout.jl
autonomous: false
requirements: [SCALE-04]
user_setup:
  - service: SCS.jl (Julia General registry weakdep)
    why: "Alternative first-order conic solver for the Clarabel-vs-SCS crossover measurement (D-20/D-21)"
    env_vars: []
    dashboard_config:
      - task: "Confirm the SCS UUID (c946c3f1-2d62-5474-9fac-a2c854d76d31) and current version (2.6.4) against https://github.com/JuliaRegistries/General/blob/master/S/SCS/Package.toml before it lands in [weakdeps]"
        location: "https://github.com/JuliaRegistries/General/blob/master/S/SCS/"

must_haves:
  truths:
    - "A researcher can select SCS as an alternative solver through the existing factory seam without any model naming a solver directly"
    - "solve_admm can be given a wall-clock budget for the whole consensus loop and reports a 'budget exceeded' status instead of hanging or crashing"
    - "The pre-existing solve_admm behavior (time_limit_s absent) is unchanged, including the loud throw on genuine non-convergence"
  artifacts:
    - path: "ext/TSODSOSCSExt.jl"
      provides: "SCS weakdep extension wired to a NEW alternative_optimizer/SCSChoice dispatch (not commercial_optimizer)"
    - path: "src/admm/solve_admm.jl"
      provides: "time_limit_s kwarg + :budget_exceeded exit path, byte-identical default"
  key_links:
    - from: "ext/TSODSOSCSExt.jl"
      to: "src/solver/factory.jl"
      via: "TSODSO.alternative_optimizer(::SCSChoice, pc) method added only when SCS is loaded"
      pattern: "alternative_optimizer"
    - from: "scripts/benchmark_ieee8500.jl (plan 25-05)"
      to: "solve_admm(...; time_limit_s=...)"
      via: "the new kwarg's wall-clock break inside the outer loop"
      pattern: "time_limit_s"
---

<objective>
Add the SCS weakdep solver extension (D-20, via a new non-"commercial" `alternative_optimizer`
dispatch) and a minimal, additive wall-clock exit to `solve_admm` (D-18), so the benchmark harness
(plan 25-05) has both prerequisites ready before it runs a single point at 8500 scale.

Purpose: SCALE-04's Clarabel-vs-SCS crossover and honest-timeout requirements need these two seams
to exist; neither depends on the 8500 fixture itself, so this plan runs independently in wave 1.
Output: `ext/TSODSOSCSExt.jl` + `[weakdeps]`/`[extensions]` wiring, `solve_admm`'s new
`time_limit_s` kwarg, and a regression test proving the default path is unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-CONTEXT.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-RESEARCH.md
@src/solver/ProblemClass.jl
@src/solver/factory.jl
@ext/TSODSOGurobiExt.jl
@src/admm/solve_admm.jl
</context>

<interfaces>
<!-- Exact current shapes this plan extends, extracted from the codebase. -->

From `src/solver/ProblemClass.jl` (marker types + export line to extend):
```julia
struct GurobiChoice end
struct MosekChoice end
export ProblemClass, LP, MILP, QP, SOCP, NLP, GurobiChoice, MosekChoice
```

From `src/solver/factory.jl` (the fallback pattern to mirror with a NEW, non-commercial name):
```julia
function commercial_optimizer(choice, pc::ProblemClass)
    error("""No commercial optimizer is available for choice $(choice) and problem class $(pc)...""")
end
export select_optimizer, commercial_optimizer
```

From `ext/TSODSOGurobiExt.jl` (full file, the module shape to copy):
```julia
module TSODSOGurobiExt
using TSODSO, Gurobi, JuMP
TSODSO.commercial_optimizer(::TSODSO.GurobiChoice, pc::TSODSO.ProblemClass) =
    optimizer_with_attributes(Gurobi.Optimizer)
end # module TSODSOGurobiExt
```

From `src/admm/solve_admm.jl` (the exact lines this plan touches):
```julia
function solve_admm(feeder, pf::ConvexBranchFlow, aggregators::AbstractVector{<:Aggregator};
    T::Int = 24, λ₀, ρ::Real, maxiter::Int = 200, tol::Real = 1e-5, ε_abs::Real = 1e-4,
    ε_rel::Real = 1e-3, τ::Real = 2.0, μ::Real = 10.0, ρ_min::Real = 1e-2, ρ_max::Real = 1e4,
    allow_export::Bool = true, reactive_consensus = false, ρ_q::Real = ρ)
    ...
    converged_flag = false
    for k in 1:maxiter
        ... # (1) AGR-OPT, (2) DSO-OPT, (3) residual bookkeeping
        if converged(residuals, ε_pri, ε_dual)      # line 474
            converged_flag = true
            break                                    # line 476
        end
        ... # dual ascent, adaptive-ρ update
    end
    converged_flag || throw(ErrorException("solve_admm FAILED to converge: ..."))  # lines 581-591
    ... # final consolidation pass: check_battery=true, check_4q, check_exact=true
    return (; welfare, dadp = λ_mat, λ = λ_mat, iters = residuals.iters, residuals,
        dso_ctx = dso.ctx, exact_maxgap, mu_q = mu_q_mat, q_devices)  # line 796
end
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify SCS.jl package legitimacy before adding the weakdep</name>
  <what-built>Nothing yet — this checkpoint gates the SCS weakdep before it is added.</what-built>
  <how-to-verify>
    SCS.jl is tagged `[ASSUMED]` in `25-RESEARCH.md`'s Package Legitimacy Audit: `slopcheck` has no
    Julia/General-registry ecosystem support, so it could not be run against this package. Visit
    `https://github.com/JuliaRegistries/General/blob/master/S/SCS/Package.toml` and confirm:
    1. The UUID is `c946c3f1-2d62-5474-9fac-a2c854d76d31`.
    2. `https://github.com/JuliaRegistries/General/blob/master/S/SCS/Versions.toml` lists `2.6.4`
       (or a newer patch/minor — record whichever is current) as an available version, and the
       registered repo is `github.com/jump-dev/SCS.jl`.
    3. The `jump-dev` GitHub org and `SCS.jl` repo look legitimate (long history, JuMP-dev-owned).
  </how-to-verify>
  <resume-signal>Type "approved" with the confirmed UUID/version, or report a discrepancy to halt.</resume-signal>
  <files>N/A — human verification only, no files modified in this task</files>
  <action>Human confirms the SCS UUID and current version against the live Julia General registry pages named in &lt;how-to-verify&gt; before Task 2 writes them into Project.toml.</action>
  <verify>
    <manual>Human visits the linked GitHub registry pages and confirms UUID c946c3f1-2d62-5474-9fac-a2c854d76d31 and the current version.</manual>
  </verify>
  <done>Human has typed "approved" with the confirmed UUID/version, or halted the plan on a discrepancy.</done>
</task>

<task type="auto">
  <name>Task 2: SCS weakdep extension via a new alternative_optimizer/SCSChoice dispatch (D-20)</name>
  <files>Project.toml, src/solver/ProblemClass.jl, src/solver/factory.jl, ext/TSODSOSCSExt.jl</files>
  <read_first>src/solver/ProblemClass.jl, src/solver/factory.jl, ext/TSODSOGurobiExt.jl, ext/TSODSOMosekExt.jl, Project.toml</read_first>
  <action>
In `Project.toml`, under `[weakdeps]` add `SCS = "&lt;UUID confirmed in Task 1&gt;"`; under
`[extensions]` add `TSODSOSCSExt = "SCS"`; under `[compat]` add `SCS = "&lt;version confirmed in
Task 1&gt;"`. In `src/solver/ProblemClass.jl`, add `struct SCSChoice end` immediately after
`MosekChoice`'s definition, with a docstring stating it is dispatched via a NEW, SEPARATE
`alternative_optimizer` function — never `commercial_optimizer` — because SCS is open-source
(D-20: routing an open-source solver through a dispatch named/documented as "commercial" is a
semantic mismatch). Add `SCSChoice` to the file's `export` line. In `src/solver/factory.jl`, add a
NEW function `alternative_optimizer(choice, pc::ProblemClass)` immediately after
`commercial_optimizer`'s fallback definition, with the SAME error-fallback shape but wording that
does NOT say "commercial" (e.g. "No alternative optimizer is available for choice $(choice) and
problem class $(pc). Alternative solvers are opt-in weakdep extensions and are never hard
dependencies. To enable one, load the solver in your environment, e.g.: import SCS # enables
alternative_optimizer(SCSChoice(), pc)"); add `alternative_optimizer` to the final `export` line
alongside `select_optimizer, commercial_optimizer`. Create `ext/TSODSOSCSExt.jl` mirroring
`ext/TSODSOGurobiExt.jl`'s module shape exactly: header comment stating SCS is opt-in/never-hard-dep,
`module TSODSOSCSExt`, `using TSODSO, SCS, JuMP`, then `TSODSO.alternative_optimizer(::TSODSO.SCSChoice,
pc::TSODSO.ProblemClass) = optimizer_with_attributes(SCS.Optimizer, "verbose" => 0)`, `end # module
TSODSOSCSExt`.
  </action>
  <verify>
    <automated>grep -q "TSODSOSCSExt" Project.toml && grep -q "struct SCSChoice" src/solver/ProblemClass.jl && grep -q "function alternative_optimizer" src/solver/factory.jl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A2 '\[weakdeps\]' Project.toml` includes the confirmed `SCS = "..."` UUID line.
    - `grep -A5 '\[extensions\]' Project.toml` includes `TSODSOSCSExt = "SCS"`.
    - `grep "function alternative_optimizer" src/solver/factory.jl` matches, and its error message
      does NOT contain the substring "commercial" (`grep -A8 "function alternative_optimizer"
      src/solver/factory.jl | grep -qv commercial` succeeds — i.e. no line mentions it).
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 — `commercial_optimizer`'s existing
      behavior/error text is byte-for-byte unchanged (must_not_break regression).
  </acceptance_criteria>
  <done>SCS reachable ONLY via TSODSO.alternative_optimizer(TSODSO.SCSChoice(), pc) when SCS is loaded; commercial_optimizer untouched; SCS never a hard [deps] entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: solve_admm time_limit_s wall-clock exit (D-18)</name>
  <files>src/admm/solve_admm.jl, test/test_admm_timeout.jl</files>
  <read_first>src/admm/solve_admm.jl (lines 185-230, 320-480, 560-600, 770-807 — signature, loop entry, convergence check + adaptive-ρ tail, final return), test/test_admm.jl (the existing "fails loud on the cap" testitem near line 127, to confirm what must stay unchanged), test/fixtures_phase7.jl or an equivalent small IEEE-13 setup helper</read_first>
  <behavior>
    - Test 1: `solve_admm(...; time_limit_s=nothing)` (default) on the existing IEEE-13 fixture
      behaves byte-identically to today — the existing `test/test_admm.jl` "fails loud on the cap"
      testitem still throws the same `ErrorException` under the same conditions.
    - Test 2: `solve_admm(...; time_limit_s=1e-9)` on a fixture whose per-iteration wall time
      exceeds 1e-9s returns (does NOT throw) a NamedTuple with `result.status == :budget_exceeded`,
      `result.iters < maxiter`, and `result.dadp === nothing` / `result.λ === nothing` (so a caller
      cannot silently mistake the mid-loop iterate for a certified price).
    - Test 3: on normal convergence (no time limit, or a generous one), the returned NamedTuple now
      also carries `result.status == :converged`, with every OTHER field unchanged from today.
  </behavior>
  <action>
Add `time_limit_s::Union{Nothing,Real} = nothing` to `solve_admm`'s keyword arguments (after
`ρ_q::Real = ρ`), documented in the docstring as "wall-clock budget for the WHOLE consensus loop
(D-18); `nothing` (default) preserves the pre-existing unbounded behavior byte-for-byte." Before
the `for k in 1:maxiter` loop, add `t0_wall_ns = time_ns()` and `budget_exceeded_flag = false`
alongside the existing `converged_flag = false`. Inside the loop, immediately AFTER the existing
`if converged(residuals, ε_pri, ε_dual); converged_flag = true; break; end` block and BEFORE the
dual-ascent update, add: `if time_limit_s !== nothing && (time_ns() - t0_wall_ns) / 1.0e9 >
time_limit_s; budget_exceeded_flag = true; break; end`. Change the "FAIL LOUD on the maxiter cap"
throw to fire only when `!converged_flag && !budget_exceeded_flag` (condition and message text
otherwise UNCHANGED). Add a new `elseif budget_exceeded_flag` branch that SKIPS the final
consolidation pass (which assumes a converged iterate and runs the battery/4Q/exactness
certificates — meaningless on a mid-loop point) and returns EARLY with `(; welfare = nothing, dadp
= nothing, λ = nothing, iters = residuals.iters, residuals, dso_ctx = dso.ctx, exact_maxgap =
nothing, mu_q = nothing, q_devices = Dict{Int,Vector{Float64}}(), status = :budget_exceeded)`. Add
`status = :converged` as an ADDITIVE new field on the existing final-return NamedTuple (do not
remove or reorder any existing field). Update the docstring's "Returns" section documenting
`time_limit_s` and the `:converged`/`:budget_exceeded` status values, stating explicitly that a
`:budget_exceeded` result's `dadp`/`λ` are `nothing` BY DESIGN — never a plausible-but-uncertified
number. Create `test/test_admm_timeout.jl` as a PLAIN `Test.jl` script (NOT `@testitem` — must run
directly per the project's recorded TestItemRunner-under-`--project=.` trap) building a small
IEEE-13 ADMM setup (via the exported `ieee13_modified()` + `build_population(:default, feeder,
:ieee13, profiles, seed)` + `ConvexBranchFlow()` + a benign `λ₀`) and asserting Tests 1-3 above.
  </action>
  <verify>
    <automated>julia --project=. test/test_admm_timeout.jl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "time_limit_s" src/admm/solve_admm.jl` shows the new kwarg AND the new wall-clock
      check inside the loop.
    - `julia --project=. test/test_admm_timeout.jl` exits 0.
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 (existing ADMM regression suite,
      including the "fails loud on the cap" testitem, is unaffected — must_not_break).
  </acceptance_criteria>
  <done>solve_admm accepts time_limit_s, returns :budget_exceeded honestly on timeout without throwing, and the default (nothing) path is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Julia General registry -> `[weakdeps]` | A new opt-in package (SCS) enters the dependency graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-25-05 | Tampering (dependency-confusion/typosquat) | `SCS` weakdep entry | mitigate | Blocking human-verify checkpoint (Task 1) confirms UUID + version against the live General registry before any `[weakdeps]` line is written; slopcheck could not cover this ecosystem |
| T-25-06 | Denial of Service | `solve_admm`'s unbounded outer loop at scale | mitigate | `time_limit_s` gives a genuine wall-clock exit reported as `:budget_exceeded`, never a silent hang |
</threat_model>

<verification>
`julia --project=. test/test_admm_timeout.jl` and `julia --project=. -e 'using Pkg; Pkg.test()'`
both exit 0; `Project.toml` shows SCS only under `[weakdeps]`/`[extensions]`/`[compat]`, never
`[deps]`.
</verification>

<success_criteria>
SCS reachable via `alternative_optimizer`/`SCSChoice` without becoming a hard dependency;
`solve_admm` bounded by an optional wall-clock budget with an honest `:budget_exceeded` exit;
default behavior byte-identical.
</success_criteria>

<output>
Create `.planning/phases/25-ieee-8500-scalability-benchmark/25-02-SUMMARY.md` when done.
</output>
