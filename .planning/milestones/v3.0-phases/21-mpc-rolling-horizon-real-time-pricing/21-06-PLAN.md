---
phase: 21-mpc-rolling-horizon-real-time-pricing
plan: 06
type: execute
wave: 5
depends_on: ["21-05"]
files_modified:
  - docs/literate/mpc_rolling_horizon.jl
  - docs/make.jl
autonomous: true
requirements: [MPC-04]

must_haves:
  truths:
    - "A live-executed literate rung page demonstrates the full receding-horizon closed loop (run_mpc) on a 24-hour demonstration horizon, honestly reporting the measured regret against the perfect-foresight day-ahead optimum and the price-deviation path under seeded synthetic forecast error, beside the ROADMAP-cited textbook terminal-equality framing (D-11, Rawlings/Mayne/Diehl citation flagged for confirmation)."
    - "The page documents, in prose, D-04's per-step certificate/fallback handling and the regret's PUBLISHED-decision-horizon scoping (never silently extended to the full T) — the SAME honesty discipline every prior v3.0 rung page (restricted_branch_flow.jl, ac_oracle.jl) already upholds."
    - "The full test suite (julia --project=. -e 'import Pkg; Pkg.test()') is green: 0 failed, only the 3 pre-existing broken tests remain broken, and the passed count has grown by exactly the number of new @testitems this phase (21-01..21-05) added."
  artifacts:
    - path: "docs/literate/mpc_rolling_horizon.jl"
      provides: "Live-executed Documenter/Literate page demonstrating run_mpc end-to-end on a 24h fixture, reporting regret + price-deviation path"
      contains: "run_mpc("
    - path: "docs/make.jl"
      provides: "mpc_rolling_horizon.jl wired into the Literate source list AND the Documenter pages= tree"
      contains: "mpc_rolling_horizon.jl"
  key_links:
    - from: "docs/make.jl"
      to: "docs/literate/mpc_rolling_horizon.jl"
      via: "Literate.markdown(...) render + a new Documenter pages= entry, mirroring restricted_branch_flow.jl's own wiring exactly"
      pattern: "mpc_rolling_horizon"
---

<objective>
Author `docs/literate/mpc_rolling_horizon.jl`, the live-executed rung page demonstrating
`run_mpc` end-to-end on a genuine 24-hour horizon (larger and more demonstrative than the CI
fixture), reporting the measured regret against the perfect-foresight day-ahead optimum and the
price-deviation path under seeded synthetic forecast error — MPC-04's literate-page requirement
— then run the phase's full-suite acceptance gate.

Purpose: this is the phase's closing deliverable. Every prior v3.0 rung page
(`restricted_branch_flow.jl`, `ac_oracle.jl`) inlines its OWN fixture verbatim (literate pages
never load test-only modules) and presents every number as LIVE-COMPUTED during the Documenter
build, never a pasted/cached result — this page follows the identical discipline, at a horizon
large enough (`T=24`, `H` a genuine multi-hour window) to be honestly demonstrative rather than
the CI fixture's deliberately tiny `T=8`/`H=3` substrate.

Output: `docs/literate/mpc_rolling_horizon.jl` (new), `docs/make.jl` gains one new Literate
source + one new Documenter `pages=` entry, and the phase closes with a green full-suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-CONTEXT.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-05-SUMMARY.md

@docs/literate/restricted_branch_flow.jl
@docs/make.jl
@src/experiments/mpc_loop.jl
@src/experiments/Scenario.jl
</context>

<interfaces>
`docs/literate/restricted_branch_flow.jl`'s exact page shape this file mirrors (verified,
`docs/literate/restricted_branch_flow.jl:1-100`):

```
# # Rung N — <Title>
#
# <narrative citing the mechanism, prior page, and any literature reference>. Every number
# shown below is RECOMPUTED live during this page's build.

using TSODSO
using TSODSO.JuMP

# ## Building the <fixture> (inlined verbatim — literate pages never load test-only modules)
const T = 24
mem_price = Float64[...]        # inline literal, NOT `using Phase4Fixtures`
...
feeder = Feeder(buses, branches, 1)
...
```

`docs/make.jl`'s exact two insertion points (verified, `docs/make.jl:20-38,62-83`):

```
for src in (
    ...
    "socp_applicability.jl",
    # <this plan appends "mpc_rolling_horizon.jl" here>
)
    Literate.markdown(joinpath(LITERATE_DIR, src), GENERATED_DIR; flavor = Literate.DocumenterFlavor())
end

pages = [
    "Home" => "index.md",
    "Models" => [ ... ],
    "Planning" => [
        "Rung 6: Stackelberg-Benders" => "generated/stackelberg_benders.md",
        "Rung 7: Nash Diagonalization & Shared Corridor" => "generated/nash_diagonalization.md",
        # <this plan appends an "MPC / Rolling-Horizon" entry to an appropriate section here>
    ],
    "API Reference" => "api.md",
]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the mpc_rolling_horizon.jl literate page</name>
  <files>docs/literate/mpc_rolling_horizon.jl, docs/make.jl</files>
  <read_first>
    - docs/literate/restricted_branch_flow.jl (full file — the exact narrative/fixture-
      construction/live-computation shape this page mirrors)
    - docs/make.jl (full file — both insertion points)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-05-SUMMARY.md (run_mpc's
      exact return shape, the measured `MPC_HIGH_PV_SCALE_MEASURED` value, and an observed
      `cert_status_trace` — cite these verbatim where relevant)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-CONTEXT.md, D-11 (the regret +
      price-deviation-path framing this page must present) and the phase's `<specifics>` block
      ("the literate page must present the regret benchmark honestly... reported as measured,
      not tuned to look small")
  </read_first>
  <action>
    Create `docs/literate/mpc_rolling_horizon.jl` with a header narrative citing this phase's
    goal (closed-loop receding-horizon RTP vs the perfect-foresight day-ahead optimum), the
    Rawlings/Mayne/Diehl terminal-equality-constraint framing (D-06's mechanism; flag the
    citation as unverified against a live source this session, per RESEARCH's own Assumptions
    Log A1 — state this caveat in the page's prose, do not silently drop it), and state plainly
    that every number below is RECOMPUTED live during the Documenter build.

    `using TSODSO` / `using TSODSO.JuMP`. Build a `T = 24`-hour fixture INLINE (verbatim
    construction, mirroring `Phase21Fixtures`'s CI-fixture SHAPE but at the full 24h scale
    genuinely demonstrative of a receding horizon — a 2-3 bus radial feeder, Thermostatic +
    PVBattery aggregator(s), the project's standard MEM price shape and ambient-temperature
    profile literals) — construct a `Scenario` (or the equivalent direct feeder/aggregator
    construction if `Scenario`'s selector set cannot address this page's own fixture) with
    `T = 24`, a genuinely multi-hour `mpc_H` (document the choice — e.g. `mpc_H = 6`, giving 19
    published steps), `mpc_terminal_soc = true`, and a NONZERO `mpc_forecast_error` (e.g. `0.08`,
    citing D-08's "±5-10%" range).

    Call `run_mpc(scenario)`. Present, in order: (1) the day-ahead SOC/price trajectory (a small
    printed table or `@show`-style output, not a plot — CairoMakie plotting is available but not
    REQUIRED by MPC-04's wording; if added, mirror `restricted_branch_flow.jl`'s own
    plotting-or-not convention, whichever it actually does); (2) the rolling published DADP path
    and its step-to-step jumps/cumulative deviation (`r.trace.dadp_trace`, `max_jump(r.trace)`,
    `mean_jump(r.trace)`); (3) the measured `r.regret` value with an EXPLICIT prose sentence
    stating its published-decision-horizon scoping (mirroring the code comment in
    `mpc_loop.jl`); (4) the per-step `cert_status_trace`, and — if this 24h fixture's `pv_scale`
    happens to stay comfortably exact throughout (a plausible, honest outcome for a moderate
    fixture) — an EXPLICIT prose note that no escalation was needed here, cross-referencing
    `test_mpc_loop.jl`'s forced-inexact regression as the place the escalation ladder IS
    exercised, rather than fabricating an artificial trigger on this demonstration fixture. Do
    NOT tune the fixture to make regret artificially small or artificially large — report
    whatever the live run measures, exactly as `restricted_branch_flow.jl`'s own
    `optimality_loss ≈ -1.43` finding is reported plainly rather than hidden.

    In `docs/make.jl`, append `"mpc_rolling_horizon.jl"` to the `for src in (...)` tuple
    (after `"socp_applicability.jl"`), and add `"Rung 8: MPC / Rolling-Horizon RTP" =>
    "generated/mpc_rolling_horizon.md"` to the `pages=` tree — placed as its OWN top-level entry
    (mirroring how "IEEE-123 Real Impedances"/"Thesis Reproduction" sit as their own entries
    under "Models" rather than forced into "Planning") OR under "Planning" beside Rung 6/7 if a
    closer thematic fit — pick whichever existing top-level grouping this project's own
    convention argues for by inspecting how the OTHER non-planning, non-core-model rungs
    (IEEE-123 impedances, thesis reproduction, SOC applicability) were placed, and match that
    precedent rather than inventing a new grouping ad hoc.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, Test; s = Scenario(; name = "literate-demo", feeder = :ieee13, T = 24, mpc_H = 6, mpc_terminal_soc = true, mpc_forecast_error = 0.08); r = run_mpc(s); @test r.steps == 24 - 6 + 1; @test isfinite(r.regret); println("regret = ", r.regret, "  max_jump = ", max_jump(r.trace), "  cert statuses = ", r.trace.cert_status_trace)'</automated> -- per-task quick check: proves the literate page's OWN Scenario call shape runs end-to-end before wiring the file into docs/make.jl's Documenter build (which is a separate, slower `julia --project=docs docs/make.jl` invocation, not run by this inline check).
  </verify>
  <done>docs/literate/mpc_rolling_horizon.jl exists, is wired into docs/make.jl's Literate list and pages= tree, and the page's own Scenario/run_mpc call runs cleanly standalone.</done>
  <acceptance_criteria>
    - `grep -n 'run_mpc(' docs/literate/mpc_rolling_horizon.jl` matches
    - `grep -n 'mpc_rolling_horizon.jl' docs/make.jl` returns at least 2 matches (the Literate-source tuple entry and the pages= entry)
    - `grep -n 'regret' docs/literate/mpc_rolling_horizon.jl` matches (the number is surfaced, not just computed)
    - The verify command exits 0 and prints a `regret = ...` line
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-suite acceptance gate</name>
  <files></files>
  <read_first>
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-01-SUMMARY.md through 21-05-SUMMARY.md (the cumulative new-@testitem count this phase added, for the expected passed-count delta)
    - CLAUDE.md / STATE.md's test-invocation hazard note (NEVER `julia --project=test -e '...@run_package_tests...'`; ALWAYS `julia --project=. -e 'import Pkg; Pkg.test()'`)
  </read_first>
  <action>
    Run `julia --project=. -e 'import Pkg; Pkg.test()'` in the background (this is the ONLY
    entrypoint that actually executes `@testitem` bodies — TestItemRunner is test-only and
    `--project=.` cannot resolve it directly; ~13-18 minutes). While it runs, tally the total
    number of NEW `@testitem`s added across plans 21-01 through 21-05 (grep `@testitem` counts
    recorded in each plan's SUMMARY, or re-grep the five new/modified test files directly:
    `test_pvbattery.jl`/`test_thermostatic.jl`/`test_fourquadbess.jl`/`test_aggregator.jl`
    (incremental additions), `test_mpc_trace.jl`, `test_mpc_window.jl`, `test_mpc_terminal.jl`,
    `test_mpc_loop.jl`). Once the run completes, confirm: `0 failed`, exactly `3` pre-existing
    broken (the known Aqua CairoMakie/Makie drift, unchanged by this phase), and `passed` equal
    to the pre-phase baseline (2563, per STATE.md/CONTEXT.md's Phase-20 green reference) PLUS the
    tallied new-item count. If the failed count is nonzero, or the broken count differs from 3,
    STOP and diagnose — do NOT proceed to close the phase on a red suite.
  </action>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <done>Full suite green: 0 failed, exactly 3 pre-existing broken, passed count matches the pre-phase baseline plus this phase's tallied new-item count.</done>
  <acceptance_criteria>
    - The background run's final summary line reports `0` failed
    - The broken count is exactly `3` (unchanged from the Phase-20 baseline)
    - The passed count increased by exactly the tallied new-@testitem count for this phase (documented in the SUMMARY)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — a Documenter build + local test run, no network/user-facing input | This plan's only "boundary" is the CI/build environment itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-17 | Repudiation | The literate page's regret number is silently cherry-picked (a re-run until a "nice" number appears) rather than reported as measured | mitigate | the action explicitly forbids tuning the fixture for a small/large regret; the page's own prose states the number is live-recomputed at build time, matching every prior rung page's discipline |
| T-21-18 | Denial of Service | The Documenter build itself (not run by this plan's automated `<verify>`, which only exercises `run_mpc` standalone) could fail on `checkdocs = :exports` if any exported MPC symbol was never wired into `api.md` | accept, flagged | plans 21-02/21-04/21-05 each append their own exports to `docs/src/api.md` incrementally; if a gap is discovered running the ACTUAL `julia --project=docs docs/make.jl` (outside this plan's Julia-suite-only closing gate), fix it as a follow-up quick task rather than blocking this plan's own full-suite acceptance, which is the phase's hard gate per the orchestrator's tooling constraint |
</threat_model>

<verification>
Run `julia --project=. -e 'import Pkg; Pkg.test()'` in the background (~13-18 min). This is the
phase's CLOSING full-suite gate — the only plan in this phase permitted to run it, per the
orchestrator's "reserve the full suite for the final acceptance plan only" constraint. Confirm
`0` failed, `3` pre-existing broken, and the passed count matches baseline + this phase's tally.
</verification>

<success_criteria>
- `docs/literate/mpc_rolling_horizon.jl` exists, is wired into `docs/make.jl`, and its own
  `run_mpc` call runs cleanly standalone.
- The full test suite is green (0 failed, 3 pre-existing broken, passed count grown by exactly
  this phase's new-item tally).
</success_criteria>

<output>
Create `.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-06-SUMMARY.md` when done.
Record the full-suite's final passed/failed/broken counts and the measured 24h-demo `regret`
value verbatim — this closes Phase 21.
</output>
