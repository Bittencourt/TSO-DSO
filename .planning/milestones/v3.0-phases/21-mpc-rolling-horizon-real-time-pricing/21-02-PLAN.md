---
phase: 21-mpc-rolling-horizon-real-time-pricing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/mpc_trace.jl
  - src/TSODSO.jl
  - docs/src/api.md
  - test/test_mpc_trace.jl
autonomous: true
requirements: [MPC-03]

must_haves:
  truths:
    - "A new exported MpcTrace struct records per-step published DADP, step-to-step price jump, cumulative deviation from the day-ahead DADP path, and per-step certificate/fallback status, following the AdmmResiduals/BendersTrace trace-struct convention verbatim (D-09)."
    - "record! enforces the SAME sequential-k fail-loud guard as AdmmResiduals's record! (never a silent double-record or a silently-skipped iteration, ArgumentError otherwise)."
    - "Price-consistency query predicates (max/mean step-to-step jump, any-certificate-failure flag) are derivable from a populated MpcTrace and documented, satisfying MPC-03's 'step-to-step price jumps, cumulative deviation' requirement with Claude's-discretion exact norms (D-10)."
  artifacts:
    - path: "src/models/mpc_trace.jl"
      provides: "MpcTrace mutable struct + MpcTrace() empty constructor + record!(trace, k, dadp, dadp_da, cert_status) + max_jump/mean_jump/any_cert_failed query functions"
      contains: "mutable struct MpcTrace"
    - path: "test/test_mpc_trace.jl"
      provides: "@testitem coverage: empty-ledger predicates, sequential-k guard, populated-ledger derived metrics"
      contains: "@testitem"
  key_links:
    - from: "src/models/mpc_trace.jl"
      to: "src/admm/residuals.jl"
      via: "record!'s _assert_sequential fail-loud guard, reimplemented locally (mirrors AdmmResiduals's own internal helper, never imported across the file-disjoint seam convention)"
      pattern: "_assert_sequential"
    - from: "docs/src/api.md"
      to: "src/models/mpc_trace.jl"
      via: "a new @autodocs Pages entry so MpcTrace's exported docstring surfaces in the rendered manual (checkdocs = :exports is module-global and blocking)"
      pattern: "mpc_trace.jl"
---

<objective>
Create `MpcTrace`, the append-only, JuMP-free diagnostics ledger that records the rolling
receding-horizon loop's per-step published DADP, step-to-step price jump, cumulative deviation
from the day-ahead DADP path, and per-step certificate/fallback status — the trace-struct
MPC-03 requires, built and tested BEFORE the orchestrator that will populate it (Wave 3/4) so
those later plans consume an already-validated, already-exported contract rather than
co-designing it under orchestration pressure.

Purpose: mirrors `AdmmResiduals` (`src/admm/residuals.jl`) — a pure data/bookkeeping type with
NO JuMP, NO solves — so it is trivially unit-testable standalone and consumable later by a
CairoMakie plotting extension or the literate rung page exactly as `AdmmResiduals` already is.
Building it now (independent of the window builder, Wave 2) keeps this plan's own scope tight
and lets Wave 3/4's orchestrator plans focus entirely on wiring, not on trace-struct design.

Output: `src/models/mpc_trace.jl` (new), one new `src/TSODSO.jl` include line, one new
`docs/src/api.md` `@autodocs` entry, and `test/test_mpc_trace.jl` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-CONTEXT.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-PATTERNS.md

@src/admm/residuals.jl
@src/TSODSO.jl
@docs/src/api.md
</context>

<interfaces>
`AdmmResiduals`'s exact trace-struct convention this plan mirrors (verified,
`src/admm/residuals.jl:63-194`):

```
mutable struct AdmmResiduals
    N::Int
    T::Int
    primal_trace::Vector{Float64}
    dual_trace::Vector{Float64}
    ...
    iters::Int
end
AdmmResiduals(N::Integer, T::Integer) = AdmmResiduals(Int(N), Int(T), Float64[], ..., 0)
AdmmResiduals() = AdmmResiduals(0, 0)

@inline function _assert_sequential(res::AdmmResiduals, k::Integer)
    expected = res.iters + 1
    k == expected || throw(ArgumentError("record!: expected sequential iteration $expected, got k=$k"))
    return nothing
end

function record!(res::AdmmResiduals, k::Integer, primal::Real, dual::Real, ...) 
    _assert_sequential(res, k)
    push!(res.primal_trace, abs(float(primal)))
    ...
    res.iters += 1
    return res
end

function converged(res::AdmmResiduals, ε_pri::Real, ε_dual::Real)
    res.iters == 0 && return false
    return last(res.primal_trace) <= ε_pri && last(res.dual_trace) <= ε_dual
end

export AdmmResiduals, record!, converged
```

`docs/src/api.md`'s existing "ADMM Decomposition" `@autodocs` block, the shape a NEW
"## MPC / Rolling-Horizon" block mirrors (verified, `docs/src/api.md:89-95`):

```
## ADMM Decomposition

```@autodocs
Modules = [TSODSO]
Pages = ["admm/ReactiveMode.jl", "admm/AgrOpt.jl", "admm/DsoOpt.jl", "admm/residuals.jl", "admm/solve_admm.jl"]
Order = [:type, :constant, :function]
```
```

`src/TSODSO.jl`'s include block this plan's new line joins (verified, `src/TSODSO.jl:105-106`,
right after the `models/ac_dual_fallback.jl` include and before the `pricing/` block):

```
include("models/ac_dual_fallback.jl")
# ... (this plan inserts include("models/mpc_trace.jl") here, or immediately after)
include("pricing/dlmp.jl")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MpcTrace struct + record! + query predicates</name>
  <files>src/models/mpc_trace.jl, src/TSODSO.jl, docs/src/api.md</files>
  <read_first>
    - src/admm/residuals.jl (full file — the exact struct/constructor/`_assert_sequential`/
      `record!`/`converged` shapes this file mirrors one-for-one)
    - src/TSODSO.jl (lines 84-111 — the `models/` include block this plan extends)
    - docs/src/api.md (full file — the `@autodocs` block convention, especially the "ADMM
      Decomposition" and "Diagnostics" blocks as the two closest shape analogs)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-CONTEXT.md, D-09/D-10 (the
      exact fields MPC-03 requires; the "Claude's discretion" note on exact norm definitions)
  </read_first>
  <behavior>
    - `MpcTrace()` constructs an empty ledger with `steps == 0` and every trace vector empty.
    - `record!(trace, 1, dadp, dadp_da, :certified_convex_dual)` succeeds; a second
      `record!(trace, 1, ...)` (repeating `k=1`) throws `ArgumentError`; `record!(trace, 3, ...)`
      (skipping `k=2`) also throws `ArgumentError`.
    - After `record!(trace, 1, 5.0, 5.0, :certified_convex_dual)` then
      `record!(trace, 2, 5.3, 5.1, :cert_failed)`: `trace.jump_trace == [0.0, 0.3]` (first step
      has no predecessor, so its jump is `0.0`; `abs(5.3-5.0)=0.3` thereafter),
      `trace.cum_deviation_trace == [0.0, 0.2]` (`abs(5.0-5.0)=0.0` then `+abs(5.3-5.1)=0.2`),
      `trace.cert_status_trace == [:certified_convex_dual, :cert_failed]`.
    - `max_jump`/`mean_jump` on an EMPTY trace return `0.0` (never `NaN`/error); on the populated
      trace above, `max_jump(trace) == 0.3`.
    - `any_cert_failed` on an empty trace returns `false`; on the populated trace above it
      returns `true` (one `:cert_failed` entry present); a trace with only `:certified_convex_dual`
      / `:local_ac_dual` entries returns `false` from `any_cert_failed` (only the literal
      `:cert_failed` symbol counts as a failure — `:local_ac_dual` is a SUCCESSFUL fallback
      escalation, not a failure, per D-04's ladder semantics).
  </behavior>
  <action>
    Create `src/models/mpc_trace.jl` with a header comment in the `src/admm/residuals.jl` shape
    (SEAM = "MpcTrace — the rolling-horizon price-consistency ledger (MPC-03)", OWNER = "plan
    21-02"), stating explicitly this is a pure data/bookkeeping type — NO JuMP, NO solves —
    mirroring `AdmmResiduals`'s own header note, so a future CairoMakie plotting extension or the
    literate page can consume it without pulling JuMP.

    Define `mutable struct MpcTrace` with fields `dadp_trace::Vector{Float64}` (the published
    price per step), `dadp_da_trace::Vector{Float64}` (the day-ahead reference price at that
    SAME absolute hour, recorded alongside for the cumulative-deviation computation and for a
    caller to reconstruct the raw deviation series), `jump_trace::Vector{Float64}`,
    `cum_deviation_trace::Vector{Float64}`, `cert_status_trace::Vector{Symbol}`, `steps::Int`.
    Add `MpcTrace() = MpcTrace(Float64[], Float64[], Float64[], Float64[], Symbol[], 0)`.

    Add a local `@inline _assert_sequential(trace::MpcTrace, k::Integer)` — reimplemented
    (never imported from `admm/residuals.jl` — this file stays file-disjoint per the project's
    established seam convention), identical logic to `AdmmResiduals`'s own helper: throw
    `ArgumentError("record!: expected sequential step $(trace.steps + 1), got k=$k")` unless
    `k == trace.steps + 1`.

    Add `record!(trace::MpcTrace, k::Integer, dadp::Real, dadp_da::Real, cert_status::Symbol)
    -> MpcTrace`: call `_assert_sequential`; compute
    `jump = trace.steps == 0 ? 0.0 : abs(float(dadp) - last(trace.dadp_trace))`; compute
    `cum = (trace.steps == 0 ? 0.0 : last(trace.cum_deviation_trace)) + abs(float(dadp) - float(dadp_da))`;
    `push!` `dadp`, `dadp_da`, `jump`, `cum`, `cert_status` onto their respective traces;
    `trace.steps += 1`; `return trace`.

    Add three query functions, mirroring `converged`'s "empty ledger ⇒ safe default" convention:
    `max_jump(trace::MpcTrace) = trace.steps == 0 ? 0.0 : maximum(trace.jump_trace)`,
    `mean_jump(trace::MpcTrace) = trace.steps == 0 ? 0.0 : sum(trace.jump_trace) / trace.steps`,
    `any_cert_failed(trace::MpcTrace) = trace.steps == 0 ? false : any(==(:cert_failed), trace.cert_status_trace)`.

    End the file with `export MpcTrace, record!, max_jump, mean_jump, any_cert_failed`.

    In `src/TSODSO.jl`, insert `include("models/mpc_trace.jl")` immediately after line 105's
    `include("models/ac_dual_fallback.jl")` and before the `pricing/` block's first include, with
    a one-line comment: "# --- MpcTrace: rolling-horizon price-consistency ledger (owned by plan
    21-02, MPC-03) --- JuMP-free, no ordering dependency; placed beside the other models/ files."

    In `docs/src/api.md`, add a new section (after "## ADMM Decomposition", before "## Diagnostics")
    titled `## MPC / Rolling-Horizon` with an `@autodocs` block: `Modules = [TSODSO]`,
    `Pages = ["models/mpc_trace.jl"]`, `Order = [:type, :constant, :function]` — this Pages list
    grows additively in Wave 2/3/4's plans as `mpc_window.jl`/`mpc_loop.jl` land.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, Test; t = MpcTrace(); @test t.steps == 0; @test max_jump(t) == 0.0; @test any_cert_failed(t) == false; record!(t, 1, 5.0, 5.0, :certified_convex_dual); record!(t, 2, 5.3, 5.1, :cert_failed); @test t.jump_trace == [0.0, 0.3]; @test t.cum_deviation_trace == [0.0, 0.2]; @test max_jump(t) == 0.3; @test any_cert_failed(t) == true; @test_throws ArgumentError record!(t, 2, 5.4, 5.1, :certified_convex_dual); @test_throws ArgumentError record!(t, 4, 5.4, 5.1, :certified_convex_dual); println("OK")'</automated>
  </verify>
  <done>MpcTrace exists, exports MpcTrace/record!/max_jump/mean_jump/any_cert_failed, is included in TSODSO.jl, and is wired into a new api.md MPC section.</done>
  <acceptance_criteria>
    - `julia --project=. -e 'using TSODSO; @assert isdefined(TSODSO, :MpcTrace) && isdefined(TSODSO, :record!) && isdefined(TSODSO, :max_jump) && isdefined(TSODSO, :mean_jump) && isdefined(TSODSO, :any_cert_failed); println("OK")'` exits 0 and prints `OK`
    - `grep -n 'models/mpc_trace.jl' docs/src/api.md` matches
    - `grep -n 'include("models/mpc_trace.jl")' src/TSODSO.jl` matches, positioned after the `ac_dual_fallback.jl` include line
    - Running the verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Permanent @testitem coverage for MpcTrace</name>
  <files>test/test_mpc_trace.jl</files>
  <read_first>
    - src/models/mpc_trace.jl (Task 1's output, full file)
    - test/test_admm_dualresid.jl (lines 40-75 — the closest existing `AdmmResiduals`
      sequential-record!/converged-predicate `@testitem` shape this file mirrors)
    - test/test_aggregator.jl (lines 1-8 — the file-header "name contains X so
      `occursin(X, ti.name)`" convention this new file must also state and follow)
  </read_first>
  <action>
    Create `test/test_mpc_trace.jl` with a file-header comment in the established shape: SEAM =
    `src/models/mpc_trace.jl` (MPC-03), every item name contains `"mpc_trace"` so
    `occursin("mpc_trace", ti.name)` selects the whole file, tagged `tags = [:mpc_trace]`.

    Add `@testitem "mpc_trace: empty ledger predicates and construction (MPC-03)"` — asserts
    `MpcTrace()` has `steps == 0`, every trace field `isempty`, and `max_jump`/`mean_jump`/
    `any_cert_failed` return their documented safe defaults on the empty ledger.

    Add `@testitem "mpc_trace: record! sequential-k fail-loud guard (mirrors AdmmResiduals)"` —
    builds a fresh `MpcTrace()`, records `k=1`, then asserts `@test_throws ArgumentError` on
    BOTH a repeated `k=1` and a skipped `k=3` call (mirrors this plan's Task 1 verify script,
    made permanent).

    Add `@testitem "mpc_trace: jump/cumulative-deviation/cert-status derived correctly across
    N steps (MPC-03 price-consistency metrics)"` — records at least 4 sequential steps with a
    mix of `:certified_convex_dual`, `:cert_failed`, and `:local_ac_dual` statuses and
    hand-computed `dadp`/`dadp_da` values; asserts every trace vector's exact expected value
    (not just non-error), `max_jump`/`mean_jump` match hand-computed values, and
    `any_cert_failed` is `true` (from the `:cert_failed` entry) while a SEPARATE all-
    `:certified_convex_dual`-and-`:local_ac_dual` trace's `any_cert_failed` is `false`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, Test; t = MpcTrace(); record!(t, 1, 4.0, 4.0, :certified_convex_dual); record!(t, 2, 4.5, 4.2, :local_ac_dual); record!(t, 3, 4.1, 4.3, :cert_failed); record!(t, 4, 4.1, 4.1, :certified_convex_dual); @test t.jump_trace == [0.0, 0.5, 0.4, 0.0]; @test t.cum_deviation_trace == [0.0, 0.3, 0.5, 0.5]; @test max_jump(t) == 0.5; @test mean_jump(t) ≈ 0.225; @test any_cert_failed(t) == true; t2 = MpcTrace(); record!(t2, 1, 1.0, 1.0, :certified_convex_dual); record!(t2, 2, 1.0, 1.0, :local_ac_dual); @test any_cert_failed(t2) == false; println("OK")'</automated> -- per-task quick check: plain Test.jl under --project=., hand-computed expected values; the persistent @testitem execution runs in this plan's closing &lt;verification&gt;.
  </verify>
  <done>test/test_mpc_trace.jl exists with three green @testitems covering empty-ledger defaults, the sequential-k guard, and hand-verified derived metrics.</done>
  <acceptance_criteria>
    - `grep -c '@testitem' test/test_mpc_trace.jl` returns 3
    - Every item name contains the substring `mpc_trace`
    - Running the verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator-supplied `record!` arguments (Wave 3/4's `mpc_loop.jl`) | The only "input" surface; a future in-process caller, not attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | A future caller double-records or skips a step, silently corrupting the trace's length invariants | mitigate | `_assert_sequential` throws `ArgumentError` loudly on either misuse, mirroring `AdmmResiduals`'s own guard exactly |
| T-21-05 | Repudiation | `any_cert_failed` silently miscounts `:local_ac_dual` (a successful fallback) as a failure, making a healthy loop look broken (or vice versa) | mitigate | Task 2's third `@testitem` explicitly asserts the `:local_ac_dual`-only trace is NOT flagged, distinguishing "escalated but resolved" from "genuinely unresolved" |
| T-21-06 | Information Disclosure | N/A — no secrets, no network surface | accept | out of scope for a local optimization library |
</threat_model>

<verification>
Targeted regression (not the full suite): `julia --project=. -e 'import Pkg; Pkg.precompile()'`
confirms the package loads cleanly after the new `src/models/mpc_trace.jl` file and its
`src/TSODSO.jl` include. Re-run both task-level `<verify>` inline scripts above (idempotent) and
confirm both print `OK`. Defer the actual `@testitem` execution to Wave 2's closing verification
and the phase-closing full-suite acceptance (21-06).
</verification>

<success_criteria>
- `MpcTrace`/`record!`/`max_jump`/`mean_jump`/`any_cert_failed` exist, are exported, and are
  documented via a new `docs/src/api.md` section.
- The sequential-`k` fail-loud guard matches `AdmmResiduals`'s contract exactly.
- Three permanent `@testitem`s exist proving the ledger's empty-state defaults, guard, and
  derived-metric correctness.
</success_criteria>

<output>
Create `.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-02-SUMMARY.md` when done.
Record `MpcTrace`'s final field list and `record!`'s exact signature verbatim — Wave 3/4's
`mpc_loop.jl` calls `record!` directly and must match this signature exactly.
</output>
