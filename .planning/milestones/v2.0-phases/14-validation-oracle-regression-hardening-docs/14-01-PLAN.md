---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures_planning.jl
  - test/test_planning_goldens.jl
autonomous: true
requirements: [PVAL-02]

must_haves:
  truths:
    - "The N=1 certified Stackelberg equilibrium (z*=0.7, cost=-0.245) is regression-pinned in a dedicated goldens file and re-verified on every test run, not left only inside test_planning_certification.jl."
    - "The N=2 Nash equilibrium (z=[0.6,0.6], x_inv=[0.3,0.3]) is regression-pinned and gated by run_nash!'s own convergence flag BEFORE the pinned value is compared."
    - "A 3-seed x 2-order Nash probe's reported spread stays within a documented, empirically-derived bound, so probe non-determinism cannot silently regress undetected while still tolerating solver noise."
  artifacts:
    - path: test/fixtures_planning.jl
      provides: "PlanningFixtures @testmodule holding N=1/N=2 golden consts + probe-spread bounds, matching fixtures_phase4.jl's computed-golden convention"
      min_lines: 30
    - path: test/test_planning_goldens.jl
      provides: "3 @testitems: N=1 gate-then-golden, N=2 gate-then-golden, N=2 probe gate-then-spread-bound"
      min_lines: 60
  key_links:
    - from: test/test_planning_goldens.jl
      to: test/fixtures_planning.jl
      via: "setup=[..., PlanningFixtures] consumption of pinned consts"
      pattern: "PlanningFixtures\\."
    - from: test/test_planning_goldens.jl
      to: src/planning/benders.jl and src/planning/nash.jl
      via: "direct calls to solve_stackelberg!/run_nash!/run_nash_probe production entrypoints, never re-implemented math"
      pattern: "solve_stackelberg!|run_nash!|run_nash_probe"
---

<objective>
Pin the two one-off validation results this phase must turn into permanent regression
infrastructure (PVAL-02): the Phase 11 BilevelJuMP-certified N=1 Stackelberg equilibrium and the
Phase 13 hand-checked N=2 Nash equilibrium, plus a bounded Nash-probe spread check. Today these
values exist only as inline consts scattered across `test/test_planning_certification.jl` and
`test/test_planning_nash.jl` — this plan extracts them into a dedicated, `fixtures_phase4.jl`-
style goldens module and a dedicated gate-then-value regression file, following the exact
convention `test/test_acceptance.jl` already established for the v1.0 operational goldens.

Purpose: make "does the planning layer still reach the SAME certified/hand-checked answer" a
permanent, fast, default-on regression — not something only re-discovered by re-reading a Phase
11/13 test file's header comment.
Output: `test/fixtures_planning.jl` (new `@testmodule PlanningFixtures`), `test/test_planning_goldens.jl`
(new, 3 `@testitem`s, tag `[:planning]`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-oracle-regression-hardening-docs/14-CONTEXT.md
@.planning/phases/14-validation-oracle-regression-hardening-docs/14-RESEARCH.md

@test/fixtures_phase4.jl
@test/test_acceptance.jl
@test/test_planning_certification.jl
@test/test_planning_nash.jl

Note on the working tree: `git status` currently shows the ROOT `Project.toml`/
`Manifest-v1.12.toml` as modified-uncommitted (CairoMakie promoted from `[weakdeps]` to
`[deps]`, a pre-existing user-local state per 14-RESEARCH.md Pitfall 5). Do NOT stage, commit,
or revert those two files as part of this plan's work — they are out of scope and unrelated to
`test/`.

<interfaces>
<!-- Exact production entrypoint signatures this plan's tests call. No codebase exploration
     needed — call these directly. -->

From src/planning/benders.jl:110 (used by testitem 1):
```
solve_stackelberg!(feeder, pf::AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator};
    λ₀, T::Int, follower_kwargs::NamedTuple, master_kwargs::NamedTuple,
    tol::Real = 1e-6, max_iter::Int = 100, checkpoint_dir::AbstractString) -> result with fields
    result.gap, result.y, result.z, result.UB
```

From src/planning/nash.jl:371 (used by testitem 2):
```
run_nash!(specs::AbstractVector{<:NamedTuple}, shared::SharedTransmission;
    z0::AbstractMatrix{<:Real}, tol_outer::Real = 1e-4, max_sweeps::Int = 50,
    checkpoint_dir::AbstractString) -> result with fields
    result.converged, result.z, result.x_inv
```

From src/planning/nash.jl:739 (used by testitem 3):
```
run_nash_probe(specs::AbstractVector{<:NamedTuple}, build_shared::Function;
    seeds::NamedTuple, orders::Tuple = (:forward, :reverse), tol_outer::Real = 1e-4,
    max_sweeps::Int = 50, checkpoint_dir::AbstractString) -> result with fields
    result.n_runs, result.runs (Vector, each with .result.converged), result.summary::String,
    result.spread.z_spread, result.spread.x_inv_spread, result.spread.cost_spread
```

From src/planning/coupling.jl:173 (used by testitem 2/3):
```
build_shared_transmission(; N::Int, T::Int, corridor_cap::Real, x_inv_max::AbstractVector{<:Real},
    c_inv::AbstractVector{<:Real}, c_op::AbstractVector{<:AbstractVector{<:Real}}) -> SharedTransmission
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/fixtures_planning.jl — PlanningFixtures goldens module</name>
  <files>test/fixtures_planning.jl</files>
  <read_first>
    test/fixtures_phase4.jl (the exact `@testmodule Phase4Fixtures begin ... end` shape, the
    top-level `const` + rationale-comment convention, the `export` line at end of module,
    the T-04-08 "defines data only, no top-level call to a not-yet-defined symbol" contract)
    test/test_planning_certification.jl lines 1-74 (header + `BilevelCertFixture`'s
    `Y_HAND`/`Z_HAND`/`OBJ_HAND` consts — the exact N=1 RE-DERIVED values and their derivation)
    test/test_planning_nash.jl lines 227-321 (the N=2 symmetric toy fixture, hand-derivation
    comment, and the pinned `result.z`/`result.x_inv` values)
    test/test_planning_nash.jl lines 610-669 (the `run_nash_probe` fixture/seeds/orders shape
    and the `result.spread.z_spread`/`x_inv_spread`/`cost_spread` fields, currently only
    asserted `>= 0 && isfinite`, never bounded)
  </read_first>
  <action>
    Create test/fixtures_planning.jl defining `@testmodule PlanningFixtures begin ... end`
    (mirror test/fixtures_phase4.jl's module shape exactly — `using TSODSO` inside, plain
    top-level `const`s with a rationale comment per value, `export` line at the end). Define:
    `N1_Y_HAND = 0.7`, `N1_Z_HAND = 0.7`, `N1_OBJ_HAND = -0.245` — comment cites
    test/test_planning_certification.jl's `BilevelCertFixture` (lines 69-74) as the RE-DERIVED
    hand-enumeration source, explicitly noting this is NOT 11-01-PLAN.md's original incorrect
    y*=1.0/z*=1.0/-0.2. Define `N2_Z_HAND = [0.6, 0.6]`, `N2_XINV_HAND = [0.3, 0.3]` — comment
    cites test/test_planning_nash.jl lines 242-256's hand-derivation (pooled capacity
    corridor_cap*(x_inv_1+x_inv_2)=1.2 caps z_i<=0.6 each, binding below the unconstrained
    z*=0.7). Define three Float64 bound consts `N2_PROBE_Z_SPREAD_MAX`,
    `N2_PROBE_XINV_SPREAD_MAX`, `N2_PROBE_COST_SPREAD_MAX` — per CONTEXT.md's Claude's-
    Discretion resolution, pin a LOOSE UPPER BOUND (not an exact spread value), since the
    equilibrium point z=[0.6,0.6] is the stable hand-derived quantity while spread-across-
    seeds/orders is a solver-numerics-sensitive diagnostic. To derive each bound: run the SAME
    3-seed x 2-order probe shape as test/test_planning_nash.jl lines 622-660
    (`build_shared_transmission(; N=2, T=1, corridor_cap=2.0, x_inv_max=[0.3,0.3],
    c_inv=[1.0,1.0], c_op=[[0.5],[0.5]])`, `seeds = (; zero=zeros(2,1),
    saturating=fill(2.0*(0.3+0.3)/2, 2, 1), skewed=[0.5;0.1;;])`, `orders=(:forward,:reverse)`)
    once locally, observe the empirical `result.spread.z_spread`/`x_inv_spread`/`cost_spread`,
    and hardcode each bound at generous headroom (document the OBSERVED value in a comment next
    to each bound, e.g. "observed z_spread ≈ X; bound pinned with 5x headroom"). End the module
    with `export N1_Y_HAND, N1_Z_HAND, N1_OBJ_HAND, N2_Z_HAND, N2_XINV_HAND,
    N2_PROBE_Z_SPREAD_MAX, N2_PROBE_XINV_SPREAD_MAX, N2_PROBE_COST_SPREAD_MAX`.
  </action>
  <acceptance_criteria>
    - File begins with `@testmodule PlanningFixtures begin` and contains `using TSODSO`
    - Contains exactly 8 named `const`s: N1_Y_HAND == 0.7, N1_Z_HAND == 0.7,
      N1_OBJ_HAND == -0.245, N2_Z_HAND == [0.6, 0.6], N2_XINV_HAND == [0.3, 0.3], and the
      three N2_PROBE_*_SPREAD_MAX Float64 bounds
    - Each of the three N2_PROBE_*_SPREAD_MAX consts has an adjacent comment stating the
      OBSERVED empirical spread value it was derived from
    - `export` line lists all 8 consts
    - The module makes no top-level call to any TSODSO solve entrypoint (mirrors
      fixtures_phase4.jl's T-04-08 contract — consts/data only)
    - `julia -e 'Meta.parseall(read("test/fixtures_planning.jl", String))'` parses without a
      syntax error
  </acceptance_criteria>
  <verify>
    <automated>julia -e 'Meta.parseall(read("test/fixtures_planning.jl", String)); println("PARSE_OK")' | grep -q PARSE_OK</automated>
  </verify>
  <done>test/fixtures_planning.jl exists with all 8 consts, correct values, rationale comments, and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Create test/test_planning_goldens.jl — gate-then-golden regression</name>
  <files>test/test_planning_goldens.jl</files>
  <read_first>
    test/fixtures_planning.jl (just created — exact const names to reference)
    test/test_acceptance.jl lines 1-30 (the "consolidates without duplicating" header
    convention and the gate-before-golden ordering pattern, e.g.
    `@test ctx.meta[:socp_maxgap] < 1e-5` then `@test isapprox(res.cost, GOLDEN_WELFARE; rtol=1e-4)`)
    test/test_planning_certification.jl lines 170-227 (the exact toy-instance construction
    and `solve_stackelberg!` call to copy verbatim for the N=1 testitem, and its
    `result.gap <= 1e-6` gate)
    test/test_planning_nash.jl lines 257-321 (the exact `build_shared_transmission`/spec/
    `run_nash!` call to copy verbatim for the N=2 testitem, and its `result.converged` gate)
    test/test_planning_nash.jl lines 618-669 (the exact `run_nash_probe` call, seeds/orders,
    and gate assertions — `result.n_runs == 6`, `all(r -> r.result.converged, result.runs)`,
    `occursin("a converged equilibrium", result.summary)`,
    `!occursin("the equilibrium", result.summary)` — to copy verbatim for the probe testitem)
  </read_first>
  <action>
    Create test/test_planning_goldens.jl. Header comment (mirror test/test_acceptance.jl
    lines 1-22's convention): state this file does NOT duplicate or re-execute
    test/test_planning_certification.jl's BilevelJuMP certification (PVAL-01 stays in-suite,
    untouched, in the same `Pkg.test` gate) — it adds the DEDICATED PVAL-02 goldens module and
    re-asserts each production entrypoint's own convergence gate before comparing against the
    pinned value. Add exactly 3 `@testitem`s, each `tags = [:planning]`:
    (1) `"planning goldens: N=1 certified Stackelberg equilibrium — gap gate then pinned golden regression (PVAL-02)"`,
    `setup = [Phase6Fixtures, ToyDeviceFixture, PlanningFixtures]` — build
    `feeder = Phase6Fixtures.two_bus_feeder()`, `dev = ToyDeviceFixture.ToyElasticDevice(2, 6.0, 1.0, 10.0)`,
    `agg = TSODSO.Aggregator(2, 0.9, [dev], [0.0])`, `λ₀ = [4.0]`,
    `follower_kwargs = (; corridor_cap = 2.0, x_inv_max = 2.0, c_inv = 1.0, c_op = [0.5])`,
    `master_kwargs = (; c_y = 0.3, y_max = 8.0, α_op_lb = -5.0, α_x_lb = 0.0)`; set
    `checkpoint_dir = mktempdir()`; call `solve_stackelberg!(feeder, LinDistFlow(), [agg]; λ₀=λ₀, T=1,
    follower_kwargs=follower_kwargs, master_kwargs=master_kwargs, tol=1e-6, max_iter=100,
    checkpoint_dir=checkpoint_dir)`; GATE first: `@test result.gap <= 1e-6`; VALUE second:
    `@test isapprox(result.y, PlanningFixtures.N1_Y_HAND; atol=1e-3)`,
    `@test isapprox(result.z[1], PlanningFixtures.N1_Z_HAND; atol=1e-3)`,
    `@test isapprox(result.UB, PlanningFixtures.N1_OBJ_HAND; atol=1e-3)`.
    (2) `"planning goldens: N=2 Nash equilibrium — convergence gate then pinned golden regression (PVAL-02)"`,
    `setup = [Phase6Fixtures, ToyDeviceFixture, PlanningFixtures]` — build
    `shared = build_shared_transmission(; N=2, T=1, corridor_cap=2.0, x_inv_max=[0.3,0.3],
    c_inv=[1.0,1.0], c_op=[[0.5],[0.5]])`, the `spec` NamedTuple and `specs = [spec, spec]`
    verbatim from test_planning_nash.jl lines 262-279; call
    `result = run_nash!(specs, shared; z0=zeros(2,1), tol_outer=1e-4, max_sweeps=50, checkpoint_dir=mktempdir())`;
    GATE first: `@test result.converged`; VALUE second:
    `@test isapprox(result.z, PlanningFixtures.N2_Z_HAND; atol=1e-3)`,
    `@test isapprox(result.x_inv, PlanningFixtures.N2_XINV_HAND; atol=1e-3)`.
    (3) `"planning goldens: N=2 multi-seed/multi-order probe — gating checks then spread-bound regression (PVAL-02)"`,
    `setup = [Phase6Fixtures, ToyDeviceFixture, PlanningFixtures]` — build the SAME `spec`/
    `specs` plus `build_shared` closure and `seeds`/`orders` verbatim from
    test_planning_nash.jl lines 622-650; call
    `result = run_nash_probe(specs, build_shared; seeds=seeds, orders=orders, tol_outer=1e-4, max_sweeps=50, checkpoint_dir=mktempdir())`;
    GATE first: `@test result.n_runs == 6`, `@test all(r -> r.result.converged, result.runs)`,
    `@test occursin("a converged equilibrium", result.summary)`,
    `@test !occursin("the equilibrium", result.summary)`; VALUE second:
    `@test result.spread.z_spread <= PlanningFixtures.N2_PROBE_Z_SPREAD_MAX`,
    `@test result.spread.x_inv_spread <= PlanningFixtures.N2_PROBE_XINV_SPREAD_MAX`,
    `@test result.spread.cost_spread <= PlanningFixtures.N2_PROBE_COST_SPREAD_MAX`.
  </action>
  <acceptance_criteria>
    - File contains exactly 3 `@testitem`s, all tagged `[:planning]`, all with
      `setup=[Phase6Fixtures, ToyDeviceFixture, PlanningFixtures]`
    - Testitem 1's `result.gap <= 1e-6` assertion appears at an earlier line number than its
      three `isapprox(..., PlanningFixtures.N1_*` assertions
    - Testitem 2's `result.converged` assertion appears at an earlier line number than its two
      `isapprox(..., PlanningFixtures.N2_*` assertions
    - Testitem 3's `result.n_runs == 6` and the "a converged equilibrium"/"the equilibrium"
      occursin pair appear at earlier line numbers than its three `<=` spread-bound assertions
    - Header comment contains the literal string `test_planning_certification.jl` and states
      PVAL-01 is not duplicated
    - Running `TestItemRunner.run_tests("test"; filter = ti -> occursin("planning goldens", ti.name))`
      from a scratch dev-linked environment (per the documented 13-02-SUMMARY.md/13-03-SUMMARY.md
      workaround: copy test/Project.toml to a scratch dir, `Pkg.develop(path=pwd())` to register
      TSODSO, run TestItemRunner from there against an explicit absolute `test/` path — never the
      bare `@run_package_tests` macro from inside a worktree) reports 3 passed, 0 failed
    - Combined runtime of the 3 testitems stays under ~60s (tiny toy fixtures only, per
      CONTEXT.md's runtime budget)
  </acceptance_criteria>
  <verify>
    <automated>julia --project=&lt;scratch-env&gt; -e 'using TestItemRunner; TestItemRunner.run_tests("&lt;abs-path&gt;/test"; filter = ti -> occursin("planning goldens", ti.name))'</automated>
  </verify>
  <done>test/test_planning_goldens.jl exists with 3 passing gate-then-golden testitems, all green from a scratch dev-linked environment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is test-only code with no untrusted input, no network, no authentication. The only
"boundary" of note is the gate-then-value assertion ordering itself (a future accidental edit
could invert it and mask a real regression).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | test/test_planning_goldens.jl assertion ordering | mitigate | gate-then-value ordering enforced by acceptance criteria (gate assertion must precede value assertion in source, checkable by line number) — a solver regression cannot be masked by only weakening the value check while the gate still fails loudly first |
| T-14-02 | Repudiation | test/fixtures_planning.jl golden constants | accept | values are hand-derivable and cross-referenced to already-certified/hand-checked test files (test_planning_certification.jl, test_planning_nash.jl); no external or untrusted source |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | no new package-manager installs in this plan; all dependencies (JuMP, TestItemRunner, TSODSO itself) already vetted and pinned since prior phases |
</threat_model>

<verification>
Full local proof before considering this plan done:
1. `julia -e 'Meta.parseall(read("test/fixtures_planning.jl", String))'` parses cleanly.
2. `TestItemRunner.run_tests("test"; filter = ti -> occursin("planning goldens", ti.name))` from
   a scratch dev-linked environment reports 3 passed, 0 failed.
3. `julia --project=. -e 'using Pkg; Pkg.test("TSODSO")'` (full suite) stays green — the new
   file must not break any existing `[:planning]`-tagged item.
</verification>

<success_criteria>
- test/fixtures_planning.jl exists with the 8 pinned/bounded consts, matching fixtures_phase4.jl's convention.
- test/test_planning_goldens.jl exists with 3 gate-then-golden `@testitem`s, all green.
- PVAL-02 roadmap success criterion ("canonical N=1/N=2 fixtures pinned as computed goldens,
  gated by BilevelJuMP agreement and diagonalization convergence") is satisfied.
</success_criteria>

<output>
Create `.planning/phases/14-validation-oracle-regression-hardening-docs/14-01-SUMMARY.md` when done
</output>
