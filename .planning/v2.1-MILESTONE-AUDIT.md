---
milestone: v2.1
audited: 2026-07-26
status: passed
scores:
  requirements: 12/12
  phases: 4/4
  integration: 6/6
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: all (15-18)
    items:
      - "Nyquist: all 4 VALIDATION.md carry nyquist_compliant: false (unflipped draft flag from manual orchestration). Tests genuinely exist and pass per each VERIFICATION.md — this is a flag-flip artifact, not a coverage gap."
  - phase: 18-directional-thesis-reproduction
    items:
      - "ROADMAP.md wording 'reactive pricing active' is ambiguous (could imply ADMM reactive_consensus=true). Phase 18 correctly uses the centralized solve_welfare :balance_q dual channel; threading reactive_consensus into solve_welfare would be a MethodError. Self-documented in 18-RESEARCH Pitfall 3. Future ROADMAP wording pass."
  - phase: tooling
    items:
      - "Invoking `@run_package_tests` via `julia -e` resolves the test root via cwd and can pick up a STALE SIBLING worktree (e.g. TSO-DSO.worktrees/pdf-documentation-thesis-results with old LOAD_SCALE=0.03), producing a spurious assert_socp_exact! failure (gap ratio 1.378 = the old pre-retune point). test/runtests.jl (real file) is immune. Always use test/runtests.jl or TestItemRunner.run_tests(<explicit path>). Generalizes local-project-toml-drift beyond .claude/worktrees/."
nyquist:
  compliant_phases: 0
  partial_phases: 4
  missing_phases: 0
  overall: partial (advisory — VALIDATION.md exists for all 4 with per-req test maps; tests exist and pass; only the nyquist_compliant flag is unflipped)
---

# Milestone v2.1 (Validation & Reproduction) — Audit

**Status: PASSED.** 12/12 active requirements satisfied, 4/4 phases verified, 6/6 cross-phase
integration seams wired, E2E flow complete. No critical blockers. Remaining items are advisory
tech-debt (documentation/tooling), not gaps.

## Requirements Coverage (3-source cross-reference)

All 12 active v2.1 requirements are **satisfied** — each appears in a phase SUMMARY's
`requirements-completed`, is covered by a phase VERIFICATION.md with `status: passed`, and is
ticked `[x]` in REQUIREMENTS.md. `REPRO-STRETCH-01` remains deferred/out-of-scope (data-gated on
thesis App. E), correctly unchecked.

| REQ | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|-----|-------|--------------|---------------------|-----------------|-------|
| EXACT-01..04 | 15 | passed | listed (15-01/02/03) | [x] | satisfied |
| REACT-01..03 | 16 | passed | listed (16-01..04) | [x] | satisfied |
| IMPED-01..03 | 17 | passed | listed (17-02/03/04) | [x] | satisfied |
| REPRO-01..02 | 18 | passed | listed (18-01/02/03) | [x] | satisfied |

## Phase Verifications

| Phase | Status | Score |
|-------|--------|-------|
| 15 AC-Exactness Oracle | passed | 4/4 must-haves |
| 16 Reactive-μ Consensus | passed | 8/8 must-haves |
| 17 Real IEEE-123 Impedances | passed | 4/4 must-haves |
| 18 Directional Thesis Reproduction | passed | 4/4 must-haves |

## Cross-Phase Integration (6/6 wired, live-verified)

| Seam | Status | Evidence |
|------|--------|----------|
| 17→18 real impedances | WIRED | `thesis_case123_repro.jl:174` + `test_thesis_repro.jl:40` call `ieee123_modified()` → `IEEE123_BRANCH_RX_OHMS` (real table), not synthetic scalars; live `socp_maxgap=3.06e-07` matches golden |
| 17→18 re-tuned scales | WIRED | `fixtures_phase7.jl:93-95` (0.05/0.12/0.083) consumed via `Phase7Fixtures.build_ieee123_aggregators` (TestItems `setup=`); script copies verbatim (documented @testmodule workaround) |
| 16→18 reactive pricing | WIRED | `decompose_dlmp(ctx)` → `extract_reactive_dlmp` reads `dual(:balance_q)` (unconditional on ConvexBranchFlow); live reactive DLMP mean=0.103 pu, range [0, 0.246] |
| 15↔17/18 exactness gate | WIRED | `assert_ac_exact!` is Phase-15-only (standalone oracle); 17/18 use `assert_socp_exact!`/`socp_maxgap` — consistent, cross-referenced as the same knife-edge phenomenon |
| 15 dispatch | WIRED | `ACPowerFlow <: AbstractPowerFlow` via `problem_class` trait through UNCHANGED `solve_welfare` |
| E2E pipeline | COMPLETE | real-impedance IEEE-123 (17) → SOCP solve + reactive DLMP (16) → exactness gate (15 mechanism) → directional repro (18); all legs trace to real code |

Orphaned: 0. Missing: 0. Dangling references: 0.

## Advisory Tech-Debt (non-blocking)

1. **Nyquist flag unflipped** on all 4 VALIDATION.md (`nyquist_compliant: false`) — artifact of manual orchestration; tests exist + pass. Optional: `/gsd:validate-phase {N}` to flip retroactively.
2. **ROADMAP wording** "reactive pricing active" — clarify it means the centralized DLMP-dual channel, not ADMM `reactive_consensus`.
3. **Test-invocation gotcha** — never use `julia -e '@run_package_tests'` (picks up stale sibling worktree); use `test/runtests.jl` or explicit-path `run_tests`.

## Verdict

Ready to complete + archive. The two headline scientific results (SOCP-inexactness knife-edge under
high-PV reverse flow; honest directional-only thesis reproduction — DSO-surplus sign flip yes, +25%
magnitude no) are delivered, verified, and integrated.
