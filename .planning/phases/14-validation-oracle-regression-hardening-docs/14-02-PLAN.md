---
phase: 14
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_planning_noninteger.jl
  - test/test_planning_coupling.jl
  - test/test_planning_nash.jl
autonomous: true
requirements: [PVAL-04]

must_haves:
  truths:
    - "Every planning-layer subproblem builder (oracle, follower, master, shared transmission) is verified, in one place, to build a model with zero binary/integer variables."
    - "A future new builder file/function under src/planning/ cannot silently skip the no-binaries guard — an automated tripwire fails loudly if the registry falls out of sync with src/planning/*.jl's actual build_* functions."
    - "A guard failure names the offending builder and variables — never a bare, context-free assertion failure."
  artifacts:
    - path: test/test_planning_noninteger.jl
      provides: "consolidated 4-builder no-binaries @testitem + source-scan tripwire"
      min_lines: 40
  key_links:
    - from: test/test_planning_noninteger.jl
      to: src/planning/subproblem.jl, src/planning/follower.jl, src/planning/master.jl, src/planning/coupling.jl
      via: "direct build_* calls + readdir/regex scan over src/planning/*.jl"
      pattern: "build_planning_oracle|build_follower|build_master|build_shared_transmission"
---

<objective>
Consolidate the no-binaries continuous-only-scope guard (PVAL-04) that today exists only
partially — a SharedTransmission-only check duplicated across `test/test_planning_coupling.jl`
and `test/test_planning_nash.jl` — into one registry-based `@testitem` that covers all four
planning-layer subproblem builders, plus a source-scanning tripwire so a future new builder
cannot silently ship without this guard.

Purpose: enforce v2.0's continuous-only investment scope (PLAN-INT-01 is explicitly deferred to
a future milestone) automatically, not by convention or by remembering to add a check per new
builder file.
Output: `test/test_planning_noninteger.jl` (new, consolidated registry + tripwire), plus a
cross-reference comment in the two existing partial checks (kept, not removed — see Task 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-oracle-regression-hardening-docs/14-CONTEXT.md
@.planning/phases/14-validation-oracle-regression-hardening-docs/14-RESEARCH.md

@test/test_planning_coupling.jl
@test/test_planning_nash.jl
@test/test_planning_certification.jl

Note on the working tree: `git status` currently shows the ROOT `Project.toml`/
`Manifest-v1.12.toml` as modified-uncommitted (CairoMakie promoted from `[weakdeps]` to
`[deps]`, a pre-existing user-local state per 14-RESEARCH.md Pitfall 5). Do NOT stage, commit,
or revert those two files as part of this plan's work.

<interfaces>
<!-- Exact builder signatures verified from src/planning/*.jl. Call these directly — no
     codebase exploration needed. -->

From src/planning/subproblem.jl:110:
```
function build_planning_oracle(feeder, pf::AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator};
    λ₀, T::Int = 24) -> PlanningOracle   # has .model::Model field
```

From src/planning/follower.jl:94:
```
function build_follower(; T::Int, corridor_cap::Real, x_inv_max::Real, c_inv::Real,
    c_op::AbstractVector{<:Real}) -> FollowerLP   # has .model::Model field
```

From src/planning/master.jl:92:
```
function build_master(; T::Int, c_y::Real, y_max::Real, α_op_lb::Real, α_x_lb::Real) -> BendersMaster
    # has .model::Model field
```

From src/planning/coupling.jl:173-174:
```
function build_shared_transmission(; N::Int, T::Int, corridor_cap::Real,
    x_inv_max::AbstractVector{<:Real}, c_inv::AbstractVector{<:Real},
    c_op::AbstractVector{<:AbstractVector{<:Real}}) -> SharedTransmission   # has .model::Model field
```

Toy fixture values to reuse (verbatim from test/test_planning_certification.jl lines 176-181,
already used elsewhere in the planning test suite):
```
feeder = Phase6Fixtures.two_bus_feeder()
dev = ToyDeviceFixture.ToyElasticDevice(2, 6.0, 1.0, 10.0)
agg = TSODSO.Aggregator(2, 0.9, [dev], [0.0])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/test_planning_noninteger.jl — consolidated 4-builder guard + tripwire</name>
  <files>test/test_planning_noninteger.jl</files>
  <read_first>
    test/test_planning_coupling.jl lines 237-252 (the existing partial, SharedTransmission-only
    no-binaries check — the exact `using JuMP: all_variables, is_binary, is_integer` idiom to
    reuse verbatim, do not hand-roll an AST scan)
    src/planning/subproblem.jl lines 59-145 (PlanningOracle struct + build_planning_oracle
    signature/guards)
    src/planning/follower.jl lines 59-115 (FollowerLP struct + build_follower signature/guards)
    src/planning/master.jl lines 57-100 (BendersMaster struct + build_master signature/guards)
    src/planning/coupling.jl lines 112-200 (SharedTransmission struct + build_shared_transmission
    signature/guards)
    test/test_planning_certification.jl lines 176-181 (toy feeder/dev/agg construction to reuse
    for build_planning_oracle, the only builder needing a feeder)
  </read_first>
  <action>
    Create test/test_planning_noninteger.jl with a header comment stating this file
    CONSOLIDATES the existing partial SharedTransmission-only no-binaries checks
    (test/test_planning_coupling.jl ~line 237, test/test_planning_nash.jl ~line 320) into one
    registry covering all four planning-layer builders, per CONTEXT.md's PVAL-04 decision
    ("dedicated @testitem that BUILDS every planning-layer model via its public builder and
    asserts zero is_binary/is_integer variables — semantic check, not a grep lint"). Add one
    `@testitem "planning PVAL-04: no-binaries guard covers all four planning-layer builders + source-scan tripwire" tags = [:planning] setup = [Phase6Fixtures, ToyDeviceFixture]` containing:
    `using TSODSO` and `using JuMP: all_variables, is_binary, is_integer`; build
    `feeder = Phase6Fixtures.two_bus_feeder()`, `dev = ToyDeviceFixture.ToyElasticDevice(2, 6.0, 1.0, 10.0)`,
    `agg = TSODSO.Aggregator(2, 0.9, [dev], [0.0])`; define
    `registry = Dict{String,Function}(...)` mapping each of these 4 builder-name strings to a
    zero-arg closure returning `.model`: `"build_planning_oracle" => () -> build_planning_oracle(feeder, LinDistFlow(), [agg]; λ₀ = [4.0], T = 1).model`;
    `"build_follower" => () -> build_follower(; T = 1, corridor_cap = 2.0, x_inv_max = 2.0, c_inv = 1.0, c_op = [0.5]).model`;
    `"build_master" => () -> build_master(; T = 1, c_y = 0.3, y_max = 8.0, α_op_lb = -5.0, α_x_lb = 0.0).model`;
    `"build_shared_transmission" => () -> build_shared_transmission(; N = 2, T = 1, corridor_cap = 2.0, x_inv_max = [0.3, 0.3], c_inv = [1.0, 1.0], c_op = [[0.5], [0.5]]).model`.
    Loop `for (name, build) in registry`: build the model, compute
    `offenders = [v for v in all_variables(model) if is_binary(v) || is_integer(v)]`, then assert
    with a message string that embeds `name`, e.g.
    `@test isempty(offenders) "builder $(name) introduced binary/integer variable(s): $(offenders)"`
    (fail-loud convention — name the builder AND the offending variables, per CONTEXT.md's
    explicit requirement). After the loop, add the tripwire: `planning_dir = joinpath(pkgdir(TSODSO), "src", "planning")`;
    scan every `.jl` file under it with `readdir(planning_dir; join = true)` + `eachline` +
    `match(r"^function (build_\w+)\(", line)`, collecting matches into `found::Set{String}`;
    assert `@test found == Set(keys(registry))` — this is the source-of-truth check that a
    future new `build_*` function in `src/planning/` cannot silently skip the registry.
  </action>
  <acceptance_criteria>
    - File contains exactly one `@testitem`, tagged `[:planning]`, `setup = [Phase6Fixtures, ToyDeviceFixture]`
    - `registry` has exactly 4 keys: "build_planning_oracle", "build_follower", "build_master",
      "build_shared_transmission" — matching the 4 verified builder function names
    - Each registry closure's keyword arguments match the exact signatures read from
      src/planning/*.jl (T=1 throughout, N=2 for build_shared_transmission)
    - The `isempty(offenders)` assertion's failure message string contains `name` (the builder
      name variable), satisfying the fail-loud convention
    - The tripwire compares `found` (regex-scanned from src/planning/*.jl) against
      `Set(keys(registry))` via `==`
    - Running `TestItemRunner.run_tests("test"; filter = ti -> occursin("no-binaries guard", ti.name))`
      from a scratch dev-linked environment reports 1 passed, 0 failed
  </acceptance_criteria>
  <verify>
    <automated>julia --project=&lt;scratch-env&gt; -e 'using TestItemRunner; TestItemRunner.run_tests("&lt;abs-path&gt;/test"; filter = ti -> occursin("no-binaries guard", ti.name))'</automated>
  </verify>
  <done>test/test_planning_noninteger.jl exists, passes, and its tripwire's found-set equals its registry's key-set against the current src/planning/ tree.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference the two existing partial checks (kept, not removed)</name>
  <files>test/test_planning_coupling.jl, test/test_planning_nash.jl</files>
  <read_first>
    test/test_planning_coupling.jl lines 237-252 (the existing SharedTransmission-only
    `@testitem`, to locate the exact insertion point for the new comment)
    test/test_planning_nash.jl lines 317-321 (the existing companion check, run AFTER a
    `run_nash!` write-back/activate mutation cycle — a genuinely different code path than a
    fresh build, which is why it is kept rather than deleted)
  </read_first>
  <action>
    In test/test_planning_coupling.jl, immediately above the
    `@testitem "planning coupling: PVAL-04 continuous-only regression — no binary/integer variable anywhere in the shared model" tags = [:planning] begin`
    line (currently line 237), add a one-line comment:
    `# NOTE: consolidated coverage of all 4 planning-layer builders now also lives in test/test_planning_noninteger.jl; this SharedTransmission-only check is kept as redundant post-build coverage (harmless, not removed).`
    In test/test_planning_nash.jl, immediately above the
    `@test all(v -> !is_binary(v) && !is_integer(v), all_variables(shared.model))` line
    (currently ~line 320), add a one-line comment:
    `# NOTE: consolidated coverage of all 4 planning-layer builders now also lives in test/test_planning_noninteger.jl; this check additionally covers the POST-run_nash!-mutation state (a genuinely different code path than a fresh build), so it is kept, not removed.`
    Do not otherwise modify either file's test bodies, assertions, or structure.
  </action>
  <acceptance_criteria>
    - test/test_planning_coupling.jl contains the new comment string immediately preceding its
      existing no-binaries `@testitem` declaration
    - test/test_planning_nash.jl contains the new comment string immediately preceding its
      existing no-binaries `@test` line
    - Both files' existing test bodies are unchanged except for the two added comment lines
      (verifiable via `git diff` showing only comment-line additions, no other line changes)
    - Running `TestItemRunner.run_tests("test"; filter = ti -> occursin("planning coupling", ti.name) || occursin("planning nash", ti.name))`
      from a scratch dev-linked environment reports the SAME pass/fail counts as a baseline run
      captured before this task's edit (no regression introduced)
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat test/test_planning_coupling.jl test/test_planning_nash.jl</automated>
  </verify>
  <done>Both existing partial checks carry a cross-reference comment to test/test_planning_noninteger.jl; no behavioral change; full planning test subset still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is test-only code with no untrusted input, no network, no authentication. The relevant
"boundary" is the src/planning/ builder surface itself — the guard exists to prevent a future
code change from silently crossing the continuous-only scope line.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | src/planning/*.jl builders (continuous-only invariant) | mitigate | consolidated no-binaries guard (test/test_planning_noninteger.jl) semantically checks `is_binary`/`is_integer` over `all_variables(model)` for all 4 builders — survives refactors that change HOW a variable is declared, not just a grep on `Bin`/`Int` literals |
| T-14-04 | Repudiation | registry-to-source-tree coverage claim | mitigate | source-scan tripwire (`readdir`+regex over src/planning/*.jl compared via Set-equality to the registry's keys) — a future new `build_*` function cannot silently ship uncovered; the tripwire itself fails loudly if it does |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | no new package-manager installs in this plan; JuMP's own `is_binary`/`is_integer` API is reused, already a project dependency |
</threat_model>

<verification>
Full local proof before considering this plan done:
1. `TestItemRunner.run_tests("test"; filter = ti -> occursin("no-binaries guard", ti.name))` from
   a scratch dev-linked environment reports 1 passed, 0 failed.
2. `TestItemRunner.run_tests("test"; filter = ti -> occursin("planning coupling", ti.name) || occursin("planning nash", ti.name))`
   from the same environment shows no regression versus a pre-edit baseline.
3. `julia --project=. -e 'using Pkg; Pkg.test("TSODSO")'` (full suite) stays green.
</verification>

<success_criteria>
- test/test_planning_noninteger.jl exists with a 4-builder registry + tripwire, green.
- The two existing partial checks are cross-referenced (not removed), byte-identical otherwise.
- PVAL-04 roadmap success criterion ("automated no-binaries guard fails CI/tests if any
  planning-layer subproblem builder introduces a binary/integer variable") is satisfied.
</success_criteria>

<output>
Create `.planning/phases/14-validation-oracle-regression-hardening-docs/14-02-SUMMARY.md` when done
</output>
