---
phase: 14
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/api.md
  - docs/literate/stackelberg_benders.jl
  - docs/literate/nash_diagonalization.jl
  - docs/make.jl
autonomous: true
requirements: [PVAL-03]

must_haves:
  truths:
    - "The Documenter build (julia --project=docs docs/make.jl) exits 0 -- it is CURRENTLY RED with 33 orphaned planning-layer docstrings, and this phase is the first opportunity to fix that standing defect."
    - "A reader can trace the PSR planning-layer problem numbers, the coupling seam (z<->p_ag, lambda_j<->pi_s), and the interpretive leader/follower choice to the actual code, via two new literate rung pages."
    - "Every number displayed on the two new rung pages comes from a live @example-executed solve, never a hardcoded literal copied from a test file."
    - "The BilevelJuMP certification story is narrated as prose/table citing test_planning_certification.jl by name -- never executed live inside the docs build (no new docs dependency)."
  artifacts:
    - path: docs/src/api.md
      provides: "new 'Planning Layer' @autodocs section, Order = [:type, :constant, :function], covering all 9 src/planning/*.jl files"
    - path: docs/literate/stackelberg_benders.jl
      provides: "Rung 6 literate page: PSR problem-number map, coupling-seam table, Phase 11 certification narrative, live solve_stackelberg! @example"
      min_lines: 40
    - path: docs/literate/nash_diagonalization.jl
      provides: "Rung 7 literate page: shared-corridor coupling model, Gauss-Seidel loop, live run_nash!/run_nash_probe @example, 'a converged equilibrium' language"
      min_lines: 40
    - path: docs/make.jl
      provides: "wires both new Literate sources into the build loop and a new 'Planning' pages subsection"
  key_links:
    - from: docs/src/api.md
      to: "src/planning/retry.jl (RETRYABLE_STATUSES const)"
      via: "Order = [:type, :constant, :function], not the [:type, :function] every other section uses"
      pattern: "Order = .type, :constant, :function."
    - from: docs/make.jl
      to: "docs/literate/stackelberg_benders.jl, docs/literate/nash_diagonalization.jl"
      via: "Literate.markdown loop + pages tree Planning entry"
      pattern: "stackelberg_benders|nash_diagonalization"
    - from: docs/literate/stackelberg_benders.jl
      to: "src/planning/benders.jl (solve_stackelberg!)"
      via: "live @example call, result.gap/result.y/result.z/result.UB displayed"
      pattern: "solve_stackelberg!"
    - from: docs/literate/nash_diagonalization.jl
      to: "src/planning/nash.jl (run_nash!, run_nash_probe)"
      via: "live @example calls, result.converged/result.z/result.x_inv/result.summary displayed"
      pattern: "run_nash!|run_nash_probe"
---

<objective>
Turn the CURRENTLY-BROKEN Documenter build green (33 exported planning-layer docstrings are
orphaned -- `checkdocs = :exports` fails `makedocs` today) and add the two new literate rung
pages PVAL-03 requires: "Rung 6: Stackelberg-Benders (planning)" and "Rung 7: Nash
Diagonalization & Shared Corridor". Both fixes are needed together -- adding only the two new
pages without the `api.md` fix leaves the build red on the pre-existing defect; fixing only
`api.md` leaves PVAL-03's own content contract (PSR problem-number map, coupling-seam table,
leader/follower certification narrative) undelivered.

Purpose: make the planning-layer math traceable to code the same way every prior rung already
is -- literate, `@example`-executed, never hand-copied numbers -- and close a standing defect
from Phases 10-13 (docs/ was never touched during those phases).
Output: `docs/src/api.md` (new "Planning Layer" section), `docs/literate/stackelberg_benders.jl`
(new), `docs/literate/nash_diagonalization.jl` (new), `docs/make.jl` (wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-oracle-regression-hardening-docs/14-CONTEXT.md
@.planning/phases/14-validation-oracle-regression-hardening-docs/14-RESEARCH.md

@docs/make.jl
@docs/src/api.md
@docs/literate/admm.jl
@docs/literate/pricing_dlmp.jl
@test/test_planning_certification.jl
@test/test_planning_nash.jl

Note on the working tree: `git status` currently shows the ROOT `Project.toml`/
`Manifest-v1.12.toml` as modified-uncommitted (CairoMakie promoted from `[weakdeps]` to
`[deps]`, a pre-existing user-local state per 14-RESEARCH.md Pitfall 5). Do NOT stage, commit,
or revert those two files, and do NOT add anything to `docs/Project.toml` beyond what already
exists there (CairoMakie, Documenter, Literate, TSODSO) -- this plan introduces zero new
dependencies, docs or otherwise.

<interfaces>
Locked architectural decision from 14-RESEARCH.md's "Anti-Pattern to Avoid" section -- not open
for re-litigation by the executor:

Rung 6 MUST NOT `using BilevelJuMP`/`using HiGHS`/`using Ipopt`. The certification story
(StrongDualityMode/ProductMode agreement, documented BigMMode+HiGHS MIQP-incapacity negative
regression, production Benders 4-way agreement) is narrated as prose/markdown citing
`test/test_planning_certification.jl` by file name -- never re-executed inside the docs build.
Executing it live would require adding BilevelJuMP/HiGHS/Ipopt as NEW direct `docs/Project.toml`
dependencies (today it has none) and would blur CLAUDE.md's "BilevelJuMP test-only, never
imported by src/" boundary onto the published docs site.

Exact production entrypoint signatures both new pages call live:

From src/planning/benders.jl:128 (Rung 6):
solve_stackelberg!(feeder, pf::AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator};
    λ₀, T::Int, follower_kwargs::NamedTuple, master_kwargs::NamedTuple,
    tol::Real = 1e-6, max_iter::Int = 100, checkpoint_dir::AbstractString) -> result with fields
    result.gap, result.y, result.z, result.UB

From src/planning/nash.jl:371 and :739 (Rung 7):
run_nash!(specs::AbstractVector{<:NamedTuple}, shared::SharedTransmission;
    z0::AbstractMatrix{<:Real}, tol_outer::Real = 1e-4, max_sweeps::Int = 50,
    checkpoint_dir::AbstractString) -> result with fields
    result.converged, result.z, result.x_inv

run_nash_probe(specs::AbstractVector{<:NamedTuple}, build_shared::Function;
    seeds::NamedTuple, orders::Tuple, tol_outer::Real = 1e-4, max_sweeps::Int = 50,
    checkpoint_dir::AbstractString) -> result with fields
    result.n_runs, result.runs, result.summary::String, result.spread
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix docs/src/api.md -- add the Planning Layer @autodocs section</name>
  <files>docs/src/api.md</files>
  <read_first>
    docs/src/api.md (every existing section's shape, e.g. "ADMM Decomposition" lines 88-94, and
    the Order = [:type, :function] used by every one of them today)
    src/planning/retry.jl lines 1-40 (the export solve_with_retry!, RETRYABLE_STATUSES line
    and the top-level const RETRYABLE_STATUSES = (...) definition -- the first exported
    top-level const in the project's whole src/ tree)
    src/TSODSO.jl lines 107-128 (the include order of all 9 planning/*.jl files, confirming
    the exact relative paths to list)
  </read_first>
  <action>
    Append a new "## Planning Layer" H2 section to docs/src/api.md, placed after the existing
    "## Experiment Harness" section (currently the file's last section), following the SAME
    structural shape as every other section (an @autodocs fenced Documenter code block under
    the heading). The block's Modules value must be [TSODSO]. Its Pages array must list
    exactly these 9 paths, in this order: "planning/retry.jl", "planning/checkpoint.jl",
    "planning/trace.jl", "planning/subproblem.jl", "planning/follower.jl", "planning/master.jl",
    "planning/benders.jl", "planning/coupling.jl", "planning/nash.jl". Its Order value must be
    [:type, :constant, :function] -- deliberately DIFFERENT from every existing section's
    [:type, :function] -- because src/planning/retry.jl's exported RETRYABLE_STATUSES is a
    top-level const (Documenter category :constant) and would otherwise remain orphaned,
    leaving exactly one checkdocs = :exports failure after the rest are fixed.
  </action>
  <acceptance_criteria>
    - docs/src/api.md contains a "## Planning Layer" heading after "## Experiment Harness"
    - Its @autodocs block's Pages array lists exactly the 9 paths above, in that order
    - That block's Order line reads Order = [:type, :constant, :function], not
      [:type, :function]
    - Running the docs build command below completes without raising the missing_docs error
      (exits 0) -- the red-to-green transition for this phase's docs prerequisite, verifiable
      at this point even before Task 2/3 add the two new Literate pages (the existing 6-page
      pages tree is untouched by this task alone)
  </acceptance_criteria>
  <verify>
    <automated>julia --project=docs docs/make.jl</automated>
  </verify>
  <done>docs/src/api.md has the new Planning Layer section with the correct Order list, and the docs build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/literate/stackelberg_benders.jl -- Rung 6</name>
  <files>docs/literate/stackelberg_benders.jl</files>
  <read_first>
    docs/literate/admm.jl (full file -- the rung-page structural convention: title comment,
    "why this page" paragraph citing requirement IDs, math blocks, using TSODSO / using
    TSODSO: Bus, Branch, Feeder import convention at lines 48-49, live @example execution,
    "genuinely computed, never hardcoded" display idiom at lines 104-116)
    test/test_planning_certification.jl lines 1-64 (the empirical certification story: header
    narrative, RE-DERIVED N=1 hand enumeration, StrongDualityMode/ProductMode agreement, the
    documented BigMMode+HiGHS MIQP-incapacity negative regression, production Benders 4-way
    agreement) and lines 170-227 (the exact toy-instance construction and solve_stackelberg!
    call to reuse)
    src/planning/benders.jl lines 42-127 (solve_stackelberg!'s own docstring -- exact math/
    kwarg names for the PSR-problem-number-to-code-symbol map)
    src/planning/follower.jl lines 59-93 (build_follower's docstring -- the coupling-seam
    z<->p_ag / dual naming to reflect in the seam table)
    .planning/phases/14-validation-oracle-regression-hardening-docs/14-RESEARCH.md
    "Anti-Pattern to Avoid" section (the locked narrate-only-BilevelJuMP recommendation)
  </read_first>
  <action>
    Create docs/literate/stackelberg_benders.jl as a new Literate source following
    docs/literate/admm.jl's exact structural convention: an opening
    "# # Rung 6 -- Stackelberg-Benders (Planning)" title comment; a "why this page" paragraph
    citing PLAN-04/PLAN-05/PLAN-06/PLAN-07/PVAL-01 by ID; a PSR-problem-number-to-code-symbol
    map in prose/markdown mapping the follower LP alpha(z) to build_follower/solve_follower!,
    the Benders master to build_master/add_optimality_cut!/add_feasibility_cut!/solve_master!,
    and the outer loop to solve_stackelberg!; a coupling-seam markdown table mapping
    z <-> p_import (oracle) / p_ag (follower's x_op) and lambda_j <-> pi_s (the follower's
    coupling constraint dual). Narrate, in prose citing test/test_planning_certification.jl by
    file name, the Phase 11 empirical certification story: StrongDualityMode + ProductMode
    agreement, the documented BigMMode+HiGHS MIQP-incapacity negative regression, and the
    production Benders 4-way agreement -- explicitly state this page does NOT re-execute
    BilevelJuMP live and does not add BilevelJuMP/HiGHS/Ipopt to docs/Project.toml. Import only
    `using TSODSO` and `using TSODSO: Bus, Branch, Feeder` (verbatim from admm.jl lines 48-49)
    -- no direct solver imports. Build the SAME toy instance as
    test_planning_certification.jl lines 176-181 (a two-bus-shaped feeder/dev/agg inline,
    `λ₀ = [4.0]`, the same follower_kwargs/master_kwargs values used there). Assign
    `checkpoint_dir = mktempdir()` as a preceding line, then call `solve_stackelberg!` live in
    an @example-executed block with `tol = 1e-6`, `max_iter = 100`,
    `checkpoint_dir = checkpoint_dir`. Display `result.gap`, `result.y`, `result.z`,
    `result.UB` as separate top-level expressions (mirroring admm.jl's display idiom) -- do not
    print a hardcoded 0.7 or -0.245 anywhere in executable code; only the live result's own
    field access.
  </action>
  <acceptance_criteria>
    - File starts with a "# # Rung 6 -- Stackelberg-Benders (Planning)" comment title
    - Contains `using TSODSO` and does NOT contain `using BilevelJuMP`, `using HiGHS`, or
      `using Ipopt` anywhere
    - Contains a live call to `solve_stackelberg!` as a top-level (non-comment) statement
    - Contains at least one bare top-level expression each for `result.gap`, `result.y`,
      `result.z`, `result.UB`
    - Contains the literal string `test_planning_certification.jl` in a comment
    - Contains a coupling-seam table/prose mapping `z`, `p_import`, `p_ag`
    - No literal `0.7` or `-0.245` numeric constant appears outside a `#`-comment
    - `julia --project=docs -e` running `Literate.markdown` on this file alone (flavor =
      Literate.DocumenterFlavor(), output to docs/src/generated/) completes without error
  </acceptance_criteria>
  <verify>
    <automated>julia --project=docs -e "using Literate; Literate.markdown(joinpath(\"docs\",\"literate\",\"stackelberg_benders.jl\"), joinpath(\"docs\",\"src\",\"generated\"); flavor=Literate.DocumenterFlavor())"</automated>
  </verify>
  <done>docs/literate/stackelberg_benders.jl exists, executes standalone via Literate.markdown, narrates the certification story without importing BilevelJuMP/HiGHS/Ipopt.</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/literate/nash_diagonalization.jl -- Rung 7 -- and wire docs/make.jl</name>
  <files>docs/literate/nash_diagonalization.jl, docs/make.jl</files>
  <read_first>
    docs/literate/admm.jl (iteration-loop narrative convention)
    docs/literate/pricing_dlmp.jl lines 104-128 (the decomposition/table display convention --
    "reaching the displayed NamedTuple without a thrown error IS the validation")
    test/test_planning_nash.jl lines 227-321 (N=2 symmetric toy fixture + hand-derivation
    comment) and lines 610-669 (run_nash_probe seeds/orders shape, gate assertions, and the
    "a converged equilibrium" / "the equilibrium" language at lines 664-665)
    src/planning/nash.jl (run_nash!/run_nash_probe docstrings for exact math/coupling-seam
    terms and the NashTrace two-level diagnostics shape)
    docs/make.jl (current Literate loop at lines 20-27 and pages tree at lines 51-62 -- the
    exact insertion points)
  </read_first>
  <action>
    Create docs/literate/nash_diagonalization.jl: an opening
    "# # Rung 7 -- Nash Diagonalization & Shared Corridor" title comment; a "why this page"
    paragraph citing NASH-01/NASH-02/NASH-03/NASH-04 by ID; math/prose describing the shared-
    corridor coupling model (build_shared_transmission's pooled capacity row) and the
    Gauss-Seidel outer loop (run_nash!); a description of the two-level convergence
    diagnostics (NashTrace, inner Benders gap vs. outer Nash residual). Import only
    `using TSODSO` and `using TSODSO: Bus, Branch, Feeder`. Build the SAME N=2 symmetric toy
    fixture as test/test_planning_nash.jl lines 262-279 (build_shared_transmission with N=2,
    T=1, corridor_cap=2.0, x_inv_max=[0.3,0.3], c_inv=[1.0,1.0], c_op=[[0.5],[0.5]]; the spec
    NamedTuple; specs = [spec, spec]). Assign `checkpoint_dir = mktempdir()`, call `run_nash!`
    live with `z0 = zeros(2, 1)`, `tol_outer = 1e-4`, `max_sweeps = 50`, display
    `result.converged`, `result.z`, `result.x_inv` as top-level expressions. Then build the
    multi-seed/multi-order probe exactly as test/test_planning_nash.jl lines 622-650 (the
    seeds/orders NamedTuples, the build_shared closure), call `run_nash_probe` live, display
    `result.summary` and `result.spread` as top-level expressions. Prose must use the phrase
    "a converged equilibrium" when describing the outcome and must NOT use the phrase "the
    equilibrium", matching test/test_planning_nash.jl lines 664-665's structural rule. Then
    edit docs/make.jl: append `"stackelberg_benders.jl"` and `"nash_diagonalization.jl"` (in
    that order, each with a trailing `# NEW: Rung 6` / `# NEW: Rung 7` comment) to the existing
    `for src in (...)` tuple immediately after `"admm.jl"`; add a new
    `"Planning" => ["Rung 6: Stackelberg-Benders" => "generated/stackelberg_benders.md", "Rung 7: Nash Diagonalization & Shared Corridor" => "generated/nash_diagonalization.md"]`
    entry to the `pages` array immediately after the existing `"Models" => [...]` entry and
    before `"API Reference" => "api.md"`.
  </action>
  <acceptance_criteria>
    - docs/literate/nash_diagonalization.jl exists, starts with
      "# # Rung 7 -- Nash Diagonalization & Shared Corridor"
    - Contains live calls to both `run_nash!` and `run_nash_probe` as top-level statements
    - Contains the exact substring "a converged equilibrium" and does NOT contain the
      substring "the equilibrium" anywhere in prose
    - docs/make.jl's `for src in (...)` tuple contains both "stackelberg_benders.jl" and
      "nash_diagonalization.jl" after "admm.jl"
    - docs/make.jl's `pages` array contains a "Planning" entry with both new generated .md
      paths, positioned before "API Reference" => "api.md"
    - Running `julia --project=docs docs/make.jl` exits 0 (full build green: Task 1's api.md
      fix + both new Literate pages + wiring all present simultaneously)
    - docs/src/generated/stackelberg_benders.md and docs/src/generated/nash_diagonalization.md
      exist after the build and are non-empty
  </acceptance_criteria>
  <verify>
    <automated>julia --project=docs docs/make.jl && test -s docs/src/generated/nash_diagonalization.md && test -s docs/src/generated/stackelberg_benders.md</automated>
  </verify>
  <done>Both new rung pages exist and render; docs/make.jl wires them in; the full docs build (api.md fix + 2 new pages) exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation build with no untrusted input, no network-facing surface at build time,
and no authentication. The relevant boundary is the published-docs-vs-src/ drift risk: a docs
page that silently diverges from the actual code it claims to describe.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | docs/literate/stackelberg_benders.jl, docs/literate/nash_diagonalization.jl | mitigate | every displayed number comes from a live @example-executed solve_stackelberg!/run_nash!/run_nash_probe call, never a copy-pasted literal from a test/goldens file (matches the project's existing T-01-09/T-09-03 convention on every prior rung page) |
| T-14-06 | Tampering | docs/Project.toml dependency boundary | mitigate | Rung 6 narrates (never executes) the BilevelJuMP certification live, so no new direct docs dependency is introduced and CLAUDE.md's "BilevelJuMP test-only" boundary is preserved on the published site |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan; Documenter/Literate/TSODSO are already pinned, existing docs/Project.toml dependencies |
</threat_model>

<verification>
Full local proof before considering this plan done:
1. `julia --project=docs docs/make.jl` exits 0 after Task 1 alone (api.md fix only).
2. `julia --project=docs -e` running `Literate.markdown` on stackelberg_benders.jl alone
   completes without error after Task 2.
3. `julia --project=docs docs/make.jl` exits 0 after Task 3 (full build: api.md + both new
   pages + wiring), and both generated markdown files are non-empty.
</verification>

<success_criteria>
- docs/src/api.md has the new Planning Layer section with the correct Order list.
- Both new Literate rung pages exist, execute live, and narrate (never execute) the BilevelJuMP
  certification story.
- The full Documenter build exits 0 -- PVAL-03's roadmap success criterion is satisfied.
</success_criteria>

<output>
Create `.planning/phases/14-validation-oracle-regression-hardening-docs/14-03-SUMMARY.md` when done
</output>
