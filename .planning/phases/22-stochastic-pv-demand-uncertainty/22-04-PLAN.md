---
phase: 22-stochastic-pv-demand-uncertainty
plan: 4
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - src/experiments/run_stochastic.jl
  - src/TSODSO.jl
  - docs/src/api.md
  - test/test_run_stochastic.jl
autonomous: true
requirements: [STOCH-03]

must_haves:
  truths:
    - "Researcher can call run_stochastic(s::Scenario) as one entry point that materializes
       S disjoint-seeded in-sample scenarios, solves the extensive form, drives the
       out-of-sample harness across H disjoint-seeded held-out scenarios, and reports the
       realized-vs-in-sample welfare gap (D-09/D-10)."
    - "In-sample and held-out seed families are provably disjoint — no held-out scenario
       replays an in-sample draw."
    - "Repeated-run stability is checked and asserted BEFORE any golden literal welfare-gap
       value is pinned in the test suite (D-11, measurement-before-golden)."
    - "Two calls to run_stochastic with the SAME Scenario (same seed) return identical
       results (INFRA-04 reproducibility, mirrors run_mpc's own same-seed guarantee)."
  artifacts:
    - path: "src/experiments/run_stochastic.jl"
      provides: "run_stochastic(s::Scenario) -> (; in_sample, oos) entry point"
      exports: ["run_stochastic"]
    - path: "test/test_run_stochastic.jl"
      provides: "@testitem coverage of guard ordering, seed disjointness, reproducibility,
        and D-11 measurement-before-golden"
      contains: "setup = [Phase22Fixtures]"
  key_links:
    - from: "src/experiments/run_stochastic.jl"
      to: "src/experiments/Scenario.jl"
      via: "s.stoch_S / s.stoch_probabilities / s.stoch_H_oos read directly"
      pattern: "stoch_S"
    - from: "src/experiments/run_stochastic.jl"
      to: "src/models/stochastic_welfare.jl"
      via: "build_stochastic_welfare(...) for the in-sample extensive form, then
        build_stochastic_oos_harness(...)/solve_stochastic_oos_step!(...) per held-out
        scenario"
      pattern: "build_stochastic_welfare"
---

<objective>
Wire the orchestrator: `run_stochastic(s::Scenario)`, the phase's ONE entry point (mirrors
`run_mpc`'s independent-sibling shape, D-01/D-02). It materializes S disjoint-seeded in-sample
scenario populations, solves the extensive form via `build_stochastic_welfare`, then drives
`build_stochastic_oos_harness`/`solve_stochastic_oos_step!` across H disjoint-seeded held-out
scenarios, pinning the first-stage battery schedule to the in-sample optimum and reporting the
realized-vs-in-sample welfare gap (STOCH-03) — with a measurement-before-golden stability
check preceding any pinned literal in the test suite (D-11).

Purpose: this is the single call a researcher (and the literate page, plan 22-05) makes.
Output: `src/experiments/run_stochastic.jl` (new), `src/TSODSO.jl` (wired), `docs/src/api.md`
(wired), `test/test_run_stochastic.jl` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-CONTEXT.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-RESEARCH.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-02-SUMMARY.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-03-SUMMARY.md

<interfaces>
From src/experiments/mpc_loop.jl (the primary analog — `run_mpc`'s materialization block,
lines 144-178, and its guard-first ordering, lines 144-166):

function run_mpc(s::Scenario)
    s.mpc_H > s.T && throw(ArgumentError(...))
    s.mpc_step > s.mpc_H && throw(ArgumentError(...))
    feeder = build_feeder(s.feeder)
    profiles = generate_profiles(; seed = sub_seed(s.seed, :profiles), T = s.T)
    λ₀ = build_price(s.price, s.T, profiles)
    aggs = build_population(s.population, feeder, s.feeder, profiles, sub_seed(s.seed, :population))
    pf = ConvexBranchFlow()
    ...
    return (; trace, day_ahead_welfare, realized_welfare, regret, day_ahead_dadp, steps)
end

From src/experiments/materialize.jl (exact signatures, already exported from TSODSO):
sub_seed(master::Integer, tag::Symbol) -> Int
build_feeder(sym::Symbol) -> Feeder
build_price(sym::Symbol, T::Int, profiles) -> Vector{Float64}
build_population(sym::Symbol, feeder, feeder_sym::Symbol, profiles, seed::Integer) -> Vector{<:Aggregator}

From src/data/profiles.jl:
generate_profiles(; seed::Integer, T::Int=24) -> (; demand::Vector{Float64}, pv::Vector{Float64})

From src/experiments/Scenario.jl (this phase's plan 22-01, already on disk before this task
runs): `s.stoch_S::Int` (band 3-5), `s.stoch_probabilities::Vector{Float64}` (length ==
stoch_S, sums to 1, materialized-uniform default), `s.stoch_H_oos::Int` (band 5-10).

From src/models/stochastic_welfare.jl (this phase's plans 22-02/22-03, already on disk):
build_stochastic_welfare(feeder, pf, scenario_aggs; probabilities, T, λ₀) ->
    (; model, ctxs, probabilities, welfare, dadp, expected_dadp, socp_maxgap)
build_stochastic_oos_harness(feeder, pf, aggregators; T, λ₀) -> StochasticOosHarness
    (fields: model, ctx, agg_bus, feeder, p_import, battery_pins, ppv_handles, tout_handles,
    agg_pdc_handles)
solve_stochastic_oos_step!(h::StochasticOosHarness; max_attempts::Int=4) -> Model

From src/devices/PVBattery.jl / src/devices/Aggregator.jl — this plan needs the SOLVED
in-sample battery trajectory to pin the harness: after `build_stochastic_welfare` returns,
walk `r.ctxs[1].meta[:agg_device_vars]` (scenario 1's copy — every scenario's battery is
TIED to it, so scenario 1's solved `value.(p_ch)`/`value.(p_dch)` IS the shared first-stage
schedule) to find each battery-like device's `v.p_ch`/`v.p_dch` `VariableRef` vectors, read
`value.(...)`, and feed those into the harness's `battery_pins[k].pin_p_ch`/`pin_p_dch` via
`set_parameter_value.`.

Reproducibility idiom (materialize.jl's `sub_seed`, mirrors `mpc_loop.jl`'s own disjoint-tag
discipline): in-sample scenario `k` uses two DISTINCT tags — `Symbol(:stoch_insample_profiles_,
k)` for the profile draw and `Symbol(:stoch_insample_population_, k)` for the population
build (mirrors `run_mpc`'s own `:profiles`/`:population` tag separation); held-out scenario
`h` uses a DISJOINT family `Symbol(:stoch_oos_profiles_, h)` / `Symbol(:stoch_oos_population_,
h)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_stochastic(s::Scenario) orchestrator — in-sample + out-of-sample welfare gap (D-09/D-10)</name>
  <files>src/experiments/run_stochastic.jl, src/TSODSO.jl, docs/src/api.md</files>
  <read_first>
    src/experiments/mpc_loop.jl (whole file — the primary analog's materialization block and
    guard-first ordering, per this plan's <interfaces> block), src/experiments/materialize.jl,
    src/experiments/Scenario.jl (the `stoch_*` fields, plan 22-01), src/models/stochastic_welfare.jl
    (the full file as plans 22-02/22-03 left it), src/TSODSO.jl (include order — insert
    `include("experiments/run_stochastic.jl")` at the VERY END, after
    `include("experiments/mpc_loop.jl")`, mirroring how `mpc_loop.jl` itself was wired LAST as
    an independent entry point), docs/src/api.md (the `## Stochastic PV/Demand Uncertainty`
    section plan 22-02 created — extend its `Pages` list)
  </read_first>
  <behavior>
    - `run_stochastic(s)` with the default `Scenario(; name = "t")` materializes `s.stoch_S`
      in-sample scenarios and `s.stoch_H_oos` held-out scenarios, all from DISJOINT seed
      families (no two `generate_profiles` draws across the two families are `==`).
    - The returned `NamedTuple` has fields `in_sample` (containing `welfare`, `dadp`,
      `expected_dadp`, `probabilities`) and `oos` (containing `welfare_h::Vector{Float64}`,
      `realized_welfare::Float64`, `welfare_gap::Float64`).
    - Two calls to `run_stochastic(s)` with the identical `s` return `==`-identical
      `in_sample.welfare`/`oos.welfare_gap` (same-seed reproducibility).
  </behavior>
  <action>
    Create `src/experiments/run_stochastic.jl` with a module-header comment (SEAM:
    stochastic extensive-form closed orchestrator, STOCH-01..03; OWNER: plan 22-04; states
    `run_stochastic` is an INDEPENDENT entry point per D-01/D-02 — NOT wired through
    `run_scenario`'s `:centralized`/`:admm` strategy dispatch, mirrors `run_mpc`'s own
    positioning note verbatim). Implement `run_stochastic(s::Scenario) -> NamedTuple`.
    Materialize: `feeder = build_feeder(s.feeder)`; `pf = ConvexBranchFlow()`. Build S
    in-sample scenario aggregator populations: for `k in 1:s.stoch_S`, `profiles_k =
    generate_profiles(; seed = sub_seed(s.seed, Symbol(:stoch_insample_profiles_, k)), T =
    s.T)`, `λ₀ = build_price(s.price, s.T, profiles_k)` (compute ONCE, from `k=1`'s profile,
    reused for every scenario — `:mem`'s shape is deterministic and profile-independent per
    its own docstring, so this is not a hidden per-scenario price divergence), `aggs_k =
    build_population(s.population, feeder, s.feeder, profiles_k, sub_seed(s.seed,
    Symbol(:stoch_insample_population_, k)))`. Collect `scenario_aggs =
    [aggs_1, ..., aggs_S]`. Call `r = build_stochastic_welfare(feeder, pf, scenario_aggs;
    probabilities = s.stoch_probabilities, T = s.T, λ₀ = λ₀, allow_export = s.allow_export)`.
    Read the SOLVED shared first-stage battery schedule from `r.ctxs[1].meta[:agg_device_vars]`
    (scenario 1's copy — every scenario's battery is tied to it): for every `(bus, varlist)`
    and every `v` with `haskey(v, :soc0)`, record `(; bus, p_ch = value.(v.p_ch), p_dch =
    value.(v.p_dch))` into `in_sample_battery::Vector{<:NamedTuple}`. Build H held-out
    scenario aggregator populations the SAME way but with the DISJOINT tag family
    `Symbol(:stoch_oos_profiles_, h)`/`Symbol(:stoch_oos_population_, h)` for `h in
    1:s.stoch_H_oos`. Build the out-of-sample harness ONCE: `h_oos =
    build_stochastic_oos_harness(feeder, pf, held_out_aggs[1]; T = s.T, λ₀ = λ₀, allow_export
    = s.allow_export)` — using held-out scenario 1's aggregator LIST as the harness's device
    STRUCTURE template (same bus/device composition as every in-sample scenario, per this
    phase's structural-congruence convention); pin the harness's `battery_pins` to
    `in_sample_battery`'s values via `set_parameter_value.(pin.pin_p_ch, batt.p_ch)`/
    `set_parameter_value.(pin.pin_p_dch, batt.p_dch)` for each matching `bus` — set ONCE,
    before the held-out loop (D-09's build-once contract). For `h in 1:s.stoch_H_oos`: slide
    every `ppv_handles`/`tout_handles`/`agg_pdc_handles` entry to `held_out_aggs[h]`'s own
    device data (`Ppv_param` from the matching device's `Ppv` field, `Tout_param` from
    `Tout[1:(T-1)]`, `Pdc_param` from the aggregator's `Pdc` field) via `set_parameter_value.`,
    then `solve_stochastic_oos_step!(h_oos)` and record `welfare_h[h] =
    objective_value(h_oos.model)`. Compute `realized_welfare = sum(welfare_h) /
    s.stoch_H_oos` (uniform-weight average — Claude's-discretion default, documented in the
    docstring) and `welfare_gap = realized_welfare - r.welfare` (D-09's realized-vs-in-sample
    gap; `r.welfare` is the in-sample extensive form's own expected-welfare objective value).
    Guard-first ordering (mirrors `run_mpc`'s own "boundary guards FIRST, before any
    materialization"): state explicitly in the docstring that, unlike `run_mpc`'s cross-field
    `mpc_H > T` check, this function needs NO additional guard beyond what `Scenario`'s own
    constructor already enforces — `stoch_S`/`stoch_H_oos`/`stoch_probabilities` are
    independently bounded at construction, with no cross-field interaction to re-check here.
    Return `(; in_sample = (; welfare = r.welfare, dadp = r.dadp, expected_dadp =
    r.expected_dadp, probabilities = r.probabilities, socp_maxgap = r.socp_maxgap), oos =
    (; welfare_h, realized_welfare, welfare_gap))`. Export `run_stochastic`. Wire
    `include("experiments/run_stochastic.jl")` into `src/TSODSO.jl` at the VERY END (after
    `include("experiments/mpc_loop.jl")`), mirroring that file's own end-of-module
    positioning comment. Extend the `## Stochastic PV/Demand Uncertainty` `@autodocs`
    section's `Pages` list in `docs/src/api.md` to `["models/stochastic_welfare.jl",
    "experiments/run_stochastic.jl"]`.
  </action>
  <acceptance_criteria>
    `run_stochastic` is exported from `TSODSO`; `src/TSODSO.jl` includes the new file at the
    documented end-of-module position; `docs/src/api.md`'s Stochastic section lists both
    files. Every behavior assertion above holds. `run_stochastic(Scenario(name="t"))` returns
    a `NamedTuple` with `in_sample`/`oos` fields of the documented shape.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using Test, TSODSO; s = Scenario(name = "t", feeder = :ieee13, T = 6, stoch_S = 3, stoch_H_oos = 5); r = run_stochastic(s); @test length(r.in_sample.dadp) == 3; @test length(r.in_sample.expected_dadp) == 6; @test length(r.oos.welfare_h) == 5; @test isfinite(r.oos.welfare_gap); r2 = run_stochastic(s); @test r.in_sample.welfare == r2.in_sample.welfare; @test r.oos.welfare_gap == r2.oos.welfare_gap; println("welfare_gap=", r.oos.welfare_gap); println("OK")'</automated>
  </verify>
  <done>
    `run_stochastic` exists, is exported, is wired into `TSODSO.jl` and `docs/src/api.md`,
    and every behavior/acceptance assertion above passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: test_run_stochastic.jl — seed disjointness, reproducibility, D-11 measurement-before-golden</name>
  <files>test/test_run_stochastic.jl</files>
  <read_first>
    test/test_mpc_loop.jl (if present, the `run_mpc`-consuming `@testitem` convention — else
    fall back to test_mpc_window.jl's `@testitem`/`setup=[...]` shape), test/fixtures_phase22.jl,
    src/experiments/Scenario.jl (stoch_* fields)
  </read_first>
  <action>
    Create `test/test_run_stochastic.jl` with `@testitem`s tagged `[:run_stochastic]`,
    `setup = [Phase22Fixtures]`. Item 1 ("seed disjointness"): construct a `Scenario` and,
    using `Phase22Fixtures`' own seed literal as the master, independently derive the
    in-sample and held-out `sub_seed` families the SAME way `run_stochastic` does internally
    (`Symbol(:stoch_insample_profiles_, k)` for `k in 1:s.stoch_S`,
    `Symbol(:stoch_oos_profiles_, h)` for `h in 1:s.stoch_H_oos`) and assert the two derived
    seed sets are disjoint (`isempty(intersect(insample_seeds, oos_seeds))`). Item 2
    ("reproducibility"): call `run_stochastic(s)` twice with the identical `s` and assert
    `==`-identical `in_sample.welfare`, `in_sample.dadp`, and `oos.welfare_gap`. Item 3
    ("D-11 measurement-before-golden — repeated-run stability precedes any pinned literal"):
    run `run_stochastic(s)` THREE times with the SAME `s` (a fresh call each time, not a
    cached result) and assert all three runs' `welfare_gap` are pairwise `==` (bit-for-bit
    stability, exploiting this project's own deterministic-seeded-draw guarantee) BEFORE —
    textually AFTER this stability assertion in the file, never before it — writing any
    `@test r.oos.welfare_gap ≈ <pinned literal>`-style golden assertion; add ONE such golden
    assertion using the value actually measured in this run, with a comment citing D-11 and
    stating explicitly that the golden was pinned only after the stability check above
    passed in this same test run.
  </action>
  <acceptance_criteria>
    `test/test_run_stochastic.jl` exists with 3 `@testitem`s tagged `[:run_stochastic]` and
    `setup = [Phase22Fixtures]`, covering seed disjointness, reproducibility, and D-11's
    measurement-before-golden ordering (a repeated-run stability assertion textually
    precedes the one pinned golden literal in the file).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using Test, TSODSO; s = Scenario(name = "t", feeder = :ieee13, T = 6, stoch_S = 3, stoch_H_oos = 5); insample_seeds = [sub_seed(s.seed, Symbol(:stoch_insample_profiles_, k)) for k in 1:s.stoch_S]; oos_seeds = [sub_seed(s.seed, Symbol(:stoch_oos_profiles_, h)) for h in 1:s.stoch_H_oos]; @test isempty(intersect(insample_seeds, oos_seeds)); r1 = run_stochastic(s); r2 = run_stochastic(s); r3 = run_stochastic(s); @test r1.oos.welfare_gap == r2.oos.welfare_gap == r3.oos.welfare_gap; println("measured welfare_gap=", r1.oos.welfare_gap); println("OK")'</automated>
  </verify>
  <done>
    test/test_run_stochastic.jl exists with the three documented @testitem's; seed
    disjointness and repeated-run stability are confirmed by the inline verification above,
    matching the ordering the @testitem file itself asserts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `Scenario` selector fields -> `run_stochastic`'s materialization block | Trusted (already validated by `Scenario`'s own constructor) selectors drive seed derivation and population construction |
| In-sample vs held-out seed families | Must never collide — a collision would silently evaluate a held-out scenario that is actually an in-sample draw, invalidating the whole out-of-sample claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Tampering | in-sample/held-out `sub_seed` tag families | mitigate | Disjoint tag PREFIXES (`:stoch_insample_*` vs `:stoch_oos_*`) by construction, plus an explicit disjointness `@test` in Task 2 — a future edit that accidentally reused a tag would be caught immediately, not silently |
| T-22-07 | Repudiation | pinning a golden `welfare_gap` literal in the test suite | mitigate | D-11's measurement-before-golden ordering is enforced by TEXT ORDER in the test file (stability assertion precedes the golden) and by this plan's own acceptance criteria, not left to reviewer memory |

No new package installs this phase — the SC row is intentionally omitted.
</threat_model>

<verification>
Both tasks' `<verify>` scripts pass under `julia --project=.` (plain scripts, no
TestItemRunner). `git diff --stat` touches only `files_modified`.
</verification>

<success_criteria>
`run_stochastic(s::Scenario)` exists, is exported, is wired into `TSODSO.jl` and
`docs/src/api.md`, and demonstrably satisfies D-09/D-10/D-11: disjoint seed families,
build-once-pinned out-of-sample re-solves, a reported realized-vs-in-sample welfare gap, and
measurement-before-golden test discipline. `test/test_run_stochastic.jl` exists with matching
coverage.
</success_criteria>

<output>
Create `.planning/phases/22-stochastic-pv-demand-uncertainty/22-04-SUMMARY.md` when done.
</output>
