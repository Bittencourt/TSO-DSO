---
phase: 22-stochastic-pv-demand-uncertainty
plan: 3
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/models/stochastic_welfare.jl
  - test/test_stochastic_oos_harness.jl
autonomous: true
requirements: [STOCH-03]

must_haves:
  truths:
    - "A single-scenario, build-once welfare-shaped model exists whose first-stage battery
       schedule is Parameter-pinned (not re-derived) and whose PV/demand/ambient inputs can be
       re-slid to a fresh held-out scenario and re-solved without rebuilding (D-09)."
    - "num_variables/num_constraints are provably unchanged across repeated re-solves at
       different held-out PV/demand draws and different pinned schedules (build-once
       invariant, mirrors MpcWindow's own test_mpc_window.jl convention)."
    - "The pinned p_ch/p_dch values are genuinely binding — the solved value.(p_ch) equals the
       pinned value, not merely constraint-satisfied-by-coincidence."
  artifacts:
    - path: "src/models/stochastic_welfare.jl"
      provides: "StochasticOosHarness struct + build_stochastic_oos_harness +
        solve_stochastic_oos_step! — the Parameter-pinned single-scenario re-solve harness"
      exports: ["StochasticOosHarness", "build_stochastic_oos_harness", "solve_stochastic_oos_step!"]
    - path: "test/test_stochastic_oos_harness.jl"
      provides: "@testitem coverage of the build-once invariant and pin-correctness"
      contains: "setup = [Phase22Fixtures]"
  key_links:
    - from: "src/models/stochastic_welfare.jl (build_stochastic_oos_harness)"
      to: "src/models/mpc_window.jl (build_mpc_window)"
      via: "mirrors the anonymous Parameter-plus-equality-constraint pin idiom
        (`soc[H] == terminal_param`) generalized to p_ch/p_dch"
      pattern: "set = Parameter"
---

<objective>
Add the out-of-sample harness half of the extensive-form capability (STOCH-03/D-09): a
SEPARATE, single-scenario, `solve_welfare`-shaped model built ONCE, whose first-stage battery
controls are Parameter-pinned to a caller-supplied in-sample optimum, and whose PV/demand/
ambient inputs can be re-slid per held-out scenario via `set_parameter_value` and re-solved
via `solve_with_retry!` — never rebuilt.

Purpose: this is the building block the orchestrator (plan 22-04) drives across the held-out
scenario budget to compute the realized-vs-in-sample welfare gap.
Output: `src/models/stochastic_welfare.jl` (extended), `test/test_stochastic_oos_harness.jl`
(new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-CONTEXT.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-RESEARCH.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-PATTERNS.md
@.planning/phases/22-stochastic-pv-demand-uncertainty/22-02-SUMMARY.md

<interfaces>
From src/models/mpc_window.jl (the idiom this task generalizes — read the whole
`build_mpc_window` function, lines 125-288, and its `MpcWindow` struct, lines 62-72):

struct MpcWindow{F}
    model::Model
    ctx::ModelContext
    H::Int
    agg_bus::Int
    feeder::F
    p_import::Vector{VariableRef}
    ic_handles::Vector{<:NamedTuple}
    agg_pdc_handles::Vector{<:NamedTuple}
    terminal_soc::Bool
end

The anonymous pin idiom (mpc_window.jl lines 239-244 — the EXACT pattern this task
generalizes from a single terminal target to the FULL p_ch/p_dch trajectory):

term = @variable(model, base_name = "soc_terminal_bus$(bus)", set = Parameter(parameter_value(v.soc0)))
@constraint(model, v.soc[H] == term)

`solve_mpc_window!` is a one-line delegation:
solve_mpc_window!(o::MpcWindow; max_attempts::Int=4, attempts_out=nothing) =
    solve_with_retry!(o.model; max_attempts=max_attempts, dual=true, attempts_out=attempts_out)

From src/devices/PVBattery.jl — `contribute!(d::PVBattery, ctx; T)` returns
`(; vars = (; p_ch, p_dch, soc, pv_used, soc0, Ppv_param), p_inject, utility)`. `soc0` and
`Ppv_param` are ALREADY genuine `Parameter` handles (soc0 scalar, Ppv_param length-T vector),
re-settable via `set_parameter_value`/`set_parameter_value.` without rebuilding.

From src/devices/Thermostatic.jl — `contribute!(d::Thermostatic, ctx; T)` returns
`(; vars = (; p, Tin, Tin0, Tout_param), p_inject, utility)`. `Tout_param` has length `T-1`
(the ambient-temperature slice consumed by the `T-1` recursion steps, NOT length `T` — mirror
`build_mpc_window`'s own handling of this exactly, do not assume length `T`).

From src/devices/Aggregator.jl — `contribute!(agg::Aggregator, ctx; T)` returns
`(; vars, p_inject, q_inject, utility, Pdc_param)`; `ctx.meta[:agg_device_vars]` is populated
as a side effect (`Dict{Int,Vector{Any}}` keyed by bus).

From src/planning/retry.jl:
solve_with_retry!(model::Model; max_attempts::Int=4, dual::Bool=true,
                  attempts_out::Union{Nothing,Ref{Int}}=nothing) -> Model

From this plan's own plan 22-02 (src/models/stochastic_welfare.jl, already on disk before this
task runs): the module-header comment style, boundary-guard style, and the
`select_optimizer(problem_class(pf))`/bridge-registration boilerplate this task's builder
ALSO needs (verbatim, mirrors `build_mpc_window`'s own copy of the same two lines).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StochasticOosHarness — Parameter-pinned single-scenario build-once model (D-09)</name>
  <files>src/models/stochastic_welfare.jl</files>
  <read_first>
    src/models/mpc_window.jl (whole file — the primary analog for this task, per the
    <interfaces> block above), src/models/stochastic_welfare.jl (the file this task APPENDS
    to — read what plan 22-02 already wrote: module header, boundary-guard style, bridge
    registration, `select_optimizer` usage), src/devices/PVBattery.jl (`contribute!` return
    shape), src/devices/Thermostatic.jl (`contribute!` return shape, the `Tout_param`
    length-`T-1` nuance)
  </read_first>
  <behavior>
    - Building the harness once, then calling `solve_stochastic_oos_step!` three times with
      DIFFERENT pinned `p_ch`/`p_dch` values AND different `Ppv_param`/`Pdc_param`/
      `Tout_param` slices each time leaves `num_variables(h.model)`/`num_constraints(h.model)`
      unchanged across all three solves (build-once invariant).
    - After `set_parameter_value.(pin.pin_p_ch, vals)` and a solve, `value.(v.p_ch) ==
      vals` for the pinned battery's `p_ch` (the pin is genuinely binding, not vacuous).
    - `build_stochastic_oos_harness` with empty `aggregators`, `T < 1`, or an aggregator bus
      out of range each raise `ArgumentError` before any model assembly.
  </behavior>
  <action>
    Append to `src/models/stochastic_welfare.jl` (after `build_stochastic_welfare` and its
    export line) a new section with its own header comment (D-09's out-of-sample harness —
    a SEPARATE, smaller build-once model than the S-scenario extensive form, per RESEARCH.md
    Pattern 5: "why a separate model, not the in-sample one"). Define
    `struct StochasticOosHarness{F}` with fields `model::Model`, `ctx::ModelContext`,
    `agg_bus::Int`, `feeder::F`, `p_import::Vector{VariableRef}`,
    `battery_pins::Vector{<:NamedTuple}` (one entry per battery-like device:
    `(; bus::Int, pin_p_ch, pin_p_dch)`), `ppv_handles::Vector{<:NamedTuple}` (`(; bus::Int,
    Ppv_param)`), `tout_handles::Vector{<:NamedTuple}` (`(; bus::Int, Tout_param)`),
    `agg_pdc_handles::Vector{<:NamedTuple}` (`(; bus::Int, Pdc_param)`) — mirrors
    `MpcWindow`'s field-grouping convention. Implement
    `build_stochastic_oos_harness(feeder, pf::AbstractPowerFlow,
    aggregators::AbstractVector{<:Aggregator}; T::Int, λ₀::AbstractVector{<:Real}, optimizer
    = select_optimizer(problem_class(pf)), allow_export::Bool = false) ->
    StochasticOosHarness`. Boundary guards FIRST (mirrors `build_mpc_window`): empty
    `aggregators`, `T < 1`, `length(λ₀) != T`, and any aggregator bus outside
    `1:length(feeder.buses)` — each `ArgumentError`. Build `model = Model(optimizer)` +
    register the SAME two SOC-to-nonconvex-quad bridges. `ctx = ModelContext(model)`; stash
    `ctx.meta[:feeder]`/`[:T]`. Call `contribute!(pf, ctx, feeder; T = T)` — exactly ONCE
    (this harness never needs `JuMP.unregister`, since it is a single-scenario model built
    only once, never re-`contribute!`'d). Capture `reactive = haskey(ctx.residuals, :Rq)`
    immediately after. Build the frontier `p_import` (NAMED form is safe here — this model is
    built exactly once, unlike the S-scenario loop in Task 1 of plan 22-02): free-sign if
    `allow_export`, else `>= 0`; inject into `:Rp[root]`; stash `ctx.meta[:p_import] =
    p_import`. If `reactive`, build `q_import` the same way. For every aggregator, call
    `contribute!(agg, ctx; T = T)` and push `(; bus = agg.bus, Pdc_param = res.Pdc_param)`
    onto `agg_pdc_handles`. Close `:balance_p`/`:balance_q` (named form, single build) and
    `register_constraint!`. Walk `ctx.meta[:agg_device_vars]`: for every `(bus, varlist)` and
    every `v` in `varlist` — if `haskey(v, :soc0)` (battery-like device): create TWO anonymous
    per-step pin `Parameter`s defaulting to the benign literal `0.0` for every `t` (mirrors
    `build_mpc_window`'s own "PLACEHOLDER... the caller ALWAYS calls `set_parameter_value`
    before the first solve" convention) — `pin_p_ch = @variable(model, [t = 1:T], set =
    Parameter(0.0))`, `pin_p_dch = @variable(model, [t = 1:T], set = Parameter(0.0))` — then
    `@constraint(model, [t = 1:T], v.p_ch[t] == pin_p_ch[t])` and the `p_dch` analogue (NEVER
    pin `soc` directly — Pattern 5's documented choice: "pin only p_ch/p_dch and let soc
    recursion follow" from the device's own `soc[1] == soc0` IC and recursion, unmodified);
    push `(; bus, pin_p_ch, pin_p_dch)` onto `battery_pins` and `(; bus, Ppv_param =
    v.Ppv_param)` onto `ppv_handles`; if ALSO `haskey(v, :Tout_param)` (a device may carry
    both — not for `PVBattery`, but keep the check general), push `(; bus, Tout_param =
    v.Tout_param)` onto `tout_handles`. Separately, for every `v` with `haskey(v,
    :Tout_param)` but NOT `:soc0` (the `Thermostatic` case), push `(; bus, Tout_param =
    v.Tout_param)` onto `tout_handles` too. Build the REAL objective (not a placeholder — λ₀
    never changes across held-out re-solves in this harness, unlike `MpcWindow`'s per-window
    λ₀ slide): `@objective(model, Max, ctx.meta[:objective] - sum(λ₀[t] * p_import[t] for t
    in 1:T))`. Return `StochasticOosHarness(model, ctx, aggregators[1].bus, feeder, p_import,
    battery_pins, ppv_handles, tout_handles, agg_pdc_handles)`. Implement
    `solve_stochastic_oos_step!(h::StochasticOosHarness; max_attempts::Int = 4) ->
    Model` as a ONE-LINE delegation: `solve_with_retry!(h.model; max_attempts = max_attempts,
    dual = false)` (mirrors `solve_mpc_window!`'s shape; `dual = false` — this harness never
    reports a per-scenario DADP, STOCH-03 only needs the realized welfare, per this plan's
    own scope boundary against STOCH-02). Export `StochasticOosHarness`,
    `build_stochastic_oos_harness`, `solve_stochastic_oos_step!` (append to the existing
    export line or add a new one at the end of the file).
  </action>
  <acceptance_criteria>
    `StochasticOosHarness`/`build_stochastic_oos_harness`/`solve_stochastic_oos_step!` are
    exported from `TSODSO`. Every guard in `<behavior>` throws `ArgumentError` as specified.
    Three re-solve cycles with different pin/PV/demand/ambient values leave
    `num_variables`/`num_constraints` unchanged. A solved `value.(v.p_ch)` equals the pinned
    `pin_p_ch` values exactly (within solver tolerance).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using Test, TSODSO, JuMP; buses = [Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false)]; branches = [Branch(1, 2, 1e-3, 1e-3, SMAX_NO_LIMIT)]; f = Feeder(buses, branches, 1); T = 6; λ0 = fill(4.0, T); prof = generate_profiles(seed = 301, T = T); Ppv = 0.01 .* prof.pv; Pdc = 0.02 .* prof.demand; therm = Thermostatic(2, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, fill(20.0, T)); batt = PVBattery(2, 0.95, 1.0, 0.002, 0.0, 0.008, 0.004, 3.8, 6.2, 8.9, Ppv); aggs = [Aggregator(2, 0.90, [therm, batt], Pdc)]; h = build_stochastic_oos_harness(f, ConvexBranchFlow(), aggs; T = T, λ₀ = λ0); nv0 = num_variables(h.model); nc0 = num_constraints(h.model; count_variable_in_set_constraints = true); pin = only(h.battery_pins); vbatt = only(vv for (bus, varlist) in h.ctx.meta[:agg_device_vars] for vv in varlist if haskey(vv, :p_ch)); for trial in 1:3; vals_ch = fill(0.0005 * trial, T); vals_dch = zeros(T); set_parameter_value.(pin.pin_p_ch, vals_ch); set_parameter_value.(pin.pin_p_dch, vals_dch); ppv = only(h.ppv_handles); set_parameter_value.(ppv.Ppv_param, fill(0.01 * trial, T)); pdc = only(h.agg_pdc_handles); set_parameter_value.(pdc.Pdc_param, fill(0.02 * trial, T)); solve_stochastic_oos_step!(h); @test num_variables(h.model) == nv0; @test num_constraints(h.model; count_variable_in_set_constraints = true) == nc0; @test all(isapprox.(value.(vbatt.p_ch), vals_ch; atol = 1e-6)); end; println("OK")'</automated>
  </verify>
  <done>
    `StochasticOosHarness`/`build_stochastic_oos_harness`/`solve_stochastic_oos_step!` exist,
    are exported, and every behavior/acceptance assertion above passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: test_stochastic_oos_harness.jl — build-once invariant + pin correctness</name>
  <files>test/test_stochastic_oos_harness.jl</files>
  <read_first>
    test/test_mpc_window.jl (the `@testitem`/build-once-invariance test convention this file
    mirrors, especially the item at lines 75-131), test/fixtures_phase22.jl
  </read_first>
  <action>
    Create `test/test_stochastic_oos_harness.jl` with `@testitem`s tagged
    `[:stochastic_oos_harness]`, `setup = [Phase22Fixtures]`. Item 1 ("build-once —
    num_variables/num_constraints invariant across heterogeneous re-solves"): mirrors
    `test_mpc_window.jl`'s own item at lines 75-131 — build the harness once via
    `Phase22Fixtures.stoch_feeder()`/`Phase22Fixtures.stoch_scenario_aggregators`, then run
    THREE heterogeneous re-solve cycles (different pinned `p_ch`/`p_dch`, different
    `Ppv_param`/`Pdc_param` slices, all within each device's own structural bounds so every
    cycle is feasible) and assert `num_variables`/`num_constraints` are unchanged from the
    first solve. Item 2 ("pin is genuinely binding, not vacuous"): after
    `set_parameter_value.` on `pin_p_ch`/`pin_p_dch` to a NON-TRIVIAL (nonzero, feasible)
    vector and a solve, assert `value.(v.p_ch) ≈ pin_p_ch_values` AND, separately, that
    setting a DIFFERENT pin vector and re-solving changes `value.(v.p_ch)` accordingly (the
    pin is load-bearing, not overridden by the optimizer). Item 3 ("boundary guards"):
    `@test_throws ArgumentError` for empty aggregators, `T < 1`, and an out-of-range
    aggregator bus.
  </action>
  <acceptance_criteria>
    `test/test_stochastic_oos_harness.jl` exists with 3 `@testitem`s tagged
    `[:stochastic_oos_harness]` and `setup = [Phase22Fixtures]`, covering the build-once
    invariant, pin correctness, and boundary guards.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using Test, TSODSO, JuMP; buses = [Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false)]; branches = [Branch(1, 2, 1e-3, 1e-3, SMAX_NO_LIMIT)]; f = Feeder(buses, branches, 1); T = 6; λ0 = fill(4.0, T); prof = generate_profiles(seed = 302, T = T); Ppv = 0.01 .* prof.pv; Pdc = 0.02 .* prof.demand; therm = Thermostatic(2, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, fill(20.0, T)); batt = PVBattery(2, 0.95, 1.0, 0.002, 0.0, 0.008, 0.004, 3.8, 6.2, 8.9, Ppv); aggs = [Aggregator(2, 0.90, [therm, batt], Pdc)]; @test_throws ArgumentError build_stochastic_oos_harness(f, ConvexBranchFlow(), typeof(aggs)(); T = T, λ₀ = λ0); @test_throws ArgumentError build_stochastic_oos_harness(f, ConvexBranchFlow(), aggs; T = 0, λ₀ = Float64[]); h = build_stochastic_oos_harness(f, ConvexBranchFlow(), aggs; T = T, λ₀ = λ0); pin = only(h.battery_pins); vbatt = only(vv for (bus, varlist) in h.ctx.meta[:agg_device_vars] for vv in varlist if haskey(vv, :p_ch)); valsA = fill(0.0004, T); set_parameter_value.(pin.pin_p_ch, valsA); set_parameter_value.(pin.pin_p_dch, zeros(T)); solve_stochastic_oos_step!(h); @test all(isapprox.(value.(vbatt.p_ch), valsA; atol = 1e-6)); valsB = fill(0.0007, T); set_parameter_value.(pin.pin_p_ch, valsB); solve_stochastic_oos_step!(h); @test all(isapprox.(value.(vbatt.p_ch), valsB; atol = 1e-6)); @test !all(isapprox.(value.(vbatt.p_ch), valsA; atol = 1e-6)); println("OK")'</automated>
  </verify>
  <done>
    test/test_stochastic_oos_harness.jl exists with the three documented @testitem's, and the
    inline-verified mechanics match what it asserts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Caller-supplied pin/held-out values -> `StochasticOosHarness`'s Parameters | Untrusted numeric input crosses via `set_parameter_value`/`set_parameter_value.` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | `StochasticOosHarness`'s pin Parameters | mitigate | Pin correctness is asserted directly (`value.(p_ch) == pinned value`) in Task 1/2's tests — a silently-non-binding pin (e.g. a stale default) would fail the test rather than pass unnoticed |
| T-22-06 | Repudiation | Build-once invariant | mitigate | `num_variables`/`num_constraints` equality asserted across every re-solve cycle — a silent rebuild (which would corrupt reproducibility and cost) is caught immediately |

No new package installs this phase — the SC row is intentionally omitted.
</threat_model>

<verification>
Both tasks' `<verify>` scripts pass under `julia --project=.` (plain scripts, no
TestItemRunner). `git diff --stat` touches only `files_modified`.
</verification>

<success_criteria>
`StochasticOosHarness`/`build_stochastic_oos_harness`/`solve_stochastic_oos_step!` exist,
are exported, and demonstrably satisfy D-09's build-once/Parameter-pin contract.
`test/test_stochastic_oos_harness.jl` exists with matching `@testitem` coverage.
</success_criteria>

<output>
Create `.planning/phases/22-stochastic-pv-demand-uncertainty/22-03-SUMMARY.md` when done.
</output>
