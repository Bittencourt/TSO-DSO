---
phase: 12-cut-store-benders-master-robustness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/planning/trace.jl
  - src/planning/benders.jl
  - src/planning/follower.jl
  - src/TSODSO.jl
  - test/test_planning_benders.jl
  - test/test_planning_hardening.jl
autonomous: true
requirements: [PLAN-05, PLAN-06]
must_haves:
  truths:
    - "A purpose-built BendersTrace ledger records one row per Benders iteration (LB, UB, relative gap, cut type, cut-store size, master solve status, solve time) and is structurally distinct from AdmmResiduals — no primal_trace/dual_trace fields, no eps_pri/eps_dual pair, a single relative-gap scalar instead of a two-residual pair"
    - "solve_stackelberg!'s returned NamedTuple exposes this trace, with exactly one row per iteration on BOTH the feasibility-cut and optimality-cut branches"
    - "Degenerate feasibility-cut edge cases (near-boundary z, near-zero deliverable capacity, repeated/duplicate Farkas cuts) are exercised by dedicated tests and never corrupt the persistent cut store (constraint count grows exactly as expected, no NaN/Inf row, LB stays monotone non-decreasing across an episode)"
    - "The IN-01/IN-02/IN-03/IN-06 info-level findings from 11-REVIEW.md are closed: the exhaustion error reports the trace's true last-recorded state (not a stale local variable), tol/max_iter are boundary-guarded, and a Farkas certificate is rejected unless v > 0 as well as finite"
    - "No planning-layer subproblem introduces a binary/integer variable"
  artifacts:
    - path: "src/planning/trace.jl"
      provides: "BendersTrace struct + zero-arg constructor + push! + is_converged + trace_summary, JuMP-free"
      contains: "mutable struct BendersTrace"
      min_lines: 40
    - path: "src/planning/benders.jl"
      provides: "solve_stackelberg! wired with BendersTrace instrumentation on both loop branches, tol/max_iter guards (IN-02/IN-03), trace-sourced exhaustion message (IN-01)"
      contains: "trace = BendersTrace()"
      min_lines: 220
    - path: "src/planning/follower.jl"
      provides: "solve_follower!'s Farkas guard widened to require v > 0 (IN-06)"
      contains: "v > 0"
      min_lines: 219
    - path: "test/test_planning_hardening.jl"
      provides: "@testitems: near-boundary z, near-zero deliverable capacity, repeated Farkas cuts — cut-store validity + LB monotonicity"
      contains: "@testitem"
      min_lines: 60
  key_links:
    - from: "src/planning/benders.jl"
      to: "src/planning/trace.jl"
      via: "push!(trace, k; LB=..., UB=..., gap=..., cut_type=..., n_cuts=length(master.cuts), master_status=..., solve_time=...) called once per branch, every iteration"
      pattern: "push!\\(trace, k;"
    - from: "src/planning/benders.jl"
      to: "solve_stackelberg! return NamedTuple"
      via: "additive `trace` field, non-breaking (mirrors solve_admm.jl's `residuals` field)"
      pattern: "trace,\\s*\\)"
    - from: "test/test_planning_hardening.jl"
      to: "src/planning/master.jl"
      via: "add_feasibility_cut! called directly (master-level, not full Benders loop) with genuine follower Farkas certificates"
      pattern: "add_feasibility_cut!\\(master"
---

<objective>
Give the Benders mechanics Phase 13's Nash diagonalization loop will call repeatedly a
purpose-built convergence ledger (`BendersTrace`) that is structurally incapable of being
confused with ADMM's residual-based stop, wire it into `solve_stackelberg!` on both loop
branches, and close the four cheap info-level findings from Phase 11's final review
(IN-01/IN-02/IN-03/IN-06) — then prove the persistent cut store survives three degenerate
feasibility-cut edge cases without corruption.

Purpose: `BendersTrace` is the diagnostics object Phase 13 will need one-per-distributor; getting
its shape right (single relative-gap scalar, never a primal/dual residual pair) and getting the
cut-store edge cases proven now means Phase 13 nests a second outer loop on top of mechanics
already hardened at realistic scale, not mechanics whose failure modes are still unknown.

Output: `src/planning/trace.jl` (new — `BendersTrace`, `push!`, `is_converged`, `trace_summary`),
`src/planning/benders.jl` (extended — trace wiring, IN-01/IN-02/IN-03 fixes),
`src/planning/follower.jl` (IN-06 one-line guard widening), `src/TSODSO.jl` (one new include),
`test/test_planning_benders.jl` (extended — trace assertions, tol/max_iter guard regressions),
`test/test_planning_hardening.jl` (new — three degenerate feasibility-cut edge cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-cut-store-benders-master-robustness-hardening/12-CONTEXT.md
@.planning/phases/12-cut-store-benders-master-robustness-hardening/12-PATTERNS.md
@.planning/phases/11-single-distributor-stackelberg-benders-certified/11-REVIEW.md

<interfaces>
<!-- Extracted directly from the codebase. Executor should use these directly — no further
codebase exploration needed for the ledger shape or the loop's exact instrumentation points. -->

From `src/admm/residuals.jl` (the role-match analog for `BendersTrace` — copy the SHAPE, not
the content; see the Pitfall block in `<action>` below for what NOT to copy):
```julia
mutable struct AdmmResiduals
    N::Int; T::Int
    primal_trace::Vector{Float64}; dual_trace::Vector{Float64}; rho_trace::Vector{Float64}
    eps_pri_trace::Vector{Float64}; eps_dual_trace::Vector{Float64}; price_gap_trace::Vector{Float64}
    iters::Int
end
AdmmResiduals(N::Integer, T::Integer) = AdmmResiduals(Int(N), Int(T), Float64[], Float64[], Float64[], Float64[], Float64[], Float64[], 0)
@inline function _assert_sequential(res::AdmmResiduals, k::Integer)
    expected = res.iters + 1
    k == expected || throw(ArgumentError("record!: expected sequential iteration $expected, got k=$k"))
end
function converged(res::AdmmResiduals, ε_pri::Real, ε_dual::Real)
    res.iters == 0 && return false
    return last(res.primal_trace) <= ε_pri && last(res.dual_trace) <= ε_dual
end
export AdmmResiduals, record!, converged
```
No `using JuMP` anywhere in this file — pure data, no `VariableRef`/`Model` field. `BendersTrace`
must copy this JuMP-freedom exactly.

From `src/planning/benders.jl` (the exact two instrumentation points, current line numbers):
```julia
# lines 135-143 — accumulator state initialized immediately before the loop:
UB = Inf
y_best = NaN
z_best = fill(NaN, T)
gap = NaN
for k in 1:max_iter
    ...
    # lines 157-165 — feasibility branch: checkpoints then `continue`s BEFORE UB is touched
    if !follower_res.feasible
        add_feasibility_cut!(master, follower_res.v, follower_res.u, lb_res.z)
        checkpoint_iteration!((; k, LB = lb_res.LB, UB, gap = NaN, z_k = lb_res.z, feasible = false), k; dir = checkpoint_dir)
        continue
    end
    ...
    # lines 184-207 — optimality branch: gap computed, checkpointed, convergence checked
    gap = (UB - lb_res.LB) / max(1, abs(UB))
    checkpoint_iteration!((; k, LB = lb_res.LB, UB, gap, z_k = lb_res.z, feasible = true), k; dir = checkpoint_dir)
    if gap <= tol
        return (; y = y_best, z = z_best, UB, LB = lb_res.LB, gap, iters = k, oracle, follower, master)
    end
end
# lines 210-213 — the IN-01 stale-gap exhaustion message to fix:
error("solve_stackelberg!: exhausted $max_iter iteration(s) without converging (last gap=$gap, tol=$tol) — refusing to silently return a non-converged result")
```

From `src/planning/follower.jl` lines 195-205 (the IN-06 guard to widen — one line, in place, no
second guard function):
```julia
v = dual_objective_value(f.model)
u = dual.(f.coupling)
isfinite(v) && all(isfinite, u) || error(
    "solve_follower!: HiGHS returned a non-finite Farkas certificate " *
    "(v=$v, u=$u) — refusing to emit a feasibility cut from it",
)
return (; feasible = false, v, u)
```

From `src/planning/master.jl` (the field this task's trace instrumentation reads, never mutates):
```julia
struct BendersMaster{Y, Z, AOP, AX}
    ...
    cuts::Vector{Any}   # a bookkeeping log of every cut appended, NOT consumed by solve_master!
end
```

From `src/planning/checkpoint.jl` (the IN-03 fix's cited contract, line ~49):
```julia
0 <= iter <= 99999 || throw(ArgumentError("iter must be in 0:99999 (5-digit zero-padded filename contract), got $iter"))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BendersTrace ledger + wire into solve_stackelberg! + close IN-01/IN-02/IN-03/IN-06</name>
  <files>src/planning/trace.jl, src/planning/benders.jl, src/planning/follower.jl, src/TSODSO.jl</files>
  <read_first>
    - src/admm/residuals.jl (whole file — the shape to copy: struct, zero/sized constructors,
      sequential-`k` guard, query predicate, export line; the file header's "pure data, no JuMP"
      contract)
    - src/planning/benders.jl (whole file, especially lines 109-217 — the loop, both branches,
      the return tuple, the exhaustion error)
    - src/planning/follower.jl lines 164-217 (`solve_follower!`, the exact guard to widen)
    - src/planning/master.jl lines 30-66 (`BendersMaster.cuts` field doc — read-only source for
      cut-store-growth instrumentation, never a new mutator)
    - src/planning/checkpoint.jl lines 41-64 (`checkpoint_iteration!`'s `0:99999` contract, cited
      by the IN-03 fix)
    - .planning/phases/12-cut-store-benders-master-robustness-hardening/12-PATTERNS.md, the
      `src/planning/trace.jl` and `src/planning/benders.jl` sections (full derivation of the
      struct shape, the Pitfall block on what NOT to copy from `AdmmResiduals`, the exact
      instrumentation point citations)
    - .planning/phases/11-single-distributor-stackelberg-benders-certified/11-REVIEW.md,
      findings IN-01, IN-02, IN-03, IN-06 (the exact defect + suggested fix text)
  </read_first>
  <behavior>
    - `BendersTrace()` constructs an empty ledger: all eight vector fields `isempty`, `iters == 0`
    - `push!(trace, 1; LB=0.0, UB=Inf, gap=NaN, cut_type=:feasibility, n_cuts=1, master_status=:OPTIMAL, solve_time=0.01)`
      appends exactly one row to every trace field and sets `trace.iters == 1`; `UB=Inf`/`gap=NaN`
      are ACCEPTED (legitimate sentinel values on a feasibility-branch or first-ever row), never
      guarded away
    - `push!(trace, 3; ...)` on an empty trace (or any non-sequential `k`) throws `ArgumentError`
      (mirrors `AdmmResiduals`'s `_assert_sequential`)
    - `push!(trace, 1; ..., cut_type=:bogus, ...)` throws `ArgumentError` (only `:optimality`/
      `:feasibility` accepted)
    - `push!(trace, 1; LB=NaN, ...)` throws `ArgumentError` (LB must always be a real finite LP
      objective value — unlike UB/gap it has no legitimate non-finite state)
    - `is_converged(trace, tol)` is `false` on an empty trace; after one row with `gap=1e-8`,
      `is_converged(trace, 1e-6) == true` and `is_converged(trace, 1e-9) == false`
    - `trace_summary(trace)` on an empty trace returns `iters == 0`; after two rows, returns the
      LAST row's `LB`/`UB`/`gap` and `max_cuts == maximum(trace.n_cuts_trace)`
    - `solve_stackelberg!`'s returned NamedTuple has a `trace::TSODSO.BendersTrace` field with
      `trace.iters == result.iters` and `length(trace.gap_trace) == result.iters` on the existing
      end-to-end convergence fixture (`test_planning_benders.jl`'s first testitem)
    - `solve_stackelberg!(...; tol = NaN, ...)` and `tol = -1.0` (all else valid) throw
      `ArgumentError` before any `build_*` call (IN-02)
    - `solve_stackelberg!(...; max_iter = 100_000, ...)` (all else valid) throws `ArgumentError`
      before any `build_*` call (IN-03)
    - On `max_iter = 1` exhaustion (the EXISTING regression in `test_planning_benders.jl`), the
      raised `ErrorException`'s message contains the trace's last-recorded `LB` value as a
      substring (proves the message reads the ledger, not a possibly-stale loop-local `gap`)
    - `solve_follower!`'s existing WR-04 feasibility-branch regression
      (`test_planning_benders.jl`'s second testitem, `x_inv_max=0.25`) remains green after the
      `v > 0` guard is added — the genuine HiGHS certificate on this fixture already satisfies
      `v > 0`, so the widened guard must not reject it
  </behavior>
  <action>
    Create `src/planning/trace.jl` with a `# SEAM:`/`# OWNER:` header naming this plan and roadmap
    criterion 2 ("purpose-built struct, explicitly NOT a copy of AdmmResiduals's dual-ascent
    residual test"). Define `mutable struct BendersTrace iter_trace::Vector{Int};
    LB_trace::Vector{Float64}; UB_trace::Vector{Float64}; gap_trace::Vector{Float64};
    cut_type_trace::Vector{Symbol}; n_cuts_trace::Vector{Int};
    master_status_trace::Vector{Symbol}; solve_time_trace::Vector{Float64}; iters::Int end`.
    Zero-arg constructor `BendersTrace() = BendersTrace(Int[], Float64[], Float64[], Float64[],
    Symbol[], Int[], Symbol[], Float64[], 0)`. An internal `@inline function
    _assert_sequential_trace(trace::BendersTrace, k::Integer)` mirroring `AdmmResiduals`'s
    `_assert_sequential` (own name — a distinct, non-exported helper, no dispatch collision
    needed since it is file-local). Define `function Base.push!(trace::BendersTrace, k::Integer;
    LB::Real, UB::Real, gap::Real, cut_type::Symbol, n_cuts::Integer, master_status::Symbol,
    solve_time::Real)`: call `_assert_sequential_trace`; guard `cut_type in (:optimality,
    :feasibility)` else `ArgumentError`; guard `isfinite(LB)` else `ArgumentError` (LB is always a
    real LP objective value — no legitimate non-finite state, unlike UB/gap); do NOT guard
    `UB`/`gap` for finiteness — `UB=Inf` (before any optimality iteration) and `gap=NaN` (every
    feasibility-branch row) are legitimate sentinel values, document this explicitly in a comment
    citing this behavior list; guard `isfinite(solve_time) && solve_time >= 0` and `n_cuts >= 0`,
    else `ArgumentError`; `push!` each scalar onto its matching vector field (reusing `Base.push!`
    on `Vector` — no naming collision since dispatch is on `BendersTrace` vs `Vector`); increment
    `trace.iters`; return `trace`. Define `function is_converged(trace::BendersTrace, tol::Real)`:
    `trace.iters == 0 && return false`; `return last(trace.gap_trace) <= tol` (mirrors
    `AdmmResiduals.converged`'s empty-ledger-false + last-row idiom, but reads ONE scalar gap, not
    a primal/dual pair). Define `function trace_summary(trace::BendersTrace)`: on an empty trace
    return `(; iters = 0, final_LB = NaN, final_UB = NaN, final_gap = NaN, max_cuts = 0)`;
    otherwise return `(; iters = trace.iters, final_LB = last(trace.LB_trace), final_UB =
    last(trace.UB_trace), final_gap = last(trace.gap_trace), max_cuts =
    maximum(trace.n_cuts_trace))`. Export `BendersTrace, is_converged, trace_summary` (NOT
    `push!` — it is a `Base` function extended with a new method, already globally callable via
    `using TSODSO`, re-exporting it is redundant). Add `include("planning/trace.jl")` to
    `src/TSODSO.jl` as the THIRD line of the `planning/` include block (after `checkpoint.jl`,
    before `subproblem.jl`) — it has zero load-time dependencies (no JuMP), so it loads as early
    as `admm/residuals.jl` does in its own block; extend the block's existing comment with one
    sentence explaining `trace.jl`'s position and its `benders.jl` consumer.

    In `src/planning/benders.jl`: add `isfinite(tol) && tol > 0 || throw(ArgumentError(
    "solve_stackelberg! needs tol to be finite and > 0 (got tol=$tol)"))` and `max_iter <= 99_999
    || throw(ArgumentError("solve_stackelberg! needs max_iter <= 99_999 (checkpoint_iteration!'s
    5-digit zero-padded filename contract, src/planning/checkpoint.jl), got
    max_iter=$max_iter"))` to the existing three-line guard block (same one-line-per-guard,
    `||`-throw style — IN-02/IN-03). Immediately before `for k in 1:max_iter`, add `trace =
    BendersTrace()` alongside the existing `UB`/`y_best`/`z_best`/`gap` accumulator
    initialization. At the top of the loop body (before `lb_res = solve_master!(master)`), add
    `t_iter0 = time()`. In the feasibility branch, immediately after the `add_feasibility_cut!`
    call and before `checkpoint_iteration!`, add `push!(trace, k; LB = lb_res.LB, UB = UB, gap =
    NaN, cut_type = :feasibility, n_cuts = length(master.cuts), master_status =
    Symbol(termination_status(master.model)), solve_time = time() - t_iter0)`. In the optimality
    branch, immediately after `gap = (UB - lb_res.LB) / max(1, abs(UB))` and before/alongside the
    existing `checkpoint_iteration!` call, add the matching `push!(trace, k; LB = lb_res.LB, UB =
    UB, gap = gap, cut_type = :optimality, n_cuts = length(master.cuts), master_status =
    Symbol(termination_status(master.model)), solve_time = time() - t_iter0)`. Add `trace` as an
    additive field to the converged-return NamedTuple (`(; y = y_best, z = z_best, UB, LB =
    lb_res.LB, gap, iters = k, oracle, follower, master, trace)`). Replace the exhaustion
    `error(...)` (IN-01) to read the trace instead of the loop-local `gap`: `last_LB =
    last(trace.LB_trace); last_UB = last(trace.UB_trace); last_gap = last(trace.gap_trace);
    error("solve_stackelberg!: exhausted $max_iter iteration(s) without converging (last
    recorded LB=$last_LB, UB=$last_UB, gap=$last_gap [gap may be NaN if the final iteration was a
    feasibility cut], tol=$tol) — refusing to silently return a non-converged result")` — every
    iteration pushes exactly one trace row on either branch, so `trace.iters == max_iter` and
    `last(...)` is always well-defined at this point, never empty.

    In `src/planning/follower.jl` line ~201, widen the existing single guard from
    `isfinite(v) && all(isfinite, u) || error(...)` to `isfinite(v) && v > 0 && all(isfinite, u)
    || error(...)`, updating the error message body to also name the non-positive case (e.g.
    "HiGHS returned a non-finite or non-positive Farkas certificate ... refusing to emit a
    feasibility cut that would fail to exclude z_k") — a one-line, in-place widening of the
    EXISTING guard, no second/parallel guard function (IN-06).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("planning", ti.name) && occursin("benders", ti.name)' 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/planning/trace.jl` defines `mutable struct BendersTrace` with the eight named fields
      and exports `BendersTrace, is_converged, trace_summary`
    - `grep -c 'using JuMP\|using MOI' src/planning/trace.jl` == 0 (JuMP-free, mirrors
      `AdmmResiduals`)
    - `grep -c 'primal_trace\|dual_trace\|eps_pri\|eps_dual' src/planning/trace.jl` == 0
      (structurally distinct from `AdmmResiduals`, roadmap criterion 2 / PATTERNS.md Pitfall)
    - `grep -c 'push!(trace, k;' src/planning/benders.jl` == 2 (one call site per branch)
    - `grep -c 'trace = BendersTrace()' src/planning/benders.jl` == 1
    - `grep -c 'isfinite(tol) && tol > 0' src/planning/benders.jl` == 1 (IN-02)
    - `grep -c 'max_iter <= 99_999' src/planning/benders.jl` == 1 (IN-03)
    - `grep -c 'last(trace.LB_trace)' src/planning/benders.jl` == 1 (IN-01, exhaustion message
      sources from the trace)
    - `grep -c 'isfinite(v) && v > 0' src/planning/follower.jl` == 1 (IN-06)
    - `grep -c 'include("planning/trace.jl")' src/TSODSO.jl` == 1, positioned before
      `include("planning/subproblem.jl")`
    - `grep -in 'Bin\b\|::Integer\b' src/planning/trace.jl src/planning/benders.jl src/planning/follower.jl`
      shows no binary/integer `@variable` declaration (criterion 4 — trivially true, this task
      adds zero new `@variable` calls anywhere)
    - The existing `test_planning_benders.jl` end-to-end and feasibility-branch testitems remain
      green (regression: WR-04's follower Farkas test still passes after the `v > 0` widening)
    - `julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("planning", ti.name) && occursin("benders", ti.name)'` exits 0
  </acceptance_criteria>
  <done>BendersTrace exists as a JuMP-free, structurally-distinct-from-AdmmResiduals ledger;
  solve_stackelberg! records one row per iteration on both branches and returns it; IN-01's
  exhaustion message, IN-02/IN-03's boundary guards, and IN-06's Farkas-sign guard are all
  closed.</done>
</task>

<task type="auto">
  <name>Task 2: BendersTrace assertions + degenerate feasibility-cut edge-case tests</name>
  <files>test/test_planning_benders.jl, test/test_planning_hardening.jl</files>
  <read_first>
    - test/test_planning_benders.jl (whole file — the existing toy fixture, `mktempdir() do dir`
      convention, the three existing `@testitem`s to extend)
    - test/test_planning_master.jl lines 109-127 (the finiteness-guard testitem shape — "build
      once, snapshot a count, attempt degenerate inputs, assert the store is UNCHANGED" — the
      idiom this task's edge-case tests copy)
    - test/test_planning_follower.jl (the `@testitem` tag/name convention: `[:planning]`, names
      contain "planning" and the seam name)
    - .planning/phases/12-cut-store-benders-master-robustness-hardening/12-CONTEXT.md, the
      "Edge-case & load-test scope" section (the three named edge cases: near-boundary z,
      zero-volume feasible set, repeated/duplicate Farkas cuts)
    - src/planning/follower.jl (`build_follower`/`solve_follower!` signatures — used directly, at
      the follower level, to generate genuine Farkas certificates for the edge-case tests)
    - src/planning/master.jl (`build_master`/`add_feasibility_cut!`/`solve_master!` signatures)
  </read_first>
  <action>
    In `test/test_planning_benders.jl`, extend the FIRST testitem (end-to-end convergence) with
    trace assertions immediately after its existing `checkpoint_files == result.iters` check:
    `@test result.trace isa TSODSO.BendersTrace`; `@test result.trace.iters == result.iters`;
    `@test length(result.trace.gap_trace) == result.iters`; `@test result.trace.cut_type_trace[end]
    == :optimality`; `@test TSODSO.is_converged(result.trace, 1e-6)`; `@test
    result.trace.n_cuts_trace[end] == length(result.master.cuts)`. Extend the SECOND testitem
    (WR-04 feasibility-branch) with: `@test count(==(:feasibility), result.trace.cut_type_trace)
    >= 1` (matching its existing `any(c -> c.kind == :feasibility, result.master.cuts)`
    assertion); `@test all(diff(result.trace.n_cuts_trace) .>= 0)` (cut-store growth is
    monotone non-decreasing, never shrinks). Add a NEW `@testitem "planning benders: tol/max_iter
    boundary guards reject NaN/negative tol and max_iter > 99_999 before any build call (IN-02/
    IN-03)" tags = [:planning] setup = [Phase6Fixtures, ToyDeviceFixture]` reusing the SAME toy
    fixture literals as the existing testitems, asserting `@test_throws ArgumentError
    solve_stackelberg!(...; tol = NaN, ...)`, `tol = -1.0`, and `max_iter = 100_000` (three
    separate calls, all other kwargs held at their existing valid toy-fixture values) — these are
    cheap (the guards fire before any `build_*` call, so no solver is ever invoked). Extend the
    THIRD (existing `max_iter=1` exhaustion) testitem with `@test occursin("last recorded LB",
    err.msg)` (IN-01 — the message now sources from the trace, not a stale loop-local `gap`).

    Create `test/test_planning_hardening.jl` (new) with a header comment mirroring
    `test_planning_master.jl`'s convention (Seam: phase-12 hardening pass, deepens PLAN-05/PLAN-06,
    no new requirement IDs; tags `[:planning]`, names contain "planning" and "hardening"). Write
    three `@testitem`s, each operating at the MASTER level (via `build_master`/
    `add_feasibility_cut!`/`solve_master!` and `build_follower`/`solve_follower!` directly — NOT
    the full `solve_stackelberg!` loop, per CONTEXT.md's scope for these edge cases):
    (a) "near-boundary z — deliverable-cap ± 1e-9 stays valid, cut store finite": `build_follower(;
    T=1, corridor_cap=2.0, x_inv_max=0.25, c_inv=1.0, c_op=[0.5])` (deliverable cap = 0.5, the
    WR-04 fixture's own cap); `solve_follower!(f, [0.5 - 1e-9])` (expect `feasible=true`);
    `solve_follower!(f, [0.5 + 1e-9])` (expect `feasible=false`, genuine certificate); feed the
    infeasible result's `(v, u)` into `add_feasibility_cut!` on a freshly `build_master`'d model
    (same `c_y=0.3, y_max=8.0, α_op_lb=-5.0, α_x_lb=0.0, T=1`); assert `length(master.cuts) == 1`,
    all cut fields `isfinite`; `solve_master!(master)`; assert `termination_status(master.model) ==
    MOI.OPTIMAL`. (b) "near-zero deliverable capacity (x_inv_max -> 1e-9) still yields a valid,
    finite feasibility cut": `build_follower(; T=1, corridor_cap=2.0, x_inv_max=1e-9, c_inv=1.0,
    c_op=[0.5])` (guard `x_inv_max > 0` still passes, deliverable cap ≈ 2e-9, effectively a
    zero-volume feasible set without violating `build_follower`'s `corridor_cap > 0` guard);
    `solve_follower!(f, [1.0])` (far over cap, expect `feasible=false`); add the certificate to a
    fresh master; assert cut-store validity and `solve_master!` still returns `OPTIMAL`. (c)
    "repeated/duplicate Farkas cuts are tolerated — cut store stays finite and valid, LB monotone
    non-decreasing": obtain ONE genuine certificate (reuse case (a)'s infeasible result); call
    `add_feasibility_cut!(master, v, u, z_k)` TWICE with IDENTICAL arguments on a fresh master;
    assert `length(master.cuts) == 2` and `num_constraints` grew by exactly 2 (duplicates
    TOLERATED, not deduped — Claude's Discretion per CONTEXT.md, document this choice in a comment
    at the top of the testitem); then, on a separate fresh master, run a 4-iteration episode
    interleaving one `add_optimality_cut!` and one `add_feasibility_cut!` per round with
    increasingly tight cut coefficients, calling `solve_master!` after each round and collecting
    `LB` into a `Vector{Float64}`; assert `all(diff(LBs) .>= -1e-9)` (monotone non-decreasing,
    matching the persistent-cut-store contract: cuts are only ever appended, never removed, so
    the LP relaxation can only tighten).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->(:planning in ti.tags)' 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@testitem' test/test_planning_hardening.jl` >= 3
    - `grep -c 'tags = \[:planning\]' test/test_planning_hardening.jl` matches the number of
      `@testitem`s in the file (every item tagged)
    - The three new edge-case testitems all pass: near-boundary z, near-zero deliverable
      capacity, repeated Farkas cuts
    - `test_planning_benders.jl`'s extended trace assertions pass on both the end-to-end and
      WR-04 feasibility-branch testitems
    - The new tol/max_iter boundary-guard testitem passes (three `@test_throws ArgumentError`)
    - The extended exhaustion testitem's `occursin("last recorded LB", err.msg)` assertion passes
    - `julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->(:planning in ti.tags)'` exits 0 (full `:planning`-tagged suite green, no Phase 1-11 regression)
  </acceptance_criteria>
  <done>Degenerate feasibility-cut edge cases (near-boundary z, near-zero deliverable capacity,
  repeated Farkas cuts) are exercised by dedicated tests and never corrupt the persistent cut
  store; BendersTrace's contents are asserted against the existing end-to-end and
  feasibility-branch fixtures; IN-01/IN-02/IN-03/IN-06 all have a passing regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benders loop iterate `z_k` -> `add_feasibility_cut!`/`add_optimality_cut!` | A degenerate trial (near-boundary, near-zero-capacity, duplicate certificate) must never corrupt the persistent, never-rebuilt cut store |
| `solve_stackelberg!`'s own exhaustion diagnostic -> the caller reading the raised error | The reported LB/UB/gap must reflect the TRUE last-recorded iteration state, never a stale loop-local variable from an older iteration |
| `solve_follower!`'s infeasible branch -> `add_feasibility_cut!` | A degenerate `v <= 0` Farkas multiplier would produce a cut that fails to exclude the trial point it was meant to rule out, silently cycling the master |

Web/network/injection/auth threats are explicitly out of scope (single-process, in-process Julia
numerical-optimization library, no network surface).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `add_feasibility_cut!`/`add_optimality_cut!` at degenerate trial points (near-boundary z, near-zero deliverable capacity) | mitigate | existing WR-03 finiteness guards (unmodified) plus three new dedicated edge-case tests asserting `master.cuts`/`num_constraints` grow exactly as expected and `solve_master!` still returns `OPTIMAL`, never leaving the persistent store in a partially-mutated state |
| T-12-02 | Tampering | Duplicate/repeated Farkas cuts appended to the same master | accept | duplicates are TOLERATED by design (Claude's Discretion per CONTEXT.md) — the cut store stays valid (each row independently finite, LP still solves OPTIMAL) even though redundant rows exist; a dedicated test proves the store's validity and LB monotonicity are unaffected by the redundancy |
| T-12-03 | Repudiation | `solve_stackelberg!`'s max-iter exhaustion diagnostic (IN-01) | mitigate | the error message now reads `BendersTrace`'s last recorded row (guaranteed non-empty — one row per iteration on either branch) instead of a loop-local `gap` that could be stale/NaN from an older optimality iteration |
| T-12-04 | Tampering | A degenerate `v <= 0` Farkas multiplier silently producing a vacuous, non-excluding feasibility cut (IN-06) | mitigate | `solve_follower!`'s existing finiteness guard widened to also require `v > 0` before a certificate is ever handed to `add_feasibility_cut!`; the existing WR-04 feasibility-branch regression is re-verified green as production evidence the widened guard does not reject a genuine certificate |
| T-12-SC | Tampering | npm/pip/cargo-equivalent (Julia Pkg) installs | accept | zero new packages this plan (`Project.toml`/`Manifest.toml` unchanged — package legitimacy gate not applicable to a Julia-only hardening pass) |
</threat_model>

<verification>
- `planning`-tagged, `benders`-named and `hardening`-named `@testitem`s all GREEN
- Full suite green: `julia --project=. -e 'using Pkg; Pkg.test()'` (no Phase 1-11 regression) —
  run at the wave boundary per 12-VALIDATION.md's sampling rate, not as a per-task gate
- No binary/integer `@variable` anywhere in any file this plan touches
</verification>

<success_criteria>
- Feasibility-cut edge cases (near-boundary/near-zero-capacity/duplicate `z`) are exercised by
  dedicated tests and handled without corrupting the persistent cut store (roadmap criterion 1)
- Benders UB/LB gap convergence diagnostics are finalized as `BendersTrace`, structurally distinct
  from `AdmmResiduals` — no primal/dual residual pair, a single relative-gap scalar (roadmap
  criterion 2)
- No planning-layer subproblem introduces a binary/integer variable (roadmap criterion 4)
</success_criteria>

<decisions_covered>
CONTEXT.md decisions implemented by this plan (Task 1 + Task 2):
- Purpose-built `BendersTrace` (single relative-gap scalar, cheap always-on NamedTuple-shaped
  rows via parallel vectors, no opt-in verbose flag) — Task 1.
- Exposure via `solve_stackelberg!`'s returned NamedTuple (additive `trace` field); CairoMakie
  plotting helper explicitly DEFERRED to Phase 14 (no `src/diagnostics/` change this plan) — Task 1.
- Degenerate feasibility-cut testitems (near-boundary z, near-zero-volume feasible set,
  repeated/duplicate Farkas cuts) — Task 2.
- Duplicate Farkas cuts TOLERATED (not deduped), documented + tested — Task 2.
- Code/test placement: extend `benders.jl`/`follower.jl` + existing `test_planning_*.jl` files;
  ONE new file `test/test_planning_hardening.jl` — Task 1 + Task 2, no new hardening module.
- IN-01/IN-02/IN-03/IN-06 cheap info-level closes from 11-REVIEW.md — Task 1.
</decisions_covered>

<output>
Create `.planning/phases/12-cut-store-benders-master-robustness-hardening/12-01-SUMMARY.md` when done.
</output>
</content>
