---
phase: 15-ac-exactness-oracle
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - test/fixtures_phase4.jl
  - test/test_ac_oracle.jl
  - docs/literate/ac_oracle.jl
  - docs/make.jl
autonomous: true
requirements: [EXACT-04]
must_haves:
  truths:
    - "A high-PV/reverse-flow stress fixture exercises the exactness boundary, and assert_ac_exact! surfaces the genuine finding at that fixture — reported as a per-hour gap, never tolerance-adjusted away"
    - "A local-optimum artifact (Ipopt single-start sensitivity) is distinguished from a genuine relaxation finding via a two-start comparison, so the reported gap is defensible as a relaxation property, not solver noise"
    - "The literate rung page docs/literate/ac_oracle.jl documents the exactness result beside the thesis equations, citing Farivar & Low (2013) / Gan, Li, Topcu & Low (2015), and executes live via docs/make.jl"
  artifacts:
    - path: "test/fixtures_phase4.jl"
      provides: "build_high_pv_aggregators exposes a pv_scale::Real=0.5 passthrough kwarg (previously hardcoded internally to 0.5), preserving every existing call site's default behavior byte-for-byte"
      contains: "pv_scale::Real = 0.5"
      min_lines: 300
    - path: "test/test_ac_oracle.jl"
      provides: "a high-PV stress @testitem asserting a POSITIVE (non-throwing) exactness finding plus a two-start Ipopt local-optimum guard"
      contains: "EXACT-04"
      min_lines: 140
    - path: "docs/literate/ac_oracle.jl"
      provides: "live-executed literate rung page comparing SOCP vs AC-OPF via assert_ac_exact!, citing the underlying exactness literature"
      contains: "assert_ac_exact!"
      min_lines: 45
    - path: "docs/make.jl"
      provides: "ac_oracle.jl registered in BOTH the Literate source tuple and the pages list"
      contains: "ac_oracle.jl"
      min_lines: 90
  key_links:
    - from: "docs/literate/ac_oracle.jl"
      to: "src/models/ac_oracle.jl (assert_ac_exact!)"
      via: "assert_ac_exact!(ctx_socp, ctx_ac; ...) called live during docs/make.jl execution — a real, solved comparison, never a hardcoded placeholder"
      pattern: "assert_ac_exact!\\("
    - from: "test/fixtures_phase4.jl"
      to: "test/test_ac_oracle.jl"
      via: "Phase4Fixtures.build_high_pv_aggregators(feeder; pv_scale=<tuned>) pins voltage at/near vmax², the one regime the SOC relaxation can go genuinely inexact"
      pattern: "pv_scale\\s*="
---

<objective>
Expose the ALREADY-EXISTING (but currently hardcoded) `pv_scale` knob on
`Phase4Fixtures.build_high_pv_aggregators` as a real passthrough kwarg, use it to tune the
high-PV stress fixture into the genuinely-boundary regime, and add a `test_ac_oracle.jl`
`@testitem` that asserts `assert_ac_exact!` surfaces the resulting finding (exact or inexact) as
a per-hour report — a POSITIVE test, never `@test_throws`. Guard against an Ipopt local-optimum
artifact masquerading as a relaxation gap via a two-start comparison. Write the literate rung page
`docs/literate/ac_oracle.jl`, citing Farivar & Low (2013) and Gan, Li, Topcu & Low (2015) by name,
and register it in `docs/make.jl` so the reported numbers execute live and can never drift from
the real `src/` code.

Purpose: this is the milestone's headline deliverable — a genuinely independent oracle
exercising the ONE regime (over-voltage pinned at `V²max`) where the SOC relaxation is documented
to go inexact, with the result (whichever way it comes out) written up as a citable finding
rather than silently tuned away.

Output: `test/fixtures_phase4.jl` (extended — `pv_scale` kwarg), `test/test_ac_oracle.jl`
(extended — the stress `@testitem`), `docs/literate/ac_oracle.jl` (new), `docs/make.jl` (extended
— two registrations).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ac-exactness-oracle/15-RESEARCH.md
@.planning/phases/15-ac-exactness-oracle/15-PATTERNS.md
@.planning/phases/15-ac-exactness-oracle/15-VALIDATION.md

<interfaces>
<!-- Extracted directly from the codebase this session. Executor should use these directly. -->

From `test/fixtures_phase4.jl:197-233` (the EXACT current signature — note `pv_scale` is NOT
yet a caller-facing kwarg here, only on the internal `_house_aggregator` helper it calls with a
hardcoded `0.5` literal; this plan's Task 1 fixes exactly this gap):
```julia
function build_high_pv_aggregators(feeder; seed::Integer = 20260406)
    N = length(feeder.buses)
    return [
        _house_aggregator(
            feeder, bus;
            seed = seed, φ = 0.95,
            pv_scale = 0.5,       # <- hardcoded; this plan makes it a passthrough kwarg
            load_scale = 0.2,
            batt_pmax = 0.1, batt_emax = 0.2, batt_soc0 = 0.1,
        ) for bus in 2:N
    ]
end
```
`_house_aggregator` (`fixtures_phase4.jl:109-160`) ALREADY accepts `pv_scale::Real = 1.0` as its
own kwarg — only `build_high_pv_aggregators` fails to expose it to its own callers. The fixture's
own "Calibration note" docstring (`fixtures_phase4.jl:209-216`, already present, do not rewrite)
documents that `pv_scale ≫ 0.5` pins voltage at `V²max` — the exact regime EXACT-04 targets.

From `src/models/welfare_solve.jl:99-110` (the EXISTING, already-supported `rtol_exact` override
point — used here for a deliberate, documented diagnostic loosening, NOT a production change):
```julia
function solve_welfare(feeder, pf::AbstractPowerFlow, aggregators; T::Int=24, λ₀,
    optimizer = select_optimizer(problem_class(pf)), allow_local::Bool=false,
    τ::Real = ..., rtol_exact::Real = 1e-4, allow_export::Bool = false)
```
`rtol_exact` gates ONLY the internal `assert_socp_exact!` call inside `solve_welfare` (PF-04 —
whether solve_welfare itself REFUSES to return `ctx_socp` at all). Passing a permissive
`rtol_exact` (e.g. `1.0`) to the `ConvexBranchFlow` call lets a genuinely loose-relaxation
solution through so it can be independently compared — the ACTUAL exactness verdict for this
milestone's finding comes from `assert_ac_exact!`'s own standard tolerance (`rtol=1e-4`), never
from this loosened internal gate. Document this override explicitly as intentional in the test's
comments; it changes ZERO code in `welfare_solve.jl`.

From `test/test_exactness.jl:138-190` (the high-PV `@testitem` STRUCTURE to mirror — but see
Pitfall warning below on the assertion polarity):
```julia
@testitem "exact: high-PV / over-voltage SOCP solve stays exact, prices NOT refused (PF-04)" tags =
    [:exact] setup = [Phase4Fixtures] begin
    feeder = Phase4Fixtures.high_pv_feeder()
    aggs = Phase4Fixtures.build_high_pv_aggregators(feeder)
    λ₀ = Phase4Fixtures.mem_price_profile()
    ...
end
```
For this plan's stress test, call `Phase4Fixtures.build_high_pv_aggregators(feeder; pv_scale =
<tuned>)` (the new kwarg from Task 1) instead of the no-kwarg call above, and DO NOT assert
`maxgap < 1e-5` — the whole point is the opposite regime.

From `docs/make.jl:20-35,53-68` (the exact two registration points):
```julia
for src in ("toy_dc.jl", "lindistflow.jl", "convex_branch_flow.jl", "prosumer_welfare.jl",
    "pricing_dlmp.jl", "admm.jl", "stackelberg_benders.jl", "nash_diagonalization.jl")
    Literate.markdown(joinpath(LITERATE_DIR, src), GENERATED_DIR; flavor = Literate.DocumenterFlavor())
end
...
pages = [
    "Home" => "index.md",
    "Models" => [
        "Rung 0: Toy DC" => "generated/toy_dc.md",
        "Rung 1-2: LinDistFlow" => "generated/lindistflow.md",
        "Rung 3: SOCP + Exactness" => "generated/convex_branch_flow.md",
        "Rung 3: Devices + GLB-CVX" => "generated/prosumer_welfare.md",
        "Rung 4: DADP/DLMP Pricing" => "generated/pricing_dlmp.md",
        "Rung 5: ADMM Decomposition" => "generated/admm.md",
    ],
    ...
```
Both edits are required — `checkdocs = :exports` (`docs/make.jl:81`) fails the build if a new
exported symbol's docstring isn't surfaced somewhere in the manual, and the literate tuple must
include the source or `generated/ac_oracle.md` never gets created.

From `docs/literate/convex_branch_flow.jl` (whole file, 91 lines — the exact structure/tone to
mirror: markdown-comment sections, `using TSODSO`, a real solve, then a "Validation" section
displaying real solved values, never a hardcoded placeholder).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose pv_scale on build_high_pv_aggregators + high-PV stress test with a positive exactness finding + two-start guard (EXACT-04)</name>
  <files>test/fixtures_phase4.jl, test/test_ac_oracle.jl</files>
  <read_first>
    - test/fixtures_phase4.jl (whole file — `_house_aggregator` lines 109-160, `high_pv_feeder`
      lines 176-195, `build_high_pv_aggregators` lines 197-233 including its own "Calibration
      note" docstring)
    - test/test_exactness.jl lines 138-190 (the high-PV `@testitem` structure to mirror, with
      the assertion-polarity warning below)
    - src/models/welfare_solve.jl lines 99-116, 240-258 (the `rtol_exact` override point and the
      internal PF-04 gate this task deliberately loosens for ONE diagnostic call, never for
      production use)
    - src/solver/factory.jl line 66 (`select_optimizer(::NLP)` — the default Ipopt attribute set
      the two-start guard's SECOND start must differ from)
    - .planning/phases/15-ac-exactness-oracle/15-RESEARCH.md lines 458-502 (Pitfall 2 — the
      local-optimum guard rationale; Pitfall 4 — the exact anti-pattern of tolerance-adjusting a
      genuine gap away, with its own "Warning signs" list) and lines 564-579 (Open Question 1 —
      the pv_scale tuning range, 1.0-2.0, to start from)
    - .planning/phases/15-ac-exactness-oracle/15-PATTERNS.md lines 443-508 (the exact stress-test
      derivation and the explicit "this is a POSITIVE test, NOT @test_throws" warning)
  </read_first>
  <behavior>
    - `Phase4Fixtures.build_high_pv_aggregators(feeder)` (no `pv_scale` kwarg supplied) behaves
      IDENTICALLY to before this task (default `pv_scale = 0.5`) — the existing
      `test_exactness.jl` high-PV `@testitem` remains green, unmodified, as a regression
    - `Phase4Fixtures.build_high_pv_aggregators(feeder; pv_scale = <tuned>)` produces aggregators
      whose PV back-feed is scaled by the supplied value, threaded through to `_house_aggregator`
    - On the tuned stress fixture, `solve_welfare(..., ConvexBranchFlow(), ...; rtol_exact = 1.0,
      allow_export = true)` returns a `ctx_socp` (does not throw, thanks to the deliberately
      loosened diagnostic tolerance)
    - `solve_welfare(..., ACPowerFlow(), ...; allow_local = true, allow_export = true)` (default
      Ipopt attributes) and a SECOND call with an explicit alternate Ipopt attribute set both
      return a finite objective, and the two objectives agree within `rtol = 1e-3` (the
      local-optimum guard)
    - `assert_ac_exact!(ctx_socp, ctx_ac; rtol = 1e-4, atol = 1e-6)` on this stress fixture
      returns a report with `!isempty([row.t for row in report.hours if !row.exact])` — a
      POSITIVE (expected) finding, never wrapped in `@test_throws`
    - For at least one inexact hour, either a bus voltage is at/near `vmax²` OR a branch carries
      reverse (`P<0`) flow — the voltage-binding/reverse-flow diagnostic
  </behavior>
  <action>
    In `test/fixtures_phase4.jl`, change `function build_high_pv_aggregators(feeder;
    seed::Integer = 20260406)` to `function build_high_pv_aggregators(feeder; seed::Integer =
    20260406, pv_scale::Real = 0.5)`, and change the `_house_aggregator(...; seed=seed, φ=0.95,
    pv_scale=0.5, load_scale=0.2, batt_pmax=0.1, batt_emax=0.2, batt_soc0=0.1)` call's hardcoded
    `pv_scale=0.5` literal to `pv_scale=pv_scale` (the new parameter). Extend the function's
    docstring with one sentence: "The `pv_scale` kwarg defaults to `0.5` (the documented ≈1.04 pu
    EXACT regime, `test_exactness.jl`'s own high-PV case); a caller-supplied `pv_scale ≫ 0.5`
    pins voltage at `V²max` — the one regime where SOC exactness genuinely fails (EXACT-04,
    plan 15-03)." Do not touch any other function in this file, and do not alter the default's
    numeric value (every EXISTING call site — `test_exactness.jl`'s high-PV `@testitem` — must
    remain byte-identical).

    In `test/test_ac_oracle.jl`, add a new `@testitem "ac_oracle: high-PV stress fixture surfaces
    the genuine SOCP/AC exactness finding at the exactness boundary (EXACT-04)" tags =
    [:ac_oracle] setup = [Phase4Fixtures] begin ... end` that: guards on
    `isdefined(TSODSO, :assert_ac_exact!) && isdefined(TSODSO, :ACPowerFlow)`; `import Ipopt`
    inside the item (for the second Ipopt attribute set); builds `feeder =
    Phase4Fixtures.high_pv_feeder()`, `aggs = Phase4Fixtures.build_high_pv_aggregators(feeder;
    pv_scale = 1.5)` (start at this value; if the resulting report shows zero inexact hours, widen
    to `2.0`, then `1.2`, iterating empirically — RESEARCH Open Question 1's own recommended range
    — until the fixture genuinely pins a bus voltage near `vmax²`; hard-code the FIRST value found
    to work, do not leave a search loop in the committed test), `λ₀ =
    Phase4Fixtures.mem_price_profile()`. Solve `ctx_socp, cost_socp, _ = solve_welfare(feeder,
    ConvexBranchFlow(), aggs; T = Phase4Fixtures.T, λ₀ = λ₀, allow_export = true, rtol_exact =
    1.0)` — comment explaining the deliberate `rtol_exact` loosening (quoted from `<interfaces>`
    above: this is a diagnostic override of `solve_welfare`'s OWN internal PF-04 gate so the
    loose-relaxation solution is returned for comparison; the milestone's actual finding comes
    from `assert_ac_exact!`'s own standard `rtol=1e-4` below, not from this loosened gate). Solve
    `ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T = Phase4Fixtures.T, λ₀ = λ₀,
    allow_local = true, allow_export = true)` (default `optimizer = select_optimizer(NLP())`).
    Solve a SECOND AC start: `ctx_ac2, cost_ac2, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T
    = Phase4Fixtures.T, λ₀ = λ₀, allow_local = true, allow_export = true, optimizer =
    optimizer_with_attributes(Ipopt.Optimizer, "print_level" => 0, "mu_strategy" => "adaptive"))`
    (a different Ipopt interior-point strategy — genuine solver-trajectory diversity without
    touching `solve_welfare`'s own signature). Assert `@test isapprox(cost_ac, cost_ac2; rtol =
    1e-3, atol = 1e-3)` labeled as the local-optimum guard (Pitfall 2) — if this ever fails on a
    future solver upgrade, it flags a LOCAL-OPTIMUM finding distinct from an exactness finding, and
    must not be conflated with the `assert_ac_exact!` comparison below. Call `report =
    TSODSO.assert_ac_exact!(ctx_socp, ctx_ac; rtol = 1e-4, atol = 1e-6)`. Assert
    `inexact_hours = [row.t for row in report.hours if !row.exact]; @test !isempty(inexact_hours)`
    (the POSITIVE, expected finding — NEVER `@test_throws` here, per RESEARCH's explicit warning).
    For `t★ = first(inexact_hours)`, diagnose: `pv_socp = ctx_socp.meta[:pf_vars]`; `N =
    length(feeder.buses)`; `B = length(feeder.branches)`; `voltage_bound_hit =
    any(value(pv_socp.v[j, t★]) >= feeder.buses[j].vmax^2 - 1e-3 for j in 1:N)`;
    `reverse_flow = any(value(pv_socp.P[b, t★]) < 0 for b in 1:B)`; `@test voltage_bound_hit ||
    reverse_flow` (Pitfall 4's diagnostic — the disagreement is investigated, not merely asserted
    non-empty). Add a comment naming this hour/finding as the milestone's documented result,
    cross-referenced in `docs/literate/ac_oracle.jl` (Task 2).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ac_oracle", ti.name) || occursin("exact", ti.name)' 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pv_scale::Real = 0.5" test/fixtures_phase4.jl` == 1 (in
      `build_high_pv_aggregators`'s signature)
    - `grep -c "pv_scale = pv_scale" test/fixtures_phase4.jl` == 1 (the passthrough, replacing the
      old hardcoded `pv_scale = 0.5` literal at the `_house_aggregator` call site)
    - The EXISTING `test_exactness.jl` high-PV `@testitem` (`build_high_pv_aggregators(feeder)`,
      no kwarg) remains GREEN — regression proof the default is unchanged
    - `grep -c "@test_throws" test/test_ac_oracle.jl` == 1 total in the file (still only the
      structural-mismatch case from plan 15-02 — this task's new item adds ZERO `@test_throws`)
    - `grep -c "isempty(inexact_hours)" test/test_ac_oracle.jl` >= 1 and it is asserted `!isempty`
      (positive finding), never `isempty`
    - `grep -c "isapprox(cost_ac, cost_ac2" test/test_ac_oracle.jl` == 1 (the two-start guard)
    - `grep -c "voltage_bound_hit || reverse_flow" test/test_ac_oracle.jl` == 1 (the diagnostic
      gate, Pitfall 4)
    - `julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ac_oracle", ti.name) || occursin("exact", ti.name)'` exits 0
  </acceptance_criteria>
  <done>build_high_pv_aggregators exposes pv_scale without altering any existing call site's
  behavior; the stress @testitem surfaces a genuine, diagnosed exactness finding as a POSITIVE
  assertion, guarded against an Ipopt local-optimum artifact by a two-start comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Literate rung page docs/literate/ac_oracle.jl + register in docs/make.jl (EXACT-04)</name>
  <files>docs/literate/ac_oracle.jl, docs/make.jl</files>
  <read_first>
    - docs/literate/convex_branch_flow.jl (whole file, 91 lines — the exact structure/tone to
      mirror: math-comment sections, a real `solve_welfare` call, a "Validation" section
      displaying real solved values)
    - docs/make.jl (whole file — the Literate source tuple at lines 20-35 and the `pages` list at
      lines 53-68, BOTH requiring an edit)
    - test/test_ac_oracle.jl (as extended by Task 1 in this same plan — the exact tuned
      `pv_scale`, the two `solve_welfare` calls, and the diagnosed finding to narrate here as
      real, executed prose, not a re-derived example)
    - .planning/phases/15-ac-exactness-oracle/15-RESEARCH.md, Open Question 3 (lines 589-598 —
      the citation requirement) and the "Sources / Secondary" section (Farivar & Low 2013; Gan,
      Li, Topcu & Low 2015 — exact citation text to reuse)
  </read_first>
  <action>
    Create `docs/literate/ac_oracle.jl` opening with a markdown header block (mirror
    `convex_branch_flow.jl:1-38`'s style) titled "# Rung 3 — AC-Exactness Oracle: Certifying the
    SOCP Relaxation Against a True Nonconvex AC-OPF", explaining: this page certifies
    `ConvexBranchFlow`'s SOC relaxation against a GENUINELY INDEPENDENT nonconvex formulation
    (`ACPowerFlow`, enforcing `l·v = P²+Q²` as an equality) rather than re-solving the same relaxed
    cone through a different solver; cite Farivar & Low, "Branch Flow Model: Relaxations and
    Convexification" (2013) and Gan, Li, Topcu & Low, "Exact Convex Relaxation of Optimal Power
    Flow in Radial Networks" (2015) by name in one paragraph, framing the over-voltage/
    reverse-flow regime as the literature's own documented exactness boundary. Add `using TSODSO`,
    then build the SAME high-PV stress fixture Task 1's test uses
    (`Phase4Fixtures`-equivalent construction inlined here since literate pages do not `using`
    test-only `@testmodule`s — construct `Bus`/`Branch`/`Feeder`/aggregator objects directly,
    reusing the SAME `high_pv_feeder`/aggregator-building LOGIC as `fixtures_phase4.jl`'s
    `high_pv_feeder`/`_house_aggregator`, at the SAME tuned `pv_scale` Task 1 settled on — do not
    silently pick a different value). Solve `ctx_socp, cost_socp, _ =
    solve_welfare(feeder, ConvexBranchFlow(), aggs; T=..., λ₀=..., allow_export=true, rtol_exact =
    1.0)` and `ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T=..., λ₀=...,
    allow_local=true, allow_export=true)`, narrating the same `rtol_exact` override rationale
    Task 1's test comments use. Call `report = assert_ac_exact!(ctx_socp, ctx_ac; rtol=1e-4,
    atol=1e-6)` and display it (`report.obj_gap`, `report.hours`) as REAL, solved output — never a
    hardcoded placeholder, matching `docs/make.jl`'s own "the rendered numbers cannot drift from
    the real src/ code" discipline. Add a closing "## Finding" markdown section narrating the
    ACTUAL result Task 1 diagnosed (inexact hour(s), voltage-binding or reverse-flow condition) as
    a citable milestone finding — framed as "documented", never as a defect requiring a fix.

    In `docs/make.jl`, add `"ac_oracle.jl"` to the `for src in (...)` tuple (line ~20-27),
    positioned immediately after `"convex_branch_flow.jl"` (thematic adjacency). Add
    `"Rung 3: AC-Exactness Oracle" => "generated/ac_oracle.md"` to the `pages` list's `"Models"`
    array (line ~53-62), immediately after the `"Rung 3: SOCP + Exactness"` entry. Both edits are
    required — `checkdocs = :exports` fails the build if `ACPowerFlow`'s/`assert_ac_exact!`'s/
    `recover_voltage_angles`'s exported docstrings aren't surfaced anywhere in the manual (the
    `api.md` `@autodocs` block already covers this generically, but the literate registration is
    what actually generates `generated/ac_oracle.md` in the first place — omitting the tuple entry
    means the `pages` reference points at a file that was never built).
  </action>
  <verify>
    <automated>julia --project=docs docs/make.jl 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"ac_oracle.jl"' docs/make.jl` == 1, positioned in the `for src in (...)` tuple
    - `grep -c 'generated/ac_oracle.md' docs/make.jl` == 1, positioned in the `pages` "Models" list
    - `grep -c "Farivar" docs/literate/ac_oracle.jl` >= 1
    - `grep -c "Gan" docs/literate/ac_oracle.jl` >= 1
    - `grep -c "assert_ac_exact!" docs/literate/ac_oracle.jl` >= 1
    - `grep -c "ACPowerFlow()" docs/literate/ac_oracle.jl` >= 1
    - `julia --project=docs docs/make.jl` exits 0 (the literate page executes live, no
      hardcoded/stale numbers, `checkdocs = :exports` passes)
  </acceptance_criteria>
  <done>docs/literate/ac_oracle.jl exists, executes live via docs/make.jl, cites the underlying
  exactness literature by name, and documents the ACTUAL (not hypothetical) finding from Task 1's
  stress fixture as a citable milestone result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The stress fixture's `rtol_exact` diagnostic override | Must remain scoped to this ONE diagnostic call (never become the project's new default — `welfare_solve.jl`'s own `rtol_exact::Real = 1e-4` default is untouched) |
| The two-start Ipopt comparison | Must genuinely distinguish a local-optimum artifact from a relaxation finding — conflating them would misattribute the milestone's headline result |
| The literate page's displayed numbers | Must be REAL, live-executed output (Documenter `@example` blocks), never a hardcoded placeholder that could drift from `src/` |

Web/network/injection/auth threats are explicitly out of scope (single-process, in-process Julia
numerical-optimization library, no network surface).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `ACPowerFlow.contribute!` units convention | mitigate | Closed in plan 15-01; unaffected here |
| T-15-02 | Repudiation | `assert_ac_exact!` throw-on-gap risk | mitigate | Closed in plan 15-02; this plan's stress test itself asserts the NON-throwing positive-finding shape, reinforcing the contract at the point of highest temptation to "fix" a red test |
| T-15-03 | Tampering | Ipopt single-start local-optimum artifact reported as a relaxation gap | mitigate | Task 1's two-start comparison (default vs `mu_strategy=>"adaptive"` Ipopt attributes via `solve_welfare`'s existing `optimizer` kwarg), asserting objective agreement within `rtol=1e-3` before trusting the exactness comparison |
| T-15-04 | Tampering | `ctx_socp`/`ctx_ac` diverging on problem data | mitigate | Task 1 builds both from the SAME `feeder`/`aggs`/`λ₀`/`T` local variables, `allow_export=true` matched explicitly on both calls |
| T-15-05 | Tampering | `recover_voltage_angles` phasor recursion | mitigate | Closed in plan 15-01; unaffected here |
| T-15-06 | Repudiation | A genuine high-PV inexactness tolerance-adjusted away instead of investigated | mitigate | Task 1's diagnostic gate (`voltage_bound_hit \|\| reverse_flow`) requires the disagreement be INVESTIGATED, not merely asserted; Task 2's literate page documents the finding as citable, explicitly not a defect to fix |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- `ac_oracle`-named and `exact`-named `@testitem`s all GREEN
- `julia --project=docs docs/make.jl` succeeds (the literate rung page executes live — project
  convention, `docs/make.jl`'s own header: "the rendered numbers cannot drift from the real
  `src/` code")
- Full suite green: `julia --project=. -e 'using Pkg; Pkg.test()'` (phase gate, per
  15-VALIDATION.md's sampling rate)
</verification>

<success_criteria>
- A high-PV/reverse-flow stress fixture exercises the exactness boundary, and the result — exact
  or genuinely inexact — is documented in the literate rung page beside the thesis equations
  (roadmap Phase 15 success criterion 4)
- Every EXACT-01..EXACT-04 requirement now has a passing, non-throwing-on-genuine-gap test path
  and a live-executed documentation page
</success_criteria>

<output>
Create `.planning/phases/15-ac-exactness-oracle/15-03-SUMMARY.md` when done.
</output>
</content>
