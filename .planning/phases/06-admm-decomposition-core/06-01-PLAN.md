---
phase: 06-admm-decomposition-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/TSODSO.jl
  - src/admm/AgrOpt.jl
  - src/admm/DsoOpt.jl
  - src/admm/residuals.jl
  - src/admm/solve_admm.jl
  - test/fixtures_phase6.jl
  - test/test_admm.jl
autonomous: true
requirements: [ADMM-01]
must_haves:
  truths:
    - "The four src/admm/*.jl seam files load via the TSODSO include graph without error"
    - "A researcher can build a 2-bus dual-sign-anchor feeder + aggregator fixture and reuse the IEEE-13 ground fixture"
    - "The AdmmResiduals struct records primal/dual residual traces and iteration count"
    - "The cross-validation and build-once @testitems exist and are RED (fail loudly, do not crash discovery)"
  artifacts:
    - path: "src/admm/residuals.jl"
      provides: "AdmmResiduals tracking struct + constructor + push!-style recorder"
      contains: "struct AdmmResiduals"
      min_lines: 30
    - path: "test/fixtures_phase6.jl"
      provides: "Phase6Fixtures @testmodule: two_bus_feeder + build_two_bus_aggregators (seeded, analytic price)"
      contains: "@testmodule Phase6Fixtures"
      min_lines: 40
    - path: "test/test_admm.jl"
      provides: "RED @testitems for cross-validation (2-bus + IEEE-13) and build-once/resolve"
      contains: "@testitem"
      min_lines: 30
    - path: "src/TSODSO.jl"
      provides: "include-graph wiring of the four src/admm files after the pricing seam"
      contains: "include(\"admm/solve_admm.jl\")"
  key_links:
    - from: "src/TSODSO.jl"
      to: "src/admm/residuals.jl"
      via: "include after pricing/welfare.jl"
      pattern: "include\\(\"admm/residuals.jl\"\\)"
    - from: "test/fixtures_phase6.jl"
      to: "generate_profiles"
      via: "seeded profile draw for the 2-bus aggregator"
      pattern: "generate_profiles"
---

<objective>
Lay the Phase-6 foundation: wire the `src/admm/` module into the TSODSO include graph,
create the four seam stub files, fill the `AdmmResiduals` tracking struct, and stand up the
Wave-0 RED test harness (the 2-bus dual-sign-anchor fixture plus the failing cross-validation
and build-once `@testitem`s). This is the single-owner scaffold every Wave-2/Wave-3 plan
builds on.

Purpose: ADMM is orchestration over already-validated Phase 1-5 builders (RESEARCH Pattern 4).
Before any subproblem or loop is written, the module must load, the shared residual type must
exist, and the failing correctness gate (RESEARCH Pattern 5) must be pinned so downstream plans
drive it green. This plan owns the ONLY shared edit surface (`src/TSODSO.jl`), keeping Wave 2
file-disjoint.

Output: `src/admm/{AgrOpt,DsoOpt,solve_admm}.jl` (comment-only stubs), `src/admm/residuals.jl`
(filled), `test/fixtures_phase6.jl`, `test/test_admm.jl` (RED), and the include-graph edit.
</objective>

<phase_goal>
**As a** researcher, **I want to** solve the operational problem via the ADMM AGR-OPT/DSO-OPT
decomposition, **so that** I recover the centralized SOCP optimum and its DADP duals on every
fixture small enough to solve monolithically.

> NOTE (MVP reformat): the ROADMAP Phase-6 `**Goal:**` is prose, not a user story. It was
> reformatted faithfully above (role = researcher; capability = the ADMM decomposition;
> outcome = recovering the centralized optimum + duals). No scope was invented. This is a
> numerical-algorithm phase: the "vertical slice" is the algorithm itself, so the failing
> end-to-end test (the centralized cross-validation) is pinned RED here in Wave 1, the two
> ADMM blocks are built in Wave 2, and Wave 3 assembles the loop and turns the gate green —
> prioritizing 2-bus convergence before IEEE-13, per MVP ordering.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admm-decomposition-core/06-RESEARCH.md
@.planning/phases/06-admm-decomposition-core/06-VALIDATION.md

<interfaces>
<!-- Seams the executor wires against. Extracted from the codebase — no exploration needed. -->

From src/TSODSO.jl (the include graph; ADMM includes go LAST, after pricing/welfare.jl):
  include("pricing/welfare.jl")   # social = prosumer + DSO surplus split (plan 05-05, PRICE-03)
  # end # module TSODSO   ← ADMM includes are inserted BEFORE this line

From src/data/Feeder.jl:
  struct Bus{T<:Real};    Bus(id, vmin, vmax, is_root)      # is_root::Bool
  struct Branch{T<:Real}; Branch(from, to, r, x, smax)
  struct Feeder{T<:Real}; Feeder(buses, branches, root)     # runs assert_radial + assert_magnitudes
  const SMAX_NO_LIMIT      # sentinel from units/PerUnit.jl (just under the 0<smax<100 band)

From src/data/profiles.jl:
  generate_profiles(; seed, T) -> (; demand, pv)            # seeded StableRNGs, reproducible

From test/fixtures_phase4.jl (@testmodule Phase4Fixtures — REUSED for IEEE-13):
  const T = 24
  build_ieee13_ground_aggregators(feeder; seed=20260718)    # residential-scaled, allow_export=true
  # ieee13_modified() built-in feeder lives in src/data/ieee13.jl (call inside a function, not at load)

From src/models/welfare_solve.jl (the centralized ground truth the harness cross-validates against):
  solve_welfare(feeder, pf, aggregators; T, λ₀, allow_export=false) -> (ctx, objective::Float64, dadp)

From src/pricing/dlmp.jl:
  extract_dlmp(ctx; bus=nothing, T=nothing) -> Matrix{Float64}   # (N,T) DADP, PF-04-gated
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the src/admm include graph + create the three subproblem/loop stub files</name>
  <files>src/TSODSO.jl, src/admm/AgrOpt.jl, src/admm/DsoOpt.jl, src/admm/solve_admm.jl</files>
  <read_first>
    - src/TSODSO.jl (the full include graph + the "each seam file declares its own exports;
      TSODSO.jl is never a shared edit surface" convention — this plan is the sole owner of
      the ADMM include edit, mirroring how plan 05-01 wired the pricing includes)
    - 06-RESEARCH.md § "Recommended Project Structure" (the src/admm/ layout: AgrOpt.jl,
      DsoOpt.jl, residuals.jl, solve_admm.jl — wired AFTER models/oracle.jl and the pricing
      files) and § "Pattern 4" (ADMM is orchestration; no Phase-5 file needs an additive change
      because AGR-OPT reuses Aggregator.contribute! via option (a), ignoring its stray :Rp write)
  </read_first>
  <action>
    Create three COMMENT-ONLY stub files under src/admm/: AgrOpt.jl (owned by plan 06-02),
    DsoOpt.jl (owned by plan 06-03), solve_admm.jl (owned by plan 06-04). Each stub is a header
    comment naming its seam, its owning plan, and the thesis equations it will implement (3.46
    for AGR-OPT, 3.47 for DSO-OPT, 3.31 dual for the loop) — no code, no exports yet, exactly
    like the Phase-5 pricing stubs. Then edit src/TSODSO.jl additively: insert the four ADMM
    includes immediately BEFORE `end # module TSODSO`, after `include("pricing/welfare.jl")`,
    in dependency order: residuals.jl, AgrOpt.jl, DsoOpt.jl, solve_admm.jl (the loop consumes
    the other three). Confirm in a header comment that NO Phase-5 source file is modified — the
    AGR-OPT builder reuses `Aggregator.contribute!` directly (RESEARCH Pattern 4 option (a)),
    so no builder call site needs to be exposed. Do not touch any other src/ file.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO' 2>&1 | tail -5 && test -f src/admm/AgrOpt.jl && test -f src/admm/DsoOpt.jl && test -f src/admm/solve_admm.jl</automated>
  </verify>
  <acceptance_criteria>
    - `src/admm/AgrOpt.jl`, `src/admm/DsoOpt.jl`, `src/admm/solve_admm.jl` exist and are
      comment-only (no `function`, no `export`)
    - `grep -c 'include("admm/' src/TSODSO.jl` returns 4 (residuals, AgrOpt, DsoOpt, solve_admm)
    - The four ADMM includes appear AFTER `include("pricing/welfare.jl")` and BEFORE
      `end # module TSODSO`
    - `julia --project=. -e 'using TSODSO'` loads without error (the empty stubs contribute nothing)
    - No file under src/ other than src/TSODSO.jl and the new src/admm/*.jl is modified
      (`git status --porcelain src/` shows only these paths)
  </acceptance_criteria>
  <done>The ADMM module is on the include graph, TSODSO loads clean, and the three
  subproblem/loop stubs await their Wave-2/Wave-3 owners.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill the AdmmResiduals tracking struct in src/admm/residuals.jl</name>
  <files>src/admm/residuals.jl</files>
  <read_first>
    - 06-RESEARCH.md § "System Architecture Diagram" (the residual definitions:
      R_{p,j}[t] = value(pag_j[t]) − value(pag_dso_j[t]); the dual residual ρ·Δ(coupling);
      primal-residual stopping + maxiter cap) and § "Pattern 2" (the dual-ascent update)
    - src/core/ModelContext.jl (the project's mutable-struct + throw-loudly conventions to mirror)
  </read_first>
  <behavior>
    - An empty `AdmmResiduals(N, T)` (or `AdmmResiduals()`) constructs with zero recorded iters
    - `record!(res, k, primal_maxabs, dual_maxabs)` appends the per-iteration worst primal and
      dual residual magnitudes and increments the stored iteration count
    - After recording K iterations, the struct exposes `primal_trace`/`dual_trace` (length-K
      vectors) and `iters == K`
    - A `converged(res, tol)` predicate is true iff the LAST recorded primal residual ≤ tol
  </behavior>
  <action>
    Fill src/admm/residuals.jl with a `mutable struct AdmmResiduals` holding the primal-residual
    trace, the dual-residual trace, and the iteration count (the small struct RESEARCH's diagram
    calls for). Provide a constructor and a `record!(res, k, primal_maxabs, dual_maxabs)` recorder
    that pushes the per-iteration worst-|R_p| and worst-|ρ·Δ| onto the traces, plus a
    `converged(res, tol)` primal-residual predicate. Keep it a pure data/bookkeeping type — NO
    JuMP, NO solves — so both Phase 6 (primal-only stopping) and Phase 7 (adds the dual-residual
    stop + adaptive-ρ) reuse it. Cite the residual definitions from RESEARCH Pattern 2 in the
    docstring. `export AdmmResiduals, record!, converged` (the file declares its own exports per
    the include-graph convention).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; r=AdmmResiduals(2,24); record!(r,1,1.0,0.5); record!(r,2,1e-6,1e-6); @assert r.iters==2; @assert converged(r,1e-5); println("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `src/admm/residuals.jl` contains `struct AdmmResiduals` and `export AdmmResiduals`
    - The one-liner above prints `ok` (constructs, records two iterations, `iters==2`,
      `converged(r,1e-5)` true after a 1e-6 final primal residual)
    - `converged(r, tol)` returns false when the last primal residual exceeds `tol`
    - The file imports no solver and builds no JuMP model (`grep -vc '^#' ...` shows no
      `optimize!`/`Model(`)
  </acceptance_criteria>
  <done>AdmmResiduals is a reusable, JuMP-free residual ledger with primal-residual convergence
  and a recorder the Wave-3 loop drives.</done>
</task>

<task type="auto">
  <name>Task 3: Phase6Fixtures (2-bus dual-sign anchor) + Wave-0 RED cross-validation/build-once harness</name>
  <files>test/fixtures_phase6.jl, test/test_admm.jl</files>
  <read_first>
    - test/fixtures_phase4.jl (the @testmodule pattern, the CONTRACT "defines functions/consts
      ONLY, never calls a later-wave symbol at load time", the seeded `_house_aggregator`
      helper, and `build_ieee13_ground_aggregators` which this harness REUSES for IEEE-13)
    - test/runtests.jl (`@run_package_tests`; items discovered under test/ and src/) and
      test/test_pricing_dlmp.jl (an example of RED-then-green @testitem naming with substring
      filters)
    - 06-VALIDATION.md § "Per-Task Verification Map" + § "Wave 0 Requirements" (the exact filter
      substrings: "admm", "resolve"/build-once, "crossval", "ieee13"; the 2-bus is the dual-sign
      anchor) and 06-RESEARCH.md § "Pattern 5" (the cross-validation metric + tolerances)
    - src/data/Feeder.jl (Bus/Branch/Feeder constructors) and src/data/profiles.jl
      (`generate_profiles`) for the seeded 2-bus aggregator
  </read_first>
  <action>
    Create test/fixtures_phase6.jl as `@testmodule Phase6Fixtures` (using TSODSO), obeying the
    Phase4Fixtures CONTRACT (define functions/consts only; never evaluate a later-wave ADMM
    symbol at module-load time). Provide `two_bus_feeder()`: root bus 1 (is_root, v fixed 1.0) +
    one load bus 2, a single branch 1→2 with SMALL r/x (near-lossless, so the analytic DADP at
    bus 2 ≈ λ₀ — the dual-SIGN anchor: positive price = marginal cost of consumption) and the
    `SMAX_NO_LIMIT` sentinel on the branch. Provide `build_two_bus_aggregators(feeder; seed)`:
    ONE small seeded aggregator at bus 2 (reuse the Phase4Fixtures `_house_aggregator` shape via
    `generate_profiles`, scaled small so the solve is feasible and near-lossless), plus a pinned
    `RHO_2BUS` starting ρ and a `mem_price_profile`-style λ₀ (reuse Phase4Fixtures.mem_price_profile
    or a small constant λ₀ vector length T). Export the fixture symbols.

    Then create test/test_admm.jl with RED `@testitem`s (names containing the VALIDATION filter
    substrings) that FAIL until Wave 3 fills solve_admm — do NOT make them pass here:
    (a) `"admm: cross-validation 2-bus (crossval)"` — solve_welfare vs solve_admm on the 2-bus:
        ADMM welfare ≈ objective_value, ADMM λ ≈ extract_dlmp(centralized) at the load bus, sign
        pinned positive; (b) `"admm: cross-validation ieee13 (crossval)"` — same on
        build_ieee13_ground_aggregators + ieee13_modified with allow_export=true, DSO-OPT exact;
        (c) `"admm: build-once resolve (resolve)"` — assert the subproblem models' num_variables /
        num_constraints are unchanged across a loop step (no per-iteration rebuild). Reference
        `solve_admm`/`AgrOpt`/`DsoOpt` so the items are RED (UndefVarError / not-yet-implemented),
        with `setup=[Phase6Fixtures, Phase4Fixtures]`. Keep them loud-failing, not crashing
        discovery (the runner must stay healthy).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter = ti -> occursin("admm", ti.name)' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures_phase6.jl` contains `@testmodule Phase6Fixtures`, `two_bus_feeder`, and
      `build_two_bus_aggregators`, and exports them
    - `julia --project=. -e 'using TSODSO; include-free construct'`: the 2-bus feeder builds
      (radial, root fixed) and `build_two_bus_aggregators` returns one Aggregator at bus 2
      (verifiable by a throwaway REPL line; the module must not call ADMM symbols at load time)
    - `test/test_admm.jl` defines at least three `@testitem`s whose names contain `admm` and,
      respectively, `crossval` (×2, one containing `ieee13`) and `resolve`
    - Running the `admm`-filtered items reports FAILURES (RED), not discovery crashes / load
      errors — the runner completes and lists the failing items
    - fixtures_phase6.jl makes NO top-level call to `solve_admm`/`AgrOpt`/`DsoOpt`
      (`grep -n 'solve_admm\|AgrOpt\|DsoOpt' test/fixtures_phase6.jl` returns nothing)
  </acceptance_criteria>
  <done>The 2-bus dual-sign anchor + IEEE-13 reuse are in place, and the failing cross-validation
  and build-once gates are pinned for Wave 3 to drive green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → published gate | A RED stub that silently passes (or crashes discovery) would hide the Phase-6 correctness gate before it is even built |
| fixture generation → reproducibility | An unseeded/global-RNG fixture makes the 2-bus anchor non-reproducible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test/test_admm.jl RED stubs | mitigate | Items reference not-yet-defined ADMM symbols so they fail loudly; acceptance criteria assert they are RED, not passing/crashing |
| T-06-06 | Repudiation | test/fixtures_phase6.jl | mitigate | 2-bus aggregator drawn from seeded `generate_profiles` (StableRNGs), reproducible bit-for-bit (RESEARCH Security Domain) |
| T-06-08 | Tampering | src/TSODSO.jl include edit | mitigate | Additive include only, after pricing/welfare.jl; acceptance asserts no other src/ file changes and TSODSO still loads |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package install this phase (RESEARCH Package Legitimacy Audit: not applicable — Manifest.toml unchanged) |
</threat_model>

<verification>
- `julia --project=. -e 'using TSODSO'` loads with the four ADMM includes present
- `AdmmResiduals` records/queries correctly (Task 2 one-liner prints `ok`)
- The `admm`-filtered `@testitem`s run RED (cross-validation ×2 + build-once), runner healthy
- `Pkg.test()` shows Phases 1-5 still green (no regression from the include edit)
</verification>

<success_criteria>
- src/admm/ is wired and loads; residuals.jl filled; three subproblem/loop stubs created
- Phase6Fixtures (2-bus anchor + IEEE-13 reuse) available to downstream tests
- RED cross-validation + build-once harness pinned (the false-convergence net, pre-armed)
</success_criteria>

<output>
Create `.planning/phases/06-admm-decomposition-core/06-01-SUMMARY.md` when done.
</output>
