---
phase: 24-discrete-integer-investment-expansion
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - src/planning/benders.jl
  - test/test_planning_benders_integer.jl
autonomous: true
requirements: [INT-02]

must_haves:
  truths:
    - "Researcher can call solve_stackelberg!(...; master = <prebuilt BendersMasterInteger>) and reach the integer master, mirroring the existing follower = nothing injection seam exactly; the default (master = nothing) path is byte-identical to today"
    - "The loop's own termination criterion for the integer path is an EXCLUSIVE exact match against a caller-supplied known_optimum (D-13/D-14's decided fallback) — never an || with the continuous loop's inherited tol = 1e-6 gap, which would let the loop falsely converge on the certified path before the exact match is confirmed — and this uses its OWN new, empirically-measured atol constant, never tol's value"
    - "Every optimality-branch iteration on the integer path fires apply_integer_cuts! (LL cut always, no-good on detected stall), with the no-good count and a converged_via attribution surfaced in the return value — never silently absorbed"
  artifacts:
    - path: "src/planning/benders.jl"
      provides: "master = nothing / known_optimum kwargs, apply_integer_cuts! wiring, nogood_count/converged_via return fields"
      contains: "master === nothing"
  key_links:
    - from: "src/planning/benders.jl"
      to: "src/planning/master_integer.jl"
      via: "apply_integer_cuts!(master, lb_res, Q_nu)"
      pattern: "apply_integer_cuts!"
    - from: "src/planning/benders.jl"
      to: "src/planning/trace.jl"
      via: "push!(trace, ...; nogood_count = ...)"
      pattern: "nogood_count"
---

<objective>
Wire the integer master into `solve_stackelberg!` via the `master = nothing` injection kwarg
(D-08, mirroring the existing `follower = nothing` seam in the SAME signature), thread
`apply_integer_cuts!` into the optimality branch (generic, no-op for the continuous path), add
the `known_optimum` kwarg that implements D-13/D-14's decided termination fallback, and update
the file's own documented "no `build_*`/`Model(` call outside the single construction point"
invariant to describe the new seam rather than leave it silently false.

Purpose: this is where the phase's mechanism actually becomes REACHABLE end-to-end — the first
plan that calls `build_master_integer` through the SAME production entrypoint every continuous
Stackelberg run already uses, and the first plan where a genuine `:nogood_assisted` vs clean
convergence attribution can be observed on a real run.

Output: `src/planning/benders.jl` (additive, byte-identical-by-default changes) and
`test/test_planning_benders_integer.jl` (new end-to-end smoke test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-discrete-integer-investment-expansion/24-CONTEXT.md
@.planning/phases/24-discrete-integer-investment-expansion/24-RESEARCH.md

@src/planning/benders.jl
@src/planning/master_integer.jl
@test/fixtures_phase6.jl
</context>

<interfaces>
<!-- benders.jl's current shape, read in full this session -- reproduced here so the executor
     never has to re-open the file to find these exact anchors. Line numbers refer to the file
     BEFORE this plan's edits. -->

Docstring invariant to UPDATE (line 62, inside the `# Algorithm` step 2 prose):
```
2. BUILD ONCE, outside the loop: `oracle = build_planning_oracle(...)`,
   `follower = follower === nothing ? build_follower(...) : follower`,
   `master = build_master(; master_kwargs..., T = T)`. No `build_*`/`Model(` call appears
   anywhere inside the `for k in 1:max_iter` loop below.
```
Must become (additive rewrite, same paragraph): describe that `master` ALSO now accepts an
injected pre-built value (mirroring the `follower` sentence immediately below it), so the
invariant reads "no `build_*`/`Model(` call appears inside this function OTHER than the two
conditional builder calls at construction time, both of which are skippable via injection" — do
not just delete the old sentence, replace it with one that remains TRUE after this plan's change.

Function signature (line 133-145) — add `master = nothing` as the LAST keyword (mirrors
`follower`'s own trailing position) and a NEW `known_optimum::Union{Nothing,Real} = nothing`:
```julia
function solve_stackelberg!(
    feeder, pf::AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator};
    λ₀, T::Int, follower_kwargs::NamedTuple, master_kwargs::NamedTuple,
    tol::Real = 1e-6, max_iter::Int = 100,
    checkpoint_dir::AbstractString = datadir("planning_checkpoints"),
    follower = nothing, master = nothing, known_optimum::Union{Nothing,Real} = nothing,
)
```

Existing follower/kwargs mutual-exclusion guard (lines 168-178) — mirror this EXACT shape for
`master`/`master_kwargs`:
```julia
follower === nothing || isempty(follower_kwargs) || throw(ArgumentError(
    "solve_stackelberg!: supply either follower_kwargs or follower, not both " *
    "(got follower=$follower, follower_kwargs=$follower_kwargs)"))
```

Construction point (lines 180-185) — the ONLY place `build_master`/`Model(` may appear:
```julia
oracle = build_planning_oracle(feeder, pf, aggregators; λ₀ = λ₀, T = T)
follower = follower === nothing ? build_follower(; follower_kwargs..., T = T) : follower
master = build_master(; master_kwargs..., T = T)
```

Optimality-branch cut appends (lines 271-275) — `apply_integer_cuts!` call goes IMMEDIATELY
after these two lines, before the `cost_k = ...` line:
```julia
add_optimality_cut!(master, :op, -oracle_res.cost, oracle_res.π, lb_res.z)
add_optimality_cut!(master, :x, follower_res.cost, follower_res.π_s, lb_res.z)
```

`cost_k`/`gap`/convergence check (lines 279-285, 296-330):
```julia
cost_k = master.c_y * lb_res.y + follower_res.cost - oracle_res.cost
if cost_k < UB
    UB = cost_k; y_best = lb_res.y; z_best = copy(lb_res.z)
end
gap = (UB - lb_res.LB) / max(1, abs(UB))
checkpoint_iteration!(...)
push!(trace, k; LB = lb_res.LB, UB = UB, gap = gap, cut_type = :optimality,
      n_cuts = length(master.cuts), master_status = master_status_k,
      oracle_status = Symbol(termination_status(oracle.model)),
      retry_count = (master_attempts[] - 1) + (oracle_attempts[] - 1), solve_time = t_solve)
if gap <= tol
    return (; y = y_best, z = z_best, UB, LB = lb_res.LB, gap, iters = k,
            oracle, follower, master, trace)
end
```

`apply_integer_cuts!(master, lb_res, Q_nu)` (from plan 24-03) is generic: no-op returning
`(; nogood_fired = false)` for `BendersMaster`, real LL/no-good logic returning
`(; nogood_fired::Bool)` for `BendersMasterInteger`. `Q_nu = follower_res.cost - oracle_res.cost`
(the recourse value, excluding the leader's own `c_y*y` term — exactly `cost_k` minus that term).

Self-contained smoke-test fixture (reproduces `test/fixtures_phase6.jl`'s `two_bus_feeder()` +
`test/test_planning_oracle.jl`'s `ToyDeviceFixture.ToyElasticDevice` WITHOUT depending on either
`@testmodule` — both are TestItems-only and unreachable from a raw script per this project's
documented `julia --project=test -e '...'` sibling-worktree hazard; a plain `julia --project=.`
script must define its own throwaway copies):
```julia
using TSODSO, JuMP
struct ToyElasticDevice <: AbstractDevice
    bus::Int; a::Float64; b::Float64; Pmax::Float64
end
function TSODSO.contribute!(d::ToyElasticDevice, ctx::ModelContext; T::Int)
    m = ctx.model
    p = @variable(m, [t = 1:T], lower_bound = 0.0, upper_bound = d.Pmax)
    p_inject = AffExpr[-p[t] for t in 1:T]
    utility = sum(d.a * p[t] - (d.b / 2) * p[t]^2 for t in 1:T)
    return (; vars = (; p), p_inject, utility)
end
feeder = Feeder([Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false)],
                [Branch(1, 2, 1e-3, 1e-3, SMAX_NO_LIMIT)], 1)
dev = ToyElasticDevice(2, 6.0, 1.0, 10.0)
agg = Aggregator(2, 0.9, [dev], [0.0])
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: master=nothing / known_optimum injection + docstring update + guard</name>
  <files>src/planning/benders.jl, test/test_planning_benders_integer.jl</files>
  <action>
  In `src/planning/benders.jl`:
  1. Add `master = nothing` and `known_optimum::Union{Nothing,Real} = nothing` to
     `solve_stackelberg!`'s keyword list (see `<interfaces>` exact signature). Update the
     function's own top-level docstring signature block to list both new keywords.
  2. Add a mutual-exclusion guard mirroring the existing `follower`/`follower_kwargs` one
     EXACTLY: `master === nothing || isempty(master_kwargs) || throw(ArgumentError("solve_stackelberg!: supply either master_kwargs or master, not both (got master=$master, master_kwargs=$master_kwargs)"))`,
     placed immediately after the existing follower guard.
  3. Add a boundary guard for `known_optimum`:
     `known_optimum === nothing || isfinite(known_optimum) || throw(ArgumentError("solve_stackelberg! needs known_optimum to be finite when supplied, got known_optimum=$known_optimum"))`.
  4. Change the construction line `master = build_master(; master_kwargs..., T = T)` to
     `master = master === nothing ? build_master(; master_kwargs..., T = T) : master` —
     mirrors the `follower` line immediately above it VERBATIM in structure.
  5. Update the docstring's "BUILD ONCE" paragraph (see `<interfaces>` for exact target text) to
     describe the injection seam for `master`, alongside the existing `follower` description —
     do not leave the old "no `build_*`/`Model(` call appears anywhere inside the loop" sentence
     silently inaccurate; restate it so it remains literally true (the two conditional builder
     calls are the ONLY `build_*`/`Model(`-adjacent expressions in the whole function, both
     skippable via injection, and BOTH still execute strictly BEFORE the `for k in 1:max_iter`
     loop).
  6. **Measure `KNOWN_OPTIMUM_ATOL` empirically before hardcoding it (mirrors 24-01 Task 1's A2
     "does it stall" empirical-check pattern — do NOT assume a threshold, measure it).** Build
     the D-12 fixture's `oracle`/`follower` once, solve at a representative interior point
     (e.g. `z = 1.0`, away from either box boundary), and read the solver's OWN achieved
     duality-gap diagnostics directly — `abs(objective_value(oracle.model) - dual_objective_value(oracle.model))`
     and `abs(objective_value(follower.model) - dual_objective_value(follower.model))` — rather
     than assuming a number. This is a direct empirical measurement of achieved precision (no
     second reference solve needed: `dual_objective_value` IS the solver's own certified bound at
     the primal solution). Set `KNOWN_OPTIMUM_ATOL = max(1e-9, 10 * max(gap_oracle, gap_follower))`
     with a code comment recording the ACTUAL measured `gap_oracle`/`gap_follower` values and the
     date — if the measured gaps are comfortably below `1e-10` (10x margin under `1e-9`), keep
     `KNOWN_OPTIMUM_ATOL = 1e-9` and say so; if not, the constant must reflect the measured
     precision, never a hopeful guess (this project has already been bitten by exactly this class
     of problem — quick task `260823-gea` found a gate sitting at the solver's achievable
     precision — do not repeat it). Declare `const KNOWN_OPTIMUM_ATOL = <measured value>` near the
     top of the file (module scope, alongside the file's other top-level `using` statements, or
     immediately above `solve_stackelberg!`), with a comment citing D-13/D-14 and stating this is
     a NEW, dedicated threshold for the exact enumeration-match termination fallback —
     deliberately distinct from the `tol` kwarg's inherited `1e-6`, so it is never mistaken for
     "reusing" the continuous loop's tolerance (the standing anti-certificate-laundering bar).

  7. **Make the termination criteria MUTUALLY EXCLUSIVE — this is load-bearing, not a style
     choice.** Immediately before the existing `gap = (UB - lb_res.LB) / max(1, abs(UB))` line,
     keep that line COMPLETELY UNCHANGED (it still feeds `trace`/the returned `gap` field with
     its existing meaning — this is a reporting quantity, not the termination gate, on the
     `known_optimum`-supplied path). Then compute:
     `converged_now = known_optimum === nothing ? (gap <= tol) : isapprox(UB, known_optimum; atol = KNOWN_OPTIMUM_ATOL)`
     — an EXCLUSIVE branch, NEVER an `||` of the two criteria. An `||` would let the loop return
     the moment `gap <= tol` holds even when `known_optimum` IS supplied, silently reintroducing
     the continuous loop's inherited `1e-6` tolerance on EXACTLY the certified path D-13 forbids
     it on (plan-checker Blocker 2) — on this fixture's tiny 1-D convex problem `gap` drops below
     `1e-6` quickly, so an `||` would make the "exact enumeration match" criterion never actually
     get exercised in practice, even though it looks present in the code. Replace the
     `if gap <= tol` early-return check with `if converged_now`. The ONLY behavioral change when
     `known_optimum === nothing` is that `converged_now` reduces EXACTLY to `gap <= tol` —
     byte-identical to today.

  8. **Prove the mutual exclusivity directly, at the unit level** (do not rely solely on the
     full loop's natural behavior to demonstrate this): in the new test file (below), replicate
     the `converged_now` formula as a small standalone expression and assert TWO cases: (a)
     `known_optimum = nothing`, `gap = 0.0` (well under any `tol`) → `converged_now == true`,
     mirrors today; (b) `known_optimum` set to a value CLEARLY outside `KNOWN_OPTIMUM_ATOL` of
     `UB`, while `gap` is ALSO `<= tol` (a deliberately adversarial case — the exact scenario the
     forbidden `||` would have wrongly accepted) → `converged_now == false`. Case (b) is the
     literal regression against Blocker 2's reintroduced-tolerance failure mode.

  This task does NOT yet wire `apply_integer_cuts!` or the no-good/`converged_via` return fields
  — that is Task 2, kept separate so this task's own regression (byte-identical default path) is
  easy to verify in isolation before the more invasive change lands.

  Create `test/test_planning_benders_integer.jl` with ONE `@testitem` tagged `[:planning]`
  (mirroring the header/style of `test_planning_benders.jl`) proving the DEFAULT path is
  byte-identical: call `solve_stackelberg!` on the D-12 fixture (reuse `Phase6Fixtures`/
  `ToyDeviceFixture` via `setup=[Phase6Fixtures, ToyDeviceFixture]`, the normal TestItems idiom —
  the raw-script restriction only applies to THIS plan's own `<verify>` command below, not to
  the actual committed test file) with `master=nothing, known_optimum=nothing` supplied
  EXPLICITLY (not omitted), and assert the result matches `PlanningFixtures.N1_Y_HAND`/
  `N1_Z_HAND`/`N1_OBJ_HAND` exactly as `test_planning_goldens.jl`'s existing N=1 golden does —
  proving the new kwargs' mere PRESENCE (not their omission) leaves the default path unchanged.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/TSO-DSO && julia --project=. -e '
using TSODSO, JuMP
struct ToyElasticDevice <: AbstractDevice
    bus::Int; a::Float64; b::Float64; Pmax::Float64
end
function TSODSO.contribute!(d::ToyElasticDevice, ctx::ModelContext; T::Int)
    m = ctx.model
    p = @variable(m, [t = 1:T], lower_bound = 0.0, upper_bound = d.Pmax)
    p_inject = AffExpr[-p[t] for t in 1:T]
    utility = sum(d.a * p[t] - (d.b / 2) * p[t]^2 for t in 1:T)
    return (; vars = (; p), p_inject, utility)
end
feeder = Feeder([Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false)],
                [Branch(1, 2, 1e-3, 1e-3, SMAX_NO_LIMIT)], 1)
dev = ToyElasticDevice(2, 6.0, 1.0, 10.0)
agg = Aggregator(2, 0.9, [dev], [0.0])
follower_kwargs = (; corridor_cap = 2.0, x_inv_max = 2.0, c_inv = 1.0, c_op = [0.5])
master_kwargs = (; c_y = 0.3, y_max = 8.0, α_op_lb = -5.0, α_x_lb = 0.0)
result = mktempdir() do dir
    solve_stackelberg!(feeder, LinDistFlow(), [agg]; λ₀ = [4.0], T = 1,
        follower_kwargs = follower_kwargs, master_kwargs = master_kwargs,
        tol = 1e-6, max_iter = 100, checkpoint_dir = dir, master = nothing, known_optimum = nothing)
end
@assert isapprox(result.y, 0.7; atol = 1e-3)
@assert isapprox(result.UB, -0.245; atol = 1e-3)
println("byte-identical default path OK: y=", result.y, " UB=", result.UB)
# Blocker-2 regression: converged_now must be an EXCLUSIVE branch, never (gap<=tol || exact-match).
_converged_now(known_optimum, gap, tol, UB, atol) =
    known_optimum === nothing ? (gap <= tol) : isapprox(UB, known_optimum; atol = atol)
@assert _converged_now(nothing, 0.0, 1e-6, 5.0, 1e-9) == true
@assert _converged_now(5.0, 0.0, 1e-6, 999.0, 1e-9) == false  # gap<=tol holds but UB is nowhere near known_optimum -> must NOT converge (this is the forbidden || case)
@assert _converged_now(5.0, 1.0, 1e-6, 5.0, 1e-9) == true     # gap is NOT <= tol but UB matches known_optimum exactly -> must converge
println("mutual-exclusivity OK: the forbidden || escape hatch is closed")
'</automated>
  </verify>
  <done>solve_stackelberg! accepts master=nothing/known_optimum=nothing explicitly with zero behavioral change vs the pre-existing default (y≈0.7, UB≈-0.245 on the D-12 fixture); converged_now is a mutually-exclusive branch (proven by the adversarial case where gap<=tol holds but UB does not match known_optimum); the docstring invariant is updated, not silently false.</done>
</task>

<task type="auto">
  <name>Task 2: apply_integer_cuts! wiring + nogood_count/converged_via return + end-to-end smoke</name>
  <files>src/planning/benders.jl, test/test_planning_benders_integer.jl</files>
  <action>
  In `src/planning/benders.jl`, immediately after the two existing `add_optimality_cut!(master, :op, ...)`/`add_optimality_cut!(master, :x, ...)` calls (and BEFORE the `cost_k = ...` line), add:
  `Q_nu = follower_res.cost - oracle_res.cost` and `integer_cut_res = apply_integer_cuts!(master, lb_res, Q_nu)`
  — a single generic call (no `isa` branching): no-op for `BendersMaster`, real LL/no-good logic
  for `BendersMasterInteger` (plan 24-03's dispatch). Accumulate a running
  `nogood_total::Int` (initialize `nogood_total = 0` alongside the loop's other accumulator
  state, right next to `UB`/`y_best`/`z_best`/`gap`/`trace`), incrementing it by 1 whenever
  `integer_cut_res.nogood_fired` is `true`. Thread `nogood_count = integer_cut_res.nogood_fired ? 1 : 0`
  into the EXISTING `push!(trace, k; ...)` call on the optimality branch (add this ONE new
  keyword to that call; the feasibility-branch `push!` call does not need it, since
  `apply_integer_cuts!` is never invoked on that branch — the follower/oracle sequencing is
  unchanged, WR-01 ordering stays intact). On the converged-return path
  (`if converged_now ... return (; y = y_best, ..., trace)`), add TWO new fields to the returned
  NamedTuple: `nogood_count = nogood_total` and
  `converged_via = nogood_total > 0 ? :nogood_assisted : :clean` — appended AFTER the existing
  fields (`oracle, follower, master, trace`), so every EXISTING field's position/value is
  unchanged for any caller destructuring by name (Julia NamedTuple field access is name-based,
  never position-based, so this is safe for all existing call sites).

  Extend `test/test_planning_benders_integer.jl` with a SECOND `@testitem`: build a
  `BendersMasterInteger` via `build_master_integer(; T=1, K=4, c_y=0.3, y_max=8.0, α_op_lb=-5.0,
  α_x_lb=0.0)` on the D-12 fixture, call `solve_stackelberg!(...; master_kwargs = NamedTuple(),
  master = <that master>, max_iter = 50, checkpoint_dir = mktempdir())` (no `known_optimum` yet —
  plan 24-05's certification harness supplies that), and assert it EITHER converges within
  `max_iter` OR raises the existing loud `ErrorException` naming the exhausted count (either
  outcome is acceptable at this smoke-test stage — the point is proving the wiring runs without
  a `MethodError`/`UndefVarError`, not yet proving convergence correctness, which plan 24-05
  certifies). If it converges, additionally assert `result.y` is within `1e-6` of a multiple of
  `8.0/16 = 0.5` (a genuine lattice point) and that `result.nogood_count >= 0` and
  `result.converged_via in (:clean, :nogood_assisted)` are both present and well-typed.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/TSO-DSO && julia --project=. -e '
using TSODSO, JuMP
struct ToyElasticDevice2 <: AbstractDevice
    bus::Int; a::Float64; b::Float64; Pmax::Float64
end
function TSODSO.contribute!(d::ToyElasticDevice2, ctx::ModelContext; T::Int)
    m = ctx.model
    p = @variable(m, [t = 1:T], lower_bound = 0.0, upper_bound = d.Pmax)
    p_inject = AffExpr[-p[t] for t in 1:T]
    utility = sum(d.a * p[t] - (d.b / 2) * p[t]^2 for t in 1:T)
    return (; vars = (; p), p_inject, utility)
end
feeder = Feeder([Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false)],
                [Branch(1, 2, 1e-3, 1e-3, SMAX_NO_LIMIT)], 1)
dev = ToyElasticDevice2(2, 6.0, 1.0, 10.0)
agg = Aggregator(2, 0.9, [dev], [0.0])
follower_kwargs = (; corridor_cap = 2.0, x_inv_max = 2.0, c_inv = 1.0, c_op = [0.5])
imaster = build_master_integer(; T = 1, K = 4, c_y = 0.3, y_max = 8.0, α_op_lb = -5.0, α_x_lb = 0.0)
try
    result = mktempdir() do dir
        solve_stackelberg!(feeder, LinDistFlow(), [agg]; λ₀ = [4.0], T = 1,
            follower_kwargs = follower_kwargs, master_kwargs = NamedTuple(),
            master = imaster, max_iter = 50, checkpoint_dir = dir)
    end
    @assert result.nogood_count >= 0
    @assert result.converged_via in (:clean, :nogood_assisted)
    println("integer end-to-end smoke OK: y=", result.y, " nogood_count=", result.nogood_count, " converged_via=", result.converged_via)
catch e
    println("integer end-to-end smoke: loop raised as expected on this smoke pass (acceptable): ", sprint(showerror, e))
end
'</automated>
  </verify>
  <done>apply_integer_cuts! fires on the optimality branch for both master types; nogood_count/converged_via are surfaced on the returned NamedTuple; the end-to-end smoke test runs without a MethodError/UndefVarError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `solve_stackelberg!`'s `master`/`master_kwargs` mutual-exclusion guard | Silently picking one when both are supplied would be an ambiguous, hard-to-diagnose caller error — mirrors the existing `follower` guard's own threat disposition |
| `known_optimum`'s exact-match termination path | A caller-supplied wrong `known_optimum` would make the loop falsely report convergence at the WRONG lattice point — this is why the criterion is an EXCLUSIVE branch (never `||`ed with `gap<=tol`) and why `KNOWN_OPTIMUM_ATOL` is empirically measured against the solvers' own achieved precision rather than guessed, and why plan 24-05's certification is the only place this kwarg is ever populated (never guessed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Tampering | `master`/`master_kwargs` mutual exclusion | mitigate | Guard mirrors the existing, already-reviewed `follower`/`follower_kwargs` guard verbatim in structure and wording |
| T-24-11 | Repudiation | `KNOWN_OPTIMUM_ATOL` vs `tol`, and `converged_now`'s branch structure | mitigate | `KNOWN_OPTIMUM_ATOL` is declared as a distinct named module-level constant, set from an EMPIRICAL measurement of the solvers' own achieved duality gap (never a guessed literal, per Warning 1) with a comment recording the measurement; `converged_now` is a mutually EXCLUSIVE branch on `known_optimum === nothing`, never an `||`, proven by Task 1's own adversarial unit-level regression (Blocker 2) — both are greppable and cannot be confused with `tol`/silently reintroduce it in a future review |
| T-24-12 | Denial of Service | Integer loop with no `known_optimum` and no natural convergence | accept | The existing `max_iter` exhaustion raises the SAME loud `ErrorException` as today (D-10) — never a silent hang; this is the correctly-honest behavior for the explicitly-deferred "production termination criterion for large lattices" open item, not a defect to fix in this phase |

No new package installs this plan (RESEARCH.md Package Legitimacy Audit: "Not applicable") — the
SC (supply-chain) row is intentionally omitted.
</threat_model>

<verification>
Run the new test file's `@testitem`s via the full suite, plus the EXISTING PVAL-01/PVAL-02
certification/golden `@testitem`s (unchanged files, must still pass byte-identically) — do NOT
invoke TestItemRunner directly (documented sibling-worktree hazard, MEMORY.md). Use
`julia --project=. -e 'import Pkg; Pkg.test()'` (background, ~23 min) for the full-suite gate,
or the per-task `<automated>` scripts above for a fast per-task check.
</verification>

<success_criteria>
- `solve_stackelberg!` accepts `master`/`known_optimum` kwargs; the default (`master = nothing`,
  `known_optimum = nothing`) path is proven byte-identical against the existing PVAL-02 N=1
  golden (`y≈0.7`, `UB≈-0.245`).
- The docstring's "no `build_*`/`Model(` outside the single construction point" invariant is
  updated to remain literally true, not silently falsified.
- `apply_integer_cuts!` fires generically on the optimality branch; `nogood_count`/
  `converged_via` are present on every returned NamedTuple.
- The end-to-end integer smoke test (`build_master_integer` through `solve_stackelberg!`) runs
  without a `MethodError`/`UndefVarError`, either converging with a well-typed
  `nogood_count`/`converged_via` or raising the existing loud exhaustion error.
- All existing PVAL-01/PVAL-02 tests still pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/24-discrete-integer-investment-expansion/24-04-SUMMARY.md` when done.
</output>
