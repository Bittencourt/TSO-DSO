---
phase: 24-discrete-integer-investment-expansion
plan: 06
type: execute
wave: 6
depends_on: ["24-05"]
files_modified:
  - docs/literate/integer_investment.jl
  - docs/make.jl
autonomous: true
requirements: [INT-04]

must_haves:
  truths:
    - "A live-executed literate page documents the binary-expansion lattice (D-01/D-02/D-03/D-04), the LL/no-good cut mechanism (Findings 1/2), the PVAL-04 guard-lift rationale (D-06/D-07), the mip_rel_gap fix (Finding 4), the enumeration certificate + D-15 certificates, the D-16 no-good/converged_via attribution, and BOTH documented negative results (δ_min non-derivability / Finding 3, and the D-11 BilevelJuMP-unavailable finding) — with zero hardcoded literals, mirroring the existing stackelberg_benders.jl style"
    - "The page is wired into docs/make.jl's Literate loop and pages tree, and the full test suite is green (no new regressions vs the known baseline)"
  artifacts:
    - path: "docs/literate/integer_investment.jl"
      provides: "the INT-04 literate rung page"
      min_lines: 80
    - path: "docs/make.jl"
      provides: "integer_investment.jl wired into the Literate loop + pages tree"
      contains: "integer_investment.jl"
---

<objective>
Close out Phase 24 with the INT-04 deliverable: a live-executed Documenter/Literate.jl rung page
narrating the phase's full arc — the lattice design and its documented `y_max`-unreachability
artifact, the Laporte–Louveaux cut mechanism and why the continuous cuts remain valid alongside
it, the conscious PVAL-04 guard lift, the required MILP exactness fix, the enumeration
certificate and this phase's own two new certificates, the no-good anti-stall fallback's honest
attribution, and BOTH of RESEARCH.md's documented negative results (the `δ_min` non-derivability
and the BilevelJuMP-unavailable-on-this-fixture finding) — then run the full suite to confirm
zero regressions.

Purpose: this is the project's standing "rich, step-by-step documentation of every modeling
decision" requirement (CLAUDE.md) applied to Phase 24's own genuinely new mathematics, and the
final phase-gate proving nothing upstream (Phases 1-23) regressed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-discrete-integer-investment-expansion/24-CONTEXT.md
@.planning/phases/24-discrete-integer-investment-expansion/24-RESEARCH.md

@docs/literate/stackelberg_benders.jl
@docs/make.jl
</context>

<interfaces>
<!-- docs/make.jl's current Literate loop + pages tree (read in full this session) -- the exact
     anchors to extend, additively, no reordering of existing entries. -->

```julia
for src in (
    "toy_dc.jl", "lindistflow.jl", "convex_branch_flow.jl", "ac_oracle.jl",
    "restricted_branch_flow.jl", "prosumer_welfare.jl", "pricing_dlmp.jl", "admm.jl",
    "stackelberg_benders.jl", "nash_diagonalization.jl", "ieee123_impedances.jl",
    "thesis_reproduction_ieee123.jl", "thesis_reproduction_assumptions.jl",
    "socp_applicability.jl", "ieee8500_scaling.jl", "mpc_rolling_horizon.jl",
    "stochastic_pv_demand.jl", "meshed_reactive_price.jl", "experiments.jl",
)
    Literate.markdown(joinpath(LITERATE_DIR, src), GENERATED_DIR;
                       flavor = Literate.DocumenterFlavor())
end
```
Add `"integer_investment.jl"` as a NEW entry (with an inline comment `# NEW: Rung 11 Discrete/
Integer Investment Expansion (INT-01..04)`, mirroring every other "NEW:" comment convention
already in this loop) — position it AFTER `"meshed_reactive_price.jl"` (the most recently shipped
v3.0 rung) and BEFORE `"experiments.jl"` (which stays last, per the file's own existing
convention of keeping the experiment harness page at the end).

```julia
pages = [
    "Home" => "index.md",
    "Models" => [ ... ],
    "Experiments" => [ "The Experiment Harness" => "generated/experiments.md" ],
    "Planning" => [
        "Rung 6: Stackelberg-Benders" => "generated/stackelberg_benders.md",
        "Rung 7: Nash Diagonalization & Shared Corridor" => "generated/nash_diagonalization.md",
    ],
    "API Reference" => "api.md",
]
```
Add a THIRD entry to the `"Planning"` array: `"Rung 11: Discrete/Integer Investment Expansion" =>
"generated/integer_investment.md"` — this page belongs under `"Planning"` (it extends the
Stackelberg-Benders planning loop), not `"Models"`.

`docs/literate/stackelberg_benders.jl`'s own style (read in full this session): a `# #` H1 title
comment, `# ##` H2 section comments, prose paragraphs as `#`-prefixed comment blocks, a table
mapping symbols, an explicit "narrated, not re-executed" section for material already certified
elsewhere (that page does NOT re-run BilevelJuMP live — this page must do the SAME for the same
reason: no `using BilevelJuMP`/`HiGHS`/`Ipopt` anywhere in a published Documenter page, per
CLAUDE.md's "validation oracle only" rule), then a live `using TSODSO` code section that actually
CALLS the real entrypoints and prints real numbers.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: write docs/literate/integer_investment.jl</name>
  <files>docs/literate/integer_investment.jl</files>
  <action>
  Create `docs/literate/integer_investment.jl` mirroring `stackelberg_benders.jl`'s structure
  (title, PSR/problem-number-style mapping section if applicable, a narrated-not-re-executed
  section for the BilevelJuMP finding, then a live executed section). Cover, in this order:

  1. **The lattice (D-01/D-02/D-03/D-04):** the pure binary-expansion formula
     `y_inv = (y_max/2^K)*Σ 2^(k-1) b_k`, why `2^K` (not `2^K-1`) is the chosen divisor, and the
     resulting documented artifact that `y_max` itself is never attainable
     (`y_max*(1-2^-K)` at all-ones) — state this is an ACCEPTED, not fixed, consequence. Show the
     K=4 default lattice explicitly (`{0, 0.5, ..., 7.5}` for `y_max=8.0`).
  2. **The genuine integrality gap (D-04):** state plainly that the continuous optimum
     (`y*≈0.7`) is NOT on this lattice, so a nonzero gap between the continuous and integer
     answers is EXPECTED and is not "fixed."
  3. **The Laporte-Louveaux cut (Finding 1)** — the exact algebraic form (reuse plan 24-03's
     `<interfaces>` derivation verbatim: tight at the incumbent, implied-elsewhere at every other
     corner), cited to Laporte & Louveaux (1993)/Birge & Louveaux (2011 §5.2), and WHY the
     continuous `add_optimality_cut!`/`add_feasibility_cut!` cuts remain valid and keep firing
     unmodified alongside it (Finding 2's GBD-style convexity argument, one paragraph, not a
     full re-derivation).
  4. **The MILP exactness fix (Finding 4):** one paragraph on why `mip_rel_gap=>0.0` was
     required in `src/solver/factory.jl` for the outer loop's termination claim to be sound.
  5. **The PVAL-04 guard lift (D-06/D-07):** state it is a scoped, greppable, per-builder
     exemption — the operational layer and the continuous planning layer remain binary-free.
  6. **Termination (D-13/D-14) and the honest negative result (Finding 3):** state plainly that
     a rigorous, generic `δ_min` is NOT derivable for this problem class (cite the envelope-
     theorem/SOCP-dual-price argument in one paragraph), so the phase uses the enumeration-backed
     exact-match fallback instead, and that a production criterion for large (non-enumerable)
     lattices is an explicitly DEFERRED open item — not silently solved here.
  7. **No-good anti-stall + honest attribution (D-16):** explain `nogood_count`/`converged_via`,
     and that a run needing no-goods is reported as `:nogood_assisted`, never presented as clean
     LL convergence.
  8. **The BilevelJuMP non-blocker (D-10/D-11), narrated NOT re-executed:** state the two
     independent verified reasons it is unavailable on this fixture (binary-leader rejection by
     `StrongDualityMode`/`ProductMode`; the pre-existing MIQP incapacity for `BigMMode`+HiGHS),
     citing `test/test_planning_certification.jl`/`test/test_planning_certification_integer.jl`
     by name — explicitly do NOT add `using BilevelJuMP` to this page.
  9. **Live-executed section:** `using TSODSO` only (no BilevelJuMP/HiGHS/Ipopt imports, mirrors
     `stackelberg_benders.jl`'s own discipline), build the D-12 fixture, call
     `build_master_integer` + `solve_stackelberg!(...; master=..., known_optimum=...)` — since
     this page cannot easily embed the full enumeration harness inline without duplicating
     `test_planning_certification_integer.jl`'s helper, either (a) inline a SHORT copy of the
     ternary-search enumeration (a few lines, self-contained, same formula as the test) so the
     page is genuinely independent and live-executed end-to-end, or (b) run the loop WITHOUT
     `known_optimum` for a bounded `max_iter` and report the observed `nogood_count`/
     `converged_via`/best `y` found — pick (a) if it fits cleanly in a literate page's scope
     (preferred, matches "live-executed, no hardcoded literals"), falling back to (b) with an
     explicit note if (a) proves too long for a docs page. Print the real numbers obtained
     (`result.y`, `result.UB`, `result.nogood_count`, `result.converged_via`) — never a literal
     copied from a test file.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/TSO-DSO && wc -l docs/literate/integer_investment.jl && [ "$(grep -c 'using BilevelJuMP\|using HiGHS\|using Ipopt' docs/literate/integer_investment.jl)" = "0" ] && echo "no test-only solver imports OK"</automated>
  </verify>
  <done>docs/literate/integer_investment.jl exists (min ~80 lines), covers all nine required sections, contains no BilevelJuMP/HiGHS/Ipopt imports, and its live-executed section calls the real production entrypoints with no hardcoded literals.</done>
</task>

<task type="auto">
  <name>Task 2: wire into docs/make.jl + full-suite acceptance</name>
  <files>docs/make.jl</files>
  <action>
  In `docs/make.jl`, add `"integer_investment.jl"` to the `Literate.markdown` loop's tuple (see
  `<interfaces>` for exact position: after `"meshed_reactive_price.jl"`, before
  `"experiments.jl"`), with the `# NEW: Rung 11 ...` comment. Add
  `"Rung 11: Discrete/Integer Investment Expansion" => "generated/integer_investment.md"` as a
  new entry in the `"Planning"` pages array (after the existing two Rung 6/7 entries). Do not
  reorder or touch any other entry.

  Run the full test suite (`julia --project=. -e 'import Pkg; Pkg.test()'`, background, ~23 min
  per MEMORY.md's own baseline) and confirm the result matches the project's known steady-state
  baseline (2358 pass / 1 known-false Aqua/CairoMakie-drift failure) PLUS every new test added by
  plans 24-01 through 24-05 — i.e. total pass count increases by exactly the number of new
  `@testitem`s this phase added, with zero NEW failures beyond the pre-existing known-false one.
  If the Documenter build itself is runnable in this environment
  (`julia --project=docs docs/make.jl`), run it too and confirm `integer_investment.md` renders
  without error; if the docs environment is not set up for a full site build in this session,
  note that explicitly in the plan's own summary rather than silently skipping it.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/TSO-DSO && grep -n "integer_investment" docs/make.jl</automated>
  </verify>
  <done>docs/make.jl wires integer_investment.jl into both the Literate loop and the Planning pages tree; the full test suite is green (no new regressions beyond the known-false Aqua/CairoMakie drift).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| The literate page's live-executed section | A published Documenter page is public-facing (thesis/paper-adjacent) — any hardcoded literal that silently drifts from the real code would misrepresent the framework's actual behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-16 | Tampering | Literate page's numeric claims | mitigate | Every number printed comes from a live call to `solve_stackelberg!`/`build_master_integer` inside the page itself (Documenter's `@example` blocks execute during `makedocs`) — never a copy-pasted literal from a test file, mirroring `stackelberg_benders.jl`'s own established discipline |
| T-24-17 | Information Disclosure | `using BilevelJuMP`/`HiGHS`/`Ipopt` in a published page | mitigate | Task 1 explicitly forbids these imports on this page (CLAUDE.md's "validation oracle only, test-only dependency" rule) — verified by Task 1's own grep-based check |

No new package installs this plan (RESEARCH.md Package Legitimacy Audit: "Not applicable") — the
SC (supply-chain) row is intentionally omitted.
</threat_model>

<verification>
Task 1's automated check confirms the page exists and imports nothing test-only. Task 2's full
suite run (`julia --project=. -e 'import Pkg; Pkg.test()'`, background) is the phase's own final
acceptance gate — do NOT invoke TestItemRunner directly (documented sibling-worktree hazard,
MEMORY.md).
</verification>

<success_criteria>
- `docs/literate/integer_investment.jl` exists, covers all nine required narrative sections, is
  live-executed with zero hardcoded literals, and imports no test-only solver package.
- `docs/make.jl` wires the new page into both the Literate loop and the `"Planning"` pages tree.
- The full test suite is green: no regressions beyond the project's pre-existing known-false
  Aqua/CairoMakie-drift failure, and every new `@testitem` from plans 24-01 through 24-05 passes.
- All four REQUIREMENTS (INT-01, INT-02, INT-03, INT-04) are addressed by at least one plan in
  this phase (traceable across 24-01 through 24-06).
</success_criteria>

<output>
Create `.planning/phases/24-discrete-integer-investment-expansion/24-06-SUMMARY.md` when done.
</output>
