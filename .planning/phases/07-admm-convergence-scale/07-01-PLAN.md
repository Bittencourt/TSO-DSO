---
phase: 07-admm-convergence-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Project.toml
  - Manifest.toml
  - Manifest-v1.10.toml
  - Manifest-v1.11.toml
  - Manifest-v1.12.toml
  - test/Manifest.toml
  - src/TSODSO.jl
  - src/diagnostics/plots.jl
  - ext/TSODSOMakieExt.jl
  - src/data/ieee123.jl
  - src/admm/residuals.jl
  - test/fixtures_phase7.jl
  - test/test_admm_dualresid.jl
  - test/test_admm_adaptive.jl
  - test/test_ieee123.jl
  - test/test_ieee123_admm.jl
  - test/test_diagnostics_plot.jl
autonomous: true
requirements: [ADMM-02, ADMM-05]
must_haves:
  truths:
    - "The package resolves and `using TSODSO` loads on Julia 1.10/1.11/1.12 with CairoMakie declared only as a weakdep (the core solve never imports Makie)."
    - "AdmmResiduals records primal, dual, ρ, ε_pri, ε_dual and price-gap traces per iteration and stays JuMP-free."
    - "converged(res, ε_pri, ε_dual) is a two-residual predicate (both primal AND dual below their per-unit thresholds)."
    - "A RED @testitem harness exists for dual-residual, adaptive-ρ, transit-node, IEEE-123 fixture, IEEE-123 convergence, and plotting isolation, guarded so the runner never crashes while stubs are unfilled."
  artifacts:
    - path: "src/diagnostics/plots.jl"
      provides: "Exported generic functions plot_convergence / plot_price_convergence (stubs, no CairoMakie import)"
      contains: "function plot_convergence"
    - path: "ext/TSODSOMakieExt.jl"
      provides: "Weakdep extension module scaffold (filled by plan 07-06)"
      contains: "module TSODSOMakieExt"
    - path: "src/data/ieee123.jl"
      provides: "IEEE-123 fixture seam (stub; filled by plan 07-02)"
    - path: "src/admm/residuals.jl"
      provides: "Extended AdmmResiduals ledger + two-residual converged predicate"
      contains: "rho_trace"
    - path: "test/fixtures_phase7.jl"
      provides: "Phase7Fixtures @testmodule (IEEE-123 seeded aggregator population)"
      contains: "@testmodule Phase7Fixtures"
  key_links:
    - from: "src/TSODSO.jl"
      to: "src/diagnostics/plots.jl"
      via: "include after the admm seams"
      pattern: "include\\(\"diagnostics/plots.jl\"\\)"
    - from: "Project.toml"
      to: "ext/TSODSOMakieExt.jl"
      via: "[weakdeps]/[extensions] CairoMakie → TSODSOMakieExt"
      pattern: "TSODSOMakieExt = \"CairoMakie\""
---

## Phase Goal

**As a** PhD researcher, **I want to** run ADMM with correct primal+dual stopping,
per-unit-normalized adaptive ρ, and plottable convergence diagnostics that scale to the
IEEE 123-node voltage case, **so that** the decomposed transactive prices are trustworthy
on the research-target regimes.

> NOTE (MVP mode): the ROADMAP Phase-7 goal is PROSE, not a user story. The story above is a
> faithful reformat (role = researcher) of that goal — no scope invented. Surfaced per the
> user-story template requirement.

<objective>
Lay the single-owner foundation for Phase 7 so the four dependent plans (07-02 fixture, 07-03
subproblem ρ-setter/transit, 07-04 solve_admm, 07-06 plotting) can proceed in parallel without
ever touching a shared file again. This plan owns every shared edit surface: the include graph
(`TSODSO.jl`), the dependency manifests, the diagnostics ledger (`residuals.jl`), and the RED
test harness.

Purpose: Phase 7 hardens the Phase-6 ADMM loop (RESEARCH Summary). Every dependent plan needs
(a) the extended JuMP-free `AdmmResiduals` traces, (b) the wired-in diagnostics/plot + IEEE-123
seams, (c) CairoMakie declared as a weakdep, and (d) failing @testitems that pin the target API.
Doing all shared-surface work here keeps Wave 2 file-disjoint.

Output: CairoMakie weakdep wiring + re-resolved committed manifests; `src/diagnostics/plots.jl`
generic-function stubs + exports; `ext/TSODSOMakieExt.jl` scaffold; `src/data/ieee123.jl` stub;
extended `AdmmResiduals` + two-residual `converged`; `test/fixtures_phase7.jl` @testmodule; and
five RED @testitem files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admm-convergence-scale/07-RESEARCH.md
@.planning/phases/07-admm-convergence-scale/07-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Current AdmmResiduals (src/admm/residuals.jl) — Phase 6 shape, to be EXTENDED: -->
```julia
mutable struct AdmmResiduals
    N::Int; T::Int
    primal_trace::Vector{Float64}
    dual_trace::Vector{Float64}
    iters::Int
end
AdmmResiduals(N::Integer, T::Integer) = ...
record!(res, k, primal_maxabs, dual_maxabs)     # Phase-6 4-arg form
converged(res, tol)                             # Phase-6 primal-only predicate
```
<!-- Existing weakdep-extension pattern (Project.toml + ext/TSODSOGurobiExt.jl): -->
```toml
[weakdeps]
Gurobi = "2e9cd046-0924-5485-92f1-d5272153d98b"
MosekTools = "1ec41992-ff65-5c91-ac43-2df89e9693a4"
[extensions]
TSODSOGurobiExt = "Gurobi"
TSODSOMosekExt = "MosekTools"
```
<!-- Fixture-population builders reused for the IEEE-123 aggregator population: -->
<!-- Phase4Fixtures._house_aggregator(feeder, bus; seed, φ, pv_scale, load_scale, batt_pmax, batt_emax, batt_soc0) -->
<!-- generate_profiles(seed=..., T=...) → (; pv, demand); Aggregator(bus, φ, [devices...], Pdc) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CairoMakie weakdep + manifests + include-graph wiring + diagnostics/plot scaffold</name>
  <read_first>
    - Project.toml (the existing [weakdeps]/[extensions]/[compat] blocks — mirror them exactly)
    - ext/TSODSOGurobiExt.jl AND ext/TSODSOMosekExt.jl (the weakdep-module scaffold to mirror for Makie)
    - src/TSODSO.jl (the include graph; new includes go AFTER the admm/ block)
    - 07-RESEARCH.md Pattern 6 (the TSODSOMakieExt extension + Project.toml wiring) and the
      Package Legitimacy Audit table (CairoMakie approved, registry-verified) and Assumption A1
      (UUID/version confirmed at resolve time, fail-loud if wrong)
  </read_first>
  <action>
    Wire CairoMakie as a WEAKDEP (never a hard [deps] entry — RESEARCH anti-pattern "hard
    CairoMakie dependency"): add `CairoMakie` under `[weakdeps]`, `TSODSOMakieExt = "CairoMakie"`
    under `[extensions]`, and `CairoMakie = "0.15"` under `[compat]` in Project.toml, alongside the
    existing Gurobi/Mosek entries. Confirm the CairoMakie UUID against the LOCAL Julia registry
    before committing (per A1 — a wrong UUID must fail loudly at `Pkg.resolve`, not be trusted from
    the RESEARCH literal). Re-resolve and commit the version-specific manifests
    (Manifest.toml, Manifest-v1.10.toml, Manifest-v1.11.toml, Manifest-v1.12.toml, test/Manifest.toml)
    via `Pkg.resolve` under each Julia version so the pinned env stays reproducible (INFRA-01);
    do NOT install CairoMakie into [deps] (weakdeps are recorded, not force-installed).

    Create `src/diagnostics/plots.jl` holding ONLY the exported generic functions
    `plot_convergence` and `plot_price_convergence` (empty `function ... end` method-less
    declarations + `export`), with NO CairoMakie import — the core-package stubs the extension
    extends (RESEARCH Pattern 6). Create `ext/TSODSOMakieExt.jl` as a scaffold module
    `TSODSOMakieExt` (`using TSODSO, CairoMakie`) with NO method bodies yet (plan 07-06 fills the
    `TSODSO.plot_convergence`/`plot_price_convergence` methods). Create `src/data/ieee123.jl` as a
    comment-only STUB seam (plan 07-02 fills `ieee123_modified`/`build_ieee123`).

    Wire three new `include(...)` lines into `src/TSODSO.jl`: `data/ieee123.jl` in the data block
    (after `data/ieee13.jl`), and `diagnostics/plots.jl` AFTER the `admm/` seams (it consumes
    `AdmmResiduals`). Keep the include order dependency-correct.
  </action>
  <acceptance_criteria>
    Project.toml has CairoMakie under [weakdeps]+[extensions]+[compat]; all five manifests
    re-resolved and committed; `using TSODSO` loads on 1.12 (and resolves on 1.10/1.11 manifests)
    WITHOUT importing CairoMakie; `plot_convergence`/`plot_price_convergence` are exported generic
    functions with no methods; `ext/TSODSOMakieExt.jl` and `src/data/ieee123.jl` exist and are
    included by TSODSO.jl.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.resolve(); using TSODSO; @assert isdefined(TSODSO, :plot_convergence) && isdefined(TSODSO, :plot_price_convergence)'</automated>
  </verify>
  <done>
    Project.toml has CairoMakie under [weakdeps]+[extensions]+[compat]; all five manifests
    re-resolved and committed; `using TSODSO` loads on 1.12 (and resolves on 1.10/1.11 manifests)
    WITHOUT importing CairoMakie; `plot_convergence`/`plot_price_convergence` are exported generic
    functions with no methods; `ext/TSODSOMakieExt.jl` and `src/data/ieee123.jl` exist and are
    included by TSODSO.jl.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AdmmResiduals ledger + two-residual converged predicate (JuMP-free)</name>
  <read_first>
    - src/admm/residuals.jl (the full current struct, record!, converged — the Phase-6 4-arg form)
    - 07-RESEARCH.md Pattern 5 (the extended AdmmResiduals fields), Pattern 2 (the CORRECT dual
      residual s = ρ·Δ(pag_dso), z-block — the trace SEMANTICS this ledger stores), Pattern 3
      (per-unit ε_pri/ε_dual the traces record for plotting threshold lines)
    - src/admm/solve_admm.jl lines 184-199 (the current record! call site the extension must stay
      compatible with — this plan does NOT edit solve_admm.jl; 07-04 does)
  </read_first>
  <behavior>
    - AdmmResiduals gains rho_trace, eps_pri_trace, eps_dual_trace, price_gap_trace Vector{Float64}
      fields (all empty on construction), keeping it a pure JuMP-free data type.
    - record! accepts the new per-iteration quantities (primal 2-norm ‖r‖, dual 2-norm ‖s‖, ρ,
      ε_pri, ε_dual, price_gap) and pushes each to its trace; the sequential-k fail-loud guard is
      preserved; all traces stay equal length == iters.
    - converged(res, ε_pri, ε_dual) returns true iff BOTH last(primal_trace) ≤ ε_pri AND
      last(dual_trace) ≤ ε_dual; false on an empty ledger. (Keep or overload the Phase-6
      converged(res, tol) so no existing caller breaks until 07-04 switches it.)
    - The docstring records that dual_trace now stores ‖s‖ = ρ·‖Δ(pag_dso)‖ (z-block, Boyd) — the
      Phase-6 ρ·Δa diagnostic is superseded.
  </behavior>
  <action>
    Extend the `AdmmResiduals` mutable struct in src/admm/residuals.jl with the four new
    `Vector{Float64}` traces (rho_trace, eps_pri_trace, eps_dual_trace, price_gap_trace) per
    RESEARCH Pattern 5; update both inner constructors to initialize them empty. Update `record!`
    to the extended arity (accept ρ, ε_pri, ε_dual, price_gap alongside the primal/dual norms),
    keeping the `k == res.iters + 1` sequential guard and the non-negativity of the residual
    magnitudes. Add the two-residual `converged(res, ε_pri, ε_dual)` method (both residuals below
    threshold, empty ⇒ false); retain a Phase-6-compatible `converged(res, tol)` so the current
    solve_admm still compiles until 07-04 lands. Keep the module JuMP-free (no `using JuMP`) — the
    hard reuse contract (Phase 8 harness + the plot extension consume it without a solver). Update
    the file's export list and docstrings to state dual_trace now stores the Boyd z-block residual
    ‖s‖ = ρ·‖Δ(pag_dso)‖ (Pattern 2).
  </action>
  <acceptance_criteria>
    AdmmResiduals has rho_trace/eps_pri_trace/eps_dual_trace/price_gap_trace; record! pushes them
    with all traces length-consistent; converged(res, ε_pri, ε_dual) is a two-residual predicate;
    residuals.jl imports no JuMP; the ledger @testitems (Task 3 harness) that don't depend on
    solve_admm go green for the ledger behavior.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("resid", ti.name) || occursin("diag", ti.name))'</automated>
  </verify>
  <done>
    AdmmResiduals has rho_trace/eps_pri_trace/eps_dual_trace/price_gap_trace; record! pushes them
    with all traces length-consistent; converged(res, ε_pri, ε_dual) is a two-residual predicate;
    residuals.jl imports no JuMP; the ledger @testitems (Task 3 harness) that don't depend on
    solve_admm go green for the ledger behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: RED @testitem harness + Phase7Fixtures @testmodule (IEEE-123 aggregator population)</name>
  <read_first>
    - test/test_admm.jl (the RED-then-green isdefined-guard pattern to mirror; the crossval item
      shape) and test/fixtures_phase6.jl + test/fixtures_phase4.jl (the @testmodule + seeded
      _house_aggregator / build_ieee13_ground_aggregators population pattern to mirror)
    - test/runtests.jl (@run_package_tests discovery) and 07-VALIDATION.md Per-Task Verification
      Map (the required filter substrings: dual, admm, rho, adaptive, transit, dso, ieee123,
      crossval, diag, resid, plot, makie)
    - 07-RESEARCH.md §"Wave 0 Gaps" (the exact test-file list) and Open Q2 (85 load nodes ⇒ seeded
      per-node aggregators, NOT full thesis device detail — reuse Phase4 builders)
  </read_first>
  <action>
    Create `test/fixtures_phase7.jl` as a `@testmodule Phase7Fixtures` that DEFINES ONLY (no
    top-level call to any not-yet-filled seam — threat T-06-01 contract): a `build_ieee123` /
    `ieee123_modified` aggregator population helper that takes the `feeder` as an argument and
    returns one seeded aggregator per LOAD node (reusing the Phase4 `_house_aggregator` shape via
    `generate_profiles`, StableRNGs-seeded ⇒ reproducible), sized to keep the voltage-constrained
    case feasible and voltage-binding (Open Q2); plus the pinned λ₀ profile and the adaptive-ρ
    config constants (ε_abs, ε_rel, τ, μ, ρ_min, ρ_max, ρ₀). Do NOT call the fixture feeder builder
    at module top level.

    Create five RED @testitem files, each with every behavioral assertion BEHIND an
    `isdefined`/`hasmethod` guard so the runner never crashes while the Wave-2/3 stubs are unfilled
    (mirror test_admm.jl). Item names MUST contain the VALIDATION filter substrings:
    - `test/test_admm_dualresid.jl` — items named with "dualresid"/"admm": the corrected Boyd dual
      residual s = ρ·Δ(pag_dso) and the primal+dual 2-norm per-unit two-residual STOP (ADMM-02).
    - `test/test_admm_adaptive.jl` — items named with "adaptive"/"rho": set_rho! quadratic-coeff
      update, the build-once-survives-ρ-change assertion (num_variables/num_constraints invariant
      across a ρ change), scale-invariant convergence (same ε/τ/μ on 2-bus AND IEEE-13); plus a
      "transit"/"dso" item for the transit-node relaxation (ADMM-02).
    - `test/test_ieee123.jl` — items named with "ieee123": fixture constructs (radial, magnitude
      bands, contiguous ids, root correct, edges == N−1) — will go green in 07-02.
    - `test/test_ieee123_admm.jl` — items named with "ieee123"/"crossval": IEEE-123 ADMM converges
      in ~tens of iters, λ_j → DADP, PF-04 exact at convergence — the failing END-TO-END test (MVP
      first deliverable) that 07-05 greens.
    - `test/test_diagnostics_plot.jl` — items named with "plot"/"makie": plot_convergence /
      plot_price_convergence have NO applicable method for AdmmResiduals without CairoMakie (core
      suite stays plot-free), and the ledger records the traces the plots consume (ADMM-05).
    Register the new fixture module and test files with the runner so discovery picks them up.
  </action>
  <acceptance_criteria>
    Phase7Fixtures @testmodule loads (defines-only, no top-level seam call); all five test files are
    discovered; every behavioral assertion sits behind an isdefined/hasmethod guard so the runner
    reports RED assertions (not a crash); item names carry the required filter substrings; the
    full suite still runs without an infrastructure crash.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("dualresid",ti.name)||occursin("adaptive",ti.name)||occursin("ieee123",ti.name)||occursin("plot",ti.name)||occursin("transit",ti.name))' ; echo "harness ran (RED items expected until Waves 2-4)"</automated>
  </verify>
  <done>
    Phase7Fixtures @testmodule loads (defines-only, no top-level seam call); all five test files are
    discovered; every behavioral assertion sits behind an isdefined/hasmethod guard so the runner
    reports RED assertions (not a crash); item names carry the required filter substrings; the
    full suite still runs without an infrastructure crash.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new dependency → pinned env | CairoMakie enters Project.toml/Manifest (supply-chain surface) |
| core solve → viz stack | the headless CI + core solve must never import Makie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | CairoMakie leaking into the core solve / headless CI | mitigate | CairoMakie declared ONLY under [weakdeps]; core `using TSODSO` verified to load without Makie (Task 1 verify); plot fns are method-less stubs in core |
| T-07-02 | Tampering | dual_trace stores the wrong (Phase-6 ρ·Δa) semantics | mitigate | Ledger docstring + Pattern-2 semantics pinned; 07-04 fills the correct z-block value; dualresid @testitem asserts it |
| T-07-SC | Tampering | Supply-chain (new plotting dep) | accept | CairoMakie/Makie are the canonical Julia viz packages (Makie org, registry-verified per RESEARCH Package Legitimacy Audit); pinned in the committed Manifest (INFRA-01); UUID confirmed against local registry at resolve (A1), fail-loud on mismatch |
</threat_model>

<verification>
- `julia --project=. -e 'import Pkg; Pkg.resolve(); using TSODSO'` loads clean, no CairoMakie import.
- residuals.jl carries no `using JuMP`; `converged(res, ε_pri, ε_dual)` exists.
- All five RED test files discovered; runner does not crash; names carry the filter substrings.
</verification>

<success_criteria>
- CairoMakie is a weakdep with re-resolved committed manifests (1.10/1.11/1.12 + test).
- AdmmResiduals extended (rho/eps/price traces) + two-residual converged predicate, JuMP-free.
- diagnostics/plots.jl stubs + ext scaffold + ieee123.jl stub wired into TSODSO.jl.
- RED harness + Phase7Fixtures in place; downstream plans have file-disjoint surfaces.
</success_criteria>

<output>
Create `.planning/phases/07-admm-convergence-scale/07-01-SUMMARY.md` when done.
</output>
