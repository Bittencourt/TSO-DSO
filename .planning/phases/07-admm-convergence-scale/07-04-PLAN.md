---
phase: 07-admm-convergence-scale
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-03]
files_modified:
  - src/admm/solve_admm.jl
  - test/test_admm_dualresid.jl
  - test/test_admm_adaptive.jl
autonomous: true
requirements: [ADMM-02]
must_haves:
  truths:
    - "solve_admm computes the Boyd dual residual s = ρ·‖Δ(pag_dso)‖ (the z-block change, NOT ρ·Δa) and stops only when BOTH the primal 2-norm ≤ ε_pri AND the dual 2-norm ≤ ε_dual."
    - "Stopping tolerances are per-unit-normalized (ε_pri = √p·ε_abs + ε_rel·max(‖a‖,‖pag_dso‖); ε_dual = √n·ε_abs + ε_rel·‖λ‖)."
    - "ρ adapts by residual balancing (τ·ρ when ‖r‖ > μ‖s‖, ρ/τ when ‖s‖ > μ‖r‖), clamped to [ρ_min, ρ_max] and frozen once both residuals are within ~10× tolerance; on a ρ change set_rho! updates both subproblems' quadratic penalty (no rebuild)."
    - "The same (ε_abs, ε_rel, τ, μ, ρ_min, ρ_max) converge BOTH the 2-bus and the IEEE-13 case (scale-invariance); the Phase-6 centralized cross-validation still passes; hitting the iteration cap still fails loudly."
  artifacts:
    - path: "src/admm/solve_admm.jl"
      provides: "Adaptive-ρ, two-residual-stopping ADMM loop (Boyd §3.3/§3.4.1)"
      contains: "pag_dso_prev"
  key_links:
    - from: "src/admm/solve_admm.jl"
      to: "set_rho!(dso, ρ) / set_rho!(agr, ρ)"
      via: "residual-balancing ρ change on both subproblems"
      pattern: "set_rho!"
    - from: "src/admm/solve_admm.jl"
      to: "AdmmResiduals two-residual converged"
      via: "record! extended traces + converged(res, ε_pri, ε_dual)"
      pattern: "converged\\(residuals"
---

## Phase Goal

**As a** PhD researcher, **I want to** run ADMM with correct primal+dual stopping,
per-unit-normalized adaptive ρ, and plottable convergence diagnostics that scale to the
IEEE 123-node voltage case, **so that** the decomposed transactive prices are trustworthy
on the research-target regimes.

<objective>
Harden the Phase-6 `solve_admm` loop in place (do NOT rewrite — RESEARCH Primary recommendation):
add the correct Boyd dual residual, per-unit two-residual stopping, and residual-balancing adaptive
ρ that drives `set_rho!` (07-03) on both subproblems. This is the thinnest vertical slice that
makes IEEE-13 converge end-to-end with adaptive ρ and a correct optimality test — the MVP
regression milestone before scaling to IEEE-123 (07-05).

Purpose: Success Criterion 1 (stops on both residuals with per-unit adaptive ρ, no hard-coded
scale-specific penalty, fail-loud cap). Fixes the two most important correctness bugs: the
z-block dual residual (Pitfall 2, false-convergence) and the missing quadratic-penalty ρ update
(Pitfall 1), while removing the Phase-6 fixture-empirical fixed ρ.

Output: the extended `solve_admm`; green dualresid + adaptive @testitems; Phase-6 crossval
regression intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-admm-convergence-scale/07-RESEARCH.md
@src/admm/solve_admm.jl
@src/admm/residuals.jl
@CLAUDE.md

<interfaces>
<!-- Consumed from 07-03 (subproblem ρ-setters, build-once preserved): -->
```julia
set_rho!(agr::AgrOpt, ρ::Real)   # sets pag[t]² diagonal coeff to −0.5ρ, no rebuild
set_rho!(dso::DsoOpt, ρ::Real)   # sets pag_dso[j,t]² diagonal coeff to +0.5ρ, no rebuild
```
<!-- Consumed from 07-01 (extended ledger): -->
```julia
record!(res, k, ‖r‖₂, ‖s‖₂, ρ, ε_pri, ε_dual, price_gap)   # extended arity
converged(res, ε_pri, ε_dual)                              # two-residual predicate
```
<!-- Existing loop internals to modify (solve_admm.jl): a_prev/dual_maxabs (ρ·Δa) → pag_dso_prev/‖s‖;
     primal_maxabs (∞-norm) → 2-norm ‖r‖; λ dict; c dict; agr_by_bus; load_nodes; ρf. -->
```julia
# Boyd §3.3 / §3.4.1 (thesis App. B.30-B.32, UNSCALED — do NOT rescale λ on a ρ change):
#   r = ‖a − pag_dso‖₂ ; s = ρ·‖pag_dso − pag_dso_prev‖₂ ; p = n = length(load_nodes)*T
#   ε_pri = √p·ε_abs + ε_rel·max(‖a‖₂, ‖pag_dso‖₂) ; ε_dual = √n·ε_abs + ε_rel·‖λ‖₂
#   balancing τ=2, μ=10 ; clamp [ρ_min,ρ_max] ; freeze once r ≤ 10ε_pri AND s ≤ 10ε_dual
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct Boyd dual residual + per-unit two-residual stopping</name>
  <read_first>
    - src/admm/solve_admm.jl lines 147-210 (the ADMM state: a_prev/dual_maxabs ρ·Δa diagnostic, the
      ∞-norm primal_maxabs, the record!/converged call, the dual ascent + c refresh) and 254-266
      (the DADP sign/return)
    - src/admm/residuals.jl (the extended record!/converged from 07-01)
    - 07-RESEARCH.md Pattern 2 (s = ρ·Δ(pag_dso), the z-block, store pag_dso_prev exactly as a_prev
      is stored; Phase-6 tracked ρ·Δa — the wrong block), Pattern 3 (the per-unit 2-norm ε_pri/ε_dual,
      p = n = n_load_nodes·T, ε_abs≈1e-4, ε_rel≈1e-3), Pitfall 2 (primal-only stop = false
      convergence), Pitfall 3 (get the dual-residual ρ scale right), the "extended stopping" Code
      Example sketch
  </read_first>
  <behavior>
    - The loop maintains pag_dso_prev (a JuMP-free Dict/array) and computes s = ρ·‖pag_dso −
      pag_dso_prev‖₂ (2-norm over all (j,t)); the primal residual is the 2-norm ‖a − pag_dso‖₂.
    - ε_pri and ε_dual are computed per iteration from the per-unit formula and recorded to the
      ledger alongside ρ and a price_gap (‖λ^k − λ^{k−1}‖₂).
    - The loop stops iff BOTH ‖r‖ ≤ ε_pri AND ‖s‖ ≤ ε_dual (converged(res, ε_pri, ε_dual)); a
      primal-only-satisfied-but-dual-not iterate does NOT stop (a dualresid @testitem pins this).
    - A hand-computed 2-bus iterate reproduces the expected s and ε_dual (Pitfall 3 scale check).
    - Hitting maxiter without both residuals converged still THROWS loudly.
  </behavior>
  <action>
    In src/admm/solve_admm.jl, replace the Phase-6 primal-only ∞-norm stopping with the Boyd
    two-residual per-unit form (Patterns 2/3). Store `pag_dso_prev` (initialized to zeros, updated
    each iteration exactly as `a_prev` is today) and compute the DUAL residual as
    `s = ρ·norm2(pag_dso − pag_dso_prev)` (the z-block — retire the ρ·Δa diagnostic). Compute the
    PRIMAL residual as the 2-norm `norm2(a − pag_dso)`. Compute `p = n = length(load_nodes)*T` and
    `ε_pri = √p·ε_abs + ε_rel·max(norm2(a), norm2(pag_dso))`, `ε_dual = √n·ε_abs + ε_rel·norm2(λ)`
    with ε_abs, ε_rel as keyword defaults (≈1e-4, 1e-3). record! the extended tuple (‖r‖, ‖s‖, ρ,
    ε_pri, ε_dual, price_gap). Switch the stop to `converged(residuals, ε_pri, ε_dual)` (both
    residuals). Keep the UNSCALED dual ascent `λ ← λ + ρ·r` and the `c = −pag_dso` refresh, and do
    NOT rescale λ when ρ changes (Pattern 4 note; only the scaled form would). Preserve the
    fail-loud maxiter cap and the converged final consolidation pass (PF-04 + App. C gates) verbatim.
  </action>
  <acceptance_criteria>
    solve_admm stores pag_dso_prev, computes s = ρ·‖Δ(pag_dso)‖ and the primal 2-norm, per-unit
    ε_pri/ε_dual, records the extended traces, and stops only on both residuals; a hand-computed
    2-bus iterate matches; the "dualresid" @testitems are GREEN; the fail-loud cap still throws.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("dualresid", ti.name))'</automated>
  </verify>
  <done>
    solve_admm stores pag_dso_prev, computes s = ρ·‖Δ(pag_dso)‖ and the primal 2-norm, per-unit
    ε_pri/ε_dual, records the extended traces, and stops only on both residuals; a hand-computed
    2-bus iterate matches; the "dualresid" @testitems are GREEN; the fail-loud cap still throws.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Residual-balancing adaptive ρ (clamp + freeze) driving set_rho!</name>
  <read_first>
    - src/admm/solve_admm.jl (the loop after Task 1; the `ρf` handle; agr_by_bus + dso built once)
    - src/admm/AgrOpt.jl + src/admm/DsoOpt.jl set_rho! (from 07-03 — the quadratic-coeff updater to
      call on a ρ change)
    - 07-RESEARCH.md Pattern 4 (the balancing schedule τ=2, μ=10; clamp [ρ_min≈1e-2, ρ_max≈1e4];
      freeze once r ≤ 10ε_pri AND s ≤ 10ε_dual; UNSCALED-λ note — do not rescale λ), Pattern 1
      ("call set_rho! ONLY when ρ actually changed", guard ρ_new != ρ_old, convexity ρ>0), Pitfall 6
      (oscillation/ill-conditioning: clamp + freeze + keep τ,μ; α=1 no over-relaxation), Pitfall 1
      (update BOTH subproblems' quadratic coeff in lockstep with the linear update), the State of the
      Art row (fixed ρ 5/100 → adaptive)
  </read_first>
  <behavior>
    - When ‖r‖ > μ‖s‖ the loop multiplies ρ by τ; when ‖s‖ > μ‖r‖ it divides ρ by τ; otherwise ρ is
      unchanged. ρ is clamped to [ρ_min, ρ_max].
    - On a ρ change (ρ_new != ρ_old only) the loop calls set_rho!(dso, ρ) and set_rho!(agr_by_bus[j], ρ)
      for every load node, and uses the new ρ in the subsequent dual ascent — the penalty and the
      ascent step use the SAME ρ (Pitfall 1).
    - Adaptation freezes once both residuals are within ~10× tolerance (a ρ_frozen flag), and stays
      frozen thereafter.
    - The SAME (ε_abs, ε_rel, τ, μ, ρ_min, ρ_max) defaults converge BOTH the 2-bus and IEEE-13 cases
      (scale-invariance) — no per-fixture ρ needed. λ is never rescaled on a ρ change.
    - The Phase-6 centralized cross-validation (test_admm.jl 2-bus + IEEE-13 crossval items) still
      passes (welfare + DADP match centralized); num_variables/num_constraints stay iteration-
      independent even across ρ changes (build-once survives).
  </behavior>
  <action>
    Extend solve_admm's keyword signature with the adaptive-ρ config (ε_abs, ε_rel, τ=2.0, μ=10.0,
    ρ_min, ρ_max, and treat the existing `ρ` keyword as the INITIAL ρ₀ so all Phase-6 call sites and
    the crossval tests keep working). After the dual ascent each iteration, apply the balancing rule
    (Pattern 4): `ρ_new = ‖r‖ > μ‖s‖ ? τ·ρ : (‖s‖ > μ‖r‖ ? ρ/τ : ρ)`, then `clamp(ρ_new, ρ_min,
    ρ_max)`; if `ρ_new != ρ` update ρ and call `set_rho!(dso, ρ)` + `set_rho!(agr_by_bus[j], ρ)` for
    every load node (Pattern 1 guard). Maintain a `ρ_frozen` flag: once `‖r‖ ≤ 10ε_pri && ‖s‖ ≤
    10ε_dual`, freeze adaptation (Boyd convergence theory). Keep α=1 (no over-relaxation — out of
    MVP scope, Pitfall 6). Do NOT rescale λ on a ρ change (unscaled form). Ensure the loop uses the
    live ρ consistently in solve_agr!/solve_dso!, the dual ascent, and set_rho!. Update the
    solve_admm docstring to describe the adaptive-ρ policy and the retired fixed-ρ behavior.
  </action>
  <acceptance_criteria>
    solve_admm adapts ρ by residual balancing (τ=2, μ=10) with clamp + freeze, calls set_rho! on
    both subproblems only on a ρ change, never rescales λ; the SAME config converges 2-bus AND
    IEEE-13 (scale-invariant); the Phase-6 crossval regression is still GREEN; build-once survives ρ
    changes; the "adaptive"/"rho" @testitems are GREEN.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.run_tests(joinpath(pwd(),"test"); filter=ti->occursin("adaptive", ti.name) || occursin("rho", ti.name) || occursin("crossval", ti.name))'</automated>
  </verify>
  <done>
    solve_admm adapts ρ by residual balancing (τ=2, μ=10) with clamp + freeze, calls set_rho! on
    both subproblems only on a ρ change, never rescales λ; the SAME config converges 2-bus AND
    IEEE-13 (scale-invariant); the Phase-6 crossval regression is still GREEN; build-once survives ρ
    changes; the "adaptive"/"rho" @testitems are GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iterate → stop decision | a wrong residual/threshold flips convergence |
| ρ policy → subproblem penalty | penalty and dual step must share the same ρ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Repudiation | False convergence from the wrong-block dual residual (ρ·Δa) or primal-only stop | mitigate | Switch to s = ρ·Δ(pag_dso) (z-block) + two-residual stop; a dualresid @testitem asserts a primal-satisfied/dual-unsatisfied iterate does NOT stop; centralized cross-validation is the net (Pitfall 2) |
| T-07-10 | Tampering | Penalty ρ and ascent ρ diverge (quadratic coeff stale on a ρ change) | mitigate | set_rho! called in lockstep with the linear update on a ρ change; crossval welfare/DADP match asserted; build-once-survives-ρ-change (Pitfall 1) |
| T-07-11 | Denial of service | Adaptive-ρ oscillation / SOCP ill-conditioning ratchets ρ or limit-cycles | mitigate | clamp [ρ_min,ρ_max] + freeze once within ~10× tol + fixed τ=2/μ=10, α=1; fail-loud cap still throws on non-convergence (Pitfall 6) |
| T-07-12 | Tampering | λ incorrectly rescaled on a ρ change corrupts the price trajectory | mitigate | Unscaled form kept (λ ← λ + ρ·r); explicit no-rescale per Pattern 4; DADP cross-validated against centralized |
</threat_model>

<verification>
- Dual residual is the z-block s = ρ·‖Δ(pag_dso)‖; stop requires both residuals below per-unit ε.
- Same config converges 2-bus AND IEEE-13; Phase-6 crossval still green; fail-loud cap intact.
- ρ clamps + freezes; set_rho! called only on a change; build-once survives ρ changes.
</verification>

<success_criteria>
- Two-residual per-unit stopping + residual-balancing adaptive ρ (no hard-coded scale-specific ρ).
- Phase-6 centralized cross-validation regression passes; fail-loud maxiter preserved.
</success_criteria>

<output>
Create `.planning/phases/07-admm-convergence-scale/07-04-SUMMARY.md` when done.
</output>
