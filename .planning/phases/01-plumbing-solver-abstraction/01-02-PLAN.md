---
phase: 01-plumbing-solver-abstraction
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/units/PerUnit.jl
  - src/data/Feeder.jl
  - src/data/topology.jl
autonomous: true
requirements: [DATA-01, DATA-02, INFRA-05]
must_haves:
  truths:
    - "A researcher builds a radial feeder from immutable, JuMP-free per-unit structs"
    - "A non-tree feeder (wrong branch count, disconnected, or not exactly one root) raises a clear error at construction"
    - "Magnitude-sanity assertions fire on out-of-band per-unit electrical/monetary quantities"
    - "SI inputs are converted to per-unit once, at ingestion, before struct construction"
  artifacts:
    - path: "src/units/PerUnit.jl"
      provides: "PerUnitBase{T}, to_pu_* conversions, assert_magnitudes"
      contains: "struct PerUnitBase"
    - path: "src/data/Feeder.jl"
      provides: "immutable Bus{T}, Branch{T}, Feeder{T} + validating constructor"
      contains: "struct Feeder"
    - path: "src/data/topology.jl"
      provides: "sparse incidence + assert_radial tree check"
      contains: "function assert_radial"
  key_links:
    - from: "src/data/Feeder.jl"
      to: "assert_radial"
      via: "inner/outer constructor calls validation before freezing"
      pattern: "assert_radial"
    - from: "src/units/PerUnit.jl"
      to: "Feeder"
      via: "assert_magnitudes(feeder) magnitude tripwires"
      pattern: "assert_magnitudes"
---

<objective>
Deliver the JuMP-free data keystone: an immutable, concretely-typed feeder model whose construction is a
validation invariant, and a per-unit system that converts SI inputs once at ingestion with loud
magnitude tripwires. Fills the `units/` and `data/` seam stubs created in plan 01-01, driving the
`perunit`, `feeder`, and `topology` Wave 0 @testitems green.

Purpose: this is the data half of the walking skeleton — everything downstream (the toy solve, and
every later phase's power flow) consumes these frozen structs. Correctness of the radial invariant and
the per-unit discipline is the whole deliverable (DATA-01, DATA-02, INFRA-05).
Output: `src/units/PerUnit.jl`, `src/data/Feeder.jl`, `src/data/topology.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-plumbing-solver-abstraction/01-RESEARCH.md
@.planning/phases/01-plumbing-solver-abstraction/SKELETON.md

<interfaces>
<!-- Target contracts (RESEARCH Patterns 4 & 5). Fill the empty stubs from plan 01-01; add `export`s here. -->

src/units/PerUnit.jl:
  struct PerUnitBase{T<:Real} with fields S_base (MVA), V_base (kV)
  Z_base(::PerUnitBase), I_base(::PerUnitBase)
  to_pu_power(x_MW, ::PerUnitBase), to_pu_impedance(z_Ω, ::PerUnitBase)
  assert_magnitudes(::Feeder)   # @assert voltage ∈ [0.8,1.2], impedance <5, 0<smax<100

src/data/Feeder.jl:
  struct Bus{T<:Real}    fields: id::Int, vmin::T, vmax::T, is_root::Bool
  struct Branch{T<:Real} fields: from::Int, to::Int, r::T, x::T, smax::T
  struct Feeder{T<:Real} fields: buses::Vector{Bus{T}}, branches::Vector{Branch{T}}, root::Int
  Feeder(buses, branches, root) outer constructor calls assert_radial then returns Feeder{T}

src/data/topology.jl:
  assert_radial(buses, branches, root) -> sparse incidence A (throws ArgumentError on non-tree)
  Theorem: edges==nodes-1 ∧ BFS-connected-from-root ∧ exactly one is_root ⟺ radial tree
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-unit system with convert-once ingestion + magnitude tripwires (INFRA-05)</name>
  <files>src/units/PerUnit.jl, test/test_perunit.jl</files>
  <read_first>
    - .planning/phases/01-plumbing-solver-abstraction/01-RESEARCH.md (§Pattern 5 Per-unit system, §Common Pitfalls 5, §Assumptions A2/A4 magnitude bands)
    - src/units/PerUnit.jl (the empty stub from plan 01-01)
    - test/test_perunit.jl (the red Wave 0 @testitem to satisfy)
  </read_first>
  <behavior>
    - `PerUnitBase(S_base, V_base)` constructs; `Z_base` returns `V_base^2/S_base`, `I_base` returns `S_base/(sqrt(3)*V_base)`.
    - `to_pu_power(x_MW, base)` and `to_pu_impedance(z_Ω, base)` return `x_MW/S_base` and `z_Ω/Z_base(base)`.
    - `assert_magnitudes(feeder)` passes for an in-band feeder (voltage ∈ [0.8,1.2], per-unit impedance <5, 0<smax<100).
    - `assert_magnitudes` throws `AssertionError` for a feeder with voltage bounds outside [0.8,1.2] or an implausible impedance/limit.
  </behavior>
  <action>
    Fill `src/units/PerUnit.jl` with `struct PerUnitBase{T<:Real}` (fields `S_base`, `V_base`), the
    `Z_base`/`I_base` helpers, and `to_pu_power`/`to_pu_impedance` convert-at-ingestion helpers per
    RESEARCH Pattern 5. Implement `assert_magnitudes(f::Feeder)` with the documented sanity bands
    (voltage ∈ [0.8,1.2] pu, `0 ≤ r,x < 5`, `0 < smax < 100`) as loud `@assert` tripwires against
    SI/pu mixing (Pitfall 5), each with a message naming the offending bus/branch. Keep prices in $/MWh
    with a magnitude band. `export PerUnitBase, Z_base, I_base, to_pu_power, to_pu_impedance,
    assert_magnitudes`. Add a doc comment stating the chosen documented base (placeholder
    `S_base=1.0 MVA`, `V_base=4.16 kV` per RESEARCH Open-Question 3 / A4 — superseded by real fixtures
    in Phase 4). Do NOT call `to_pu_*` inside any model builder — conversion lives here at ingestion only.
    Turn the `test_perunit` @testitem(s) green, including a `@test_throws AssertionError` for out-of-band input.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("perunit", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - `src/units/PerUnit.jl` contains `struct PerUnitBase` and `function assert_magnitudes`.
    - `@run_package_tests filter=ti->occursin("perunit", ti.name)` passes (green).
    - The suite includes a `@test_throws AssertionError` proving magnitude tripwires fire on out-of-band input.
    - `PerUnit.jl` exports `PerUnitBase`, `to_pu_power`, `to_pu_impedance`, `assert_magnitudes`.
    - No `to_pu_*` call appears in any `src/models/` file.
  </acceptance_criteria>
  <done>Per-unit base, convert-once helpers, and magnitude assertions implemented; perunit @testitem green including the out-of-band throw.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Immutable feeder structs + radial (tree) validation (DATA-01, DATA-02)</name>
  <files>src/data/Feeder.jl, src/data/topology.jl, test/test_feeder.jl, test/test_topology.jl</files>
  <read_first>
    - .planning/phases/01-plumbing-solver-abstraction/01-RESEARCH.md (§Pattern 4 immutable feeder + radial validation, §Don't Hand-Roll graph connectivity BFS)
    - src/data/Feeder.jl, src/data/topology.jl (empty stubs from plan 01-01)
    - test/test_feeder.jl, test/test_topology.jl (red Wave 0 @testitems)
  </read_first>
  <behavior>
    - A valid radial feeder (N buses, N-1 branches, connected from root, exactly one `is_root`) constructs successfully.
    - A feeder with the wrong branch count (≠ N-1) throws `ArgumentError` naming the count mismatch.
    - A disconnected feeder (right edge count but not reachable from root) throws `ArgumentError` naming reachable/total.
    - A feeder with zero or >1 `is_root` buses throws `ArgumentError`.
    - Constructing a feeder whose voltage/impedance/limit magnitudes are out-of-band throws (magnitude tripwire fires ON THE LIVE construction path, not only in a standalone test).
    - `Bus`, `Branch`, `Feeder` are immutable (mutating a field errors).
  </behavior>
  <action>
    Fill `src/data/Feeder.jl` with immutable, concretely-typed parametrized structs `Bus{T<:Real}`,
    `Branch{T<:Real}`, `Feeder{T<:Real}` (fields per <interfaces>). Add the outer constructor
    `Feeder(buses, branches, root)` that builds the inner `Feeder{T}`, then calls
    `assert_radial(buses, branches, root)` AND `assert_magnitudes(feeder)` BEFORE returning it —
    both validations are construction invariants (radial topology + per-unit magnitude sanity), so the
    walking-skeleton solve path in plan 01-04 exercises criterion #5 live, not just in a unit test; never
    mutate after construction. (`assert_magnitudes` is defined in `src/units/PerUnit.jl` from Task 1, which
    is included before `Feeder.jl` in `TSODSO.jl`.) Fill
    `src/data/topology.jl` with `assert_radial` per RESEARCH Pattern 4: check `B == N-1` (throw
    `ArgumentError` naming counts), build a sparse node-branch incidence via `SparseArrays.sparse`, run a
    ~15-line BFS from `root` over an adjacency list to assert full connectivity (throw naming
    `reachable/total`), and assert `sum(is_root)==1` (throw). Return the incidence matrix for reuse by the
    model layer. Use the theorem `edges==nodes-1 ∧ connected ⟺ tree` — no cycle detection, no Graphs.jl
    dependency. `export Bus, Branch, Feeder` from Feeder.jl and `assert_radial` from topology.jl. Drive the
    `feeder` and `topology` @testitems green, including `@test_throws ArgumentError` for each non-tree case.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->(occursin("feeder", ti.name) || occursin("topology", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/Feeder.jl` contains `struct Feeder` (immutable) and a `Feeder(buses, branches, root)` constructor calling BOTH `assert_radial` and `assert_magnitudes`.
    - `src/data/topology.jl` contains `function assert_radial` using `SparseArrays.sparse` and a BFS connectivity check.
    - `@run_package_tests filter=ti->(occursin("feeder", ti.name) || occursin("topology", ti.name))` passes.
    - The topology suite includes `@test_throws ArgumentError` for: wrong branch count, disconnected graph, and ≠1 root.
    - The feeder suite includes a `@test_throws` proving `Feeder(...)` construction rejects an out-of-band magnitude feeder (assert_magnitudes on the live path).
    - No `Graphs` dependency is added to `Project.toml`.
  </acceptance_criteria>
  <done>Immutable feeder structs construct only when radial; non-tree feeders throw clear ArgumentErrors; feeder + topology @testitems green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| researcher input → struct construction | Feeder/base data is untrusted: may be non-radial, mis-scaled (SI vs pu), or absurd magnitude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Malformed/adversarial feeder (non-tree, disconnected, multi-root) → silent-wrong downstream results | mitigate | `assert_radial` runs at construction: edge-count + BFS connectivity + single-root checks throw `ArgumentError` before any `Feeder` exists (DATA-02) |
| T-01-03 | Tampering | Mis-scaled input (Ω where pu expected, out-of-band voltage) → numerically plausible but wrong | mitigate | `assert_magnitudes` tripwires (voltage ∈ [0.8,1.2], impedance <5, 0<smax<100) fire loudly; conversion confined to `units/` at ingestion (INFRA-05, Pitfall 5) |
| T-01-05 | Denial of Service | Absurd-magnitude input propagating into later solver blowup | accept | Magnitude bands bound electrical/monetary inputs at ingestion; anything past the band fails loudly rather than reaching a solver |
</threat_model>

<verification>
- `@run_package_tests filter=ti->occursin("perunit", ti.name)` green (incl. out-of-band `@test_throws`).
- `@run_package_tests filter=ti->(occursin("feeder", ti.name) || occursin("topology", ti.name))` green (incl. non-tree `@test_throws`).
- Structs are immutable (no mutating setter compiles).
</verification>

<success_criteria>
- Radial feeder builds from immutable JuMP-free structs; non-tree raises a clear error (DATA-01, DATA-02).
- One documented per-unit system; conversion once at ingestion; magnitude assertions guard scale mistakes (INFRA-05).
</success_criteria>

<output>
Create `.planning/phases/01-plumbing-solver-abstraction/01-02-SUMMARY.md` when done.
</output>
