---
phase: 09-documentation-regression-acceptance-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_acceptance.jl
  - test/test_pricing_fit.jl
autonomous: true
requirements: [EXP-04]

must_haves:
  truths:
    - "Running the full test suite (Pkg.test()) exercises a single consolidated acceptance gate that reproduces IEEE-13 congestion (exact relaxation + recovered DADP + ADMM approx centralized) and IEEE-123 voltage (exact relaxation + recovered DADP + ADMM approx centralized) end-to-end."
    - "The thesis +$1819/+25% welfare headline appears only as a NON-FAILING @info + broken cross-check inside the acceptance gate — never a hard-failing assertion."
    - "The FIT-vs-DADP welfare ratio is pinned as a regression golden in test/test_pricing_fit.jl, extending (not duplicating) the existing FIT-focused test file."
  artifacts:
    - path: "test/test_acceptance.jl"
      provides: "SC3 consolidated E2E acceptance gate: IEEE-13 congestion + IEEE-123 voltage testitems"
      contains: "tags = [\n    :acceptance,\n]"
    - path: "test/test_pricing_fit.jl"
      provides: "Extended FIT regression golden (extends existing @testitem, does not add a new storage tier)"
  key_links:
    - from: "test/test_acceptance.jl"
      to: "src/models/oracle.jl operational_oracle + src/admm/solve_admm.jl solve_admm + src/pricing/dlmp.jl extract_dlmp"
      via: "setup = [Phase4Fixtures] / setup = [Phase7Fixtures], calling the real entrypoints"
      pattern: "operational_oracle\\(|solve_admm\\("
    - from: "test/test_pricing_fit.jl"
      to: "src/pricing/fit.jl fit_baseline"
      via: "pinned inline golden constant + rtol assertion (existing project convention)"
      pattern: "fit_baseline\\("
---

<objective>
Close EXP-04 and the SC3 v1 acceptance-gate success criterion: a single consolidated
`test/test_acceptance.jl` `@testitem` file that reproduces BOTH headline cases
(IEEE-13 congestion, IEEE-123 voltage) end-to-end — exact SOC relaxation, recovered DADP,
and ADMM matching the centralized optimum — reusing the ALREADY-pinned per-phase
tolerances and goldens (never inventing new/looser ones, per CONTEXT.md's lock). Also
extend `test/test_pricing_fit.jl` with a pinned FIT-vs-DADP regression golden.

Purpose: this is the phase's other named requirement (EXP-04) and the milestone's final
acceptance gate — independent of the literate-docs work (plans 09-01/09-02/09-04/09-05),
touching only `test/`.
Output: `test/test_acceptance.jl` (new), `test/test_pricing_fit.jl` (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-CONTEXT.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-RESEARCH.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-PATTERNS.md

@test/test_ieee13.jl
@test/test_ieee123_admm.jl
@test/fixtures_phase4.jl
@test/fixtures_phase7.jl
@test/test_pricing_fit.jl
@test/test_pricing_welfare.jl
@src/models/oracle.jl
@src/admm/solve_admm.jl
@src/pricing/dlmp.jl
</context>

<interfaces>
Real entrypoints + EXISTING pinned tolerances this plan reuses (never invents new ones):

From `src/models/oracle.jl`:
```
operational_oracle(feeder, pf, aggregators; λ₀, T=24, allow_export=false) -> (; cost, π, dadp, ctx)
```

From `src/admm/solve_admm.jl`:
```
solve_admm(feeder, pf::ConvexBranchFlow, aggregators; T=24, λ₀, ρ, maxiter=200,
           ε_abs=1e-4, ε_rel=1e-3, τ=2.0, μ=10.0, ρ_min=1e-2, ρ_max=1e4, allow_export=true)
    -> (; welfare, dadp, λ, iters, residuals, dso_ctx, exact_maxgap)
```

From `src/pricing/dlmp.jl`: `extract_dlmp(ctx; bus=nothing, T=nothing)`.

`test/fixtures_phase4.jl` (`@testmodule Phase4Fixtures`) exports: `T` (=24),
`mem_price_profile()`, `build_ieee13_ground_aggregators(feeder; seed=20260718)`.

`test/fixtures_phase7.jl` (`@testmodule Phase7Fixtures`) exports: `T` (=24),
`ieee123_lambda0()`, `build_ieee123_aggregators(feeder; seed=SEED_IEEE123, load_buses=nothing)`,
`RHO0` (=5.0), `EPS_ABS` (=1e-5), `EPS_REL` (=1e-4), `TAU` (=2.0), `MU` (=10.0),
`RHO_MIN` (=1e-2), `RHO_MAX` (=1e4).

Existing PINNED goldens to REUSE verbatim (never re-derive) from `test/test_ieee13.jl`
(the "ieee13 ground: pinned computed golden regression" `@testitem`):
```
GOLDEN_V9_16      = 1.0436080536
GOLDEN_WELFARE    = -4823.1598620624
GOLDEN_DADP16     = 1.4024313925
GOLDEN_SUM_DADP   = 96.7166853441
THESIS_V9_16      = 1.0493             # non-failing thesis cross-check ONLY
```
Existing PINNED tolerances to reuse from `test/test_ieee123_admm.jl`: `res.iters < 300`,
`res.iters <= 100`, welfare `rtol=1e-4`, `exact_maxgap < 1e-3`, DADP match
`atol=1e-2, rtol=1e-3`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IEEE-13 congestion acceptance testitem (SC3, EXP-04)</name>
  <files>test/test_acceptance.jl</files>
  <read_first>test/test_ieee13.jl, test/fixtures_phase4.jl, src/models/oracle.jl, src/admm/solve_admm.jl</read_first>
  <behavior>
  - Given the modified IEEE-13 feeder + `Phase4Fixtures.build_ieee13_ground_aggregators` +
    `Phase4Fixtures.mem_price_profile()`, `operational_oracle(...; allow_export=true)` must
    return `ctx.meta[:socp_maxgap] < 1e-5` (PF-04 exact) and `res.cost` matching the
    EXISTING `GOLDEN_WELFARE = -4823.1598620624` at `rtol=1e-4` (reused, not re-derived).
  - `solve_admm(...; ρ=100.0, allow_export=true)` on the SAME feeder/aggregators/λ₀ must
    return `welfare` matching `res.cost` at `rtol=1e-4` (ADMM-03/04) and `λ` matching
    `res.dadp` at `atol=1e-2, rtol=1e-3` (recovered DADP).
  - The thesis `THESIS_V9_16 = 1.0493` cross-check must use `@info` + a `broken` `@test`
    (never a hard failure) — mirroring `test/test_ieee13.jl`'s existing non-failing pattern.
  </behavior>
  <action>
  Create `test/test_acceptance.jl` with a header comment naming it the SC3 consolidated
  v1 acceptance gate (EXP-04), referencing `test/test_ieee13.jl` and
  `test/test_ieee123_admm.jl` as the per-phase tests it consolidates without duplicating.
  Add `@testitem "acceptance: IEEE-13 congestion — exact relaxation + DADP + ADMM≈centralized (SC3)" tags = [:acceptance] setup = [Phase4Fixtures] begin ... end`
  following the skeleton in 09-PATTERNS.md / 09-RESEARCH.md Code Examples: build
  `TSODSO.ieee13_modified()`, `Phase4Fixtures.build_ieee13_ground_aggregators(feeder; seed = 20260718)`,
  `λ₀ = Phase4Fixtures.mem_price_profile()`; call `operational_oracle` and assert
  `ctx.meta[:socp_maxgap] < 1e-5` and `isapprox(res.cost, -4823.1598620624; rtol = 1e-4)`
  (the EXISTING golden, copied verbatim — cite its source file in a comment); call
  `solve_admm(feeder, ConvexBranchFlow(), aggs; T=24, λ₀=λ₀, ρ=100.0, allow_export=true)`
  and assert `isapprox(admm.welfare, res.cost; rtol=1e-4)` and
  `isapprox(admm.λ, res.dadp; atol=1e-2, rtol=1e-3)`. Add the non-failing thesis
  cross-check (`@info` + `gap = abs(v9_16 - 1.0493)`; `@test gap < 1e-2 broken = (gap >= 1e-2)`)
  reading `v9_16 = sqrt(value(res.ctx.meta[:pf_vars].v[10, 16]))` (node 9 -> struct index 10,
  hour 16 — the same A1 mapping as `test_ieee13.jl`).
  </action>
  <acceptance_criteria>
  `test/test_acceptance.jl` exists with the IEEE-13 `@testitem` tagged `:acceptance`;
  running it exercises `operational_oracle` + `solve_admm` on the real ground-truth
  aggregators (no new fixture, no new tolerance invented); the thesis cross-check uses
  `broken = (gap >= 1e-2)` so it can never redden the suite.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.test(; test_args=["acceptance"])'</automated>
  </verify>
  <done>The IEEE-13 acceptance testitem passes: exact relaxation, golden welfare/DADP match, ADMM≈centralized, thesis cross-check non-failing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IEEE-123 voltage acceptance testitem (SC3, EXP-04)</name>
  <files>test/test_acceptance.jl</files>
  <read_first>test/test_ieee123_admm.jl, test/fixtures_phase7.jl</read_first>
  <behavior>
  - Given the modified IEEE-123 feeder + `Phase7Fixtures.build_ieee123_aggregators` +
    `Phase7Fixtures.ieee123_lambda0()`, the centralized `solve_welfare` DADP (via
    `extract_dlmp`) and the `solve_admm` result must satisfy the SAME contract as
    `test/test_ieee123_admm.jl`: `res.iters < 300`, `res.iters <= 100`,
    `isapprox(res.welfare, obj_c; rtol=1e-4)`, `res.exact_maxgap < 1e-3`,
    `isapprox(res.λ, dlmp_c; atol=1e-2, rtol=1e-3)`.
  </behavior>
  <action>
  Add a second `@testitem "acceptance: IEEE-123 voltage — exact relaxation + DADP + ADMM≈centralized (SC3)" tags = [:acceptance] setup = [Phase7Fixtures] begin ... end`
  to `test/test_acceptance.jl`, following `test/test_ieee123_admm.jl`'s body nearly
  verbatim (same feeder `ieee123_modified()`, same `Phase7Fixtures.build_ieee123_aggregators`,
  same `Phase7Fixtures.RHO0`/`EPS_ABS`/`EPS_REL`/`TAU`/`MU`/`RHO_MIN`/`RHO_MAX` — reuse the
  IDENTICAL config constants, never retune). Solve centralized via `solve_welfare` +
  `extract_dlmp` for the cross-validation DADP, then `solve_admm` with the SAME adaptive-ρ
  config, asserting the SAME five contract lines listed in `<behavior>`. This item is the
  SC3 half that specifically targets ADMM-03 cross-validation at IEEE-123 scale — do not
  weaken any of the five reused tolerances.
  </action>
  <acceptance_criteria>
  `test/test_acceptance.jl` contains a second `@testitem` tagged `:acceptance` for
  IEEE-123, asserting all five contract lines with the EXACT tolerances already pinned in
  `test/test_ieee123_admm.jl` (no new/looser tolerance introduced).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.test(; test_args=["acceptance"])'</automated>
  </verify>
  <done>The IEEE-123 acceptance testitem passes with the same tolerances as the existing Phase-7 regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FIT-vs-DADP regression golden extension (EXP-04)</name>
  <files>test/test_pricing_fit.jl</files>
  <read_first>test/test_pricing_fit.jl, test/test_pricing_welfare.jl, src/pricing/fit.jl</read_first>
  <behavior>
  - `fit_baseline(feeder, ConvexBranchFlow(), FitFixtures.aggregators(seed=20260718); T=FitFixtures.T)`
    on the existing `FitFixtures` 3-bus scenario returns a DETERMINISTIC `res.ratio`
    (bit-for-bit reproducible under the fixed seed, per the file's own existing
    reproducibility testitem). Pin that `res.ratio` value as a NEW inline `const` golden +
    `@test isapprox(res.ratio, GOLDEN; rtol = 1e-4)` regression assertion, extending the
    file's existing `@testitem` set (do NOT create a JLD2/CSV golden — inline constant is
    the established convention, per `test/test_pricing_welfare.jl`'s `RATIO_GOLDEN`
    pattern).
  </behavior>
  <action>
  Add a new `@testitem "fit: FIT-vs-DADP ratio regression golden (EXP-04)" setup = [FitFixtures] tags = [:fit] begin ... end`
  to `test/test_pricing_fit.jl` (append after the existing 5 testitems, do not modify
  them). Run `fit_baseline(FitFixtures.feeder(), ConvexBranchFlow(), FitFixtures.aggregators(seed = 20260718); T = FitFixtures.T)`,
  capture `res.ratio` as the FIRST-trusted-solve golden (name it e.g. `FIT_RATIO_GOLDEN`,
  a literal `Float64` — the executor must actually run the solve to observe and record
  this number, exactly how every other golden in this codebase was captured, per
  `test/test_ieee13.jl`'s header comment), then assert
  `@test isapprox(res.ratio, FIT_RATIO_GOLDEN; rtol = 1e-4)`. This is DISTINCT from and
  ADDITIVE to `test/test_pricing_welfare.jl`'s existing `RATIO_GOLDEN` (which pins the
  ratio on the IEEE-13 ground scenario) — this one pins it on the small self-contained
  `FitFixtures` scenario this file already owns, giving EXP-04 a FIT-focused regression
  independent of the IEEE-13 fixture.
  </action>
  <acceptance_criteria>
  `test/test_pricing_fit.jl` gains exactly one new `@testitem` with a literal
  `FIT_RATIO_GOLDEN` constant and a tight `rtol=1e-4` regression assertion against it; the
  five pre-existing `@testitem`s in the file are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.test(; test_args=["fit"])'</automated>
  </verify>
  <done>All `fit`-tagged testitems (5 existing + 1 new) pass; the new golden regression is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> `src/` entrypoints | Local `Pkg.test()` execution only; no untrusted input, no network, no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Repudiation (silent tolerance drift) | `test/test_acceptance.jl`, `test/test_pricing_fit.jl` | accept | Researcher-authored regression pins on a local test suite; no untrusted input. Mitigated structurally by REUSING (never loosening) the already-pinned per-phase tolerances (CONTEXT.md lock) rather than inventing new acceptance-specific thresholds that could mask drift. |
</threat_model>

<verification>
`julia --project=. -e 'using Pkg; Pkg.test(; test_args=["acceptance"])'` passes both new
testitems; `julia --project=. -e 'using Pkg; Pkg.test(; test_args=["fit"])'` passes all
6 `fit`-tagged testitems; a full `julia --project=. -e 'using Pkg; Pkg.test()'` run stays
green (no regression in the pre-existing 1933 tests).
</verification>

<success_criteria>
`test/test_acceptance.jl` exists with two `:acceptance`-tagged testitems (IEEE-13,
IEEE-123) reproducing exact relaxation + recovered DADP + ADMM≈centralized using ONLY
already-pinned tolerances/goldens; `test/test_pricing_fit.jl` gains one new pinned
FIT-vs-DADP ratio regression golden.
</success_criteria>

<output>
Create `.planning/phases/09-documentation-regression-acceptance-gate/09-03-SUMMARY.md` when done
</output>
