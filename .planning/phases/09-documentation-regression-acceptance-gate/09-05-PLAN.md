---
phase: 09-documentation-regression-acceptance-gate
plan: 05
type: execute
wave: 3
depends_on: ["09-04"]
files_modified:
  - .github/workflows/CI.yml
autonomous: false
requirements: [EXP-03]

must_haves:
  truths:
    - "CI runs the docs build (Documenter + Literate, @example execution) as a single dedicated job, not a leg of the 1.10/1.11/1.12 test matrix."
    - "The deploydocs repo slug is confirmed against the real GitHub org/repo before this project ever pushes to gh-pages from CI."
  artifacts:
    - path: ".github/workflows/CI.yml"
      provides: "New `docs` job: checkout -> setup-julia (single version) -> julia --project=docs docs/make.jl"
      contains: "docs:"
  key_links:
    - from: ".github/workflows/CI.yml docs job"
      to: "docs/make.jl"
      via: "run: julia --project=docs docs/make.jl"
      pattern: "docs/make\\.jl"
---

<objective>
Wire the docs build into CI as a single dedicated job (RESEARCH Pitfall 6 — never a leg
of the 3-way Julia-version test matrix, since docs content is Julia-version-invariant),
mirroring the existing `format` job's shape. Close out the phase with the two
manual-only verifications flagged in 09-VALIDATION.md: the `deploydocs` repo-slug
placeholder (not discoverable from local repo state — this checkout has no git remote
configured) and the CairoMakie figure aesthetics eyeball check.

Purpose: the last EXP-03 deliverable — the docs build becomes a CI-enforced gate, and
the one genuinely-human-only decision (the real GitHub repo slug) is confirmed rather
than silently guessed.
Output: `.github/workflows/CI.yml` with a new `docs` job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-CONTEXT.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-RESEARCH.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-VALIDATION.md

@.github/workflows/CI.yml
@docs/make.jl
</context>

<interfaces>
Existing `format` job in `.github/workflows/CI.yml` (the structural template to mirror —
full file already read into context above): `checkout@v4` -> `setup-julia@v2` (single
pinned version, `'1.11'`) -> a single run step. The new `docs` job follows the identical
shape, swapping the run step for `julia --project=docs docs/make.jl` with `CI: 'true'` in
its environment (so `docs/make.jl`'s existing `get(ENV, "CI", nothing) == "true"` gates
fire correctly for `prettyurls` and the plan-09-04 `deploydocs` guard).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the docs CI job</name>
  <files>.github/workflows/CI.yml</files>
  <read_first>.github/workflows/CI.yml, docs/make.jl</read_first>
  <action>
  Add a new top-level job `docs:` to `.github/workflows/CI.yml`, alongside the existing
  `test:` and `format:` jobs (do not modify either). Mirror the `format` job's structural
  shape: `name: Documentation build (Documenter + Literate)`, `runs-on: ubuntu-latest`,
  a `timeout-minutes` budget (e.g. 30 — docs builds re-solve real SOCP/ADMM cases, give it
  headroom above `format`'s 20), steps `actions/checkout@v4` -> `julia-actions/setup-julia@v2`
  pinned to a SINGLE version (`'1.11'`, matching `format`'s choice — RESEARCH Pitfall 6:
  do NOT add a version matrix here, docs content is Julia-version-invariant) ->
  `julia-actions/cache@v2` -> a `Build docs` step running
  `julia --project=docs docs/make.jl` with `env: CI: 'true'` so the existing
  `get(ENV, "CI", nothing) == "true"` gates in `docs/make.jl` (prettyurls,
  and the plan-09-04 CI-gated `deploydocs`) activate correctly under Actions. Do not add
  a `deploydocs`-specific secrets/token step in THIS task — that is the human-verify
  checkpoint below, confirming the actual GitHub org/repo slug and (if `deploydocs` is to
  push for real) the `DOCUMENTER_KEY`/`GITHUB_TOKEN` wiring, which depends on information
  not present in the local repo (no git remote configured in this checkout).
  </action>
  <acceptance_criteria>
  `.github/workflows/CI.yml` gains a new `docs:` job with a single pinned Julia version
  (no matrix), running `julia --project=docs docs/make.jl` with `CI: 'true'` in its
  environment; the existing `test:` and `format:` jobs are byte-identical to before this
  edit (diff shows only an added job).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/CI.yml')); assert 'docs' in d['jobs']; assert 'matrix' not in d['jobs']['docs'].get('strategy',{}); print('OK')"</automated>
  </verify>
  <done>The workflow YAML parses, contains a `docs` job with no strategy matrix, and the pre-existing `test`/`format` jobs are unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Researcher confirms the deploydocs repo slug + docs CI job goes green (+ optional CairoMakie aesthetics)</name>
  <files>docs/make.jl (review only — repo slug confirmation), .github/workflows/CI.yml (review only)</files>
  <action>PAUSE for the researcher. All automation is complete (the `docs` job is wired and the full local docs build already exits 0 per plan 09-04's verification); this task only presents the deploy-configuration gap for human confirmation — the real GitHub org/repo slug is not discoverable from this local checkout (no git remote configured) and CairoMakie figure aesthetics are subjective. Do not modify code before the resume signal.</action>
  <acceptance_criteria>Researcher has either updated the `deploydocs(; repo = ...)` placeholder to the real slug (or explicitly deferred it), and confirmed (or scheduled) a green run of the new `docs` job in GitHub Actions.</acceptance_criteria>
  <verify><human-check>Researcher reviews the placeholder repo slug and the docs job definition, then replies via the resume signal.</human-check></verify>
  <done>Researcher typed "approved" (slug confirmed/updated, CI job accepted), or described a correction to apply.</done>
  <what-built>
  The docs job now builds the full Documenter + Literate site (all six abstraction-ladder
  rungs) in CI as a single dedicated job. `docs/make.jl` (plan 09-04) carries a CI-gated
  `deploydocs(; repo = "github.com/PLACEHOLDER-ORG/PLACEHOLDER-REPO.git")` call whose repo
  slug is a clearly-marked placeholder — it is not discoverable from this local checkout
  (no git remote is configured). CairoMakie convergence/price figures (`docs/literate/admm.jl`,
  plan 09-04) are structurally wired (guarded by `Base.find_package("CairoMakie")`) but
  their VISUAL aesthetics were never manually eyeballed (CairoMakie is a build-time-only
  render in this environment).
  </what-built>
  <how-to-verify>
  1. Replace the placeholder in `docs/make.jl`'s `deploydocs(; repo = ...)` call with the
     real GitHub `org/repo` slug for this project (or confirm the placeholder is
     intentionally left for a later, separate PR that also configures GitHub Pages).
  2. Push this branch (or open a PR) and confirm the new `docs` job in
     `.github/workflows/CI.yml` runs green in GitHub Actions.
  3. If GitHub Pages deployment is desired now: confirm `DOCUMENTER_KEY` (or the
     `permissions: contents: write` + `GITHUB_TOKEN` route) is configured in the repo's
     Actions secrets before merging to the branch `deploydocs` triggers from.
  4. Optional (advisory, non-blocking, carried over from the Phase-7 deferred item): in a
     non-headless environment with CairoMakie installed, run
     `julia --project=docs docs/make.jl` and open `docs/build/generated/admm.md`'s
     rendered figure (or run `plot_convergence`/`plot_price_convergence` directly and
     `save(...)` a PDF) to eyeball the convergence-plot aesthetics.
  </how-to-verify>
  <resume-signal>Type "approved" once the repo slug is confirmed/updated and the docs CI job is verified green (or describe what needs to change).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.github/workflows/CI.yml` docs job -> GitHub Actions secrets | `CI: 'true'` env var only in this task; any future `DOCUMENTER_KEY`/`GITHUB_TOKEN` wiring is GitHub Actions' native secret injection, never printed/logged by this job |
| `docs/make.jl` `deploydocs` -> gh-pages | Gated on `CI` env var (plan 09-04); this plan's checkpoint confirms the real repo slug before any real push is possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering (unintended docs deploy) | `docs/make.jl` `deploydocs` (wired in 09-04) | mitigate | CI-env-var gate already in place; THIS plan's human-verify checkpoint additionally confirms the real repo slug is correct before it is ever exercised for real, rather than letting a placeholder silently reach production. |
| T-09-02 | Information Disclosure | `.github/workflows/CI.yml` `docs` job | mitigate | The job never `echo`/logs `ENV` contents; any `DOCUMENTER_KEY`/`GITHUB_TOKEN` remains GitHub Actions' native secret-masked injection, matching the project's `test`/`format` jobs' existing convention (`secrets.CODECOV_TOKEN` passed opaquely, never printed). |
</threat_model>

<verification>
The `docs` job's YAML is valid and contains no version matrix. The human-verify
checkpoint confirms the repo slug and (if applicable) that the job runs green in GitHub
Actions once pushed.
</verification>

<success_criteria>
`.github/workflows/CI.yml` runs the docs build as a single dedicated job on every push/PR
(mirroring `format`'s cost profile, not the 3-way test matrix); the real deploy
configuration (repo slug, secrets) is confirmed by a human before this project relies on
it, closing the last EXP-03 deliverable and the phase.
</success_criteria>

<output>
Create `.planning/phases/09-documentation-regression-acceptance-gate/09-05-SUMMARY.md` when done
</output>
