---
phase: 09-documentation-regression-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/literate/lindistflow.jl
  - docs/literate/convex_branch_flow.jl
autonomous: true
requirements: [EXP-03]

must_haves:
  truths:
    - "A researcher opening the LinDistFlow literate source sees thesis eqs. 3.31/3.32/3.33/3.43 stated immediately beside the real solve_linear code that implements them."
    - "A researcher opening the Convex Branch-Flow literate source sees thesis eqs. 3.39/3.43/3.45 (the SOC cone + exactness copy) beside the real solve_welfare/ConvexBranchFlow code, and the rendered PF-04 exactness gap is a genuine solved number, not a hardcoded placeholder."
  artifacts:
    - path: "docs/literate/lindistflow.jl"
      provides: "Rung 1-2 LinDistFlow literate page: math -> assumptions -> real solve_linear validation"
      min_lines: 40
    - path: "docs/literate/convex_branch_flow.jl"
      provides: "Rung 3 SOCP + exactness-copy literate page: math -> real solve_welfare validation -> PF-04 exactness gap displayed"
      min_lines: 40
  key_links:
    - from: "docs/literate/lindistflow.jl"
      to: "src/models/linear_solve.jl solve_linear"
      via: "direct function call, no reimplemented @constraint/@objective"
      pattern: "solve_linear\\("
    - from: "docs/literate/convex_branch_flow.jl"
      to: "src/models/welfare_solve.jl solve_welfare + src/models/exactness.jl assert_socp_exact!"
      via: "direct function call; ctx.meta[:socp_maxgap] displayed as the validation step"
      pattern: "solve_welfare\\("
---

<objective>
Author the first two literate documentation pages of the abstraction ladder (EXP-03,
per CONTEXT.md's locked "one literate page per rung" scope): Rung 1-2 (LinDistFlow linear
branch-flow) and Rung 3 (SOCP Convex Branch-Flow + the LinDistFlow exactness copy). Each
page follows the existing `docs/literate/toy_dc.jl` structure (math -> assumptions ->
real-solve validation) and calls ONLY real `src/` entrypoints — no reimplemented JuMP
model, no new solve path.

Purpose: close EXP-03 for two of the five new abstraction-ladder rungs, proving the
literate-page pattern replicates cleanly before the remaining three pages (plan 09-02)
and the docs/make.jl wiring (plan 09-04) land.
Output: `docs/literate/lindistflow.jl`, `docs/literate/convex_branch_flow.jl` — both
standalone-includable Julia sources (Literate front-matter is plain `#` comments, so
`include(...)` executes them identically to the eventual Documenter `@example` blocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-CONTEXT.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-RESEARCH.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-PATTERNS.md

@docs/literate/toy_dc.jl
@src/powerflow/LinDistFlow.jl
@src/models/linear_solve.jl
@src/devices/Interruptible.jl
@src/powerflow/ConvexBranchFlow.jl
@src/models/exactness.jl
@src/models/welfare_solve.jl
@test/test_exactness.jl
</context>

<interfaces>
Real entrypoints this plan calls (never reimplemented):

From `src/models/linear_solve.jl`:
```
solve_linear(feeder, pf::AbstractPowerFlow, devices::Vector{<:AbstractDevice}; T::Int=1, λ₀)
    -> (ctx::ModelContext, objective::Float64, dadp::Vector{Float64})
```

From `src/devices/Interruptible.jl`:
```
Interruptible(bus::Integer, Pmin::Real, Pmax::Real, a::Real, b::Real)  # b > 0 required
```

From `src/models/welfare_solve.jl`:
```
solve_welfare(feeder, pf::AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator};
              T::Int=24, λ₀, allow_export::Bool=false) -> (ctx, objective::Float64, dadp::Vector{Float64})
```
`ctx.meta[:socp_maxgap]` is stashed by `solve_welfare` itself (via `assert_socp_exact!`)
whenever the formulation stashes a squared current `:l` — only `ConvexBranchFlow` does.

From `src/devices/Aggregator.jl`:
```
Aggregator(bus::Integer, φ::Real, devices::AbstractVector{<:AbstractDevice}, Pdc::AbstractVector{<:Real})
```
`φ ∈ (0,1]`; `devices` non-empty.

Feeder primitives (`src/data/Feeder.jl`, already used identically in `toy_dc.jl` /
`test/test_exactness.jl`): `Bus(id, vmin, vmax, is_root)`, `Branch(from, to, r, x, smax)`,
`Feeder(buses, branches, root)` — construction itself validates radial topology + magnitudes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LinDistFlow literate page (rung 1-2, thesis 3.31-3.33/3.43)</name>
  <files>docs/literate/lindistflow.jl</files>
  <read_first>docs/literate/toy_dc.jl, src/powerflow/LinDistFlow.jl, src/models/linear_solve.jl, src/devices/Interruptible.jl</read_first>
  <action>
  Create `docs/literate/lindistflow.jl` replicating `toy_dc.jl`'s verbatim shape (title
  comment line, short "what this page proves" paragraph, math block, real
  construction/solve code, bare-expression display lines, validation step) — never a
  `jldoctest`. Title: "# # Rung 1-2 — LinDistFlow (Linear Branch Flow)". State this page
  proves the residual-seam contract (PF-01/PF-02) generalizes from the toy single-node
  balance to a real two-bus branch-flow network with a flexible device, citing thesis eqs.
  3.31 (active nodal balance), 3.32 (reactive nodal balance), 3.33 (squared-voltage variable
  `v = |V|^2`), and 3.43 (the loss-less voltage-drop relation `v_to = v_from - 2(rP+xQ)`,
  i.e. `LinDistFlow`'s `l -> 0` specialization of the eventual SOCP exactness copy — cite
  that it is the ancestor `ConvexBranchFlow` (plan 09-01 Task 2) builds on). Build a small
  2-bus radial `Feeder` (`Bus(1, 0.95, 1.05, true)`, `Bus(2, 0.95, 1.05, false)`, one
  `Branch(1, 2, r, x, smax)` with a modest r/x e.g. 0.02/0.03 and a slack `smax` like 10.0 —
  mirror the shape already used in `test/test_pricing_fit.jl`'s `FitFixtures.feeder()`), one
  `Interruptible(2, Pmin, Pmax, a, b)` device at bus 2 with `b > 0`. Call
  `solve_linear(feeder, LinDistFlow(), [device]; T = 1, λ₀ = [value])` — never build a
  parallel `Model`/`@constraint`. Display, as bare expressions (no `println`, no `@test`):
  `objective`, `dadp`, and `haskey(ctx.residuals, :Rq)` (proving LinDistFlow's reactive
  channel — contrast with `toy_dc.jl`'s DC-only `:Rp`-only balance). This IS the page's
  validation step (math -> assumptions -> validation structure, per CONTEXT.md).
  </action>
  <acceptance_criteria>
  `docs/literate/lindistflow.jl` exists, is standalone-includable, contains a fenced
  ` ```math ``` ` block citing "3.31" and "3.43", calls `solve_linear(...)` (no
  `@constraint`/`@objective`/`Model(` at the top level of the page itself — only inside the
  called `src/` function), and the last three top-level expressions are bare displays of
  `objective`, `dadp`, `haskey(ctx.residuals, :Rq)`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'include("docs/literate/lindistflow.jl")'</automated>
  </verify>
  <done>Page created; `include(...)` runs to completion with no error, printing/evaluating a finite objective and a length-1 dadp vector.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convex Branch-Flow + exactness literate page (rung 3, thesis 3.39/3.43-3.45, PF-04)</name>
  <files>docs/literate/convex_branch_flow.jl</files>
  <read_first>docs/literate/toy_dc.jl, src/powerflow/ConvexBranchFlow.jl, src/models/exactness.jl, src/models/welfare_solve.jl, test/test_exactness.jl</read_first>
  <action>
  Create `docs/literate/convex_branch_flow.jl` in the same verbatim shape. Title:
  "# # Rung 3 — SOCP Convex Branch-Flow \& the LinDistFlow Exactness Copy". Math block
  cites thesis eq. 3.39 (the rotated SOC cone `[0.5·l, v_i, P, Q] ∈ RotatedSecondOrderCone()`,
  equivalent to `l·v ≥ P²+Q²`), eq. 3.43 (the exactness-copy voltage drop on the auxiliary
  `v̂`), and eq. 3.45 (squared-magnitude voltage bounds enforced on BOTH `v` and `v̂`) —
  explain in one short paragraph WHY the copy is not decorative: bounding `v̂ ≤ V²max`
  drives the loss current `l` down until the cone holds with equality at the optimum
  (contrast with the previous page's `LinDistFlow`, which is this model with `l -> 0`).
  Build a small 2-bus radial `Feeder` matching `test/test_exactness.jl`'s fixture shape
  (`Bus(1,0.95,1.05,true)`, `Bus(2,0.95,1.05,false)`, `Branch(1,2,0.01,0.02,10.0)`) with one
  `Aggregator(2, φ, [Interruptible(2, Pmin, Pmax, a, b)], Pdc)` (a single-device aggregator
  is a valid `Aggregator` — devices need only be non-empty). Call
  `solve_welfare(feeder, ConvexBranchFlow(), [agg]; T = 1, λ₀ = [value], allow_export = true)`
  — never build a parallel `Model`/cone constraint. Display `objective`, `dadp`, and
  `ctx.meta[:socp_maxgap]` (the PF-04 exactness certificate `solve_welfare` already ran and
  stashed internally) as the page's validation step. State in prose that
  `assert_socp_exact!` runs INSIDE `solve_welfare` and THROWS (refusing prices) on an
  inexact relaxation — reaching a rendered `socp_maxgap` number at all is therefore the
  passing certificate, not a separate assertion this page makes.
  </action>
  <acceptance_criteria>
  `docs/literate/convex_branch_flow.jl` exists, is standalone-includable, contains a fenced
  math block citing "3.39", "3.43", and "3.45", calls `solve_welfare(..., ConvexBranchFlow(), ...)`
  (no top-level `@constraint`/`RotatedSecondOrderCone`/`Model(` in the page itself), and
  displays `ctx.meta[:socp_maxgap]` as a bare top-level expression.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'include("docs/literate/convex_branch_flow.jl")'</automated>
  </verify>
  <done>Page created; `include(...)` runs to completion with no error, printing/evaluating a finite objective and a `socp_maxgap` well under 1e-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Literate page -> `src/` entrypoint | `@example`/`include`-executed blocks call real `solve_linear`/`solve_welfare`; no untrusted input crosses here (local research library, no network/user-input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering (silent math fork) | `docs/literate/lindistflow.jl`, `docs/literate/convex_branch_flow.jl` | mitigate | Every page calls ONLY the real `src/` entrypoint (`solve_linear`, `solve_welfare`) — verified by the acceptance criterion that no `@constraint`/`@objective`/`Model(` appears at the page's own top level; a parallel hand-rolled formulation would silently diverge from the validated core. |
</threat_model>

<verification>
Both pages `include(...)` cleanly (exit 0, no thrown error) and print/evaluate real,
finite numbers (objective, dadp, and — for the SOCP page — a `socp_maxgap` under 1e-4).
No fenced code in the pages hardcodes a result; every displayed number comes from the
`include`d solve.
</verification>

<success_criteria>
`docs/literate/lindistflow.jl` and `docs/literate/convex_branch_flow.jl` exist, follow the
`toy_dc.jl` math -> assumptions -> validation structure, cite the correct thesis equation
numbers, and execute cleanly via plain `include(...)` (proving they will also execute
cleanly as `@example` blocks once wired into `docs/make.jl` by plan 09-04).
</success_criteria>

<output>
Create `.planning/phases/09-documentation-regression-acceptance-gate/09-01-SUMMARY.md` when done
</output>
