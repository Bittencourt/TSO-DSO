---
phase: 09-documentation-regression-acceptance-gate
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - docs/literate/admm.jl
  - docs/Project.toml
  - docs/make.jl
  - docs/src/index.md
autonomous: true
requirements: [EXP-03]

must_haves:
  truths:
    - "A researcher opening the ADMM literate source sees the AGR-OPT/DSO-OPT split (3.46/3.47) and the dual-ascent update beside the real solve_admm code, cross-validated against the real centralized solve_welfare on the same page."
    - "julia --project=docs docs/make.jl builds all six pages (toy_dc + the five new rungs) end to end and exits 0, with no @example execution error."
    - "checkdocs is tightened from :none to :exports without breaking the build (warnonly still covers residual gaps)."
    - "CairoMakie convergence figures render when CairoMakie is installed in the docs environment, and the same source degrades gracefully (no error) when it is not."
  artifacts:
    - path: "docs/literate/admm.jl"
      provides: "Rung 5 ADMM decomposition + convergence literate page"
      min_lines: 45
    - path: "docs/make.jl"
      provides: "Full Documenter+Literate build: 6 Literate.markdown calls (flavor=DocumenterFlavor), nested Models pages tree, checkdocs=:exports, CI-gated deploydocs"
    - path: "docs/Project.toml"
      provides: "CairoMakie added as a docs-only [deps] entry (0.15 compat, matching root [weakdeps])"
  key_links:
    - from: "docs/make.jl"
      to: "docs/literate/*.jl (all six sources)"
      via: "Literate.markdown(...; flavor = Literate.DocumenterFlavor())"
      pattern: "Literate\\.markdown"
    - from: "docs/literate/admm.jl"
      to: "src/admm/solve_admm.jl + src/models/welfare_solve.jl + ext/TSODSOMakieExt.jl plot_convergence"
      via: "direct function calls, CairoMakie guarded by Base.find_package"
      pattern: "solve_admm\\("
---

<objective>
Complete EXP-03's literate-page set with the final rung (ADMM decomposition +
convergence, thesis 3.46/3.47), then extend `docs/make.jl` to build ALL SIX pages
(the existing `toy_dc.jl` plus the five new rungs from plans 09-01/09-02/this plan) as
one coherent Documenter site: migrate off the deprecated `documenter=true` kwarg,
add a nested "Models" `pages` tree, tighten `checkdocs` to `:exports`, and wire a
CI-gated `deploydocs` call (repo slug is a clearly-marked placeholder — confirmed by a
human-verify checkpoint in plan 09-05, since it is not discoverable from local repo
state: this checkout has no git remote configured).

Purpose: this is the wave that turns five independent literate sources into one
buildable docs site — the last piece of EXP-03 before the docs CI job (plan 09-05).
Output: `docs/literate/admm.jl`, `docs/Project.toml` (+CairoMakie), `docs/make.jl`
(fully extended), `docs/src/index.md` (linked to the generated pages).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-CONTEXT.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-RESEARCH.md
@.planning/phases/09-documentation-regression-acceptance-gate/09-PATTERNS.md

@docs/make.jl
@docs/literate/toy_dc.jl
@docs/Project.toml
@docs/src/index.md
@src/admm/solve_admm.jl
@src/models/welfare_solve.jl
@ext/TSODSOMakieExt.jl
@test/test_diagnostics_plot.jl
@test/fixtures_phase4.jl
</context>

<interfaces>
Real entrypoints this plan calls (never reimplemented):

From `src/admm/solve_admm.jl`:
```
solve_admm(feeder, pf::ConvexBranchFlow, aggregators; T=24, λ₀, ρ, maxiter=200,
           ε_abs=1e-4, ε_rel=1e-3, τ=2.0, μ=10.0, ρ_min=1e-2, ρ_max=1e4, allow_export=true)
    -> (; welfare, dadp, λ, iters, residuals, dso_ctx, exact_maxgap)
```

From `src/models/welfare_solve.jl`: `solve_welfare(feeder, pf, aggregators; T, λ₀, allow_export)`.

From `ext/TSODSOMakieExt.jl` (weakdep-gated — methods exist ONLY when CairoMakie is loaded):
```
TSODSO.plot_convergence(res::AdmmResiduals; filename=nothing) -> Makie.Figure
TSODSO.plot_price_convergence(res::AdmmResiduals; filename=nothing) -> Makie.Figure
```
Guard pattern (mirroring `test/test_diagnostics_plot.jl`):
`if Base.find_package("CairoMakie") !== nothing; using CairoMakie; plot_convergence(res); end`.

Current `docs/make.jl` (to extend in place — full file already read into context above):
uses `Literate.markdown(joinpath(LITERATE_DIR, "toy_dc.jl"), GENERATED_DIR; documenter = true)`
(deprecated kwarg since Literate 2.9.0) and `makedocs(...; pages = ["Home" => "index.md", "Rung 0: Toy DC" => "generated/toy_dc.md"], checkdocs = :none, warnonly = [:missing_docs, :cross_references])`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ADMM literate page (rung 5, thesis 3.46-3.47) + docs/Project.toml CairoMakie dep</name>
  <files>docs/literate/admm.jl, docs/Project.toml</files>
  <read_first>docs/literate/toy_dc.jl, src/admm/solve_admm.jl, src/models/welfare_solve.jl, ext/TSODSOMakieExt.jl, test/test_diagnostics_plot.jl, test/fixtures_phase4.jl, docs/Project.toml</read_first>
  <action>
  In `docs/Project.toml`, add `CairoMakie = "13f3f980-e62b-5c42-98c6-ff1f3baf88f0"` under
  `[deps]` and `CairoMakie = "0.15"` under `[compat]` (matching root `Project.toml`'s
  `[weakdeps]` compat — do NOT touch root `Project.toml`'s `[weakdeps]`/`[extensions]`
  block, that boundary stays untouched by this phase). Then create
  `docs/literate/admm.jl` in the `toy_dc.jl` shape. Title: "# # Rung 5 — ADMM
  Decomposition \& Convergence". Math block cites thesis 3.46 (AGR-OPT per-node block)
  and 3.47 (DSO-OPT whole-network block), and the dual-ascent update `λ_j <- λ_j + ρ·R_p,j`
  — name the build-once / re-solve discipline (ADMM-03/04: no per-iteration JuMP rebuild)
  as the practical payoff. Build a small 2-3 bus radial `Feeder` with 1-2 `Aggregator`s
  (reuse a minimal device mix, small `T` like 4 for a fast doc build). Solve the SAME
  scenario twice: `solve_welfare(feeder, ConvexBranchFlow(), aggs; T=..., λ₀=..., allow_export=true)`
  (centralized ground truth) and `solve_admm(feeder, ConvexBranchFlow(), aggs; T=..., λ₀=...,
  ρ=..., allow_export=true)` (decomposed). Display `admm.iters`, the welfare gap
  `abs(admm.welfare - obj_c)`, and `admm.exact_maxgap` as the validation step (ADMM ≈
  centralized). Guard a CairoMakie figure at the end:
  `if Base.find_package("CairoMakie") !== nothing; using CairoMakie; plot_convergence(admm.residuals); end`
  so the SAME source degrades gracefully (no error, just skips the figure) when
  CairoMakie is absent from the active environment — but note `docs/Project.toml` now
  HARD-depends on it (docs-only environments may; the root package's weakdep discipline
  is untouched), so the docs build itself always has it available.
  </action>
  <acceptance_criteria>
  `docs/Project.toml` has `CairoMakie` under both `[deps]` and `[compat]` ("0.15"); root
  `Project.toml`'s `[weakdeps]`/`[extensions]` are UNCHANGED (verify with a diff/grep — no
  edit to that file in this task). `docs/literate/admm.jl` exists, cites "3.46" and
  "3.47", calls `solve_welfare` AND `solve_admm` on the SAME scenario, displays the welfare
  gap as a bare expression, and guards its `plot_convergence` call with
  `Base.find_package("CairoMakie") !== nothing`.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=docs -e 'include("docs/literate/admm.jl")'</automated>
  </verify>
  <done>Page created; `include(...)` runs to completion with a small welfare gap (ADMM≈centralized) and no error whether or not CairoMakie renders.</done>
</task>

<task type="auto">
  <name>Task 2: docs/make.jl full extension — migrate, wire all 6 pages, tighten checkdocs, CI-gated deploydocs</name>
  <files>docs/make.jl, docs/src/index.md</files>
  <read_first>docs/make.jl, docs/src/index.md</read_first>
  <action>
  Rewrite the `Literate.markdown` section of `docs/make.jl`: replace the single
  `documenter = true` call with a `for src in ("toy_dc.jl", "lindistflow.jl",
  "convex_branch_flow.jl", "prosumer_welfare.jl", "pricing_dlmp.jl", "admm.jl")` loop
  calling `Literate.markdown(joinpath(LITERATE_DIR, src), GENERATED_DIR; flavor =
  Literate.DocumenterFlavor())` (the non-deprecated API, Literate >= 2.9 — migrating the
  existing `toy_dc.jl` call in the SAME edit, per RESEARCH Pitfall 1). Extend the
  `makedocs(...)` `pages` argument with a nested `"Models" => [...]` group listing all
  five new generated pages under sensible "Rung N: ..." labels (keep `"Home" => "index.md"`
  first) — exact ordering/labels are at your discretion, matching the abstraction-ladder
  framing. Tighten `checkdocs` from `:none` to `:exports` (cross-version-safe on the
  1.10 floor, per RESEARCH Pitfall 4) — KEEP `warnonly = [:missing_docs,
  :cross_references]` unchanged so the build stays green while surfacing gaps. Keep
  `remotes = nothing` UNCHANGED (Pitfall 5 / Phase-1 rationale — bare/worktree checkouts).
  After the `makedocs(...)` call, add
  `if get(ENV, "CI", nothing) == "true"; deploydocs(; repo = "github.com/PLACEHOLDER-ORG/PLACEHOLDER-REPO.git"); end`
  — reusing the file's EXISTING `get(ENV, "CI", nothing) == "true"` idiom (already used
  for `prettyurls` on the line above) rather than inventing a different CI-detection
  mechanism; mark the placeholder clearly with an inline comment stating it must be
  confirmed against the real GitHub org/repo slug before this ever runs in CI (plan 09-05
  Task 2 is the human-verify checkpoint that confirms it). Update `docs/src/index.md`
  to add a short "Model Documentation" section linking each of the five new generated
  pages by relative path (`generated/lindistflow.md` etc.), alongside the existing
  "Rung 0: Toy DC" link.
  </action>
  <acceptance_criteria>
  `docs/make.jl` contains exactly one `Literate.markdown` call site (the `for` loop) with
  `flavor = Literate.DocumenterFlavor()` and no `documenter = true` anywhere in the file;
  `pages` lists all six generated pages under a `"Models"` group; `checkdocs = :exports`;
  `warnonly` unchanged; a CI-gated `deploydocs` call exists with a clearly-commented
  placeholder repo slug. `docs/src/index.md` links all five new pages.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=docs docs/make.jl</automated>
  </verify>
  <done>The full docs build exits 0: all six literate sources render, checkdocs=:exports does not fail the build (warnonly covers gaps), and no @example execution error occurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Literate page -> `src/` entrypoint | `@example` blocks execute real `solve_welfare`/`solve_admm`; no untrusted input |
| `docs/make.jl` -> GitHub Pages (`deploydocs`) | Would push using CI-injected `DOCUMENTER_KEY`/`GITHUB_TOKEN`; must never run from a local/untrusted checkout |
| `docs/Project.toml` -> CairoMakie dependency | Docs-only environment dependency addition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering (silent math fork) | `docs/literate/admm.jl` | mitigate | Calls ONLY `solve_welfare`/`solve_admm`; the displayed welfare gap comes from a genuine dual-ascent solve, never a hardcoded/reimplemented number. |
| T-09-01 | Tampering (unintended docs deploy) | `docs/make.jl` `deploydocs` call | mitigate | Wrapped in `if get(ENV, "CI", nothing) == "true"`, reusing the file's existing CI-detection idiom (Pitfall 5) — never called unconditionally; the repo slug is a clearly-marked placeholder pending the plan-09-05 human-verify checkpoint, so a wrong/real slug is never silently baked in without confirmation. |
| T-09-04 | Supply chain (dependency) | `docs/Project.toml` CairoMakie `[deps]` addition | accept | CairoMakie is already a reviewed, pinned root `[weakdeps]` package (0.15 compat) from the Phase-7 legitimacy review; adding it to the docs-only environment introduces no new external audit surface, and root `Project.toml`'s `[weakdeps]`/`[extensions]` block is left untouched. |
</threat_model>

<verification>
`julia --project=docs docs/make.jl` exits 0 and builds all six pages with no `@example`
execution error and no `checkdocs` failure (residual gaps are warnings only, per
`warnonly`). `docs/literate/admm.jl` standalone-includes cleanly under both
`--project=docs` (CairoMakie present) and `--project=.` (CairoMakie absent, guarded skip).
</verification>

<success_criteria>
The docs site builds end to end from a clean `julia --project=docs docs/make.jl` run,
covering all six abstraction-ladder rungs, with `checkdocs = :exports` and a CI-gated
`deploydocs` call whose repo slug is a clearly-flagged placeholder.
</success_criteria>

<output>
Create `.planning/phases/09-documentation-regression-acceptance-gate/09-04-SUMMARY.md` when done
</output>
