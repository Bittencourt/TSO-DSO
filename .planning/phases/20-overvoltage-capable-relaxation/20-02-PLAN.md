---
phase: 20-overvoltage-capable-relaxation
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/powerflow/RestrictedBranchFlow.jl
  - src/TSODSO.jl
  - test/test_restricted_branch_flow.jl
autonomous: true
requirements: [OVR-01]

must_haves:
  truths:
    - "Researcher can solve the EXACT-04 fixture via a NEW RestrictedBranchFlow formulation dispatched through the EXISTING solve_welfare entrypoint (D-02), with zero if-formulation== branching anywhere."
    - "The restriction is a genuine feasible-set RESTRICTION (Gan-Low OPF-ε: shrink v's own upper bound by a measured ε), never a relaxation tightening (D-01)."
    - "Once RestrictedBranchFlow is in place, the EXISTING, UNMODIFIED assert_socp_exact! (PF-04) passes on EXACT-04 at its DEFAULT tolerance (no rtol_exact=1.0 diagnostic override needed) — the free validation signal RESEARCH.md flags."
    - "Plain ConvexBranchFlow/DC/LinDistFlow behavior is BYTE-IDENTICAL before and after this plan (new formulation, zero existing-caller drift)."
  artifacts:
    - path: "src/powerflow/RestrictedBranchFlow.jl"
      provides: "RestrictedBranchFlow <: AbstractPowerFlow struct + contribute! (delegates to ConvexBranchFlow, shrinks v's upper bound by ε) + problem_class + export"
      contains: "struct RestrictedBranchFlow <: AbstractPowerFlow"
    - path: "src/TSODSO.jl"
      provides: "include(\"powerflow/RestrictedBranchFlow.jl\") registered immediately after ACPowerFlow.jl"
      contains: "powerflow/RestrictedBranchFlow.jl"
  key_links:
    - from: "src/powerflow/RestrictedBranchFlow.jl"
      to: "src/powerflow/ConvexBranchFlow.jl"
      via: "contribute!(pf::RestrictedBranchFlow, ctx, feeder; T) delegates to contribute!(ConvexBranchFlow(), ctx, feeder; T) before shrinking v's bound"
      pattern: "contribute!\\(ConvexBranchFlow\\(\\)"
    - from: "src/powerflow/RestrictedBranchFlow.jl"
      to: "src/models/welfare_solve.jl"
      via: "problem_class(::RestrictedBranchFlow) = SOCP() routes solve_welfare's optimizer factory unchanged"
      pattern: "problem_class\\(::RestrictedBranchFlow\\)"
---

<objective>
Implement the D-01-resolved Gan–Low OPF-ε restriction as a new `AbstractPowerFlow` concrete
subtype, `RestrictedBranchFlow`, dispatched through the existing `solve_welfare` seam with zero
change to `welfare_solve.jl` itself — the exact `ACPowerFlow` v2.1 precedent (D-02).

Purpose: this is OVR-01's core deliverable. The mechanism (RESEARCH.md, HIGH confidence, verified
against the paper's own PDF text) is a one-line change: shrink the existing per-bus squared-voltage
upper bound from `vmax²` to `vmax² − ε` for a single measured scalar `ε` (plan 20-01's
`ε_measured`, times a documented safety multiplier). This is provably a genuine feasible-set
RESTRICTION (Theorem 2 / OPF-ε, Section IV-D), not a relaxation tightening — the returned point is
guaranteed AC-feasible whenever C1 holds (which RESEARCH.md analytically argues holds comfortably
on this fixture's tiny impedances).

Output: `src/powerflow/RestrictedBranchFlow.jl` (new), `src/TSODSO.jl` gains one new `include`, and
`test/test_restricted_branch_flow.jl` gains the free-validation-signal test (PF-04 passes at
default tolerance) plus the default-path regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-overvoltage-capable-relaxation/20-CONTEXT.md
@.planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md
@.planning/phases/20-overvoltage-capable-relaxation/20-PATTERNS.md
@.planning/phases/20-overvoltage-capable-relaxation/20-01-SUMMARY.md

@src/powerflow/ConvexBranchFlow.jl
@src/powerflow/ACPowerFlow.jl
@src/TSODSO.jl
@test/test_restricted_branch_flow.jl
</context>

<interfaces>
From `src/powerflow/ConvexBranchFlow.jl` (verified, the formulation this plan delegates to):

```
struct ConvexBranchFlow <: AbstractPowerFlow end
function contribute!(::ConvexBranchFlow, ctx::ModelContext, feeder; T::Int = 1)
    # creates m.v[j,t], m.v̂[j,t], m.P[b,t], m.Q[b,t], m.l[b,t]≥0 on ctx.model
    # fixes v[root,:] and v̂[root,:] to 1.0
    # for j in 1:N, t in 1:T; j==feeder.root && continue
    #     set_lower_bound(v[j,t], vb.vmin^2); set_upper_bound(v[j,t], vb.vmax^2)
    #     set_lower_bound(v̂[j,t], vb.vmin^2); set_upper_bound(v̂[j,t], vb.vmax^2)
    # ... rotated SOC cone, vdrop (3.33), cpydrop (3.43), smax cone, :Rp/:Rq accumulation
    ctx.meta[:pf_vars] = (; v, v̂, P, Q, l)   # <- this plan's contribute! reads pv.v from HERE
    return ctx
end
problem_class(::ConvexBranchFlow) = SOCP()
export ConvexBranchFlow
```

From `src/powerflow/ACPowerFlow.jl` (verified, the header/docstring SHAPE this new file mirrors —
"SEAM:"/"OWNER:" header, `problem_class(::ACPowerFlow) = NLP()` trait pattern, and the
"Dispatched through the EXISTING solve_welfare entrypoint with ZERO change to that file" framing).

From `src/TSODSO.jl` (verified, exact include block this plan extends):
```
# line 47: include("powerflow/ConvexBranchFlow.jl")
#
# --- Independent nonconvex AC-OPF oracle (peer formulation, owned by plan 15-01, EXACT-01) ---
# line 52: include("powerflow/ACPowerFlow.jl")
#
# --- Power-flow → problem-class routing trait ... ---
# line 57: include("solver/problem_class_trait.jl")
```
Insert the new `include("powerflow/RestrictedBranchFlow.jl")` between lines 52 and 57 (i.e.
immediately after `ACPowerFlow.jl`, before `problem_class_trait.jl`) — mirrors exactly how
`ACPowerFlow.jl` was inserted after `ConvexBranchFlow.jl`.

From `test/test_ac_oracle.jl` (verified, the EXACT-04 stress test's exact `pv_scale = 1.2` call
shape this plan's regression test reuses):
```
ctx_socp, cost_socp, _ = solve_welfare(feeder, ConvexBranchFlow(), aggs; T = Phase4Fixtures.T,
    λ₀ = λ₀, allow_export = true, rtol_exact = 1.0)   # diagnostic override — returns the inexact point
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RestrictedBranchFlow — delegate + shrink v's own upper bound by ε</name>
  <files>src/powerflow/RestrictedBranchFlow.jl, src/TSODSO.jl</files>
  <read_first>
    - src/powerflow/ConvexBranchFlow.jl (full file — the delegated `contribute!`'s exact
      variable-bound-setting loop, lines 138-145, that this plan's shrink loop mirrors)
    - src/powerflow/ACPowerFlow.jl (full file — the header/docstring/export shape template)
    - src/TSODSO.jl (lines 40-57 — the exact include block this plan extends)
    - .planning/phases/20-overvoltage-capable-relaxation/20-01-SUMMARY.md (the measured
      `ε_measured` value from plan 20-01 Task 2, printed by the `@info` line in
      `test/test_restricted_branch_flow.jl`'s second `@testitem`)
    - .planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md, "Recommended
      construction: RestrictedBranchFlow" (the worked JuMP sketch this plan implements)
  </read_first>
  <action>
    Create `src/powerflow/RestrictedBranchFlow.jl` with a header comment in the `ACPowerFlow.jl`
    shape: SEAM = "Gan-Low OPF-ε restricted-feasible-set formulation (OVR-01)", OWNER = "plan
    20-02", and a paragraph stating this is a genuine RESTRICTION (not a relaxation tightening;
    D-01) dispatched through the EXISTING `solve_welfare` entrypoint with ZERO change to that file.

    Define `struct RestrictedBranchFlow <: AbstractPowerFlow; ε::Float64; end` with an outer
    constructor `RestrictedBranchFlow(; ε::Real = _EXACT04_MEASURED_ε) = RestrictedBranchFlow(Float64(ε))`.
    Above the struct, define `const _EXACT04_MEASURED_ε = <plan 20-01's ε_measured value> * 1.25`
    as a local const (mirrors `ConvexBranchFlow.jl`'s `_SMAX_NO_LIMIT` local-const pattern) — take
    the EXACT printed `ε_measured` value from `20-01-SUMMARY.md`/the test log, multiply by a
    documented `1.25×` safety margin (D-03: measured, not searched; mirrors this project's
    `DSO_BAND_HI = 1.5×max|dso|` precedent), and write a comment directly above the const showing
    the derivation: "measured on the EXACT-04 fixture (test/test_restricted_branch_flow.jl, plan
    20-01 Task 2): ε_measured = <value> pu²; 1.25× safety multiplier ⇒ ε = <computed value> pu²
    (D-03/D-07 provenance — never re-derive without updating this comment)."

    Define `contribute!(pf::RestrictedBranchFlow, ctx::ModelContext, feeder; T::Int = 1)`: first
    line delegates `contribute!(ConvexBranchFlow(), ctx, feeder; T = T)` (identical SOC cone +
    exactness copy + apparent-power cone + balance accumulation — correctness-drift avoidance per
    RESEARCH.md's explicit recommendation to delegate rather than duplicate). Then `pv =
    ctx.meta[:pf_vars]`; for `j in 1:length(feeder.buses), t in 1:T`, `j == feeder.root &&
    continue`, call `set_upper_bound(pv.v[j, t], feeder.buses[j].vmax^2 - pf.ε)` — JuMP allows
    re-tightening an already-bounded variable, so this is a second `set_upper_bound` call on the
    SAME `v[j,t]` `ConvexBranchFlow.contribute!` just bounded, not a new variable. Add an explicit
    comment immediately above this loop: "Gan-Low OPF-ε (Theorem 2/Section IV-D, eq. 18) — shrink
    ONLY v's own bound. Do NOT touch v̂'s bound (ConvexBranchFlow's cpydrop copy, thesis 3.43): v̂
    is a DIFFERENT, LOWER-bound shadow (plan 20-01 Task 1 confirmed v ≥ v̂ numerically) — shrinking
    it would be a no-op at best, a silent double-restriction at worst." After the loop, stash
    `ctx.meta[:restriction_ε] = pf.ε` (D-08 provenance) and `ctx.meta[:formulation] =
    :RestrictedBranchFlow` (D-08 provenance, consumed by plan 20-03's certificate). Return `ctx`.

    Add `problem_class(::RestrictedBranchFlow) = SOCP()` (same tight-gap Clarabel factory
    `ConvexBranchFlow` uses — no solver change) and `export RestrictedBranchFlow`.

    In `src/TSODSO.jl`, add `include("powerflow/RestrictedBranchFlow.jl")` immediately after line
    52's `include("powerflow/ACPowerFlow.jl")` and before line 57's
    `include("solver/problem_class_trait.jl")`, with a one-line comment: "# --- Gan-Low OPF-ε
    restricted formulation (owned by plan 20-02, OVR-01) --- included right after ACPowerFlow.jl:
    it delegates to ConvexBranchFlow.contribute! and must load after it."
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; @assert isdefined(TSODSO, :RestrictedBranchFlow); @assert TSODSO.problem_class(TSODSO.RestrictedBranchFlow()) isa TSODSO.SOCP; println("OK")'</automated>
  </verify>
  <done>src/powerflow/RestrictedBranchFlow.jl exists, exports RestrictedBranchFlow, and src/TSODSO.jl includes it right after ACPowerFlow.jl; `using TSODSO` loads without error.</done>
  <acceptance_criteria>
    - `grep -n 'struct RestrictedBranchFlow' src/powerflow/RestrictedBranchFlow.jl` matches
    - `grep -n 'set_upper_bound(pv.v\[j, t\], feeder.buses\[j\].vmax\^2 - pf.ε)' src/powerflow/RestrictedBranchFlow.jl` matches (or an equivalent expression computing `vmax^2 - pf.ε`)
    - `grep -n 'cpydrop\|set_upper_bound(pv.v̂' src/powerflow/RestrictedBranchFlow.jl` returns NO matches (confirms `v̂`'s bound is never touched)
    - `grep -n 'RestrictedBranchFlow.jl' src/TSODSO.jl` shows it positioned after the `ACPowerFlow.jl` include line and before `problem_class_trait.jl`
    - The verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Free PF-04 validation signal + default-path regression guard</name>
  <files>test/test_restricted_branch_flow.jl</files>
  <read_first>
    - src/models/welfare_solve.jl (lines 240-258 — confirms `assert_socp_exact!` fires
      automatically inside `solve_welfare` whenever `:l` is stashed, at whatever `rtol_exact` the
      caller passes; default `rtol_exact = 1e-4`)
    - src/models/exactness.jl (the `assert_socp_exact!` docstring — its default `rtol=1e-4,
      atol=1e-6`, and `ctx.meta[:socp_maxgap]` as the stashed absolute residual)
    - test/test_ac_oracle.jl (lines 180-277 — the EXACT-04 stress test's fixture-build call shape
      and the documented pre-existing finding: `ConvexBranchFlow` alone requires `rtol_exact = 1.0`
      to avoid `assert_socp_exact!` throwing on this fixture)
    - test/test_restricted_branch_flow.jl (the two `@testitem`s plan 20-01 added — this task adds
      a third and fourth to the SAME file)
  </read_first>
  <action>
    In `test/test_restricted_branch_flow.jl`, add a third `@testitem "restricted_branch_flow:
    RestrictedBranchFlow solves EXACT-04 through solve_welfare at PF-04's DEFAULT tolerance (OVR-01,
    free validation signal)" tags = [:restricted_branch_flow] setup = [Phase4Fixtures]`. Build the
    SAME `feeder`/`aggs` (`pv_scale = 1.2`)/`λ₀` as the first two items. Call
    `ctx, cost, dadp = solve_welfare(feeder, RestrictedBranchFlow(), aggs; T = Phase4Fixtures.T, λ₀
    = λ₀, allow_export = true)` — deliberately WITHOUT any `rtol_exact` override (the DEFAULT
    `1e-4` is what `assert_socp_exact!` uses internally). This call must NOT throw — if it does,
    the restriction did not close the gap and the plan has failed at the most basic level. Assert
    `ctx.meta[:socp_maxgap] < 1e-5` (RESEARCH.md's prediction: the residual should collapse from
    EXACT-04's documented `≈10.4` to the benign-feeder scale `~1e-7`; `1e-5` is a safe
    order-of-magnitude gate, not a tight pin). Print `@info "RestrictedBranchFlow socp_maxgap on
    EXACT-04"` `ctx.meta[:socp_maxgap]`. Also assert `ctx.meta[:formulation] ==
    :RestrictedBranchFlow` and `ctx.meta[:restriction_ε] > 0.0` (the D-08 provenance stash from
    Task 1).

    Add a fourth `@testitem "restricted_branch_flow: plain ConvexBranchFlow on EXACT-04 is
    UNCHANGED by RestrictedBranchFlow's existence (default-path regression)" tags =
    [:restricted_branch_flow] setup = [Phase4Fixtures]`. Reproduce the EXACT body of
    `test/test_ac_oracle.jl`'s EXACT-04 `@testitem` (lines 180-277: build feeder/aggs/λ₀ with
    `pv_scale = 1.2`; solve `ConvexBranchFlow()` with `rtol_exact = 1.0`; solve `ACPowerFlow()`
    twice with the two documented Ipopt attribute variants; assert `isapprox(cost_ac, cost_ac2;
    rtol=1e-3, atol=1e-3)`; call `assert_ac_exact!`; assert `!isempty(inexact_hours)` and the
    voltage-bound-hit/reverse-flow diagnostic). This is a DELIBERATE duplication (not a call into
    the other test file) so that a future accidental edit to `ConvexBranchFlow.jl`'s bound-setting
    loop (the one Task 1 warned against touching) is caught by TWO independent test files, not one
    — cheap insurance for the exact code path Task 1's anti-pattern warning protects.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, JuMP, Test; Thoriz = 24; mem_price = Float64[3.8,3.7,3.6,3.6,3.7,4.0,4.8,5.8,6.5,6.2,5.9,5.7,5.6,5.8,6.0,6.8,8.2,9.0,8.6,7.4,6.2,5.2,4.4,4.0]; temperature = Float64[19,18,17,16,16,17,19,21,23,26,28,30,31,32,32,31,29,27,25,23,22,21,20,19]; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false), Bus(3,0.95,1.05,false)]; branches = [Branch(1,2,0.05,0.05,99.0), Branch(2,3,0.05,0.05,99.0)]; feeder = Feeder(buses, branches, 1); pv_scale = 1.2; aggs = map(2:length(feeder.buses)) do bus; prof = generate_profiles(seed = 20260406 + bus, T = Thoriz); Ppv = Float64[pv_scale * p for p in prof.pv]; Pdc = Float64[0.2 * d for d in prof.demand]; therm = Thermostatic(bus, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, temperature); defer = Deferrable(bus, 8, 16, 1.0, 0.5, 0.5); batt = PVBattery(bus, 0.95, 1.0, 0.1, 0.0, 0.2, 0.1, 3.8, 6.2, 8.9, Ppv); Aggregator(bus, 0.95, [therm, defer, batt], Pdc); end; ctx, cost, dadp = solve_welfare(feeder, RestrictedBranchFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_export = true); @test ctx.meta[:socp_maxgap] < 1e-5; @test ctx.meta[:formulation] == :RestrictedBranchFlow; @test ctx.meta[:restriction_ε] > 0.0; println("socp_maxgap (RestrictedBranchFlow) = ", ctx.meta[:socp_maxgap]); ctx_socp, cost_socp, _ = solve_welfare(feeder, ConvexBranchFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_export = true, rtol_exact = 1.0); ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_local = true, allow_export = true); report = assert_ac_exact!(ctx_socp, ctx_ac; rtol = 1e-4, atol = 1e-6); inexact_hours = [row.t for row in report.hours if !row.exact]; @test !isempty(inexact_hours); println("inexact_hours (plain ConvexBranchFlow, unchanged) = ", inexact_hours)'</automated> -- per-task quick check: plain Test.jl under --project=., inlines the EXACT-04 fixture; asserts the SAME behavioral claims as @testitem 3 (RestrictedBranchFlow default-tolerance solve) and @testitem 4 (ConvexBranchFlow's own inexactness unchanged) — @testitem execution itself is deferred to this plan's closing &lt;verification&gt;
  </verify>
  <done>Four green @testitems in test/test_restricted_branch_flow.jl: sign spot-check, ε measurement, RestrictedBranchFlow solving EXACT-04 at PF-04 default tolerance, and the ConvexBranchFlow default-path regression guard.</done>
  <acceptance_criteria>
    - `grep -c '@testitem' test/test_restricted_branch_flow.jl` returns 4
    - The third item's body contains `RestrictedBranchFlow()` with NO `rtol_exact` kwarg in that
      specific `solve_welfare` call
    - The fourth item's body contains `ConvexBranchFlow()` and `assert_ac_exact!` and asserts
      `!isempty(inexact_hours)` — i.e. still documents the KNOWN inexactness of the plain
      relaxation, proving `RestrictedBranchFlow`'s existence changed nothing about it
    - Running the verify command exits 0 (all `@test` assertions pass; the full four-@testitem run is deferred to this plan's closing `<verification>`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Researcher-supplied `RestrictedBranchFlow(; ε)` kwarg | The only new "input" this plan adds; a researcher-chosen scalar, not attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering | `contribute!`'s shrink loop accidentally touching `v̂`'s bound instead of (or in addition to) `v`'s | mitigate | Task 2's `acceptance_criteria` greps for the absence of any `v̂`-bound edit; the anti-pattern is also called out explicitly in the code comment (Task 1) |
| T-20-05 | Denial of Service | A pathological `ε ≥ (vmax² − vmin²)` collapses the feasible set to empty, making every `RestrictedBranchFlow` solve infeasible | accept | researcher-supplied kwarg on a local research tool, not an attacker-controlled surface; the measured default (plan 20-01) is far below this threshold by construction |
| T-20-06 | Repudiation | Silent drift of `ConvexBranchFlow`'s own behavior caused by the new sibling formulation | mitigate | Task 2's default-path regression `@testitem` re-runs the EXACT `test_ac_oracle.jl` EXACT-04 assertions independently |
</threat_model>

<verification>
Run `julia --project=. -e 'import Pkg; Pkg.test()'` (background, ~12-20 min — the ONLY entrypoint that actually executes `@testitem` bodies: TestItemRunner is test-only, so `--project=.` cannot resolve it, and even under `--project=test` a bare `include` of an `@testitem` file runs ZERO tests). Confirm all four `@testitem`s in `test/test_restricted_branch_flow.jl` pass and `test/test_ac_oracle.jl`'s own EXACT-04 item is still green in the same run.
</verification>

<success_criteria>
- `RestrictedBranchFlow` solves the EXACT-04 fixture through `solve_welfare` and passes
  `assert_socp_exact!` at its DEFAULT tolerance (no diagnostic override).
- `ConvexBranchFlow`'s own EXACT-04 behavior (the documented inexactness finding) is unchanged.
- D-08's provenance stash (`ctx.meta[:formulation]`, `ctx.meta[:restriction_ε]`) is present and
  populated on every `RestrictedBranchFlow` solve.
</success_criteria>

<output>
Create `.planning/phases/20-overvoltage-capable-relaxation/20-02-SUMMARY.md` when done. Record the
observed `ctx.meta[:socp_maxgap]` value (Task 2) — plan 20-03's certificate tolerance derivation
needs a fixture-scale reference point.
</output>
