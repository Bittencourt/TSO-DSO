---
phase: 20-overvoltage-capable-relaxation
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - src/models/restriction_exactness.jl
  - src/TSODSO.jl
  - test/test_restricted_branch_flow.jl
autonomous: true
requirements: [OVR-02]

must_haves:
  truths:
    - "A NEW, named, exported certificate — assert_restriction_exact! — peer to assert_socp_exact!/assert_ac_exact!, certifies the RestrictedBranchFlow solution AC-feasible via the existing AC oracle AND reports the optimality loss vs the unrestricted (inexact) SOCP bound in the SAME call (D-05)."
    - "The certificate throws by default and neutralizes to a diagnostic via report=true (D-06), matching the assert_4q_complementarity! contract."
    - "The certificate's rtol/atol are MEASURED on the actual EXACT-04 restricted-vs-AC residual, never copied from assert_ac_exact!'s or assert_socp_exact!'s defaults (D-07)."
    - "On certification, a provenance marker distinguishing restricted-convex-dual prices from plain-SOCP or fallback prices is stashed on the context (D-08)."
  artifacts:
    - path: "src/models/restriction_exactness.jl"
      provides: "assert_restriction_exact!(ctx_restricted, ctx_ac; rtol, atol, unrestricted_cost, report) -> (; ac_feasible, optimality_loss, obj_gap, hours)"
      contains: "function assert_restriction_exact!"
  key_links:
    - from: "src/models/restriction_exactness.jl"
      to: "src/models/ac_oracle.jl"
      via: "assert_restriction_exact! calls assert_ac_exact!(ctx_restricted, ctx_ac; rtol, atol) internally, reusing its per-hour comparison loop with FRESH tolerance values"
      pattern: "assert_ac_exact!\\(ctx_restricted"
    - from: "src/models/restriction_exactness.jl"
      to: "src/powerflow/RestrictedBranchFlow.jl"
      via: "on certification success/failure, stashes ctx_restricted.meta[:price_provenance] built on top of the Task-1-stashed ctx.meta[:formulation]"
      pattern: "price_provenance"
---

<objective>
Implement OVR-02: a new, named, exported certificate `assert_restriction_exact!` that certifies a
solved `RestrictedBranchFlow` context AC-feasible (via the existing AC oracle) and reports the
optimality loss versus the unrestricted (inexact) SOCP bound — in ONE call, with its OWN measured
tolerances, never a reused tolerance (D-05/D-06/D-07).

Purpose: this is the phase's headline validity gate. D-01..D-04's restriction only matters if it
can be independently certified — this certificate is that proof, peer to `assert_socp_exact!`
(the SOCP-cone gate) and `assert_ac_exact!` (the AC-cross-check comparator this certificate
internally reuses with FRESH, self-derived tolerance values rather than duplicating its per-hour
loop).

Output: `src/models/restriction_exactness.jl` (new), one new `TSODSO.jl` include, and
`test/test_restricted_branch_flow.jl` gains the certificate's throw/report/optimality-loss tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-overvoltage-capable-relaxation/20-CONTEXT.md
@.planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md
@.planning/phases/20-overvoltage-capable-relaxation/20-PATTERNS.md
@.planning/phases/20-overvoltage-capable-relaxation/20-02-SUMMARY.md

@src/models/ac_oracle.jl
@src/models/exactness.jl
@src/models/complementarity_4q.jl
@src/powerflow/RestrictedBranchFlow.jl
@test/test_restricted_branch_flow.jl
</context>

<interfaces>
From `src/models/ac_oracle.jl` (verified, the comparator this new certificate wraps — NEVER
duplicate its per-hour loop, call it with fresh `rtol`/`atol` instead):
```
function assert_ac_exact!(ctx_socp::ModelContext, ctx_ac::ModelContext; rtol::Real = 1e-4,
                           atol::Real = 1e-6) -> (; obj_gap, hours)
    # hours::Vector{NamedTuple} — one row (; t, vgap, pgap, qgap, exact) per hour
    # NEVER throws on a numeric gap; throws ONLY on a structural T mismatch
end
```

From `src/models/complementarity_4q.jl` (verified, the throw/report contract this new certificate
copies verbatim):
```
if report
    @warn msg
else
    error(msg)
end
```

From `src/powerflow/RestrictedBranchFlow.jl` (this plan's own prior wave, verified by plan 20-02):
`contribute!` stashes `ctx.meta[:formulation] = :RestrictedBranchFlow` and
`ctx.meta[:restriction_ε] = pf.ε` — this plan's certificate reads/extends that stash.

From `src/models/welfare_solve.jl`/`test/test_ac_oracle.jl` (verified, the `rtol_exact = 1.0`
diagnostic-override call shape that produces the "unrestricted (inexact) SOCP bound" D-05
requires for the optimality-loss comparison):
```
ctx_unrestricted, cost_unrestricted, _ = solve_welfare(feeder, ConvexBranchFlow(), aggs; T = ...,
    λ₀ = λ₀, allow_export = true, rtol_exact = 1.0)   # PF-04 neutralized so the loose bound returns
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: assert_restriction_exact! — AC-feasibility + optimality-loss + provenance</name>
  <files>src/models/restriction_exactness.jl, src/TSODSO.jl</files>
  <read_first>
    - src/models/ac_oracle.jl (full file — `assert_ac_exact!`'s exact signature/return shape this
      new certificate calls internally)
    - src/models/complementarity_4q.jl (full file — the throw/report neutralization pattern AND
      the measured-tolerance docstring provenance-table convention this new certificate's docstring
      must follow)
    - src/models/exactness.jl (the `assert_socp_exact!` docstring — sibling certificate style)
    - src/TSODSO.jl (lines 72-92 — confirms `models/ac_oracle.jl` is included at line 91; this new
      file's include must come AFTER it)
    - .planning/phases/20-overvoltage-capable-relaxation/20-02-SUMMARY.md (the observed
      `ctx.meta[:socp_maxgap]` value from plan 20-02 — the scale reference for this plan's own
      tolerance measurement)
  </read_first>
  <action>
    Create `src/models/restriction_exactness.jl` with a header comment in the
    `complementarity_4q.jl` shape: SEAM = "restricted-SOCP AC-feasibility + optimality-loss
    certificate (OVR-02)", OWNER = "plan 20-03", naming the peer certificates
    (`assert_socp_exact!`, `assert_ac_exact!`, `assert_4q_complementarity!`) and stating up front
    that its `rtol`/`atol` are MEASURED on THIS comparison (`RestrictedBranchFlow` vs
    `ACPowerFlow`), never copied from either peer (D-07/certificate-laundering guard).

    Define `assert_restriction_exact!(ctx_restricted::ModelContext, ctx_ac::ModelContext; rtol::Real
    = <measured>, atol::Real = <measured>, unrestricted_cost::Union{Real,Nothing} = nothing,
    report::Bool = false) -> (; ac_feasible::Bool, optimality_loss::Union{Float64,Nothing},
    obj_gap::Float64, hours::Vector{NamedTuple})`. Implementation: call `report_ac =
    assert_ac_exact!(ctx_restricted, ctx_ac; rtol = rtol, atol = atol)` — REUSING the existing
    per-hour comparison loop (never re-implement it) but supplying THIS function's own,
    freshly-derived `rtol`/`atol` (not `assert_ac_exact!`'s defaults). Compute `ac_feasible =
    all(row.exact for row in report_ac.hours)`. Compute `optimality_loss = unrestricted_cost ===
    nothing ? nothing : objective_value(ctx_restricted.model) - unrestricted_cost` — a NAMED field,
    never silently folded into `obj_gap`. Stash the D-08 provenance marker UNCONDITIONALLY (both on
    pass and fail, so the marker always reflects reality):
    `ctx_restricted.meta[:price_provenance] = (; formulation = :RestrictedBranchFlow, certificate =
    :assert_restriction_exact!, status = ac_feasible ? :certified_convex_dual : :cert_failed)`. If
    `!ac_feasible`: build a loud message naming `rtol`/`atol`/the worst per-hour gap from
    `report_ac.hours` and the phase citation ("Gan-Low OPF-ε, Theorem 2; OVR-02"); if `report`,
    `@warn` it (this is D-09's trigger point — the CALLER is responsible for invoking the fallback
    only after seeing `ac_feasible == false` from a `report = true` call, never automatically
    inside this function); else `error(msg)`. Return `(; ac_feasible, optimality_loss,
    obj_gap = report_ac.obj_gap, hours = report_ac.hours)` in EITHER path (report mode always
    returns the full diagnostic, mirroring `assert_4q_complementarity!`'s "return a diagnostic on
    success" contract).

    Derive `rtol`/`atol` by ACTUALLY measuring: on the EXACT-04 fixture (feeder/aggs `pv_scale =
    1.2`), solve `ctx_restricted, cost_restricted, _ = solve_welfare(feeder,
    RestrictedBranchFlow(), aggs; T = Phase4Fixtures.T, λ₀ = λ₀, allow_export = true)` and
    `ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T = Phase4Fixtures.T, λ₀ = λ₀,
    allow_local = true, allow_export = true)`, call the file's OWN `assert_ac_exact!(ctx_restricted,
    ctx_ac; rtol = 1e-4, atol = 1e-6)` diagnostically to READ the observed `vgap`/`pgap`/`qgap`
    scale (RESEARCH.md predicts this collapses to `~1e-6`-`~1e-7`, matching Theorem 2's exactness
    prediction). Set the function's default `rtol`/`atol` to roughly one order of magnitude above
    THAT measured floor (mirroring `assert_4q_complementarity!`'s "~16× above the measured floor"
    sizing discipline) — write the EXACT observed numbers into the docstring as a provenance table
    in the same shape as `complementarity_4q.jl`'s "Tolerance provenance (D-07...)" section,
    explicitly contrasting them against `assert_ac_exact!`'s `1e-4`/`1e-6` defaults to show they
    were NOT copied.

    In `src/TSODSO.jl`, add `include("models/restriction_exactness.jl")` immediately after line
    91's `include("models/ac_oracle.jl")` (this file calls `assert_ac_exact!`, so it must load
    after it), with a one-line comment noting OWNER = plan 20-03, OVR-02.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; @assert isdefined(TSODSO, :assert_restriction_exact!); println("OK")'</automated>
  </verify>
  <done>src/models/restriction_exactness.jl exists, exports assert_restriction_exact!, and its default rtol/atol are documented as measured on the actual EXACT-04 restricted-vs-AC residual (not copied from any other certificate).</done>
  <acceptance_criteria>
    - `grep -n 'function assert_restriction_exact!' src/models/restriction_exactness.jl` matches once
    - `grep -n 'assert_ac_exact!(ctx_restricted' src/models/restriction_exactness.jl` matches (confirms reuse, not reimplementation, of the per-hour loop)
    - `grep -n 'rtol::Real = 1e-4, atol::Real = 1e-6' src/models/restriction_exactness.jl` returns NO matches (confirms the defaults are NOT `assert_ac_exact!`'s literal copied values)
    - `grep -n 'price_provenance' src/models/restriction_exactness.jl` matches
    - The verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Certificate tests — throw/report polarities + optimality-loss field</name>
  <files>test/test_restricted_branch_flow.jl</files>
  <read_first>
    - src/models/restriction_exactness.jl (this plan's own Task 1 output — exact signature/defaults)
    - test/test_ac_oracle.jl (lines 80-130 — the "reports all-exact, never throws, never resolves
      to a Bool" test shape this plan's positive-path test mirrors)
    - test/test_restricted_branch_flow.jl (the four existing `@testitem`s from plans 20-01/20-02)
  </read_first>
  <action>
    In `test/test_restricted_branch_flow.jl`, add a fifth `@testitem "restricted_branch_flow:
    assert_restriction_exact! certifies AC-feasible on EXACT-04 and reports optimality loss (D-05)"
    tags = [:restricted_branch_flow] setup = [Phase4Fixtures]`. Build the standard EXACT-04
    fixture; solve `ctx_restricted` via `RestrictedBranchFlow()` and `ctx_ac` via `ACPowerFlow()`
    (both `allow_export = true`, `ctx_ac` also `allow_local = true`); ALSO solve the unrestricted
    diagnostic bound `ctx_unrestricted, cost_unrestricted, _ = solve_welfare(feeder,
    ConvexBranchFlow(), aggs; T = Phase4Fixtures.T, λ₀ = λ₀, allow_export = true, rtol_exact =
    1.0)`. Call `report = assert_restriction_exact!(ctx_restricted, ctx_ac; unrestricted_cost =
    cost_unrestricted)` — no `report = true` here, so a non-throwing return IS the assertion that
    certification passed. Assert `report.ac_feasible == true`, `report.optimality_loss !== nothing`,
    `report isa NamedTuple`, `!(report isa Bool)`. Assert
    `ctx_restricted.meta[:price_provenance].status == :certified_convex_dual`. Print `@info
    "restriction optimality loss vs unrestricted SOCP bound"` `report.optimality_loss`.

    Add a sixth `@testitem "restricted_branch_flow: assert_restriction_exact! throws by default and
    neutralizes under report=true (D-06)" tags = [:restricted_branch_flow] setup =
    [Phase4Fixtures]`. Construct a DELIBERATELY-mismatched pair — reuse the `fixed_ctx(T)` helper
    shape from `test/test_ac_oracle.jl` lines 152-169 (two fixed-value contexts differing only in
    horizon `T`, one `T=1` one `T=2`) so the underlying `assert_ac_exact!` structural guard fires
    (a clean, deterministic way to force a certificate failure without depending on solver
    behavior). Assert `@test_throws Exception assert_restriction_exact!(ctx1, ctx2)` (default,
    throws). Then assert calling it again with `report = true` on the SAME mismatched pair either
    (a) also throws (if the underlying T-mismatch is itself a hard structural guard `assert_ac_exact!`
    never neutralizes) — in which case add a comment explaining that `report=true` neutralizes
    AC-INFEASIBILITY findings, not STRUCTURAL mismatches, mirroring `assert_ac_exact!`'s own
    T-mismatch-is-never-neutralized contract — OR (b) if `report=true` is reached, assert it returns
    a `NamedTuple` with `ac_feasible == false` rather than raising. Pick whichever branch the
    actual `assert_ac_exact!` T-mismatch guard produces (read its source in `src/models/ac_oracle.jl`
    to confirm before writing the assertion) and assert that behavior explicitly — do not guess.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, JuMP, Test; Thoriz = 24; mem_price = Float64[3.8,3.7,3.6,3.6,3.7,4.0,4.8,5.8,6.5,6.2,5.9,5.7,5.6,5.8,6.0,6.8,8.2,9.0,8.6,7.4,6.2,5.2,4.4,4.0]; temperature = Float64[19,18,17,16,16,17,19,21,23,26,28,30,31,32,32,31,29,27,25,23,22,21,20,19]; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false), Bus(3,0.95,1.05,false)]; branches = [Branch(1,2,0.05,0.05,99.0), Branch(2,3,0.05,0.05,99.0)]; feeder = Feeder(buses, branches, 1); pv_scale = 1.2; aggs = map(2:length(feeder.buses)) do bus; prof = generate_profiles(seed = 20260406 + bus, T = Thoriz); Ppv = Float64[pv_scale * p for p in prof.pv]; Pdc = Float64[0.2 * d for d in prof.demand]; therm = Thermostatic(bus, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, temperature); defer = Deferrable(bus, 8, 16, 1.0, 0.5, 0.5); batt = PVBattery(bus, 0.95, 1.0, 0.1, 0.0, 0.2, 0.1, 3.8, 6.2, 8.9, Ppv); Aggregator(bus, 0.95, [therm, defer, batt], Pdc); end; ctx_restricted, cost_restricted, _ = solve_welfare(feeder, RestrictedBranchFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_export = true); ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_local = true, allow_export = true); ctx_unrestricted, cost_unrestricted, _ = solve_welfare(feeder, ConvexBranchFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_export = true, rtol_exact = 1.0); report = assert_restriction_exact!(ctx_restricted, ctx_ac; unrestricted_cost = cost_unrestricted); @test report.ac_feasible == true; @test report.optimality_loss !== nothing; @test ctx_restricted.meta[:price_provenance].status == :certified_convex_dual; println("optimality_loss = ", report.optimality_loss); N2, B2 = 2, 1; feeder2 = Feeder([Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false)], [Branch(1, 2, 0.01, 0.02, 10.0)], 1); function fixed_ctx(Tm); m = Model(select_optimizer(LP())); @variable(m, v[1:N2, 1:Tm]); @variable(m, P[1:B2, 1:Tm]); @variable(m, Q[1:B2, 1:Tm]); @variable(m, l[1:B2, 1:Tm]); fix.(v, 1.0; force = true); fix.(P, 0.0; force = true); fix.(Q, 0.0; force = true); fix.(l, 0.0; force = true); @objective(m, Max, 0); optimize!(m); ctxm = TSODSO.ModelContext(m); ctxm.meta[:feeder] = feeder2; ctxm.meta[:T] = Tm; ctxm.meta[:pf_vars] = (; v, P, Q, l); return ctxm; end; ctx1 = fixed_ctx(1); ctx2 = fixed_ctx(2); @test_throws Exception assert_restriction_exact!(ctx1, ctx2); println("OK: certificate throws on structural T-mismatch")'</automated> -- per-task quick check: plain Test.jl under --project=., asserts the SAME behavioral claims as @testitem 5 (certificate positive path + optimality loss) and @testitem 6 (throw on structural mismatch) — the report=true neutralization polarity and the full six-@testitem run are deferred to this plan's closing &lt;verification&gt;
  </verify>
  <done>Six green @testitems total in test/test_restricted_branch_flow.jl, including the certificate's positive-path (AC-feasible, optimality loss reported) and throw/report polarity tests.</done>
  <acceptance_criteria>
    - `grep -c '@testitem' test/test_restricted_branch_flow.jl` returns 6
    - The fifth item asserts `report.ac_feasible == true` and `report.optimality_loss !== nothing`
    - The sixth item contains `@test_throws Exception assert_restriction_exact!`
    - Running the verify command exits 0 (all `@test` assertions pass; the full six-@testitem run is deferred to this plan's closing `<verification>`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `unrestricted_cost` kwarg (researcher-supplied `Float64`/`nothing`) | Only new input; a local numeric value the researcher computes and passes, not attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | Certificate silently reuses `assert_ac_exact!`'s DEFAULT `rtol=1e-4, atol=1e-6` instead of its own measured values (certificate-laundering, PITFALLS.md Pitfall 1) | mitigate | Task 1's acceptance criteria greps explicitly for the ABSENCE of the literal `1e-4`/`1e-6` default pair as this function's own defaults |
| T-20-08 | Repudiation | `price_provenance` marker not updated on a FAILED certification, leaving a stale `:certified_convex_dual` stash from a prior successful call on the same reused `ctx` | mitigate | Task 1 stashes `price_provenance` UNCONDITIONALLY (both branches), never only on success |
| T-20-09 | Denial of Service | `unrestricted_cost = nothing` (default) silently produces `optimality_loss = nothing`, and a caller downstream treats `nothing` as `0.0` | accept | documented named-field contract (`Union{Float64,Nothing}`); the D-05 requirement is satisfied by returning `nothing` explicitly rather than a silently-wrong number |
</threat_model>

<verification>
Run `julia --project=. -e 'import Pkg; Pkg.test()'` (background, ~12-20 min — the ONLY entrypoint that actually executes `@testitem` bodies: TestItemRunner is test-only, so `--project=.` cannot resolve it, and even under `--project=test` a bare `include` of an `@testitem` file runs ZERO tests). Confirm all six `@testitem`s in `test/test_restricted_branch_flow.jl` pass and `test/test_ac_oracle.jl` is still green in the same run (no shared file was touched by this plan besides `src/TSODSO.jl`'s include list and the new certificate file).
</verification>

<success_criteria>
- `assert_restriction_exact!` exists, is exported, throws by default, neutralizes under
  `report=true`, and its `rtol`/`atol` are documented as measured on the actual restricted-vs-AC
  residual.
- The certificate returns a NAMED optimality-loss field, never folding it into `obj_gap`.
- The D-08 provenance marker is stashed correctly on both success and failure.
</success_criteria>

<output>
Create `.planning/phases/20-overvoltage-capable-relaxation/20-03-SUMMARY.md` when done. Record the
measured `rtol`/`atol` values and the observed EXACT-04 restricted-vs-AC residual scale — plan
20-04's fallback and plan 20-05's literate page both cite this.
</output>
