---
phase: 20-overvoltage-capable-relaxation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_restricted_branch_flow.jl
  - src/models/ac_oracle.jl
autonomous: true
requirements: [OVR-01]

must_haves:
  truths:
    - "The existing thesis exactness copy (v̂, 3.43/3.45) is numerically confirmed to be a LOWER-bound shadow (v ≥ v̂ everywhere on a solved EXACT-04 point), falsifying or confirming RESEARCH.md Assumption A1 BEFORE any restriction code is written (D-01)."
    - "A measured, citable Gan–Low 'modification gap' ε exists, derived from an actual AC-oracle-solved point on the EXACT-04 fixture — never guessed, never bisection-searched (D-03, D-04)."
  artifacts:
    - path: "test/test_restricted_branch_flow.jl"
      provides: "New test file; sign-relationship spot-check @testitem + ε-measurement @testitem"
      contains: "@testitem"
    - path: "src/models/ac_oracle.jl"
      provides: "recover_lossfree_shadow_voltage(ctx) -> Matrix{Float64}, the Gan–Low v̂_GL post-processing helper"
      contains: "recover_lossfree_shadow_voltage"
  key_links:
    - from: "test/test_restricted_branch_flow.jl"
      to: "src/models/ac_oracle.jl"
      via: "TSODSO.recover_lossfree_shadow_voltage(ctx_ac) call"
      pattern: "recover_lossfree_shadow_voltage"
    - from: "test/test_restricted_branch_flow.jl"
      to: "test/fixtures_phase4.jl"
      via: "setup=[Phase4Fixtures]; Phase4Fixtures.high_pv_feeder()/build_high_pv_aggregators(feeder; pv_scale=1.2)"
      pattern: "Phase4Fixtures"
---

<objective>
Resolve the two research-flagged numeric spot-checks BEFORE any restriction code exists, and produce
the one new piece of infrastructure the restriction mechanism needs: a pure post-processing function
that computes Gan–Low's loss-free "shadow voltage" `v̂_GL(s)` from an already-solved `ACPowerFlow`
context, plus the measured Gan–Low "modification gap" `ε` this phase's `RestrictedBranchFlow` (plan
20-02) will use as its default shrink amount.

Purpose: RESEARCH.md's Assumption A1 (the existing thesis `v̂` is a LOWER-bound shadow, `v ≥ v̂`,
making its own upper bound non-binding in the over-voltage regime — the reason a NEW mechanism is
needed) is a solid symbolic derivation but explicitly flagged as NOT yet numerically spot-checked.
Confirming it live, on the actual EXACT-04 solved point, is the mandatory first gate (RESEARCH.md:
"if this check fails, the derivation above has an error and must be redone before proceeding").
D-03 additionally requires the restriction's shrink parameter to be a MEASURED default (never
searched) — this plan produces that number.

Output: `test/test_restricted_branch_flow.jl` (new — the sign-relationship regression + the
ε-measurement regression, both permanent CI evidence, not throwaway scripts) and
`src/models/ac_oracle.jl` gains `recover_lossfree_shadow_voltage`, the one genuinely-new piece of
math this phase needs (peer to the file's existing `recover_voltage_angles`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-overvoltage-capable-relaxation/20-CONTEXT.md
@.planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md
@.planning/phases/20-overvoltage-capable-relaxation/20-PATTERNS.md

@src/models/ac_oracle.jl
@src/powerflow/ConvexBranchFlow.jl
@test/test_ac_oracle.jl
@test/fixtures_phase4.jl
</context>

<interfaces>
From `src/models/ac_oracle.jl` (existing, verified from source — the analog this plan's new function mirrors):

```
function recover_voltage_angles(ctx::ModelContext) -> Matrix{ComplexF64}
    # reads ctx.meta[:feeder], ctx.meta[:T], ctx.meta[:pf_vars]; no JuMP var created, no solve.
    # signed bidirectional adjacency: children[bus] :: Vector{Tuple{Int,Int}} (neighbor, ±branch)
```

`ctx.meta[:pf_vars]` stash shapes (both verified from source):
  - `ConvexBranchFlow` / `RestrictedBranchFlow`: `(; v, v̂, P, Q, l)`
  - `ACPowerFlow`: `(; v, P, Q, l)` (no `v̂`)

`Feeder`/`Bus`/`Branch` (from `src/data/Feeder.jl`, verified):
  - `Bus{T}(id::Int, vmin::T, vmax::T, is_root::Bool)`
  - `Branch{T}(from::Int, to::Int, r::T, x::T, smax::T)`
  - `Feeder{T}(buses, branches, root::Int)` — `feeder.buses`, `feeder.branches`, `feeder.root`

`Phase4Fixtures` (test/fixtures_phase4.jl, a TestItems `@testmodule`, verified exports):
  `T`, `mem_price_profile`, `high_pv_feeder()`, `build_high_pv_aggregators(feeder; seed=20260406, pv_scale=0.5)`.
  `high_pv_feeder()` is the actual EXACT-04 substrate: a 3-bus radial fixture (root + 2 buses,
  `r=x=0.05` branches) — NOT a 13-bus network despite the "IEEE-13" shorthand elsewhere in this
  project (RESEARCH.md Open Question 1). Use "the EXACT-04 fixture" / "the high-PV stress fixture"
  in all new code/comments this plan adds, not "IEEE-13".

`solve_welfare` signature (src/models/welfare_solve.jl, verified):
  `solve_welfare(feeder, pf::AbstractPowerFlow, aggregators; T, λ₀, optimizer=select_optimizer(problem_class(pf)), allow_local=false, τ=..., rtol_exact=1e-4, allow_export=false) -> (ctx, objective, dadp)`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: v ≥ v̂ sign-relationship spot-check on a solved EXACT-04 point</name>
  <files>test/test_restricted_branch_flow.jl</files>
  <read_first>
    - test/test_ac_oracle.jl (lines 180-277: the EXACT-04 `@testitem` — `setup=[Phase4Fixtures]`
      pattern, the `rtol_exact = 1.0` diagnostic-override call shape, the `pv_scale = 1.2` operating
      point)
    - test/fixtures_phase4.jl (lines 176-241: `high_pv_feeder`, `build_high_pv_aggregators`)
    - src/powerflow/ConvexBranchFlow.jl (lines 116-234: `contribute!` — confirms `ctx.meta[:pf_vars]
      = (; v, v̂, P, Q, l)`, the `cpydrop` constraint (3.43) this spot-check is falsifying/confirming
      the sign of)
    - .planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md, the section "Why the
      *existing* thesis exactness copy (`v̂`, 3.43/3.45) does not close this gap" (the symbolic
      derivation `v[j] ≥ v̂[j]` this task numerically spot-checks)
  </read_first>
  <action>
    Create `test/test_restricted_branch_flow.jl`. Add a file header comment in the project's
    established style (see `test/test_ac_oracle.jl` lines 1-10 for the shape): name the SEAM
    (OVR-01..04), state every item name contains "restricted_branch_flow" so
    `occursin("restricted_branch_flow", ti.name)` selects the whole file, and flag this first
    `@testitem` as the BLOCKING analytic spot-check RESEARCH.md requires before any
    `RestrictedBranchFlow` code exists.

    Add a `@testitem "restricted_branch_flow: v ≥ v̂ sign-relationship spot-check on the EXACT-04
    fixture (RESEARCH Assumption A1)" tags = [:restricted_branch_flow] setup = [Phase4Fixtures]`
    block. Inside: build `feeder = Phase4Fixtures.high_pv_feeder()`,
    `aggs = Phase4Fixtures.build_high_pv_aggregators(feeder; pv_scale = 1.2)`,
    `λ₀ = Phase4Fixtures.mem_price_profile()`. Call
    `solve_welfare(feeder, ConvexBranchFlow(), aggs; T = Phase4Fixtures.T, λ₀ = λ₀, allow_export =
    true, rtol_exact = 1.0)` — the SAME `rtol_exact = 1.0` diagnostic override
    `test/test_ac_oracle.jl`'s EXACT-04 item uses, so the inexact SOCP solution is returned for
    inspection instead of refused (this is a read of `v`/`v̂`, not a claim about exactness). Read
    `pv = ctx.meta[:pf_vars]`, `N = length(feeder.buses)`. Compute
    `mingap = minimum(value(pv.v[j, t]) - value(pv.v̂[j, t]) for j in 1:N, t in 1:Phase4Fixtures.T)`.
    Assert `mingap >= -1e-9` (a small numerical-noise floor around the theoretical `v ≥ v̂`, i.e.
    `v - v̂ ≥ 0` everywhere) via `@test mingap >= -1e-9`. Print `mingap` via `@info "v-v̂ min gap"
    mingap` so the value is citable later (D-12's literate page). Add a comment directly above the
    assertion: "RESEARCH.md Assumption A1: the existing thesis exactness copy is a LOWER-bound
    shadow (v ≥ v̂), opposite Gan-Low's UPPER-bound shadow — this is the reason Phase 20 needs a
    genuinely new mechanism, not an adjustment of the existing v̂ bound. If this assertion ever
    fails, STOP: the derivation in 20-RESEARCH.md has an error and every downstream plan (20-02..05)
    needs re-deriving before proceeding."
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, JuMP, Test; Thoriz = 24; mem_price = Float64[3.8,3.7,3.6,3.6,3.7,4.0,4.8,5.8,6.5,6.2,5.9,5.7,5.6,5.8,6.0,6.8,8.2,9.0,8.6,7.4,6.2,5.2,4.4,4.0]; temperature = Float64[19,18,17,16,16,17,19,21,23,26,28,30,31,32,32,31,29,27,25,23,22,21,20,19]; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false), Bus(3,0.95,1.05,false)]; branches = [Branch(1,2,0.05,0.05,99.0), Branch(2,3,0.05,0.05,99.0)]; feeder = Feeder(buses, branches, 1); pv_scale = 1.2; aggs = map(2:length(feeder.buses)) do bus; prof = generate_profiles(seed = 20260406 + bus, T = Thoriz); Ppv = Float64[pv_scale * p for p in prof.pv]; Pdc = Float64[0.2 * d for d in prof.demand]; therm = Thermostatic(bus, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, temperature); defer = Deferrable(bus, 8, 16, 1.0, 0.5, 0.5); batt = PVBattery(bus, 0.95, 1.0, 0.1, 0.0, 0.2, 0.1, 3.8, 6.2, 8.9, Ppv); Aggregator(bus, 0.95, [therm, defer, batt], Pdc); end; ctx, cost, dadp = solve_welfare(feeder, ConvexBranchFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_export = true, rtol_exact = 1.0); pv = ctx.meta[:pf_vars]; N = length(feeder.buses); mingap = minimum(value(pv.v[j, t]) - value(pv.v̂[j, t]) for j in 1:N, t in 1:Thoriz); @test mingap >= -1e-9; println("mingap = ", mingap)'</automated> -- per-task quick check: plain Test.jl under --project=., inlines the EXACT-04 fixture verbatim from test/fixtures_phase4.jl (TestItemRunner is test-only and cannot run under --project=.; @testitem execution is deferred to this plan's closing &lt;verification&gt;)
  </verify>
  <done>test/test_restricted_branch_flow.jl exists with one green @testitem confirming `v[j,t] - v̂[j,t] >= -1e-9` for every bus/hour on the EXACT-04 fixture.</done>
  <acceptance_criteria>
    - `grep -c '@testitem' test/test_restricted_branch_flow.jl` returns at least 1
    - The @testitem's body contains both `Phase4Fixtures.high_pv_feeder()` and `pv_scale = 1.2`
    - Running the verify command exits 0 (the `@test` assertion does not throw) and prints
      a `mingap = ...` line with the measured value
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: recover_lossfree_shadow_voltage helper + measured ε on the EXACT-04 fixture</name>
  <files>src/models/ac_oracle.jl, test/test_restricted_branch_flow.jl</files>
  <read_first>
    - src/models/ac_oracle.jl (full file — `recover_voltage_angles`'s docstring/BFS-adjacency shape
      is the DIRECT template; `assert_ac_exact!`'s position in the file, so the new function lands
      BEFORE it)
    - .planning/phases/20-overvoltage-capable-relaxation/20-RESEARCH.md, section "Measuring ε"
      (the exact recursion: `AccumLossR[j] := Σ r_b'·l[b']` over branches strictly downstream of
      `j`; `P̌[b] = P[b] + AccumLossR[to]`; `v̂_GL[to] = v̂_GL[from] − 2·(r·P̌ + x·Q̌)`;
      `v̂_GL[root] = v[root]`; `ε := max_{j,t} (v̂_GL[j,t] − v[j,t])`)
    - src/powerflow/ACPowerFlow.jl (lines 120-211: confirms `ctx.meta[:pf_vars] = (; v, P, Q, l)`,
      NO `v̂` — the AC oracle's stash shape this new function reads)
  </read_first>
  <action>
    In `src/models/ac_oracle.jl`, add a new function `recover_lossfree_shadow_voltage(ctx::ModelContext)
    -> Matrix{Float64}`, positioned immediately AFTER `recover_voltage_angles` and BEFORE
    `assert_ac_exact!`. Give it a docstring in the same shape as `recover_voltage_angles`'s (state
    it is pure post-processing: reads `ctx.meta[:feeder]`/`[:T]`/`[:pf_vars]` only, creates no JuMP
    variable, invokes no solver, writes nothing back to `ctx`), citing Gan, Li, Topcu & Low 2015
    Definition 3 / eq. (18) and this project's RESEARCH.md "Measuring ε" section.

    Implementation: build a ROOTED parent/child tree via ONE breadth-first traversal from
    `feeder.root` — this is a DIFFERENT adjacency shape than `recover_voltage_angles`'s signed
    bidirectional `children` list, because this function walks each bus's SUBTREE, not just
    immediate neighbors. Produce three structures: `order::Vector{Int}` (BFS visit order, root
    first), `children_of::Vector{Vector{Int}}` (bus -> list of tree-child buses), and
    `branch_of_child::Dict{Int,Int}` (child bus -> the branch index connecting it to its tree
    parent). For each `t in 1:T`: (1) reverse-BFS loss accumulation — initialize
    `AccumLossR, AccumLossX = zeros(N), zeros(N)`; iterate `i in reverse(order)`; for each
    `c in children_of[i]` with `b = branch_of_child[c]`, `br = feeder.branches[b]`:
    `AccumLossR[i] += br.r * value(pv.l[b, t]) + AccumLossR[c]` and the `x`-analog into
    `AccumLossX[i]`. (2) forward recursion — `v̂_GL[feeder.root, t] = value(pv.v[feeder.root, t])`;
    for each non-root `i in order` (skip the root, first element) with `b = branch_of_child[i]`,
    `br = feeder.branches[b]`: `P̌ = value(pv.P[b, t]) + AccumLossR[i]`,
    `Q̌ = value(pv.Q[b, t]) + AccumLossX[i]`,
    `v̂_GL[i, t] = v̂_GL[br.from, t] - 2 * (br.r * P̌ + br.x * Q̌)`. Return the `(N, T)` `Matrix{Float64}`.
    Add `recover_lossfree_shadow_voltage` to this file's final `export` line (alongside
    `recover_voltage_angles, assert_ac_exact!`). Update the file's header comment to note the OWNER
    of this addition as "plan 20-01" and its SEAM as "OVR-01/OVR-03 modification-gap measurement".

    Then, in `test/test_restricted_branch_flow.jl`, add a second `@testitem
    "restricted_branch_flow: measured Gan-Low modification gap ε on the EXACT-04 fixture (D-03)"
    tags = [:restricted_branch_flow] setup = [Phase4Fixtures]`. Build the SAME
    `feeder`/`aggs`/`λ₀` as Task 1 (pv_scale = 1.2). Solve
    `ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T = Phase4Fixtures.T, λ₀ = λ₀,
    allow_local = true, allow_export = true)` — a genuine AC-feasible operating point (the
    RESEARCH.md "Measuring ε" recipe step 1). Compute
    `v̂_GL = TSODSO.recover_lossfree_shadow_voltage(ctx_ac)`, `pv_ac = ctx_ac.meta[:pf_vars]`,
    `N = length(feeder.buses)`. Sanity-check Lemma 1 first: assert
    `minimum(v̂_GL[j, t] - value(pv_ac.v[j, t]) for j in 1:N, t in 1:Phase4Fixtures.T) >= -1e-9`
    (Gan-Low's `v ≤ v̂(s)` always holds — this is the OPPOSITE sign from Task 1's `v ≥ v̂`, confirming
    the two shadows are genuinely distinct mechanisms). Compute
    `ε_measured = maximum(v̂_GL[j, t] - value(pv_ac.v[j, t]) for j in 1:N, t in 1:Phase4Fixtures.T)`.
    Assert `ε_measured > 0.0` (a nonzero, sensible modification gap). Print
    `@info "measured Gan-Low modification gap (before safety multiplier)" ε_measured` — this exact
    printed value is what plan 20-02's `RestrictedBranchFlow` default kwarg will hardcode (times a
    documented `1.25×` safety multiplier per D-03's "measured, not searched" requirement, mirroring
    this project's `DSO_BAND_HI = 1.5×max|dso|` precedent) with a citation back to this test item.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, JuMP, Test; Thoriz = 24; mem_price = Float64[3.8,3.7,3.6,3.6,3.7,4.0,4.8,5.8,6.5,6.2,5.9,5.7,5.6,5.8,6.0,6.8,8.2,9.0,8.6,7.4,6.2,5.2,4.4,4.0]; temperature = Float64[19,18,17,16,16,17,19,21,23,26,28,30,31,32,32,31,29,27,25,23,22,21,20,19]; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false), Bus(3,0.95,1.05,false)]; branches = [Branch(1,2,0.05,0.05,99.0), Branch(2,3,0.05,0.05,99.0)]; feeder = Feeder(buses, branches, 1); pv_scale = 1.2; aggs = map(2:length(feeder.buses)) do bus; prof = generate_profiles(seed = 20260406 + bus, T = Thoriz); Ppv = Float64[pv_scale * p for p in prof.pv]; Pdc = Float64[0.2 * d for d in prof.demand]; therm = Thermostatic(bus, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, temperature); defer = Deferrable(bus, 8, 16, 1.0, 0.5, 0.5); batt = PVBattery(bus, 0.95, 1.0, 0.1, 0.0, 0.2, 0.1, 3.8, 6.2, 8.9, Ppv); Aggregator(bus, 0.95, [therm, defer, batt], Pdc); end; ctx_ac, cost_ac, _ = solve_welfare(feeder, ACPowerFlow(), aggs; T = Thoriz, λ₀ = mem_price, allow_local = true, allow_export = true); v̂_GL = TSODSO.recover_lossfree_shadow_voltage(ctx_ac); pv_ac = ctx_ac.meta[:pf_vars]; N = length(feeder.buses); @test minimum(v̂_GL[j, t] - value(pv_ac.v[j, t]) for j in 1:N, t in 1:Thoriz) >= -1e-9; ε_measured = maximum(v̂_GL[j, t] - value(pv_ac.v[j, t]) for j in 1:N, t in 1:Thoriz); @test ε_measured > 0.0; println("ε_measured = ", ε_measured)'</automated> -- per-task quick check: plain Test.jl under --project=., inlines the EXACT-04 fixture; requires Task 1's recover_lossfree_shadow_voltage to already be defined
  </verify>
  <done>src/models/ac_oracle.jl exports recover_lossfree_shadow_voltage; test/test_restricted_branch_flow.jl has two green @testitems, and the second prints the measured ε_measured value for plan 20-02 to consume.</done>
  <acceptance_criteria>
    - `grep -n 'function recover_lossfree_shadow_voltage' src/models/ac_oracle.jl` matches exactly once, positioned before `function assert_ac_exact!` in the same file
    - `grep -n 'export recover_voltage_angles, assert_ac_exact!' src/models/ac_oracle.jl` returns 0 matches (the old export line was replaced) AND `grep -n 'recover_lossfree_shadow_voltage' src/models/ac_oracle.jl | grep -c export` is at least 1
    - Running the verify command exits 0 (both `@test` assertions pass) and prints a positive `ε_measured = ...` line
    - `julia --project=. -e 'using TSODSO; @assert isdefined(TSODSO, :recover_lossfree_shadow_voltage)'` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Researcher-supplied Julia structs (Feeder/Bus/Branch, aggregator lists) | The only "input" surface; no network/user-facing boundary in this local research library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Repudiation | `recover_lossfree_shadow_voltage` silently returns a wrong value on a malformed (non-tree) feeder | accept | `Feeder`'s own constructor already runs `assert_radial` (DATA-02) before any `Feeder` can exist — this function inherits that guarantee, no new validation needed |
| T-20-02 | Tampering | A sign/accumulation bug in the new BFS-subtree recursion silently produces a wrong `ε` | mitigate | Task 2's Lemma-1 sanity assertion (`v̂_GL ≥ v` everywhere) is a falsifiable numeric gate that would catch a sign error before any downstream plan trusts the measured `ε` |
| T-20-03 | Information Disclosure | N/A — no secrets, no network surface | accept | out of scope for a local optimization library |
</threat_model>

<verification>
Run `julia --project=. -e 'import Pkg; Pkg.test()'` (background, ~12-20 min — the ONLY entrypoint that actually executes `@testitem` bodies: TestItemRunner is test-only, so `--project=.` cannot resolve it, and even under `--project=test` a bare `include` of an `@testitem` file runs ZERO tests). Confirm both new `@testitem`s in `test/test_restricted_branch_flow.jl` pass and no other test file changed (this plan touches only `test/test_restricted_branch_flow.jl` and `src/models/ac_oracle.jl`); spot-check that `test/test_ac_oracle.jl`'s own items are still green in the same run.
</verification>

<success_criteria>
- RESEARCH.md Assumption A1 (`v ≥ v̂`) is numerically confirmed on the EXACT-04 fixture, or the plan STOPS and flags the derivation error rather than proceeding.
- `recover_lossfree_shadow_voltage` exists, exported, and satisfies Lemma 1 (`v̂_GL ≥ v`) numerically.
- A measured `ε_measured` value is printed and citable for plan 20-02.
</success_criteria>

<output>
Create `.planning/phases/20-overvoltage-capable-relaxation/20-01-SUMMARY.md` when done. Record the
exact measured `mingap` (Task 1) and `ε_measured` (Task 2) values in the summary — plan 20-02 needs
them verbatim.
</output>
