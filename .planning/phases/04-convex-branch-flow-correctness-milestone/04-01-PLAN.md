---
phase: 04-convex-branch-flow-correctness-milestone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/TSODSO.jl
  - src/solver/problem_class_trait.jl
  - src/powerflow/ConvexBranchFlow.jl
  - src/data/ieee13.jl
  - src/models/exactness.jl
  - src/models/oracle.jl
  - test/test_convex_branch_flow.jl
  - test/test_exactness.jl
  - test/test_ieee13.jl
  - test/test_oracle.jl
  - test/test_conformance.jl
  - test/fixtures_phase4.jl
autonomous: true
requirements: [PF-03, PF-04, OPT-02, OPT-03, DATA-03, SEAM-01]

must_haves:
  truths:
    - "The package precompiles with all five new Phase-4 files wired into the include graph in dependency order"
    - "Every Phase-4 requirement has a RED @testitem that fails because a stub is empty (missing symbol), never because the runner crashes"
    - "select_optimizer(SOCP()) resolves to a Clarabel factory with tol_gap 1e-8"
    - "The generic trait problem_class(::AbstractPowerFlow) == QP() exists so DC/LinDistFlow keep their solver"
  artifacts:
    - path: "src/solver/problem_class_trait.jl"
      provides: "generic problem_class(::AbstractPowerFlow) = QP() trait"
      contains: "problem_class"
    - path: "src/powerflow/ConvexBranchFlow.jl"
      provides: "comment-only SEAM/OWNER stub (filled by 04-02)"
    - path: "src/data/ieee13.jl"
      provides: "comment-only stub (filled by 04-03)"
    - path: "src/models/exactness.jl"
      provides: "comment-only stub (filled by 04-05)"
    - path: "src/models/oracle.jl"
      provides: "comment-only stub (filled by 04-04)"
    - path: "test/fixtures_phase4.jl"
      provides: "@testmodule Phase4Fixtures: high-PV/over-voltage fixture, MEM+temperature profiles, aggregator builder"
      contains: "@testmodule Phase4Fixtures"
    - path: "test/test_ieee13.jl"
      provides: "RED @testitems for DATA-03 + OPT-02"
  key_links:
    - from: "src/TSODSO.jl"
      to: "src/powerflow/ConvexBranchFlow.jl, src/solver/problem_class_trait.jl, src/data/ieee13.jl, src/models/exactness.jl, src/models/oracle.jl"
      via: "include() in dependency order"
      pattern: "include\\(\"powerflow/ConvexBranchFlow.jl\"\\)"
---

<objective>
Lay the Phase-4 foundation as a single serialized Wave-1 plan: wire every new Phase-4
source file into `src/TSODSO.jl`, create the comment-only SEAM/OWNER stub files that
Waves 2–4 fill (so no later wave ever re-touches the include graph), add the generic
`problem_class(::AbstractPowerFlow) = QP()` solver-routing trait, confirm `SOCP()` routes
to Clarabel, and stand up the RED `@testitem` harness + shared `Phase4Fixtures` test
module for all six phase requirements.

Purpose: Isolate the ONLY shared-file edit (`src/TSODSO.jl`) and the failing end-to-end
tests to one plan so the Wave-2 formulation/fixture/oracle plans are fully file-disjoint
and parallel, and so the vertical slice (SOCP solves exactly on IEEE-13) has a concrete
failing target from the first commit.
Output: A precompiling package with 5 wired stub files, the QP() trait, and 4 RED test
files + `fixtures_phase4.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-convex-branch-flow-correctness-milestone/04-RESEARCH.md
@.planning/phases/04-convex-branch-flow-correctness-milestone/04-VALIDATION.md
@src/TSODSO.jl
@test/fixtures_phase3.jl
@test/test_welfare_solve.jl
@test/test_conformance.jl

<interfaces>
<!-- Contracts the executor writes stubs and tests against. From the codebase. -->
From src/solver/ProblemClass.jl:
  abstract type ProblemClass end
  struct LP/MILP/QP/SOCP/NLP <: ProblemClass end
From src/solver/factory.jl:
  select_optimizer(::SOCP) -> optimizer_with_attributes(Clarabel.Optimizer, "verbose"=>false, "tol_gap_abs"=>1e-8, "tol_gap_rel"=>1e-8)
From src/powerflow/AbstractPowerFlow.jl:
  abstract type AbstractPowerFlow end ; function contribute! end
From src/models/welfare_solve.jl:
  solve_welfare(feeder, pf::AbstractPowerFlow, aggregators; T=24, λ₀, optimizer=select_optimizer(QP()), allow_local=false, τ=1e-6) -> (ctx, objective, dadp)
Test convention: each @testitem name CONTAINS its filter substring ("socp","exact","ieee13","ground","oracle","conformance"); shared data via @testmodule + setup=[Phase4Fixtures]; Phase4Fixtures must only DEFINE functions/consts (no top-level calls to symbols filled by later waves).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the include graph, add the problem_class trait, create stub files</name>
  <files>src/TSODSO.jl, src/solver/problem_class_trait.jl, src/powerflow/ConvexBranchFlow.jl, src/data/ieee13.jl, src/models/exactness.jl, src/models/oracle.jl</files>
  <read_first>
    - src/TSODSO.jl (the include graph and its ownership comments; note it exports nothing and is the sole assembly point)
    - src/solver/ProblemClass.jl (QP/SOCP singleton types) and src/solver/factory.jl (SOCP→Clarabel)
    - src/powerflow/AbstractPowerFlow.jl (the abstract type the trait dispatches on)
    - src/powerflow/DCPowerFlow.jl (the SEAM/OWNER header + comment-only-stub convention established in earlier phases)
    - 04-RESEARCH.md "Recommended Project Structure" and Pattern 5 (problem_class trait)
  </read_first>
  <action>
    Create `src/solver/problem_class_trait.jl` defining the GENERIC trait `problem_class(::AbstractPowerFlow) = QP()` (with a docstring citing Pattern 5 and INFRA-02) and `export problem_class`. This generic default keeps DC/LinDistFlow on the QP() backend; the `::ConvexBranchFlow => SOCP()` method is added later by 04-02, so the trait exists independent of the SOCP formulation (decouples Wave-2 parallelism).
    Create four comment-only stub files, each with a `# SEAM: ... # OWNER: plan 04-0N.` header matching the DCPowerFlow.jl convention and NO exports (they are filled by their owning plans): `src/powerflow/ConvexBranchFlow.jl` (OWNER 04-02, PF-03), `src/data/ieee13.jl` (OWNER 04-03, DATA-03), `src/models/exactness.jl` (OWNER 04-05, PF-04), `src/models/oracle.jl` (OWNER 04-04, OPT-03/SEAM-01).
    Edit `src/TSODSO.jl` to add the includes IN DEPENDENCY ORDER: `data/ieee13.jl` right after `data/profiles.jl`; `powerflow/ConvexBranchFlow.jl` right after `powerflow/LinDistFlow.jl`; `solver/problem_class_trait.jl` AFTER the powerflow formulations block (it needs both `QP` from solver/ and `AbstractPowerFlow` from powerflow/ in scope); `models/exactness.jl` and `models/oracle.jl` AFTER `models/welfare_solve.jl`. Add `# --- ... (owned by plan 04-0N, REQ) ---` section comments matching existing style.
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.precompile(); using TSODSO; @assert TSODSO.problem_class(TSODSO.LinDistFlow()) isa TSODSO.QP; @assert TSODSO.problem_class(TSODSO.DCPowerFlow()) isa TSODSO.QP; println("OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `using TSODSO` precompiles and loads without error (all 5 new files are valid Julia).
    - `problem_class(LinDistFlow())` and `problem_class(DCPowerFlow())` both return a `QP` instance.
    - `src/TSODSO.jl` contains `include("powerflow/ConvexBranchFlow.jl")`, `include("data/ieee13.jl")`, `include("solver/problem_class_trait.jl")`, `include("models/exactness.jl")`, `include("models/oracle.jl")`.
    - The four stub files contain a `# OWNER: plan 04-0N` header and export nothing (`grep -v '^#' <file> | grep -c export` returns 0).
  </acceptance_criteria>
  <done>Package precompiles; QP() trait live; five files wired; stubs export nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Stand up the RED @testitem harness for all six requirements</name>
  <files>test/test_convex_branch_flow.jl, test/test_exactness.jl, test/test_ieee13.jl, test/test_oracle.jl, test/test_conformance.jl</files>
  <read_first>
    - test/test_welfare_solve.jl and test/test_conformance.jl (the @testitem style, tags, setup=[...], occursin-substring naming)
    - test/runtests.jl (@run_package_tests discovery)
    - 04-RESEARCH.md "Phase Requirements → Test Map" and "Wave 0 Gaps" (the exact files + substrings + behaviors)
    - 04-VALIDATION.md "Per-Task Verification Map" (filter substrings: socp, exact, ieee13, ground, oracle)
  </read_first>
  <action>
    Create four new test files, each with `@testitem`s whose NAMES CONTAIN the required filter substring so `occursin(...)` selects them. These are RED placeholders that will be turned green by Waves 2–4; each should reference the not-yet-defined symbol so it fails on a missing symbol (RED for the right reason), not a runner crash. Use `setup = [Phase4Fixtures]` where fixtures are needed.
    `test/test_convex_branch_flow.jl` — items named "socp: ..." asserting `isdefined(TSODSO, :ConvexBranchFlow)`, that `contribute!(ConvexBranchFlow(), ctx, feeder; T)` stashes `ctx.meta[:pf_vars]` with keys `(:v,:v̂,:P,:Q,:l)`, and `problem_class(ConvexBranchFlow()) isa SOCP` (PF-03; filled by 04-02).
    `test/test_exactness.jl` — items named "exact: ..." asserting `isdefined(TSODSO, :assert_socp_exact!)`, that it PASSES on an exact solve and `@test_throws` on an inexact stashed `pf_vars`, and that a high-PV/over-voltage SOCP solve stays exact (PF-04; filled by 04-05).
    `test/test_ieee13.jl` — items named "ieee13: ..." asserting `isdefined(TSODSO, :ieee13_modified)`, the fixture constructs (radial+magnitudes pass), 11 buses / 10 branches / root at index 1 (DATA-03; filled by 04-03).
    `test/test_oracle.jl` — items named "oracle: ..." asserting `isdefined(TSODSO, :operational_oracle)` and that it returns a NamedTuple with `cost`/`π`/`dadp` and accepts stub kwargs `z`, `role`, `objective_hook`, `horizon_state` (OPT-03/SEAM-01; filled by 04-04).
    Edit `test/test_conformance.jl`: ADD a new item named "conformance: DC↔LinDistFlow↔SOCP interchange (crit 4, SOCP arm)" that will assert the SAME aggregator/solve call swapped to `ConvexBranchFlow()` still solves and exposes a finite dadp (RED now; filled by 04-02). Do NOT modify the existing DC↔LinDistFlow item.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin(r"socp|exact|ieee13|oracle", ti.name)' 2>&1 | grep -Eq "Test (Failed|Error)|MethodError|UndefVarError|not defined" && echo "RED-as-expected" || (echo "harness did not run / no red items" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - The four new test files exist and are discovered by `@run_package_tests` (they appear in the run, not skipped).
    - Filtering by each substring (`socp`, `exact`, `ieee13`, `oracle`) selects at least one item.
    - Items fail RED due to a missing symbol / undefined function (`UndefVarError`/`isdefined==false`), NOT a `LoadError` that aborts discovery.
    - `test/test_conformance.jl` still contains the original DC↔LinDistFlow item unchanged, plus the new SOCP-arm item.
  </acceptance_criteria>
  <done>Four RED test files + a RED SOCP conformance arm exist, discovered, failing for the right reason.</done>
</task>

<task type="auto">
  <name>Task 3: Build the shared Phase4Fixtures test module + confirm SOCP→Clarabel routing</name>
  <files>test/fixtures_phase4.jl</files>
  <read_first>
    - test/fixtures_phase3.jl (the @testmodule pattern: consts + functions only, no top-level calls; `setup=[...]` consumption)
    - src/data/profiles.jl `generate_profiles(; seed, T=24, ...)` signature (returns `(; demand, pv)`)
    - 04-RESEARCH.md "Code Examples" (build_ieee13_aggregators sketch, φ∈[0.85,0.95]) and Open Questions 1–2 (profiles are figure-bound → approximate; document)
    - src/data/Feeder.jl (Bus/Branch/Feeder), src/units/PerUnit.jl (magnitude bands; VOLTAGE_PU_MAX=1.2 so an over-voltage stress fixture must keep bus BOUNDS in-band while letting the solve push v toward vmax)
  </read_first>
  <action>
    Create `test/fixtures_phase4.jl` as `@testmodule Phase4Fixtures begin ... end` defining ONLY consts and functions (never call `TSODSO.ieee13_modified` at module top level — it is filled later; call it inside the consuming @testitems). Provide:
    (a) `mem_price_profile()` — a length-24 MEM/wholesale price `λ₀` vector approximating thesis Fig 4.5 (documented as digitized/approximate per Open Q1);
    (b) `temperature_profile()` — a length-24 exterior-temperature vector approximating Fig 4.2;
    (c) `build_ieee13_aggregators(feeder; seed)` — build one `Aggregator` per non-root bus (indices 2:11) each holding Thermostatic + Deferrable + PVBattery devices parametrized per RESEARCH (battery price triple λ_max=8.9/λ_min=3.8/λ_med=6.2 ¢$/kWh consistent units, φ∈[0.85,0.95]), fed by seeded `generate_profiles(seed=..., T=24)`;
    (d) `high_pv_feeder()` + `build_high_pv_aggregators()` — a small radial fixture with MASSIVE PV back-feed and tight voltage headroom that drives the network toward the UPPER voltage bound (the over-voltage / reverse-flow regime that stresses SOC exactness, RESEARCH Pitfall 1); use a fixed seed so it is reproducible (threat T-04-06).
    Export all of the above and `const T = 24`.
    Also confirm SOCP routing: add a tiny `@testitem "socp: SOCP() routes to a Clarabel factory with tight gap (INFRA-02)"` (in test/test_convex_branch_flow.jl created in Task 2 — append here) asserting `select_optimizer(SOCP())` builds a factory and that `Model(select_optimizer(SOCP()))` constructs; document that no factory edit was needed.
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.test()' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures_phase4.jl` defines `@testmodule Phase4Fixtures` and exports `mem_price_profile`, `temperature_profile`, `build_ieee13_aggregators`, `high_pv_feeder`, `build_high_pv_aggregators`, `T`.
    - Importing `Phase4Fixtures` via `setup=[Phase4Fixtures]` does NOT error (no top-level call to a later-wave symbol).
    - `mem_price_profile()` and `temperature_profile()` return length-24 vectors; a fixed seed makes `build_high_pv_aggregators()` reproducible.
    - The full `Pkg.test()` run completes (Phase-4 items RED, all prior phases GREEN — no regression, no runner crash).
  </acceptance_criteria>
  <done>Phase4Fixtures module ready and consumable; SOCP→Clarabel routing confirmed; suite runs with only intended RED items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → solve | Fixture data (profiles, PV levels, φ) crosses into the optimization as parameters; wrong magnitudes silently corrupt welfare/prices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Repudiation | high-PV fixture RNG | mitigate | Seeded `generate_profiles` / fixed seed in `build_high_pv_aggregators` — reproducible stress fixture (threat T-03-03 pattern) |
| T-04-08 | Tampering | fixtures_phase4 module eval | mitigate | Phase4Fixtures defines functions/consts only; no top-level call to later-wave symbols, so a partial-wave state cannot corrupt test discovery |
| T-04-SC | Tampering | package installs | accept | Phase 4 installs NO packages (04-RESEARCH Package Legitimacy Audit is informational); nothing to verify |
</threat_model>

<verification>
- `using TSODSO` precompiles with the 5 new files wired.
- `problem_class(::AbstractPowerFlow)==QP()` live; `select_optimizer(SOCP())` builds a Clarabel factory.
- RED @testitems exist for socp/exact/ieee13/oracle and a SOCP conformance arm, all failing on missing symbols.
- `Pkg.test()` runs to completion with prior phases green.
</verification>

<success_criteria>
Foundation ready: include graph wired, QP() trait live, SOCP routing confirmed, RED harness + Phase4Fixtures in place — Waves 2–4 are file-disjoint on `src/TSODSO.jl`.
</success_criteria>

<output>
Create `.planning/phases/04-convex-branch-flow-correctness-milestone/04-01-SUMMARY.md` when done.
</output>
