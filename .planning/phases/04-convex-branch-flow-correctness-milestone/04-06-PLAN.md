---
phase: 04-convex-branch-flow-correctness-milestone
plan: 06
type: execute
wave: 4
depends_on: [04-02, 04-03, 04-04, 04-05]
files_modified:
  - test/test_ground_truth.jl
autonomous: false
requirements: [OPT-02, OPT-03]

must_haves:
  truths:
    - "The full GLB-CVX SOCP solve on the modified IEEE-13 feeder via Clarabel returns OPTIMAL, the exactness gate ran (ctx.meta[:socp_maxgap] < τ), and the nodal-balance dual (DADP) is finite"
    - "Clarabel(SOCP) and Ipopt(NLP) objectives agree within tolerance (cross-solver sanity)"
    - "A computed golden (v₉[16], objective, DADP) is pinned as the primary reproducibility anchor; the thesis v₉[16] ≈ 1.0493 holds as an APPROXIMATE cross-check within a documented tolerance (not a hard exact-match gate)"
    - "operational_oracle on the IEEE-13 scenario returns a finite frontier coupling dual π"
  artifacts:
    - path: "test/test_ground_truth.jl"
      provides: "IEEE-13 SOCP solve + pinned-golden regression + thesis cross-check + cross-solver check"
      contains: "socp_maxgap"
      min_lines: 40
  key_links:
    - from: "test/test_ground_truth.jl"
      to: "operational_oracle / solve_welfare(ieee13_modified, ConvexBranchFlow(), aggs)"
      via: "full centralized SOCP solve on the built-in feeder"
      pattern: "ConvexBranchFlow"
    - from: "test/test_ground_truth.jl"
      to: "pinned golden constants"
      via: "regression assertion vs first computed golden + thesis approximate cross-check"
      pattern: "1.0493"
---

<objective>
Close the vertical slice: run the full GLB-CVX social-welfare solve over
`ConvexBranchFlow` on the modified IEEE-13 feeder through `operational_oracle`, prove it
is OPTIMAL + exact + cross-solver-consistent, and pin the ground-truth regression — a
COMPUTED golden as the primary reproducibility anchor, with the thesis number
`v₉[16] ≈ 1.0493` asserted as an approximate cross-check within a documented tolerance.

Purpose: The centralized ground truth every later rung (pricing, ADMM) is validated
against (OPT-02, OPT-03). Handles RESEARCH Open Q1 honestly: pin what is reproducible,
cross-check the thesis figure, document any gap.
Output: `test/test_ground_truth.jl`; the "ground"/"ieee13" solve+regression @testitems
turn green; a human-verify checkpoint confirms the A1 magnitude reading + accepts the
pinned golden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-convex-branch-flow-correctness-milestone/04-RESEARCH.md
@.planning/phases/04-convex-branch-flow-correctness-milestone/04-VALIDATION.md
@src/models/welfare_solve.jl
@src/models/oracle.jl
@src/data/ieee13.jl
@src/powerflow/ConvexBranchFlow.jl
@test/fixtures_phase4.jl
@test/test_welfare_solve.jl

<interfaces>
From src/models/oracle.jl (04-04): operational_oracle(feeder, pf, aggregators; λ₀, T, z, role, objective_hook, horizon_state) -> (; cost, π, dadp, ctx).
From src/models/welfare_solve.jl (04-05): solve_welfare routes ConvexBranchFlow → SOCP by trait; stashes ctx.meta[:socp_maxgap]; cross-solver via optimizer=select_optimizer(NLP()), allow_local=true.
From src/data/ieee13.jl (04-03): ieee13_modified() (root index 1 = thesis node 0; thesis node 9 → struct index 10).
From test/fixtures_phase4.jl (04-01): build_ieee13_aggregators(feeder; seed), mem_price_profile(), temperature_profile().
ctx.meta[:pf_vars].v is the SQUARED voltage ⇒ |V| = sqrt(value(v[bus,t])) (Assumption A1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full GLB-CVX SOCP solve on IEEE-13 (OPTIMAL, exact, cross-solver) — OPT-02/OPT-03</name>
  <files>test/test_ground_truth.jl</files>
  <read_first>
    - test/test_welfare_solve.jl (the end-to-end multi-aggregator solve pattern + the Clarabel-vs-Ipopt cross-solver check with allow_local=true)
    - 04-RESEARCH.md "GLB-CVX solve on the SOCP formulation (OPT-02)" code sketch and Pitfall 4 (Clarabel primary, Ipopt cross-check, never SCS)
    - test/fixtures_phase4.jl (build_ieee13_aggregators, mem_price_profile, T=24)
    - src/models/oracle.jl (operational_oracle return shape)
  </read_first>
  <action>
    Fill the "ground:" solve items (names contain both "ground" and "ieee13" so both filters select them). Build the scenario: `feeder = ieee13_modified()`, `aggs = build_ieee13_aggregators(feeder; seed=20260718)`, `λ₀ = mem_price_profile()` (length 24). Call `operational_oracle(feeder, ConvexBranchFlow(), aggs; λ₀, T=24)`. Assert: the solve reached OPTIMAL (it returned — `solve_welfare` throws otherwise); `cost` is finite and within a magnitude sanity bound; `dadp` is length 24 and all finite; the exactness gate RAN and passed (`ctx.meta[:socp_maxgap] < 1e-5`); the frontier coupling dual `π` is finite. Add the cross-solver sanity check: re-solve the SAME assembly via `solve_welfare(feeder, ConvexBranchFlow(), aggs; T=24, λ₀, optimizer=select_optimizer(NLP()), allow_local=true)` and assert the two objectives agree within rtol/atol ~1e-3 (Pitfall 4 — a doubled current from a bad cone scaling would surface as disagreement). Do NOT assert thesis numbers here (that is Task 2).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ground", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - The IEEE-13 ConvexBranchFlow solve completes (OPTIMAL) and `ctx.meta[:socp_maxgap] < 1e-5` (exact).
    - `dadp` is length 24, all finite; `π` finite.
    - Clarabel(SOCP) and Ipopt(NLP) objectives agree within ~1e-3 rtol/atol.
    - Item names contain "ground" and "ieee13".
  </acceptance_criteria>
  <done>The full GLB-CVX SOCP solve on IEEE-13 is OPTIMAL, exact, and cross-solver-consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the ground-truth regression (computed golden + thesis cross-check) — OPT-02/OPT-03</name>
  <files>test/test_ground_truth.jl</files>
  <read_first>
    - 04-RESEARCH.md Open Question 1 + Assumptions A1/A2/A3 (thesis numbers partly figure-bound; pin a computed golden, assert thesis v₉[16] to ~1e-3; confirm |V| vs squared) and the recommendation to treat PF-04 + fixture as HARD gates and OPT-02/03 as a pinned computed golden
    - 04-VALIDATION.md "Manual-Only Verifications" (exact thesis match is a documented cross-check, not a gate)
    - src/powerflow/ConvexBranchFlow.jl (v is SQUARED voltage ⇒ |V|=sqrt; node 9 → struct index 10, hour t=16)
  </read_first>
  <action>
    Add a "ground:" regression item. FIRST run the solve once to obtain the current computed values, then PIN them as `const` golden literals IN THE TEST FILE: the voltage magnitude at thesis node 9, hour 16 = `sqrt(value(ctx.meta[:pf_vars].v[10, 16]))` (struct index 10 = thesis node 9; take the square root because `v` is |V|² — Assumption A1), the objective/welfare, and the DADP vector (or a hash/summary of it). Assert the live solve reproduces these pinned goldens to a tight tolerance (~1e-4) — this is the PRIMARY reproducibility anchor that catches numerical drift. THEN add an APPROXIMATE thesis cross-check: assert the pinned `v₉[16]` is within a documented, LOOSER tolerance (~1e-2) of the thesis value 1.0493, as a `@test` that is allowed to be a soft/informational comparison — do NOT hard-fail the suite solely on an exact thesis mismatch (the profiles/house-counts are figure-bound, Open Q1/A2/A3). Comment the file thoroughly: state that `v` is squared so `|V|=sqrt(v)`, the node→index mapping, that 1.0493 is asserted as |V| magnitude (A1), and that any gap from the thesis welfare $1819 is expected and documented (the inputs are partly figure-bound). Record the observed `(v₉[16], objective)` and the gap-to-thesis in the plan SUMMARY.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ground", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - The test pins computed golden constants for `v₉[16]` (as sqrt of the squared variable at struct index 10, t=16), the objective, and the DADP, and asserts the live solve matches them to ~1e-4.
    - The thesis value 1.0493 appears in the file and is asserted as an approximate |V| cross-check within a documented (~1e-2) tolerance, NOT as an exact hard-fail gate.
    - A comment documents the |V|=sqrt(v) reading (A1), the node 9 → index 10 mapping, and that any welfare gap vs the thesis $1819 is documented.
  </acceptance_criteria>
  <done>The computed golden is pinned as the reproducibility anchor; the thesis number is a documented approximate cross-check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Researcher confirms the A1 magnitude reading + accepts the pinned golden</name>
  <files>test/test_ground_truth.jl (review only — no code change)</files>
  <action>PAUSE for the researcher. All automation is complete (solve is OPTIMAL, exact, cross-solver-consistent, golden pinned); this task only presents the results below for human confirmation of Assumption A1 and acceptance of the pinned golden. Do not modify code before the resume signal.</action>
  <verify><human-check>Researcher runs the "ground" filter, reviews the SUMMARY values, and replies via the resume signal.</human-check></verify>
  <done>Researcher typed "approved" (A1 confirmed as |V| magnitude and the pinned golden accepted), or a correction was captured and the regression re-pinned.</done>
  <what-built>
    A full GLB-CVX SOCP solve on the modified IEEE-13 feeder that is OPTIMAL, certified
    exact (max|l·v−(P²+Q²)| < 1e-5), cross-solver-consistent (Clarabel vs Ipopt), with a
    pinned computed-golden regression and an approximate thesis cross-check on v₉[16].
  </what-built>
  <how-to-verify>
    1. Run: `julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ground", ti.name)'` — all green.
    2. In the plan SUMMARY, review the recorded observed values: `v₉[16]` (as |V| = sqrt of the squared variable at struct index 10, hour 16), the welfare/objective, and the gap vs the thesis figures (v₉[16] ≈ 1.0493, welfare ≈ $1819).
    3. Confirm Assumption A1: `v₉[16] ≈ 1.0493` is the VOLTAGE MAGNITUDE |V₉| (≈1.05 peak on the thesis "Tensión [p.u.]" axis), i.e. `sqrt(v)`, NOT the squared variable `v` (~1.0243). If it should be the squared value instead, say so and the regression will be re-pinned.
    4. Confirm the pinned computed golden is accepted as the reproducibility anchor and that any documented gap from the thesis welfare $1819 (attributable to figure-bound MEM/temperature profiles and the house-count inconsistency A3) is acceptable for v1.
  </how-to-verify>
  <resume-signal>Type "approved" (A1 = magnitude, golden accepted), or describe the correction (e.g. "v₉[16] is squared" / "digitize Fig 4.5 to close the welfare gap").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solved model → pinned golden | A golden pinned to a buggy solve would silently bless wrong numbers as "ground truth" for every later phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | golden pinned to a buggy solve | mitigate | Golden pinned ONLY after OPTIMAL + exactness gate (socp_maxgap<τ) + Clarabel-vs-Ipopt objective agreement; human-verify checkpoint confirms A1 + accepts the golden |
| T-04-15 | Tampering | A1 magnitude-vs-squared misread | mitigate | Assert on `sqrt(value(v))` (|V|); human checkpoint explicitly confirms 1.0493 is |V| not v² |
| T-04-07 | Information | DADP sign convention | accept | Phase 4 only needs the dual finite/available; sign + decomposition validation is Phase 5 (PRICE-01/02) — documented, not gated here |
| T-04-SC | Tampering | package installs | accept | No packages installed this plan |
</threat_model>

<verification>
- IEEE-13 ConvexBranchFlow solve: OPTIMAL, socp_maxgap<τ, dadp+π finite, Clarabel≈Ipopt.
- Computed golden pinned (v₉[16]=sqrt(v[10,16]), objective, DADP) matched to ~1e-4; thesis 1.0493 approximate cross-check documented.
- Human-verify checkpoint confirms A1 and accepts the golden.
</verification>

<success_criteria>
The centralized monolithic solve reproduces the thesis DADP/voltage numbers as ground
truth (pinned computed golden + approximate thesis cross-check on v₉[16] ≈ 1.0493), with
the nodal active-balance dual available and the frontier coupling dual returned via
operational_oracle. (OPT-02, OPT-03)
</success_criteria>

<output>
Create `.planning/phases/04-convex-branch-flow-correctness-milestone/04-06-SUMMARY.md` when done.
Record the observed v₉[16], objective, and the gap vs thesis figures in the SUMMARY.
</output>
