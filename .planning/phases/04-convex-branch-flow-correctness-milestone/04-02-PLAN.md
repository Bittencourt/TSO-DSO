---
phase: 04-convex-branch-flow-correctness-milestone
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/powerflow/ConvexBranchFlow.jl
  - test/test_convex_branch_flow.jl
  - test/test_conformance.jl
autonomous: true
requirements: [PF-03]

must_haves:
  truths:
    - "ConvexBranchFlow is a drop-in third AbstractPowerFlow subtype: DC, LinDistFlow and SOCP are interchangeable by dispatch with zero edits to devices or assembly"
    - "The SOC cone l·v ≥ P²+Q² is imposed per branch/time as a RotatedSecondOrderCone with the 0.5 factor"
    - "The LinDistFlow exactness copy (aux v̂ + affine bounds on both v and v̂) is part of the formulation"
    - "problem_class(::ConvexBranchFlow) == SOCP()"
  artifacts:
    - path: "src/powerflow/ConvexBranchFlow.jl"
      provides: "SOCP DistFlow formulation + exactness copy (thesis 3.31-3.45)"
      contains: "RotatedSecondOrderCone"
      min_lines: 60
  key_links:
    - from: "src/powerflow/ConvexBranchFlow.jl"
      to: "ctx.residuals[:Rp] / :Rq"
      via: "add_to_residual! with affine loss terms (−r·l / −x·l)"
      pattern: "add_to_residual!\\(ctx, :Rp"
    - from: "src/powerflow/ConvexBranchFlow.jl"
      to: "ctx.meta[:pf_vars]"
      via: "stash (; v, v̂, P, Q, l) for the PF-04 exactness gate"
      pattern: "pf_vars"
---

<objective>
Implement `ConvexBranchFlow <: AbstractPowerFlow` — the SOCP DistFlow branch-flow
relaxation WITH the LinDistFlow exactness copy — as a drop-in third power-flow
formulation that writes affine `:Rp`/`:Rq` contributions (now including the `−r·l`/`−x·l`
loss terms) and adds its cone / voltage-drop / apparent-power constraints, exactly
mirroring `LinDistFlow.jl`. Register the SOCP solver route via the trait.

Purpose: The correctness keystone's model. Every constraint traces to thesis eqs.
3.31–3.45; the exactness copy is what makes the recovered prices physically meaningful
(RESEARCH Pitfall 1).
Output: `src/powerflow/ConvexBranchFlow.jl`; the "socp" and SOCP-arm "conformance"
@testitems turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-convex-branch-flow-correctness-milestone/04-RESEARCH.md
@src/powerflow/LinDistFlow.jl
@src/powerflow/AbstractPowerFlow.jl
@src/core/ModelContext.jl

<interfaces>
From src/powerflow/LinDistFlow.jl (the pattern to mirror exactly):
  struct LinDistFlow <: AbstractPowerFlow end
  contribute!(::LinDistFlow, ctx::ModelContext, feeder; T::Int=1): creates v[j,t],P[b,t],Q[b,t]; fix.(v[root,:],1.0;force=true); set_lower/upper_bound(v[j,t], vmin²/vmax²); vdrop constraint; per-bus inflow−outflow into :Rp/:Rq via add_to_residual!(ctx,:Rp,j,t,expr); stashes ctx.meta[:pf_vars]=(;v,P,Q).
From src/core/ModelContext.jl:
  add_to_residual!(ctx,:Rp,i,t,expr) — expr MUST be affine (convert(AffExpr,·)); loss term −r·l is affine in l, so it flows through unchanged.
From src/solver/problem_class_trait.jl (04-01): problem_class(::AbstractPowerFlow)=QP() generic.
Feeder: feeder.buses (Bus.vmin/vmax), feeder.branches (Branch.from/to/r/x/smax), feeder.root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConvexBranchFlow struct + contribute! (SOC cone + exactness copy, eqs 3.31–3.45)</name>
  <files>src/powerflow/ConvexBranchFlow.jl</files>
  <read_first>
    - src/powerflow/LinDistFlow.jl IN FULL (variable creation, root fix, squared bounds, vdrop, the inflow/outflow :Rp/:Rq loop, the pf_vars stash) — this is the template; the SOCP file is LinDistFlow plus l, v̂, cone, loss terms, true drop, copy drop, Smax
    - 04-RESEARCH.md Pattern 1 (rotated SOC: `[0.5*l, v_from, P, Q] in RotatedSecondOrderCone()` ⇒ l·v ≥ P²+Q²), Pattern 2 (single aux v̂ per bus, copy drop 3.43 expanded, bounds 3.45 on BOTH v and v̂), Pattern 3 (loss-term node assignment: incoming branch (i,j) contributes +P − r·l at child j; Assumptions A6 reactive frontier, A7 forward Smax only)
    - 04-RESEARCH.md "Anti-Patterns" (do NOT drop the 0.5 factor; do NOT omit the exactness copy)
    - src/core/ModelContext.jl add_to_residual! (affine-only contract)
  </read_first>
  <behavior>
    - contribute!(ConvexBranchFlow(), ctx, feeder; T) creates variables `v[j,t]`, `v̂[j,t]`, `P[b,t]`, `Q[b,t]`, and `l[b,t] >= 0`.
    - Root fixed: `fix.(v[root,:],1.0)` AND `fix.(v̂[root,:],1.0)`.
    - Squared bounds vmin²/vmax² applied to BOTH v and v̂ at every non-root bus (3.45).
    - Per branch/time: rotated SOC cone `[0.5*l, v[from], P, Q] in RotatedSecondOrderCone()` (3.39); true voltage drop `v[to] == v[from] − 2(rP+xQ) + (r²+x²)l` (3.33); copy drop `v̂[to] == v̂[from] − 2(r(P+rl)+x(Q+xl))` (3.43); apparent-power limit `P²+Q² ≤ smax²` where smax<large-sentinel (3.36).
    - Per-bus balances into :Rp/:Rq: incoming branch to j adds `+P − r·l` (:Rp) / `+Q − x·l` (:Rq); outgoing branch from j adds `−P` / `−Q` (3.31/3.32, affine).
    - Stashes `ctx.meta[:pf_vars] = (; v, v̂, P, Q, l)`.
  </behavior>
  <action>
    Create `struct ConvexBranchFlow <: AbstractPowerFlow end` with a docstring listing every thesis equation it implements (3.31, 3.32, 3.33, 3.36, 3.39, 3.43, 3.45) and its differences from `LinDistFlow` (adds `l`, `v̂`, the cone, loss terms, true drop, copy drop, Smax). Implement `contribute!(::ConvexBranchFlow, ctx::ModelContext, feeder; T::Int=1)` mirroring `LinDistFlow.contribute!` structure. CRITICAL details: the rotated cone MUST carry the `0.5` factor on `l` (missing it silently doubles allowed current — RESEARCH Anti-Pattern); the copy drop 3.43 is written purely in the ORIGINAL `P,Q,l` plus the new `v̂` (do NOT create separate P̂/Q̂ variables — Pattern 2); bounds 3.45 apply to BOTH `v` and `v̂`; the loss terms in the balance stay affine (linear in `l`) so they pass `add_to_residual!` unchanged; charge `−r·l`/`−x·l` at the CHILD node of the incoming branch (Pattern 3 / Pitfall 6). Apply the forward apparent-power limit `P[b,t]²+Q[b,t]² ≤ smax²` only where a real limit exists (skip when `smax` is at/above the large sentinel used by the fixture, per Assumption A7 / Open Q2). Stash `ctx.meta[:pf_vars] = (; v, v̂, P, Q, l)`. Add `problem_class(::ConvexBranchFlow) = SOCP()` and `export ConvexBranchFlow` (the trait method is added to the function from 04-01; do not redefine the generic).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("socp", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - `src/powerflow/ConvexBranchFlow.jl` contains `RotatedSecondOrderCone` and the literal `0.5` scaling on the current term (`grep -n "0.5" src/powerflow/ConvexBranchFlow.jl` matches the cone line).
    - `contribute!(ConvexBranchFlow(), ctx, feeder; T)` stashes `ctx.meta[:pf_vars]` whose keys are exactly `(:v, :v̂, :P, :Q, :l)`.
    - `problem_class(ConvexBranchFlow()) isa SOCP`.
    - The file creates a `v̂` variable and bounds both `v` and `v̂` (grep shows two `set_lower_bound`/`set_upper_bound` families or a shared loop over both).
    - All "socp" @testitems pass.
  </acceptance_criteria>
  <done>ConvexBranchFlow implements 3.31–3.45 with the exactness copy and rotated cone; socp tests green.</done>
</task>

<task type="auto">
  <name>Task 2: DC↔LinDistFlow↔SOCP interchange conformance (crit 4, SOCP arm)</name>
  <files>test/test_conformance.jl, test/test_convex_branch_flow.jl</files>
  <read_first>
    - test/test_conformance.jl (existing DC↔LinDistFlow item; the "swap ONLY the pf argument, no edit to device/solve" contract)
    - src/models/welfare_solve.jl (that solve_welfare takes any AbstractPowerFlow as `pf` and closes :Rq only when the formulation provides it)
    - 04-RESEARCH.md Pattern 3 (interchangeability claim)
  </read_first>
  <action>
    Fill the RED "conformance: DC↔LinDistFlow↔SOCP interchange (crit 4, SOCP arm)" item added in 04-01: build one small radial feeder + one aggregator ONCE, then call `solve_welfare` three times differing ONLY in the `pf` argument (`DCPowerFlow()`, `LinDistFlow()`, `ConvexBranchFlow()`), asserting each returns a finite objective and a length-T finite `dadp` with no edit to the device or `solve_welfare` between calls. Do not require the three objectives to be equal (SOCP carries losses the linear models drop) — assert only that the SWAP is a zero-edit dispatch (all three solve and expose a finite priced dual), which is success criterion 4. Keep the existing DC↔LinDistFlow closed-form item untouched. Also complete any remaining "socp:" dispatch assertions in test_convex_branch_flow.jl (that ConvexBranchFlow allocates :Rq — reactive-capable — and stashes :l).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("conformance", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - The SOCP-arm conformance item passes: three `solve_welfare` calls differing only in `pf` each yield finite objective + finite length-T dadp.
    - The original DC↔LinDistFlow closed-form conformance item still passes unchanged.
    - No `if formulation ==`-style branching is introduced anywhere (grep of src shows none).
  </acceptance_criteria>
  <done>SOCP joins the interchange contract; conformance tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulation → residual/objective | The formulation writes physical branch/voltage terms; a wrong cone scaling or dropped loss term silently corrupts every voltage and price |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | RotatedSecondOrderCone scaling | mitigate | Enforce the `0.5*l` factor (`‖x‖²≤2tu`); acceptance grep + the exactness gate (04-05) + cross-solver check (04-06) would expose a doubled current |
| T-04-09 | Tampering | loss-term node assignment | mitigate | Charge `−r·l`/`−x·l` at the child node of the incoming branch (Pitfall 6); mirror LinDistFlow inflow/outflow loop |
| T-04-10 | Tampering | omitted exactness copy | mitigate | v̂ + 3.43/3.45 are written as part of the formulation (Pattern 2, Anti-Pattern); PF-04 gate (04-05) refuses prices if omission makes the relaxation inexact |
| T-04-SC | Tampering | package installs | accept | No packages installed this plan |
</threat_model>

<verification>
- "socp" @testitems green: ConvexBranchFlow exists, stashes pf_vars with :l, routes to SOCP().
- Cone line carries the 0.5 factor; v̂ + bounds on both v and v̂ present.
- Conformance: DC↔LinDistFlow↔SOCP all solve by swapping only `pf`.
</verification>

<success_criteria>
The SOCP Convex Branch Flow formulation with the LinDistFlow exactness copy exists as a
drop-in third AbstractPowerFlow subtype, traced to thesis 3.31–3.45, interchangeable with
DC/LinDistFlow by dispatch. (PF-03)
</success_criteria>

<output>
Create `.planning/phases/04-convex-branch-flow-correctness-milestone/04-02-SUMMARY.md` when done.
</output>
