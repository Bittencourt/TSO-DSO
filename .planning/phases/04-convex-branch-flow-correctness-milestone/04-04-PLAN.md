---
phase: 04-convex-branch-flow-correctness-milestone
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/models/oracle.jl
  - test/test_oracle.jl
autonomous: true
requirements: [OPT-03, SEAM-01]

must_haves:
  truths:
    - "operational_oracle(feeder, pf, aggregators; λ₀, ...) returns a NamedTuple exposing cost, π (frontier coupling dual), dadp, and ctx"
    - "When z pins the frontier import, π is the dual of that pin; when z is free (nothing), π is the frontier-node DADP"
    - "The SEAM-01 extension interfaces exist as INERT stubs: multi-scenario objective_hook, rolling-horizon horizon_state parameter, meshed-formulation note, and the coupling-flow z with an explicit leader/follower role"
  artifacts:
    - path: "src/models/oracle.jl"
      provides: "operational_oracle wrapper + SEAM-01 stubs + _coupling_dual"
      contains: "operational_oracle"
      min_lines: 40
  key_links:
    - from: "src/models/oracle.jl"
      to: "solve_welfare"
      via: "operational_oracle wraps solve_welfare and reads its dual"
      pattern: "solve_welfare"
---

<objective>
Implement `operational_oracle(z) → (cost, π)` as a thin, additive wrapper over
`solve_welfare`, exposing the frontier coupling dual, plus the SEAM-01 extension
interfaces as INERT stubs (multi-scenario objective hook, rolling-horizon parameter,
meshed slot note, coupling-flow `z`↔p_ag / `π`↔π_s with a leader/follower role). Stubs
only — the planning layer is additive in a later milestone.

Purpose: So the future Stackelberg-Nash planning game (deferred v2) wraps the operational
solve without a rewrite (OPT-03, SEAM-01).
Output: `src/models/oracle.jl` exporting `operational_oracle`; the "oracle" @testitems
turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-convex-branch-flow-correctness-milestone/04-RESEARCH.md
@src/models/welfare_solve.jl
@src/core/ModelContext.jl

<interfaces>
From src/models/welfare_solve.jl:
  solve_welfare(feeder, pf, aggregators; T=24, λ₀, optimizer=select_optimizer(QP()), allow_local=false, τ=1e-6) -> (ctx, objective, dadp)
  ctx.meta[:p_import] (Vector, priced active frontier import at root); ctx.meta[:q_import] when reactive.
  ctx.constraints[:balance_p] (registered; dual = DADP per bus/time).
From src/solver/problem_class_trait.jl (04-01): problem_class(pf) -> ProblemClass.
JuMP Parameter API (RESEARCH Pattern 6): @variable(m, s0 in Parameter(v)); set_parameter_value(s0, x).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: operational_oracle wrapper + _coupling_dual + SEAM-01 stubs</name>
  <files>src/models/oracle.jl</files>
  <read_first>
    - src/models/welfare_solve.jl IN FULL (what it returns, what it stashes in ctx.meta, the registered :balance_p) — the oracle wraps it and reads its dual
    - 04-RESEARCH.md Pattern 6 (operational_oracle signature, the SEAM-01 stub inventory table, z↔p_ag / π_s = dual of the coupling from the PSR note, distributor = leader) and the "JuMP Parameter API (VERIFIED)" note
    - src/core/ModelContext.jl (ctx.meta / ctx.constraints access)
  </read_first>
  <behavior>
    - operational_oracle(feeder, pf, aggregators; λ₀, T=24, z=nothing, role=:follower, objective_hook=identity, horizon_state=nothing) returns `(; cost, π, dadp, ctx)`.
    - With z=nothing: `π` equals the frontier-node (root) DADP (from dual of :balance_p at root).
    - The default optimizer is `select_optimizer(problem_class(pf))` so a ConvexBranchFlow oracle solves as SOCP and a LinDistFlow oracle solves as QP — without naming a solver.
    - `role`, `objective_hook`, `horizon_state`, `z` are accepted and documented as SEAM-01 stubs; passing them does not change the Phase-4 result (objective_hook defaults to identity; horizon_state currently ignored/documented).
  </behavior>
  <action>
    Implement `operational_oracle(feeder, pf, aggregators; λ₀, T=24, z=nothing, role::Symbol=:follower, objective_hook::Function=identity, horizon_state=nothing)` calling `solve_welfare(feeder, pf, aggregators; T, λ₀, optimizer = select_optimizer(problem_class(pf)))` and returning `(; cost, π, dadp, ctx)` where `cost` is the returned objective, `dadp` is the returned DADP vector, and `π = _coupling_dual(ctx, z)`. Implement `_coupling_dual(ctx, z)`: when `z === nothing`, return the frontier/root DADP (the dual of `ctx.constraints[:balance_p]` at `feeder.root` — read `feeder` from `ctx.meta[:feeder]`); when `z` is a setpoint, document that it pins the frontier import (the coupling flow z↔p_ag) and return the dual of that pin (for Phase 4, a documented stub: since pinning is not yet wired into solve_welfare, return the root DADP and note the z-pin is the PLAN-01/02 extension point — do NOT implement the pin). Document the full SEAM-01 stub inventory in the docstring (the four interfaces + which v2 requirement makes each concrete), including the note that the meshed-formulation slot IS the `AbstractPowerFlow` seam plus a documented bypass of `assert_radial` for a future `MeshedFlow`. `export operational_oracle`. Keep it thin and additive — no change to `solve_welfare`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("oracle", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - `operational_oracle` exists and returns a NamedTuple with fields `cost`, `π`, `dadp`, `ctx`.
    - Called with `z=nothing` on a small LinDistFlow feeder, it solves and `π` is finite and equals the root DADP.
    - The signature accepts `z`, `role`, `objective_hook`, `horizon_state` kwargs (a call passing all four does not error).
    - `src/models/oracle.jl` contains `operational_oracle` and `_coupling_dual`; docstring lists the four SEAM-01 stubs.
    - `solve_welfare` is NOT modified by this plan.
  </acceptance_criteria>
  <done>operational_oracle wraps solve_welfare, exposes (cost, π), and carries inert SEAM-01 stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the oracle + SEAM-01 @testitems green</name>
  <files>test/test_oracle.jl</files>
  <read_first>
    - test/test_welfare_solve.jl (how a small aggregator/solve is set up in tests; using JuMP for value())
    - 04-RESEARCH.md Test Map OPT-03/SEAM-01 row (oracle returns (cost,π); stub kwargs role/objective_hook/z + rolling Parameter present)
  </read_first>
  <action>
    Fill the RED "oracle:" items: build a small radial feeder + one aggregator (reuse the Phase3-style helpers or a minimal inline aggregator) and call `operational_oracle(feeder, LinDistFlow(), [agg]; λ₀, T)`; assert the returned `(; cost, π, dadp, ctx)` has finite `cost`, finite `π`, and `length(dadp)==T`. Assert the SEAM-01 stub kwargs are accepted: a second call passing `z=nothing, role=:leader, objective_hook=identity, horizon_state=nothing` succeeds and returns the same shape. Use LinDistFlow (not SOCP) so this Wave-2 test is independent of 04-02. Do not assert exact ground-truth numbers (that is 04-06).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("oracle", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    - "oracle" @testitems pass on a LinDistFlow feeder: finite cost/π, length-T dadp.
    - A call passing all four SEAM-01 stub kwargs succeeds and returns `(; cost, π, dadp, ctx)`.
  </acceptance_criteria>
  <done>oracle tests green using an existing formulation (no SOCP dependency).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oracle → dual read | The oracle exposes a price (π) to future planning code; returning a wrong/loose dual would silently mislead the deferred planning layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | π provenance (coupling dual) | mitigate | π derived from the registered `:balance_p` dual that `solve_welfare` already gates behind `assert_solved!(dual=true)`; oracle adds no new solve path |
| T-04-13 | Spoofing | SEAM-01 stubs pretending to be implemented | mitigate | Stubs documented as INERT with the v2 requirement that makes each concrete; z-pin explicitly NOT implemented (returns root DADP) — no silent partial behavior |
| T-04-SC | Tampering | package installs | accept | No packages installed this plan |
</threat_model>

<verification>
- operational_oracle returns (; cost, π, dadp, ctx) with finite cost/π on a LinDistFlow feeder.
- SEAM-01 stub kwargs (z, role, objective_hook, horizon_state) accepted and inert.
- solve_welfare untouched.
</verification>

<success_criteria>
`operational_oracle(z) → (cost, π)` returns the frontier coupling dual, and the SEAM-01
extension interfaces exist as stubs with an explicit leader/follower role. (OPT-03, SEAM-01)
</success_criteria>

<output>
Create `.planning/phases/04-convex-branch-flow-correctness-milestone/04-04-SUMMARY.md` when done.
</output>
