---
phase: 21-mpc-rolling-horizon-real-time-pricing
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified:
  - src/experiments/Scenario.jl
  - src/models/mpc_window.jl
  - test/test_mpc_terminal.jl
autonomous: true
requirements: [MPC-01, MPC-02]

must_haves:
  truths:
    - "Scenario gains minimal additive @kwdef fields (mpc_H, mpc_step, mpc_terminal_soc, mpc_forecast_error) with defaults that are no-ops for the :centralized/:admm strategies, never touching SCENARIO_VALID_STRATEGIES or run_scenario's dispatch (D-12, Pitfall 7)."
    - "A pure, JuMP-free nominal-plant state-propagation function re-derives each stateful device's NEXT measured state from its OWN device recursion equation evaluated on the REALIZED first-interval control and the TRUE (not forecast-perturbed) exogenous input — D-05's exact 'apply each step's first-interval optimal controls to the ground-truth device dynamics' reading."
    - "A seeded, INDEPENDENT forecast-error draw (never the global RNG, never reusing the :profiles/:population sub_seed tags) produces a bounded multiplicative perturbation of PV and demand, regenerated per window per D-08."
    - "The hard terminal-SOC condition (D-06) demonstrably PREVENTS the end-of-horizon myopic battery dump/hoard artifact on the phase's own CI fixture when enabled, and the artifact is PRESENT when disabled — measured, not assumed (MPC-02's own acceptance criterion)."
  artifacts:
    - path: "src/experiments/Scenario.jl"
      provides: "mpc_H::Int, mpc_step::Int, mpc_terminal_soc::Bool, mpc_forecast_error::Float64 additive fields + constructor guards"
      contains: "mpc_forecast_error"
    - path: "src/models/mpc_window.jl"
      provides: "propagate_soc, propagate_tin, draw_forecast_error — pure state-propagation/forecast-draw primitives (unexported, orchestration-internal)"
      contains: "function propagate_soc"
    - path: "test/test_mpc_terminal.jl"
      provides: "@testitem MPC-02 dump/hoard A/B regression driving build_mpc_window directly across a manual multi-step loop"
      contains: "@testitem"
  key_links:
    - from: "src/models/mpc_window.jl"
      to: "src/experiments/materialize.jl"
      via: "draw_forecast_error derives its stream from sub_seed(master, tag) with a NEW tag (:mpc_forecast_pv_<t>/:mpc_forecast_demand_<t>), never :profiles/:population"
      pattern: "sub_seed\\("
    - from: "test/test_mpc_terminal.jl"
      to: "src/models/mpc_window.jl"
      via: "build_mpc_window(...; terminal_soc=false) vs (...; terminal_soc=true), driven by a manual per-step loop calling set_parameter_value! + solve_mpc_window! + propagate_soc/propagate_tin"
      pattern: "terminal_soc = (true|false)"
---

<objective>
Add D-12's minimal additive `Scenario` fields, the pure nominal-plant state-propagation and
seeded forecast-error-draw primitives `run_mpc` (plan 21-05) will consume, and — using ONLY
plan 21-03's `build_mpc_window`/`solve_mpc_window!` directly, before the full orchestrator
exists — the MPC-02 regression proving the hard terminal-SOC condition prevents the
end-of-horizon dump/hoard artifact.

Purpose: validates the phase's SECOND-hardest deliverable (after build-once mechanics, plan
21-03) at its own, minimal, orchestrator-free level — a receding-horizon loop's dump/hoard
artifact is a property of `build_mpc_window`'s `terminal_soc` toggle plus a state-propagation
recursion, NOT of the (not-yet-built) full `run_mpc` orchestrator. Proving it here, against the
primitive directly, means Wave 4's `run_mpc` build inherits an ALREADY-validated terminal
condition instead of co-discovering it under full end-to-end pressure.

Output: `src/experiments/Scenario.jl` gains four fields (D-12), `src/models/mpc_window.jl` gains
three orchestration-internal helper functions (D-05/D-08), and `test/test_mpc_terminal.jl`
(new) demonstrates the present/absent artifact pair (MPC-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-CONTEXT.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-PATTERNS.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-03-SUMMARY.md

@src/experiments/Scenario.jl
@src/experiments/materialize.jl
@src/data/profiles.jl
@src/models/mpc_window.jl
@src/models/welfare_solve.jl
</context>

<interfaces>
`Scenario`'s exact `@kwdef` + inner-constructor pattern this plan extends (verified,
`src/experiments/Scenario.jl:92-214`):

```
Base.@kwdef struct Scenario
    name::String
    ...
    μ::Float64 = 10.0

    function Scenario(name::String, feeder::Symbol, ..., μ::Float64)
        # ... existing guards ...
        if τ_ratio <= 0 || μ <= 0
            throw(ArgumentError("Scenario: τ_ratio/μ must be > 0; got τ_ratio=$τ_ratio, μ=$μ"))
        end
        return new(name, feeder, ..., μ)
    end
end
```
This plan appends four fields AFTER `μ` (both in the `@kwdef` field list and the inner
constructor's positional-argument list and `return new(...)` call — all three must move
together), with guards added in the SAME `if ... throw(ArgumentError(...))` idiom as every
existing guard in this file. `SCENARIO_VALID_STRATEGIES` (line 32) and `run_scenario`'s
`if`/`elseif` (src/experiments/run.jl) are NOT touched (Pitfall 7).

`sub_seed`'s exact signature this plan's `draw_forecast_error` calls (verified,
`src/experiments/materialize.jl:16-25`):

```
sub_seed(master::Integer, tag::Symbol) -> Int    # pure hash derivation, NEVER touches the global RNG
```

`StableRNGs`/`markov_path`'s exact seeded-draw idiom this plan mirrors (verified,
`src/data/profiles.jl:89,221`):

```
rng = StableRNGs.LehmerRNG(seed)
u = rand(rng)          # a single draw, reproducible via rng
```

Plan 21-03's `MpcWindow`/`build_mpc_window`/`solve_mpc_window!` (read the EXACT signatures from
`21-03-SUMMARY.md` — the sketch below is illustrative only):

```
o = build_mpc_window(feeder, pf, aggregators; H, terminal_soc) -> MpcWindow
o.p_import::Vector{VariableRef}          # length H, for set_objective_coefficient
o.ic_handles::Vector{<:NamedTuple}       # (; bus, kind::Symbol(:soc|:Tin), ic_param, terminal_param)
solve_mpc_window!(o; max_attempts=4, attempts_out=nothing)
```

Day-ahead SOC-trajectory extraction idiom (verified pattern, `src/models/welfare_solve.jl`'s
`ctx.meta[:agg_device_vars]` stash, the SAME stash `assert_battery_complementarity!` reads):

```
ctx_da, welfare_da, _ = solve_welfare(feeder, pf, aggs; T = T, λ₀ = λ₀_full, allow_export = true)
soc_da = Dict(bus => [value(v.soc[t]) for t in 1:T]
              for (bus, varlist) in ctx_da.meta[:agg_device_vars] for v in varlist if haskey(v, :soc))
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scenario.jl D-12 additive fields</name>
  <files>src/experiments/Scenario.jl</files>
  <read_first>
    - src/experiments/Scenario.jl (full file — the exact field list, inner-constructor
      positional order, and every existing guard's phrasing)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md, "Pitfall 6:
      Scenario.jl's DrWatson savename schema is NOT hash-stable under ANY new field" and
      "Pitfall 7: SCENARIO_VALID_STRATEGIES has no :mpc selector" (both RESOLVED — read the
      resolutions, they constrain HOW this task adds fields)
  </read_first>
  <behavior>
    - `Scenario(; name = "t")` (every other field defaulted) constructs successfully with
      `mpc_H == 6`, `mpc_step == 1`, `mpc_terminal_soc == true`, `mpc_forecast_error == 0.05`.
    - `Scenario(; name = "t", mpc_H = 0)` throws `ArgumentError`; `mpc_step = 0` throws
      `ArgumentError`; `mpc_forecast_error = -0.1` and `mpc_forecast_error = 1.0` both throw
      `ArgumentError` (the valid range is `0 <= mpc_forecast_error < 1`, a bounded FRACTION —
      `1.0` would make a perturbed value's sign flip possible, non-physical).
    - `SCENARIO_VALID_STRATEGIES` is textually UNCHANGED by this task (still exactly
      `(:centralized, :admm)`).
    - Every PRE-EXISTING `Scenario(...)` call site in the test suite that supplies no `mpc_*`
      kwarg still constructs successfully (the new fields are purely additive with sane
      defaults — no existing positional-argument call site exists in this codebase, since
      `Scenario` is `@kwdef`-only; grep confirms every call site uses keyword args).
  </behavior>
  <action>
    In `src/experiments/Scenario.jl`, add four new `@kwdef` fields immediately after `μ::Float64
    = 10.0`: `mpc_H::Int = 6` (window length; sane default for the `T=24` day-ahead fixture,
    giving `T - H + 1 = 19` published steps), `mpc_step::Int = 1` (step size between successive
    windows — kept `1` for this rung, a hook for a future coarser stride), `mpc_terminal_soc::Bool
    = true` (D-06's hard terminal condition, on by default), `mpc_forecast_error::Float64 = 0.05`
    (D-08's bounded PV/demand perturbation magnitude, ±5% by default). Add the SAME four names,
    in the SAME order, to the inner constructor's positional-argument list (immediately after
    `μ::Float64`) and to the trailing `return new(...)` call. Add guards in the existing file's
    idiom, positioned after the `τ_ratio`/`μ` guard: `mpc_H < 1` throws `ArgumentError` naming
    "window length H"; `mpc_step < 1` throws `ArgumentError` naming "step size"; `!(0 <=
    mpc_forecast_error < 1)` throws `ArgumentError` naming "a bounded fraction in [0, 1)".
    Extend the struct's docstring's "# Fields" section with the four new entries, EXPLICITLY
    noting (mirroring D-12's own documented cost) that these fields are no-ops for the
    `:centralized`/`:admm` strategies — consumed only by the phase's own `run_mpc(scenario)`
    entry point (plan 21-05), NEVER by `run_scenario`'s `strategy` dispatch — and that adding
    them changes every `Scenario`'s `savename` STRING (a documented, accepted cost per
    RESEARCH.md Pitfall 6's resolution) while preserving every EXISTING numeric golden result
    (the new fields are read by no `:centralized`/`:admm` code path).

    Do NOT touch `SCENARIO_VALID_STRATEGIES`, `run.jl`'s `if`/`elseif`, or any other file.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, Test; s = Scenario(; name = "t"); @test s.mpc_H == 6; @test s.mpc_step == 1; @test s.mpc_terminal_soc == true; @test s.mpc_forecast_error == 0.05; @test_throws ArgumentError Scenario(; name = "t", mpc_H = 0); @test_throws ArgumentError Scenario(; name = "t", mpc_step = 0); @test_throws ArgumentError Scenario(; name = "t", mpc_forecast_error = -0.1); @test_throws ArgumentError Scenario(; name = "t", mpc_forecast_error = 1.0); @test Scenario(; name = "t", mpc_forecast_error = 0.0) isa Scenario; @test TSODSO.SCENARIO_VALID_STRATEGIES == (:centralized, :admm); println("OK")'</automated>
  </verify>
  <done>Scenario gains four additive mpc_* fields with guards; SCENARIO_VALID_STRATEGIES and run.jl are untouched.</done>
  <acceptance_criteria>
    - `grep -n 'mpc_H::Int = 6' src/experiments/Scenario.jl` matches
    - `grep -n 'mpc_forecast_error::Float64 = 0.05' src/experiments/Scenario.jl` matches
    - `git diff --stat src/experiments/run.jl` (or an equivalent "this file is untouched" check) shows NO changes to `src/experiments/run.jl`
    - The verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nominal-plant state propagation + seeded forecast-error draw</name>
  <files>src/models/mpc_window.jl</files>
  <read_first>
    - src/models/mpc_window.jl (plan 21-03's output, full file — these new functions land at
      the END of this file, after `solve_mpc_window!`, since they are orchestration-adjacent
      primitives peer to the window builder, not the window itself)
    - src/experiments/materialize.jl (lines 16-25 — `sub_seed`'s exact signature)
    - src/data/profiles.jl (lines 85-95 — the `rand(rng)` idiom, and the file's own warning
      about NEVER touching the global RNG)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-CONTEXT.md, D-05 ("apply
      each step's first-interval optimal controls to the ground-truth device dynamics") and
      D-08 ("seeded bounded perturbation... constant within a window, regenerated per step")
  </read_first>
  <behavior>
    - `propagate_soc(0.1, 0.02, 0.0, 0.95, 1.0) == 0.1 + 0.95*0.02` (pure charge, no discharge,
      hand-computable) and `propagate_soc(0.1, 0.0, 0.02, 0.95, 1.0) == 0.1 - 0.02/0.95` (pure
      discharge) — both exact floating-point equalities, no tolerance needed for a pure
      arithmetic function.
    - `propagate_tin(22.0, 0.5, 0.2, 0.05, 25.0) == 22.0 + 0.2*(25.0-22.0) - 0.05*0.5`
      (hand-computable exact equality).
    - `draw_forecast_error(seed, t, 0.0)` returns `(; pv_factor = 1.0, demand_factor = 1.0)` for
      EVERY `seed`/`t` (a `magnitude = 0.0` call is a deterministic no-op — sanity floor).
    - `draw_forecast_error(seed, t, magnitude)` for `magnitude > 0` returns
      `pv_factor`/`demand_factor` BOTH strictly inside `[1 - magnitude, 1 + magnitude]`.
    - `draw_forecast_error(seed, t, m)` called TWICE with the SAME `(seed, t, m)` returns the
      IDENTICAL `NamedTuple` (deterministic); called with a DIFFERENT `t` (same `seed`/`m`)
      returns a DIFFERENT draw with overwhelming probability (independent per-step stream, D-08).
    - Neither function ever calls `Random.seed!` or touches the global RNG (verifiable by grep:
      no `Random.seed!`/bare `rand()` without an explicit `rng` argument appears in this file).
  </behavior>
  <action>
    At the end of `src/models/mpc_window.jl` (after `solve_mpc_window!` and its `export` line —
    these three functions stay UNEXPORTED, orchestration-internal, consumed via `TSODSO.`
    qualification by `run_mpc` and this plan's own tests, mirroring how internal helpers
    elsewhere in this codebase are tested without being part of the public API surface), add:

    `propagate_soc(soc::Real, p_ch1::Real, p_dch1::Real, η::Real, Δt::Real) -> Float64` —
    docstring citing D-05 and the SAME SOC recursion `PVBattery`/`FourQuadBESS` already encode
    (thesis eq. 3.6): `soc + (η*p_ch1 - p_dch1/η)*Δt`, evaluated OUTSIDE the optimizer on the
    REALIZED (solved) first-interval controls `p_ch1`/`p_dch1` — never re-solving anything, a
    pure arithmetic re-derivation of the device's own equation.

    `propagate_tin(Tin::Real, p1::Real, α::Real, β::Real, Tout_true::Real) -> Float64` —
    docstring citing D-05 and the RC/ETP recursion `Thermostatic` already encodes (thesis eq.
    3.2): `Tin + α*(Tout_true - Tin) - β*p1`. Explicitly document that `Tout_true` MUST be the
    GROUND-TRUTH ambient value at that absolute hour, never the forecast-perturbed slice the
    window's OWN optimizer saw when choosing `p1` — D-05's "model mismatch enters only via
    forecast error" reading means the physical recursion always uses truth, only the optimizer's
    CHOICE was made under a forecast.

    `draw_forecast_error(seed::Integer, t::Integer, magnitude::Real) -> NamedTuple` — docstring
    citing D-08. Guards: `0 <= magnitude < 1` else `ArgumentError` (mirrors `Scenario`'s own
    guard — defensive-in-depth, this function may be called directly). If `magnitude == 0`,
    short-circuit `return (; pv_factor = 1.0, demand_factor = 1.0)` (no draw at all — avoids a
    wasted RNG construction on the deterministic no-error path). Otherwise: derive TWO
    INDEPENDENT seeds via `sub_seed(seed, Symbol("mpc_forecast_pv_", t))` and
    `sub_seed(seed, Symbol("mpc_forecast_demand_", t))` (fresh tags, NEVER `:profiles`/
    `:population` — Pitfall 5's independent-stream discipline, extended with a per-`t` suffix so
    each window's draw is genuinely regenerated, D-08's "regenerated per step"); construct a
    FRESH `StableRNGs.LehmerRNG` from each derived seed; compute
    `pv_factor = 1.0 + magnitude * (2 * rand(rng_pv) - 1)` and the `demand_factor` analog from
    the SECOND rng; return `(; pv_factor, demand_factor)`. Never call `Random.seed!` or use the
    global RNG anywhere in this function.

    In `src/TSODSO.jl`, no new include is needed (these functions land in the ALREADY-included
    `models/mpc_window.jl`) — confirm the file still loads cleanly after this addition.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, Test; @test TSODSO.propagate_soc(0.1, 0.02, 0.0, 0.95, 1.0) == 0.1 + 0.95*0.02; @test TSODSO.propagate_soc(0.1, 0.0, 0.02, 0.95, 1.0) == 0.1 - 0.02/0.95; @test TSODSO.propagate_tin(22.0, 0.5, 0.2, 0.05, 25.0) == 22.0 + 0.2*(25.0-22.0) - 0.05*0.5; d0 = TSODSO.draw_forecast_error(1, 1, 0.0); @test d0.pv_factor == 1.0 && d0.demand_factor == 1.0; d1 = TSODSO.draw_forecast_error(42, 3, 0.1); @test 0.9 <= d1.pv_factor <= 1.1; @test 0.9 <= d1.demand_factor <= 1.1; d1b = TSODSO.draw_forecast_error(42, 3, 0.1); @test d1 == d1b; d2 = TSODSO.draw_forecast_error(42, 4, 0.1); @test d2 != d1; @test_throws ArgumentError TSODSO.draw_forecast_error(1, 1, 1.0); println("OK")'</automated>
  </verify>
  <done>propagate_soc/propagate_tin/draw_forecast_error exist in mpc_window.jl, are pure/deterministic/RNG-isolated, and match the hand-computed expected values.</done>
  <acceptance_criteria>
    - `grep -n 'function propagate_soc\|function propagate_tin\|function draw_forecast_error' src/models/mpc_window.jl` returns 3 matches
    - `grep -n 'Random.seed!\|GLOBAL_RNG' src/models/mpc_window.jl` returns NO matches
    - `grep -n 'sub_seed(seed' src/models/mpc_window.jl` returns at least 2 matches (the two independent tags)
    - The verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: test_mpc_terminal.jl — MPC-02 dump/hoard A/B regression against build_mpc_window directly</name>
  <files>test/test_mpc_terminal.jl</files>
  <read_first>
    - src/models/mpc_window.jl (Tasks 1-2's cumulative output plus plan 21-03's builder, full file)
    - test/fixtures_phase21.jl (plan 21-03's output, full file)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md, "Terminal-SOC
      wiring (MPC-02, D-06)" code example (the day-ahead SOC-trajectory extraction idiom)
  </read_first>
  <action>
    Create `test/test_mpc_terminal.jl` with a file-header comment: SEAM = MPC-02, every item
    name contains `"mpc_terminal"`, `tags = [:mpc_terminal]`, `setup = [Phase21Fixtures]`.

    Add ONE `@testitem "mpc_terminal: hard terminal-SOC condition prevents end-of-horizon
    dump/hoard, present when disabled (MPC-02)"`. Inside:

    1. Build `feeder = Phase21Fixtures.mpc_feeder()`, `aggs = Phase21Fixtures.build_mpc_aggregators(feeder)`,
       `T = Phase21Fixtures.T`, `H = Phase21Fixtures.H`, `λ₀ = Phase21Fixtures.mpc_lambda0()`.
    2. Solve the day-ahead perfect-foresight benchmark ONCE:
       `ctx_da, welfare_da, _ = solve_welfare(feeder, ConvexBranchFlow(), aggs; T = T, λ₀ = λ₀,
       allow_export = true)`, then extract `soc_da = Dict(bus => [value(v.soc[t]) for t in 1:T]
       for (bus, varlist) in ctx_da.meta[:agg_device_vars] for v in varlist if haskey(v, :soc))`
       (the RESEARCH-verified idiom).
    3. Define a small LOCAL helper (a `let`/inner function inside the `@testitem`, not a new
       exported symbol) `run_mini_loop(terminal_soc::Bool) -> Float64` that: builds
       `o = build_mpc_window(feeder, ConvexBranchFlow(), aggs; H = H, terminal_soc = terminal_soc)`;
       initializes `soc_measured = <the aggregator's battery device's own d.soc0 literal>`
       (the SAME value the day-ahead solve started from); loops `t in 1:(T - H + 1)`: sets the
       window's `soc` `ic_handles` entry's `ic_param` to `soc_measured` via `set_parameter_value`;
       if `terminal_soc`, sets its `terminal_param` to `soc_da[bus][min(t + H - 1, T)]`; sets
       every `Ppv_param`/`Tout_param`/`Pdc_param` slice (reached via
       `o.ctx.meta[:agg_device_vars]`/`o.agg_pdc_handles`) to the TRUE ground-truth slice
       `[t, t+H-1]` (NO forecast error in this regression — isolate the terminal-condition
       effect alone, not compounded with D-08's perturbation); slides `λ₀` via
       `set_objective_coefficient` for each `o.p_import[τ]` using `λ₀[t+τ-1]`; calls
       `solve_mpc_window!(o)`; reads the FIRST-interval solved `p_ch`/`p_dch` for the battery
       device (via `o.ctx.meta[:agg_device_vars]`); updates
       `soc_measured = TSODSO.propagate_soc(soc_measured, value(p_ch[1]), value(p_dch[1]), η, Δt)`
       (using the SAME `η`/`Δt` the fixture's `PVBattery` was constructed with). Returns the
       FINAL `soc_measured` after the loop.
    4. Call `soc_final_disabled = run_mini_loop(false)` and `soc_final_enabled =
       run_mini_loop(true)`. Read `soc_da_final = soc_da[<battery bus>][T - H + 1 + H - 1]` (the
       day-ahead trajectory's value at the last window's terminal hour — the reference the
       enabled case is pinned toward).
    5. MEASURE (do not assume) the two deviations: `dev_disabled = abs(soc_final_disabled -
       soc_da_final)` and `dev_enabled = abs(soc_final_enabled - soc_da_final)`. Assert
       `dev_enabled < dev_disabled` (the ENABLED case tracks the day-ahead trajectory strictly
       better) AND assert `dev_disabled` exceeds a small measured floor (e.g. `dev_disabled >
       5 * dev_enabled`, or an absolute floor derived from THIS run's own printed values — pick
       whichever margin the actual measured numbers support; `@info` both deviations so the
       margin is citable in the plan's SUMMARY). If the fixture's own scale makes the disabled
       case's dump/hoard direction ambiguous (e.g. `dev_disabled` too close to `dev_enabled`),
       widen `T`/`H`'s ratio or `Phase21Fixtures`'s battery headroom rather than weakening the
       assertion — the artifact must be UNAMBIGUOUSLY measured, per MPC-02's own acceptance
       criterion, not merely plausible.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, JuMP, Test; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false)]; branches = [Branch(1,2,1e-3,1e-3,SMAX_NO_LIMIT)]; feeder = Feeder(buses, branches, 1); T = 8; H = 3; prof = generate_profiles(seed = 20260809 + 2, T = T); Ppv = Float64[0.01 * p for p in prof.pv]; Pdc = Float64[0.02 * d for d in prof.demand]; Tout = Float64[19,18,17,16,16,17,19,21]; therm = Thermostatic(2, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, Tout); soc0 = 0.2*0.02; batt = PVBattery(2, 0.95, 1.0, 0.1*0.02, 0.0, 0.4*0.02, soc0, 3.8, 6.2, 8.9, Ppv); agg = Aggregator(2, 0.9, AbstractDevice[therm, batt], Pdc); λ0 = fill(4.0, T); ctx_da, welfare_da, _ = solve_welfare(feeder, ConvexBranchFlow(), [agg]; T = T, λ₀ = λ0, allow_export = true); soc_da = only([value(v.soc[t]) for t in 1:T] for (b,vs) in ctx_da.meta[:agg_device_vars] for v in vs if haskey(v,:soc)); println("soc_da = ", soc_da); o = build_mpc_window(feeder, ConvexBranchFlow(), [agg]; H = H, terminal_soc = false); println("window built OK, ic_handles kinds = ", [h.kind for h in o.ic_handles])'</automated> -- per-task quick check: plain Test.jl under --project=., proves the day-ahead trajectory extraction + a terminal_soc=false window both build/solve on the exact fixture shape this test drives; the full A/B measured-margin assertion is written directly into the persistent @testitem (Task 3's own file) and exercised in this plan's closing &lt;verification&gt;.
  </verify>
  <done>test/test_mpc_terminal.jl exists with one green @testitem proving dev_enabled &lt; dev_disabled with a measured, documented margin.</done>
  <acceptance_criteria>
    - `grep -c '@testitem' test/test_mpc_terminal.jl` returns 1
    - The item's body contains BOTH `terminal_soc = false` and `terminal_soc = true` calls to `build_mpc_window`
    - The item's body contains an `@info` (or equivalent printed) statement reporting both measured deviations
    - The item asserts `dev_enabled < dev_disabled` (or the equivalent named comparison) — not merely that both are finite
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher-supplied `Scenario` mpc_* kwargs and `draw_forecast_error`'s `seed`/`t`/`magnitude` args | The only "input" surface; a future in-process orchestrator caller, not attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Tampering | `draw_forecast_error` accidentally reuses `:profiles`/`:population`'s `sub_seed` tag, silently coupling the forecast-error stream to the ground-truth profile stream (breaking INFRA-04-style independence) | mitigate | Task 2's `acceptance_criteria` greps confirm two DISTINCT, freshly-derived tags; the behavior block asserts different `t` values yield different draws |
| T-21-11 | Repudiation | The MPC-02 regression's measured margin is too close to call, silently passing on noise rather than a genuine artifact | mitigate | Task 3's action explicitly instructs widening the fixture rather than weakening the assertion if the margin is ambiguous; the `@info` print makes the measured numbers citable/auditable in the SUMMARY |
| T-21-12 | Information Disclosure | N/A — no secrets, no network surface | accept | out of scope for a local optimization library |
</threat_model>

<verification>
Targeted regression (not the full suite): `julia --project=. -e 'import Pkg; Pkg.precompile()'`
confirms clean compilation after the `Scenario.jl`/`mpc_window.jl` edits. Re-run all three
task-level `<verify>` inline scripts (idempotent) and confirm all print `OK`/expected output.
Defer the actual `@testitem` execution (including `test_mpc_terminal.jl`'s full A/B measured
assertion) to the phase-closing full-suite acceptance (21-06).
</verification>

<success_criteria>
- `Scenario` carries D-12's four additive fields, guarded, with `SCENARIO_VALID_STRATEGIES`
  untouched.
- `propagate_soc`/`propagate_tin`/`draw_forecast_error` exist, are pure/deterministic, and never
  touch the global RNG.
- A measured, documented margin proves the hard terminal-SOC condition prevents the dump/hoard
  artifact on the phase's CI fixture (MPC-02).
</success_criteria>

<output>
Create `.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-04-SUMMARY.md` when done.
Record the exact measured `dev_disabled`/`dev_enabled` values and the margin chosen — plan 21-05's
full `run_mpc` orchestrator and the literate page (21-06) may cite these numbers.
</output>
