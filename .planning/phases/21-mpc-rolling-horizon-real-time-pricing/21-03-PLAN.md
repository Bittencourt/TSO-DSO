---
phase: 21-mpc-rolling-horizon-real-time-pricing
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/models/mpc_window.jl
  - src/TSODSO.jl
  - docs/src/api.md
  - test/fixtures_phase21.jl
  - test/test_mpc_window.jl
autonomous: true
requirements: [MPC-01, MPC-02]

must_haves:
  truths:
    - "A researcher can build a fixed-length window [τ=1:H] welfare-shaped model EXACTLY ONCE (build_mpc_window), mirroring solve_welfare's SHAPE via PlanningOracle's OWN precedent, and re-solve it many times via set_parameter_value! + set_objective_coefficient with num_variables/num_constraints provably UNCHANGED (D-01, D-02 read as the centralized welfare MATH shape, D-03)."
    - "The window builder makes the SEAM-01 horizon_state stub (src/models/oracle.jl:76-80) load-bearing: soc0/Tin0 initial-condition Parameters and Ppv_param/Tout_param/Pdc_param forecast-slice Parameters (from plan 21-01) are wired and settable per absolute hour without a rebuild."
    - "A build-time terminal_soc::Bool toggle adds (true) or omits (false) a hard equality soc[H]==terminal_target constraint per battery-like device, the mechanism MPC-02's dump/hoard regression (plan 21-04) exercises (D-06); Thermostatic carries NO terminal condition on Tin (D-07)."
    - "The frontier price λ₀ is NEVER a Parameter — it slides via set_objective_coefficient on p_import, mirroring AgrOpt.jl/DsoOpt.jl's own documented avoidance of the Parameter-times-variable bilinear failure (Pitfall 2)."
  artifacts:
    - path: "src/models/mpc_window.jl"
      provides: "MpcWindow struct + build_mpc_window(feeder, pf, aggregators; H, terminal_soc=true) + solve_mpc_window!(o; max_attempts=4, attempts_out=nothing)"
      contains: "struct MpcWindow"
    - path: "test/fixtures_phase21.jl"
      provides: "Phase21Fixtures @testmodule: mpc_feeder()/build_mpc_aggregators (short-T CI fixture) + mpc_high_pv_feeder()/build_mpc_high_pv_aggregators (Wave 4's forced-inexact fixture)"
      contains: "@testmodule Phase21Fixtures"
    - path: "test/test_mpc_window.jl"
      provides: "@testitem coverage: build-once num_variables/num_constraints invariance across re-solves at different soc0/Tin0/terminal-target/forecast-slice states, and non-no-op Parameter re-solve correctness"
      contains: "@testitem"
  key_links:
    - from: "src/models/mpc_window.jl"
      to: "src/planning/subproblem.jl"
      via: "build_mpc_window mirrors build_planning_oracle's SHAPE (model build, contribute! reuse, free-sign frontier, balance registration) — never calling solve_welfare itself inside the model builder"
      pattern: "select_optimizer\\(problem_class\\(pf\\)\\)"
    - from: "src/models/mpc_window.jl"
      to: "src/devices/PVBattery.jl"
      via: "reads ctx.meta[:agg_device_vars] (populated by Aggregator.contribute!) to collect each device's soc0/Tin0/Ppv_param/Tout_param Parameter handles — no new device call, pure consumption of plan 21-01's widened contract"
      pattern: "agg_device_vars"
---

<objective>
Build `MpcWindow`/`build_mpc_window`/`solve_mpc_window!` — the build-once, fixed-length
`[τ=1:H]` welfare-shaped JuMP model a receding-horizon step re-solves via `set_parameter_value!`
+ `set_objective_coefficient`, never a rebuild. This is MPC-01's core mechanism and the seam
MPC-02's terminal-SOC toggle attaches to.

Purpose: mirrors the v2.0 `PlanningOracle` precedent (`src/planning/subproblem.jl`) — the ONLY
existing build-once/`Parameter`-re-solve model in this codebase — generalized from a single
`z`-pin to the full set of per-step-varying inputs plan 21-01 just widened: battery/thermostatic
initial conditions, PV/ambient/demand forecast slices, and an optional hard terminal-SOC target.
`welfare_solve.jl`/`oracle.jl` remain byte-for-byte untouched (D-01/D-03) — this is a WHOLLY NEW
module reusing `contribute!(pf,...)`/`contribute!(agg,...)` verbatim, exactly as `PlanningOracle`
already does one level up in the planning layer. D-02's "each window solves the centralized
`solve_welfare` problem" is read here as describing the MATH shape (centralized welfare, not
ADMM decomposition), consistent with D-01/D-03's explicit build-once/never-rebuilt requirement
and `PlanningOracle`'s own precedent of mirroring `solve_welfare`'s shape in a NEW builder rather
than literally calling the function (RESEARCH.md Open Question 1, resolved).

Output: `src/models/mpc_window.jl` (new), one new `test/fixtures_phase21.jl` fixture module (the
phase's small CI substrate, reused by every later wave), and `test/test_mpc_window.jl` proving
the build-once invariant and genuine (non-no-op) Parameter re-solve correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-CONTEXT.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-PATTERNS.md
@.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-01-SUMMARY.md

@src/planning/subproblem.jl
@src/models/welfare_solve.jl
@src/core/ModelContext.jl
@src/devices/Aggregator.jl
@src/admm/AgrOpt.jl
@test/fixtures_phase4.jl
@test/fixtures_phase6.jl
@test/test_planning_oracle.jl
</context>

<interfaces>
`PlanningOracle`'s exact build-once shape this plan generalizes (verified,
`src/planning/subproblem.jl:110-211`):

```
model = Model(select_optimizer(problem_class(pf)))
JuMP.add_bridge(model, JuMP.MOI.Bridges.Constraint.RSOCtoNonConvexQuadBridge)
JuMP.add_bridge(model, JuMP.MOI.Bridges.Constraint.SOCtoNonConvexQuadBridge)
ctx = ModelContext(model)
ctx.meta[:feeder] = feeder; ctx.meta[:T] = T; ctx.meta[:problem_class] = problem_class(pf)
contribute!(pf, ctx, feeder; T = T)
@variable(model, p_import[t = 1:T])                      # FREE-SIGN frontier (no lower bound)
for t in 1:T; add_to_residual!(ctx, :Rp, feeder.root, t, p_import[t]); end
reactive = haskey(ctx.residuals, :Rq)                     # captured BEFORE aggregator writes
if reactive; @variable(model, q_import[t=1:T]); ...; end
for agg in aggregators; contribute!(agg, ctx; T = T); end
@constraint(model, balance_p[j=1:N,t=1:T], ctx.residuals[:Rp][j,t] == 0)
register_constraint!(ctx, :balance_p, balance_p)
if reactive; ...balance_q...; end
@variable(model, z[t=1:T] in Parameter(0.0)); @constraint(model, pin[t=1:T], p_import[t] == z[t])
@objective(model, Max, ctx.meta[:objective] - sum(λ₀[t] * p_import[t] for t in 1:T))
```

Plan 21-01's widened device return-tuple shapes this plan CONSUMES (read from
`ctx.meta[:agg_device_vars]`, a `Dict{Int,Vector{Any}}` keyed by aggregator bus — see
`src/devices/Aggregator.jl:191-193`):

```
PVBattery.contribute!    -> (; vars = (; p_ch, p_dch, soc, pv_used, soc0, Ppv_param), ...)
Thermostatic.contribute! -> (; vars = (; p, Tin, Tin0, Tout_param), ...)
FourQuadBESS.contribute! -> (; vars = (; p_ch, p_dch, soc, q, soc0), ...)
Aggregator.contribute!   -> (; vars, p_inject, q_inject, utility, Pdc_param)   # Pdc_param at TOP level, NOT inside vars
```

`set_objective_coefficient` price-slide idiom this plan's caller (Wave 4) will use — NEVER a
`Parameter` for `λ₀` (verified, `src/admm/AgrOpt.jl:282-290`):

```
for t in 1:agr.T
    set_objective_coefficient(agr.model, agr.pag[t], -λ_j[t] - ρ * c_j[t])
end
```
This plan's `MpcWindow` struct must expose `p_import::Vector{VariableRef}` (length `H`) so that
caller can do `set_objective_coefficient(o.model, o.p_import[τ], -λ0_window[τ])` for
`τ = 1:H` without touching this file again.

Build-once regression idiom (verified, `test/test_planning_oracle.jl:69-87`):

```
nv0 = num_variables(o.model)
nc0 = num_constraints(o.model; count_variable_in_set_constraints = true)
set_parameter_value.(o.z, fill(0.01, T)); optimize!(o.model)
set_parameter_value.(o.z, fill(-0.02, T)); optimize!(o.model)
@test num_variables(o.model) == nv0
@test num_constraints(o.model; count_variable_in_set_constraints = true) == nc0
```
`count_variable_in_set_constraints = true` is REQUIRED (Parameters are variable-in-
`MOI.Parameter`-set constraints; omitting the flag hides exactly what a rebuild would change).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MpcWindow struct + build_mpc_window (build-once model, terminal-SOC toggle)</name>
  <files>src/models/mpc_window.jl, src/TSODSO.jl, docs/src/api.md</files>
  <read_first>
    - src/planning/subproblem.jl (full file — the EXACT build-once shape this file generalizes)
    - src/devices/Aggregator.jl (lines 144-196 — confirms `ctx.meta[:agg_device_vars]`'s exact
      shape and that `Pdc_param` is returned at the Aggregator's OWN top level, not nested)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-01-SUMMARY.md (the EXACT
      widened return-tuple field names plan 21-01 produced — read this, do not assume the
      `<interfaces>` sketch above is byte-perfect if the summary records a deviation)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md, "Terminal-SOC
      wiring (MPC-02, D-06)" and "Pitfall 5: Window-boundary shrinkage vs a fixed build-once
      horizon length" (the FIXED-`H`-throughout convention this builder implements)
  </read_first>
  <behavior>
    - `build_mpc_window(feeder, pf, aggregators; H = 3)` throws `ArgumentError` on `isempty(aggregators)`,
      `H < 1`, or an aggregator bus outside `1:length(feeder.buses)` — mirroring
      `build_planning_oracle`'s own boundary-guard idiom, BEFORE any model assembly.
    - The returned `MpcWindow.model` has `num_variables`/`num_constraints` that are IDENTICAL
      across two `build_mpc_window` calls with the SAME arguments (deterministic construction).
    - Every stateful device's `soc0`/`Tin0` Parameter (from `ctx.meta[:agg_device_vars]`) is
      reachable from the returned `MpcWindow` (via `o.ic_handles` or equivalent), and
      `set_parameter_value` on it followed by `solve_mpc_window!` changes the solved `soc[1]`/
      `Tin[1]` value accordingly — NOT a no-op.
    - With `terminal_soc = true`, a battery-like device's window has ONE additional equality
      constraint pinning `soc[H]` to a settable Parameter target; with `terminal_soc = false`,
      that constraint is ABSENT (a genuinely different model — this is the ONE build-time toggle
      this plan permits per RESEARCH's own "one rebuild per regression run, not per MPC step"
      guidance).
    - `solve_mpc_window!(o)` never adds a variable or constraint to `o.model` (delegates
      entirely to `solve_with_retry!`).
  </behavior>
  <action>
    Create `src/models/mpc_window.jl` with a header comment in the `src/planning/subproblem.jl`
    shape: SEAM = "build-once receding-horizon window model (MPC-01/MPC-02)", OWNER = "plan
    21-03", stating explicitly that `welfare_solve.jl`/`oracle.jl` are UNMODIFIED (D-01/D-03) and
    that D-02's "solves the centralized `solve_welfare` problem" is read as the MATH shape, per
    this plan's `<objective>` resolution.

    Define `struct MpcWindow{F}` with fields `model::Model`, `ctx::ModelContext`, `H::Int`,
    `agg_bus::Int`, `feeder::F`, `p_import::Vector{VariableRef}`, `ic_handles::Vector{<:NamedTuple}`
    (one entry per stateful device: `(; bus::Int, kind::Symbol, ic_param, terminal_param)` where
    `kind` is `:soc` or `:Tin` and `terminal_param` is `nothing` unless `kind == :soc &&
    terminal_soc`), `agg_pdc_handles::Vector{<:NamedTuple}` (`(; bus::Int, Pdc_param)`),
    `terminal_soc::Bool`.

    Define `build_mpc_window(feeder, pf::AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator};
    H::Int, terminal_soc::Bool = true) -> MpcWindow`. Boundary guards FIRST (mirror
    `build_planning_oracle` lines 119-128): empty `aggregators`, `H < 1`, aggregator bus out of
    range — each a loud `ArgumentError` before any model assembly. Then: `model =
    Model(select_optimizer(problem_class(pf)))`; register the SAME two cross-solver bridges
    `build_planning_oracle`/`solve_welfare` register; `ctx = ModelContext(model)`;
    `ctx.meta[:feeder] = feeder; ctx.meta[:T] = H; ctx.meta[:problem_class] = problem_class(pf)`;
    `contribute!(pf, ctx, feeder; T = H)`; a FREE-SIGN `p_import[τ = 1:H]` injected into `:Rp` at
    `feeder.root` (mirrors `PlanningOracle`'s free-sign frontier — export must stay ALLOWED so
    the window can legitimately traverse the overvoltage boundary per D-04, never import-only);
    capture `reactive = haskey(ctx.residuals, :Rq)` immediately after, BEFORE any aggregator
    write (WR-03 ordering); if `reactive`, a free-sign `q_import[τ=1:H]` too. Loop
    `for agg in aggregators; contribute!(agg, ctx; T = H); end` — this is where plan 21-01's
    widened `soc0`/`Tin0`/`Ppv_param`/`Tout_param`/`Pdc_param` Parameters get created and stashed
    into `ctx.meta[:agg_device_vars]`/each `Aggregator.contribute!` return (capture the LATTER
    per-aggregator to collect `Pdc_param`). Close `:balance_p`/`:balance_q` exactly like
    `build_planning_oracle` (size guard + `@constraint` + `register_constraint!`).

    THEN, walk `ctx.meta[:agg_device_vars]` (a `Dict{Int,Vector{Any}}`) to populate `ic_handles`:
    for every `(bus, varlist)` pair and every `v` in `varlist`, if `haskey(v, :soc0)` push
    `(; bus, kind = :soc, ic_param = v.soc0, terminal_param = nothing)`; if `haskey(v, :Tin0)`
    push `(; bus, kind = :Tin, ic_param = v.Tin0, terminal_param = nothing)` — NO terminal
    handling for `:Tin` (D-07: no terminal condition on thermostatic temperature, ever). If
    `terminal_soc`, for every `:soc`-kind entry ALSO add a terminal target
    `@variable(model, term in Parameter(parameter_value(entry.ic_param)))` (defaulting to the
    SAME numeric value as the device's own IC Parameter — a benign, always-overridden default)
    and `@constraint(model, v.soc[H] == term)` where `v` is the SAME device varlist entry the
    `ic_param` came from; replace that `ic_handles` entry's `terminal_param` with `term`
    (immutable NamedTuple — build a NEW entry, don't mutate in place). When `!terminal_soc`, skip
    this block entirely (no such constraint exists in the model at all — a genuinely different
    model, per the `<behavior>` block above).

    Set `p_import` on the objective via a PLACEHOLDER coefficient (e.g. `zeros(H)`) at build
    time: `@objective(model, Max, ctx.meta[:objective] - sum(0.0 * p_import[τ] for τ in 1:H))` —
    the caller (Wave 4) ALWAYS calls `set_objective_coefficient` before the first solve, so the
    placeholder value is never actually optimized against; state this explicitly in the
    docstring so a future reader does not mistake `0.0` for a modeling claim. NEVER wrap
    `p_import` itself in a `Parameter` here (Pitfall 2).

    Return `MpcWindow(model, ctx, H, aggregators[1].bus, feeder, p_import, ic_handles,
    agg_pdc_handles, terminal_soc)`.

    Define `solve_mpc_window!(o::MpcWindow; max_attempts::Int = 4, attempts_out::Union{Nothing,Ref{Int}}
    = nothing) = solve_with_retry!(o.model; max_attempts = max_attempts, dual = true,
    attempts_out = attempts_out)` — a one-line delegation, no new variable/constraint ever.

    End the file with `export MpcWindow, build_mpc_window, solve_mpc_window!`.

    In `src/TSODSO.jl`, insert `include("models/mpc_window.jl")` immediately after
    `include("models/mpc_trace.jl")` (plan 21-02's line) — both are `models/` files, no ordering
    dependency between them, grouped for diff locality — with a one-line comment naming plan
    21-03 and MPC-01/02.

    In `docs/src/api.md`, extend the `## MPC / Rolling-Horizon` section's `Pages` list (created
    by plan 21-02) to add `"models/mpc_window.jl"`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, JuMP, Test; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false)]; branches = [Branch(1,2,1e-3,1e-3,SMAX_NO_LIMIT)]; feeder = Feeder(buses, branches, 1); H = 3; Ppv = fill(0.05, H); Tout = fill(25.0, H); Pdc = fill(0.05, H); therm = Thermostatic(2, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, Tout); batt = PVBattery(2, 0.95, 1.0, 0.05, 0.0, 0.2, 0.1, 3.8, 6.2, 8.9, Ppv); agg = Aggregator(2, 0.9, AbstractDevice[therm, batt], Pdc); o = build_mpc_window(feeder, ConvexBranchFlow(), [agg]; H = H, terminal_soc = true); @test o isa TSODSO.MpcWindow; @test o.H == H; @test length(o.p_import) == H; soc_handle = only(h for h in o.ic_handles if h.kind == :soc); @test soc_handle.terminal_param !== nothing; tin_handle = only(h for h in o.ic_handles if h.kind == :Tin); @test tin_handle.terminal_param === nothing; nv0 = num_variables(o.model); nc0 = num_constraints(o.model; count_variable_in_set_constraints = true); for τ in 1:H; set_objective_coefficient(o.model, o.p_import[τ], -4.0); end; set_parameter_value(soc_handle.ic_param, 0.15); set_parameter_value(soc_handle.terminal_param, 0.05); solve_mpc_window!(o); @test num_variables(o.model) == nv0; @test num_constraints(o.model; count_variable_in_set_constraints = true) == nc0; println("OK")'</automated>
  </verify>
  <done>src/models/mpc_window.jl exists, exports MpcWindow/build_mpc_window/solve_mpc_window!, and the toggle/Parameter/no-rebuild properties hold on a tiny inline fixture.</done>
  <acceptance_criteria>
    - `grep -n 'struct MpcWindow' src/models/mpc_window.jl` matches
    - `grep -n 'in Parameter(0.0)\|Parameter(λ' src/models/mpc_window.jl` returns NO matches (λ₀/p_import never a Parameter — Pitfall 2)
    - `grep -n 'terminal_soc' src/models/mpc_window.jl` returns at least 3 matches (kwarg, conditional block, struct field)
    - `grep -n 'solve_welfare(' src/models/mpc_window.jl` returns NO matches (the window NEVER calls solve_welfare internally, per the objective's D-02 resolution)
    - The verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase21Fixtures — the phase's short-T CI substrate + high-PV forced-inexact fixture</name>
  <files>test/fixtures_phase21.jl</files>
  <read_first>
    - test/fixtures_phase6.jl (full file — the SELF-CONTAINED `@testmodule` convention this
      file follows: build feeder/aggregators INSIDE functions, take `feeder` as an argument,
      never call a not-yet-defined symbol at module load time)
    - test/fixtures_phase4.jl (lines 176-241 — `high_pv_feeder`/`build_high_pv_aggregators`, the
      shape this file's forced-inexact fixture mirrors at a SHORT horizon)
    - .planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-RESEARCH.md, "Wave 0 Gaps"
      (the explicit recommendation: a 2-3 bus radial feeder with Thermostatic + PVBattery at a
      SHORT T, e.g. 6-8, small H, e.g. 2-3 — no Deferrable, per Pitfall 8's reset-per-window
      honesty concern)
  </read_first>
  <action>
    Create `test/fixtures_phase21.jl` with a file-header comment in the `fixtures_phase6.jl`
    shape: SEAM = "Phase-21 CI fixture (MPC-01..04)", stating this module is SELF-CONTAINED (no
    cross-`@testmodule` load-time dependency, mirroring `fixtures_phase6.jl`'s own discipline
    rather than `fixtures_phase19.jl`'s cross-reference, since Phase 21 needs no Phase-6-specific
    reuse) and that it DELIBERATELY excludes `Deferrable` (RESEARCH Pitfall 8: its within-window
    energy budget resets every window under a rolling horizon, which is honest-but-confusing for
    a minimal CI fixture whose whole point is closed-loop state CONTINUITY).

    Define `@testmodule Phase21Fixtures`. Constants: `const T = 8` (short day-ahead CI horizon),
    `const H = 3` (fixed window length — `T - H + 1 = 6` published steps, Pitfall 5), `const
    BATT_λ_MIN = 3.8`, `BATT_λ_MED = 6.2`, `BATT_λ_MAX = 8.9` (reuse the project's standard App.
    C triple), `const SEED_MPC = 20260809`, `const LOAD_SCALE_MPC = 0.02`, `const PV_SCALE_MPC =
    0.01`, `const LAMBDA0_MPC = 4.0`.

    `temperature_profile(Tsteps::Int = T) -> Vector{Float64}`: the first `Tsteps` entries of the
    project's standard 24h ambient shape (reuse the exact 24-value literal from
    `fixtures_phase6.jl`'s own `temperature_profile()`, sliced/cycled to `Tsteps` via `mod1`).

    `mpc_lambda0(Tsteps::Int = T) -> Vector{Float64}`: `fill(LAMBDA0_MPC, Tsteps)` (flat, mirrors
    `Phase6Fixtures.two_bus_lambda0`'s flat-price anchor convention).

    `mpc_feeder() -> Feeder`: a 2-bus radial fixture built INSIDE the function (never at module
    top level): root bus 1 + load bus 2, ONE branch `Branch(1, 2, 1e-3, 1e-3, SMAX_NO_LIMIT)`
    (near-lossless, uncongested — mirrors `Phase6Fixtures.two_bus_feeder` exactly, so
    `ConvexBranchFlow` stays comfortably exact on the happy-path fixture).

    `build_mpc_aggregators(feeder; seed::Integer = SEED_MPC, Tsteps::Int = T) -> Vector{<:Aggregator}`:
    ONE aggregator at bus 2 holding `Thermostatic` + `PVBattery` ONLY (no `Deferrable` — Pitfall
    8), seeded via `generate_profiles(seed = seed + 2, T = Tsteps)`, scaled with
    `LOAD_SCALE_MPC`/`PV_SCALE_MPC`, mirroring `Phase6Fixtures.build_two_bus_aggregators`'s exact
    construction shape minus the `Deferrable` member.

    `mpc_high_pv_feeder() -> Feeder`: a 3-bus radial fixture (root + two downstream buses),
    mirroring `Phase4Fixtures.high_pv_feeder()` EXACTLY (branches `r=x=0.05`, `SMAX_NO_LIMIT`) —
    the substrate Wave 4's forced-inexact certificate-escalation test drives.

    `build_mpc_high_pv_aggregators(feeder; seed::Integer = SEED_MPC, pv_scale::Real,
    Tsteps::Int = T) -> Vector{<:Aggregator}`: mirrors `Phase4Fixtures.build_high_pv_aggregators`'s
    shape (Thermostatic + PVBattery only, small `load_scale`, tiny battery headroom,
    caller-supplied `pv_scale` with NO default — Wave 4's task must MEASURE the `pv_scale` that
    genuinely trips the inline cone-residual check at this fixture's short `Tsteps`/`H`, per this
    project's "measured, not guessed" discipline; document that measurement in Wave 4's own
    plan/summary, not here).

    Export every constant and function defined above.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, Test; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false)]; branches = [Branch(1,2,1e-3,1e-3,SMAX_NO_LIMIT)]; feeder = Feeder(buses, branches, 1); T = 8; prof = generate_profiles(seed = 20260809 + 2, T = T); Ppv = Float64[0.01 * p for p in prof.pv]; Pdc = Float64[0.02 * d for d in prof.demand]; Tout = Float64[19,18,17,16,16,17,19,21][1:T]; therm = Thermostatic(2, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, Tout); batt = PVBattery(2, 0.95, 1.0, 0.1*0.02, 0.0, 0.4*0.02, 0.2*0.02, 3.8, 6.2, 8.9, Ppv); agg = Aggregator(2, 0.9, AbstractDevice[therm, batt], Pdc); ctx, cost, dadp = solve_welfare(feeder, ConvexBranchFlow(), [agg]; T = T, λ₀ = fill(4.0, T), allow_export = true); @test isfinite(cost); @test length(dadp) == T; println("OK")'</automated> -- per-task quick check: plain Test.jl under --project=., proves the HAPPY-PATH fixture shape (mirrored inline here, not via Phase21Fixtures directly — TestItemRunner is test-only) is solvable end-to-end via the UNMODIFIED solve_welfare before the fixture module is trusted by test_mpc_window.jl.
  </verify>
  <done>test/fixtures_phase21.jl exists, exports the phase's short-T CI fixture and the (as-yet uncalibrated) high-PV fixture, and the happy-path shape solves cleanly through the existing solve_welfare.</done>
  <acceptance_criteria>
    - `grep -n '@testmodule Phase21Fixtures' test/fixtures_phase21.jl` matches
    - `grep -n 'Deferrable' test/fixtures_phase21.jl` returns NO matches (deliberate exclusion, Pitfall 8)
    - `grep -n 'function mpc_feeder\|function build_mpc_aggregators\|function mpc_high_pv_feeder\|function build_mpc_high_pv_aggregators' test/fixtures_phase21.jl` returns 4 matches
    - The verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: test_mpc_window.jl — build-once invariance + non-no-op Parameter re-solve correctness</name>
  <files>test/test_mpc_window.jl</files>
  <read_first>
    - src/models/mpc_window.jl (Task 1's output, full file)
    - test/fixtures_phase21.jl (Task 2's output, full file)
    - test/test_planning_oracle.jl (lines 59-124 — the EXACT build-once/re-solve `@testitem`
      shape this file mirrors, generalized from `z` to `soc0`/`Tin0`/terminal-target/forecast
      slices)
  </read_first>
  <action>
    Create `test/test_mpc_window.jl` with a file-header comment: SEAM =
    `src/models/mpc_window.jl` (MPC-01/MPC-02), every item name contains `"mpc_window"`,
    `tags = [:mpc_window]`, `setup = [Phase21Fixtures]`.

    Add `@testitem "mpc_window: build_mpc_window guards (empty aggregators, H<1, bus range)"` —
    mirrors `test_planning_oracle.jl`'s first `@testitem` shape against `build_mpc_window`'s
    three boundary guards.

    Add `@testitem "mpc_window: build-once — num_variables/num_constraints invariant across
    re-solves at DIFFERENT soc0/Tin0/terminal-target/forecast-slice states (MPC-01)"` — builds
    `feeder = Phase21Fixtures.mpc_feeder()`, `aggs = Phase21Fixtures.build_mpc_aggregators(feeder)`,
    `H = Phase21Fixtures.H`, `o = build_mpc_window(feeder, ConvexBranchFlow(), aggs; H = H,
    terminal_soc = true)`. Snapshot `nv0`/`nc0` (with `count_variable_in_set_constraints = true`).
    Run AT LEAST three re-solve cycles, each setting a DIFFERENT combination of: every
    `ic_handles` entry's `ic_param` (and `terminal_param` where present) to a new feasible value,
    every device's `Ppv_param`/`Tout_param`/`Pdc_param` (reached via `ctx.meta[:agg_device_vars]`/
    the aggregator handle) to a new forecast-perturbed slice, and `set_objective_coefficient` on
    every `o.p_import[τ]` to a new `λ₀` slice — then `solve_mpc_window!(o)`. After all cycles,
    assert `num_variables(o.model) == nv0` and
    `num_constraints(o.model; count_variable_in_set_constraints = true) == nc0`.

    Add `@testitem "mpc_window: set_parameter_value on soc0 is NOT a no-op — the solved soc[1]
    trajectory genuinely moves (MPC-01)"` — builds the SAME fixture, solves once at
    `soc0 = <value A>`, reads `value(soc_var[1])` (should equal `<value A>` — the IC constraint
    holds by construction), re-solves at a DIFFERENT `soc0 = <value B>` with the SAME
    `λ₀`/forecast slices, and asserts the NEW `value(soc_var[1]) == <value B>` and differs from
    the first solve's value — proving the Parameter genuinely drives the model, not a
    silently-ignored input.

    Add `@testitem "mpc_window: terminal_soc toggle produces a STRUCTURALLY different model
    (MPC-02 mechanism)"` — builds TWO windows on the SAME fixture, one with `terminal_soc = true`
    and one with `terminal_soc = false`; asserts
    `num_constraints(true_window.model; count_variable_in_set_constraints = true) >
    num_constraints(false_window.model; count_variable_in_set_constraints = true)` (the terminal
    equality is a GENUINE additional constraint, never a silent no-op toggle) and that every
    `ic_handles` entry of KIND `:soc` on the `false` window has `terminal_param === nothing` while
    the `true` window's does not.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO, JuMP, Test; buses = [Bus(1,0.95,1.05,true), Bus(2,0.95,1.05,false)]; branches = [Branch(1,2,1e-3,1e-3,SMAX_NO_LIMIT)]; feeder = Feeder(buses, branches, 1); H = 3; Ppv = fill(0.01, H); Tout = fill(20.0, H); Pdc = fill(0.02, H); therm = Thermostatic(2, 0.2, 0.05, 15.0, 30.0, 22.0, 0.0, 1.0, 0.5, Tout); batt = PVBattery(2, 0.95, 1.0, 0.002, 0.0, 0.008, 0.004, 3.8, 6.2, 8.9, Ppv); agg = Aggregator(2, 0.9, AbstractDevice[therm, batt], Pdc); o_true = build_mpc_window(feeder, ConvexBranchFlow(), [agg]; H = H, terminal_soc = true); o_false = build_mpc_window(feeder, ConvexBranchFlow(), [agg]; H = H, terminal_soc = false); nct = num_constraints(o_true.model; count_variable_in_set_constraints = true); ncf = num_constraints(o_false.model; count_variable_in_set_constraints = true); @test nct > ncf; ht = only(h for h in o_true.ic_handles if h.kind == :soc); hf = only(h for h in o_false.ic_handles if h.kind == :soc); @test ht.terminal_param !== nothing; @test hf.terminal_param === nothing; for τ in 1:H; set_objective_coefficient(o_false.model, o_false.p_import[τ], -4.0); end; set_parameter_value(hf.ic_param, 0.006); solve_mpc_window!(o_false); soc_var = only(vs for (b,vs) in o_false.ctx.meta[:agg_device_vars] for vv in vs if haskey(vv,:soc)); println("OK")'</automated> -- per-task quick check: plain Test.jl under --project=., inline tiny fixture; the persistent @testitem execution runs in this plan's closing &lt;verification&gt;.
  </verify>
  <done>test/test_mpc_window.jl exists with four green @testitems covering guards, build-once invariance, non-no-op Parameter re-solve, and the terminal-SOC structural toggle.</done>
  <acceptance_criteria>
    - `grep -c '@testitem' test/test_mpc_window.jl` returns 4
    - The build-once item's body calls `set_parameter_value`/`set_objective_coefficient` AT LEAST 3 times across distinct re-solve cycles before its final `num_variables`/`num_constraints` assertion
    - Running the verify command exits 0 and prints `OK`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Researcher-supplied `H`/`terminal_soc` kwargs and per-step Parameter values | The only "input" surface; a future in-process orchestrator caller, not attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Tampering | `build_mpc_window` accidentally rebuilds internally (e.g. re-calling `contribute!` inside `solve_mpc_window!`) | mitigate | Task 3's build-once `@testitem` asserts `num_variables`/`num_constraints` invariance across 3+ heterogeneous re-solve cycles, not just one |
| T-21-08 | Repudiation | `λ₀` accidentally becomes a `Parameter` somewhere, reproducing the ADMM Pitfall-1 bilinear failure silently (a `MOI.Bridges` error at solve time, not at build time) | mitigate | Task 1's `acceptance_criteria` greps for the absence of `Parameter(λ`/`in Parameter(0.0)` anywhere touching `p_import` |
| T-21-09 | Information Disclosure | N/A — no secrets, no network surface | accept | out of scope for a local optimization library |
</threat_model>

<verification>
Targeted regression (not the full suite): `julia --project=. -e 'import Pkg; Pkg.precompile()'`
confirms clean compilation after the new `src/models/mpc_window.jl` file and its `TSODSO.jl`
include. Re-run all three task-level `<verify>` inline scripts (idempotent) and confirm all print
`OK`. Defer `@testitem` execution to the phase-closing full-suite acceptance (21-06).
</verification>

<success_criteria>
- `build_mpc_window`/`solve_mpc_window!` exist and mirror `build_planning_oracle`'s build-once
  shape, generalized to the widened device Parameters.
- `num_variables`/`num_constraints` are provably unchanged across 3+ heterogeneous re-solve
  cycles on the phase's own CI fixture.
- The `terminal_soc` toggle produces a genuinely different model (more constraints when `true`).
</success_criteria>

<output>
Create `.planning/phases/21-mpc-rolling-horizon-real-time-pricing/21-03-SUMMARY.md` when done.
Record `MpcWindow`'s final field list and `build_mpc_window`/`solve_mpc_window!`'s exact
signatures verbatim — Wave 3/4's `mpc_loop.jl` calls these directly and must match exactly.
</output>
