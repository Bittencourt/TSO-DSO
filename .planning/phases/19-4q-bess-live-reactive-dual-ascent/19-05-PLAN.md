---
phase: 19-4q-bess-live-reactive-dual-ascent
plan: 05
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/models/complementarity_4q.jl
  - src/models/welfare_solve.jl
  - test/test_fourquadbess.jl
autonomous: true
requirements: [MESH-04]

must_haves:
  truths:
    - "assert_4q_complementarity! exists as a new, exported, throw-by-default certificate — a peer of assert_socp_exact!/assert_battery_complementarity! — with its OWN WR-01-idiom (atol+rtol·magnitude) tolerance, never a reused constant."
    - "The certificate's tolerance is calibrated against this device's own solver noise floor on a benign fixture (measurement-before-golden), not copied from PVBattery's Pmax²-scaled constant."
    - "assert_battery_complementarity! (the OLD check) never silently matches a FourQuadBESS's vars — the two checks are mutually exclusive over the same ctx.meta[:agg_device_vars] stash."
    - "A report kwarg neutralizes the throw (mirrors assert_socp_exact!'s rtol/atol diagnostic-neutralization idiom) without any src/ edit, and a deliberately-constructed negative-effective-price + grid-charging fixture demonstrates the honest boundary (D-08): the certificate legitimately throws there unless report=true."
  artifacts:
    - path: "src/models/complementarity_4q.jl"
      provides: "assert_4q_complementarity!(ctx; rtol, atol, report=false)"
      exports: ["assert_4q_complementarity!"]
      contains: "function assert_4q_complementarity!"
    - path: "test/test_fourquadbess.jl"
      provides: "certificate pass/throw/report-kwarg tests, including the negative-price honest-boundary fixture"
  key_links:
    - from: "src/models/complementarity_4q.jl"
      to: "ctx.meta[:agg_device_vars]"
      via: "haskey(v, :q) dispatch, distinct from assert_battery_complementarity!'s haskey(v,:p_ch)&&haskey(v,:p_dch)"
      pattern: "haskey\\(v, :q\\)"
    - from: "src/models/welfare_solve.jl"
      to: "src/models/complementarity_4q.jl"
      via: "assert_battery_complementarity!'s loop condition excludes 4Q devices: haskey(v,:p_ch)&&haskey(v,:p_dch)&&!haskey(v,:q)"
      pattern: "!haskey\\(v, :q\\)"
---

<objective>
Implement `assert_4q_complementarity!` (MESH-04 clause 2, D-05..D-08): a NEW, named, exported
post-solve certificate — peer of `assert_socp_exact!`/`assert_battery_complementarity!` — that
numerically verifies `p_ch[t]·p_dch[t] ≈ 0` for `FourQuadBESS` devices, throwing by default with
its OWN independently-derived WR-01-idiom tolerance (never `assert_battery_complementarity!`'s
`Pmax²`-scaled constant, per D-07 and Pitfall 2), and a `report` kwarg that neutralizes the throw
for diagnostic runs. Tighten the OLD `assert_battery_complementarity!`'s duck-type guard so it
never silently runs against a `FourQuadBESS`'s vars (Pitfall 2). Prove, on a deliberately
constructed negative-effective-price + grid-charging fixture, that the certificate legitimately
throws there — the honest boundary plan 19-02's derivation docstring predicts (D-08).

Purpose: this closes MESH-04's second clause — "re-derived for the 4Q case OR replaced by a
hard post-solve numeric check — never silently inherited." Both routes per D-05: the derivation
already lives in `FourQuadBESS.jl`'s docstring (plan 19-02); this plan is the numeric check that
derivation demands.

Output: `src/models/complementarity_4q.jl` implemented; `src/models/welfare_solve.jl`'s
`assert_battery_complementarity!` loop condition tightened to exclude 4Q devices; new
`test/test_fourquadbess.jl` items for benign-pass, violation-throw, report-kwarg-neutralize, and
the honest-boundary negative-price case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-CONTEXT.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-RESEARCH.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-PATTERNS.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-02-SUMMARY.md
@.planning/spikes/MANIFEST.md
</context>

<interfaces>
`assert_socp_exact!` (`src/models/exactness.jl`, full file already read by the planner) — the
throw-by-default WR-01 shape to mirror exactly:
```
function assert_socp_exact!(ctx::ModelContext; rtol::Real=1e-4, atol::Real=1e-6)
    ...
    tol = atol + rtol*max(abs(lhs), abs(rhs))
    maxratio = max(maxratio, gap/tol)
    maxratio <= 1 || error("... prices REFUSED ...")
    return maxgap
end
```
`assert_battery_complementarity!` (`src/models/welfare_solve.jl:301-321`, already read) — the
duck-typed stash-iteration idiom to mirror, and the EXACT loop condition to tighten:
```
function assert_battery_complementarity!(ctx::ModelContext; τ::Real, T::Int=ctx.meta[:T])
    haskey(ctx.meta, :agg_device_vars) || return nothing
    for (bus, varlist) in ctx.meta[:agg_device_vars]
        for v in varlist
            (haskey(v, :p_ch) && haskey(v, :p_dch)) || continue   # a battery
            pmax = has_upper_bound(v.p_ch[1]) ? upper_bound(v.p_ch[1]) : 1.0
            scale² = max(abs(pmax), 1e-8)^2
            for t in 1:T
                prod = value(v.p_ch[t]) * value(v.p_dch[t])
                prod < τ*scale² || error("Battery complementarity violated ...")
            end
        end
    end
    return nothing
end
```
`FourQuadBESS.contribute!`'s returned `vars` (plan 19-02): `(; p_ch, p_dch, soc, q)` — the `:q`
key is the distinguishing field the two checks dispatch on.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: assert_4q_complementarity! with a measured (not copied) tolerance</name>
  <files>src/models/complementarity_4q.jl</files>
  <read_first>
    src/models/exactness.jl (full file — the WR-01 throw-by-default shape and its docstring's
    rationale for the `atol`/`rtol` split, to write an analogous rationale for this new device)
    src/models/welfare_solve.jl:260-321 (the OLD check, and the `ctx.meta[:agg_device_vars]`
    stash shape it duck-types over)
    src/devices/FourQuadBESS.jl (plan 19-02 — the `vars` NamedTuple shape `(;p_ch,p_dch,soc,q)`
    and the derivation docstring this certificate implements against)
  </read_first>
  <behavior>
    - `assert_4q_complementarity!(ctx; rtol, atol)` iterates `ctx.meta[:agg_device_vars]`,
      selecting ONLY entries with `haskey(v,:p_ch) && haskey(v,:p_dch) && haskey(v,:q)` (the 4Q
      shape) — a `PVBattery`'s vars (`haskey(v,:q) == false`) are never touched by this
      function.
    - For each selected device/time, computes `gap = value(p_ch[t])*value(p_dch[t])` and a
      WR-01 combined tolerance scaled by the device's OWN rating (e.g.
      `max(Pch_max,Pdch_max)²`, recovered the same `has_upper_bound`/`upper_bound` way the OLD
      check does), throwing `error(...)` (never `@assert`) naming the bus/time/values/tolerance
      when `gap` exceeds it.
    - Returns the worst observed `gap`-to-tolerance ratio (or the max gap) on success, mirroring
      `assert_socp_exact!`'s "return a diagnostic on success" contract.
    - Is a no-op (returns `nothing` or `0.0`) when `ctx.meta[:agg_device_vars]` is absent or
      contains no 4Q device — mirrors `assert_battery_complementarity!`'s early-return shape.
  </behavior>
  <action>
    Fill in `src/models/complementarity_4q.jl` (currently a comment-only stub from plan 19-01):
    implement `assert_4q_complementarity!(ctx::ModelContext; rtol::Real, atol::Real, T::Int =
    ctx.meta[:T]) -> Float64`. Selection condition: `haskey(v,:p_ch) && haskey(v,:p_dch) &&
    haskey(v,:q)`. Tolerance: `tol = atol + rtol*scale²` where `scale = max(Pch_max, Pdch_max)`
    recovered from `has_upper_bound(v.p_ch[1]) ? upper_bound(v.p_ch[1]) : 1.0` and
    `has_upper_bound(v.p_dch[1]) ? upper_bound(v.p_dch[1]) : 1.0` respectively (device-scaled,
    WR-01 idiom, mirroring `assert_socp_exact!`'s `atol + rtol·max(...)` combined-bound shape
    rather than `assert_battery_complementarity!`'s single-`Pmax` shape, since `Pch_max` and
    `Pdch_max` can differ under D-02). On violation: `error("4Q-BESS complementarity violated at
    bus $bus, t=$t: p_ch·p_dch=$prod exceeds atol+rtol·scale²=$tol (rtol=$rtol, atol=$atol,
    scale=$scale) — MESH-04 clause 2; if this fixture has a negative effective nodal price and
    grid-charging enabled, this MAY be the honest boundary D-08 documents rather than a bug —
    see FourQuadBESS.jl's complementarity derivation docstring")`. Add a `report::Bool = false`
    kwarg: when `true`, replace the `error(...)` call with `@warn` carrying the SAME message and
    return the worst ratio anyway (mirrors D-06's "kwarg to report" neutralization, and
    `assert_socp_exact!`'s existing precedent of a caller-supplied `rtol`/`atol` override — this
    function additionally offers the EXPLICIT `report` boolean since D-06 names it specifically,
    rather than relying on the caller loosening `rtol`/`atol` to the point of vacuity). Export
    `assert_4q_complementarity!`. Do NOT pick `rtol`/`atol` defaults yet in this action step —
    Task 1's SECOND half (below) measures them.

    MEASURE (measurement-before-golden, D-07): using the plan's existing `FourQuadBESS` test
    fixture pattern (a standalone `ModelContext(Model(select_optimizer(SOCP())))`, one
    `FourQuadBESS`, a BENIGN linear price coefficient applied via
    `@objective(m, Max, res.utility - λ_test*sum(res.p_inject))` with `λ_test` chosen so the
    App. C dominance clearly holds — e.g. a positive, in-band price, no grid-charging
    incentive), solve it several times (varying `λ_test` across a small representative sweep,
    e.g. 3-5 values spanning the device's own `λ_min..λ_max` band) and record the empirically
    observed `p_ch[t]·p_dch[t]` noise floor at the Clarabel-solved optimum. Pin `rtol`/`atol`
    defaults in the function signature to comfortably clear that measured floor with margin
    (mirror `assert_socp_exact!`'s "an order below the legacy value" sizing discipline) — record
    the measured floor and the chosen defaults' rationale in the function's docstring (a
    citable, not asserted-from-thin-air, tolerance — mirrors `assert_socp_exact!`'s own
    docstring rationale sections).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("fourquadbess", ti.name) && occursin("complementarity", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `assert_4q_complementarity!` is exported and callable with only `ctx` (defaults for
      `rtol`/`atol` present and documented with a measured rationale, not a copied constant).
    - On a benign, strictly-dominated (positive in-band price) `FourQuadBESS` solve, the
      function returns without throwing.
    - `grep -n "haskey(v, :q)" src/models/complementarity_4q.jl` finds the selection condition.
    - The function's docstring cites the specific measured noise-floor value(s) that motivated
      the chosen default `rtol`/`atol`.
  </acceptance_criteria>
  <done>
    assert_4q_complementarity! implemented, exported, with a measured (documented) default
    tolerance distinct from assert_battery_complementarity!'s.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Disambiguate the OLD check + honest-boundary and report-kwarg tests</name>
  <files>src/models/welfare_solve.jl, test/test_fourquadbess.jl</files>
  <read_first>
    src/models/welfare_solve.jl:301-321 (`assert_battery_complementarity!`'s exact loop
    condition line to tighten)
    src/models/complementarity_4q.jl (post-Task-1 state — the NEW certificate's selection
    condition, to confirm mutual exclusivity)
  </read_first>
  <behavior>
    - `assert_battery_complementarity!` never iterates a `FourQuadBESS`'s vars (its loop
      condition explicitly excludes anything with a `:q` key).
    - A deliberately-constructed fixture (asymmetric grid-charging caps enabled, a forced
      NEGATIVE effective price via a negative `λ_test` in the same standalone-solve harness
      Task 1 used) produces a genuine `p_ch·p_dch` co-activation that EXCEEDS
      `assert_4q_complementarity!`'s tolerance, and the function throws by default.
    - The SAME violating fixture, re-run with `report = true`, does NOT throw (returns/warns
      instead) — proving D-06's neutralization kwarg works without any `src/` edit.
  </behavior>
  <action>
    In `src/models/welfare_solve.jl`, change `assert_battery_complementarity!`'s loop condition
    from `(haskey(v, :p_ch) && haskey(v, :p_dch)) || continue` to `(haskey(v, :p_ch) &&
    haskey(v, :p_dch) && !haskey(v, :q)) || continue` — the MINIMAL one-line change per Pitfall
    2, so a `FourQuadBESS`'s vars (which DO carry `:p_ch`/`:p_dch` plus the distinguishing `:q`)
    are now explicitly skipped by the OLD check. Update the docstring's one-line note that this
    check is battery-active-power-only and explicitly excludes any device also carrying a
    reactive decision (naming `FourQuadBESS`/MESH-04 and pointing at
    `assert_4q_complementarity!` as the peer check for that device type).

    In `test/test_fourquadbess.jl`, add: (a) an item proving `assert_battery_complementarity!`
    silently skips a `FourQuadBESS`-only aggregator's stash (construct the stash manually or via
    a small solve, call the OLD function directly, assert it returns `nothing` without throwing
    REGARDLESS of the `FourQuadBESS`'s p_ch/p_dch values — since it should never even LOOK at
    them); (b) the honest-boundary item per `<behavior>` above — construct the negative-price +
    grid-charging violating fixture, `@test_throws ErrorException
    assert_4q_complementarity!(ctx; rtol=..., atol=...)` (using Task 1's measured defaults), and
    document in a comment WHY this is expected (cites D-08 and the derivation docstring's
    step 3); (c) the `report = true` neutralization item on the SAME violating fixture, asserting
    no exception is raised and the returned/logged diagnostic still names the violation.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("fourquadbess", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "!haskey(v, :q)" src/models/welfare_solve.jl` finds the tightened condition.
    - The pre-existing battery-complementarity tests in `test_welfare_solve.jl`/
      `test_pvbattery.jl` (PVBattery-only fixtures) still pass unmodified — the tightened
      condition changes nothing for a device without `:q`.
    - The honest-boundary item throws `ErrorException` (not silently passing) on the
      constructed negative-price + grid-charging fixture.
    - The `report = true` item does NOT throw on the SAME fixture.
    - Full suite (`julia --project=. -e 'import Pkg; Pkg.test()'`) matches plan 19-01's recorded
      baseline plus only the new, expected-green Phase-19 items — zero pre-existing item flips.
  </acceptance_criteria>
  <done>
    The two complementarity checks are mutually exclusive over the same stash; the D-08 honest
    boundary is demonstrated and its report kwarg proven to work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Post-solve numeric certificate | The only thing standing between a degenerate simultaneous charge/discharge and a published, cited research result |
| Tolerance provenance | Must be traceable to a measurement, never an assumed-safe copy of a differently-derived constant (certificate-laundering, the v3.0 standing bar) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Repudiation | Certificate-laundering (reusing `assert_battery_complementarity!`'s tolerance) | mitigate | Task 1 measures this device's OWN noise floor before pinning a default; Task 2 proves the two checks are structurally mutually exclusive, never both silently matching the same device |
| T-19-11 | Repudiation | A silently-unwired new certificate (Pitfall 2 — running the OLD check by accident, never the NEW one) | mitigate | Task 2's item (a) explicitly proves the OLD check skips a 4Q device; plan 19-06/19-07 wire the NEW check's call site into `solve_agr!`/`solve_admm`'s final consolidation block, verified by a dedicated "the certificate actually executes" test there |
| T-19-12 | Repudiation | Silent scope-narrowing of D-08's honest finding | accept — by design, not a gap | The certificate is EXPECTED to throw in the documented negative-price + grid-charging regime; Task 2 proves this is the correct, intended behavior (a citable finding), not muted via a loosened default tolerance |
</threat_model>

<verification>
`julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("fourquadbess", ti.name))'` — all green, including the honest-boundary throw and its report-kwarg neutralization.
</verification>

<success_criteria>
`assert_4q_complementarity!` exists with a measured, independent tolerance; the OLD check never
silently matches a 4Q device; the D-08 honest boundary is demonstrated and reportable.
</success_criteria>

<output>
Create `.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-05-SUMMARY.md` when done
</output>
