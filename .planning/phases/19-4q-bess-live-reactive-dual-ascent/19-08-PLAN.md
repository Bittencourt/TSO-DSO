---
phase: 19-4q-bess-live-reactive-dual-ascent
plan: 08
type: execute
wave: 6
depends_on: ["19-07"]
files_modified:
  - test/fixtures_phase19.jl
  - test/test_admm_reactive.jl
  - test/test_ieee123_admm.jl
autonomous: true
requirements: [MESH-04, MESH-05]

must_haves:
  truths:
    - "A FourQuadBESS-bearing aggregator variant of the 2-bus fixture exists and is the primary, CI-gated evidence for MESH-05's live convergence (D-13) — never IEEE-13."
    - "D-03/D-14: Under :live, solve_admm's welfare/λ/μ agree with the centralized solve_welfare within tolerances that were MEASURED on this fixture before being pinned (measurement-before-golden) — never a copied/assumed tolerance, and q trajectories are explicitly NOT part of this acceptance gate (D-03's μ≈0 degeneracy note)."
    - "A liveness regression proves the mechanism is genuinely live: two :live runs differing ONLY in the reactive coupling target converge to DIFFERENT μ/q trajectories (never a no-op that merely doesn't crash)."
    - "IEEE-13 4Q-BESS supporting evidence runs under the existing bounded-retry quarantine, explicitly NOT gating CI (D-13)."
    - "The full test suite's pass/fail/broken counts, re-measured against plan 19-01's recorded pre-Phase-19 baseline, show ONLY new, expected-green Phase-19 items added — zero pre-existing regression (ROADMAP success criterion 4, final gate)."
  artifacts:
    - path: "test/fixtures_phase19.jl"
      provides: "Phase19Fixtures @testmodule: a FourQuadBESS-bearing aggregator on Phase6Fixtures.two_bus_feeder()"
      contains: "@testmodule Phase19Fixtures"
  key_links:
    - from: "test/test_admm_reactive.jl"
      to: "src/admm/solve_admm.jl"
      via: "solve_admm(...; reactive_consensus=:live) cross-validated against solve_welfare(...)'s welfare/λ/μ"
      pattern: "reactive_consensus\\s*=\\s*:live"
    - from: "test/test_ieee123_admm.jl"
      to: "test/fixtures_retry.jl"
      via: "AdmmRetryFixtures.retry_flaky_admm_solve wrapping the IEEE-13 4Q-BESS supporting-evidence call"
      pattern: "retry_flaky_admm_solve"
---

<objective>
Close out Phase 19 (MESH-04 + MESH-05): build the `Phase19Fixtures` test module (a
`FourQuadBESS`-bearing aggregator on the SAME 2-bus fixture the project's existing ADMM
cross-validation tests already use — `Phase6Fixtures.two_bus_feeder()`), measure the solver
noise floor on it BEFORE pinning any cross-validation tolerance (measurement-before-golden,
D-14), prove `:live` mode's welfare/λ/μ agreement against the centralized `solve_welfare`
within those measured tolerances, prove the mechanism is genuinely live (not a no-op), add the
IEEE-13 quarantined supporting-evidence item (D-13), and run the final full-suite byte-identity
diff against plan 19-01's recorded baseline (ROADMAP success criterion 4).

Purpose: this is the phase's acceptance gate. Every prior plan built one piece; this plan is the
only place all the pieces are exercised together against the goal-backward truths.

Output: `test/fixtures_phase19.jl` (new `Phase19Fixtures` module); `test/test_admm_reactive.jl`
extended with the `:live` convergence/cross-validation/liveness items; `test/test_ieee123_admm.jl`
extended with a quarantined 4Q-BESS supporting-evidence item; a final full-suite pass/fail/broken
count reconciled against plan 19-01's baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-CONTEXT.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-RESEARCH.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-VALIDATION.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-01-SUMMARY.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-07-SUMMARY.md
@.planning/spikes/MANIFEST.md
</context>

<interfaces>
`test/fixtures_phase6.jl` (`Phase6Fixtures`, full file already read by the planner) — the fixture
this plan's `Phase19Fixtures` module extends WITHOUT modifying: `T`, `two_bus_feeder()`,
`two_bus_lambda0()`, `RHO_2BUS`, `build_two_bus_aggregators(feeder; seed)` (returns a
`Thermostatic + Deferrable + PVBattery` aggregator at bus 2). `Phase19Fixtures` adds a SIBLING
builder returning an aggregator with the SAME devices PLUS a `FourQuadBESS`, on the SAME feeder.

`test/fixtures_retry.jl` (`AdmmRetryFixtures.retry_flaky_admm_solve`, full file already read) —
wraps a zero-arg closure, retries ONLY on the documented Clarabel `NUMERICAL_ERROR`-class flake.

`test/test_admm_reactive.jl` (full file already read) — the 3 existing `@testitem`s this plan's
new items sit ALONGSIDE (same file, per RESEARCH.md's explicit "do NOT create a separate file"
recommendation, so one test run demonstrates all 3 modes coexist).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Phase19Fixtures + measurement-before-golden tolerance calibration</name>
  <files>test/fixtures_phase19.jl</files>
  <read_first>
    test/fixtures_phase6.jl (full file — the EXACT builder shape to mirror)
    .planning/spikes/MANIFEST.md (the WR-01 scale-free tolerance-measurement discipline binding
    on this task's calibration)
    src/devices/FourQuadBESS.jl (plan 19-02 constructor signature)
  </read_first>
  <action>
    Create `test/fixtures_phase19.jl` as a `@testmodule Phase19Fixtures` (TestItemRunner
    auto-discovers it project-wide, no `include` needed — mirrors every existing
    `@testmodule`). Add a function `build_two_bus_aggregators_4q(feeder; seed =
    Phase6Fixtures.SEED_2BUS)` mirroring `Phase6Fixtures.build_two_bus_aggregators` EXACTLY
    (same `Thermostatic`/`Deferrable`/`PVBattery` triple, same seeded profile draw) PLUS one
    `FourQuadBESS` member at the same bus, with concrete, small, interior-feasible parameters
    consistent with the fixture's near-lossless/uncongested scale (document the chosen
    `Pch_max`/`Pdch_max`/`Smax`/η/λ triple values and WHY they keep the 2-bus solve interior —
    mirror the existing fixture's `LOAD_SCALE_2BUS`/`PV_SCALE_2BUS` sizing discipline). This
    module depends on `Phase6Fixtures` (`using`/reference it directly — TestItemRunner
    `@testmodule`s may reference each other's exported symbols when both are listed in a
    `setup = [...]` array by consuming test items) — do NOT redefine `two_bus_feeder`/`T`/etc.

    MEASURE (measurement-before-golden, D-14): using this fixture, run
    `solve_welfare(feeder, ConvexBranchFlow(), aggs_4q; allow_export=true)` (centralized) and
    `solve_admm(feeder, ConvexBranchFlow(), aggs_4q; T, λ₀, ρ=RHO_2BUS, allow_export=true,
    reactive_consensus=:live)` (ADMM) SEVERAL times (varying the RNG seed passed to
    `build_two_bus_aggregators_4q` across 3-5 seeds, per the project's own repeated-run
    stability discipline, e.g. `.planning/spikes/003-phase18-fragility-tolerance/`) and record
    the empirically observed `|welfare_admm - welfare_centralized|`, `|λ_admm - λ_centralized|`,
    and `|μ_admm - μ_centralized|` gaps across those runs. Document the measured gaps and the
    resulting chosen tolerances (own, independently-derived per gap — NOT one shared constant)
    in this task's own commentary/docstring inside the fixture file, for Task 2 to consume.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("fourquadbess", ti.name) || occursin("reactive", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `Phase19Fixtures.build_two_bus_aggregators_4q(feeder)` returns a `Vector{<:Aggregator}` of
      length 1, with the aggregator's `devices` containing a `FourQuadBESS` alongside the
      existing 3 device types.
    - The measured welfare/λ/μ gaps (from the multi-seed sweep) are recorded and cited in the
      fixture file's own docstring/comments — not invented.
    - The chosen tolerances for Task 2 are each independently justified by a measured number,
      not copied from an existing test's tolerance.
  </acceptance_criteria>
  <done>
    Phase19Fixtures exists with a FourQuadBESS-bearing 2-bus aggregator variant; welfare/λ/μ
    cross-validation tolerances are measured, documented, and ready for Task 2 to pin.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: :live convergence + cross-validation + liveness regression</name>
  <files>test/test_admm_reactive.jl</files>
  <read_first>
    test/test_admm_reactive.jl (full file — the 3 existing items this task's new items sit
    beside, same file, same `setup=[Phase6Fixtures]` convention extended with `Phase19Fixtures`)
    test/fixtures_phase19.jl (post-Task-1 state — the measured tolerances)
  </read_first>
  <behavior>
    - `solve_admm(...; reactive_consensus = :live)` on the `Phase19Fixtures`-built aggregator
      CONVERGES (does not throw the maxiter `ErrorException`) within the documented iteration
      bound from plan 19-07.
    - The converged `welfare`/`λ`/`μ` agree with `solve_welfare`'s centralized welfare/`λ`/reactive
      price within Task 1's measured tolerances (q trajectories are explicitly NOT compared,
      per D-03's degeneracy note — assert this omission is deliberate via a comment, not an
      oversight).
    - Two `:live` runs on the SAME fixture, differing ONLY in the reactive coupling target
      (e.g. a different seed changing the device's `q_inject` magnitude, or an explicitly
      perturbed `Smax`), converge to DIFFERENT `μ`/`q_devices` trajectories — proving the
      mechanism reacts to its input rather than returning a static/degenerate result.
  </behavior>
  <action>
    Add 3 new `@testitem`s to `test/test_admm_reactive.jl` (tag `[:admm, :reactive]`, `setup =
    [Phase6Fixtures, Phase19Fixtures]`, names containing "live" so they are independently
    filterable): (1) convergence — call `solve_admm(...; reactive_consensus = :live)` on the
    `Phase19Fixtures` aggregator and assert it returns (does not throw), recording `iters`;
    (2) cross-validation — additionally call the centralized `solve_welfare` on the SAME
    aggregator/feeder and assert `isapprox(welfare_admm, welfare_centralized; ...)`,
    `isapprox(λ_admm, extract_dlmp(centralized_ctx)[load_bus,:]; ...)`, and
    `isapprox(μ_admm, extract_reactive_dlmp(centralized_ctx)[load_bus,:]; ...)`, each using
    Task 1's independently-measured tolerance (never one shared `atol`/`rtol` for all three);
    (3) liveness — build TWO `Phase19Fixtures` aggregators differing only in the seed/Smax
    perturbation described in `<behavior>`, run `:live` on both, and assert the two resulting
    `μ`/`q_devices` outputs are NOT approximately equal (a `!isapprox` or an explicit
    difference-exceeds-a-floor assertion) — the CR-01 "tests passing ≠ mechanism live" guard.
    Also re-run the 3 PRE-EXISTING items in this file unmodified and confirm they still pass
    (do not touch their code).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("reactive", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - All 3 new `:live` items pass; all 3 pre-existing items pass unmodified.
    - The cross-validation item's 3 `isapprox` calls each cite a DIFFERENT, independently
      measured tolerance (grep the test file for 3 distinct tolerance literals/variables, not
      one repeated constant).
    - The liveness item's assertion genuinely fails if the two runs were accidentally identical
      (sanity-check this by temporarily making the two fixtures identical and confirming the
      test fails, then reverting — document this sanity-check in the task's own verification
      notes, do NOT leave the reverted state uncommitted).
  </acceptance_criteria>
  <done>
    :live mode's convergence, cross-validation (welfare+λ+μ, each own tolerance), and liveness
    are all proven on the primary CI-gated small-radial fixture.
  </done>
</task>

<task type="auto">
  <name>Task 3: IEEE-13 quarantined supporting evidence + final byte-identity gate</name>
  <files>test/test_ieee123_admm.jl</files>
  <read_first>
    test/test_ieee123_admm.jl (existing IEEE-13/123 ADMM items and their current
    `setup=[..., AdmmRetryFixtures]` usage — mirror the exact wrapping pattern)
    test/fixtures_retry.jl (`retry_flaky_admm_solve`'s exact call contract)
    .planning/phases/19-4q-bess-live-reactive-dual-ascent/19-01-SUMMARY.md (the recorded
    pre-Phase-19 baseline pass/fail/broken counts this task's final gate diffs against)
  </read_first>
  <action>
    Add ONE new `@testitem` to `test/test_ieee123_admm.jl` (tag `[:admm, :reactive]`, name
    containing "ieee13" and "4q" so it is independently filterable and explicitly NOT matched
    by the CI-gating `occursin("reactive", ti.name)` filter alone if that filter is used
    elsewhere to select ONLY the fast small-fixture items — verify this file's existing tagging
    convention and follow it) that builds an IEEE-13 aggregator set with ONE `FourQuadBESS`
    added (mirroring how the existing IEEE-13 aggregators are already built in this file,
    modified additively) and calls `solve_admm(...; reactive_consensus = :live)` WRAPPED in
    `AdmmRetryFixtures.retry_flaky_admm_solve` (mirroring this file's existing flaky-item
    wrapping exactly) — supporting evidence only, D-13, explicitly NOT part of the primary
    CI-gated evidence set (document this in a comment above the item, citing D-13 and quick
    task 260726-vn2).

    Then run the FULL suite: `julia --project=. -e 'import Pkg; Pkg.test()'`. Reconcile its
    pass/fail/broken counts against plan 19-01's recorded baseline: the ONLY delta must be new,
    expected-green Phase-19 items (device tests, certificate tests, aggregator/DsoOpt/AgrOpt/
    solve_admm reactive-mode tests, the new fixtures, this IEEE-13 item) — zero pre-existing
    item may have flipped from pass to fail/broken, and the ONE pre-existing accepted failure
    (Aqua CairoMakie stale-deps) must still be the ONLY pre-existing non-green item. Record the
    final counts and the explicit before/after diff in this task's SUMMARY — this is the
    concrete, checkable evidence for ROADMAP success criterion 4.
  </action>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - The new IEEE-13 4Q-BESS item is wrapped in `retry_flaky_admm_solve` and is NOT part of any
      filter expression documented as "CI-gating" in `19-VALIDATION.md`/this file's own header
      comments.
    - Full-suite pass count = plan 19-01's baseline pass count + (number of new Phase-19 test
      items across all 8 plans); fail/broken counts unchanged except for the ONE pre-existing
      accepted Aqua failure remaining exactly as it was.
    - Every requirement ID (MESH-04, MESH-05) has at least one passing test item directly
      exercising it, citable by test-item name.
  </acceptance_criteria>
  <done>
    IEEE-13 4Q-BESS supporting evidence runs under the existing quarantine, not CI-gating; the
    full suite's final byte-identity diff against the pre-Phase-19 baseline shows zero
    regression — ROADMAP success criterion 4 is met and evidenced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase acceptance gate | The last point at which a silent regression, a laundered tolerance, or a non-live "live" mechanism could slip through undetected before the phase is declared shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-18 | Repudiation | Cross-validation tolerance provenance | mitigate | Task 1 measures welfare/λ/μ gaps across a multi-seed sweep BEFORE Task 2 pins any tolerance; each of the 3 gets its OWN measured value, never one shared constant |
| T-19-19 | Repudiation | "mechanism is live" claim | mitigate | Task 2's liveness item is REQUIRED to fail under a deliberately-reverted sanity check before being accepted as passing for the right reason |
| T-19-20 | Repudiation | Byte-identity of the default path (success criterion 4) | mitigate | Task 3's final full-suite diff against plan 19-01's EXPLICITLY recorded pre-Phase-19 baseline is the concrete evidence, not an assumption |
</threat_model>

<verification>
`julia --project=. -e 'import Pkg; Pkg.test()'` — final counts reconciled against plan 19-01's
baseline: only new, expected-green Phase-19 items added, zero pre-existing regression.
</verification>

<success_criteria>
MESH-04 and MESH-05 are both fully evidenced on the primary small-radial fixture (D-13); IEEE-13
support runs quarantined; the phase's byte-identity default-path guarantee (ROADMAP success
criterion 4) is diffed and proven, not merely asserted.
</success_criteria>

<output>
Create `.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-08-SUMMARY.md` when done
</output>
