---
phase: 19-4q-bess-live-reactive-dual-ascent
plan: 06
type: execute
wave: 4
depends_on: ["19-03", "19-04", "19-05"]
files_modified:
  - src/admm/AgrOpt.jl
autonomous: true
requirements: [MESH-05]

must_haves:
  truths:
    - "build_agr_opt accepts the same 3-state reactive mode as build_dso_opt; under OFF/CERTIFIED, AgrOpt is byte-identical to pre-Phase-19 (qag stays the constant -Pdc*tanφ vector, no live coupling variable)."
    - "Under LIVE, AgrOpt gains a genuine qag_live coupling variable pinned to the aggregator's total q_inject (device sum + inelastic constant), with its own ρ_q-scaled quadratic penalty, mirroring pag's existing pinning/penalty shape exactly."
    - "solve_agr! can update qag_live's linear objective coefficient from a reactive price μ_j and a netflow-style target d_j, mirroring pag's -λ_j-ρ*c_j update exactly, and can invoke the NEW 4Q complementarity certificate (assert_4q_complementarity!) via a check_4q kwarg alongside the existing check_battery kwarg."
  artifacts:
    - path: "src/admm/AgrOpt.jl"
      provides: "build_agr_opt(...; reactive_mode=OFF, ρ_q); AgrOpt.qag_live field; solve_agr!(...; μ_j, d_j, ρ_q, check_4q); set_rho_q!"
      contains: "qag_live"
  key_links:
    - from: "src/admm/AgrOpt.jl"
      to: "src/devices/Aggregator.jl"
      via: "qag_live[t] == res.q_inject[t] + agg's inelastic reactive term, mirroring pag[t] == res.p_inject[t] - agg.Pdc[t]"
      pattern: "qag_live\\[t\\]"
    - from: "src/admm/AgrOpt.jl"
      to: "src/models/complementarity_4q.jl"
      via: "check_4q && assert_4q_complementarity!(agr.ctx; ...)"
      pattern: "assert_4q_complementarity!"
---

<objective>
Give `AgrOpt` (the per-node ADMM subproblem) the live reactive-consensus scaffolding: under the
3-state `reactive_consensus` mode (plan 19-01/19-03), promote the currently-constant `qag`
placeholder field into a genuine coupling variable `qag_live` when `LIVE`, pinned to the
aggregator's TOTAL reactive injection (the widened `q_inject` from plan 19-04), with its own
`ρ_q`-scaled quadratic penalty mirroring `pag`'s existing shape exactly. Extend `solve_agr!` to
update `qag_live`'s linear coefficient from a reactive price `μ_j` and target `d_j` (the reactive
analogs of the existing `λ_j`/`c_j`), and wire plan 19-05's `assert_4q_complementarity!` in
behind a new `check_4q` kwarg, mirroring the existing `check_battery` convention exactly.

Purpose: this is the AGR-OPT (block-1) half of MESH-05's live μ-dual-ascent — the counterpart to
plan 19-03's DSO-OPT (block-2) unpinning. Plan 19-07's outer loop in `solve_admm.jl` is the ONLY
remaining piece that actually drives `μ`/`λ` between these two already-built subproblems.

Output: `build_agr_opt` accepting `reactive_mode`; `AgrOpt.qag_live` field (mirrors `.pag`);
`solve_agr!` accepting `μ_j`/`d_j`/`ρ_q`/`check_4q`; a new `set_rho_q!(agr::AgrOpt, ...)` peer of
`DsoOpt`'s; existing AGR-OPT tests unaffected under OFF/CERTIFIED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-CONTEXT.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-RESEARCH.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-PATTERNS.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-03-SUMMARY.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-04-SUMMARY.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-05-SUMMARY.md
</context>

<interfaces>
Current `src/admm/AgrOpt.jl` (full file already read by the planner) — `AgrOpt` struct fields:
`model, ctx, pag::Vector{VariableRef}, qag::Vector{Float64}, T, bus, ρ`. `build_agr_opt`
(lines 92-118):
```
res = contribute!(agg, ctx; T = T)          # now ALSO returns res.q_inject (plan 19-04)
@variable(model, pag[1:T])
@constraint(model, coupling[t=1:T], pag[t] == res.p_inject[t] - agg.Pdc[t])
register_constraint!(ctx, :agr_coupling, coupling)
tanφ = reactive_factor(agg.φ)
qag = Float64[-agg.Pdc[t]*tanφ for t in 1:T]     # CONSTANT today — becomes qag_live under LIVE
@objective(model, Max, ctx.meta[:objective] - 0.5*ρ*sum(pag[t]^2 for t in 1:T))
return AgrOpt(model, ctx, collect(pag), qag, T, agg.bus, Float64(ρ))
```
`solve_agr!` (lines 166-204) — the linear-coefficient update to mirror for `qag_live`:
```
for t in 1:agr.T
    set_objective_coefficient(agr.model, agr.pag[t], -λ_j[t] - ρ*c_j[t])
end
...
if check_battery
    assert_battery_complementarity!(agr.ctx; τ=τ_batt, T=agr.T)
end
```
`set_rho!` (lines 230-235) — the batch shape `set_rho_q!` mirrors, on `agr.pag`/`-0.5ρ`.

From plan 19-04, `Aggregator.contribute!` now returns `(; vars, p_inject, q_inject, utility)`
where `q_inject[t] = Σ_d device q_inject[t]` (zero if no 4Q device). NOTE: `res.q_inject` is
ONLY the device-summed part — the aggregator's OWN inelastic term `-agg.Pdc[t]*tanφ` is added
SEPARATELY inside `Aggregator.contribute!`'s `:Rq` write, but is NOT part of `res.q_inject`
(re-verify this against plan 19-04's actual `Aggregator.jl` diff before writing this plan's
code — if `res.q_inject` turns out to include the inelastic term already, `qag_live`'s pinning
target simplifies to `res.q_inject[t]` alone; if not, it must be
`res.q_inject[t] - agg.Pdc[t]*tanφ`, mirroring how `pag` pins to `res.p_inject[t] - agg.Pdc[t]`
i.e. device terms plus the aggregator's own inelastic contribution, sign per eq. 3.23).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: qag_live coupling variable + ρ_q objective term under LIVE</name>
  <files>src/admm/AgrOpt.jl</files>
  <read_first>
    src/admm/AgrOpt.jl (full file, especially `build_agr_opt` 69-118, `AgrOpt` struct 59-67)
    src/devices/Aggregator.jl (post-plan-19-04 state — confirm the EXACT composition of
    `res.q_inject` vs. the aggregator's own inelastic `-Pdc*tanφ` term, per the Interfaces note
    above, before writing the pinning constraint)
    src/admm/ReactiveMode.jl (plan 19-01 — `normalize_reactive_mode`)
    src/admm/DsoOpt.jl (post-plan-19-03 state — the exact `LIVE`-branch shape this plan mirrors
    at AGR-OPT scale: same declare-but-do-not-pin discipline is NOT applicable here since AGR-OPT
    genuinely PINS `qag_live` to the aggregator's own reactive total, exactly as `pag` pins to
    the aggregator's own active total — re-read `build_dso_opt`'s LIVE branch only to confirm
    which side owns the pin vs. the ascent, per the file header's block-1/block-2 division)
  </read_first>
  <behavior>
    - Under `OFF`/`CERTIFIED`, `build_agr_opt` is byte-identical to today: `agr.qag` is the
      SAME constant `Float64` vector, no `qag_live` variable exists in the model, the objective
      is unchanged.
    - Under `LIVE`, a genuine `qag_live[1:T]` JuMP variable is declared and PINNED (via an
      equality constraint, mirroring `pag`'s `coupling` constraint) to the aggregator's total
      reactive injection; the model's objective gains a `-0.5*ρ_q*Σ qag_live[t]^2` term
      (Max-objective sign, mirroring `pag`'s existing `-0.5*ρ*Σ pag[t]^2`).
    - `num_variables`/`num_constraints` differ ONLY between LIVE and non-LIVE builds — this is a
      static, build-time structural difference (never re-checked mid-loop; build-once still
      applies).
  </behavior>
  <action>
    Add a new kwarg `reactive_mode = false` to `build_agr_opt` (accepting Bool/Symbol/enum via
    `normalize_reactive_mode`, normalized as the FIRST line of the function body) and `ρ_q::Real
    = ρ` (mirrors plan 19-03's `DsoOpt` default). Add a new field `qag_live` to the `AgrOpt`
    struct (position immediately after `qag`), typed to hold `nothing` or a
    `Vector{VariableRef}`. Keep the EXISTING `qag = Float64[-agg.Pdc[t]*tanφ for t in 1:T]`
    computation UNCHANGED and UNCONDITIONAL (byte-identical under every mode — this constant
    remains available regardless, per the file's existing docstring framing it as the
    "CONSTANT net reactive injection"). Under `mode == LIVE` only: declare `@variable(model,
    qag_live[1:T])`; pin it via `@constraint(model, qag_coupling[t=1:T], qag_live[t] ==
    <the aggregator's total reactive expression established from the Interfaces-note
    re-verification above>)`; `register_constraint!(ctx, :agr_qag_coupling, qag_coupling)`
    (mirrors `:agr_coupling`'s registration); extend the ONE `@objective` call (build the
    Max-objective expression as an accumulator before the single `@objective` call, exactly as
    plan 19-03 did for `DsoOpt`'s Min-objective, so `OFF`/`CERTIFIED` never construct the extra
    term) to additionally subtract `0.5*ρ_q*sum(qag_live[t]^2 for t in 1:T)`. Under
    `OFF`/`CERTIFIED`, set the new struct field to `nothing`. Update the `AgrOpt(...)`
    construction call and the struct's/function's docstrings (extend, don't rewrite, the
    existing `qag` field docstring's "PLACEHOLDER for a FUTURE reactive-consensus... extension"
    sentence — replace "FUTURE" language with "Phase 19 LIVE mode" and point at the new field).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("admm", ti.name) || occursin("aggregator", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `build_agr_opt(agg, T; ρ=ρ).qag_live === nothing` (default, no kwarg passed).
    - `build_agr_opt(agg, T; ρ=ρ, reactive_mode=:live, ρ_q=1.0).qag_live isa Vector{VariableRef}`
      of length `T`.
    - Under LIVE, `num_constraints` includes the new `qag_coupling` equalities; under
      OFF/CERTIFIED, `num_variables`/`num_constraints` are UNCHANGED from a pre-plan build
      (assert via an explicit before/after count on the SAME aggregator fixture).
    - Every pre-existing `test_admm*`/`test_aggregator*` item passes unmodified.
  </acceptance_criteria>
  <done>
    build_agr_opt/AgrOpt structurally support a genuine LIVE reactive coupling variable pinned
    to the aggregator's total q, with zero effect under OFF/CERTIFIED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: solve_agr!'s μ/d coefficient update, set_rho_q!, and the check_4q wiring point</name>
  <files>src/admm/AgrOpt.jl</files>
  <read_first>
    src/admm/AgrOpt.jl (post-Task-1 state — `solve_agr!` 166-204, `set_rho!` 230-235)
    src/models/complementarity_4q.jl (plan 19-05 — `assert_4q_complementarity!`'s exact kwargs)
  </read_first>
  <behavior>
    - `solve_agr!` accepts NEW optional kwargs `μ_j::Union{Nothing,AbstractVector}=nothing`,
      `d_j::Union{Nothing,AbstractVector}=nothing`, `ρ_q::Union{Nothing,Real}=nothing`: when
      `agr.qag_live !== nothing && μ_j !== nothing`, it updates `qag_live[t]`'s linear
      coefficient to `-μ_j[t] - ρ_q*d_j[t]` for every `t`, mirroring `pag`'s
      `-λ_j[t]-ρ*c_j[t]` update in the SAME loop (not a second loop). When `agr.qag_live ===
      nothing`, passing `μ_j` is a caller error (throws `ArgumentError`, fail loud — never a
      silent no-op that could mask a caller forgetting to build with `reactive_mode=:live`).
    - `solve_agr!` accepts a NEW `check_4q::Bool=false` kwarg: when `true`, calls
      `assert_4q_complementarity!(agr.ctx; ...)` in the SAME post-solve block as the existing
      `check_battery` call (not a separate pass).
    - `set_rho_q!(agr::AgrOpt, ρ_q::Real)` mirrors `set_rho!` exactly, batch-updating
      `qag_live[t]^2`'s coefficient to `-0.5*ρ_q`; throws `ArgumentError` if `agr.qag_live ===
      nothing`.
  </behavior>
  <action>
    Extend `solve_agr!`'s signature with `μ_j = nothing, d_j = nothing, ρ_q = nothing,
    check_4q::Bool = false` (defaults preserve every existing call site's behavior verbatim).
    Immediately after the existing length-guards for `λ_j`/`c_j`, add: if `μ_j !== nothing`,
    guard `agr.qag_live !== nothing || throw(ArgumentError("solve_agr!: μ_j supplied but this " *
    "AgrOpt was built without reactive_mode=:live"))`, and length-guard `μ_j`/`d_j` against
    `agr.T` the SAME way `λ_j`/`c_j` are guarded. In the SAME `for t in 1:agr.T` loop that
    updates `agr.pag[t]`'s coefficient, add (conditionally, only when `μ_j !== nothing`):
    `set_objective_coefficient(agr.model, agr.qag_live[t], -μ_j[t] - ρ_q*d_j[t])` — do NOT add
    a second loop. In the existing `if check_battery ... end` block, add a sibling `if check_4q;
    assert_4q_complementarity!(agr.ctx; T = agr.T); end` (pass through any additional kwargs
    the certificate needs, e.g. its own `rtol`/`atol`, as new `solve_agr!` passthrough kwargs
    with the SAME measured defaults plan 19-05 pinned — do not invent new defaults here). Add
    `set_rho_q!(agr::AgrOpt, ρ_q::Real)` immediately after the existing `set_rho!`, mirroring its
    exact batch-flatten-then-one-call shape on `agr.qag_live`, with the `ArgumentError` guard
    from `<behavior>` as its first line. Export `set_rho_q!` alongside the file's existing
    exports (note: `DsoOpt.jl` also exports a `set_rho_q!` for its own type — Julia dispatches
    on the argument type `AgrOpt` vs `DsoOpt`, so both exports coexisting is correct method-table
    behavior, not a naming collision; confirm no `MethodError`/redefinition warning appears when
    both files load).
  </action>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - Calling `solve_agr!(agr, λ_j, c_j, ρ)` (no new kwargs) on an OFF/CERTIFIED-built `AgrOpt`
      behaves byte-identically to pre-plan (every pre-existing `test_admm*` item passes
      unmodified).
    - Calling `solve_agr!` with `μ_j` set on an OFF/CERTIFIED-built `AgrOpt` throws
      `ArgumentError`.
    - Calling `solve_agr!` with `μ_j`/`d_j`/`ρ_q` set on a LIVE-built `AgrOpt` updates
      `qag_live`'s coefficient without a second solve loop or JuMP rebuild
      (`num_variables`/`num_constraints` unchanged before/after the call).
    - `check_4q = true` on an `AgrOpt` whose aggregator has a `FourQuadBESS` invokes
      `assert_4q_complementarity!` (verify via a deliberately-violating fixture that the SAME
      `ErrorException` message this function produces surfaces from `solve_agr!`, proving the
      call is genuinely wired, not merely present in source — mirrors Pitfall 2's "prove the
      certificate executes" requirement).
    - `set_rho_q!` on a non-LIVE `AgrOpt` throws `ArgumentError`; on a LIVE one it mutates the
      coefficient without changing `num_variables`/`num_constraints`.
    - Full suite pass/fail/broken counts match plan 19-01's baseline plus only new,
      expected-green Phase-19 items.
  </acceptance_criteria>
  <done>
    solve_agr! drives qag_live from a reactive price/target pair exactly like pag, wires the
    NEW 4Q certificate alongside the existing battery check, and set_rho_q! adapts ρ_q
    build-once-style — all with zero effect on OFF/CERTIFIED call sites.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-node AGR-OPT reactive coupling | The x-block half of the live μ-ascent; a wiring bug here (wrong sign, wrong target) would make plan 19-07's outer loop chase a moving target that never actually reflects the aggregator's true reactive injection |
| Certificate call-site wiring | The exact Pitfall-2 risk this plan resolves for MESH-05: a `check_4q` kwarg that exists in source but is never actually reached from `solve_admm`'s final block would be a silent coverage gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Tampering | `qag_live`'s pinning target | mitigate | Task 1's read_first mandates re-verifying the EXACT composition of `res.q_inject` against plan 19-04's actual diff before writing the pinning constraint — never assumed from this plan's own guess |
| T-19-14 | Repudiation | `check_4q` never actually reached | mitigate | Task 2's acceptance criteria require a deliberately-violating fixture to surface the certificate's exact error message THROUGH `solve_agr!`, not merely asserting the kwarg exists |
</threat_model>

<verification>
`julia --project=. -e 'import Pkg; Pkg.test()'` — full suite matches plan 19-01's baseline plus
new, expected-green Phase-19 items; zero pre-existing regression.
</verification>

<success_criteria>
AGR-OPT structurally mirrors DSO-OPT's live reactive coupling (plan 19-03): a genuine
`qag_live` variable, its own `ρ_q` penalty, a `solve_agr!` update path for μ/d, and the NEW 4Q
certificate wired in behind `check_4q` — ready for plan 19-07's outer loop to drive both blocks.
</success_criteria>

<output>
Create `.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-06-SUMMARY.md` when done
</output>
