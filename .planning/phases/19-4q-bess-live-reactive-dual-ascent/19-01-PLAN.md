---
phase: 19-4q-bess-live-reactive-dual-ascent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/TSODSO.jl
  - src/devices/FourQuadBESS.jl
  - src/models/complementarity_4q.jl
  - src/admm/ReactiveMode.jl
  - test/test_reactive_mode.jl
autonomous: true
requirements: [MESH-04, MESH-05]

must_haves:
  truths:
    - "The full pre-existing test suite passes at the SAME rate before and after this plan (no behavioral src/ change lands in this plan — only inert stub files + a self-contained enum)."
    - "The three new Phase-19 seam files exist and are wired into the module include graph, ready for later plans to fill in without touching src/TSODSO.jl again."
    - "ReactiveMode is a genuinely usable 3-state enum with Bool back-compat, independently unit-tested before any ADMM file consumes it."
  artifacts:
    - path: "src/admm/ReactiveMode.jl"
      provides: "ReactiveMode enum (OFF/CERTIFIED/LIVE) + normalize_reactive_mode(m) accepting Bool|Symbol|ReactiveMode"
      exports: ["ReactiveMode", "OFF", "CERTIFIED", "LIVE", "normalize_reactive_mode"]
    - path: "src/devices/FourQuadBESS.jl"
      provides: "Comment-only SEAM/OWNER stub, filled by plan 19-02"
    - path: "src/models/complementarity_4q.jl"
      provides: "Comment-only SEAM/OWNER stub, filled by plan 19-05"
    - path: "test/test_reactive_mode.jl"
      provides: "Unit coverage for normalize_reactive_mode's Bool/Symbol/enum/invalid dispatch"
  key_links:
    - from: "src/TSODSO.jl"
      to: "src/devices/FourQuadBESS.jl"
      via: "include(\"devices/FourQuadBESS.jl\") positioned after PVBattery.jl, before Aggregator.jl"
      pattern: "include\\(\"devices/FourQuadBESS\\.jl\"\\)"
    - from: "src/TSODSO.jl"
      to: "src/admm/ReactiveMode.jl"
      via: "include(\"admm/ReactiveMode.jl\") positioned after residuals.jl, before AgrOpt.jl"
      pattern: "include\\(\"admm/ReactiveMode\\.jl\"\\)"
---

<objective>
Wire the three new Phase-19 seam files into `src/TSODSO.jl`'s include graph (mirroring the
project's own "stub now, fill later" convention — e.g. how `data/ieee123.jl` was wired empty by
plan 07-01 and filled by 07-02), fully implement the self-contained `ReactiveMode` 3-state enum
(needed by plans 19-03/19-06/19-07), and capture the phase's byte-identity baseline (gate-then-
golden, ROADMAP success criterion 4) BEFORE any later plan makes a behavioral change to
`src/devices/Aggregator.jl`, `src/admm/DsoOpt.jl`, `src/admm/AgrOpt.jl`, or `src/admm/solve_admm.jl`.

Purpose: every later Phase-19 plan (19-02 through 19-08) needs to add file CONTENT without ever
touching `src/TSODSO.jl` again — this is what lets 19-02 (device) and 19-03 (DsoOpt) run in the
SAME wave with zero `files_modified` overlap. `ReactiveMode` is genuinely self-contained (no
JuMP, no other Phase-19 file) so it is fully built here, not merely stubbed.

Output: `src/TSODSO.jl` updated with 3 new `include(...)` lines; `src/devices/FourQuadBESS.jl`
and `src/models/complementarity_4q.jl` as comment-only stub files; `src/admm/ReactiveMode.jl`
fully implemented and exported; `test/test_reactive_mode.jl` unit-testing it; a recorded baseline
test-suite pass count for the phase's final byte-identity diff (Plan 19-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-CONTEXT.md
@.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-Phase-19 byte-identity baseline (gate-then-golden)</name>
  <files>none (read-only; no file is created or modified by this task)</files>
  <read_first>
    .planning/STATE.md (the "[test-invocation hazard]" note — the correct invocation and the
    last-known-good pass count 2358 pass / 1 fail / 3 broken)
  </read_first>
  <action>
    Run the full test suite EXACTLY via `julia --project=. -e 'import Pkg; Pkg.test()'` (never
    `--project=test`). Record the exact pass/fail/broken/error counts and the full text of the
    ONE known-accepted failure (the Aqua CairoMakie stale-deps drift) verbatim. This is the
    baseline Plan 19-08's final task diffs against to prove ROADMAP success criterion 4
    ("With no FourQuadBESS device present and the dual-ascent step disabled, ADMM and centralized
    welfare results are byte-identical to pre-milestone behavior") — record the counts in this
    task's own command output (the plan SUMMARY captures them; no separate file is written).
    Do this BEFORE Task 2 below touches src/TSODSO.jl, so the baseline is unambiguously
    pre-Phase-19.
  </action>
  <verify>
    <automated>julia --project=. -e 'import Pkg; Pkg.test()'</automated>
  </verify>
  <acceptance_criteria>
    - Command completes and reports a pass/fail/broken count.
    - The counts match (or are explicitly reconciled against) STATE.md's last-known-good
      2358 pass / 1 fail / 3 broken figure; any drift is called out explicitly in the task's
      recorded output, not silently accepted.
  </acceptance_criteria>
  <done>Baseline pass/fail/broken counts recorded, to be diffed against in Plan 19-08.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the three Phase-19 seam stubs + implement ReactiveMode</name>
  <files>src/TSODSO.jl, src/devices/FourQuadBESS.jl, src/models/complementarity_4q.jl, src/admm/ReactiveMode.jl, test/test_reactive_mode.jl</files>
  <read_first>
    src/TSODSO.jl (full include graph — the comment blocks explain WHY each block is ordered
    as it is; follow the same "SOLE owner of this shared edit" discipline for the 3 new lines)
    src/admm/DsoOpt.jl:1-45 (the existing `reactive_consensus::Bool` scaffolding this enum will
    promote in plan 19-03 — read so the enum's 3 states map 1:1 onto OFF=false/CERTIFIED=true/LIVE=new)
    src/data/ieee123.jl (the project's own "wired here as a stub, filled by a later plan" precedent
    — copy this comment-header shape for the two new stub files)
  </read_first>
  <action>
    In `src/TSODSO.jl`: add `include("devices/FourQuadBESS.jl")` immediately after the existing
    `include("devices/PVBattery.jl")   # DEV-04` line and before `include("devices/Aggregator.jl")`.
    Add `include("models/complementarity_4q.jl")` immediately after
    `include("models/exactness.jl")` and before `include("models/oracle.jl")`. Add
    `include("admm/ReactiveMode.jl")` immediately after `include("admm/residuals.jl")` and
    before `include("admm/AgrOpt.jl")`. Each new line gets a one-line trailing comment naming
    the owning Phase-19 plan and requirement ID (MESH-04 for the device/certificate files,
    MESH-05 for ReactiveMode.jl), mirroring the file's existing comment style exactly.

    Create `src/devices/FourQuadBESS.jl` as a comment-only stub: a `# src/devices/FourQuadBESS.jl`
    header, a `# SEAM: four-quadrant (P,Q) battery + inverter device (MESH-04).` line, a
    `# OWNER: plan 19-02.` line, and 2-3 prose lines describing what it will become (standalone
    4Q battery, no PV field, asymmetric grid-charging caps, apparent-power cone) — NO code, no
    `struct`, no `export`. Create `src/models/complementarity_4q.jl` the same way: header,
    `# SEAM: 4Q-BESS post-solve complementarity certificate (MESH-04 clause 2).`,
    `# OWNER: plan 19-05.`, prose describing the new named certificate function
    (peer of `assert_socp_exact!`/`assert_battery_complementarity!`, own WR-01 tolerance,
    throw-by-default with a report kwarg per D-06/D-07) — no code yet.

    Fully implement `src/admm/ReactiveMode.jl` (self-contained, zero dependency on any other
    Phase-19 file): declare `@enum ReactiveMode OFF CERTIFIED LIVE`; a function
    `normalize_reactive_mode(m)::ReactiveMode` dispatching on `m isa Bool` (`true → CERTIFIED`,
    `false → OFF`, per D-12 back-compat), `m isa ReactiveMode` (identity), and `m isa Symbol`
    (`:off → OFF`, `:certified → CERTIFIED`, `:live → LIVE`), throwing `ArgumentError` (never
    `@assert`, project convention) on any other value or unrecognized `Symbol`, naming the
    received value in the message. Export `ReactiveMode, OFF, CERTIFIED, LIVE,
    normalize_reactive_mode`. Give the enum and function full docstrings in the house style
    (mirroring `src/admm/DsoOpt.jl`'s header-comment block), explicitly stating: `OFF` reproduces
    today's constant reactive draw, `CERTIFIED` reproduces today's pinned one-shot `qag_dso`
    read (byte-identical to `reactive_consensus = true` pre-Phase-19), `LIVE` is the NEW
    Phase-19 μ-dual-ascent path — this enum is the SINGLE source of truth for that 3-way
    distinction, consumed identically by `build_dso_opt`, `build_agr_opt`, and `solve_admm`.

    Write `test/test_reactive_mode.jl` as a `@testitem` (tag `[:reactive]`, name containing
    "reactive_mode") asserting: `normalize_reactive_mode(false) == TSODSO.OFF`,
    `normalize_reactive_mode(true) == TSODSO.CERTIFIED`, `normalize_reactive_mode(:off) ==
    TSODSO.OFF`, `normalize_reactive_mode(:certified) == TSODSO.CERTIFIED`,
    `normalize_reactive_mode(:live) == TSODSO.LIVE`, `normalize_reactive_mode(TSODSO.LIVE) ==
    TSODSO.LIVE` (identity), and `@test_throws ArgumentError normalize_reactive_mode(:bogus)` /
    `@test_throws ArgumentError normalize_reactive_mode(1)`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; using TSODSO; TestItemRunner.runtests(TSODSO; filter=ti->occursin("reactive_mode", ti.name))'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'include("devices/FourQuadBESS.jl")' src/TSODSO.jl` equals 1, positioned between
      the PVBattery.jl and Aggregator.jl include lines (verify via `grep -n` line ordering).
    - `grep -c 'include("models/complementarity_4q.jl")' src/TSODSO.jl` equals 1, between
      exactness.jl and oracle.jl.
    - `grep -c 'include("admm/ReactiveMode.jl")' src/TSODSO.jl` equals 1, between residuals.jl
      and AgrOpt.jl.
    - `src/devices/FourQuadBESS.jl` and `src/models/complementarity_4q.jl` contain zero `struct`,
      `function`, or `export` tokens (comment-only, per `grep -v '^#' | grep -c` returning 0 for
      any of those tokens).
    - `test/test_reactive_mode.jl`'s `@testitem` passes.
    - Full suite (`julia --project=. -e 'import Pkg; Pkg.test()'`) still matches Task 1's
      recorded baseline exactly (the two stub files and the new enum are load-bearing for
      NOTHING yet, so zero pre-existing test can be affected).
  </acceptance_criteria>
  <done>
    Three new files exist and are wired into the include graph; ReactiveMode is fully
    implemented, exported, and unit-tested; the full suite is unchanged from Task 1's baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module include order | `src/TSODSO.jl` is the sole assembly point; an out-of-order include can silently reference an undefined symbol at load time |
| Research-code-as-golden-input | This is a local research library — no network/auth surface; the only "trust boundary" is whether a stubbed/new file can be silently mis-wired into the wrong include position and corrupt an existing formulation's load-time behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | src/TSODSO.jl include graph | mitigate | New includes are comment-only stubs or a zero-dependency enum; acceptance criteria grep-verify exact line count and position so a misplaced include cannot silently shadow an existing seam |
| T-19-02 | Repudiation | Byte-identity baseline (success criterion 4) | mitigate | Task 1 captures the EXACT pre-Phase-19 pass/fail/broken counts before any file is touched, giving Plan 19-08 an unambiguous diff target rather than a post-hoc, possibly-contaminated reconstruction |
</threat_model>

<verification>
Full suite: `julia --project=. -e 'import Pkg; Pkg.test()'` must report the SAME counts before
and after this plan's Task 2. Targeted: the new `test_reactive_mode.jl` item passes.
</verification>

<success_criteria>
- `src/TSODSO.jl` has exactly 3 new, correctly-positioned `include(...)` lines.
- `ReactiveMode`/`OFF`/`CERTIFIED`/`LIVE`/`normalize_reactive_mode` are exported and unit-tested.
- The two device/certificate stub files are inert (comment-only).
- The full suite's pass/fail/broken counts are unchanged from the pre-Phase-19 baseline.
</success_criteria>

<output>
Create `.planning/phases/19-4q-bess-live-reactive-dual-ascent/19-01-SUMMARY.md` when done
</output>
