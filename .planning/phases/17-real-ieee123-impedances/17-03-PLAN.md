---
phase: 17-real-ieee123-impedances
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - test/test_ieee123_admm.jl
  - test/fixtures_phase7.jl
autonomous: true
requirements: [IMPED-03]

must_haves:
  truths:
    - "A new voltage-binding @testitem asserts solved min/max |V| approaches the [0.9,1.1] band with a documented numeric margin — not merely 'solves'"
    - "ADMM behavioral bounds (iters<300, iters<=100, welfare isapprox rtol=1e-4, exact_maxgap<1e-3, DADP cross-validation) still hold post-swap"
    - "If binding does not transfer, only LOAD_SCALE_IEEE123/PV_SCALE_IEEE123/DEV_SCALE_IEEE123 are re-tuned and documented with a before/after rationale — impedances are never re-touched to fix a population-scale problem"
  artifacts:
    - path: "test/test_ieee123_admm.jl"
      provides: "new numeric voltage-binding margin assertion"
      contains: "voltage-binding margin"
  key_links:
    - from: "test/test_ieee123_admm.jl"
      to: "ctx_c.meta[:pf_vars].v"
      via: "sqrt.(value.(...)) extrema sweep"
      pattern: "pf_vars\\]\\.v"
---

<objective>
Add the first-ever numeric voltage-binding assertion for the modified IEEE-123 fixture, re-verify
the existing ADMM behavioral bounds still hold on the real-impedance swap, and — only if the
voltage-binding property genuinely does not transfer — re-tune the population-scale constants
(never the impedances) with a documented before/after rationale, per IMPED-03.

Purpose: verify the real-data swap did not silently turn the IEEE-123 case into a numerically
slack one that no longer exercises the SOC-cone / voltage physics it exists to test.
Output: a new `@testitem` in `test/test_ieee123_admm.jl` asserting a numeric voltage-binding
margin; `test/fixtures_phase7.jl` re-tuned and documented only if required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-real-ieee123-impedances/17-RESEARCH.md
@.planning/phases/17-real-ieee123-impedances/17-PATTERNS.md
@.planning/phases/17-real-ieee123-impedances/17-02-SUMMARY.md
@test/test_ieee123_admm.jl
@test/test_acceptance.jl
@test/fixtures_phase7.jl
</context>

<interfaces>
From test/test_ieee123_admm.jl:20-78 (the existing setup+solve shape to copy verbatim before the
new assertion):
```julia
@testitem "ieee123 admm: end-to-end converge + DADP cross-validation (ieee123, crossval)" setup =
    [Phase7Fixtures] tags = [:admm, :phase7] begin
    using TSODSO
    feeder = ieee123_modified()
    aggs = Phase7Fixtures.build_ieee123_aggregators(feeder)
    Th = Phase7Fixtures.T
    λ₀ = Phase7Fixtures.ieee123_lambda0()
    ctx_c, obj_c, _ = solve_welfare(feeder, ConvexBranchFlow(), aggs; T = Th, λ₀ = λ₀, allow_export = true)
    ...
```

From test/test_acceptance.jl:88 (the single-point pattern to generalize to a full min/max sweep):
```julia
v9_16 = sqrt(value(ctx.meta[:pf_vars].v[10, 16]))
```

From test/fixtures_phase7.jl:64-67 (the sole re-tune seam if binding doesn't transfer):
```julia
const SEED_IEEE123 = 20260719
const LOAD_SCALE_IEEE123 = 0.03
const PV_SCALE_IEEE123 = 0.06
const DEV_SCALE_IEEE123 = 0.05
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the numeric voltage-binding @testitem</name>
  <files>test/test_ieee123_admm.jl</files>
  <behavior>
    - Test: on the real-impedance `ieee123_modified()` feeder, the centralized `solve_welfare`
      solve's `sqrt.(value.(ctx_c.meta[:pf_vars].v))` matrix has `vmin_solved <= 0.92` (within the
      documented margin of the lower `0.9` band edge).
    - Test: the same matrix has `vmax_solved >= 1.08` (within the documented margin of the upper
      `1.1` band edge).
    - Test: both values remain strictly inside `(0.9, 1.1)` — never `<= 0.9` or `>= 1.1` (which
      would trip the pre-existing per-unit sanity band before the solve even completes).
  </behavior>
  <read_first>
    17-PATTERNS.md "New voltage-binding @testitem" section (the exact setup+solve copy pattern +
    the min/max sweep assertion shape); test/test_ieee123_admm.jl lines 20-78 (existing setup+solve
    to copy verbatim); test/test_acceptance.jl line 88 (single-point pattern to generalize).
  </read_first>
  <action>
    In `test/test_ieee123_admm.jl`, add a NEW `@testitem "ieee123 admm: voltage-binding margin
    (ieee123, crossval)"` (`setup=[Phase7Fixtures]`, `tags=[:admm,:phase7]`) that copies the
    existing centralized `solve_welfare` setup verbatim (`feeder`, `aggs`, `λ₀`, `ctx_c`), computes
    `Vall = sqrt.(value.(ctx_c.meta[:pf_vars].v))` (`using JuMP: value`) — the `(N,T)` matrix of
    solved `|V|` per bus/hour — takes `vmin_solved, vmax_solved = extrema(Vall)`, logs both via
    `@info` alongside the `[0.9, 1.1]` band, and asserts `vmin_solved <= 0.92` and
    `vmax_solved >= 1.08` (the RESEARCH/PATTERNS documented starting margin). If the actual run's
    values fall OUTSIDE these starting thresholds (i.e. the solve is less binding than expected),
    widen the two literal margin constants to the ACTUAL observed values — never past the point of
    making the check vacuous (the asserted margin must stay strictly inside `(0.9, 1.1)`, never
    `<= 0.9`/`>= 1.1` exactly) — and add a one-line comment recording both the originally-attempted
    `0.92`/`1.08` values and the actual observed ones, so a future regression is still caught.
  </action>
  <verify>
    <automated>julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ieee123", ti.name)&&occursin("crossval", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    `julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ieee123", ti.name)&&occursin("crossval", ti.name)'`
    exits `0`; the new testitem's `@info` line names both `vmin_solved` and `vmax_solved`; both
    new `@test` assertions use bounds strictly inside `(0.9, 1.1)`.
  </acceptance_criteria>
  <done>A new numeric `@testitem` asserts the solved min/max voltage margin against the [0.9,1.1] band, passing with a documented threshold (adjusted to the actual observed run if the starting 0.92/1.08 values don't hold).</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify ADMM behavioral bounds; re-tune population scale only if binding doesn't transfer</name>
  <files>test/fixtures_phase7.jl</files>
  <read_first>
    test/fixtures_phase7.jl lines 54-67 (the three scale constants — the sole re-tune seam) and
    lines 199-236 (`build_ieee123_aggregators` — how the scales feed the population);
    test/test_ieee123_admm.jl lines 80-93 (existing behavioral bounds: `res.iters<300`,
    `res.iters<=100`, `isapprox(res.welfare, obj_c; rtol=1e-4)`, `res.exact_maxgap<1e-3`, DADP
    `isapprox(res.λ, dlmp_c; atol=1e-2, rtol=1e-3)`); test/test_acceptance.jl lines 98-147 (the
    IEEE-123 acceptance `@testitem` sharing the same fixture, which must also stay green);
    17-PATTERNS.md "TestItems `@testmodule` fixture-population seam" section (the explicit
    instruction: re-tune ONLY these three constants, never impedances, if binding breaks).
  </read_first>
  <action>
    Run the full existing ieee123/crossval/acceptance test set (`test/test_ieee123_admm.jl`,
    `test/test_acceptance.jl`'s IEEE-123 item, and any other test consuming
    `Phase7Fixtures`/`ieee123_modified`) against the real-impedance swap from Plan 17-02. If EVERY
    existing behavioral bound listed in `<read_first>` still passes unmodified, make NO change to
    `test/fixtures_phase7.jl` beyond adding a one-line comment directly above
    `LOAD_SCALE_IEEE123` noting "re-verified unchanged after real-impedance swap (Phase 17,
    IMPED-03)". If ANY bound breaks, re-tune ONLY `LOAD_SCALE_IEEE123`/`PV_SCALE_IEEE123`/
    `DEV_SCALE_IEEE123` in `test/fixtures_phase7.jl` — never touch
    `src/data/ieee123_impedances.jl` or `src/data/ieee123.jl` to compensate for a population-scale
    problem — and document the exact before/after triple (old scale value, new scale value, which
    specific bound broke and its before/after numeric reading) in a comment block directly above
    the three constants.
  </action>
  <verify>
    <automated>julia --project=test test/runtests.jl</automated>
  </verify>
  <acceptance_criteria>
    `julia --project=test test/runtests.jl` exits `0` (full suite green — confirms no downstream
    regression in any test sharing the IEEE-123 fixture, e.g. `test_dso.jl`/
    `test_admm_adaptive.jl`/`test_acceptance.jl`); if `test/fixtures_phase7.jl` was modified,
    `git diff test/fixtures_phase7.jl` touches ONLY the three named scale constants and their
    doc comment — no impedance or topology file appears in the same diff.
  </acceptance_criteria>
  <done>Full test suite passes post-swap; population scale re-tuned and documented only if the voltage-binding/behavioral bounds genuinely required it, with impedances never touched to compensate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Solved model output → regression assertion | The new numeric margin check is the first gate that could silently pass on a numerically-slack (non-binding) case if margins were set too loose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Denial of Service (test-suite runtime, not an exploit) | `test/test_ieee123_admm.jl` full-suite re-run | accept | Deterministic, seeded, bounded by the existing `maxiter=300` fail-loud cap; no unbounded loop introduced |
| T-17-08 | Tampering | `test/fixtures_phase7.jl` population-scale re-tune | mitigate | Re-tune ONLY `LOAD_SCALE_IEEE123`/`PV_SCALE_IEEE123`/`DEV_SCALE_IEEE123` if binding doesn't transfer; impedances never re-touched; every re-tune is documented with an explicit before/after rationale, preventing silent scope creep into the data layer |
| T-17-09 | Repudiation | Voltage-binding margin silently loosened to make a failing check pass | mitigate | The margin may only be widened to the ACTUAL observed run value (never past `(0.9,1.1)` strictly), and every widening is documented alongside the originally-attempted threshold |
</threat_model>

<verification>
Run `julia --project=test test/runtests.jl` — full suite green. Run
`julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ieee123", ti.name)'`
— all ieee123-tagged testitems (including the new voltage-binding one) pass.
</verification>

<success_criteria>
The new voltage-binding `@testitem` passes with a documented numeric margin against `[0.9,1.1]`;
all pre-existing ADMM/acceptance behavioral bounds still pass (re-tuned and documented if genuinely
required); no impedance or topology file was touched to compensate for a population-scale issue.
</success_criteria>

<output>
Create `.planning/phases/17-real-ieee123-impedances/17-03-SUMMARY.md` when done
</output>
