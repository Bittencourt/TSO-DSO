---
phase: 17-real-ieee123-impedances
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/data/ieee123_impedances.jl
  - src/data/ieee123.jl
  - test/test_ieee123.jl
autonomous: true
requirements: [IMPED-02]

must_haves:
  truths:
    - "src/data/ieee123_impedances.jl exists as a generated per-segment Ω const table keyed by the existing IEEE123_EDGES tuples"
    - "ieee123_modified() converts Ω→pu via to_pu_impedance at ingestion (the framework's single documented SI→pu seam), never inside the reduction script"
    - "IEEE123_EDGES and IEEE123_SWITCH_EDGES are byte-identical before and after the swap — topology is untouched"
    - "the existing 0 < r,x < 5 pu positivity tripwire (assert_magnitudes) still holds against real data"
  artifacts:
    - path: "src/data/ieee123_impedances.jl"
      provides: "IEEE123_BRANCH_RX_OHMS const Dict, generated by scripts/reduce_ieee123_impedances.jl"
      contains: "const IEEE123_BRANCH_RX_OHMS"
    - path: "src/data/ieee123.jl"
      provides: "real-impedance ingestion replacing the two uniform synthetic scalars"
      contains: "to_pu_impedance(r_Ω"
  key_links:
    - from: "src/data/ieee123.jl"
      to: "src/data/ieee123_impedances.jl"
      via: "include(\"ieee123_impedances.jl\") + IEEE123_BRANCH_RX_OHMS[(p, c)] lookup"
      pattern: "IEEE123_BRANCH_RX_OHMS\\["
    - from: "src/data/ieee123.jl"
      to: "src/units/PerUnit.jl"
      via: "to_pu_impedance call at ingestion"
      pattern: "to_pu_impedance\\("
---

<objective>
Run the Plan-17-01 reduction script to generate the committed per-segment Ω impedance table, wire
`src/data/ieee123.jl` to consume it (replacing the two uniform synthetic scalars) via the
framework's existing `to_pu_impedance` ingestion seam, and extend `test/test_ieee123.jl` with a
pinned real-data spot-check — all while leaving the feeder's topology byte-identical.

Purpose: make the modified IEEE-123 fixture's per-branch impedances real and citable instead of a
single representative placeholder value, per IMPED-02.
Output: `src/data/ieee123_impedances.jl` (generated const table), `src/data/ieee123.jl` (modified
ingestion), `test/test_ieee123.jl` (extended with a real-data pinned spot-check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-real-ieee123-impedances/17-RESEARCH.md
@.planning/phases/17-real-ieee123-impedances/17-PATTERNS.md
@.planning/phases/17-real-ieee123-impedances/17-01-SUMMARY.md
@src/data/ieee123.jl
@src/units/PerUnit.jl
@test/test_ieee123.jl
</context>

<interfaces>
From src/data/ieee123.jl:56-80 (the exact block being edited — two dead scalars removed, loop
replaced):
```julia
const IEEE123_LINE_R = 0.005
const IEEE123_LINE_X = 0.0025
const IEEE123_SWITCH_R = 0.0003
const IEEE123_SWITCH_X = 0.00015
```

From src/data/ieee123.jl:428-435 (`ieee123_modified()`'s branch-build loop — the exact lines
being replaced):
```julia
branches = Branch{Float64}[]
for (p, c) in IEEE123_EDGES
    is_switch = (p, c) in IEEE123_SWITCH_EDGES
    r = is_switch ? IEEE123_SWITCH_R : IEEE123_LINE_R
    x = is_switch ? IEEE123_SWITCH_X : IEEE123_LINE_X
    smax = p == IEEE123_ROOT_TERMINAL ? s_head : s_int
    push!(branches, Branch(remap[p], remap[c], r, x, smax))
end
```

From src/units/PerUnit.jl:53 (the existing ingestion seam, reused verbatim, never reinvented):
```julia
to_pu_impedance(z_Ω, b::PerUnitBase) = z_Ω / Z_base(b)
```

From src/TSODSO.jl:29 (the SOLE include point for `ieee123.jl` — no sibling include is added):
```julia
include("data/ieee123.jl")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit the per-segment impedance const table</name>
  <files>src/data/ieee123_impedances.jl</files>
  <read_first>
    17-PATTERNS.md "src/data/ieee123_impedances.jl (model/config, generated const table)" section
    (exact provenance header + emitted const shape, keyed by the original `IEEE123_EDGES`
    tuples, values in Ω not pu); src/data/ieee123.jl lines 82-215 (the full `IEEE123_EDGES`
    122-tuple list + `IEEE123_SWITCH_EDGES` 5-tuple set the emitted table must key against);
    17-01-SUMMARY.md (confirms the reduction script's non-`--verify` emission path).
  </read_first>
  <action>
    Run `julia scripts/reduce_ieee123_impedances.jl` (no `--verify` flag) to execute the full
    parse+reduce+emit path built in Plan 17-01, writing `src/data/ieee123_impedances.jl`. Confirm
    the emitted file declares `const IEEE123_BRANCH_RX_OHMS::Dict{Tuple{Int,Int},Tuple{Float64,Float64}}`
    with exactly 117 entries (the 122 `IEEE123_EDGES` tuples minus the 5 `IEEE123_SWITCH_EDGES`
    tuples), keyed by the ORIGINAL (pre-relabel) terminal-pair tuples exactly as they appear in
    `IEEE123_EDGES`, with values in Ω (not pu — conversion happens later, in Task 2), and carrying
    the GENERATED/DO-NOT-HAND-EDIT provenance header (source URL, fetch date, Fortescue-reduction
    note, and the "converted once at ingestion via `to_pu_impedance`" cross-reference).
  </action>
  <verify>
    <automated>julia -e 'include("src/data/ieee123_impedances.jl"); @assert length(IEEE123_BRANCH_RX_OHMS) == 117; @assert haskey(IEEE123_BRANCH_RX_OHMS, (149,1))'</automated>
  </verify>
  <acceptance_criteria>
    `julia -e 'include("src/data/ieee123_impedances.jl"); @assert length(IEEE123_BRANCH_RX_OHMS) == 117; @assert haskey(IEEE123_BRANCH_RX_OHMS, (149,1))'`
    runs without error; `grep -c 'const IEEE123_BRANCH_RX_OHMS' src/data/ieee123_impedances.jl`
    equals `1`; the first non-blank line of the file starts with `#` and contains both
    `GENERATED` and `DO NOT hand-edit`.
  </acceptance_criteria>
  <done>`src/data/ieee123_impedances.jl` exists, committed, with exactly 117 Ω-valued entries keyed by the untouched `IEEE123_EDGES` tuples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap ieee123.jl's branch-build loop to consume real per-segment Ω data</name>
  <files>src/data/ieee123.jl, test/test_ieee123.jl</files>
  <behavior>
    - Test: a known non-switch branch `(149, 1)` (linecode 1, length 0.4 per the vendored data)
      produces pu `(r, x)` equal to `to_pu_impedance(0.057967 * 0.4, IEEE123_BASE)` /
      `to_pu_impedance(0.118756 * 0.4, IEEE123_BASE)` within `atol = 1e-6`.
    - Test: `IEEE123_EDGES` / `IEEE123_SWITCH_EDGES` are byte-identical before/after (git diff
      shows zero lines changed on those two consts).
    - Test: every branch still satisfies the existing `0 < r,x < 5` pu tripwire (`assert_magnitudes`,
      run automatically at `Feeder(...)` construction inside `ieee123_modified()`).
  </behavior>
  <read_first>
    17-PATTERNS.md "src/data/ieee123.jl (MODIFY)" section (the exact loop replacement + Pattern 3
    Ω→pu conversion, verbatim); src/data/ieee123.jl lines 417-439 (`ieee123_modified()` — the loop
    being replaced) and lines 13-32 (the "DATA PROVENANCE / TRANSCRIPTION NOTE" header to update);
    test/test_ieee123.jl lines 41-64 (existing "voltage band + per-unit magnitude sanity"
    `@testitem` — keep UNMODIFIED) and lines 66-90 (the pinned-value spot-check style to mirror
    for the new assertion).
  </read_first>
  <action>
    In `src/data/ieee123.jl`, add `include("ieee123_impedances.jl")` immediately after the
    `using SparseArrays` line (before any `const` declaration), so `IEEE123_BRANCH_RX_OHMS` loads
    transitively through `src/TSODSO.jl`'s single existing `include("data/ieee123.jl")` line — do
    NOT add a second top-level include in `TSODSO.jl`. Remove the dead `IEEE123_LINE_R`/
    `IEEE123_LINE_X` const declarations (keep `IEEE123_SWITCH_R`/`IEEE123_SWITCH_X` unchanged —
    switch/regulator segments intentionally retain their existing near-ideal synthetic value, per
    RESEARCH Assumption A2). Inside `ieee123_modified()`'s branch-build loop, replace the
    `r = is_switch ? IEEE123_SWITCH_R : IEEE123_LINE_R` / `x = ...` two-liner with an
    `if is_switch ... else` branch that looks up `r_Ω, x_Ω = IEEE123_BRANCH_RX_OHMS[(p, c)]` and
    converts `r, x = to_pu_impedance(r_Ω, IEEE123_BASE), to_pu_impedance(x_Ω, IEEE123_BASE)`,
    exactly per Pattern 3. Update the file's "DATA PROVENANCE / TRANSCRIPTION NOTE" header
    paragraph (the "PER-UNIT R/X MAGNITUDES are REPRESENTATIVE" section) to point at
    `ieee123_impedances.jl`'s new provenance header instead of describing the retired
    representative-value approach — leave every OTHER paragraph in that same header block
    (topology, relabeling, switch documentation) completely UNCHANGED. Do NOT edit `IEEE123_EDGES`
    or `IEEE123_SWITCH_EDGES` in any way. In `test/test_ieee123.jl`, add ONE new `@testitem` (name
    containing `"ieee123"`, `tags = [:phase7]`) implementing the pinned `(149,1)` spot-check from
    `<behavior>` above, mirroring the existing `remap[1] == 2` pinned-value style; leave the
    pre-existing `br.r > 0`/`br.x > 0`/`0 < br.smax < 100` `@testitem` completely UNMODIFIED (it is
    a free regression net that already re-validates real data via `assert_magnitudes`).
  </action>
  <verify>
    <automated>julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ieee123", ti.name)'</automated>
  </verify>
  <acceptance_criteria>
    `git diff --unified=0 src/data/ieee123.jl | grep -E '^[+-]' | grep -c 'IEEE123_EDGES\|IEEE123_SWITCH_EDGES ='` equals `0` (topology arrays untouched — the explicit IMPED-02 check);
    `grep -c 'IEEE123_LINE_R\|IEEE123_LINE_X' src/data/ieee123.jl` equals `0` (dead scalars removed);
    `julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ieee123", ti.name)'` exits `0`, all ieee123-tagged testitems pass; `grep -c '@testitem' test/test_ieee123.jl` equals `5` (4 pre-existing + 1 new).
  </acceptance_criteria>
  <done>`ieee123_modified()` builds real per-segment pu impedances via `to_pu_impedance`, topology untouched, existing positivity tripwire re-verified, new pinned spot-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Generated data file → package ingestion | `ieee123_impedances.jl`'s Ω values cross into per-unit space exactly once, at `ieee123_modified()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | `src/data/ieee123.jl` (Ω→pu ingestion) | mitigate | Reuse `to_pu_impedance` verbatim (the single documented SI→pu seam); `assert_magnitudes` re-validates `0≤r,x<5` automatically on every real value at `Feeder()` construction — a free regression gate against a unit-conversion bug |
| T-17-05 | Repudiation | `IEEE123_EDGES`/`IEEE123_SWITCH_EDGES` topology drift | mitigate | Explicit acceptance check: `git diff` shows zero changed lines on both consts — topology stays byte-identical per IMPED-02 |
| T-17-06 | Tampering | Dead-code constants left alongside the new table | mitigate | Remove `IEEE123_LINE_R`/`IEEE123_LINE_X` once the per-segment table lands, avoiding two parallel, inconsistent impedance sources in the same file |
</threat_model>

<verification>
Run `julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("ieee123", ti.name)'`
— all ieee123-tagged testitems (5 total) pass. Run `git diff src/data/ieee123.jl` and confirm no
line inside the `IEEE123_EDGES`/`IEEE123_SWITCH_EDGES` array literals changed.
</verification>

<success_criteria>
`ieee123_modified()` builds without error using real per-segment Ω→pu impedances; topology
(`IEEE123_EDGES`, `IEEE123_SWITCH_EDGES`) byte-identical to pre-plan; the existing `0 < r,x < 5`
positivity tripwire and the new pinned real-data spot-check both pass.
</success_criteria>

<output>
Create `.planning/phases/17-real-ieee123-impedances/17-02-SUMMARY.md` when done
</output>
