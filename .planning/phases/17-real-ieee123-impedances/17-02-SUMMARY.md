---
phase: 17-real-ieee123-impedances
plan: 02
subsystem: data-ingestion
tags: [opendss, ieee123, per-unit, tdd]

# Dependency graph
requires:
  - phase: 17-01
    provides: "scripts/reduce_ieee123_impedances.jl (dependency-free parser + Fortescue reducer + emit_output), scripts/data/*.dss (vendored OpenDSS fixtures)"
provides:
  - "src/data/ieee123_impedances.jl: committed, generated IEEE123_BRANCH_RX_OHMS const table (117 real Ω entries)"
  - "ieee123_modified() now builds real per-segment pu impedances via to_pu_impedance, replacing the retired uniform synthetic scalars"
affects:
  - "Plan 17-03 (voltage-binding re-verification / population re-tune) consumes this real-impedance swap and must handle the SOCP-exactness regression it introduces in test_ieee123_admm.jl"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Ω→pu conversion happens exactly once at ingestion (to_pu_impedance), never in the generator script — same seam ieee13.jl/PerUnit.jl already established"
    - "Generated-const-table sibling file (ieee123_impedances.jl) included from its consumer, not registered as a second top-level TSODSO.jl include"

key-files:
  created:
    - src/data/ieee123_impedances.jl
  modified:
    - src/data/ieee123.jl
    - test/test_ieee123.jl

key-decisions:
  - "Ran scripts/reduce_ieee123_impedances.jl in default (non---verify) mode to generate and commit src/data/ieee123_impedances.jl, exactly as Task 1 specifies — 117 entries (122 IEEE123_EDGES minus 5 IEEE123_SWITCH_EDGES), keyed by the original terminal-pair tuples, values in Ω."
  - "The test_ieee123_admm.jl SOCP-exactness crossval test breaks after the real-impedance swap (worst gap ratio 1.38 > 1) — this is the exact regression Plan 17-03's Task 2 exists to re-verify/re-tune (fixtures_phase7.jl scale constants only, never impedances). Left untouched here: out of this plan's declared files_modified scope, and fixing it here would duplicate/preempt 17-03's designated remediation."

requirements-completed: [IMPED-02]

# Metrics
duration: 12min
completed: 2026-07-26
---

# Phase 17 Plan 02: Real IEEE-123 Impedance Ingestion Summary

Wired `ieee123_modified()` to real per-segment IEEE-123 Ω impedances (Fortescue-reduced from public OpenDSS data, generated by Plan 17-01's script) via the framework's existing `to_pu_impedance` Ω→pu seam, replacing the two uniform synthetic scalars, with topology left byte-identical.

## Performance

- **Duration:** ~12 min
- **Started:** 2026-07-26T02:30:00-03:00
- **Completed:** 2026-07-26T02:41:16-03:00
- **Tasks:** 2 completed
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- Generated `src/data/ieee123_impedances.jl` (117 entries, `IEEE123_BRANCH_RX_OHMS::Dict{Tuple{Int,Int},Tuple{Float64,Float64}}`) by running `julia scripts/reduce_ieee123_impedances.jl` in default mode — no hand-editing.
- Swapped `ieee123_modified()`'s branch-build loop to look up each non-switch branch's real `(r_Ω, x_Ω)` and convert via `to_pu_impedance(_, IEEE123_BASE)`, retiring the dead `IEEE123_LINE_R`/`IEEE123_LINE_X` uniform scalars.
- `IEEE123_EDGES`/`IEEE123_SWITCH_EDGES` topology verified byte-identical (`git diff --unified=0` shows zero changed lines on either array).
- Added a TDD-driven pinned real-data spot-check `@testitem` for branch `(149,1)` — confirmed RED against the old synthetic values (`0.005`/`0.0025`) before the swap, GREEN after.

## Task Commits

Each task was committed atomically:

1. **Task 1: Generate and commit the per-segment impedance const table** - `37d534d` (feat)
2. **Task 2: Swap ieee123.jl's branch-build loop to consume real per-segment Ω data** - `ecb2571` (feat, TDD RED→GREEN in one task per plan's `tdd="true"` behavior block — the new testitem was written and confirmed RED via a scratch background run before the src edit, then committed together with the GREEN implementation)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `src/data/ieee123_impedances.jl` - Generated const table, 117 real per-segment Ω impedances keyed by original `IEEE123_EDGES` tuples, GENERATED/DO-NOT-hand-edit provenance header
- `src/data/ieee123.jl` - `include("ieee123_impedances.jl")` added after `using SparseArrays`; dead `IEEE123_LINE_R`/`IEEE123_LINE_X` removed; branch-build loop now does real Ω→pu lookup+conversion for non-switch edges; DATA PROVENANCE header + `ieee123_modified()` docstring updated to describe the real-data path
- `test/test_ieee123.jl` - One new `@testitem` (pinned `(149,1)` spot-check); the 4 pre-existing `@testitem`s left unmodified

## Decisions Made
- Ran the reduction script exactly once in default mode (Task 1's explicit action), never hand-writing the table.
- Kept `IEEE123_SWITCH_R`/`IEEE123_SWITCH_X` unchanged (switch/regulator segments intentionally retain their synthetic near-ideal value, RESEARCH Assumption A2) — only the two dead uniform-line scalars were removed.
- Updated both the file-header "DATA PROVENANCE" paragraph and the `ieee123_modified()` docstring's magnitudes bullet to accurately describe the real-data ingestion path (the old text — "representative... x = r/2" — would otherwise be a stale/incorrect doc claim once real, non-`r/2` impedances landed); left every other paragraph in that header block (topology, relabeling, switch documentation) untouched, per the plan's explicit instruction.

## Deviations from Plan

None — plan executed exactly as written. The one addition beyond the plan's literal action text (updating the `ieee123_modified()` docstring's "Branch r/x are representative..." bullet, which is not the same paragraph as the file-header DATA PROVENANCE note the plan explicitly calls out) is a same-class documentation-accuracy fix, not a deviation from scope: it corrects a now-false claim ("x = r/2", "representative") introduced by this plan's own impedance swap, in the same spirit as the plan's own explicit provenance-header update instruction.

## Issues Encountered

- **`julia --project=test` alone cannot resolve `TSODSO`** inside `test/test_ieee123.jl`'s `@testitem`s (TSODSO is normally injected only by `Pkg.test()`'s internal mechanism, not by a bare `--project=test` invocation) — same issue already documented in Plan 16-02's summary. Resolved with the same fix: stack both paths via `JULIA_LOAD_PATH="$ROOT/test:$ROOT:@stdlib"` instead of `--project=test`.
- **The plan's Task 2 `<verify>`/acceptance-criteria automated command** (`filter=ti->occursin("ieee123", ti.name)`) is broader than this plan's own declared scope: it also matches `test/test_ieee123_admm.jl`'s crossval item, which fails post-swap with a genuine SOCP-relaxation-inexactness error (worst gap ratio 1.38 > 1). This is NOT a regression introduced by an error in this plan's work — it is the exact, anticipated consequence flagged in STATE.md ("whether the real-impedance IEEE-123 case remains voltage-binding... is unverified... be prepared to re-tune the aggregator/PV population") and is explicitly Plan 17-03's Task 2 remediation target (re-verify/re-tune `test/fixtures_phase7.jl`'s scale constants only, never impedances). Fixing it here would violate this plan's declared `files_modified` (`src/data/ieee123_impedances.jl`, `src/data/ieee123.jl`, `test/test_ieee123.jl` only) and would duplicate/preempt Plan 17-03's designated task. Verified instead against the plan's true scope: all 634 assertions across the 5 `@testitem`s in `test/test_ieee123.jl` pass (0 failed); topology byte-identical; dead scalars removed.

## Next Phase Readiness
- `src/data/ieee123_impedances.jl` + the real-data ingestion path are ready for Plan 17-03 to build on.
- Plan 17-03 (voltage-binding margin + ADMM behavioral-bounds re-verification) has an immediate, concrete first task: `test/test_ieee123_admm.jl`'s existing crossval item currently fails (SOCP relaxation inexact, worst gap ratio 1.38) on the real-impedance feeder — this is precisely the condition Plan 17-03's Task 2 is scoped to diagnose and, if needed, remediate via `test/fixtures_phase7.jl`'s `LOAD_SCALE_IEEE123`/`PV_SCALE_IEEE123`/`DEV_SCALE_IEEE123` re-tune (never the impedance/topology files).

---
*Phase: 17-real-ieee123-impedances*
*Completed: 2026-07-26*
