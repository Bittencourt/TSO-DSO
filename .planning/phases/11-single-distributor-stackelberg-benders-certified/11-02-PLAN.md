---
phase: 11-single-distributor-stackelberg-benders-certified
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/planning/benders.jl
  - src/TSODSO.jl
  - test/test_planning_benders.jl
autonomous: true
requirements: [PLAN-06]
must_haves:
  truths:
    - "A single-distributor Stackelberg equilibrium (flexibility-investment leader, transmission-reinforcement follower, operational welfare oracle) solves end-to-end via a hand-rolled Benders loop on the Phase-11 toy fixture, converging to a reported relative UB/LB gap at or below the documented tolerance"
    - "The oracle (PlanningOracle) and the follower (FollowerLP) are each built EXACTLY ONCE outside the loop; the loop mutates neither by rebuilding — only set_parameter_value/re-solve and persistent master cut-row growth"
    - "A feasibility cut is produced (and never mistaken for an optimality cut) whenever the master proposes a z_k the follower cannot deliver"
    - "checkpoint_iteration! fires exactly once per Benders iteration; hitting the iteration cap without convergence raises loudly, naming the last observed gap, rather than silently returning a non-converged result"
    - "No planning-layer subproblem introduces a binary/integer variable"
  artifacts:
    - path: "src/planning/benders.jl"
      provides: "solve_stackelberg! — the outer Benders orchestration loop over PlanningOracle/FollowerLP/BendersMaster"
      contains: "function solve_stackelberg!"
      min_lines: 90
    - path: "test/test_planning_benders.jl"
      provides: "@testitems: end-to-end convergence + hand-enumerated cross-check, iteration-cap loud-failure regression, checkpoint-fires-per-iteration regression"
      contains: "@testitem"
      min_lines: 70
  key_links:
    - from: "src/planning/benders.jl"
      to: "src/planning/subproblem.jl"
      via: "solve_planning_oracle!(oracle, z_k) called once per iteration, never rebuilding the oracle"
      pattern: "solve_planning_oracle!\\(oracle"
    - from: "src/planning/benders.jl"
      to: "src/planning/follower.jl"
      via: "solve_follower!(follower, z_k) called directly, never wrapped in solve_with_retry!"
      pattern: "solve_follower!\\(follower"
    - from: "src/planning/benders.jl"
      to: "src/planning/master.jl"
      via: "add_optimality_cut!/add_feasibility_cut!/solve_master! — the persistent cut-row API from plan 11-01"
      pattern: "add_optimality_cut!\\(master"
    - from: "src/planning/benders.jl"
      to: "src/planning/checkpoint.jl"
      via: "checkpoint_iteration! called exactly once per Benders iteration (D-10)"
      pattern: "checkpoint_iteration!"
---

<objective>
Wire the Phase-10 `PlanningOracle` and this phase's new `FollowerLP`/`BendersMaster` (plan 11-01)
into a single hand-rolled Benders outer loop: `solve_stackelberg!` builds all three subproblems
ONCE, then iterates — solve master, re-solve oracle+follower at the master's trial `z_k`, add an
optimality or feasibility cut, checkpoint, check the UB/LB gap — until convergence or a
fail-loud iteration cap.

Purpose: this is PLAN-06's own literal scope — the single-distributor Stackelberg equilibrium
solving end-to-end. It is deliberately NOT the certification gate (plan 11-03 owns that); this
plan proves the loop CONVERGES on the Phase-11 toy fixture with a documented gap tolerance, using
the oracle sign convention derived in plan 11-01's `<sign_convention>` note and the follower's
own empirically-pinned dual sign from plan 11-01 Task 1.

Output: `src/planning/benders.jl` (`solve_stackelberg!`), `src/TSODSO.jl` wired with the sixth
`planning/` include, `test/test_planning_benders.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-single-distributor-stackelberg-benders-certified/11-CONTEXT.md
@.planning/phases/11-single-distributor-stackelberg-benders-certified/11-RESEARCH.md
@.planning/phases/11-single-distributor-stackelberg-benders-certified/11-01-PLAN.md

<interfaces>
<!-- The exact signatures this loop calls, reused verbatim from plan 11-01 / Phase 10 — read
these plans' own SUMMARYs if any signature below looks stale, but treat this block as
authoritative for what to CALL. -->

From src/planning/subproblem.jl (Phase 10, UNMODIFIED):
```julia
o = build_planning_oracle(feeder, pf, aggregators; λ₀, T)   # build ONCE, outside the loop
res = solve_planning_oracle!(o, z_trial)   # -> (; cost, π, π_s, dadp, ctx); cost is a MAX-sense
                                            # welfare value; π is ALREADY the gradient of -welfare
                                            # (Phase 10 D-06) — see 11-01-PLAN.md <sign_convention>
```

From src/planning/follower.jl (plan 11-01):
```julia
f = build_follower(; T, corridor_cap, x_inv_max, c_inv, c_op)   # build ONCE
res = solve_follower!(f, z_trial)
# feasible:   (; feasible=true,  cost, π_s)     -- cost is a genuine MIN-sense cost
# infeasible: (; feasible=false, v,    u)       -- Farkas certificate, NEVER retried
```

From src/planning/master.jl (plan 11-01):
```julia
m = build_master(; T, c_y, y_max, α_op_lb, α_x_lb)   # build ONCE
add_optimality_cut!(m, :op, cost_k, grad_k, z_k)      # oracle's cut lands on α_op
add_optimality_cut!(m, :x,  cost_k, grad_k, z_k)      # follower's cut lands on α_x
add_feasibility_cut!(m, v_k, u_k, z_k)                # follower's Farkas ray only
solve_master!(m; max_attempts=4) -> (; y, z, LB)      # routes through solve_with_retry! internally
```

From src/planning/checkpoint.jl (Phase 10, UNMODIFIED):
```julia
checkpoint_iteration!(state, iter::Int; dir::AbstractString = datadir("planning_checkpoints"))
```
`dir` is an explicit keyword so tests can pass `mktempdir()` — `solve_stackelberg!` should accept
and forward a `checkpoint_dir` keyword rather than hard-coding `datadir(...)`.

From 11-01-PLAN.md `<sign_convention>` (the oracle's cut, ALREADY DERIVED — reuse, do not
re-derive): `cost_k = -oracle_res.cost`, `grad_k = oracle_res.π` (unnegated) for the `:op`
epigraph cut.

From 11-01-PLAN.md `<toy_fixture>` (the exact numeric constants this plan's own test reuses):
`T=1`, `feeder=Phase6Fixtures.two_bus_feeder()`, `λ₀=[4.0]`,
`dev=ToyDeviceFixture.ToyElasticDevice(2, 6.0, 1.0, 10.0)`,
`agg=TSODSO.Aggregator(2, 0.9, [dev], [0.0])`; follower `corridor_cap=2.0, x_inv_max=2.0,
c_inv=1.0, c_op=[0.5]`; master `c_y=0.3, y_max=8.0, α_op_lb=-5.0, α_x_lb=0.0`; hand-enumerated
optimum `y*=1.0, z*=1.0`, total leader cost `-0.2`.

From 11-RESEARCH.md Pattern 2 (the UB/LB loop shape — structurally DIFFERENT from ADMM's
residual criterion, do not reuse `AdmmResiduals`):
```julia
for k in 1:MAX_ITER
    lb_res = solve_master!(master)                       # LB = lb_res.LB, z_k = lb_res.z
    oracle_res   = solve_planning_oracle!(oracle, lb_res.z)
    follower_res = solve_follower!(follower, lb_res.z)    # NEVER solve_with_retry!-wrapped
    if !follower_res.feasible
        add_feasibility_cut!(master, follower_res.v, follower_res.u, lb_res.z)
        checkpoint_iteration!((; k, LB = lb_res.LB, UB, gap = NaN, z_k = lb_res.z, feasible=false), k; dir=checkpoint_dir)
        continue                                          # a feasibility cut does not update UB
    end
    add_optimality_cut!(master, :op, -oracle_res.cost, oracle_res.π, lb_res.z)
    add_optimality_cut!(master, :x,  follower_res.cost, follower_res.π_s, lb_res.z)
    UB = min(UB, master.c_y * lb_res.y + follower_res.cost - oracle_res.cost)
    gap = (UB - lb_res.LB) / max(1, abs(UB))
    checkpoint_iteration!((; k, LB = lb_res.LB, UB, gap, z_k = lb_res.z, feasible=true), k; dir=checkpoint_dir)
    gap <= tol && return (; y = lb_res.y, z = lb_res.z, UB, LB = lb_res.LB, gap, iters = k, oracle, follower, master)
    k == MAX_ITER && error("solve_stackelberg! exhausted $MAX_ITER iterations without converging (gap=$gap) — refusing to silently return a non-converged result")
end
```
This sketch is illustrative of the SHAPE only — the executor must adapt exact variable names to
`master.jl`'s actual field names (`master.c_y`, `lb_res.y`, `lb_res.z`, `lb_res.LB`) from plan
11-01's implementation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: solve_stackelberg! — build-once orchestration, UB/LB gap loop, checkpointing, fail-loud maxiter</name>
  <files>src/planning/benders.jl, src/TSODSO.jl</files>
  <read_first>
    - src/admm/solve_admm.jl lines 1-60, 116-151 (the boundary-guard-then-build-once-then-loop
      shape, and the fail-loud maxiter error MESSAGE STYLE to mirror — name the last observed
      gap/knobs, cite the requirement)
    - src/planning/subproblem.jl (`solve_planning_oracle!`'s exact NamedTuple shape — read
      before calling it)
    - src/planning/follower.jl, src/planning/master.jl (created in plan 11-01 — read their
      ACTUAL exported signatures before calling them; do not assume field names beyond what
      11-01-PLAN.md's interfaces block documents)
    - src/planning/checkpoint.jl (`checkpoint_iteration!`'s `dir` keyword contract)
    - .planning/phases/11-single-distributor-stackelberg-benders-certified/11-01-PLAN.md
      `<sign_convention>` block (the oracle's cut sign — reuse verbatim, do not re-derive)
    - .planning/phases/11-single-distributor-stackelberg-benders-certified/11-RESEARCH.md
      §"Pattern 2" (UB/LB gap formula, CONTEXT-locked default tolerance `1e-6`), §"Pitfall O1"
      (why `master.jl`'s `y_max`/box-to-`[0,y_inv]` design keeps every proposed `z_k` inside the
      oracle's own feasible band — do not remove that bound)
  </read_first>
  <behavior>
    - `solve_stackelberg!(feeder, pf, aggregators, follower_params, master_params; λ₀, T,
      tol=1e-6, max_iter=100, checkpoint_dir=datadir("planning_checkpoints"))` builds the oracle,
      follower, and master EXACTLY ONCE (no `build_*`/`Model(` call inside the iteration loop)
    - On the Phase-11 toy fixture (T=1), the loop converges within `max_iter=100` with a reported
      relative gap `(UB-LB)/max(1,|UB|) <= 1e-6`
    - Every iteration calls `checkpoint_iteration!` exactly once, regardless of whether that
      iteration produced a feasibility or an optimality cut
    - `max_iter=1` on the toy fixture raises `ErrorException` naming the exhausted iteration count
      and the last observed gap — it does not return a non-converged result
  </behavior>
  <action>
    Create src/planning/benders.jl with a `# SEAM:`/`# OWNER:` header naming PLAN-06 and citing
    the 11-01 `<sign_convention>` derivation this file consumes. Define `solve_stackelberg!(
    feeder, pf::AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator}; λ₀,
    T::Int, follower_kwargs::NamedTuple, master_kwargs::NamedTuple, tol::Real = 1e-6,
    max_iter::Int = 100, checkpoint_dir::AbstractString = datadir("planning_checkpoints")) ->
    NamedTuple`. Boundary guards mirror `solve_admm`: `T >= 1`, `max_iter >= 1`, `length(λ₀) == T`,
    each `throw(ArgumentError(...))` before any build call. BUILD ONCE (outside the loop):
    `oracle = build_planning_oracle(feeder, pf, aggregators; λ₀ = λ₀, T = T)`;
    `follower = build_follower(; follower_kwargs..., T = T)`;
    `master = build_master(; master_kwargs..., T = T)`. Initialize `UB = Inf`. Loop
    `for k in 1:max_iter`: `lb_res = solve_master!(master)`; `oracle_res =
    solve_planning_oracle!(oracle, lb_res.z)`; `follower_res = solve_follower!(follower,
    lb_res.z)` (direct call — NEVER `solve_with_retry!`-wrapped, per plan 11-01's follower
    contract); if `!follower_res.feasible`: `add_feasibility_cut!(master, follower_res.v,
    follower_res.u, lb_res.z)`, call `checkpoint_iteration!` with a NamedTuple recording `k`, the
    current `LB`/`UB`, `gap = NaN`, `z_k = lb_res.z`, `feasible = false`, then `continue` (a
    feasibility cut never updates `UB`, per CONTEXT.md's locked loop mechanics); else: form the
    oracle's `:op` cut with `cost_k = -oracle_res.cost` and `grad_k = oracle_res.π` (the
    11-01-derived sign convention — do NOT negate `π`), the follower's `:x` cut with
    `cost_k = follower_res.cost` and `grad_k = follower_res.π_s` (the plan-11-01-pinned sign,
    used exactly as `solve_follower!` returns it), both via `add_optimality_cut!`; compute
    `UB = min(UB, master.c_y * lb_res.y + follower_res.cost - oracle_res.cost)` (leader
    investment cost + follower's genuine cost − oracle's welfare, per the sign-convention note);
    `gap = (UB - lb_res.LB) / max(1, abs(UB))`; call `checkpoint_iteration!` recording `k`,
    `LB = lb_res.LB`, `UB`, `gap`, `z_k = lb_res.z`, `feasible = true`; if `gap <= tol`, return
    `(; y = lb_res.y, z = lb_res.z, UB, LB = lb_res.LB, gap, iters = k, oracle, follower,
    master)`. After the loop, `error("solve_stackelberg!: exhausted $max_iter iteration(s)
    without converging (last gap=$gap, tol=$tol) — refusing to silently return a non-converged
    result")` mirroring `solve_admm`'s diagnostic-rich style. Export `solve_stackelberg!`. Append
    `include("planning/benders.jl")` as the SIXTH (final) line of the `planning/` include block
    in src/TSODSO.jl, after `master.jl`, extending the block's comment to note this is the outer
    loop consuming all five prior planning/ files.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; TestItemRunner.runtests(filter=ti->occursin("planning", ti.name) && occursin("benders", ti.name))' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/planning/benders.jl` defines `function solve_stackelberg!(feeder, pf::
      AbstractPowerFlow, aggregators::AbstractVector{<:Aggregator}; λ₀, T::Int,
      follower_kwargs::NamedTuple, master_kwargs::NamedTuple, tol::Real = 1e-6,
      max_iter::Int = 100, checkpoint_dir::AbstractString = datadir("planning_checkpoints"))`,
      exported
    - `grep -n 'build_planning_oracle\|build_follower\|build_master' src/planning/benders.jl`
      shows each call exactly ONCE, and none of them appear inside the `for k in 1:max_iter`
      loop body (structural build-once confirmation — inspect the loop's line range)
    - `grep -c 'solve_with_retry!' src/planning/benders.jl` returns 0 (this file never calls the
      retry wrapper directly — it is reached only inside `solve_planning_oracle!`/`solve_master!`)
    - `grep -c 'checkpoint_iteration!' src/planning/benders.jl` >= 1, and it appears in BOTH the
      feasibility-cut branch and the optimality-cut branch (two call sites, or one shared call
      site reached by both branches)
    - `grep -c 'include("planning/benders.jl")' src/TSODSO.jl` == 1, as the SIXTH line of the
      `planning/` block
    - `grep -in 'Bin\b\|::Integer' src/planning/benders.jl` shows no binary/integer declaration
      (this file declares no `@variable` at all — confirm no new JuMP model is built here)
  </acceptance_criteria>
  <done>solve_stackelberg! builds the oracle/follower/master exactly once, iterates the UB/LB
  Benders loop with feasibility- and optimality-cut branches, checkpoints every iteration, and
  raises loudly on iteration-cap exhaustion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end convergence test, hand-enumerated cross-check, iteration-cap regression</name>
  <files>test/test_planning_benders.jl, src/planning/benders.jl</files>
  <read_first>
    - src/planning/benders.jl (as written by Task 1 in this same plan — read the actual return
      NamedTuple shape before writing assertions against it)
    - test/test_planning_retry.jl lines 1-40, 87-94 (the `@test_throws ErrorException` +
      `occursin("exhausted", ...)` idiom for the fail-loud maxiter regression)
    - .planning/phases/11-single-distributor-stackelberg-benders-certified/11-01-PLAN.md
      `<toy_fixture>` block (exact numeric constants and the hand-enumerated optimum
      `y*=1.0, z*=1.0`, total leader cost `-0.2`)
    - test/test_planning_oracle.jl lines 144-164 (`ToyDeviceFixture.ToyElasticDevice` — reuse via
      `setup = [Phase6Fixtures, ToyDeviceFixture]`, do not redefine)
  </read_first>
  <behavior>
    - `solve_stackelberg!` on the Phase-11 toy fixture (via `mktempdir()` as `checkpoint_dir`,
      keeping the test hermetic) converges within `max_iter=100` with `result.gap <= 1e-6`
    - The converged `result.y` and `result.z[1]` are each within a documented numerical tolerance
      (e.g. `atol = 1e-3`) of the hand-enumerated `y*=1.0`, `z*=1.0`
    - `solve_stackelberg!(...; max_iter = 1)` on the same fixture throws `ErrorException` whose
      message contains `"exhausted"`
    - Every checkpoint file written during a convergent run is a canonical `iter_NNNNN.jld2` name,
      and the count of checkpoint files equals `result.iters`
  </behavior>
  <action>
    In test/test_planning_benders.jl (new file), write `@testitem`s tagged `[:planning]` (names
    contain "planning" and "benders"), `setup = [Phase6Fixtures, ToyDeviceFixture]`: (a) build the
    Phase-11 toy fixture inline exactly as documented in 11-01-PLAN.md's `<toy_fixture>` block
    (`feeder = Phase6Fixtures.two_bus_feeder()`, `dev = ToyDeviceFixture.ToyElasticDevice(2, 6.0,
    1.0, 10.0)`, `agg = TSODSO.Aggregator(2, 0.9, [dev], [0.0])`, `λ₀ = [4.0]`,
    `follower_kwargs = (; corridor_cap = 2.0, x_inv_max = 2.0, c_inv = 1.0, c_op = [0.5])`,
    `master_kwargs = (; c_y = 0.3, y_max = 8.0, α_op_lb = -5.0, α_x_lb = 0.0)`); (b) end-to-end
    convergence test — `mktempdir() do dir; result = solve_stackelberg!(feeder, LinDistFlow(),
    [agg]; λ₀ = λ₀, T = 1, follower_kwargs = follower_kwargs, master_kwargs = master_kwargs,
    tol = 1e-6, max_iter = 100, checkpoint_dir = dir); @test result.gap <= 1e-6; @test
    isapprox(result.y, 1.0; atol = 1e-3); @test isapprox(result.z[1], 1.0; atol = 1e-3);
    checkpoint_files = filter(f -> occursin(r"^iter_\d{5}\.jld2$", basename(f)), readdir(dir;
    join = true)); @test length(checkpoint_files) == result.iters; end`; (c) iteration-cap
    loud-failure regression — `mktempdir() do dir; err = nothing; try
    solve_stackelberg!(feeder, LinDistFlow(), [agg]; λ₀ = λ₀, T = 1, follower_kwargs =
    follower_kwargs, master_kwargs = master_kwargs, tol = 1e-6, max_iter = 1, checkpoint_dir =
    dir) catch e; err = e end; @test err isa ErrorException; @test occursin("exhausted",
    err.msg); end`. If the convergence test's numerical tolerance needs adjusting after an actual
    run (e.g. the loop converges to a slightly different `y*`/`z*` than the hand-derived values
    due to LP degeneracy at the toy scale), widen `atol` and document why in a comment — do NOT
    change `src/planning/benders.jl`'s cut-sign logic to force a match; if the converged values
    are qualitatively wrong (e.g. `y* < 0` or `z* < 0`, which the model's own bounds should
    already prevent), that is a genuine bug to fix in `benders.jl`, not a tolerance issue.
  </action>
  <verify>
    <automated>julia --project=. -e 'using Pkg; Pkg.test()' 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Convergence test passes: `result.gap <= 1e-6`, `result.y ≈ 1.0`, `result.z[1] ≈ 1.0`
      (documented tolerance)
    - Checkpoint-count regression passes: exactly `result.iters` canonical checkpoint files exist
      in the hermetic `mktempdir()`
    - Iteration-cap regression passes: `max_iter = 1` throws `ErrorException` containing
      `"exhausted"`
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 (full suite green, zero Phase 1-10
      regressions, `test_planning_follower.jl`/`test_planning_master.jl` from plan 11-01 still
      green)
  </acceptance_criteria>
  <done>solve_stackelberg! converges end-to-end on the Phase-11 toy fixture within the documented
  1e-6 relative gap tolerance, matches the hand-enumerated optimum within tolerance, checkpoints
  every iteration, and raises loudly on iteration-cap exhaustion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Master's candidate `z_k` -> oracle/follower re-solves | An LP-relaxation-driven candidate `z_k` could, in principle, exceed the oracle's own feasible band (Pitfall O1); the master's `y_max`/`[0,y_inv]` box (plan 11-01) is the designed mitigation — this plan must not silently remove or widen it without re-verifying oracle feasibility |
| Iteration loop -> checkpoint persistence | A crash mid-iteration must not corrupt or skip a checkpoint write; reuses Phase 10's `safesave`-backed `checkpoint_iteration!` verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | A candidate `z_k` outside the oracle's feasible band crashing the loop mid-run with no checkpoint of the failure point | mitigate | The master's own `y_max=8.0` box (plan 11-01, derived safely below the device's `Pmax=10.0`) keeps every proposed `z_k` inside the oracle's empirically-verified feasible band on this fixture; the convergence test itself is the regression proof |
| T-11-06 | Tampering | A feasibility cut silently updating `UB` (treating an infeasible trial as if it were a valid upper bound) | mitigate | The loop's `continue` on `!follower_res.feasible` skips the `UB` update entirely — an explicit acceptance criterion checks the feasibility branch never reaches the `UB = min(...)` line |
| T-11-07 | Repudiation | A non-converged run silently returned as if converged | mitigate | The fail-loud `max_iter` regression is a permanent, always-run acceptance criterion |
| T-11-SC | Tampering | npm/pip/cargo-equivalent (Julia Pkg) installs | accept | Zero new packages this plan |
</threat_model>

<verification>
- `planning`-tagged, `benders`-named `@testitem`s all GREEN
- Full suite green: `julia --project=. -e 'using Pkg; Pkg.test()'`
- No binary/integer `@variable` anywhere in `src/planning/benders.jl` (none declared at all —
  this file builds no JuMP model of its own)
</verification>

<success_criteria>
- A single-distributor Stackelberg equilibrium solves end-to-end via the Benders loop with
  upper/lower-bound gap convergence detection (PLAN-06, ROADMAP success criterion 3)
- checkpoint_iteration! fires per Benders iteration exactly as designed in Phase 10 D-10
- No planning-layer subproblem introduces a binary/integer variable
</success_criteria>

<decisions_covered>
CONTEXT.md decisions implemented by this plan:
- Convergence: relative gap `(UB-LB)/max(1,|UB|) <= tol` (default 1e-6), documented formula, plus
  an iteration cap that raises loudly on exhaustion (never silent) — Task 1 + Task 2.
- Every cut-producing solve (oracle, follower, master) goes through the correct gate:
  `solve_with_retry!`/strict `assert_solved!` for the oracle and master (inherited from their own
  build-once modules), direct `optimize!`/certificate-read for the follower — Task 1 (structural
  acceptance criteria on which files call `solve_with_retry!`).
- `checkpoint_iteration!` fires per Benders iteration exactly as designed in Phase 10 D-10 —
  Task 1 (both loop branches call it) + Task 2 (checkpoint-count regression).
</decisions_covered>

<output>
Create `.planning/phases/11-single-distributor-stackelberg-benders-certified/11-02-SUMMARY.md` when done.
</output>
