---
phase: 25-ieee-8500-scalability-benchmark
plan: 05
type: execute
wave: 4
depends_on: ["25-02", "25-04"]
files_modified:
  - src/models/exactness.jl
  - scripts/benchmark_ieee8500.jl
  - test/test_benchmark_ieee8500.jl
autonomous: true
requirements: [SCALE-04, SCALE-05]

must_haves:
  truths:
    - "A fresh, per-fixture SOCP-exactness noise floor is measured before any exactness claim is made at 8500 scale — never the IEEE-13/123 tolerances reused (anti-certificate-laundering)"
    - "The benchmark harness reports every attempted point, including timeouts and non-convergence, never silently"
    - "The harness's --quick mode is CI-affordable and produces deterministic goldens (iteration counts, model dimensions, termination status — never wall time)"
    - "The Clarabel-vs-SCS crossover is identified by measurement, with the DADP-drift diagnostic reporting each solver's own requested tolerance alongside the drift"
  artifacts:
    - path: "scripts/benchmark_ieee8500.jl"
      provides: "fixture-parametrized density-sweep harness (ieee13/ieee123/ieee8500-mv/ieee8500) with noise-floor calibration, D-18 timeout, D-19 metrics, D-20/D-21 SCS comparison, --quick mode"
    - path: "src/models/exactness.jl"
      provides: "socp_relaxation_gap(ctx) — a new, non-throwing sibling of assert_socp_exact! for noise-floor measurement only"
      exports: ["socp_relaxation_gap"]
    - path: "test/test_benchmark_ieee8500.jl"
      provides: "D-16 deterministic goldens (iter count, model dims, termination status) on the --quick point"
  key_links:
    - from: "scripts/benchmark_ieee8500.jl --calibrate-noise-floor"
      to: "src/models/exactness.jl socp_relaxation_gap"
      via: "the spike-002 tolerance-ladder re-solve method"
      pattern: "socp_relaxation_gap"
    - from: "scripts/benchmark_ieee8500.jl"
      to: "solve_admm(...; time_limit_s=...) / select_optimizer(SOCP(); time_limit=...)"
      via: "D-18's per-point timeout, reported as a budget_exceeded row, never a hang"
      pattern: "budget_exceeded"
---

<objective>
Measure the IEEE-8500-scale SOCP cone-residual noise floor (spike 002's method, never reusing the
IEEE-13/123 tolerances), then build the fixture-parametrized benchmark harness that sweeps
{ieee13, ieee123, ieee8500-mv, ieee8500} × density × {Clarabel, SCS} × {centralized, ADMM},
recording D-19's metrics honestly — including timeouts and non-convergence — with a CI-affordable
`--quick` mode backing D-16's deterministic goldens.

Purpose: SCALE-04's measured scaling numbers and SCALE-05's re-certified exactness gate both depend
on this harness existing and using a genuinely fresh tolerance, not an inherited one — this is
where the phase's central measurement happens.
Output: `src/models/exactness.jl`'s new `socp_relaxation_gap`, `scripts/benchmark_ieee8500.jl`,
`test/test_benchmark_ieee8500.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-CONTEXT.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-RESEARCH.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-VALIDATION.md
@.planning/spikes/MANIFEST.md
@src/models/exactness.jl
@src/solver/factory.jl
@scripts/socp_applicability_sweep.jl
@scripts/repro_stability_check.jl
@src/admm/solve_admm.jl
@ext/TSODSOSCSExt.jl
</context>

<interfaces>
<!-- Exact contracts this plan consumes, extracted from the codebase and prior plans. -->

From `src/models/exactness.jl` (the gap computation to extract into a non-throwing sibling —
DO NOT modify this existing function):
```julia
function assert_socp_exact!(ctx::ModelContext; rtol::Real = 1e-4, atol::Real = 1e-6)
    pv = ctx.meta[:pf_vars]; feeder = ctx.meta[:feeder]; T = ctx.meta[:T]
    maxgap = 0.0
    for (b, br) in enumerate(feeder.branches), t in 1:T
        lhs = value(pv.l[b, t]) * value(pv.v[br.from, t])
        rhs = value(pv.P[b, t])^2 + value(pv.Q[b, t])^2
        gap = abs(lhs - rhs)
        maxgap = max(maxgap, gap)
        ...
    end
    maxratio <= 1 || error("SOCP relaxation INEXACT: ...")
    return maxgap
end
```

From `src/solver/factory.jl` (the kwarg-splat seam this harness uses for BOTH the tolerance ladder
and Clarabel's native per-solve timeout — no factory change needed):
```julia
select_optimizer(::SOCP; attrs...) = optimizer_with_attributes(Clarabel.Optimizer,
    "verbose" => false, "tol_gap_abs" => 1e-8, "tol_gap_rel" => 1e-8,
    (String(k) => v for (k, v) in pairs(attrs))...)
# usage: select_optimizer(SOCP(); tol_gap_abs=1e-10, tol_gap_rel=1e-10, time_limit=120)
```

From `src/admm/solve_admm.jl` (plan 25-02's new kwarg, used here):
```julia
solve_admm(feeder, pf, aggregators; T, λ₀, ρ, time_limit_s = 120, ...)
# returns (; ..., status)  where status ∈ (:converged, :budget_exceeded)
```

From `ext/TSODSOSCSExt.jl` / `src/solver/factory.jl` (plan 25-02's new dispatch, used here):
```julia
Model(TSODSO.alternative_optimizer(TSODSO.SCSChoice(), TSODSO.SOCP()))
```

From `scripts/socp_applicability_sweep.jl` (the try/catch-per-point + CLI-dispatch shape to mirror):
```julia
function main(args)
    which = isempty(args) || startswith(first(args), "--") ? "both" : first(args)
    if which in ("both", "ieee123")
        df = sweep(:ieee123); CSV.write(joinpath(OUT, "ieee123_sweep.csv"), df); report(df, ...)
    end
end
main(ARGS)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: socp_relaxation_gap sibling + noise-floor calibration mode</name>
  <files>src/models/exactness.jl, scripts/benchmark_ieee8500.jl</files>
  <read_first>src/models/exactness.jl (full file), .planning/spikes/MANIFEST.md (spike 002's method + its 48%-false-positive lesson), .planning/phases/25-ieee-8500-scalability-benchmark/25-RESEARCH.md (Code Examples "Noise-floor calibration ladder"), scripts/repro_stability_check.jl (DrWatson output scaffold), src/solver/factory.jl</read_first>
  <action>
In `src/models/exactness.jl`, add a NEW exported function `socp_relaxation_gap(ctx::ModelContext)
-> Float64` that duplicates `assert_socp_exact!`'s gap-computation loop (the `lhs`/`rhs`/`gap`/
`maxgap` lines only) WITHOUT the throw and WITHOUT the `rtol`/`atol` classification — returns the
raw `maxgap`. Docstring: "Calibration-only: NEVER use in place of `assert_socp_exact!`'s gate — it
does not throw and cannot refuse a bad price. Exists so per-fixture noise-floor calibration
(spike-002's method) can measure the solver's own residual floor across a tolerance ladder BEFORE
choosing `assert_socp_exact!`'s `atol`/`rtol` for a new fixture (anti-certificate-laundering)." Do
NOT modify `assert_socp_exact!`'s existing body — this is a wholly new, additive function. Create
`scripts/benchmark_ieee8500.jl` with the DrWatson scaffold (`@quickactivate`, `OUT =
projectdir("results","ieee8500_benchmark")`, `mkpath(OUT)`, mirroring `repro_stability_check.jl`).
Add a `--calibrate-noise-floor --fixture <name>` CLI mode (string→symbol map: `"ieee13"=>:ieee13,
"ieee123"=>:ieee123, "ieee8500-mv"=>:ieee8500_mv, "ieee8500"=>:ieee8500`) that: (a) builds the
requested fixture + a SMALL benign population (low density, non-congested) via `build_feeder`/
`build_population`; (b) solves the centralized welfare problem across the tolerance ladder
`[1e-6,1e-7,1e-8,1e-9,1e-10]`, sweeping BOTH `tol_gap_abs` and `tol_gap_rel` together via
`select_optimizer(SOCP(); tol_gap_abs=tol, tol_gap_rel=tol)`; (c) calls `socp_relaxation_gap(ctx)`
after EACH solve (never `assert_socp_exact!`, to avoid throwing mid-calibration) and appends
`(fixture, tol, measured_gap)` rows to `results/ieee8500_benchmark/noise_floor_calibration.csv`;
(d) identifies the floor as the LAST ladder point where `measured_gap` still decreased by >1% from
the previous point, prints both the floor `tol` and its `measured_gap`. Run this mode for BOTH
`ieee8500-mv` (cheap) and `ieee8500` (headline) and commit the resulting CSV. Record the two
measured floors as `const IEEE8500_MV_EXACT_ATOL`/`IEEE8500_EXACT_ATOL` constants near the top of
`scripts/benchmark_ieee8500.jl` (leaving `rtol` at `assert_socp_exact!`'s existing default `1e-4`
unless the measured evidence argues otherwise — state the choice either way in a comment citing the
calibration run and its date), for Task 2's harness to consume.
  </action>
  <verify>
    <automated>julia --project=. scripts/benchmark_ieee8500.jl --calibrate-noise-floor --fixture ieee8500-mv</automated>
  </verify>
  <acceptance_criteria>
    - `grep "function socp_relaxation_gap" src/models/exactness.jl` matches; a diff of
      `assert_socp_exact!`'s existing lines 78-107 before/after this task is EMPTY (byte-identical
      — must_not_break).
    - The verify command exits 0 and writes `results/ieee8500_benchmark/noise_floor_calibration.csv`
      with 5 rows for `ieee8500-mv`; re-running with `--fixture ieee8500` appends 5 more rows for
      the headline fixture.
    - The comment beside `IEEE8500_MV_EXACT_ATOL`/`IEEE8500_EXACT_ATOL` contains the word
      "measured" and does NOT contain "reused from IEEE-13"/"reused from IEEE-123" (anti-
      certificate-laundering — grep negative check).
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 (exactness.jl's existing tests
      unaffected by the additive function).
  </acceptance_criteria>
  <done>socp_relaxation_gap exists as a non-throwing sibling; per-fixture noise floors are measured and committed, never inherited from IEEE-13/123.</done>
</task>

<task type="auto">
  <name>Task 2: density-sweep harness — {fixture}×{density}×{Clarabel,SCS}×{centralized,ADMM}, D-18/D-19/D-21, --quick</name>
  <files>scripts/benchmark_ieee8500.jl</files>
  <read_first>scripts/socp_applicability_sweep.jl (full try/catch/CLI/CSV pattern), src/planning/benders.jl (time_ns idiom, lines ~204-269), src/admm/solve_admm.jl (time_limit_s, plan 25-02), ext/TSODSOSCSExt.jl (plan 25-02), .planning/phases/25-ieee-8500-scalability-benchmark/25-VALIDATION.md (the exact documented --quick command)</read_first>
  <action>
Extend `scripts/benchmark_ieee8500.jl`'s main mode with CLI flags: `--fixture {ieee13, ieee123,
ieee8500-mv, ieee8500}` (via Task 1's string→symbol map), `--density &lt;comma-list&gt;` (default
`0.1,0.25,0.5,1.0` — D-01's illustrative grid, Claude's discretion, documented inline), `--solver
{clarabel,scs,both}` (default `both`), `--time-limit &lt;seconds&gt;` (D-18's per-point cap; default
120 — Claude's discretion, documented inline; pass it as BOTH `select_optimizer(SOCP();
time_limit=...)` for centralized solves AND `solve_admm(...; time_limit_s=...)` for ADMM), and
`--quick` (forces `fixture=ieee8500-mv`, `density=[minimum(default grid)]`, `solver=clarabel` only
— the exact VALIDATION.md-documented CI-affordable single point). For each non-excluded `(fixture,
density, solver)` combination: (a) build the feeder + a density-filtered population (deterministic
seeded sample of `round(Int, density*length(buses))` load buses — document the exact sampling
rule inline); (b) solve CENTRALIZED via `solve_welfare(...)` wrapped in try/catch (one bad point
never kills the run, mirroring `socp_applicability_sweep.jl`), with `time_ns()`-bracketed
assembly-vs-solve timing (D-19: bracket JuMP model build separately from `optimize!`); if
`termination_status(model) == MOI.TIME_LIMIT`, record a `budget_exceeded` row (D-18) instead of
proceeding to exactness classification; otherwise record the REAL termination status explicitly
(Clarabel `NUMERICAL_ERROR` is known standing debt, D-19) and, only on an optimal/almost-optimal
status, the exactness verdict via `socp_relaxation_gap` classified against Task 1's calibrated
`atol` for this fixture; (c) solve ADMM via `solve_admm(...; time_limit_s=<cap>)`, recording
`result.status`, `result.iters`, and peak memory (`Sys.maxrss()` sampled before/after — no
BenchmarkTools dependency, per RESEARCH's "Don't Hand-Roll"); (d) when `solver in
(:scs,:both)`, ALSO solve the SAME centralized point via `Model(TSODSO.alternative_optimizer(
TSODSO.SCSChoice(), TSODSO.SOCP()))` and record the D-21 DADP-drift diagnostic
(`norm(dadp_scs .- dadp_clarabel)`) ALONGSIDE both solvers' own requested tolerance
(`tol_gap_abs` for Clarabel, `eps_abs` for SCS — RESEARCH Pitfall 5: never imply the two numbers
are comparable). Write EVERY row (including `budget_exceeded` and caught-`ErrorException` rows)
to `results/ieee8500_benchmark/density_sweep.csv` — never silently drop a point (SCALE-04 success
criterion 4). Print a run-summary table at the end, mirroring `socp_applicability_sweep.jl`'s
printed-report convention.
  </action>
  <verify>
    <automated>julia scripts/benchmark_ieee8500.jl --fixture ieee8500-mv --quick</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0, completes well under VALIDATION.md's 120s feedback-latency
      budget, and writes ≥1 row to `results/ieee8500_benchmark/density_sweep.csv` with a non-empty
      `termination_status`.
    - A manual one-off run with a deliberately tiny `--time-limit 1` on a point known to need more
      iterations produces a `budget_exceeded` row instead of hanging or crashing — record this
      exercise in the plan's execution notes (the SUMMARY must state it was tested, even though it
      induces a deliberately slow condition unsuited to an automated CI assertion).
    - `grep -c "" results/ieee8500_benchmark/density_sweep.csv` rows all have non-empty
      `termination_status` and `admm_status` fields (no silently blank field).
    - `grep -ic "BenchmarkTools" scripts/benchmark_ieee8500.jl Project.toml` returns 0 (no new
      dependency).
  </acceptance_criteria>
  <done>Harness sweeps fixture×density×solver×{centralized,ADMM}, reports every point honestly (including timeouts/non-convergence), and --quick is CI-affordable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-16 deterministic goldens on the --quick point</name>
  <files>test/test_benchmark_ieee8500.jl</files>
  <read_first>scripts/benchmark_ieee8500.jl (Task 2's --quick output shape), .planning/phases/25-ieee-8500-scalability-benchmark/25-CONTEXT.md (D-15/D-16), .planning/phases/25-ieee-8500-scalability-benchmark/25-VALIDATION.md (Per-Task Verification Map)</read_first>
  <behavior>
    - Test 1: running `julia --project=. scripts/benchmark_ieee8500.jl --fixture ieee8500-mv
      --quick` twice in the same session produces IDENTICAL ADMM iteration count, model
      variable/constraint counts, and termination status both times (stability check BEFORE
      pinning the golden).
    - Test 2: the golden test asserts those THREE deterministic quantities exactly, and asserts
      NOTHING about wall time (D-16: wall time is recorded, never asserted).
  </behavior>
  <action>
Create `test/test_benchmark_ieee8500.jl` as a PLAIN `Test.jl` script (not `@testitem` — this test
invokes an external script as a subprocess, an integration test per VALIDATION.md's table, and
must run directly per the recorded TestItemRunner-under-`--project=.` trap) that: runs `julia
--project=. scripts/benchmark_ieee8500.jl --fixture ieee8500-mv --quick` as a subprocess, parses
the resulting `density_sweep.csv`'s quick-mode row, and asserts D-16's three deterministic
quantities against values PINNED during this task's own authoring (after confirming 2-run
stability locally, per Phase 22 D-11's measurement-before-golden convention — record the
confirmation date in a comment). Explicitly do NOT assert on wall time / elapsed seconds anywhere
in the file.
  </action>
  <verify>
    <automated>julia --project=. test/test_benchmark_ieee8500.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=. test/test_benchmark_ieee8500.jl` exits 0.
    - `grep -iE "@test.*(elapsed|wall.?time)" test/test_benchmark_ieee8500.jl` returns NO matches.
    - A comment in the file states the date and run-count of the stability check performed before
      pinning the golden.
  </acceptance_criteria>
  <done>D-16's deterministic goldens (iter count, model dims, termination status) are pinned and green, with no wall-time assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Committed harness output CSVs -> literate page (plan 25-06) | Project-authored, regenerable data consumed downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-25-11 | Repudiation | A silently dropped non-convergent or timed-out sweep point | mitigate | Every row (including `budget_exceeded`/caught-error rows) is written to the CSV — Task 2's acceptance criteria grep-check no blank status field |
| T-25-12 | Tampering | Reusing IEEE-13/123's exactness tolerance instead of a fresh measurement | mitigate | Task 1's calibration ladder + the grep-enforced "never reused" comment check |
</threat_model>

<verification>
`julia scripts/benchmark_ieee8500.jl --fixture ieee8500-mv --quick` and `julia --project=.
test/test_benchmark_ieee8500.jl` both exit 0; the noise-floor CSV and density-sweep CSV are
committed with no blank status fields.
</verification>

<success_criteria>
The harness measures a fresh per-fixture exactness noise floor, sweeps density×solver×mode
honestly (never dropping a point), and its --quick mode backs deterministic, wall-time-free
goldens.
</success_criteria>

<output>
Create `.planning/phases/25-ieee-8500-scalability-benchmark/25-05-SUMMARY.md` when done.
</output>
