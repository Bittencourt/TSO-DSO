---
phase: 25-ieee-8500-scalability-benchmark
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/data/ieee8500.jl
  - src/TSODSO.jl
  - test/test_ieee8500.jl
autonomous: true
requirements: [SCALE-01, SCALE-02]

must_haves:
  truths:
    - "A researcher can construct both the headline MV+LV and MV-only control IEEE-8500 fixtures and get a valid, constructed Feeder"
    - "The CT5 service transformer's per-unit impedance reflects the corrected 3-winding reduction (r≈3.00, x≈2.72 pu at S_base=0.5 MVA), not the superseded placeholder"
    - "The IMPEDANCE_PU_MAX and SMAX_PU_MAX tripwires are cleared honestly at S_base=0.5 MVA, with no src/units/PerUnit.jl constant touched"
    - "The fixture's real bus count is stated explicitly in its docstring, not implied by the historical '8500-node' name (IN-02)"
    - "ieee8500_mv_load_buses() has one definite, documented contract (deduplicated ascending MV bus ids) — never left ambiguous"
    - "D-06's ~9-order per-unit impedance spread is measured and printed by the test suite, never silently unreported"
    - "D-02: Two fixtures are committed — the full MV+LV headline fixture and the MV-only control — so cost and conditioning can be attributed specifically to the LV rungs"
    - "D-05: S_base is 0.5 MVA, the only base clearing both the impedance and smax tripwires under the corrected transformer reduction, with the 1 MVA non-comparability stated in the docstring"
    - "D-07: Voltage bands are per-voltage-level — MV 0.9-1.1, LV floor 0.88 taken from the source Loads.dss Vminpu — never a uniform MV band imposed at LV service points"
    - "D-08: Only the head branch carries a real thermal limit (27.5 MVA substation rating); interior branches use the SMAX_NO_LIMIT sentinel"
    - "D-09: Multi-voltage-base ingestion changes no core struct — per-level PerUnitBase selection happens once at ingestion via to_pu_impedance, never inside the reduction script"
  artifacts:
    - path: "src/data/ieee8500.jl"
      provides: "ieee8500_modified() headline fixture + ieee8500_mv_modified() MV-only control fixture"
      exports: ["ieee8500_modified", "ieee8500_mv_modified", "ieee8500_mv_relabel_map", "ieee8500_load_nodes", "ieee8500_mv_load_buses", "ieee8500_capacitor_buses"]
    - path: "test/test_ieee8500.jl"
      provides: "construction/invariant tests including the pinned corrected-transformer regression trap"
      min_lines: 60
  key_links:
    - from: "src/data/ieee8500.jl"
      to: "src/data/ieee8500_impedances.jl (plan 25-01)"
      via: "IEEE8500_MV_BRANCH_RX_OHMS / IEEE8500_XFMR_EDGES lookups converted once via to_pu_impedance"
      pattern: "to_pu_impedance"
    - from: "ieee8500_modified()"
      to: "Feeder(buses, branches, root)"
      via: "the construction-as-invariant pattern — assert_radial + assert_magnitudes run inside the constructor"
      pattern: "Feeder\\(buses, branches"
---

<objective>
Build the two committed IEEE-8500 fixtures — the headline full MV+LV feeder and the MV-only
control (D-02) — from the generated impedance table (plan 25-01), applying multi-voltage-base
per-unit conversion at `S_base = 0.5 MVA` (D-05 REVISED), per-voltage-level voltage bands (D-07),
head-only thermal limit (D-08), and the near-ideal regulator treatment (D-13). Also expose the bus-
role helper functions (load buses, capacitor buses) that plan 25-04's population/device layer
needs.

Purpose: SCALE-01's committed fixture and SCALE-02's per-unit ingestion both land here — this is
the first point at which `Feeder`'s own construction-time invariants (`assert_radial`,
`assert_magnitudes`) validate the whole 8500-scale topology for real.
Output: `src/data/ieee8500.jl`, the `src/TSODSO.jl` include-graph wiring, `test/test_ieee8500.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-CONTEXT.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-RESEARCH.md
@.planning/phases/25-ieee-8500-scalability-benchmark/25-VALIDATION.md
@src/data/ieee123.jl
@src/data/Feeder.jl
@src/data/topology.jl
@src/units/PerUnit.jl
@src/data/ieee8500_impedances.jl
@test/test_ieee123.jl
</context>

<interfaces>
<!-- Contracts this plan builds against, extracted directly from the codebase. -->

From `src/data/Feeder.jl`:
```julia
struct Bus{T<:Real}; id::Int; vmin::T; vmax::T; is_root::Bool; end
struct Branch{T<:Real}; from::Int; to::Int; r::T; x::T; smax::T; end
struct Feeder{T<:Real}
    buses::Vector{Bus{T}}; branches::Vector{Branch{T}}; root::Int
    # inner constructor runs assert_radial(...) then assert_magnitudes(feeder) — cannot be bypassed
end
Feeder(buses, branches, root) -> Feeder{T}   # outer constructor, infers T
```

From `src/units/PerUnit.jl`:
```julia
struct PerUnitBase{T<:Real}; S_base::T; V_base::T; end   # MVA, kV
to_pu_power(x_MW, b::PerUnitBase) = x_MW / b.S_base
to_pu_impedance(z_Ω, b::PerUnitBase) = z_Ω / Z_base(b)      # Z_base = V_base^2/S_base
const IMPEDANCE_PU_MAX = 5.0   # per-unit r, x expected well below this
const SMAX_PU_MAX = 100.0
const SMAX_NO_LIMIT = 99.0
const VOLTAGE_PU_MIN, VOLTAGE_PU_MAX = 0.8, 1.2
```

From `src/data/ieee123.jl` (the shape to mirror — relabel map, incidence tripwire, builder function):
```julia
function ieee123_relabel_map()  # Dict{Int,Int} non-contiguous -> 1-based; adapt to Dict{String,Int}
function _ieee123_assert_incidence(branches, N)  # sparse incidence tripwire, integer-indexed, reuse as-is post-relabel
function ieee123_modified()
    remap = ieee123_relabel_map(); N = length(remap)
    buses = [Bus(i, vmin, vmax, i==1) for i in 1:N]
    branches = Branch{Float64}[...]   # r,x via to_pu_impedance(..., IEEE123_BASE); smax head vs SMAX_NO_LIMIT
    _ieee123_assert_incidence(branches, N)
    return Feeder(buses, branches, 1)
end
```

From `src/data/ieee8500_impedances.jl` (generated by plan 25-01 — the tables this plan consumes):
`IEEE8500_MV_BRANCH_RX_OHMS::Dict{Tuple{String,String},Tuple{Float64,Float64}}`,
`IEEE8500_LV_BRANCH_RX_OHMS::Dict{Tuple{String,String},Tuple{Float64,Float64}}`,
`IEEE8500_XFMR_EDGES::Dict{Tuple{String,String},NamedTuple{(:r_pct,:x_pct,:code,:kva),Tuple{Float64,Float64,String,Float64}}}`,
`IEEE8500_REGULATOR_EDGES::Set{Tuple{String,String}}`.

Per-unit conversion for `IEEE8500_XFMR_EDGES` (D-05 REVISED — the confirmed formula, verified by
hand for the CT5 code below): `r_pu = (r_pct/100) * (S_base_MVA*1000)/kva_xfmr`, `x_pu =
(x_pct/100) * (S_base_MVA*1000)/kva_xfmr`. At `S_base=0.5 MVA`, CT5 (`r_pct=3.00, x_pct=2.72,
kva=5`): `r_pu = (3.00/100)*(500/5) = 3.00`, `x_pu = (2.72/100)*(500/5) = 2.72` — matches
CONTEXT.md's pinned D-05 REVISED values exactly.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PerUnitBase + relabel + branch/bus construction for ieee8500_modified()</name>
  <files>src/data/ieee8500.jl, src/TSODSO.jl</files>
  <read_first>src/data/ieee123.jl (full file), src/data/ieee8500_impedances.jl, src/units/PerUnit.jl, src/data/Feeder.jl, src/data/topology.jl, src/TSODSO.jl (current include block around ieee123.jl), .planning/phases/25-ieee-8500-scalability-benchmark/25-CONTEXT.md (D-05/D-07/D-08/D-09 REVISED sections), .planning/phases/25-ieee-8500-scalability-benchmark/25-RESEARCH.md (Architecture Patterns §2, MV/LV base consistency)</read_first>
  <action>
Create `src/data/ieee8500.jl`. Header docstring mirrors `ieee123.jl`'s provenance-note convention,
EXPLICITLY stating: the IN-02 caveat ("IEEE-8500-node" is the historical source-feeder name,
counting per-phase nodes — this fixture's real bus count after positive-sequence collapse is a
stated MEASURED constant, never implied by the name) and the D-05 non-comparability note ("built at
`S_base = 0.5 MVA` — NOT directly comparable to the 1 MVA IEEE-13/123 fixtures' per-unit
magnitudes; this benchmark compares timings/iteration-counts/dimensions, never cross-fixture
impedance magnitudes"). Declare `const IEEE8500_MV_BASE = PerUnitBase(0.5, 12.47)` and `const
IEEE8500_LV_BASE = PerUnitBase(0.5, 0.208)` (RESEARCH Pattern 2: 12.47/0.208 ≈ 59.95 matches the
real ~60:1 transformer turns ratio — NEVER pair `12.47` with `0.12`). `include("ieee8500_impedances.jl")`
at the top of THIS file (mirrors how `ieee123.jl` includes its own generated table internally); add
exactly ONE new line to `src/TSODSO.jl`, `include("data/ieee8500.jl")`, immediately after the
existing `include("data/ieee123.jl")` line. Build `ieee8500_relabel_map() -> Dict{String,Int}`
analogous to `ieee123_relabel_map()` (union of all MV/LV/xfmr edge endpoints from the generated
dicts; identify the substation HV-side MV bus as the root from `Transformers.dss`/`Regulators.dss`
and declare it as `const IEEE8500_ROOT_BUS = "<discovered name>"` with a comment citing where it
was found). Construct `buses::Vector{Bus{Float64}}` with PER-VOLTAGE-LEVEL bands (D-07): MV buses
get `(0.9, 1.1)`; LV buses (`X*`/`SX*` triplex/load names, or any bus reachable only through a
service transformer) get `(0.88, 1.1)` (the `Loads.dss`-declared `Vminpu=.88` floor — both bands sit
strictly inside `Feeder`'s outer `[0.8,1.2]` sanity rail). Construct `branches::Vector{Branch{Float64}}`
for THREE edge classes, converting Ω/pct → pu ONCE here via `to_pu_impedance` (never inside the
reduction script, D-09): (a) MV lines from `IEEE8500_MV_BRANCH_RX_OHMS` via `IEEE8500_MV_BASE`; (b)
LV triplex from `IEEE8500_LV_BRANCH_RX_OHMS` via `IEEE8500_LV_BASE`; (c) the 1,177 service-transformer
edges from `IEEE8500_XFMR_EDGES` via the `r_pu = (r_pct/100)*(S_base_kVA/kva)` formula in the
Interfaces block above. Apply the near-ideal treatment (D-13) to every `IEEE8500_REGULATOR_EDGES`
member, reusing the SAME numeric `IEEE123_SWITCH_R`/`IEEE123_SWITCH_X` values already used by plan
25-01's reduction script (grep them out of `src/data/ieee123.jl` for the literal value, or
reference the constant directly if exported). Assign `smax`: the head branch (leaving
`IEEE8500_ROOT_BUS`) gets `to_pu_power(27.5, IEEE8500_MV_BASE)` (D-08, expect ≈55.0 pu, the 27.5
MVA substation rating from `Transformers.dss`); every other branch gets `SMAX_NO_LIMIT`. Run the
SAME incidence-tripwire pattern as `_ieee123_assert_incidence` (integer-indexed, applied AFTER
relabeling — the check itself needs no string-keyed variant). Define `ieee8500_modified() ->
Feeder{Float64}` returning `Feeder(buses, branches, root)`. Export `ieee8500_modified`,
`IEEE8500_MV_BASE`, `IEEE8500_LV_BASE`, `ieee8500_relabel_map`.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; f = TSODSO.ieee8500_modified(); @assert length(f.branches) == length(f.buses) - 1; println(length(f.buses))'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above exits 0 (confirms `Feeder`'s construction invariants — `assert_radial`
      + `assert_magnitudes` — pass for real on the full 8500-scale topology).
    - The printed bus count is close to the documented ~4,873 estimate (report the ACTUAL measured
      value in the plan SUMMARY; do not hardcode 4873 as an assertion).
    - `julia --project=. -e 'using TSODSO; f=TSODSO.ieee8500_modified(); h=first(b for b in f.branches if b.from==f.root); println(h.smax)'`
      prints a value within 1e-6 of `55.0`.
    - `grep -c "8500-node" src/data/ieee8500.jl` and `grep -c "S_base = 0.5 MVA" src/data/ieee8500.jl`
      each return ≥1.
  </acceptance_criteria>
  <done>ieee8500_modified() constructs a valid, radial, magnitude-sane Feeder from the generated table, at S_base=0.5 MVA, with per-level voltage bands and a head-only thermal limit.</done>
</task>

<task type="auto">
  <name>Task 2: MV-only control fixture (D-02) + bus-role helper exports</name>
  <files>src/data/ieee8500.jl</files>
  <read_first>src/data/ieee8500.jl (as written by Task 1), .planning/phases/25-ieee-8500-scalability-benchmark/25-CONTEXT.md (D-02/D-12), src/data/ieee123.jl (ieee123_load_nodes)</read_first>
  <action>
Add `ieee8500_mv_relabel_map() -> Dict{String,Int}` (D-02, CLOSING the "either way" ambiguity
left open pre-revision): the MV-only bus name set is the union of every `IEEE8500_MV_BRANCH_RX_OHMS`
endpoint, every `IEEE8500_REGULATOR_EDGES` endpoint, and every `mv_bus_base` (first element) of an
`IEEE8500_XFMR_EDGES` key — sorted lexicographically for a DETERMINISTIC id assignment, with
`IEEE8500_ROOT_BUS` forced to id 1 (swap it to the front after sorting). This is a NEW, SEPARATE
1-based contiguous numbering from `ieee8500_relabel_map()` (which numbers ALL headline buses,
MV+LV) — it is NOT a "subset" of the headline ids, because `Feeder` requires ids to be contiguous
`1:N` for its own bus count, and the MV-only fixture has fewer buses than the headline. Add
`ieee8500_mv_modified() -> Feeder{Float64}` built from THIS map (filter out every LV `X*`/`SX*` bus
and every xfmr edge; keep every MV line + regulator edge). `Feeder` itself holds no load data, so do
NOT aggregate LV loads onto their MV parent here — instead expose `ieee8500_mv_load_buses() ->
Vector{Int}` with this EXACT, definite contract: the `unique`-deduplicated, ascending-sorted set of
`ieee8500_mv_relabel_map()` ids for every MV bus that is the `mv_bus_base` of AT LEAST ONE
`IEEE8500_XFMR_EDGES` entry. `unique` IS applied — multiple service transformers CAN and DO hang off
the same MV bus after phase-suffix collapse, so this count is measured, NOT hardcoded to 1177; state
the actual measured count (`≤ 1177`) in the plan SUMMARY. This function answers "which MV buses need
a load," NOT "how much load" — plan 25-04's population builder (Task 2 there) does the SX -> X ->
MV real-kW summation onto these exact bus ids, using `IEEE8500_XFMR_EDGES` + `IEEE8500_LV_BRANCH_RX_OHMS`
+ `IEEE8500_LOAD_KW` directly (never a lookup keyed by an MV bus name into a table keyed by SX names
— `IEEE8500_LOAD_KW` NEVER contains an MV bus name). This keeps `Feeder` a pure topology/impedance
struct (no core-struct change, D-09) while making "each load aggregated onto its MV node" (D-02) a
population-layer concern, symmetric with how `ieee123_load_nodes()` externalizes the load/transit
split. Add `ieee8500_load_nodes() -> Vector{Int}` (the 1,177 `SX*` LV load-bus ids, relabeled via
`ieee8500_relabel_map()`, for the HEADLINE fixture — mirrors `ieee123_load_nodes()`'s role). Add
`ieee8500_capacitor_buses() -> Vector{Int}` (the 4 relabeled ids of `R20185`, `R42247`, `R42246`,
`R18242` — D-12's promoted cap buses; valid for BOTH fixtures via each fixture's OWN relabel map,
since these are MV terminals present in both). Export all five new functions
(`ieee8500_mv_relabel_map`, `ieee8500_mv_modified`, `ieee8500_load_nodes`, `ieee8500_mv_load_buses`,
`ieee8500_capacitor_buses`).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; f=TSODSO.ieee8500_mv_modified(); @assert length(f.branches)==length(f.buses)-1; @assert length(TSODSO.ieee8500_capacitor_buses())==4; @assert length(TSODSO.ieee8500_load_nodes())==1177'</automated>
  </verify>
  <acceptance_criteria>
    - `TSODSO.ieee8500_mv_modified()` constructs without error; its bus count is close to the
      ~2,519 MV estimate (report the actual measured value).
    - `TSODSO.ieee8500_capacitor_buses()` returns exactly 4 distinct `Int`s, each a valid index
      into BOTH `ieee8500_modified().buses` and `ieee8500_mv_modified().buses`.
    - `TSODSO.ieee8500_load_nodes()` returns exactly 1177 distinct `Int`s.
    - `TSODSO.ieee8500_mv_load_buses()` returns a `unique`-deduplicated, ascending-sorted
      `Vector{Int}` with length `≤ 1177` (report the ACTUAL measured count in the plan SUMMARY — do
      NOT hardcode 1177, since multiple service transformers can share one MV bus post phase-suffix
      collapse); every returned id is a valid index into `ieee8500_mv_modified().buses`.
    - `TSODSO.ieee8500_mv_relabel_map()` returns a `Dict{String,Int}` whose values are exactly
      `1:length(ieee8500_mv_modified().buses)` (contiguous, no gaps, no duplicates) with
      `IEEE8500_ROOT_BUS` mapped to `1`.
  </acceptance_criteria>
  <done>Both fixtures construct; the five bus-role/relabel helper functions are exported, ieee8500_mv_load_buses() has one definite deduplicated contract, and each returns its documented (measured, not hardcoded where applicable) count.</done>
</task>

<task type="auto">
  <name>Task 3: test/test_ieee8500.jl construction/invariant tests + pinned transformer regression trap</name>
  <files>test/test_ieee8500.jl</files>
  <read_first>test/test_ieee123.jl (full file — the @testitem shape to mirror), src/data/ieee8500.jl (as written by Tasks 1-2), .planning/phases/25-ieee-8500-scalability-benchmark/25-VALIDATION.md (the TestItemRunner trap note + quick-run command), .planning/phases/25-ieee-8500-scalability-benchmark/25-CONTEXT.md (D-05 REVISED)</read_first>
  <action>
Create `test/test_ieee8500.jl` authored with `@testitem` blocks (tagged `tags=[:phase25]`, repo
convention) covering: (1) headline fixture radial/contiguous/single-root invariant (mirrors
`test_ieee123.jl`'s "fixture is radial, contiguous, single-root" shape, substituting
`ieee8500_modified`); (2) the SAME invariant for `ieee8500_mv_modified`; (3) per-unit magnitude
sanity for both fixtures (every branch `r,x ∈ [0, IMPEDANCE_PU_MAX)`, `smax ∈ (0, SMAX_PU_MAX)`;
head branch `smax ≈ 55.0`); (4) the PINNED corrected-transformer spot-check — locate a branch in
`ieee8500_modified()` derived from a `ct5`-coded `IEEE8500_XFMR_EDGES` entry (cross-reference by
relabeled endpoint) and assert `isapprox(br.r, 3.00; atol=1e-2)` and `isapprox(br.x, 2.72;
atol=1e-2)` — the regression trap: a future accidental revert to the placeholder
`%Rs[1]+%Rs[2]`/bare-`Xhl` formula (which would give r≈1.80,x≈2.04 at S_base=0.5MVA... actually
recompute at 0.5MVA: superseded r_pct=1.8,x_pct=2.04, at S_base=0.5MVA ratio=100 → r=1.80,x=2.04)
must make THIS assertion fail loudly; (5) `ieee8500_capacitor_buses()`/`ieee8500_load_nodes()`
return the documented counts (4/1177) and are valid bus indices; `ieee8500_mv_load_buses()` returns
a `unique`-deduplicated, ascending Vector with length `≤ 1177` (assert the bound and strict
ascending-uniqueness, never a hardcoded 1177 — plan 25-03 Task 2's definite contract); (6) D-06's
"~9-order impedance spread is a REPORTED, measured property" — compute
`measured_min_pu = minimum(x for br in ieee8500_modified().branches for x in (br.r, br.x) if x > 0)`
and the analogous `measured_max_pu`, `println` both plus `log10(measured_max_pu/measured_min_pu)`
(the spread in orders of magnitude) so it is VISIBLE in every test run's output, and `@test
log10(measured_max_pu/measured_min_pu) > 3.0` as a non-degenerate-spread sanity floor (loose on
purpose — this is a reporting requirement, not a pinned regression value; D-06 explicitly rejects
engineering the spread away, so the test only proves it is measured and printed, never that it
equals a specific number). ALSO add a standalone, non-`@testitem` block guarded by `if
abspath(PROGRAM_FILE) == @__FILE__ ... end` at the bottom of the SAME file, replicating assertions
(1)-(4) and (6) as plain `@test` calls inside a `@testset` — so `julia --project=.
test/test_ieee8500.jl` (VALIDATION.md's documented quick command) runs standalone WITHOUT
TestItemRunner, per the recorded project trap.
  </action>
  <verify>
    <automated>julia --project=. test/test_ieee8500.jl</automated>
  </verify>
  <acceptance_criteria>
    - `julia --project=. test/test_ieee8500.jl` exits 0 (direct `Test.jl` invocation, no
      TestItemRunner).
    - `grep -B2 -A2 "3.00" test/test_ieee8500.jl | grep -q "atol"` and the same for `"2.72"` —
      confirms the corrected-formula regression trap literally exists with a tight tolerance.
    - `julia --project=. -e 'using Pkg; Pkg.test()'` exits 0 (full-suite regression).
  </acceptance_criteria>
  <done>test/test_ieee8500.jl exists, runs standalone, contains a pinned assertion that fails loudly if the transformer reduction formula regresses to the superseded placeholder, and prints/asserts the measured D-06 impedance spread.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Generated `src/data/ieee8500_impedances.jl` -> `Feeder` construction | Committed generated data consumed as trusted input to a construction-time invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-25-07 | Tampering/Integrity | Generated table values silently propagating a wrong per-unit number | mitigate | The pinned CT5 spot-check test (Task 3) + `assert_magnitudes`'s existing tripwires (already-invariant, re-exercised here at scale) |
| T-25-08 | Information Disclosure | N/A — no secrets, no network/auth surface in this plan | accept | Not applicable |
</threat_model>

<verification>
`julia --project=. test/test_ieee8500.jl` exits 0; both fixtures construct without throwing;
the head branch smax equals 55.0 pu; the CT5-derived branch equals r≈3.00/x≈2.72 pu.
</verification>

<success_criteria>
Both IEEE-8500 fixtures (headline MV+LV, MV-only control) construct as valid `Feeder`s at
`S_base = 0.5 MVA`, with per-level voltage bands, head-only thermal limit, and the corrected
transformer reduction — all validated by `Feeder`'s own construction-time invariants plus a pinned
regression test.
</success_criteria>

<output>
Create `.planning/phases/25-ieee-8500-scalability-benchmark/25-03-SUMMARY.md` when done.
</output>
