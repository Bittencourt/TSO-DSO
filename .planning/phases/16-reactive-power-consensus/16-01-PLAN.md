---
phase: 16-reactive-power-consensus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_admm_reactive.jl
autonomous: true
requirements: [REACT-03]

must_haves:
  truths:
    - "A researcher can read a single, dated grep-audit confirming the bare identifier μ/mu means ONLY the adaptive-ρ residual-balancing band today, resolved BEFORE any AgrOpt/DsoOpt/Dlmp production diff lands."
    - "A distinct, documented identifier set (qag_dso for the JuMP coupling variable, mu_q if a scalar handle is ever needed, reactive for the decompose_dlmp field) exists so no future code binds a new meaning to bare μ/mu."
    - "src/experiments/Scenario.jl is untouched by this plan (and every later Phase-16 plan) — the DrWatson golden-hash savename schema is not perturbed."
  artifacts:
    - path: "test/test_admm_reactive.jl"
      provides: "RED @testitem harness pinning the REACT-01/02/03 contract (reactive_consensus kwarg, qag_dso coupling variable, :balance_q certificate) plus the naming-audit header, mirroring test_admm_adaptive.jl's RED-guard convention"
      min_lines: 60
  key_links:
    - from: "test/test_admm_reactive.jl"
      to: "src/admm/DsoOpt.jl / src/admm/solve_admm.jl (Plan 16-02, not yet landed)"
      via: "hasmethod-based RED guard on the reactive_consensus keyword argument"
      pattern: "hasmethod\\(build_dso_opt"
---

<objective>
Resolve the phase's first, BLOCKING design decision — a distinct, collision-free identifier
for the new reactive-power dual/coupling-variable — and scaffold the RED @testitem harness
(`test/test_admm_reactive.jl`) that pins the REACT-01/02/03 contract before any production code
in `AgrOpt.jl`/`DsoOpt.jl`/`solve_admm.jl`/`dlmp.jl` is touched.

Purpose: Per ROADMAP.md Phase 16 Success Criterion #1 and REACT-03, the `μ`/`mu` naming
collision (new reactive dual vs. the existing adaptive-ρ band `μ::Real = 10.0` threaded through
`solve_admm.jl` → `Scenario.jl` → `run.jl` → `fixtures_phase7.jl`) MUST be resolved and documented
BEFORE any `AgrOpt`/`DsoOpt`/`Dlmp` diff lands. This plan is deliberately test-only (no
production-code diff) so it can be the unambiguous, literal "first" wave.
Output: `test/test_admm_reactive.jl` — a NEW file containing (a) a header comment documenting
the re-confirmed grep-audit and the chosen distinct identifiers, and (b) RED-guarded
`@testitem`s for the REACT-01 (qag_dso coupling), REACT-02 (certified :balance_q dual), and
REACT-03 (byte-identical default) contracts that Plans 16-02/16-03 will turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-reactive-power-consensus/16-RESEARCH.md
@.planning/phases/16-reactive-power-consensus/16-PATTERNS.md

<interfaces>
<!-- The RED harness targets a kwarg that does NOT exist yet — it must detect its ABSENCE
     without crashing the test runner, mirroring the project's isdefined-guard convention but
     adapted for a keyword-argument addition (kwargs are not part of `isdefined`/dispatch). -->

From `src/admm/DsoOpt.jl:127` (current signature, kwarg absent until Plan 16-02):
    function build_dso_opt(feeder, aggregators, T::Int; ρ::Real, λ₀)

From `src/admm/solve_admm.jl:116-132` (current signature, kwarg absent until Plan 16-02):
    function solve_admm(feeder, pf::ConvexBranchFlow, aggregators::AbstractVector{<:Aggregator};
        T::Int = 24, λ₀, ρ::Real, maxiter::Int = 200, tol::Real = 1e-5,
        ε_abs::Real = 1e-4, ε_rel::Real = 1e-3, τ::Real = 2.0, μ::Real = 10.0,
        ρ_min::Real = 1e-2, ρ_max::Real = 1e4, allow_export::Bool = true)

From `src/core/status.jl:79-94` (reused verbatim by the Plan 16-02/16-03 certificate items):
    function assert_no_slack(model::Model, cref; atol::Real = 1e-6)

Julia's 3-arg `hasmethod(f, types::Type{<:Tuple}, kwnames::Tuple{Vararg{Symbol}}=())` form is
the mechanism to detect a keyword argument's existence WITHOUT calling the function (so a RED
item never actually invokes a not-yet-existing kwarg path). Verify the exact call shape against
`?hasmethod` in the REPL if the literal form below errors — the INTENT (detect
`reactive_consensus` kwarg support before calling it) is what must be preserved, not the exact
syntax.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm the μ/mu grep-audit and pick the distinct reactive identifiers (BLOCKING)</name>
  <files>test/test_admm_reactive.jl</files>
  <read_first>
    16-RESEARCH.md § "The μ Naming Collision — Full Grep Audit" (the full same-day audit table);
    16-PATTERNS.md § "CRITICAL naming constraint" (lines 8-27, the enforcement summary);
    src/admm/solve_admm.jl:58,128 (the ONLY live `μ::Real = 10.0` kwarg binding);
    src/admm/AgrOpt.jl:47-51 (the `qag` docstring that primed the naming-collision risk);
    src/admm/DsoOpt.jl:33-34,212-213 (the DsoOpt docstring/comment naming the SAME risk).
  </read_first>
  <action>
    Re-run the grep audit live against the CURRENT tree (not just cite 16-RESEARCH.md's
    same-day snapshot — confirm nothing shifted): `grep -rln "\bμ\b" src/ test/` and
    `grep -rn "\bmu\b" src/ test/` (case-sensitive ASCII spelling, e.g. `fixtures_phase7.jl`'s
    `const MU`). Confirm the result set is EXACTLY: `src/admm/solve_admm.jl` (kwarg + docstring),
    `src/experiments/Scenario.jl` (struct field, DO NOT TOUCH), `src/experiments/run.jl`
    (thread-through), `test/fixtures_phase7.jl` (`const MU`), plus their consumer test files —
    and that every one of them means the SAME thing: the Boyd §3.4.1 adaptive-ρ
    residual-balancing imbalance band. If the grep turns up anything NEW since the research
    pass (i.e. a second, different meaning already bound to `μ`/`mu`), STOP and escalate rather
    than silently proceeding.
    Write this confirmed audit, plus the chosen distinct identifiers, as the header comment
    block of the NEW `test/test_admm_reactive.jl` (mirroring the project convention of a
    file-header seam/contract comment, e.g. `test/test_admm_adaptive.jl`'s own header): name
    the JuMP coupling variable `qag_dso` (no Greek letter — it is a variable, not a dual), the
    `decompose_dlmp` NamedTuple field `reactive`, and reserve `mu_q` ONLY if a future task needs
    a scalar/vector code handle for the extracted price — explicitly state "never bare μ or mu"
    for anything reactive-related. State explicitly, in the same header, that
    `src/experiments/Scenario.jl` is OUT OF SCOPE for the entire phase (per REACT-03 / RESEARCH
    Pitfall 3 — avoids a second, unrelated `savename` golden-hash perturbation) and that no task
    in this or later Phase-16 plans may add it to `files_modified`.
  </action>
  <acceptance_criteria>
    The header of `test/test_admm_reactive.jl` contains: (1) the literal re-run grep command(s)
    and their result file list; (2) an explicit statement that today's SOLE meaning of `μ`/`mu`
    is the adaptive-ρ band; (3) the three chosen identifiers (`qag_dso`, `reactive`, `mu_q`) with
    "never bare μ/mu" stated; (4) an explicit "`src/experiments/Scenario.jl` is NOT modified this
    phase" statement. Verify via `grep -c "qag_dso" test/test_admm_reactive.jl` >= 1 and
    `grep -c "Scenario.jl" test/test_admm_reactive.jl` >= 1 (both filtered with
    `grep -v '^#$'` is unnecessary here since these are substantive prose lines, not blank
    comment headers — confirm by inspection that the matches are in real sentences, not
    boilerplate).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -q "qag_dso" test/test_admm_reactive.jl && grep -q "Scenario.jl" test/test_admm_reactive.jl && echo OK'</automated>
  </verify>
  <done>Header comment documents the re-confirmed grep audit, the chosen identifiers (qag_dso / reactive / mu_q, never bare mu), and explicitly states Scenario.jl is out of scope for the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Author the RED @testitem harness pinning REACT-01/02/03</name>
  <files>test/test_admm_reactive.jl</files>
  <read_first>
    test/test_admm_adaptive.jl (full file — the exact RED-guard → behavioral-assert shape to
    mirror, including `setup = [Phase7Fixtures, Phase6Fixtures]` and item-name substring
    convention);
    test/test_dso.jl:18-56 (the existing 2-bus `build_dso_opt` shape assertions to extend);
    src/admm/DsoOpt.jl:206-216 (the `pag_dso`/`:balance_p` active block being mirrored, and the
    `q_draw`/`:balance_q` constant block being replaced conditionally);
    src/admm/solve_admm.jl:412-416 (the existing `:balance_p` `assert_no_slack` certificate
    block the new `:balance_q` certificate item must mirror);
    16-VALIDATION.md § "Wave 0 Requirements" (the exact file/behavior this task must scaffold).
  </read_first>
  <action>
    Append to `test/test_admm_reactive.jl` (below Task 1's header) three `@testitem`s, every
    item name containing the substring "reactive" (so
    `@run_package_tests filter=ti->occursin("reactive", ti.name)` selects them, per
    16-VALIDATION.md's quick-run command):
    (1) A RED item asserting `build_dso_opt` and `solve_admm` do NOT YET accept a
    `reactive_consensus` keyword — using the `hasmethod(...,  (:reactive_consensus,))` 3-arg
    form described in this plan's `<interfaces>` block (adjust exact syntax if it errors; the
    intent is a non-crashing "kwarg absent" probe) — guarded so the REST of the item's
    behavioral asserts (qag_dso coupling variable exists, has shape `(length(load_nodes), T)`,
    is stashed at `ctx.meta[:qag_dso]`) sit BEHIND that guard exactly like
    `test_admm_adaptive.jl`'s `isdefined(TSODSO, :set_rho!)` gate — RED until Plan 16-02 lands.
    (2) A POSITIVE, NOT-RED item asserting the `reactive_consensus` OMITTED (i.e. default) call
    to `build_dso_opt`/`solve_admm` on the 2-bus fixture (`Phase6Fixtures.two_bus_feeder`/
    `build_two_bus_aggregators`) produces TODAY's exact shape (`dso.load_nodes == [2]`,
    `haskey(dso.ctx.constraints, :balance_q)`, no `:qag_dso` key in `ctx.meta`) — this item must
    PASS right now, before Plan 16-02, and must keep passing unchanged afterward (the
    byte-identical-default regression net, REACT-03).
    (3) A RED item, gated the same way as (1), that after a converged
    `solve_admm(...; reactive_consensus = true)` on the 2-bus fixture, calling
    `assert_no_slack` on every entry of `dso_ctx.constraints[:balance_q]` does NOT throw (the
    positive-path certificate proof for REACT-02, mirroring the existing
    `test/test_admm.jl` "final published primal certified" item's `:balance_p` re-check
    pattern) — RED until Plan 16-02's certificate lands.
  </action>
  <acceptance_criteria>
    `julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("reactive", ti.name)'`
    runs to completion with NO runner crash: item (2) reports PASS now; items (1) and (3) report
    a benign "kwarg not yet supported" RED condition (not an uncaught `MethodError`/exception
    escaping the test). `grep -c "reactive" test/test_admm_reactive.jl` (filtered with
    `grep -v '^#'` to exclude header prose) shows every `@testitem` name contains "reactive".
    File is >= 60 lines (per `must_haves.artifacts`).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("reactive", ti.name)'</automated>
  </verify>
  <done>test/test_admm_reactive.jl exists with 3 @testitems (2 RED-guarded on the reactive_consensus kwarg, 1 positive default-path regression), every item name containing "reactive"; the runner completes without a crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (local, offline, single-researcher numerical codebase) | This plan is test-authoring only — no network, auth, or untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `μ`/`mu` identifier reuse | mitigate | This plan's Task 1 is the mitigation itself — a documented, re-confirmed grep-audit blocks any future diff from silently rebinding `μ`/`mu` to a second meaning. |
| T-16-02 | Tampering | `src/experiments/Scenario.jl` golden-hash `savename` | mitigate | Explicitly excluded from `files_modified` in every Phase-16 plan; Task 1's header states this out-of-scope boundary in writing. |

</threat_model>

<verification>
`julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("reactive", ti.name)'`
must run without a runner crash. Full suite `julia --project -e 'using Pkg; Pkg.test()'` must
remain green (this plan touches no production file, so this is a pure sanity check).
</verification>

<success_criteria>
- `test/test_admm_reactive.jl` exists, >= 60 lines, with the naming-audit header preceding every
  `@testitem`.
- The naming decision (`qag_dso` / `reactive` / `mu_q`, never bare `μ`/`mu`) is documented and
  will be referenced (not re-derived) by Plans 16-02/16-03.
- No production file (`src/**`) is modified by this plan.
- `src/experiments/Scenario.jl` does not appear in this plan's `files_modified`.
</success_criteria>

<output>
Create `.planning/phases/16-reactive-power-consensus/16-01-SUMMARY.md` when done
</output>
