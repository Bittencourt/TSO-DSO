---
phase: 16-reactive-power-consensus
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/pricing/dlmp.jl
  - test/test_pricing_dlmp.jl
autonomous: true
requirements: [REACT-02]

must_haves:
  truths:
    - "A researcher can call extract_reactive_dlmp(ctx) or decompose_dlmp(ctx).reactive and receive the dual of the certified :balance_q constraint — a documented, citable 5th DLMP component, distinct from (never summed into) the existing 4-way active total."
    - "The reactive price at the frontier root is degenerate (≈0, the free-sign zero-objective-coefficient q_import) while the reactive price at a load bus with a non-zero power factor is a finite, non-degenerate, reproducible number, confirmed by a finite-difference sanity check against the SAME 2-bus fixture used elsewhere in the repo (test_ac_oracle.jl's r=0.01/x=0.02 shape)."
  artifacts:
    - path: "src/pricing/dlmp.jl"
      provides: "extract_reactive_dlmp(ctx; bus, T) mirroring extract_dlmp's shape and _assert_priceable gate; decompose_dlmp gains a new `reactive` NamedTuple field"
      contains: "extract_reactive_dlmp"
    - path: "test/test_pricing_dlmp.jl"
      provides: "reactive field finite/degeneracy assertions + a hand-computed/finite-difference 2-bus reactive-price sanity pin"
  key_links:
    - from: "src/pricing/dlmp.jl extract_reactive_dlmp"
      to: "ctx.constraints[:balance_q]"
      via: "dual.(ctx.constraints[:balance_q])"
      pattern: "dual\\(bq\\[j, t\\]\\)"
    - from: "src/pricing/dlmp.jl decompose_dlmp"
      to: "extract_reactive_dlmp"
      via: "reactive = extract_reactive_dlmp(ctx)"
      pattern: "reactive = extract_reactive_dlmp"
---

<objective>
Add the citable reactive nodal price: a new `extract_reactive_dlmp` function mirroring
`extract_dlmp`'s shape exactly (same `_assert_priceable` gate, same matrix/vector return
convention), and a new `reactive` field on `decompose_dlmp`'s returned NamedTuple — reading
`dual(:balance_q)` off the NOW-CERTIFIED (Plan 16-02) reactive balance.

Purpose: REACT-02 requires a reactive nodal price be "extracted and appears as a documented,
citable 5th component in the DLMP decomposition." This is a pure post-processing addition —
`pricing/dlmp.jl` never builds or solves a model, it only reads duals — and does NOT touch the
existing 4-way active decomposition or its hard sum-to-nodal-price assertion (the `reactive`
field is a SEPARATE price signal, never folded into `total`).
Output: `extract_reactive_dlmp` exported; `decompose_dlmp(ctx)` returns
`(; energy, loss, congestion, voltage, reactive, total)`; a hand-verified 2-bus reactive-price
sanity pin in `test/test_pricing_dlmp.jl`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-reactive-power-consensus/16-RESEARCH.md
@.planning/phases/16-reactive-power-consensus/16-PATTERNS.md
@.planning/phases/16-reactive-power-consensus/16-02-SUMMARY.md

<interfaces>
<!-- The EXACT function this plan's new extract_reactive_dlmp mirrors, verbatim. -->

From `src/pricing/dlmp.jl:56-77` (`_assert_priceable` — reused, called FIRST in every new
function exactly as the existing two already do):
    function _assert_priceable(ctx::ModelContext)
        haskey(ctx.constraints, :balance_p) || throw(ArgumentError(...))
        if haskey(ctx.meta, :pf_vars) && haskey(ctx.meta[:pf_vars], :l) &&
           !haskey(ctx.meta, :socp_maxgap)
            throw(ArgumentError(...))
        end
        return nothing
    end

From `src/pricing/dlmp.jl:96-104` (`extract_dlmp` — the literal shape to copy for
`:balance_q`):
    function extract_dlmp(ctx::ModelContext; bus = nothing, T = nothing)
        _assert_priceable(ctx)
        bp = ctx.constraints[:balance_p]
        N, Tfull = size(bp)
        M = Float64[dual(bp[j, t]) for j in 1:N, t in 1:Tfull]
        bus === nothing && return M
        Tsel = T === nothing ? Tfull : Int(T)
        return M[bus, 1:Tsel]
    end

From `src/pricing/dlmp.jl:250-260` (`decompose_dlmp`'s return — the two branches to extend
additively with `reactive`):
    bus === nothing && return (; energy, loss, congestion, voltage, total)
    ...
    return (; energy = energy[bus, rows], loss = loss[bus, rows],
              congestion = congestion[bus, rows], voltage = voltage[bus, rows],
              total = total[bus, rows])

From `src/devices/Aggregator.jl:28` (the reactive-draw formula every hand-derived/finite-
difference check must use consistently):
    reactive_factor(φ::Real) = sqrt(1 - φ^2) / φ

From `test/test_ac_oracle.jl:21-24` (the lossy 2-bus fixture shape this plan's new pin item
reuses — r=0.01, x=0.02, NOT the near-lossless r=x=1e-6 fixture already used elsewhere in
`test_pricing_dlmp.jl`, because a genuinely lossy branch is what makes the load-bus reactive
price analytically non-trivial rather than ≈0):
    [Bus(1, 0.95, 1.05, true), Bus(2, 0.95, 1.05, false)],
    [Branch(1, 2, 0.01, 0.02, 10.0)],
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extract_reactive_dlmp and decompose_dlmp's reactive field</name>
  <files>src/pricing/dlmp.jl</files>
  <read_first>
    src/pricing/dlmp.jl (full file, already read this session — `_assert_priceable` at
    lines 56-77, `extract_dlmp` at 96-104, `decompose_dlmp`'s guard-list at 173-181 and return
    at 250-260 are the four edit/read points);
    16-PATTERNS.md § "src/pricing/dlmp.jl (MODIFY decompose_dlmp) — add reactive field" (the
    exact target function body, reproduced verbatim in that section).
  </read_first>
  <behavior>
    - `extract_reactive_dlmp(ctx)` on a solved `ConvexBranchFlow` ctx (which per
      `src/admm/DsoOpt.jl`'s header ALWAYS carries `:balance_q`, since `ConvexBranchFlow` sets
      `reactive = true` unconditionally) returns the `(N, T)` matrix `dual.(ctx.constraints[:balance_q])`,
      gated by `_assert_priceable(ctx)` exactly like `extract_dlmp`.
    - `extract_reactive_dlmp(ctx)` on a ctx with no `:balance_q` (e.g. hand-built for a
      DC-only test) throws `ArgumentError`, never a `KeyError`.
    - `decompose_dlmp(ctx)` returns a NamedTuple with a NEW `reactive` field
      (`propertynames(decompose_dlmp(ctx))` includes `:reactive`); `reactive` is NEVER folded
      into `total`, and the EXISTING hard 4-term sum-to-nodal-price assertion
      (`energy+loss+congestion+voltage ≈ total`) is UNCHANGED (still exactly 4 terms).
  </behavior>
  <action>
    Add `extract_reactive_dlmp(ctx::ModelContext; bus = nothing, T = nothing)` immediately
    after `extract_dlmp`, mirroring its body exactly but reading
    `ctx.constraints[:balance_q]` instead of `:balance_p`, with an added presence guard
    (`haskey(ctx.constraints, :balance_q) || throw(ArgumentError(...))`) since — unlike
    `:balance_p`, which every priceable ctx always has — a hand-built or future DC-only ctx may
    lack `:balance_q`. Gate on `_assert_priceable(ctx)` first, exactly like `extract_dlmp`. In
    `decompose_dlmp`, add `reactive = extract_reactive_dlmp(ctx)` (full matrix branch) /
    `reactive = extract_reactive_dlmp(ctx)[bus, rows]` (per-bus branch) as a 6th field on BOTH
    return statements — do NOT touch the existing `energy`/`loss`/`congestion`/`voltage`/`total`
    computation or the hard sum-to-price assertion block (lines 225-249), which stays a 4-term
    check. Because `decompose_dlmp` already REQUIRES the `:cone`/`:vdrop`/`:cpydrop`/`:smax`
    handles (ConvexBranchFlow-only), any ctx that reaches the `reactive` field is by construction
    a `ConvexBranchFlow` solve and therefore always carries `:balance_q` — no extra guard branch
    is needed inside `decompose_dlmp` itself (the guard lives in the standalone
    `extract_reactive_dlmp` for direct/DC-only callers). Update the file header's doc comment
    (lines 17-23, the "Two public functions" list) and `decompose_dlmp`'s docstring to document
    the new field, and add `extract_reactive_dlmp` to the file's `export` statement.
  </action>
  <acceptance_criteria>
    `julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("dlmp", ti.name)'`
    is fully green, including ALL pre-existing `test_pricing_dlmp.jl` items UNCHANGED (the
    4-term sum-to-price assertion must still pass with exactly 4 terms — confirm by inspection
    that no test or production line changed `energy[j,t] + loss[j,t] + congestion[j,t] +
    voltage[j,t]` to a 5-term expression). `grep -c "extract_reactive_dlmp" src/pricing/dlmp.jl`
    >= 3 (definition + `decompose_dlmp` call site ×2 + export).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("dlmp", ti.name)'</automated>
  </verify>
  <done>extract_reactive_dlmp is exported and mirrors extract_dlmp's shape/gate; decompose_dlmp returns a reactive field that is NEVER summed into total; the existing 4-term sum-to-price assertion is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-verified 2-bus reactive-price sanity pin + degeneracy/finite checks</name>
  <files>test/test_pricing_dlmp.jl</files>
  <read_first>
    test/test_pricing_dlmp.jl:20-57 (the file's OWN existing 2-bus sign-pin item — the shape to
    mirror for battery/aggregator construction, `solve_welfare` call, and assertion style);
    test/test_ac_oracle.jl:21-24,87-88,126-127 (the lossy `r=0.01, x=0.02` 2-bus fixture,
    reused here instead of the near-lossless `r=x=1e-6` fixture already used by the FIRST
    `test_pricing_dlmp.jl` item, because a genuinely lossy branch is what makes the load-bus
    reactive price analytically non-trivial);
    src/devices/Aggregator.jl:28 (`reactive_factor`, the exact formula the finite-difference
    check must use to compute the reactive-demand perturbation Δq consistently with the
    production code path);
    16-RESEARCH.md § "Hand-computed 2-bus reactive-price sanity pin" and § "Free slack,
    precisely located" (why the root's price is degenerate ≈0 and the load bus's is not).
  </read_first>
  <action>
    Add a NEW `@testitem` (name containing "dlmp" and "reactive") building the lossy 2-bus
    fixture (`Branch(1, 2, 0.01, 0.02, 10.0)`, mirroring `test_ac_oracle.jl`) with a
    non-degenerate aggregator power factor (φ = 0.9, so `tanφ = reactive_factor(0.9) > 0`) and a
    small flat `Pdc`. Solve `solve_welfare(...; allow_export = true)` and compute
    `decompose_dlmp(ctx)`. Assert: (a) `d.reactive[1, t] ≈ 0` within `atol = 1e-6` at the ROOT
    (the free-sign, zero-objective-coefficient `q_import`'s KKT stationarity forces its dual to
    exactly zero — the degenerate case per 16-RESEARCH.md's "Free slack, precisely located");
    (b) `isfinite(d.reactive[2, t])` at the load bus. Then run the finite-difference sanity
    pin: re-solve the SAME fixture with the aggregator's `φ` perturbed by a small `δ` (e.g.
    `1e-4`), compute the two reactive-demand values `q0 = -Pdc·reactive_factor(0.9)` and
    `q1 = -Pdc·reactive_factor(0.9 + δ)` using the SAME formula the production code uses, and
    assert the welfare objective's change between the two solves matches
    `d.reactive[2, t] * (q1 - q0)` to first order (a documented `atol`/`rtol` band — this is the
    "hand-computed... sanity pin" discipline from 16-RESEARCH.md Pitfall 6/7, using a
    finite-difference economic-consistency check rather than an undecomposed closed-form KKT
    derivation, consistent with Assumption A1's minimal one-shot-price scope — REACT-02 asks for
    a single reactive price, not a further loss/congestion/voltage-style decomposition of it).
    Also add a parallel (non-summed) finite-check block mirroring the file's existing pattern at
    lines 148-161: `for f in (d.energy, d.loss, d.congestion, d.voltage, d.reactive); @test
    all(isfinite, f); end` on the IEEE-13 ground fixture item (extend the EXISTING
    "decompose_dlmp four components SUM" item's finite-check loop to include `d.reactive`
    WITHOUT adding it to the sum-to-price assertion, which stays 4-term).
  </action>
  <acceptance_criteria>
    `julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("dlmp", ti.name)'`
    is fully green including the new reactive-price item. `grep -c "d.reactive\|reactive\[" test/test_pricing_dlmp.jl`
    >= 3. Full suite `julia --project -e 'using Pkg; Pkg.test()'` remains green (wave-merge
    regression gate).
  </acceptance_criteria>
  <verify>
    <automated>julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("dlmp", ti.name)'</automated>
  </verify>
  <done>A 2-bus reactive-price pin is committed and green: the root price is degenerate (≈0), the load-bus price is finite, and the finite-difference economic-consistency check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (local, offline, single-researcher numerical codebase) | No network/auth/untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Repudiation | Publishing a reactive price without the Plan 16-02 certificate | mitigate | `extract_reactive_dlmp`/`decompose_dlmp` inherit `_assert_priceable`, and the reactive value is only ever read from a `reactive_consensus=true` solve certified by Plan 16-02's `assert_no_slack` — no new certification logic is invented here (single choke-point reuse per 16-RESEARCH.md's "Don't Hand-Roll" table). |
| T-16-07 | Tampering | Silently folding `reactive` into `total` | mitigate | Task 1's acceptance criteria explicitly requires the existing 4-term sum-to-price assertion stay unchanged; `reactive` is a documented SEPARATE price signal. |

</threat_model>

<verification>
`julia --project -e 'using TestItemRunner, TSODSO; @run_package_tests filter=ti->occursin("dlmp", ti.name)'`
must be fully green. Full suite `julia --project -e 'using Pkg; Pkg.test()'` must remain green.
</verification>

<success_criteria>
- `extract_reactive_dlmp` exported and gated by `_assert_priceable`, mirroring `extract_dlmp`.
- `decompose_dlmp(ctx)` returns a `reactive` field, never summed into `total`; existing 4-term
  sum-to-price assertion unchanged.
- A 2-bus reactive-price sanity pin (root degenerate ≈0, load bus finite + finite-difference
  economic-consistency check) is committed and green.
</success_criteria>

<output>
Create `.planning/phases/16-reactive-power-consensus/16-03-SUMMARY.md` when done
</output>
