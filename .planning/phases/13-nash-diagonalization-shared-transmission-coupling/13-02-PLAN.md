---
phase: 13-nash-diagonalization-shared-transmission-coupling
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/planning/benders.jl
  - src/planning/nash.jl
  - src/diagnostics/plots.jl
  - ext/TSODSOMakieExt.jl
  - src/TSODSO.jl
  - test/test_planning_nash.jl
autonomous: true
requirements: [NASH-02, NASH-03]

must_haves:
  truths:
    - "Gauss-Seidel diagonalization across N=2 distributors converges to a fixed point, treating each distributor's Benders solve as an atomic best-response (full solve_stackelberg! convergence, never a partial pass)."
    - "The inner Benders tolerance is strictly tighter than the outer Nash tolerance, enforced by a code-level ArgumentError, not merely documented."
    - "Each atomic best-response starts from a fresh Benders cut store (no cross-best-response cut reuse) — structurally guaranteed because solve_stackelberg! is invoked fresh every time, never given a persisted master."
    - "write_back! fires immediately after each distributor's best-response, before the next distributor in the sweep reads z_{-i} — genuine Gauss-Seidel, never accidental Jacobi timing."
    - "Two-level convergence diagnostics (inner Benders gap + outer Nash residual) are recorded together in one ledger (NashTrace) and are plottable via a CairoMakie extension method."
  artifacts:
    - path: "src/planning/nash.jl"
      provides: "NashTrace, run_nash!"
      min_lines: 150
    - path: "src/planning/benders.jl"
      provides: "solve_stackelberg!'s additive, defaulted `follower` keyword (non-breaking)"
    - path: "src/diagnostics/plots.jl"
      provides: "plot_nash_convergence generic stub + export"
    - path: "ext/TSODSOMakieExt.jl"
      provides: "TSODSO.plot_nash_convergence(::NashTrace) CairoMakie method"
    - path: "test/test_planning_nash.jl"
      provides: "NashTrace unit tests, N=2 convergence + capacity-dual regression, nested-tolerance guard, Gauss-Seidel-vs-Jacobi timing regression, plot smoke test"
      min_lines: 150
  key_links:
    - from: "src/planning/nash.jl"
      to: "src/planning/benders.jl"
      via: "solve_stackelberg!(...; follower = DistributorView(shared, i)) — fresh call per best-response"
      pattern: "follower = DistributorView"
    - from: "src/planning/nash.jl"
      to: "src/planning/coupling.jl"
      via: "activate_distributor!/write_back! bracket each solve_stackelberg! call"
      pattern: "write_back!\\(shared"
    - from: "ext/TSODSOMakieExt.jl"
      to: "src/planning/nash.jl"
      via: "TSODSO.plot_nash_convergence(trace::TSODSO.NashTrace)"
      pattern: "plot_nash_convergence"
---

<objective>
Build `src/planning/nash.jl`'s core (NASH-02/03): the outer Gauss-Seidel diagonalization loop
`run_nash!` over N distributors' already-hardened Benders best-responses
(`solve_stackelberg!`, Phases 11-12, UNCHANGED call sites), each best-response fed a
per-distributor view of plan 13-01's `SharedTransmission`. Add the ONE additive, non-breaking
keyword `solve_stackelberg!` needs to accept that view. Instrument the loop with a purpose-built
two-level ledger (`NashTrace`, mirroring `BendersTrace`'s established shape) and a CairoMakie
plot method satisfying "plottable" (criterion 3).

Purpose: this is the phase's own novel outer-loop orchestration layer — everything it calls
(`solve_stackelberg!`, `SharedTransmission`'s lifecycle functions) is already validated by
Phases 11/12 and plan 13-01; this plan's job is ONLY the correct sequencing (Gauss-Seidel, never
Jacobi), the nested-tolerance guard, and the two-level diagnostics.

Output: `run_nash!`, `NashTrace`, `solve_stackelberg!`'s new `follower` keyword, and a
plottable two-level convergence figure — proven convergent on a hand-checked, genuinely
congested N=2 fixture (extends plan 13-01's own toy fixture).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-nash-diagonalization-shared-transmission-coupling/13-CONTEXT.md
@.planning/phases/13-nash-diagonalization-shared-transmission-coupling/13-RESEARCH.md
@.planning/phases/13-nash-diagonalization-shared-transmission-coupling/13-PATTERNS.md
@.planning/phases/13-nash-diagonalization-shared-transmission-coupling/13-01-SUMMARY.md

<interfaces>
<!-- src/planning/benders.jl (this repo, Phase 11/12) — solve_stackelberg!'s CURRENT contract,
     which this plan extends additively. Read in full; do not re-derive from memory. -->

solve_stackelberg!(feeder, pf, aggregators; λ₀, T, follower_kwargs::NamedTuple,
  master_kwargs::NamedTuple, tol::Real=1e-6, max_iter::Int=100,
  checkpoint_dir=datadir("planning_checkpoints")) -> NamedTuple

Current build line (benders.jl, inside the function body, BEFORE the loop):
  follower = build_follower(; follower_kwargs..., T = T)

Returns (; y, z, UB, LB, gap, iters, oracle, follower, master, trace::BendersTrace) on
convergence; raises ErrorException (naming trace's last LB/UB/gap) on max_iter exhaustion.
Boundary guards (T>=1, max_iter>=1, length(λ₀)==T, isfinite(tol)&&tol>0, max_iter<=99_999)
fire BEFORE any build_* call, each a distinct ArgumentError.

<!-- src/planning/coupling.jl (plan 13-01, this session) — the lifecycle contract nash.jl
     drives. See 13-01-SUMMARY.md for the final field/function names actually landed. -->

SharedTransmission fields: model, x_inv::Vector{VariableRef}, x_op::Matrix{VariableRef},
z (N x T Parameter array), coupling (N x T constraint container), N::Int, T::Int,
corridor_cap::Float64, x_inv_max::Vector{Float64}, c_inv::Vector{Float64},
c_op::Vector{Vector{Float64}}.

activate_distributor!(shared, i) -> unpins x_inv[i]'s bounds to [0, x_inv_max[i]].
write_back!(shared, i, z_i_converged, x_inv_i_converged) -> pins BOTH z[i,:] (Parameter) and
  x_inv[i] (both bounds) at the converged values.
DistributorView(shared, i) -> the object passed as solve_stackelberg!'s new `follower` keyword;
  solve_follower!(::DistributorView, z_trial) dispatches correctly (duck-typed, same NamedTuple
  shape as FollowerLP's solve_follower!).

<!-- src/planning/trace.jl (this repo, Phase 12) — BendersTrace's shape, which NashTrace mirrors
     structurally while documenting what is deliberately NOT copied. Read in full. -->

BendersTrace: mutable struct with Vector fields per column + `iters::Int`; empty constructor
`BendersTrace()`; `Base.push!(trace, k::Integer; kwargs...)` with a sequential-k guard
(`_assert_sequential_trace`) and per-field finiteness/range guards; `is_converged(trace, tol)`
and `trace_summary(trace)` both empty-ledger-safe (return false/NaN/0 sentinels, never throw).

<!-- src/diagnostics/plots.jl + ext/TSODSOMakieExt.jl (this repo, Phase 7) — the weakdep
     extension idiom plot_nash_convergence must follow exactly. Read both files in full. -->

Core stub idiom: `function plot_price_convergence end` + `export plot_price_convergence` in
plots.jl (NO CairoMakie import in the core). Ext method idiom:
`TSODSO.plot_price_convergence(res::TSODSO.AdmmResiduals; filename=nothing) -> Figure`, using
file-local `_iters_axis`/`_logsafe` helpers (lines 26-33 of TSODSOMakieExt.jl), a twin-axis
(`Axis(fig[1,1]; yaxisposition=:right, ...)`, `hidespines!`/`hidexdecorations!`/`linkxaxes!`)
for the two-series overlay.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: solve_stackelberg!'s additive `follower` keyword + NashTrace ledger</name>
  <files>src/planning/benders.jl, src/planning/nash.jl, test/test_planning_nash.jl</files>
  <read_first>
    src/planning/benders.jl (full — already excerpted in context)
    src/planning/trace.jl (full — already excerpted in context, the BendersTrace shape to mirror)
    .planning/phases/13-nash-diagonalization-shared-transmission-coupling/13-PATTERNS.md
    lines 337-394 ("Parameterizing solve_stackelberg!") and lines 187-246 (NashTrace shape)
  </read_first>
  <behavior>
    - `solve_stackelberg!(...; follower = nothing)`: when omitted or `nothing`, behavior is
      BYTE-IDENTICAL to before this plan (existing Phase 11/12 tests must pass unchanged).
    - `solve_stackelberg!(...; follower = some_view, follower_kwargs = (;))`: uses `some_view`
      directly instead of building a `FollowerLP`.
    - `solve_stackelberg!(...; follower = some_view, follower_kwargs = (; corridor_cap = 1.0))`
      (both non-empty/non-nothing simultaneously): raises `ArgumentError` before any build call.
    - `NashTrace()` starts empty (`iters == 0`, every trace vector `isempty`).
    - `push!(trace::NashTrace, k, i; nash_residual, benders_iters, benders_gap, benders_retries,
      cuts_rebuilt, order)` appends one row, increments `iters`; `order` must be
      `:forward`/`:reverse` (else `ArgumentError`); `benders_iters >= 0`, `benders_retries >= 0`,
      `cuts_rebuilt >= 0` (else `ArgumentError`); `benders_gap` is NEVER guarded for
      non-finiteness but is documented as ALWAYS finite in practice (see note below).
    - `is_converged(trace::NashTrace, tol_outer, N)` / `trace_summary(trace::NashTrace)` are
      empty-ledger-safe (return `false`/sentinel NamedTuple, never throw on an empty trace).
  </behavior>
  <action>
    In `src/planning/benders.jl`: add `follower = nothing` as a new keyword to
    `solve_stackelberg!`'s signature, positioned after `checkpoint_dir`. Immediately after the
    existing boundary guards (before the `oracle = build_planning_oracle(...)` line), insert:
    if `follower !== nothing && !isempty(follower_kwargs)`, throw `ArgumentError` naming both
    supplied values and stating "supply either follower_kwargs or follower, not both". Change
    the build line from `follower = build_follower(; follower_kwargs..., T = T)` to `follower =
    follower === nothing ? build_follower(; follower_kwargs..., T = T) : follower`. Update the
    docstring's `# Algorithm` step 2 to document the new keyword as additive/non-breaking
    (mirror the existing `attempts_out::Union{Nothing,Ref{Int}}` precedent's own docstring
    phrasing in `master.jl`/`retry.jl`).

    Create `src/planning/nash.jl` with the project's standard header block (mirror
    `benders.jl`'s lines 1-37): name the seam (NASH-02/03/04 — this file grows across plans
    13-02/13-03), the owner (plan 13-02 for this task's scope), and state explicitly, in
    prose, the file's THREE structural divergences from its closest analogs: (1) unlike
    `solve_stackelberg!`'s single-level loop, `run_nash!` (Task 2) is an outer loop whose body
    IS a full `solve_stackelberg!` call — no JuMP model of its own; (2) unlike
    `BendersTrace`'s one-row-per-iteration-k granularity, `NashTrace` records one row per
    `(sweep, distributor)` pair; (3) `NashTrace.benders_gap_trace` is ALWAYS finite (never the
    `NaN`-on-feasibility-branch sentinel `BendersTrace.gap_trace` carries) because a row is only
    ever pushed from a CONVERGED `solve_stackelberg!` result — a best-response that fails to
    converge within ITS OWN `max_iter` budget raises loudly inside `solve_stackelberg!` itself,
    never reaching `push!` here. State the "fresh cut store per best-response" contract here
    too: `run_nash!` (Task 2) never persists a `BendersMaster` across best-responses — every
    call to `solve_stackelberg!` builds its own oracle/follower/master from scratch, so cut
    validity is guaranteed BY CONSTRUCTION, not by a runtime check.

    Define `mutable struct NashTrace` with fields: `sweep_trace::Vector{Int}`,
    `distributor_trace::Vector{Int}`, `nash_residual_trace::Vector{Float64}`,
    `benders_iters_trace::Vector{Int}`, `benders_gap_trace::Vector{Float64}`,
    `benders_retries_trace::Vector{Int}`, `cuts_rebuilt_trace::Vector{Int}`,
    `order_trace::Vector{Symbol}`, `iters::Int`. `NashTrace()` constructs all-empty vectors,
    `iters = 0`. Define `Base.push!(trace::NashTrace, k::Integer, i::Integer; nash_residual::Real,
    benders_iters::Integer, benders_gap::Real, benders_retries::Integer, cuts_rebuilt::Integer,
    order::Symbol)` — NO sequential-`k` guard (unlike `BendersTrace`, since the natural
    incrementing key here is the row count itself, not `k`, and `k` can legitimately repeat
    across different `i` within the same sweep); guard `order in (:forward, :reverse)`,
    `benders_iters >= 0`, `benders_retries >= 0`, `cuts_rebuilt >= 0` (each a distinct
    `ArgumentError`, mirroring `BendersTrace`'s per-field guard-before-mutate discipline);
    `nash_residual`/`benders_gap` are NOT guarded for finiteness (document why in the docstring:
    both are always finite in this project's own usage, but the ledger itself makes no
    contractual promise about it, mirroring `BendersTrace.UB_trace`'s own unguarded-sentinel
    precedent). Append to every field, increment `iters`, return `trace`.

    Define `is_converged(trace::NashTrace, tol_outer::Real, N::Int) -> Bool`: `false` if
    `trace.iters < N` (not even one full sweep recorded yet); otherwise `true` iff
    `maximum(trace.nash_residual_trace[(end - N + 1):end]) <= tol_outer` (the most recently
    completed sweep's own worst-distributor residual). Define `trace_summary(trace::NashTrace)
    -> NamedTuple`: on `trace.iters == 0` return `(; iters = 0, final_sweep = 0,
    final_residual = NaN, max_benders_iters = 0, total_benders_retries = 0,
    total_cuts_rebuilt = 0)`; otherwise `(; iters = trace.iters, final_sweep =
    last(trace.sweep_trace), final_residual = last(trace.nash_residual_trace),
    max_benders_iters = maximum(trace.benders_iters_trace), total_benders_retries =
    sum(trace.benders_retries_trace), total_cuts_rebuilt = sum(trace.cuts_rebuilt_trace))`.

    Export `NashTrace` only (do NOT re-`export is_converged`/`trace_summary` — both are already
    exported by `trace.jl` in the same `TSODSO` module for `BendersTrace`; this file only ADDS
    new methods to those existing generics via multiple dispatch).

    In `src/TSODSO.jl`, add `include("planning/nash.jl")` immediately after
    `include("planning/benders.jl")` (and after Task 2 of plan 13-01 already inserted
    `planning/coupling.jl` there — this line goes LAST in that sequence) and BEFORE
    `include("diagnostics/plots.jl")` — `nash.jl` needs `solve_stackelberg!`/
    `SharedTransmission` loaded first, and `diagnostics/plots.jl`'s later `plot_nash_convergence`
    stub (Task 3) needs `NashTrace` defined first.

    Create `test/test_planning_nash.jl` with a file header documenting this task's scope
    (NashTrace unit tests + the benders.jl regression); add these `@testitem`s, tagged
    `[:planning]`, names containing "planning" and "nash":
      1. "planning nash: NashTrace push!/is_converged/trace_summary round-trip" — build an
         empty `NashTrace`, assert `trace_summary` returns the empty sentinel and
         `is_converged(trace, 1e-4, 2) == false`; push 2 rows (sweep=1, distributors 1 and 2,
         hand-picked finite values); assert `trace.iters == 2`,
         `is_converged(trace, 1e-4, 2)` matches whether `max(nash_residual row1, row2) <=
         1e-4`, and `trace_summary(trace).final_sweep == 1`.
      2. "planning nash: NashTrace push! guards reject bad order/negative counts" —
         `@test_throws ArgumentError` for `order = :sideways`, `benders_iters = -1`,
         `benders_retries = -1`, `cuts_rebuilt = -1`.
      3. "planning nash: solve_stackelberg! follower keyword is additive — existing Phase 11/12
         call sites unchanged" — re-run the EXACT Phase-11 toy-fixture call from
         `test_planning_benders.jl`'s first testitem (reuse `Phase6Fixtures`/`ToyDeviceFixture`
         setup) WITHOUT the new `follower` keyword, assert the SAME `result.y ≈ 0.7`/
         `result.z[1] ≈ 0.7` regression holds.
      4. "planning nash: solve_stackelberg! rejects follower + non-empty follower_kwargs
         together" — `@test_throws ArgumentError` calling `solve_stackelberg!` with both a
         non-`nothing` `follower` (any placeholder object with a `solve_follower!` method is
         acceptable, or reuse a `FollowerLP` built via `build_follower`) AND a non-empty
         `follower_kwargs`.
  </action>
  <acceptance_criteria>
    - All four new testitems pass.
    - The FULL existing `test_planning_benders.jl`/`test_planning_hardening.jl`/
      `test_planning_certification.jl` suites still pass unchanged (regression proof that the
      additive keyword is non-breaking).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("planning", ti.name) && (occursin("nash", ti.name) || occursin("benders", ti.name))'</automated>
  </verify>
  <done>solve_stackelberg! accepts the new `follower` keyword non-breakingly; NashTrace exists
  with a full push!/query contract; nash.jl is wired into TSODSO.jl; all four new tests plus the
  full existing benders/hardening/certification suites are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_nash! — the outer Gauss-Seidel loop</name>
  <files>src/planning/nash.jl, test/test_planning_nash.jl</files>
  <read_first>
    src/planning/nash.jl (this plan's own Task 1 output — the file this task extends)
    src/planning/benders.jl (full — solve_stackelberg!'s fail-loud/build-once/checkpoint
    discipline this loop mirrors one level up)
    test/test_planning_benders.jl lines 74-83 (the CR-01 incumbent-consistency regression
    pattern: `solve_follower!(result.follower, result.z)` re-solved AFTER `solve_stackelberg!`
    returns, to certify the INCUMBENT — this task needs the analogous re-solve to read
    `x_inv[i]`'s incumbent-consistent value)
    .planning/phases/13-nash-diagonalization-shared-transmission-coupling/13-RESEARCH.md
    lines 559-590 (Pitfall 1: Jacobi-vs-Gauss-Seidel timing; Pitfall 2: nested-tolerance
    violation) and lines 422-437 (Pattern 2: damping/relaxation `ω`, default 1.0)
  </read_first>
  <behavior>
    - `run_nash!` on the N=2 congested fixture (plan 13-01's own toy fixture, extended with
      each distributor's own operational side identical to the Phase-11 toy fixture) converges
      within `max_sweeps` to `z ≈ [0.6, 0.6]` and `x_inv ≈ [0.3, 0.3]` (both `atol=1e-3`), with
      `dual(shared.model[:capacity][1])` nonzero at the returned state.
    - `run_nash!` raises `ArgumentError` (before any solve) when any distributor's own inner
      `tol` is `>= tol_outer`.
    - `run_nash!` raises `ArgumentError` (before any solve) for `order ∉ (:forward, :reverse)`,
      `length(specs) != shared.N`, `size(z0) != (shared.N, shared.T)`, `max_sweeps < 1`,
      `!(0 < ω <= 1)`, non-finite/non-positive `tol_outer`.
    - Forward and reverse sweep orders converge to the SAME equilibrium (within `atol=1e-3`) on
      this SYMMETRIC N=2 fixture — the direct regression Pitfall 1 recommends for detecting an
      accidental Jacobi-timing bug.
    - `run_nash!` with `max_sweeps` set too low to converge raises a loud `ErrorException`
      naming the exhausted sweep count and the last recorded residual, never returning a
      non-converged result silently.
  </behavior>
  <action>
    Append to `src/planning/nash.jl`. Define
    `run_nash!(specs::AbstractVector{<:NamedTuple}, shared::SharedTransmission; z0::
    AbstractMatrix{<:Real}, tol_outer::Real = 1e-4, max_sweeps::Int = 50, order::Symbol =
    :forward, ω::Real = 1.0, checkpoint_dir::AbstractString = datadir("nash_checkpoints")) ->
    NamedTuple`. Each element of `specs` is a `NamedTuple` supplying, per distributor:
    `feeder`, `pf`, `aggregators`, `λ₀`, `master_kwargs` (all required — mirrors
    `solve_stackelberg!`'s own positional/keyword split), and OPTIONALLY `tol` (default
    `1e-6`) and `max_iter` (default `100`) via `get(spec, :tol, 1e-6)`/`get(spec, :max_iter,
    100)`.

    Boundary guards BEFORE any solve call (mirror `solve_stackelberg!`'s own
    guards-before-build discipline exactly, each a distinct `ArgumentError` naming the
    offending value): `length(specs) == shared.N`; `size(z0) == (shared.N, shared.T)`; `order
    in (:forward, :reverse)`; `max_sweeps >= 1`; `0 < ω <= 1`; `isfinite(tol_outer) &&
    tol_outer > 0`; and the NESTED-TOLERANCE guard (CONTEXT.md locked, mirrors 13-RESEARCH.md
    Pitfall 2): for every `spec` in `specs`, `get(spec, :tol, 1e-6) < tol_outer` or throw
    `ArgumentError` naming the distributor index, its own `tol`, and `tol_outer`.

    Initialize `z_prev = Matrix{Float64}(z0)` (a mutable copy), `x_inv_prev =
    zeros(shared.N)` (matches `build_shared_transmission`'s own build-time all-zero-pinned
    default), `trace = NashTrace()`, `ub_prev = fill(NaN, shared.N)`, `sweep_order = order ===
    :forward ? (1:shared.N) : (shared.N:-1:1)`.

    For `k in 1:max_sweeps`, for `i in sweep_order`: call `activate_distributor!(shared, i)`;
    call `result_i = solve_stackelberg!(spec.feeder, spec.pf, spec.aggregators; λ₀ = spec.λ₀,
    T = shared.T, follower_kwargs = NamedTuple(), master_kwargs = spec.master_kwargs, tol =
    get(spec, :tol, 1e-6), max_iter = get(spec, :max_iter, 100), checkpoint_dir =
    joinpath(checkpoint_dir, "sweep_$k", "distributor_$i"), follower = DistributorView(shared,
    i))` where `spec = specs[i]`. CR-01 PARITY (load-bearing, do not skip): immediately
    re-solve `f_res = solve_follower!(result_i.follower, result_i.z)` — since
    `solve_stackelberg!` returns the INCUMBENT `(y_best, z_best)`, which may differ from the
    LAST master trial actually solved against the shared model, `value(shared.x_inv[i])`
    immediately after `solve_stackelberg!` returns is NOT guaranteed to correspond to the
    incumbent `result_i.z` — this re-solve makes it so, mirroring
    `test_planning_benders.jl`'s own CR-01 regression pattern. THEN read `x_inv_i_converged =
    value(shared.x_inv[i])`.

    Compute `residual_i = max(maximum(abs.(result_i.z .- z_prev[i, :])), abs(x_inv_i_converged
    - x_inv_prev[i]))` (CONTEXT.md's own "‖z_i^(k+1)-z_i^(k)‖∞ plus investment change"
    formula). Compute the (possibly damped) write-back value: `z_i_new = ω == 1.0 ?
    result_i.z : (1 - ω) .* z_prev[i, :] .+ ω .* result_i.z` — document in the docstring, as an
    explicit caveat, that when `ω < 1` the written-back `x_inv[i]` is STILL
    `x_inv_i_converged` (the UNDAMPED best-response's own optimal investment for the UNDAMPED
    `result_i.z`), a documented simplification acceptable because this phase's own fixtures
    use the locked default `ω = 1.0` (no damping exercised); a future phase revisiting damping
    on a genuinely cycling fixture should re-derive the damped investment jointly, not reuse
    this shortcut uncritically.

    Call `write_back!(shared, i, z_i_new, x_inv_i_converged)`. Update `z_prev[i, :] =
    z_i_new`, `x_inv_prev[i] = x_inv_i_converged`, `ub_prev[i] = result_i.UB`. Call `push!(trace,
    k, i; nash_residual = residual_i, benders_iters = result_i.iters, benders_gap =
    result_i.gap, benders_retries = trace_summary(result_i.trace).total_retries, cuts_rebuilt =
    length(result_i.master.cuts), order = order)`. Call `checkpoint_iteration!((; k, i, z_i =
    z_i_new, x_inv_i = x_inv_i_converged, nash_residual = residual_i), (k - 1) * shared.N +
    findfirst(==(i), sweep_order); dir = joinpath(checkpoint_dir, "outer"))` — a SEPARATE
    checkpoint subdirectory (`"outer"`) from each distributor's own inner Benders checkpoints
    (already nested under `sweep_$k/distributor_$i` above), so the two checkpoint streams never
    collide.

    After the inner `for i` loop completes for sweep `k`: `outer_residual_k =
    maximum(trace.nash_residual_trace[(end - shared.N + 1):end])`; if `outer_residual_k <=
    tol_outer`, return `(; z = copy(z_prev), x_inv = copy(x_inv_prev), UB = copy(ub_prev),
    converged = true, sweeps = k, outer_residual = outer_residual_k, trace, shared, order)`.

    After the outer `for k` loop exhausts without returning: read
    `last_residual = last(trace.nash_residual_trace)` (never a stale loop-local — mirrors
    `benders.jl`'s own IN-01 discipline) and raise `error("run_nash!: exhausted $max_sweeps
    sweep(s) without converging (last recorded nash_residual=$last_residual,
    tol_outer=$tol_outer) — refusing to silently return a non-converged result")`.

    Export `run_nash!`.

    Add these `@testitem`s to `test/test_planning_nash.jl`, tagged `[:planning]`:
      5. "planning nash: N=2 Gauss-Seidel converges to the hand-checked congested equilibrium
         (z=[0.6,0.6], x_inv=[0.3,0.3], capacity dual nonzero)" — build `shared =
         build_shared_transmission(; N=2, T=1, corridor_cap=2.0, x_inv_max=[0.3,0.3],
         c_inv=[1.0,1.0], c_op=[[0.5],[0.5]])`; two `specs` entries, each identical to the
         Phase-11 toy fixture (`Phase6Fixtures.two_bus_feeder()`, `LinDistFlow()`,
         `ToyDeviceFixture.ToyElasticDevice(2,6.0,1.0,10.0)` wrapped in an `Aggregator`,
         `λ₀=[4.0]`, `master_kwargs=(; c_y=0.3, y_max=8.0, α_op_lb=-5.0, α_x_lb=0.0)`); `z0 =
         zeros(2,1)`; call `run_nash!(specs, shared; z0=z0, tol_outer=1e-4, max_sweeps=50,
         checkpoint_dir=mktempdir())`; assert `result.converged`, `isapprox(result.z,
         [0.6,0.6]; atol=1e-3)`, `isapprox(result.x_inv, [0.3,0.3]; atol=1e-3)`,
         `abs(dual(shared.model[:capacity][1])) > 1e-8`.
      6. "planning nash: nested-tolerance guard rejects inner tol >= outer tol" —
         `@test_throws ArgumentError` calling `run_nash!` with a spec whose `tol=1e-4` against
         `tol_outer=1e-4`.
      7. "planning nash: forward and reverse sweep orders agree on the symmetric N=2 fixture
         (Gauss-Seidel-vs-Jacobi timing regression, Pitfall 1)" — run the SAME N=2 fixture
         twice, `order=:forward` and `order=:reverse` (each with its OWN freshly-built
         `shared`, since `SharedTransmission` is mutated destructively), assert both converge
         and `isapprox(result_fwd.z, result_rev.z; atol=1e-3)`.
      8. "planning nash: max_sweeps exhaustion raises loudly, never a silent non-converged
         return" — same fixture, `max_sweeps=1`; assert `@test_throws ErrorException` and that
         the message contains `"exhausted"` and `"last recorded nash_residual"`.
      9. "planning nash: damping ω=0.5 still converges (no cycling on this monotone fixture)"
         — same fixture, `ω=0.5`; assert `result.converged` and `isapprox(result.z, [0.6,0.6];
         atol=1e-2)` (looser atol — damping slows convergence, does not change the fixed
         point).
  </action>
  <acceptance_criteria>
    - Testitems 5-9 all pass.
    - `run_nash!` never rebuilds `shared.model` or any distributor's oracle/follower/master
      outside `solve_stackelberg!`'s own internal build calls — no `Model(` / `build_*` call
      appears in `run_nash!`'s own body.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("planning", ti.name) && occursin("nash", ti.name)'</automated>
  </verify>
  <done>run_nash! converges the N=2 hand-checked fixture, enforces the nested-tolerance guard,
  agrees across forward/reverse orders, raises loudly on exhaustion, and tolerates damping —
  all five new tests green alongside Task 1's four.</done>
</task>

<task type="auto">
  <name>Task 3: plot_nash_convergence (core stub + CairoMakie extension method)</name>
  <files>src/diagnostics/plots.jl, ext/TSODSOMakieExt.jl, test/test_planning_nash.jl</files>
  <read_first>
    src/diagnostics/plots.jl (full — already excerpted in context)
    ext/TSODSOMakieExt.jl (full — already excerpted in context, especially
    `plot_price_convergence`'s twin-axis idiom and the `_iters_axis`/`_logsafe` helpers)
    test/test_diagnostics_plot.jl (full — the exact separate-process smoke-test idiom for a
    CairoMakie-backed method, including the CairoMakie-not-installed skip path)
  </read_first>
  <action>
    In `src/diagnostics/plots.jl`, add `function plot_nash_convergence end` with a docstring
    (mirroring `plot_price_convergence`'s own docstring style) stating it plots the OUTER
    per-sweep max Nash residual (log-scaled left axis) overlaid with the INNER per-distributor
    Benders gap trajectory (right axis) from a `NashTrace`, requires CairoMakie, and has NO
    applicable method without it (deliberate `MethodError`, threat T-07-01 parity). Add
    `export plot_nash_convergence` alongside the file's existing exports.

    In `ext/TSODSOMakieExt.jl`, add `TSODSO.plot_nash_convergence(trace::TSODSO.NashTrace;
    filename = nothing) -> Figure`. Compute `sweeps = sort(unique(trace.sweep_trace))`;
    `outer_residual = [maximum(trace.nash_residual_trace[trace.sweep_trace .== k]) for k in
    sweeps]` (the per-sweep worst-distributor residual — the OUTER curve). Build `fig =
    Figure()`, left `Axis` (`xlabel="Nash sweep k"`, `ylabel="outer Nash residual
    (per-unit)"`, `yscale=log10`, title naming both levels), plot `lines!(ax, sweeps,
    _logsafe(outer_residual); color=:purple, label="outer residual")`. Add a twin right `Axis`
    (`yaxisposition=:right`, `ylabelcolor=:seagreen`, `hidespines!`/`hidexdecorations!`/
    `linkxaxes!` against the left axis, mirroring `plot_price_convergence`'s own twin-axis
    block verbatim) labeled `"inner Benders gap (per-unit)"`; for each distinct distributor
    index `d in sort(unique(trace.distributor_trace))`, mask rows where
    `trace.distributor_trace .== d`, and `scatterlines!` that distributor's own
    `benders_gap_trace` values against their `sweep_trace` on the twin axis, one distinct color
    per distributor (reuse `:seagreen`-family or a small fixed palette, e.g. `[:seagreen,
    :orange, :teal]` indexed by `d`). Add an `axislegend`. `filename === nothing || save(filename,
    fig)`. Return `fig`.

    Add one `@testitem` to `test/test_planning_nash.jl`, tagged `[:planning]`, mirroring
    `test_diagnostics_plot.jl`'s own separate-process CairoMakie idiom EXACTLY (skip-with-message
    if `Base.find_package("CairoMakie") === nothing`; otherwise spawn a child Julia process that
    `import CairoMakie; using TSODSO`, builds a small synthetic `NashTrace` (push 4 rows: sweep 1
    distributors 1,2; sweep 2 distributors 1,2, with a strictly decreasing `nash_residual` and
    finite `benders_gap` per row), calls `TSODSO.plot_nash_convergence(trace)`, asserts the
    result `isa CairoMakie.Makie.Figure`, and prints a sentinel string this test asserts
    `occursin` on the captured stdout).
  </action>
  <acceptance_criteria>
    - `TSODSO.plot_nash_convergence` has NO applicable method when CairoMakie is not loaded
      (core stays plot-free, threat T-07-01 parity) — verified by the same `isempty(methods(...))`
      + `!any(m -> occursin("CairoMakie", m), loaded)` assertions `test_diagnostics_plot.jl`
      uses for the existing two plot functions, added as a companion assertion in the new
      testitem's headless branch.
    - The separate-process smoke test passes when CairoMakie is installed, and is
      `@test_skip`ped (not failed) when it is not.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=test -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("planning", ti.name) && occursin("nash", ti.name)'</automated>
  </verify>
  <done>plot_nash_convergence exists as a plot-free core stub plus a CairoMakie twin-axis
  extension method returning a Figure; the new smoke test is green (or cleanly skipped without
  CairoMakie); the full existing test_diagnostics_plot.jl suite is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `nash.jl` internal | Orchestration-only file — no JuMP model of its own; the boundary of interest is the ORDERING/TIMING of calls into `solve_stackelberg!`/`coupling.jl`'s lifecycle functions, not a data-trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering/Integrity | `run_nash!`'s per-best-response cut store | mitigate | `run_nash!` NEVER persists a `BendersMaster`/cut store across calls — every best-response invokes `solve_stackelberg!` fresh, which itself builds `oracle`/`follower`/`master` from scratch (Task 2's acceptance_criteria explicitly regresses "no `Model(`/`build_*` call in `run_nash!`'s own body") — cut-reuse-across-`z_{-i}`-changes (RESEARCH.md's own documented correctness risk) is structurally impossible by construction, not merely policy. |
| T-13-05 | Tampering/Integrity | `write_back!` call timing inside the sweep loop | mitigate | `write_back!` fires INLINE, immediately after each distributor's `solve_stackelberg!` (+ CR-01 re-solve) completes, BEFORE the next `i` in `sweep_order` is processed — Task 2's testitem 7 (forward-vs-reverse agreement on the symmetric fixture) is the direct regression Pitfall 1 recommends for catching an accidental Jacobi-timing bug. |
| T-13-06 | Tampering | Nested-tolerance violation (`tol_inner >= tol_outer`) | mitigate | Code-level `ArgumentError` guard in `run_nash!`, fired BEFORE any solve call — tested explicitly (testitem 6) — prevents the outer loop from "converging" vacuously on inner-solve noise (Pitfall 2). |
| T-13-07 | Repudiation | `NashTrace`'s `benders_gap_trace` finiteness claim | accept | Documented as "always finite in this project's own usage" but not code-enforced (mirrors `BendersTrace.UB_trace`'s own precedent of leaving legitimate-but-not-contractually-guaranteed fields unguarded) — low risk since a non-finite gap would only arise from a `solve_stackelberg!` internal bug already covered by Phase 11/12's own regression suite. |
</threat_model>

<verification>
Run the quick planning+nash(+benders) filter after each task; run the FULL suite
(`julia --project=. -e 'using Pkg; Pkg.test()'`) before this wave is considered merged, per
13-VALIDATION.md's "after every plan wave" sampling rate.
</verification>

<success_criteria>
`solve_stackelberg!` accepts the additive `follower` keyword with zero regressions to Phase
11/12; `run_nash!` converges the hand-checked N=2 congested fixture with the nested-tolerance
guard enforced, Gauss-Seidel timing verified (forward/reverse agreement), and damping tolerated;
`NashTrace` records both diagnostic levels together; `plot_nash_convergence` renders a two-level
Figure via the established CairoMakie weakdep extension idiom.
</success_criteria>

<output>
Create `.planning/phases/13-nash-diagonalization-shared-transmission-coupling/13-02-SUMMARY.md` when done
</output>
