---
phase: 05-distribution-pricing-dadp-dlmp-decomposition
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pricing/fit.jl
  - test/test_fit.jl
autonomous: true
requirements: [PRICE-03]
must_haves:
  truths:
    - "A researcher can solve the FIT (feed-in-tariff) baseline counterfactual — the only genuine new solve of the phase"
    - "The FIT baseline is thesis-faithful: FIT-OPT (3.24-3.28) per-prosumer with PV + flexible loads and NO battery, aggregated, then a plain AC power flow with the voltage-limit constraint NOT enforced"
    - "The FIT baseline returns a social-welfare number that the welfare-accounting plan compares against the DADP solve to compute the +25% ratio"
    - "The FIT solve routes through select_optimizer (open-source default) and is gated on assert_solved!"
  artifacts:
    - path: "src/pricing/fit.jl"
      provides: "fit_baseline / solve_fit — the FIT-OPT + AC-PF counterfactual returning a solved baseline ctx and its welfare"
      exports: ["fit_baseline"]
      contains: "function fit_baseline"
    - path: "test/test_fit.jl"
      provides: "@testitems that the FIT baseline solves OPTIMAL and returns a finite, magnitude-sane welfare"
      contains: "@testitem"
  key_links:
    - from: "src/pricing/fit.jl"
      to: "select_optimizer(problem_class(pf))"
      via: "solver factory, never naming a concrete solver"
      pattern: "select_optimizer"
    - from: "src/pricing/fit.jl"
      to: "assert_solved!"
      via: "OPTIMAL gate before reading the baseline welfare"
      pattern: "assert_solved!"
---

<objective>
Deliver the FIT (feed-in-tariff) baseline counterfactual in `src/pricing/fit.jl` (PRICE-03,
the baseline half). This is the ONE genuinely new optimization of the phase: the thesis-faithful
FIT-OPT (thesis eqs. 3.24-3.28) — a per-prosumer schedule with PV and flexible loads but NO
battery and NO network constraints, using the German-FIT price triple — aggregated (3.22-3.23)
and then evaluated on a plain AC power flow with the voltage-limit constraint deliberately NOT
enforced (the thesis "observe 3.35 not enforced" step). It returns the FIT social welfare that
the welfare-accounting plan (05-05) divides into to compute the headline +25% ratio.

Purpose: A trustworthy counterfactual so the +25%-social-welfare headline is a computed RATIO of
two solves, not a figure-bound absolute. Output: `fit_baseline`, and tests that it solves OPTIMAL
and returns a finite, magnitude-sane welfare.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md
@CLAUDE.md

<interfaces>
select_optimizer(problem_class(pf))          # solver factory; QP() for LinDistFlow, SOCP() for ConvexBranchFlow
assert_solved!(model; dual=true, allow_local=false)   # OPTIMAL gate (src/core/status.jl)
ConvexBranchFlow() / LinDistFlow()           # AbstractPowerFlow; contribute!(pf, ctx, feeder; T)
Aggregator(bus, φ, devices, Pdc)             # roll-up; contribute!(agg, ctx; T) -> (; vars, p_inject, utility)
Deferrable(bus, t0, t1, E, a, b)  Thermostatic(...)  PVBattery(...)   # devices (FIT uses PV + flexible loads, NO battery)
generate_profiles(seed=..., T=...) -> (; pv, demand)   # seeded, reproducible (INFRA-04)
Fixtures (test/fixtures_phase4.jl): ieee13_modified(), build_ieee13_ground_aggregators(feeder),
  mem_price_profile(), GROUND_LOAD_SCALE, GROUND_PV_SCALE, temperature_profile()

Thesis anchors (RESEARCH Pattern 3 + Sources):
  FIT-OPT eqs. 3.24-3.28 (page 81); import/self-consume/export split 3.25-3.27 uses max/min(Ppv, p_h)
  German-FIT price triple (page 93): λ_import=6.6, λ_export=9.6, λ_self=5.6 ¢$/kWh
  DSO benefit under FIT = B_DSO eq. 4.1 (page 93)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIT-OPT per-prosumer schedule with the three FIT flows (3.24-3.28)</name>
  <files>src/pricing/fit.jl, test/test_fit.jl</files>
  <read_first>
    - src/pricing/fit.jl (the stub header from plan 05-01)
    - src/devices/PVBattery.jl and src/devices/Deferrable.jl (device/utility structure and the aggregatable return contract; FIT reuses PV + flexible loads, drops the battery)
    - src/devices/Aggregator.jl (roll-up 3.21-3.23; the FIT aggregation mirrors this)
    - src/models/linear_solve.jl and src/models/welfare_solve.jl (the build → assert_solved! → read pattern to mirror)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Pattern 3 FIT baseline paragraph, Pitfall 4/5, Assumption A4, Open Q3)
  </read_first>
  <behavior>
    - A per-prosumer FIT schedule maximizes prosumer surplus under FIXED German-FIT prices
      (λ_import=6.6, λ_export=9.6, λ_self=5.6 ¢$/kWh) with PV + flexible loads and NO battery.
    - The import/self-consume/export split (3.25-3.27) is derived from max/min(Ppv, p_h) so
      self-consumption is priced at λ_self, exported surplus at λ_export, residual import at λ_import.
    - The per-prosumer solve is OPTIMAL and returns a finite prosumer value in the same monetary
      unit as the DADP solve (¢$/kWh-consistent; no second price unit — Pitfall 5).
  </behavior>
  <action>
    Implement the FIT-OPT prosumer sub-model (thesis eqs. 3.24-3.28) as JuMP model construction
    inside `src/pricing/fit.jl`: for each prosumer, PV availability + flexible-load variables (reuse
    the Deferrable/Thermostatic device builders; DROP the PVBattery per Assumption A4), plus the
    three FIT flow variables — self-consumption, export, import — tied to the load and PV via the
    3.25-3.27 split (`self ≤ min(Ppv, p_h)`, `export = Ppv − self` when surplus, `import = p_h − self`).
    Define the FIT prices as named module constants (`FIT_λ_IMPORT=6.6`, `FIT_λ_EXPORT=9.6`,
    `FIT_λ_SELF=5.6`, documented as the thesis page-93 German-FIT calibration, kept in the SAME
    unit as `mem_price_profile()` — Pitfall 5). Maximize the prosumer FIT surplus (device utility
    + export revenue − import cost, valuing self-consumption at λ_self). Route the solve through
    `select_optimizer(problem_class(pf))` (never name a solver — INFRA-02) and gate on
    `assert_solved!`. Add a magnitude-sanity guard on the returned surplus (finite, within the
    per-unit price band). Write an @testitem (name contains "fit") on a small seeded prosumer set
    asserting OPTIMAL + finite surplus + the flow identity `self + import == p_h` and `self + export == Ppv`.
    Use `error`/`ArgumentError` for all guards (never `@assert`).
  </action>
  <acceptance_criteria>
    - `src/pricing/fit.jl` defines the FIT-OPT prosumer sub-model and the `FIT_λ_IMPORT/EXPORT/SELF` constants (grep each).
    - @testitem `occursin("fit", ti.name)` passes: the per-prosumer FIT solve is OPTIMAL, surplus finite and magnitude-sane.
    - The FIT flow identities hold at the optimum: `self + import ≈ p_h`, `self + export ≈ Ppv` (asserted).
    - No solver named in the file (grep confirms `select_optimizer` is the only solver entry).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("fit", ti.name)'</automated>
  </verify>
  <done>The FIT-OPT per-prosumer schedule with the three German-FIT flows solves OPTIMAL and returns a magnitude-sane surplus in the DADP-consistent unit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fit_baseline — aggregate + AC-PF + return the FIT social welfare (3.22-3.23, 4.1)</name>
  <files>src/pricing/fit.jl, test/test_fit.jl</files>
  <read_first>
    - src/pricing/fit.jl (Task 1 FIT-OPT sub-model)
    - src/powerflow/ConvexBranchFlow.jl (reuse as the AC-PF: fix aggregator injections, relax/remove the voltage bounds so 3.35 is NOT enforced — RESEARCH Open Q3 recommendation)
    - src/models/welfare_solve.jl (how injections close the :Rp/:Rq balances; the frontier import at root)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Pattern 3 FIT baseline + Open Q3 "reuse ConvexBranchFlow with voltage bounds relaxed", Pitfall 4)
  </read_first>
  <behavior>
    - fit_baseline aggregates the per-prosumer FIT schedules into nodal injections (3.22-3.23)
      and evaluates a plain AC power flow (reuse ConvexBranchFlow with the voltage-limit
      constraint NOT enforced — the thesis "observe 3.35 not enforced" step).
    - It returns a NamedTuple carrying the solved baseline ctx and `social_fit` (the FIT social
      welfare / DSO benefit B_DSO, thesis 4.1), finite and magnitude-sane.
    - The solve is OPTIMAL and reproducible (seeded profiles → identical baseline).
  </behavior>
  <action>
    Implement `fit_baseline(feeder, pf, prosumers; T, λ₀, seed=...) -> (; ctx, social_fit)`:
    solve the Task-1 FIT-OPT per prosumer, aggregate to nodal net injections (thesis 3.22-3.23),
    then evaluate an AC power flow by reusing `ConvexBranchFlow` with the bus voltage bounds
    RELAXED/removed (so 3.35 is not enforced, per Open Q3 and the thesis FIT step) — fixing the
    aggregator injections and solving the resulting feasibility/PF through
    `select_optimizer(problem_class(pf))`, gated on `assert_solved!`. Compute `social_fit` as the
    FIT social welfare / DSO benefit `B_DSO` (thesis eq. 4.1). Guard the returned welfare for
    finiteness and magnitude sanity. Document that the FIT solve deliberately omits the voltage
    limit and the battery (so it is a genuine counterfactual, not the DADP solve). Reproducibility:
    all profiles flow from `generate_profiles(seed=...)`. Write an @testitem (name contains "fit")
    asserting OPTIMAL, finite `social_fit`, and bit-for-bit reproducibility across two calls with
    the same seed.
  </action>
  <acceptance_criteria>
    - `src/pricing/fit.jl` defines and exports `fit_baseline`; `grep "function fit_baseline"` succeeds.
    - @testitem `occursin("fit", ti.name)`: `fit_baseline(...)` returns OPTIMAL with a finite, magnitude-sane `social_fit`.
    - Two calls with the same seed return an identical `social_fit` (reproducibility assertion).
    - The AC-PF reuse does NOT enforce the voltage limit (documented; the FIT ctx differs structurally from the DADP welfare ctx).
    - `select_optimizer` is the only solver entry; `assert_solved!` gates the read.
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("fit", ti.name)'</automated>
  </verify>
  <done>fit_baseline returns a solved FIT counterfactual ctx and a finite, reproducible social_fit that welfare-accounting (05-05) uses as the +25% ratio denominator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FIT baseline definition → +25% ratio | A mis-specified FIT baseline (wrong prices, battery left in, voltage limit enforced) silently inflates or deflates the headline ratio |
| FIT solve → open-source solver policy | The counterfactual must route through select_optimizer, never a named commercial solver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering (silent-wrong headline) | FIT baseline specification | mitigate | Thesis-faithful FIT-OPT (no battery, no voltage limit), documented German-FIT prices as named constants, flow-identity assertions; ratio (not absolute) reported by 05-05 |
| T-05-09 | Repudiation (irreproducible baseline) | FIT scenario generation | mitigate | Seeded generate_profiles; assert two same-seed calls return identical social_fit |
| T-05-05 | Spoofing (unit mismatch) | FIT prices vs λ₀ | mitigate | FIT prices kept in the same ¢$/kWh unit as mem_price_profile(); magnitude-sanity guard on surplus (Pitfall 5) |
| T-05-10 | Elevation (commercial-solver leakage) | FIT solve backend | mitigate | select_optimizer(problem_class(pf)) only; open-source Clarabel/HiGHS default; no Gurobi/Mosek named |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `@run_package_tests filter=ti->occursin("fit", ti.name)` green.
- FIT baseline solves OPTIMAL, returns finite magnitude-sane `social_fit`, reproducible under a fixed seed.
- Flow identities `self+import≈p_h`, `self+export≈Ppv` hold; voltage limit not enforced; battery dropped.
</verification>

<success_criteria>
The FIT feed-in-tariff baseline counterfactual (FIT-OPT 3.24-3.28 + AC-PF, no battery, no voltage
limit, German-FIT prices) solves OPTIMAL through the open-source factory and returns a finite,
reproducible FIT social welfare — the denominator of the +25% headline ratio computed in 05-05.
</success_criteria>

<output>
Create `.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-03-SUMMARY.md` when done.
</output>
