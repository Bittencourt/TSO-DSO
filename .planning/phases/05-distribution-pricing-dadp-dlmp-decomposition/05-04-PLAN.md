---
phase: 05-distribution-pricing-dadp-dlmp-decomposition
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pricing/checks.jl
  - test/test_economic_direction.jl
autonomous: true
requirements: [PRICE-04]
must_haves:
  truths:
    - "A researcher can assert the economic-direction sanity checks: the distribution price falls BELOW wholesale (λ₀) at PV glut and rises ABOVE it at congestion"
    - "The check reuses the Phase-4 high-PV (over-generation) and IEEE-13 ground (head-branch congestion) fixtures"
    - "A backwards price signal (glut price above λ₀ or congestion price below λ₀) throws, not silently passes"
  artifacts:
    - path: "src/pricing/checks.jl"
      provides: "economic_direction_checks(ctx; λ₀) -> (; pv_glut_ok, congestion_ok)"
      exports: ["economic_direction_checks"]
      contains: "function economic_direction_checks"
    - path: "test/test_economic_direction.jl"
      provides: "@testitems for the PV-glut and congestion directional checks (name substrings econ/direction)"
      contains: "@testitem"
  key_links:
    - from: "src/pricing/checks.jl"
      to: "ctx.constraints[:balance_p]"
      via: "dual read of the DADP compared against λ₀ (independent of dlmp.jl for parallel execution)"
      pattern: "balance_p"
    - from: "test/test_economic_direction.jl"
      to: "Phase4Fixtures.build_high_pv_aggregators / build_ieee13_ground_aggregators"
      via: "reuse of the over-generation and congestion scenarios"
      pattern: "build_high_pv_aggregators"
---

<objective>
Deliver the economic-direction sanity checks in `src/pricing/checks.jl` (PRICE-04): assert the
distribution price falls BELOW wholesale (λ₀) at PV glut and rises ABOVE it at congestion — the
qualitative economic-correctness net that catches a backwards price signal the sum-to-price and
surplus-identity assertions would not (an internally-consistent but economically-inverted price).

This plan is file-disjoint from the other Wave-2 pricing modules and reads the DADP directly from
`ctx.constraints[:balance_p]` (the same primitive `extract_dlmp` uses) so it stays independent of
`dlmp.jl` and can be developed in parallel.

Purpose: A cheap, high-signal directional check that would fire on a dual-sign or attribution bug.
Output: `economic_direction_checks`, and tests on the Phase-4 high-PV and congestion fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md
@CLAUDE.md

<interfaces>
ctx.constraints[:balance_p]   # bus×time; dual = DADP λ_j (read directly, same primitive as extract_dlmp)
ctx.meta[:feeder]             # .root, .buses;  ctx.meta[:T]
solve_welfare(feeder, ConvexBranchFlow(), aggs; T, λ₀, allow_export=true) -> (ctx, obj, dadp)

Fixtures (test/fixtures_phase4.jl, setup=[Phase4Fixtures]):
  high_pv_feeder()  build_high_pv_aggregators(feeder)          # PV glut: over-generation, reverse flow → DADP < λ₀
  ieee13_modified()  build_ieee13_ground_aggregators(feeder)   # head-branch smax binds → congestion → DADP > λ₀
  mem_price_profile()  # λ₀ length 24

Thesis anchors (RESEARCH Pattern 4, Sources): Fig 4.5 node 9 @ 15:00 < MEM (glut);
  Fig 4.6 node 9 @ 22:00 > MEM (congestion), page 96.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: economic_direction_checks — DADP vs λ₀ direction on glut and congestion</name>
  <files>src/pricing/checks.jl, test/test_economic_direction.jl</files>
  <read_first>
    - src/pricing/checks.jl (the stub header from plan 05-01)
    - src/models/welfare_solve.jl (`:balance_p` registration; the exactness gate that makes the dual trustworthy)
    - test/fixtures_phase4.jl (`build_high_pv_aggregators`, `build_ieee13_ground_aggregators`, `high_pv_feeder`, `mem_price_profile`)
    - .planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-RESEARCH.md (Pattern 4, Pitfall 1 warning signs, PRICE-04 test map)
  </read_first>
  <behavior>
    - economic_direction_checks(ctx; λ₀) reads the per-node/hour DADP from ctx.constraints[:balance_p]
      and returns (; pv_glut_ok, congestion_ok) plus throws on a backwards signal.
    - On the high-PV (over-generation) fixture: at the over-generating node/hours the DADP is
      BELOW λ₀ (price < wholesale at PV glut).
    - On the IEEE-13 ground (head-branch-congested) fixture: at the congested node/hours the DADP
      is ABOVE λ₀ (price > wholesale at congestion).
    - A deliberately sign-flipped input triggers the throw (the check is not vacuous).
  </behavior>
  <action>
    Implement `economic_direction_checks(ctx; λ₀) -> (; pv_glut_ok, congestion_ok)` reading the
    DADP directly as `dual.(ctx.constraints[:balance_p][j, :])` (the same primitive extract_dlmp
    uses — read it directly here so this module does NOT depend on dlmp.jl, keeping Wave-2
    parallel). The caller indicates the regime (or the function inspects both the over-generation
    and congestion hours identified by the fixture). For the PV-glut regime assert `dadp[t] < λ₀[t]`
    at the over-generating node/hours; for the congestion regime assert `dadp[t] > λ₀[t]` at the
    head-branch-binding node/hours (thesis Fig 4.5/4.6). Throw an `ArgumentError`/`error` (never
    `@assert`) naming the backwards direction on violation (RESEARCH Pitfall 7 directional check).
    Add a shape guard on `length(λ₀) == T`. Cite Fig 4.5/4.6. Tests (@testitems, names contain
    "econ" and/or "direction"), using `setup=[Phase4Fixtures]`:
    - high-PV solve (`build_high_pv_aggregators` on `high_pv_feeder`, `allow_export=true`):
      `pv_glut_ok == true` and the DADP is strictly below λ₀ at the over-generation hours.
    - IEEE-13 ground solve (`build_ieee13_ground_aggregators`, `allow_export=true`):
      `congestion_ok == true` and the DADP is strictly above λ₀ at the congested hours.
    - a sign-flipped/negated λ₀ (or DADP) makes the check THROW (@test_throws) — proving it is not vacuous.
  </action>
  <acceptance_criteria>
    - `src/pricing/checks.jl` defines and exports `economic_direction_checks`; `grep "function economic_direction_checks"` succeeds.
    - @testitem `occursin("econ", ti.name) || occursin("direction", ti.name)` passes: PV-glut DADP < λ₀; congestion DADP > λ₀ on the Phase-4 fixtures.
    - A backwards-signal input throws (`@test_throws` on a negated case) — the check is non-vacuous.
    - `economic_direction_checks` reads `ctx.constraints[:balance_p]` directly (grep `balance_p`), not via `extract_dlmp` (module stays independent of dlmp.jl).
  </acceptance_criteria>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("econ", ti.name) || occursin("direction", ti.name)'</automated>
  </verify>
  <done>economic_direction_checks confirms DADP < λ₀ at PV glut and DADP > λ₀ at congestion on the Phase-4 fixtures and throws on a backwards signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DADP dual → economic interpretation | An internally-consistent price can still be economically inverted (sign/attribution bug); the directional check is the economic net |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Spoofing (backwards price signal) | economic_direction_checks | mitigate | Assert DADP < λ₀ at glut and > λ₀ at congestion on the Phase-4 fixtures; throw on violation; a negated-input @test_throws proves non-vacuity |
| T-05-11 | Tampering (shape mismatch) | λ₀ / horizon guard | mitigate | Guard length(λ₀) == T with a loud error (never @assert) before any indexing |
| T-05-01 | Elevation (untrusted dual) | reading balance_p from an ungated ctx | mitigate | Only run on a ctx produced by solve_welfare (which ran the PF-04 exactness gate); document the requirement |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `@run_package_tests filter=ti->occursin("econ", ti.name) || occursin("direction", ti.name)` green.
- PV-glut: DADP strictly below λ₀; congestion: DADP strictly above λ₀ on the Phase-4 fixtures.
- Backwards signal throws.
</verification>

<success_criteria>
The economic-direction checks pass: the distribution price falls below wholesale at PV glut and
rises above it at congestion on the Phase-4 high-PV and IEEE-13 fixtures — Success Criterion #4.
</success_criteria>

<output>
Create `.planning/phases/05-distribution-pricing-dadp-dlmp-decomposition/05-04-SUMMARY.md` when done.
</output>
