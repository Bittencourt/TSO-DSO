---
phase: 03-prosumer-device-library-social-welfare-solve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Project.toml
  - Manifest.toml
  - Manifest-v1.10.toml
  - Manifest-v1.11.toml
  - Manifest-v1.12.toml
  - src/TSODSO.jl
  - src/data/profiles.jl
  - src/devices/Thermostatic.jl
  - src/devices/Deferrable.jl
  - src/devices/PVBattery.jl
  - src/devices/Aggregator.jl
  - src/models/welfare_solve.jl
  - test/fixtures_phase3.jl
  - test/test_profiles.jl
  - test/test_thermostatic.jl
  - test/test_deferrable.jl
  - test/test_pvbattery.jl
  - test/test_aggregator.jl
  - test/test_welfare_solve.jl
autonomous: true
requirements: [DATA-04]

must_haves:
  truths:
    - "StableRNGs 1.0.4 is a pinned dependency that resolves cleanly on Julia 1.10, 1.11, and 1.12"
    - "Every Phase-3 src file is wired into the TSODSO include graph in dependency order and the package precompiles"
    - "The TestItemRunner discovers RED @testitem stubs for profiles, thermostatic, deferrable, battery, aggregator, and welfare — the runner infrastructure stays healthy (failing, not crashing)"
    - "A shared T=24 multi-period feeder/parameter fixture exists for downstream integration tests"
  artifacts:
    - path: "Project.toml"
      provides: "StableRNGs in [deps] + [compat] floor"
      contains: "StableRNGs"
    - path: "src/TSODSO.jl"
      provides: "Include graph wiring the 6 new Phase-3 seam files in dependency order"
      contains: "include(\"models/welfare_solve.jl\")"
    - path: "test/fixtures_phase3.jl"
      provides: "@testmodule Phase3Fixtures: small radial feeder + T=24 parameter vectors"
      contains: "@testmodule"
  key_links:
    - from: "src/TSODSO.jl"
      to: "src/models/welfare_solve.jl"
      via: "include after linear_solve.jl"
      pattern: "include\\(\"models/welfare_solve.jl\"\\)"
---

<objective>
Wave-1 shared-foundation plan (the ONLY plan that edits Project.toml, the Manifests, and src/TSODSO.jl). Add the `StableRNGs` dependency and re-resolve the version-specific manifests so reproducibility holds on every supported Julia; create comment-only stubs for all six Phase-3 seam files and wire them into the include graph in dependency order; and stand up the RED `@testitem` harness plus a shared T=24 fixture so Waves 2-3 can fill stubs without ever re-touching a shared file.

## Phase Goal (user story)

**As a** power-systems researcher, **I want to** assemble the full prosumer device library (thermostatic, deferrable, PV+battery), an aggregator roll-up, and the GLB-CVX social-welfare objective into a single centralized convex-QP solve over the linear branch-flow model with seeded reproducible profiles, **so that** I can run a complete multi-device welfare solve at linear fidelity — global optimum, an emergent nodal price, and honored battery complementarity — before adding SOCP complexity.

> Reformatted faithfully (role = researcher) from the ROADMAP Phase-3 prose goal; scope unchanged. Flagged to the orchestrator.

Purpose: Isolate all shared-file edits into one serialized wave so the three device/profile plans (Wave 2) and the assembly plan (Wave 3) touch only disjoint new files.
Output: Pinned StableRNGs dep + re-resolved manifests, wired include graph, RED testitem harness, T=24 fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-prosumer-device-library-social-welfare-solve/03-RESEARCH.md

<interfaces>
<!-- Established seams the executor must not break. Extracted from source this session. -->

From src/TSODSO.jl (current include graph — Phase-3 files must slot in without reordering existing lines):
  include("data/Feeder.jl"); include("data/topology.jl")
  ... include("devices/AbstractDevice.jl"); include("devices/Interruptible.jl")
  ... include("models/toy_dc.jl"); include("models/linear_solve.jl")
  # TSODSO.jl exports NOTHING — each seam file declares its own `export`s.

From Project.toml: StableRNGs is NOT present. Target: add uuid `860ef19b-820b-49d6-a774-d7a799459cd3`,
  version 1.0.4, with a matching [compat] entry `StableRNGs = "1.0.4"`.

Version-specific manifests already exist (Manifest-v1.10.toml / v1.11 / v1.12) — juliaup channels
  1.10, 1.11, 1.12 are all installed; `julia +<channel>` selects each.

TestItems convention (from test/test_device.jl): each `@testitem "<seam>: ..." tags=[:x] begin using TSODSO ... end`;
  the item NAME contains the runner filter substring. Runner entry is test/runtests.jl (`@run_package_tests`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StableRNGs dependency and re-resolve all version-specific manifests</name>
  <files>Project.toml, Manifest.toml, Manifest-v1.10.toml, Manifest-v1.11.toml, Manifest-v1.12.toml</files>
  <read_first>
    - Project.toml (current [deps]/[compat]/[weakdeps] layout)
    - .planning/phases/03-prosumer-device-library-social-welfare-solve/03-RESEARCH.md §Standard Stack (StableRNGs 1.0.4, JuliaRandom org, "Approved") + §Package Legitimacy Audit
    - Manifest-v1.10.toml header (manifest_format 2.0, project_hash) to confirm the version-specific manifest scheme
  </read_first>
  <action>
    Add `StableRNGs` (DATA-04, INFRA-04 reproducibility) as a first-class dependency. Do NOT hand-edit the Manifests — regenerate them per-channel so hashes are authoritative. For each juliaup channel in {1.10, 1.11, 1.12} run a project-activated `Pkg.add("StableRNGs")` (e.g. `julia +1.10 --project=. -e 'import Pkg; Pkg.activate("."); Pkg.add("StableRNGs"); Pkg.resolve()'`), which pins StableRNGs 1.0.4 into `[deps]` and rewrites the matching `Manifest-v1.10.toml` (then v1.11 on `+1.11`, v1.12 on `+1.12`, and the plain `Manifest.toml` on whichever channel writes it). After the adds, edit `Project.toml` `[compat]` to add `StableRNGs = "1.0.4"` (floor at the pinned version for reproducibility per RESEARCH). StableRNGs is Approved in the legitimacy audit (JuliaRandom org, authoritative registry hit) — no legitimacy checkpoint required. Do NOT add `Distributions.jl` (RESEARCH: keep the dependency surface minimal; the Markov step is a hand-rolled categorical draw).
  </action>
  <verify>
    <automated>julia +1.10 --project=. -e 'import Pkg; Pkg.instantiate(); using StableRNGs; println(StableRNGs.LehmerRNG(1))' && julia +1.11 --project=. -e 'import Pkg; Pkg.instantiate(); using StableRNGs' && julia +1.12 --project=. -e 'import Pkg; Pkg.instantiate(); using StableRNGs'</automated>
  </verify>
  <acceptance_criteria>
    - `Project.toml` `[deps]` contains `StableRNGs = "860ef19b-820b-49d6-a774-d7a799459cd3"` and `[compat]` contains `StableRNGs = "1.0.4"`.
    - `using StableRNGs; StableRNGs.LehmerRNG(1)` loads and constructs on all three channels (1.10/1.11/1.12) without a resolve error.
    - All three `Manifest-v1.1{0,1,2}.toml` files list StableRNGs 1.0.4; no other dependency (e.g. Distributions) was added.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create comment-only Phase-3 stubs and wire the include graph</name>
  <files>src/data/profiles.jl, src/devices/Thermostatic.jl, src/devices/Deferrable.jl, src/devices/PVBattery.jl, src/devices/Aggregator.jl, src/models/welfare_solve.jl, src/TSODSO.jl</files>
  <read_first>
    - src/TSODSO.jl (full — the include graph and the "empty stub, filled by one later plan" convention)
    - src/devices/Interruptible.jl (the concrete-device SEAM/OWNER header comment style to replicate)
    - .planning/phases/03-prosumer-device-library-social-welfare-solve/03-RESEARCH.md §Recommended Project Structure (file list + include order + owner mapping)
  </read_first>
  <action>
    Create each of the six new src files as a comment-only stub (SEAM/OWNER header block mirroring the Interruptible.jl style, naming the owning Wave-2/3 plan and the requirement — profiles.jl → 03-02 DATA-04; Thermostatic.jl/Deferrable.jl → 03-03 DEV-01/DEV-02; PVBattery.jl → 03-04 DEV-04; Aggregator.jl → 03-05 DEV-05; welfare_solve.jl → 03-05 OPT-01). Add NO functional code — later waves fill them and declare their own `export`s. Wire them into `src/TSODSO.jl` in dependency order per RESEARCH: `data/profiles.jl` after the `data/` block; `devices/Thermostatic.jl`, `Deferrable.jl`, `PVBattery.jl` after `devices/Interruptible.jl`; `devices/Aggregator.jl` after the three concrete devices; `models/welfare_solve.jl` after `models/linear_solve.jl`. Do not reorder or remove any existing include line. `TSODSO.jl` itself still exports nothing.
  </action>
  <verify>
    <automated>julia --project=. -e 'using TSODSO; println("precompiled")'</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist and contain only comments (no `function`/`struct`/`export`).
    - `src/TSODSO.jl` includes all six in the specified dependency order; existing include lines are unchanged.
    - `using TSODSO` precompiles with no error on the release channel.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Stand up the RED @testitem harness and the T=24 fixture</name>
  <files>test/fixtures_phase3.jl, test/test_profiles.jl, test/test_thermostatic.jl, test/test_deferrable.jl, test/test_pvbattery.jl, test/test_aggregator.jl, test/test_welfare_solve.jl</files>
  <read_first>
    - test/test_device.jl (the `@testitem "<seam>: ..."` + inline `using TSODSO` convention)
    - test/runtests.jl (`@run_package_tests` entry — the runner discovers items under test/)
    - .planning/phases/03-prosumer-device-library-social-welfare-solve/03-VALIDATION.md §Per-Task Verification Map (filter substrings) and §Wave 0 Requirements
    - src/data/Feeder.jl (Bus/Branch/Feeder constructors) to build the fixture feeder
  </read_first>
  <action>
    Create `test/fixtures_phase3.jl` holding a TestItems `@testmodule Phase3Fixtures` that exports `small_radial_feeder()` (a valid multi-bus radial `Feeder` in per-unit with a root/MEM bus) and `T` = 24 plus deterministic T-length parameter vectors (ambient-temperature `Tout`, inelastic-demand `Pdc`, PV `Ppv`, hourly wholesale price `λ₀`) for downstream integration use. Create one RED `@testitem` stub per seam, each with a NAME containing its filter substring exactly: `"profile"`, `"thermostatic"`, `"deferrable"`, `"battery"`, `"aggregator"`, `"welfare"` (the welfare file also carries the battery-complementarity intent). Each stub `begin using TSODSO ... end` and asserts the not-yet-existing symbol (e.g. `@test isdefined(TSODSO, :Thermostatic)`) so it is RED now and its owning wave turns it green. The runner must stay healthy (items FAIL, the run does not crash).
  </action>
  <verify>
    <automated>julia --project=. -e 'using TestItemRunner; @run_package_tests filter=ti->occursin("welfare", ti.name) || occursin("battery", ti.name) || occursin("profile", ti.name)' ; echo "runner exit healthy: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures_phase3.jl` defines `@testmodule Phase3Fixtures` exporting `small_radial_feeder()` and a `T == 24` horizon with matching-length parameter vectors.
    - Six new test files each define at least one `@testitem` whose name contains the required substring (profile/thermostatic/deferrable/battery/aggregator/welfare).
    - The runner discovers and RUNS the items (they are RED because the src symbols do not yet exist) without the runner process crashing.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → project env | A new third-party dependency (StableRNGs) crosses into the pinned environment |
| Julia version ↔ committed manifest | Reproducibility depends on each version-specific manifest matching its channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `Pkg.add("StableRNGs")` supply-chain add | mitigate | StableRNGs is Approved in RESEARCH §Package Legitimacy Audit (JuliaRandom org, authoritative registry hit, latest 1.0.4); pin exact version in [compat]; commit regenerated manifests. Not `[ASSUMED]`/`[SUS]` → no blocking checkpoint. |
| T-03-01 | Repudiation | Version-specific manifests | mitigate | Regenerate each `Manifest-v1.1x.toml` on its own juliaup channel rather than hand-editing, so a future clean checkout resolves bit-for-bit on 1.10/1.11/1.12 (INFRA-01/04). |
| T-03-02 | Tampering | include-graph wiring | accept | Comment-only stubs carry no logic; a mis-ordered include fails loudly at precompile (verified by Task 2 automated check). |
</threat_model>

<verification>
- `julia --project=. -e 'using TSODSO'` precompiles with all six new includes present.
- `using StableRNGs` succeeds on channels 1.10, 1.11, 1.12.
- The TestItemRunner discovers the six RED seam items and runs (does not crash).
</verification>

<success_criteria>
- StableRNGs 1.0.4 pinned in Project.toml `[deps]`+`[compat]`; manifests regenerated on all three channels.
- All six Phase-3 src stubs wired into the include graph in dependency order; package precompiles.
- RED `@testitem` harness + T=24 `@testmodule Phase3Fixtures` in place; runner healthy.
</success_criteria>

<output>
Create `.planning/phases/03-prosumer-device-library-social-welfare-solve/03-01-SUMMARY.md` when done.
</output>
