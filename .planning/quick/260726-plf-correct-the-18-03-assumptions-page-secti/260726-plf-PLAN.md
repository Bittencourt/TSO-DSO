---
quick_id: 260726-plf
description: Correct the 18-03 assumptions page — Section 8 fragility refuted, Phase-17 re-tune premise undermined
date: 2026-07-26
mode: quick
---

# Quick Task 260726-plf — Correct `thesis_reproduction_assumptions`

The last reader-facing artifact still asserting the refuted fragility claim. This page is
**published Documenter output**, so it is the highest-priority correction of the five logged.

## What must change

### (a) Section 8 — REFUTED by direct measurement

Currently: *"**No — not confirmed.** … `sign_flip_survives: false` — ALL FOUR non-zero perturbation
points FAILED OUTRIGHT … the DSO-surplus sign flip is therefore confirmed **only at the exact
Phase-17-retuned population point**, NOT across a ±2-5% neighborhood."*

Measured truth (spike 003 + `c099ee6`): at `tol_gap = 1e-10` **5/5 points solve and all five show the
sign flip**, both surpluses monotone (`dadp_dso` 2.71→4.81, `fit_dso` −183→−210).

### (b) Line ~49 — the Phase-17 re-tune premise is ALSO refuted

Currently: *"the ORIGINAL synthetic-impedance triple broke `solve_welfare`'s SOCP-exactness gate
outright on the real network (`assert_socp_exact!` threw — worst gap ratio 1.378 > 1)."*

Directly tested this session: the original triple (`0.03 / 0.06 / 0.05`) on real impedances **throws at
`tol_gap = 1e-8` with exactly ratio 1.3781586234547918, and PASSES at `1e-10`** —
`socp_maxgap = 1.673e-08`, `vpeak = 1.00198`, `dso = +0.662750` (still positive). **The re-tune was not
necessary for exactness.** The re-tuned point remains valid; it was not, however, required.

### (c) Sections 5 and 7 — evidence undermined, NOT fully refuted

Both rest on Phase 17's default-tolerance search ("any population scale pushing the solved max above
~1.02-1.03 pu drives the SOC relaxation genuinely inexact"). Its anchoring datum (1.378) is refuted,
and spike 002 found the **voltage upper bound is never active** on this feeder across a 5.5× PV range
(`vpeak ≤ 1.016` vs caps ≥ 1.05). But Phase 17's full search space was **not** re-swept here.

**Scope discipline:** mark as *evidence undermined / needs re-measurement at tight tolerance*, not as
refuted. The lower-band (`vmin ≈ 0.9487`) binding is real physics and stays.

### (d) Corroboration worth recording

The page's own line-49 datum (1.378 at `LOAD_SCALE = 0.03`) independently confirms this session's
third diagnosis: the spurious `test_thesis_repro` failure came from the stale sibling worktree, which
still carries `LOAD_SCALE_IEEE123 = 0.03`.

## Tasks

1. **`docs/literate/thesis_reproduction_assumptions.jl`** — the authoritative source. Edit **comment
   prose only**; the page is live-executed, so do not touch the `const` declarations or any executable
   line.
2. **`docs/src/generated/thesis_reproduction_assumptions.md`** — apply the same corrections so the
   committed published artifact is not stale. **It is GENERATED by Literate**; say so, since a docs
   rebuild regenerates it from (1). Keeping (1) authoritative and consistent is what makes the rebuild
   safe.

## Constraints

- Comment/prose edits only in the `.jl`. No numbers invented — every figure traceable to
  `.planning/spikes/003-phase18-fragility-tolerance/run-after-kwarg.log` or this session's
  re-tune test.
- Preserve the page's existing "(directional, public-data)" qualifier convention.
- Do **not** weaken Section 6's honesty paragraph — the +25%-magnitude-does-not-transfer finding is
  unaffected and still correct.
- Do not re-tune the fixture or touch `test/fixtures_phase7.jl`.

## must_haves

- **truths:** Section 8 states the sign flip survives ±5%; line 49 no longer presents 1.378 as a
  physical exactness failure; Sections 5/7 flagged as needing tight-tolerance re-measurement
- **artifacts:** `docs/literate/thesis_reproduction_assumptions.jl`,
  `docs/src/generated/thesis_reproduction_assumptions.md`
