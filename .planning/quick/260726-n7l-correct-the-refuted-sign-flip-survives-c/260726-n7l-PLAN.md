---
quick_id: 260726-n7l
description: Correct the refuted sign_flip_survives claim in findings.txt and 18-01-SUMMARY
date: 2026-07-26
mode: quick
---

# Quick Task 260726-n7l — Correct the refuted `sign_flip_survives` claim

## Why

Spikes 002/003 + quick task `260726-mo7` (commit `c099ee6`) showed v2.1 Phase 18-01's
`sign_flip_survives: false` is **wrong**. At `tol_gap = 1e-10` the same ±2-5% sweep solves **5/5**
and the DSO-surplus sign flip holds at **every** point, both surpluses monotone. Two of the four
recorded failures were also **misattributed** to `solve_welfare` when they were `fit_baseline`.

The false claim currently reads as current fact in two committed artifacts.

## Editorial principle

**Do not silently rewrite the historical record.** These are an archived milestone summary and a
generated measurement artifact. What was measured at default tolerance is a real record and the raw
numbers are not wrong — the *verdict* and the *attribution* were. So:

- add a prominent SUPERSEDED / CORRECTION banner carrying the corrected facts;
- annotate each specific wrong sentence inline so it cannot be read as current;
- **preserve the original text** beneath, marked as what was believed at the time.

## Tasks

### Task 1 — `results/repro_stability_check/findings.txt`

- **action:** Prepend a `!!! CORRECTION` block: superseded verdict, corrected 5-point table,
  cause (gate `atol = 1e-6` at Clarabel's noise floor on 122 branches), the misattribution, and
  evidence paths. Preserve the original report verbatim below under a heading marking it as the
  default-tolerance measurement.
- **critical caveat:** this file is **generated** by `scripts/repro_stability_check.jl:343`. A
  re-run overwrites the hand-added banner. The banner must say so and name the script fix as owed.
- **done:** no reader can take `sign_flip_survives: false` as current.

### Task 2 — `18-01-SUMMARY.md`

- **files:** `.planning/milestones/v2.1-phases/18-directional-thesis-reproduction/18-01-SUMMARY.md`
- **action:** Add a SUPERSEDED banner after the frontmatter, then annotate the five specific
  assertions: `provides:` (line ~16), `key-decisions:` (~34), the "ALL FOUR points FAIL outright —
  `solve_welfare`'s `assert_socp_exact!` throws" measured-results bullet (~65, the misattribution),
  the `sign_flip_survives: false` bullet (~66), and the Plan 18-03 readiness note (~116).
- **done:** each wrong claim carries a correction marker; the frontmatter `provides:` no longer
  states the refuted finding as current truth.

### Task 3 — out of scope, recorded

The published assumptions literate page (18-03) and Plan 18-02's golden band re-derivation
(`1.5 × max|dso|` now implies 7.211 vs pinned 5.5886) are **not** touched here. Already tracked in
STATE.md.

## Constraints

- Correct, don't delete. The original measurement stays legible.
- No code changes; no re-running of the measurement script.
- Do not touch `test_thesis_repro.jl` — it still passes (4.8074 < 5.5886).

## must_haves

- **truths:** neither artifact asserts `sign_flip_survives: false` as current; the misattribution to
  `solve_welfare` is corrected; findings.txt's generated-file fragility is stated
- **artifacts:** `results/repro_stability_check/findings.txt`, `18-01-SUMMARY.md`
- **key_links:** `.planning/spikes/003-phase18-fragility-tolerance/run-after-kwarg.log` (evidence)
