---
quick_id: 260826-8gb
description: Document this session's ADMM mid-loop conditioning-ladder finding (the IEEE-13 numerical knife-edge, its two-commit fix, and its regression posture) plus the docs-integrity CI guard, in the Documenter site where a researcher will actually find them
date: 2026-08-26
mode: quick
---

# Quick Task 260826-8gb: Document the ADMM conditioning ladder and the docs-integrity guard

## Why

This session (`.planning/debug/resolved/ieee13-admm-numerical-error.md`) found and fixed a
genuine numerical knife-edge in the ADMM mid-loop `DSO-OPT` SOCP solve, characterized *why*
it is ill-conditioned, landed a two-commit fix (`f9d6ed7`, `d099821`), retired a no-op test
wrapper (`5725f7f`), and separately hardened CI against a JuliaFormatter docstring-deletion
hazard (`8b2e6d3`, `3ba4b6f`). CLAUDE.md makes "rich, step-by-step docs of every modeling
decision and its math" a hard requirement, not optional — none of this is documented on the
Documenter site yet. This task closes that gap on the two pages a researcher would actually
land on: the Rung 5 ADMM literate page (the mechanism + fix) and the site Home page's
"Validation & regression posture" section (the regression/CI posture summary).

## Verified out of scope (do not re-audit, do not edit)

`docs/src/api.md` was audited during planning and needs **no change**: it uses `@autodocs`
keyed on file paths (not symbol names), so `LADDER_ATTR_NAMES` (`src/planning/retry.jl`,
already in `docs/make.jl`'s Pages list) is already surfaced. Coverage check performed during
planning: 71 `.jl` files under `src/` excluding `TSODSO.jl`, 69 listed in `api.md`'s Pages, 0
listed-but-missing; the 2 unlisted files (`data/ieee123_impedances.jl`,
`data/ieee8500_impedances.jl`) carry zero docstrings and cannot trigger `missing_docs`. Do
not touch `docs/src/api.md`, `docs/make.jl`, or `README.md` in this task.

## Context

- `docs/literate/admm.jl` (131 lines) — the Rung 5 ADMM literate page, all prose is `#`
  Literate.jl comments rendered as markdown by `Literate.DocumenterFlavor()`; no docstrings,
  so JuliaFormatter's `format_docstrings = true` hazard does not apply to this file's own
  content, though the file is still format-checked as part of `docs/`.
- `docs/src/index.md` — the Home page; target section is `## Validation & regression
  posture` (currently the last section in the file, ending at line 99, EOF).
- `docs/make.jl` — confirms both files are already wired into the build (`admm.jl` is in the
  `Literate.markdown` loop and the `pages` list as `generated/admm.md`; `index.md` is the
  `"Home"` page). `checkdocs = :exports` is strict (fails on a documented-but-unsurfaced
  export); `warnonly = [:cross_references]` means a broken `@ref` only warns. No Pages-list
  change is needed by this task since no new exported symbol is being added.
- `.gitignore` confirms `/docs/src/generated/` is untracked build output — do not commit
  anything under it, and do not hand-edit it.
- `.JuliaFormatter.toml`: `margin = 92`. Wrap new prose comments near that width for
  consistency with the file's existing style (not a hard formatter requirement for plain `#`
  comments — `format_docstrings` only reflows actual `"""..."""` docstrings).
- Source facts (all measured this session, do not restate anything not in this list):
  `src/admm/DsoOpt.jl` (`build_dso_opt`'s `_snapshot_ladder_attrs`/`ctx.meta[:ladder_baseline]`,
  `solve_dso!`'s `check_exact`-gated `_restore_ladder_attrs!` and mid-loop
  `solve_with_retry!` routing), `src/planning/retry.jl` (`solve_with_retry!`,
  `RETRYABLE_STATUSES`, `LADDER_ATTR_NAMES`), `test/test_admm_knifeedge_canary.jl`,
  `.planning/debug/resolved/ieee13-admm-numerical-error.md`, and the CI-integrity commits
  `8b2e6d3`/`3ba4b6f` (`.github/scripts/check_content_loss.py`,
  `.github/workflows/CI.yml`'s format job).

## Tasks

### Task A — Document the ADMM conditioning ladder on the Rung 5 literate page

- **files:** `docs/literate/admm.jl`
- **action:** Append a new top-level `##` section to the END of the file (after the existing
  "## Convergence figure (CairoMakie, guarded)" section, i.e. after current line 131).
  PROSE ONLY — do not add any new executable Julia cell (no new `solve_admm`/`solve_welfare`
  call; the toy 3-bus/`T = 4` fixture solved above is too small to hit the knife-edge, and a
  full IEEE-13 58-iteration re-solve here would be a real, unjustified docs-build cost). Use
  exactly this content (adjust only line-wrapping to taste, keep every fact and number
  verbatim, keep every inline code span on ONE line — never let a wrapped line begin with
  `|`):

  ```
  # ## Numerical robustness — the ADMM conditioning ladder (mid-loop knife-edge)
  #
  # The toy 3-bus/`T = 4` feeder solved live above is deliberately small and never
  # approaches the conditioning edge documented below — this section records a general
  # property of the ADMM mid-loop `DSO-OPT` solve, measured on the IEEE-13 fixture
  # (`test/test_admm_knifeedge_canary.jl`), not a defect specific to the toy example above.
  #
  # On IEEE-13, the mid-loop `DSO-OPT` SOCP sits on a genuine numerical knife-edge at ADMM
  # iteration 28, once adaptive-ρ has climbed one `τ = 2` doubling step from `ρ₀ = 100` to
  # `ρ = 200` (nowhere near the `ρ_max = 1e4` clamp) and both Boyd residuals sit within
  # roughly 1.5-2× of their stopping tolerances. At that iterate Clarabel's DEFAULT static
  # regularization is not always enough, and the solve can terminate `NUMERICAL_ERROR`
  # rather than `OPTIMAL`. Which side of the edge a given build lands on is decided by pure
  # floating-point/codegen perturbation, never by logic: an unreachable `include` flips the
  # outcome (A/B verified), and so does a Julia PATCH bump (1.10.11 / 1.11.9 / 1.12.5 fail;
  # 1.12.7 converges natively) — each (tree × Julia version) pair is deterministic, 5/5, not
  # a run-to-run flake.
  #
  # ### Why: an anisotropic objective Hessian, not the constraint matrix
  #
  # The `DSO-OPT` constraint matrix is well scaled and ρ-independent
  # ([`build_dso_opt`](@ref) puts ρ only in the objective): measured coefficient magnitudes
  # span roughly 0.028 to 1.2 per-unit, about a 43× spread, and that spread does not move
  # with ρ at all. The ill-conditioning is ANISOTROPIC and comes entirely from the
  # objective: the `0.5ρ` quadratic penalty is a pure diagonal Hessian sitting on only 240
  # of the model's 1536 variables (the `pag_dso[j,t]` coupling block); the remaining 1296
  # variables — voltages, squared currents, branch flows, cone slacks — carry exactly zero
  # curvature and are held up in the KKT factorization only by Clarabel's
  # `static_regularization_constant` default of `1.0e-8`. At `ρ = 200` that is roughly a
  # `1e10` diagonal spread between the two blocks, degrading linearly as ρ climbs. Retry
  # rung 2 raises `static_regularization_constant` to `1e-6` — a 100× cut of exactly that
  # spread, and the only thing it changes — and that alone converts every observed failure
  # into the same optimum at the same 58 iterations. This mechanism is strongly supported by
  # the measurements above but not solver-internally certified: Clarabel's internal KKT
  # condition number at the iteration-28 iterate was never dumped (it would need
  # product-code instrumentation), so state the caveat honestly rather than implying a
  # certified proof.
  #
  # ### The fix — route the mid-loop solve through the existing conditioning ladder
  #
  # Two additive commits, no tolerance or gate weakened:
  #
  #   - `f9d6ed7` routes [`solve_dso!`](@ref)'s MID-LOOP (`strict = false`) solve through
  #     [`solve_with_retry!`](@ref)`(dso.model; dual = false, allow_almost = true)` instead
  #     of calling `assert_solved!` directly. `MOI.NUMERICAL_ERROR` was already a member of
  #     `RETRYABLE_STATUSES`; the mid-loop branch was simply bypassing the ladder the
  #     project already ships. Rescue lands at rung 2 (`static_regularization_constant =>
  #     1e-6`).
  #   - `d099821` makes [`build_dso_opt`](@ref) snapshot the as-built ladder attributes into
  #     `ctx.meta[:ladder_baseline]` once, before any solve can touch the model, and
  #     `solve_dso!` restores that snapshot immediately before the FINAL/converged
  #     (`check_exact = true`) solve — so the PUBLISHED solve always runs at the factory's
  #     own conditioning, never an inherited mid-loop escalation.
  #
  # This cannot contaminate the published transactive price: the ADMM DADP is the OUTER
  # multiplier `λ` (`dadp == λ` inside [`solve_admm`](@ref)), never a dual read off
  # `dso.model`, and every `DsoOpt` consumer downstream reads primals only. Convergence
  # itself is unchanged — every measured environment (rescued and native) reaches the same
  # 58 iterations, with welfare agreeing to roughly `2e-9` relative across toolchains.
  #
  # ### Regression posture
  #
  # `test/test_admm_knifeedge_canary.jl` (tags `[:admm, :canary]`) hard-asserts
  # `r.iters == 58` and `isapprox(r.welfare, -4822.903616694139; rtol = 1e-6, atol = 1e-3)`
  # on the IEEE-13 fixture, and only REPORTS — never asserts — whether the ladder fired,
  # because that legitimately differs by toolchain (it fires on 1.10/1.11/1.12.5, not on
  # 1.12.7's native convergence path) and asserting it would be permanently red on one
  # side. The prior test-level retry wrapper (`AdmmRetryFixtures`, `test/fixtures_retry.jl`)
  # was retired: it re-called with no input perturbation, so it could never rescue a
  # deterministic failure, never fired in CI, and its broad `catch` matched every
  # `assert_solved!` failure — including a genuine `INFEASIBLE` — so it could have masked a
  # real regression instead of this production-code fix.
  ```

  Every `[`symbol`](@ref)` used above (`build_dso_opt`, `solve_dso!`, `solve_with_retry!`,
  `solve_admm`) is confirmed exported (verified: `export DsoOpt, build_dso_opt, solve_dso!,
  set_rho!, set_rho_q!` in `src/admm/DsoOpt.jl`; `export solve_with_retry!,
  RETRYABLE_STATUSES, LADDER_ATTR_NAMES` in `src/planning/retry.jl`; `solve_admm` already
  `@ref`'d earlier in this same file) — no dangling reference is introduced.
- **verify:**
  <automated>grep -c "escalating conditioning\|static_regularization_constant\|iteration 28" docs/literate/admm.jl | grep -v '^0$'</automated>
  Also `grep -n "^# ## Numerical robustness" docs/literate/admm.jl` → exactly 1 match, and
  `python3 -c "import re; t=open('docs/literate/admm.jl').read(); print('BAD' if re.search(r'\n#\s*\|', t) else 'OK')"` → `OK` (no wrapped line begins with `|`).
- **done:** The new section exists, contains every fact listed above with the exact measured
  numbers (58 iterations, `-4822.903616694139`, `ρ₀ = 100`/`ρ = 200`/`τ = 2`, `240`/`1536`
  variables, `1.0e-8`/`1e-6` regularization, commits `f9d6ed7`/`d099821`), and no new
  executable cell was added.

### Task B — Add the ADMM and docs-integrity summary to the site's regression-posture section

- **files:** `docs/src/index.md`
- **action:** Append two new paragraphs to the END of the `## Validation & regression
  posture` section (after the existing planning-layer paragraph ending "...enforcing the
  milestone's continuous-only scope structurally, not by convention.", i.e. after current
  line 99 / EOF). Use exactly this content:

  ```markdown

  The operational layer's ADMM decomposition ships its own numerical-robustness net: the
  mid-loop `DSO-OPT` SOCP subproblem can hit a genuine, floating-point-decided conditioning
  knife-edge on some fixtures (documented in full on [Rung 5: ADMM
  Decomposition](generated/admm.md)), so `solve_dso!`'s mid-loop solve is routed through the
  project's escalating Clarabel-conditioning retry ladder (`solve_with_retry!`), while
  `build_dso_opt` snapshots and `solve_dso!` restores the as-built conditioning immediately
  before every published, converged solve — so a mid-loop rescue can never leak into the
  reported transactive price. A pinned canary regression test
  (`test/test_admm_knifeedge_canary.jl`) hard-asserts the IEEE-13 iteration count and
  welfare converge to their known-good trajectory, while deliberately only reporting — never
  asserting — whether the ladder fired, since that legitimately differs by Julia toolchain.

  CI also guards the documentation itself against formatter-induced silent content loss:
  `JuliaFormatter`'s `format_docstrings = true` option (kept on deliberately) can drop text
  when an inline code span wraps a line and its continuation begins with `|` — CommonMark
  reads that as a markdown table row, and the formatter is idempotent afterwards, so a naive
  format-then-check job would go green with documentation missing. A dedicated content-loss
  guard (`.github/scripts/check_content_loss.py`), wired into the CI format job with `if:
  always()`, compares every tracked `.jl` file under `src/`, `ext/`, `test/`, and `docs/`
  against the pre-format git ref, normalizing away whitespace and commas so ordinary
  formatter reflow passes while a real deletion fails the build. The guard only catches loss
  introduced by a formatting pass during a CI run — it cannot catch loss already committed
  to a format-clean `HEAD`. The CI job pins the formatter itself via
  `PackageSpec(name = "JuliaFormatter", version = "2.10")`, which resolves through Pkg's
  `VersionRange` parser (no `^`/`~` operators — a leading `~` throws `ArgumentError`) to the
  range `[2.10.0, 2.11.0)`.
  ```
- **verify:**
  <automated>grep -c "check_content_loss.py\|solve_with_retry!\|knifeedge_canary" docs/src/index.md | grep -v '^0$'</automated>
  Also `grep -n "^## Validation & regression posture" docs/src/index.md` → exactly 1 match
  (section header not duplicated), and confirm the two new paragraphs are the LAST content
  in the file: `tail -5 docs/src/index.md` ends with the `VersionRange`/`[2.10.0, 2.11.0)`
  sentence.
- **done:** Both paragraphs exist at the end of the section, reference the Rung 5 page and
  the canary test by name, and state the content-loss guard's honest boundary (catches
  formatting-introduced loss only, not pre-existing loss on a clean `HEAD`).

### Task C — Verify the docs build, formatting, and no content loss

- **files:** none modified (verification only; touches `docs/literate/admm.jl`,
  `docs/src/index.md` as read-only checks)
- **action:**
  1. Snapshot the edited literate file before any formatter run, then format-check it
     WITHOUT writing (per repo convention, bare `format(path)` overwrites — always pass
     `overwrite=false` to check):
     ```
     cp docs/literate/admm.jl "$SCRATCH/admm_before_format.jl"
     julia -e 'import Pkg; Pkg.activate(temp=true); Pkg.add(Pkg.PackageSpec(name="JuliaFormatter", version="2.10")); using JuliaFormatter; println(format("docs/literate/admm.jl"; overwrite=false))'
     ```
     (`$SCRATCH` = `/tmp/claude-1000/-home-pedro-programming-TSO-DSO/fb15e907-7729-4e8c-8dfc-10ace72db910/scratchpad`.)
     If it prints `true`, the file already conforms — skip to step 2. If it prints `false`,
     re-run with `overwrite=true`, then confirm the ONLY difference from the pre-format
     snapshot is whitespace/commas (reuses `check_content_loss.py`'s own normalization):
     ```
     python3 -c "
     import re
     norm = lambda t: re.sub(r'[\s,]+', '', t)
     a = open('$SCRATCH/admm_before_format.jl').read()
     b = open('docs/literate/admm.jl').read()
     assert norm(a) == norm(b), 'CONTENT CHANGED beyond whitespace/commas'
     print('OK: format-only diff, no content lost')
     "
     ```
     `docs/src/index.md` is not a `.jl` file and is not subject to JuliaFormatter — no
     format check needed for it.
  2. Run the real docs build:
     ```
     julia --project=docs docs/make.jl
     ```
     Expect it to complete (several minutes; the page set includes the stochastic/meshed/
     planning rungs) with exit code 0 and no `checkdocs`/`missing_docs` failure. If it fails
     due to the pre-existing UNCOMMITTED `Project.toml`/`Manifest-v1.12.toml` drift
     (CairoMakie promoted to a hard dep — unrelated to this task, visible in `git status`),
     do not touch the committed manifests; instead run the same build from a clean detached
     `git worktree` at `HEAD` plus these two edits cherry-picked/copied in, per the
     verification_guidance. Report the real outcome either way.
  3. Confirm the Rung 5 page regenerates and stays untracked:
     ```
     ls -la docs/src/generated/admm.md
     git status --porcelain docs/src/generated/
     ```
     Expect the file to exist with a fresh mtime and `git status --porcelain` to print
     nothing (matches the `.gitignore` entry `/docs/src/generated/`) — do not `git add` it.
  4. Run the project's own content-loss guard as a final report (informational at this
     stage — it will legitimately flag both edited files as changed vs `HEAD` since this
     task deliberately adds new content; that is expected, not a failure. The load-bearing
     check for *accidental* loss is step 1's before/after-format diff):
     ```
     python3 .github/scripts/check_content_loss.py HEAD
     ```
- **verify:**
  <automated>julia --project=docs docs/make.jl 2>&1 | tail -20</automated>
  Command must exit 0; step 1's format-content check must print `OK` (or the initial
  `format(...; overwrite=false)` call must print `true`); step 3's `git status --porcelain
  docs/src/generated/` must print nothing.
- **done:** The docs build succeeds end-to-end on the edited tree, `docs/literate/admm.jl`
  is formatter-clean with zero content lost, `docs/src/generated/admm.md` regenerates and
  remains untracked, and the real (not assumed) outcome of every check is reported.

## Constraints

- Files in scope: `docs/literate/admm.jl`, `docs/src/index.md`. Do NOT edit
  `docs/src/api.md`, `docs/make.jl`, `README.md`, or any other root-level doc. Do NOT create
  new root-level docs. Do NOT hand-edit anything under `docs/src/generated/`.
- `docs/literate/admm.jl` EXECUTES during the docs build — Task A adds PROSE ONLY, no new
  executable cell (a full IEEE-13 58-iteration ADMM re-solve in the docs build would be a
  real, unjustified CI-time cost).
- Repo is format-clean under pinned JuliaFormatter 2.10.2 and CI is green — keep both.
  `docs/literate/*.jl` is format-checked; use `overwrite=false` to check, never bare
  `format(path)`.
- Never let a wrapped comment/prose line begin with `|` — the exact formatter bug this
  session's CI guard exists to catch. Keep every inline code span on one line.
- Do not add a GitHub Pages deploy workflow — Pages already redeploys from the existing
  `docs` CI job on push to `main`.
- Do not touch the committed `Manifest.toml`/`Manifest-v1.1x.toml` files to work around the
  pre-existing uncommitted `Project.toml` drift; use a clean detached worktree instead if the
  docs build is blocked by it.

## must_haves

- **truths:**
  - A researcher reading the Rung 5 ADMM literate page (`generated/admm.md`) finds the
    mid-loop conditioning-ladder mechanism, its C2 anisotropic-Hessian explanation with the
    honest solver-certification caveat, the two-commit fix, why it cannot contaminate
    published prices, and the canary regression test — without leaving that page.
  - A researcher reading the site Home page's "Validation & regression posture" section
    finds both the ADMM numerical-robustness net and the CI docs-integrity (content-loss)
    guard, each with its stated boundary, and a link to the full ADMM page.
  - The full Documenter build (`julia --project=docs docs/make.jl`) still succeeds
    end-to-end after these edits, and `docs/src/generated/admm.md` regenerates without ever
    being committed.
- **artifacts:**
  - `docs/literate/admm.jl` — new `## Numerical robustness — the ADMM conditioning ladder
    (mid-loop knife-edge)` section (prose only), appended after the existing convergence
    figure section.
  - `docs/src/index.md` — two new paragraphs appended to the end of `## Validation &
    regression posture`.
- **key_links:**
  - `docs/src/index.md`'s new ADMM paragraph → `generated/admm.md` (markdown link, matches
    the existing link style used elsewhere on the page).
  - `docs/literate/admm.jl`'s new section → `solve_with_retry!`/`build_dso_opt`/
    `solve_dso!`/`solve_admm` via `@ref` (all confirmed exported, so `checkdocs = :exports`
    is unaffected and no cross-reference is dangling).
</content>
